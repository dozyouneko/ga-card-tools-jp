# CLAUDE.md

Grand Archive TCG 日本語カードDB(非公式)— 静的サイト+翻訳ツールチェーン。
詳細は README.md を参照。

## セッションの役割分担

このリポジトリでは **`設計 → 開発 → レビュー` の3役**をClaude Codeセッションに分けて運用している:

- **設計担当**: 要件整理・API調査・設計書作成・画面モック作成・最終承認。実装コードは書かない
- **開発(実装)担当**: 設計書を参照してコードを実装し、Playwright実駆動で検証する
- **レビュー担当(2026-08-06〜)**: 実装を**自分でアプリを操作して**評価基準に照らし合否判定する。
  **コードは直さない**(直すと誰も再レビューしない)。不合格なら基準IDつきの具体的な指摘を返して差し戻す

進行役は2種類あり、**どちらか一方を使う**(併用しない):

- **スプリント進行役(2026-08-06〜)**: dev / review をサブエージェントとして交互に呼び、
  **実装〜レビュー**を回す。設計は別セッション(`cc-design`)が担当する
- **リード(2026-09-02〜)**: ⭐ **ユーザーとの窓口を1つにまとめた版**。design / dev / review を
  **直接**呼び、`設計 → 実装 → レビュー` を1ターミナルで回す。
  ⚠️ **入れ子にしない**(sprint を挟まない)。⚠️ **要件ヒアリングはリードがユーザーと直接行う**
  (サブエージェントは呼び出し中に質問できないため)。詳細は `.claude/agents/lead.md` と
  `docs/design/セッション体制/リードセッション構想.md`

これとは別に、**サイクルの外側**に1役ある:

- **広報担当(2026-08-10〜)**: X投稿用の**画像生成と投稿文作成**を行う。
  ⚠️ **`scripts/` 配下のコードは書かない**(恒久ツール化はissueに上げて設計担当の判断を待つ)。
  ⚠️ **投稿そのものはしない**——用意するのは下書きまでで、投稿の可否と時期は常にユーザーが決める。
  成果物は `tmp/X投稿用/<YYYYMMDD>/`(gitignore)。詳細は `.claude/agents/pr.md`

役割はセッション起動時に付与する(`.claude/agents/` の定義を `--agent` で指定。
`.claude/aliases.sh` の `cc-lead` / `cc-design` / `cc-sprint` / `cc-dev` / `cc-review` / `cc-pr` がショートカット)。
**各セッションはコンテキストを共有しない**。

⚠️ **役割が付いていないセッション**(素の `claude`・VSCode拡張から起動した直後など)は、
**頼まれた内容から自分の役割を決める**。このファイルは役割に関係なく全セッションが読むので、
起動方法にかかわらず次のとおり振る舞うこと:

| 頼まれたこと | 振る舞い |
| --- | --- |
| 「実装待ちのissueを対応して」「スプリントを回して」など**実装〜レビュー** | **スプリント進行役**(`.claude/agents/sprint.md`)。⚠️ **自分で実装せず** dev/review サブエージェントを呼ぶ |
| 要件整理・調査・設計書作成・モック | **設計担当**(`.claude/agents/design.md`)。実装コードは書かない |
| **X投稿の画像・投稿文**を作る | **広報担当**(`.claude/agents/pr.md`)。コードは書かない・投稿はしない |
| どちらとも取れる | ユーザーに確認する |

⚠️ **どの場合も「自分で実装してレビューを飛ばす」ことはしない。**
レビューを通さない差分が入ると、この運用そのものが無意味になる。

**受け渡しは `docs/design/` 配下の設計書(+モックHTML)のみ**。運用ルール:

- **対処内容(タスク)ごとにサブフォルダを切る**: `docs/design/<issue番号>-<タスク名>/`
  (例: `12-デッキ統計タブ/`・`16-フォーマット検索/`)。リリース時の確認資料は
  `リリース確認/` に日付プレフィックスつきで置く(例: `2026-07-18_スモークテスト_チェックリスト.md`)
- 設計書には改版履歴表を付け、実装済み項目と実装待ち項目を明記する
  (既存の `12-デッキ統計タブ/統計タブ_設計.md` 等の形式に倣う)
- 設計成果物はgitコミットする。`tmp/` はgitignoreのため設計成果物を置かない
- `docs/` 配下は本番配信対象外(`functions/docs/[[path]].js` が404を返す)

## タスク管理(2026-07-18〜)

⚠️ ⭐ **GitHubアカウント停止中は `gh issue list` が403で使えない。** その間の
**待ち行列・起票案の状態・GitHub復旧後の手順・いま着手できるタスク**は
**`docs/design/待ち行列/待ち行列と復旧手順.md` の1枚が正**(2026-08-31 新設)。
⭐ **どの役割のセッションも、開始時にまずここを開く**(`gh issue list --label …` の代わり)。

- ⚠️ **起票案(`docs/design/**/起票案*.md`・`派生起票案*.md`)の状態の正は、各ファイルの1行目**:
  `**状態: 生きている|完了|取り下げ**（最終確認 YYYY-MM-DD）…`。索引は一覧に徹する
  (⭐ **ファイル単体を開けば状態が分かる**＝索引が古くても被害が局所化する)
- ⚠️ ⭐ **`gh issue create` する直前に、その起票案が主張する症状を必ず実測して再現させる。**
  再現しなければ投函せず取り下げる。**起票案は「確定した事実」ではなく「投函前の下書き」**で、
  放置すると腐る(実例: スターコーリングの起票案は**約2か月放置され、実測したら症状が存在しなかった**)
- ⚠️ 復旧後の最優先は **初回cron(03:00 JST)より前にpushすること**。旧コードで `cards/**` を
  再生成されると、**どちらも「正しい生成物」に見えるうえcron側に当日のAPIドリフトが混ざり、
  手で解消する根拠が無くなる**(詳細は索引 §0)

- **タスクはGitHub Issuesで管理する**(起票・確認は `gh` CLI: `gh issue create` / `gh issue list`)
- 設計フォルダ名の `<issue番号>` はGitHubの自動採番(#1〜)を使う
- 旧独自番号(#11〜#17、tmp/デッキ構築ツールモックメモ.mdの台帳)は**別系列**。
  過去のコミット・設計フォルダ(12-/16-)の番号は旧系列のまま変更しない
- 完了時は `gh issue close <n>`、またはコミットメッセージの `closes #<n>` で自動クローズ
- **状態ラベル**(各issueに1つ付ける): `着手可` / `検討中` / `保留`(理由を本文に) / `凍結中`(解除条件を本文に)。
  規模の目安として `小タスク` / `大規模` を併用。バグは既定の `bug`、機能追加は `enhancement`
- ⚠️ `gh` の認証(~/.config/gh/)は**コンテナ再構築で消える**→ 再構築後は
  `sudo apt-get install gh`(aptソース設定含む詳細は履歴参照)+ ユーザーが `gh auth login` を再実行

### 実装・検証の結果はissueに追記する(2026-07-19〜)

開発担当セッションが実装・検証を行ったら、**対象issueにコメントで結果を追記する**
(`gh issue comment <n> --body-file <file>`)。設計書が「設計→実装」の受け渡し媒体であるのに対し、
issueコメントが「実装→設計」の報告経路になる(各セッションはコンテキストを共有しないため)。

記載する項目:

1. 実施内容(ステップ・コミットハッシュ・pushの有無)
2. 検証結果 — **設計書の「検証項目」の番号に対応づけた表**にする
3. 実行できなかった検証と代替手段、**残っている確認**(例: Playwright未導入時の代替)
4. 差分が設計書の想定と食い違った場合はその内訳と、意図内と判断した根拠
5. 設計書からの逸脱と根拠(既存表記・TRANSLATION.md等との照合結果)
6. 設計担当への確認事項 — **判断が要る項目は実装側で決めず、issueに書いて判断を待つ**
   (そこから派生するissueの起票も設計担当の判断を待つ)

実例: issue #1 のコメント(2026-07-19)

### セッション間ハンドオフ(2026-07-19〜)

セッション間の指示はissueで受け渡す。**各セッションは開始時(および「issueを確認して対応して」と言われたら)、自分の役割のラベルが付いたissueを確認して対応する**。
ラベルは **`実装待ち` → `レビュー待ち` → `設計確認待ち` → `push待ち`** の順に回り、不合格なら `実装待ち` に戻る:

- **開発担当**: `gh issue list --label 実装待ち` → 設計書+issue最新コメントの指示に沿って実装・検証
  → 報告コメント追記 → ラベルを `実装待ち`→`レビュー待ち` に付け替え
  (`gh issue edit <n> --remove-label 実装待ち --add-label レビュー待ち`)
- **レビュー担当**: `gh issue list --label レビュー待ち` → **自分でアプリを操作して**評価基準で合否判定
  → 判定コメント追記 → 合格なら `設計確認待ち`、不合格なら `実装待ち` に戻す
- **設計担当**: `gh issue list --label 設計確認待ち` → 設計意図との適合確認・確認事項への回答
  → 承認なら完了処理(クローズはpush時の `closes #<n>` でも可)、追加作業ありなら指示コメント+`実装待ち` に戻す
- **`push待ち`**(2026-08-08〜): 承認まで済み、**本番公開(push)のユーザー判断だけが残っている**状態。
  設計担当が承認時に付け、**pushが完了したら外してクローズする**
  - ⚠️ **担当がセッションではなくユーザー**である点が他のハンドオフラベルと決定的に違う。
    **どのセッションもこのラベルのissueに着手しない**(スプリント進行役も拾わない)
  - ⭐ 目的は「**未pushの本番配信物が、issueを見ただけで分かる**」状態にすること。
    自律運用ではコミットだけが進みpushは溜まるため、**溜まっている事実そのものが見えなくなる**
  - ⭐ **例外: 承認後に軽微な残作業で `実装待ち` へ戻したときは、その作業を終えた開発担当が
    `push待ち` を付けてよい**(2026-08-13・#64 で実際に発生)。⚠️ `実装待ち` に据え置くと
    **スプリント進行役が拾って空回りする**。「設計担当が付ける」の趣旨は
    **承認前のissueにこのラベルが付かないこと**なので、承認済みなら担当は問わない
- 設計完了時は設計担当が `実装待ち` を付ける。ハンドオフラベルは状態ラベルとは別軸(併用する)
- ⚠️ **`レビュー待ち` の担当は設計担当ではなくレビュー担当**(2026-08-06にラベルの意味を変更した)。
  設計担当が見るのは `設計確認待ち`。評価基準の定義は `.claude/agents/review.md` にあり、
  **必須基準(M1〜M7)が1つでも欠けたら不合格・「未検証」は不合格**として扱う

### 役割をまたぐときのルール(2026-08-06〜)

設計・開発・レビューの3役は**同じ作業ツリーを共有し、コンテキストは共有しない**
(設計は別セッション、開発とレビューはスプリントセッションのサブエージェント)。
次のルールで衝突と取りこぼしを防ぐ。

#### 1. 工程が完了したら必ずissueにコメントを残す(全役割・例外なし)

⚠️ **コメントを残さずにラベルだけ付け替えるのは禁止。** ラベルは「次が誰の番か」しか伝えず、
引き継ぎ先は前工程のコンテキストを一切持っていない。**issueに書いていないことは
「無かったこと」として扱われる**(口頭・別セッションの記憶・作業ツリーの状態は引き継がれない)。

| 役割 | 工程完了時に残すコメント | 付け替え先 |
| --- | --- | --- |
| 設計担当 | **設計完了コメント**: 設計書のパス・対象範囲・検証項目の要約・判断済み事項・未決事項 | `実装待ち` |
| 開発担当 | **実装報告コメント**: 上記「実装・検証の結果はissueに追記する」の6項目。⚠️ **コミットハッシュ必須** | `レビュー待ち` |
| レビュー担当 | **判定コメント**: 合否・必須基準(M1〜M7)の表・証跡・不合格なら指摘6点セット | 合格`設計確認待ち`/不合格`実装待ち` |
| 設計担当(最終) | **承認コメント**: 承認の理由・レビューの推奨指摘(S)をどう扱ったか・派生issueの要否 | クローズ |

- 作業の起点は必ずissue。無ければ**設計担当が起票する**(開発・レビューは自分で起票しない)
- 途中で中断するときも**そこまでの状態をコメントに残す**(再開するのが同じセッションとは限らない)

⚠️ ⭐ **記録が無いまま実装だけがコミットされている状態を見つけたら、代筆で埋めない**
(2026-08-26に現実化。実装セッションが報告を残さず終了し、5日間そのまま止まっていた)。
**別セッションの開発担当を呼び、検証項目を全部やり直させて報告を書かせる**。
その報告の冒頭に **「実装者本人ではない別セッションによる追認である」と明記**すること
(実例: `docs/design/未採番-フレーバー表示経路/実装報告_2026-08-26.md`)。

- ❌ **進行役・設計担当が git log から書き起こす**のは代筆＝規約違反。しかも検証を誰も実施していないので
  中身は「未検証」だらけになり、**レビューの必須基準でどのみち不合格**になる
- ❌ **報告を飛ばしてレビューに直行**すると、「設計書からの逸脱と根拠」を誰も説明していない状態で
  判定することになる(差し戻し時にdevが1から読み直す)
- ⭐ **検証のやり直しは思ったより安く済む**。#71 の教訓どおり **`GA_SNAPSHOT_MAX_AGE_MIN=0` で
  スナップショットを固定**すれば、日をまたいでも当時と同じ生成物を再現できる

#### 2. ラベルが担当の排他制御

- **1つのissueにハンドオフラベルは1つだけ**。自分の役割のラベルが付いていないissueは進めない
- 付け替えは**コメントを残した直後**に行う(先に付け替えると、コメントが無い状態で相手が着手する)

#### 3. レビュー依頼の前に必ずコミットする

未コミットのままだとレビュー担当が**対象を特定できず、ほかのセッションの編集と区別も付かない**。
開発担当は `レビュー待ち` に付け替える前にコミットし、**報告コメントにコミットハッシュを書く**。

#### 4. レビュー担当は作業ツリーを変更しない

⚠️ `git checkout` / `stash` / `restore` / ブランチ切り替えは**禁止**(ほかのセッションの作業中の
変更を壊す)。読むのは `git diff` / `git show` / `git log`。コードの修正もしない(差し戻す)。

#### 5. ローカルサーバーのポートを役割で分ける

サブエージェントは1つずつ動くので通常は衝突しないが、**前の担当がサーバーを止め忘れると
`EADDRINUSE` になる**。保険として**開発担当が既定ポート、レビュー担当がずらす**:

| 役割 | 静的プレビュー | Pages Functions込み |
| --- | --- | --- |
| 開発担当 | 3000 (`npm run dev`) | 8788 (`npm run pages:dev`) |
| レビュー担当 | 3100 (`PORT=3100 npm run dev`) | 8888 (`npx wrangler pages dev . --local --port 8888`) |
| 広報担当 | 3300 (`PORT=3300 npm run dev`) | — (使わない) |

⚠️ **`npm run dashboard` は 3200 を使う**(#56)。上の2役とは別枠なので、ダッシュボードを開いたまま
開発・レビューを回してよい。`PORT` で変更可(既に使用中だと `EADDRINUSE` で落ちる=#57)。

`scripts/serve.mjs` は `PORT` 環境変数を見る。⚠️ **終わったら止める**(締め作業の項目5)。

#### 6. `tmp/` は役割ごとにサブフォルダを使う

レビュー担当の証跡は `tmp/review/<issue番号>/`。⚠️ **ほかのセッションの作業物は消さない**。

#### 7. 差し戻しは2往復まで

3回目に入る前に `設計確認待ち` へ回して**設計担当に判断させる**。
3往復する不合格は、実装の不備ではなく**基準の解釈違いか仕様の曖昧さ**であることが多い。

#### 8. 越権の禁止(各役割の「やらない」)

- **設計担当は実装コードを書かない** — 書くと誰もレビューしない差分が入る
- **開発担当は仕様を決めない** — 判断が要る項目はissueに書いて設計担当の回答を待つ
- **レビュー担当はコードを直さない** — 直すと誰も再レビューしない
- いずれも「自分でやったほうが速い」場面で破りたくなる。**破るなら先にissueに書いて合意を取る**

### 自律スプリント運用(2026-08-06〜)

**ユーザーは各工程の間に入らない。** `スプリントセッション` が1つ動き、その中で
**開発担当とレビュー担当をサブエージェントとして交互に呼び出して**
`実装 → レビュー → (不合格なら)修正 → 再レビュー` を回す。
ユーザーが関わるのは**設計**と**最後の承認・push**だけ。

#### 構成

```text
ユーザー ── 設計セッション(cc-design)            … 設計書を書き `実装待ち` を付ける
                    │ issue
ユーザー ── スプリントセッション(cc-sprint)       … 進行役。自分では実装も判定もしない
                    ├─ サブエージェント dev     … 実装してコミット→報告→`レビュー待ち`
                    └─ サブエージェント review  … 操作して判定→合格`設計確認待ち`/不合格`実装待ち`
```

- **ターミナルは1つ**。`cc-sprint` を起動して「issueを対応して」と言えば、
  合格するかストップ条件に当たるまで**サブエージェントの往復で回り続ける**
- ⚠️ **サブエージェントは親ともお互いともコンテキストを共有しない。**
  受け渡しは**設計書とissueコメントだけ**という規約はそのまま
  (むしろレビュー担当が実装時の思い込みを引き継がない利点がある)
- サブエージェントは **`run_in_background: false` で1つずつ**呼ぶ。
  同時に走らせると**同じ作業ツリーを取り合う**

#### 進行役(スプリントセッション)がやること・やらないこと

- **やる**: `実装待ち` / `レビュー待ち` の確認、サブエージェントの呼び出し、
  往復回数の管理、ストップ条件の検知、ユーザーへの報告
- ⚠️ **やらない**: 自分でコードを書く・自分で合否を判定する・issueコメントを代筆する。
  **記録は必ず担当サブエージェント自身に書かせる**(進行役が書くと、次の周回で
  サブエージェントが読む前提が崩れる)

#### WIP=1(同時に進行するissueは1件だけ)

サブエージェントを1つずつ呼ぶ限り自動的に守られるが、**設計セッションが同じ作業ツリーを
共有している**点は変わらない。⚠️ 設計セッションが `docs/` 以外を触っていないことを、
コミット前に `git status --short` で確かめる。

将来これを並列化するなら `git worktree` でレビュー専用のツリーを切る。
**今の運用は「1件ずつ」で回す**(ツリーが1つしかない以上、これが唯一安全な形)。

#### ⚠️ 必ず止まってユーザーを待つ条件

次に当たったら**往復を止め、issueにコメントを残してユーザーに報告する**。
勝手に判断して進めない:

| 条件 | 止め方 |
| --- | --- |
| **pushが必要になった** | 止まる。本番公開の可否は**常にユーザーの判断**(自律運用でもこの規則は変わらない) |
| **仕様の判断が要る** | `設計確認待ち` に付け替えて止まる |
| **設計書が無い/不十分で実装できない** | `設計確認待ち` に付け替えて止まる。**推測で実装しない** |
| **差し戻しが2往復を超えた** | `設計確認待ち` に付け替えて止まる(基準の解釈違いか仕様の曖昧さが原因) |
| **`実装待ち` が空** | 止まる。**新しい作業を自分で作らない**(起票は設計担当のみ) |
| **スコープ外の変更・破壊的な操作が要る** | 止まる。設計書に無いことはやらない |
| **同じ失敗を2回繰り返した** | 止まる。3回目を試す前に状況をissueに書く |

#### 未pushコミットが積み上がることを前提にする

自律運用中は**コミットだけが進み、pushは溜まる**。

- ⚠️ **日次cron(03:00 JST)がmainに自動commit・pushする**ため、ローカルに未pushコミットを
  溜めるとリモートと乖離する。**ユーザーがpushを指示したときは、先に
  `git pull --rebase origin main` で取り込んでから**送る
  (2026-08-06に実際に乖離し、rebase無しでは弾かれた)
- ユーザーへの提示は締め作業と同じ形(未pushの内訳を出し、可否を仰ぐ)

#### 進捗はissueだけを見れば分かる状態にする

ユーザーは往復の外にいるので、**issueが唯一の状態表示**になる。
⚠️ **`--label` を2つ渡すとAND条件**になるため、ラベルごとに1回ずつ問い合わせる
(2026-08-06、まとめて渡して「残件なし」と誤報告した):

```bash
gh issue list --label 実装待ち      # 開発待ち(または差し戻し中)
gh issue list --label レビュー待ち   # レビュー中
gh issue list --label 設計確認待ち   # ユーザー(設計)の番。ここに溜まるのが正常
gh issue list --label push待ち      # 承認済み・push待ち。⚠️セッションは着手しない
```

⚠️ **ラベルの確認結果は数分で陳腐化する。** 別セッション(スプリント)が同時に動いていると、
**確認した直後に工程が進んでラベルが移る**(2026-08-06、`設計確認待ち` が空だと報告した数分後に
#44 が `実装待ち`→`レビュー待ち`→`設計確認待ち` まで進んでいた)。
→ **ラベルを引いてから行動するまでに時間が空いたら引き直す**。ユーザーから
「◯◯があるはず」と言われたら、**自分の直前の観測ではなく引き直した結果を正とする**。

「工程が完了したら必ずissueにコメントを残す」ルール(前節1)は、自律運用では
**唯一の引き継ぎ手段であると同時に、ユーザーが後から経緯を追える唯一の記録**になる。
⚠️ **省略は禁止**。

### 締め作業(2026-08-03〜・全セッション共通)

「締め作業をお願いします」と言われたら、**次を必ず全部行う**。⚠️ **1〜2は省略しない**
(2026-08-03、締めの報告後にユーザーから「メモリーやCLAUDE.mdの最新化もされていますか?」と
指摘を受けた。CLAUDE.mdの更新が漏れていた):

1. **CLAUDE.mdの最新化** — その日の作業で**恒久的な運用ルール・落とし穴・依存関係**が増減したら反映する。
   とくに ⚠️ **「あるファイル/データを消費する側が増えた」ときは、壊れたときの症状の表を更新する**
   (実例: メタ索引 `f` の消費側が #45 の1つから #46 で2つになった)
2. **メモリー(`/home/node/.claude/…/memory/`)の最新化** — 待ち行列・進行中の状態・
   自分の失敗から得た教訓を書く。⚠️ **コンテナ再構築で消える**ので、
   **恒久ルールはCLAUDE.md、セッションをまたぐ状態や教訓はメモリー**と使い分ける
3. 未コミットの変更が無いこと・未pushの内訳をユーザーに提示する(pushの可否は都度ユーザーが判断)
4. ハンドオフラベル(`実装待ち`/`レビュー待ち`/`設計確認待ち`)の残件と、次に着手できるissueを挙げる
5. 起動したローカルサーバー(`npm run dev` 等)を止める。⚠️ **`tmp/` 配下は
   ほかのセッションの作業物があるので消さない**

## git運用(2026-07-22〜・全セッション共通)

- **commitはユーザーの指示なしで実施してよい**(むしろ積極的に行う)。
  未コミットの変更を溜めると、コンテナの再構築・障害時に**作業を丸ごと失う**ため。
  区切りのよいところでこまめにコミットする
- ⚠️ **`git add -A` / `git commit -a` は使わない。自分の担当パスを明示してステージする**
  (設計担当は `docs/` と `CLAUDE.md`、開発担当は `scripts/` と生成物。
  **レビュー担当は原則コミットしない**——記録はissueコメント、作業物は `tmp/review/<issue番号>/`)。
  各セッションが**同じ作業ツリーを共有している**ため、相手が編集中のファイルを巻き込む。
  コミット前に `git status --short` で自分が触っていないパスの変更が無いか確認する
  (実例: #18 で設計担当が `2e26da09` に開発担当の未コミット変更を巻き込んだ)
  - ⚠️ ⭐ **担当パスの検査を `grep` でやるときは `git -c core.quotepath=false` を付ける**
    (2026-08-28に実際に誤判定した)。gitは**非ASCIIのパスを `"docs/…"` とクォートして出す**ため、
    `git show --name-only | grep -v '^docs/'` は**先頭が `"` になって全件が「担当パス外」に見える**。
    ⚠️ **このリポジトリの設計書パスは日本語なので、ほぼ必ず踏む**:

    ```bash
    git -c core.quotepath=false show --name-only --format= <commit> | grep -v '^docs/'
    ```

  - ⭐ **越権の検査は「変更ファイルの集合」で見る**。工程をまたぐ運用(設計→実装→レビュー)では、
    **前工程のコミット以降の差分**(`git diff --name-only <前工程>..HEAD`)を見ると、
    その担当が触ってはいけないパスに手を入れていないかが1コマンドで分かる
- **pushはユーザーの明示的な指示があるまで実行しない**。
  mainへのpushはCloudflare Pagesの自動デプロイを通じて**そのまま本番公開になる**ため、
  公開の可否はユーザーが判断する。`gh` でのissue操作(コメント・ラベル・クローズ)は
  この制限の対象外(公開サイトに影響しないため)
- **例外(2026-07-23〜): `docs/` 配下だけの変更は承認なしでpushしてよい**。
  `docs/` は `functions/docs/[[path]].js` が404を返すため**本番配信の対象外**で、
  pushしても公開サイトの内容は変わらない(実測: `/docs/` `/docs/design/…` とも404)。
  設計書がローカルにしか無い状態を減らし、コンテナ再構築での消失を防ぐのが目的。
  ⚠️ **pushは未push分を全部送る**ため、**実行前に必ず次を確認する**:

  ```bash
  git diff --stat origin/main..HEAD -- . ':(exclude)docs/*'   # ← 出力が空であること
  ```

  1行でも出力があれば**本番配信物が混ざっている**のでユーザーの指示を待つ。
  「自分のコミットは `docs/` だけ」では**不十分**(もう一方のセッションの未pushコミットも一緒に出ていく)
- ⚠️ **`CLAUDE.md` はこの例外に含まれない。** ルート直下のファイルは静的アセットとして
  **そのまま配信される**(実測: `/CLAUDE.md` `/README.md` `/package.json` `/wrangler.toml` はいずれも **200**)。
  `docs/` のように404で遮断されているわけではないので、**変更時はユーザーの指示を待つ**
  - ⚠️ **`scripts/` も配信される**(2026-08-08実測: `/scripts/lib/cards-snapshot.mjs`
    `/scripts/build-card-pages.mjs` とも **200**)。ビルドツールの変更でも**pushは本番公開**になるので、
    「サイトの見た目が変わらないから安全」と判断しない(#24の対象)
  - ⚠️ **`migrations/` も配信される**(2026-08-10実測: `/migrations/0001_init.sql` 〜 `0004_deck_format.sql`
    がいずれも **200**)。⭐ ただし**中身はスキーマ定義だけで秘匿情報は無い**ため、新しい種類の露出ではない
    (#24の対象に含める)。⭐ **新規マイグレーションはデプロイ到達マーカーとして使える**
    (新規URLなのでエッジに古いコピーが無く、404→200 で即確定できる。#4のpushで実際に約1分で確定した)
  - ⭐ **ドットフォルダ配下は配信されない。ルート直下のドットファイルは配信される**
    (2026-08-09実測・#9のpush時に確認。**キャッシュ回避クエリ付き**で測定):

    | パス | 応答 |
    |---|---|
    | `/.devcontainer/setup.sh` `/.devcontainer/devcontainer.json` | **404** |
    | `/.claude/aliases.sh` `/.claude/agents/dev.md` | **404** |
    | `/.gitignore` | **200** |
    | `/CLAUDE.md` `/package.json` `/wrangler.toml` `/robots.txt` | **200** |

    - ⚠️ **2026-08-05の「ドットフォルダも200」という記述は誤りだった**(同じURLが2026-08-09に404)。
      Pagesは**ディレクトリ名が `.` 始まりのものを配信対象から除外する**と考えるのが実測に合う
    - ⭐ **`.devcontainer/` `.claude/` の変更は本番の公開面を増やさない。**
      ただし**push自体は本番デプロイを起こす**(同じコミットの他ファイルが公開される)ので、
      **pushの承認が要る点は変わらない**
    - ⚠️ **新規ファイルを「デプロイ到達マーカー」に使うときは、ドットフォルダ配下を選ばない**
      (永久に404のままなので到達判定にならない。#9 で実際に5分間空振りした)
- 例外: GitHub Actions `build-tournaments.yml` による日次の自動commit・pushは
  ユーザー合意済みの運用のため対象外

## 本番公開の第2経路: wrangler直接デプロイ(2026-08-27〜)

**本番公開の経路は2つある。** 既定はpushだが、**GitHubが使えなくても
`wrangler pages deploy` で本番に出せる**(2026-08-27にプレビューで実証)。

| 経路 | 起こし方 | 使う場面 |
|---|---|---|
| ① push → Pages自動デプロイ | `git push origin main` | 通常。`deployments_enabled: true` のまま生きている |
| ② wrangler直接デプロイ | 下記手順 | **GitHubが止まっているとき**・push前にプレビューで見たいとき |

- ⚠️ ⭐ **②も本番公開である。** 「pushしていないから安全」ではない。
  **公開の可否は常にユーザーが判断する**という規則は、経路が変わっても一切変わらない
- ⭐ **②が使えるのは `~/.cloudflare-token`(実体は `wrangler-deck-builder`)が
  `Cloudflare Pages: 編集` を持つため**(他に D1:編集 / アカウント分析:読み取り /
  アカウント設定:読み取り / ユーザーの詳細:読み取り)。
  ⚠️ **トークン画面で `ロール`(Roll)を押すと失われる**(wranglerもダッシュボードのRUM取得も同時に落ちる)。
  ⚠️ トークン自身の権限一覧は API から読めない(`/user/tokens` が **403**)ので、確認はダッシュボード目視
- ⚠️ **②はGitHubの代替にはならない。** 回復するのは**本番公開だけ**で、
  issueハンドオフ・日次cronは止まったまま(記録は `docs/design/` 代替を続ける)

### ⚠️ 手順: 必ずステージングを作ってから実行する

⚠️ ⭐ **リポジトリ直下で `wrangler pages deploy .` を打ってはいけない。**
未追跡の **`tmp/`(2026-08-27実測 548MB)** には運営ダッシュボード出力(登録者数・閲覧数)・
X投稿用画像・**`env-backup-*.tar.gz`** があり、**公開URLに載りうる**
(⚠️ Pagesのアップロードは `.gitignore` を見ない)。

```bash
npm run deploy:preview                  # プレビューへ(既定ブランチ preview)。DEPLOY_BRANCH=xxx で変更可
CONFIRM=publish npm run deploy:prod     # ⚠️ 本番公開。ユーザーの明示的な指示を得てから
```

- ⭐ **どちらのスクリプトも `deploy:stage` を必ず経由する**(`git archive HEAD` で `tmp/deploy-stage/` に
  追跡ファイルだけを展開してから、その中で `wrangler pages deploy` を実行する)。
  ⭐ **`tmp/` 混入の罠を「覚えておく」のではなく構造で防ぐ**のが目的
- ⚠️ **`deploy:prod` は `CONFIRM=publish` が無いと exit 1 で拒否する**(`db:migrate:remote` と同じ流儀)。
  ⭐ **誤爆を防ぐと同時に、「公開の指示を受けた」ことが実行ログに残る**
- ⚠️ **ガードはあくまで誤爆防止で、判断の代わりではない。**
  **本番公開はユーザーの明示的な指示があるまで実行しない**(経路①のpushと同じ規則)

- ⭐ **`git archive HEAD` を使う理由は漏洩対策だけではない。** ②は
  **gitの状態と無関係な本番**を作れてしまう経路なので、**必ずHEADから作って乖離を生まない**。
  ズレたまま後日pushすると、次の自動デプロイが**黙って上書きする**
- ⚠️ **`--config` でのパス指定は使えない**(`Pages does not support custom paths for the
  Wrangler configuration file`)。**ステージングの中に `cd` してから実行する**
- ⚠️ `wrangler.toml` に `pages_build_output_dir = "."` があるので**ディレクトリ引数は渡さない**
- 実測(2026-08-27・4179ファイル・262MB): 新規**792ファイルのみ**のアップロードで **3.4秒**
  (残り3369はGit連携ビルドの資産を再利用)。Functionsも `Compiled Worker successfully` でビルドされる
- ⚠️ 事後に**危険物が載っていないこと**を必ず確認する(すべて404が正):
  `/tmp/dashboard/index.html` `/node_modules/…` `/.wrangler/state`

### プレビュー(`--branch` が `main` 以外)の性質

| 項目 | 2026-08-27 実測 |
|---|---|
| 本番デプロイ | **変化しない**(`a7f1e444`・`c5777923` のまま) |
| URL | `https://<ブランチ名>.ga-card-tools-jp.pages.dev` と `https://<id>.…` の2つ |
| 検索エンジン | ⭐ **`x-robots-tag: noindex` が自動で付く**(本番には付かない) |
| Functions | 動く(`/api/me`=401・`/docs/`=404 とも本番と一致)。⚠️ **ただしDiscordログインは動かない**(下記) |
| **D1バインディング** | **疎通する**(`/api/health` が本番と同一のテーブル一覧を返す) |

- ⚠️ ⭐ **プレビューのD1は本番と同じデータベースを指す**(`database_id` が同一)。
  **プレビューURLからのデッキ操作は本番データを書き換える**。閲覧はしてよいが**書き込みはしない**
- ⭐ **未pushコミットの見た目をpush前に確認できる**のが最大の利点。
  `git archive HEAD` から作るので、**そのままpushしたときに出るものと一致する**
- ⚠️ プレビューURLは**認証が無く公開されている**(推測されにくいだけ)。
  未公開の変更を載せる以上、**URLを外部に貼らない**
- ⭐ `/api/health` は**D1バインディングの疎通確認専用**のエンドポイント(認証不要・SELECTのみ)。
  新しい公開経路を試すときの**最初の1手**にする
- ⭐ **ダッシュボードで設定した環境変数・シークレットは、wranglerデプロイでも配られる**
  (2026-08-27実測: `wrangler.toml` に `[vars]` が無いのに、デプロイ記録へ
  `DISCORD_CLIENT_SECRET` が入っていた)。⭐ **「`wrangler.toml` があるとダッシュボード設定が
  無視される」ということはない**＝経路②で本番へ出してもログインは壊れない
- ⚠️ ⭐ **デッキ構築ツールはプレビューでは使えない**(2026-08-27実測)。理由は2つあり、どちらも設定漏れ:
  ① Pagesの **Preview環境に `DISCORD_CLIENT_ID` が無い**(Productionにはある)ため、
  `/api/auth/discord/authorize` の Location が **`client_id=undefined`** になる
  ② `redirect_uri` は `url.origin` から組み立てる(`functions/api/auth/discord/authorize.js`)ので
  **プレビューのURLになり、Discord側の登録済みリダイレクトURIと一致しない**
  - 直すには **①ダッシュボードでPreview環境に `DISCORD_CLIENT_ID` を追加**し、
    **②Discord Developer Portalにプレビューの callback URL を登録する**(どちらもユーザー作業)。
    ⭐ ブランチ別名URL(`https://<ブランチ名>.…`)は**デプロイし直しても変わらない**ので登録は1回で済む
  - ⚠️ ⭐ **ただし直すと安全装置が外れる。** プレビューのD1は**本番と同じDB**なので、
    ログインできるようにした瞬間から**プレビューでのデッキ操作が本番データを書き換える**。
    ⭐ 安全に使いたいなら、先に **Preview環境へ別のD1を割り当てる**(`[env.preview]` + 新DB + マイグレーション)

### 本番へ出すとき(`--branch=main`)

⚠️ **ユーザーの明示的な指示があるまで実行しない**(経路①と同じ)。到達確認の方法も同じで、
**素のURLとキャッシュ回避クエリを並べ、全数一致するまで反復する**
(「環境の注意」のPagesキャッシュの節を参照)。

## ⚠️ 大会データの週1更新（2026-08-27〜・公開済みの約束）

**Xで「大会データは週1回程度を目安に更新する」と公言した**
（<https://x.com/maru_selector/status/2092974480545226799?s=20> へのリプライ）。
⚠️ **これは対外的な約束なので、守れないと信用を損なう**。日次cronが止まっている間の**暫定の運用義務**。

- **起点 2026-08-27**。以後**週1回**、次の3つを順に実行して公開する（＝日次cronの代行）:

  ```bash
  node scripts/build-tournament-pages.mjs   # 大会スキャン＋ページ生成
  node scripts/build-card-pages.mjs         # カード/セット/sitemap（⚠️ tournaments の後）
  node scripts/gen-card-meta-index.mjs      # 日本語検索のメタ索引
  ```

  → `npm run validate` → **cronと同じ8パスでコミット** → `CONFIRM=publish npm run deploy:prod`
- ⚠️ **リマインダーの仕組みは無い**（issueもcronも止まっているため、気づけるのは人だけ）。
  ⭐ **セッション開始時に「最後の定期再生成コミットから1週間経っていないか」を見る**:

  ```bash
  git log -1 --format='%h %ci %s' --grep='^定期再生成'   # ⚠️ 行頭アンカー必須（下記）
  ```

  - ⚠️ ⭐ **`--grep` はコミット本文にも当たる。** アンカー無しだと、`定期再生成` という語を
    **説明しただけのドキュメントのコミット**を拾って**別の日付を答える**
    （2026-08-31 実測: 本物は `03ff53a5` なのに `8dcaf3ae`(運用ドキュメントの追記)を返した）

- ⭐ **GitHubが復旧して日次cronが戻れば、この約束は自動で果たされる＝この節は不要になる**。
  復旧後に削除すること（⚠️ 残すと、動いていない義務がドキュメントに残り続ける）
- ⚠️ **同時に「載っていない大会があればお知らせください」とも公言している**ので、
  問い合わせが来たら `data/tournaments/pending.json` を先に見る（未確定の大会はそこに積まれている）

## 開発コマンド

- `npm run dev` — 静的プレビュー(scripts/serve.mjs、ポート3000)
- `npm run pages:dev` — Cloudflare Pages Functions込みのローカル実行(wrangler、ポート8788)
- `npm run validate` — データ検証
- `npm run dashboard` — **運営ダッシュボード**(#56・2026-08-09〜)。登録者数・デッキ・閲覧数・運用ヘルスを
  1枚のHTMLにして**ローカルの3200番**で表示する。`--no-fetch` で取得せず再生成(オフライン可)・`--no-serve` で生成のみ
  - ⚠️ **出力は `tmp/dashboard/` 配下のみ。絶対にコミットしない**(このリポジトリは公開されており、
    登録者数・閲覧数・人気カードは運営情報。`docs/` に置くと本番配信されなくても**GitHubで誰でも読める**)
  - ⚠️ **本番D1を `--remote` で読む。SELECT 以外を書かないこと**
  - **Cloudflare APIトークンに `Account Analytics: Read` が要る**(閲覧数のみ。無くても他3セクションは出る=fail-open)。
    ⚠️ ダッシュボードでトークンを編集するときは **`Edit`(値が変わらない)と `Roll`(再発行される)を取り違えない**
  - ⚠️ **Web Analytics の `count` に `avg(sampleInterval)` を掛けてはいけない**(#56 で実測)。
    `count` は**すでにサンプリング補正済み**で、掛けると**約10倍に過大評価**する。
    根拠: 日別グループとパス別グループの総 `count` が完全一致し、全行が `sampleInterval` の倍数に丸まる
  - ⚠️ **site tag(`27d8…`)と site token(`79f3…`)は別物**。GraphQL の `siteTag` に後者を渡すと
    **エラーにならず0件**になる。HTMLのビーコンに書くのは後者
  - 詳細は `docs/design/56-運営ダッシュボード/`
- ⚠️ **新セットが公式APIに入ったら `data/translations.js` の `meta.sets` に手で追記する**(#40)。
  セットページ・`sitemap.xml`・トップの静的リンクは**日次cronがAPIから自動生成する**が、
  **エキスパンション絞り込み(`#f-set`)の選択肢だけは手書き**で、更新の号令が無い。
  追記を忘れると**そのセットで絞り込めず、ラベルが素のprefix**になる
  (実例: SP4 が 2026-07-02 にAPI追加され、**32枚が約1か月間ずっと絞り込めなかった**)
  - 追記の形: `{ label: "…（PREFIX）", prefixes: ["PREFIX"] },` を**発売日の新しい順**の位置へ。
    ⚠️ `release_date` が `1970-01-01`(API未設定)のセットは並べ替えに使えないので `created_at` で判断する
  - 追記後は **`npm run build:cards` を実行して生成物もコミットする**(ラベルが約35ファイルに出る)。
    忘れると翌朝のcronが同じ差分を無関係な自動コミットとしてpushする
    - ✅ **`tmp/api-cache/` のキャッシュ2つ(カードスナップショット・featured-sets)は
      どちらも有効期限つきになった**(#52・#53。既定60分・超えると自動で取り直す)ので、
      **`--refresh` を手で付ける必要は無くなった**。期限は環境変数 `GA_SNAPSHOT_MAX_AGE_MIN`
      (未設定=60 / `0`=無期限=オフライン作業用 / 正の整数=その分数)が**2つまとめて**支配する
    - ✅ 書き込みは原子的(一時ファイル+rename)・壊れたJSONは自動で取り直す(#54)。
      **キャッシュを手で消して復旧する手順は不要になった**
    - ⚠️ 過去の実例(#51): 7/27取得のキャッシュのまま回したところ、**33件のはずが61件**＋無関係な8セットページに
      差分が出て、`sets/prdevp/index.html` は**再生成すらされなかった**(stderrは `セット56ページ`)。
      **エラーは一切出ない**ので、差分の件数を数える以外に気づく手段がない
      → **設計書に「期待されるファイル数」を書く**教訓は有効期限があっても変わらない
      (#51はそれで検出できた。期限内60分の窓は今も残る)
  - ✅ 検出経路は2つある(#40): **`npm run validate` が exit 1 で落ちる**(人を止める)/
    **`build:cards` が完走サマリに1行出す**(cronログに痕跡。⚠️ **exit 1 にはしない**——
    cronの後段に到達しないとその日の大会データの取り込みごと失われるため)
  - ⚠️ **cronは `validate` を実行しない**ので、**新セット当日の自動publishは止められない**。
    PRDスポイラー等でセットが増えたら**自分で `meta.sets` を確認する**
  - ⚠️ 共有URLは壊れない(`setKeyOf` は添字ではなくprefixを書く・#20の設計)。
    ⚠️ ⭐ **ただし「並び順は自由に変えてよい」ではなくなった**(2026-08-30・未採番「版順序の非決定性」)。
    `meta.sets` の並びは **`setOrder()` 経由で `build-card-pages.mjs` の版の正規順序(第2キー)にも効く**ため、
    **並べ替えると同日発売のセットどうしで代表画像(`og:image`)・収録セット表の行順が動く**
    (＝カードページの生成物に差分が出る)。⭐ **新セットの「追記」は安全**
    (先頭に足すので既存カードの同日比較に影響しない)。**並べ替えるときだけ `build:cards` の差分を確認する**
  - ⚠️ ⭐ **「新セットが来たら手で足す辞書」は `meta.sets` だけではない**(2026-08-20発覚)。
    同じ型の抜けが `data/translations.js` の**2箇所**で実際に起きていた。**どちらも fail-open で
    `npm run validate` が緑のまま通った**(＝#40 と同じく気づけなかった):

    | 辞書 | 抜けていた量 | 症状 | 今 |
    |---|---|---|---|
    | `meta.subtypes` | 10種 / 94枚(すべてPRD系) | サブタイプ行が**素の英字**(`VELTECH` 等) | ✅ **解消**(2026-08-21) |
    | `terms` | 15種(カウンター語) | **ハイライトも用語解説も出ない**(⭐ 訳文には既に日本語表記がある) | ✅ **解消**(2026-08-21) |

    - ✅ **検出経路が3つある**(2026-08-21 に2つ・**2026-08-30 に3つ目**。設計書
      `docs/design/未採番-辞書の抜けと検出経路/` と `docs/design/未採番-用語ハイライトの語形ずれ/`。
      ⚠️ **どれも未pushなので本番にはまだ出ていない**):

      | 経路 | 何を見るか | 強さ |
      |---|---|---|
      | `npm run validate` | `data/card-meta-index.json` に出る全サブタイプが `meta.subtypes` にあるか(**片方向**。辞書にだけある `SHENJU` では落とさない) | ⭐ **exit 1 で人を止める**(成功時は `meta.subtypes covers all subtypes — N種`) |
      | `npm run validate`(**2026-08-30〜**) | ⭐ **生成済みカードページの日本語効果文に、`term-hl`/`card-name-hl` の外側で `terms` の中核語が残っていないか**(**片方向**。用語解説にだけ出るものでは落とさない) | ⭐ **exit 1 で人を止める**(成功時は `card term highlights up to date — 2495 ページ`) |
      | `node scripts/check-terminology.mjs` | 英語効果文の `<語> counter` が `terms` にあるか | 一覧に出すだけ(⚠️ **exit code は変えない**。赤くすると使われなくなるため) |

      - ⚠️ ⭐ **3つ目の緑を「全部直った証拠」として扱わない。** 検査は**生成物の字面**を見るので、
        **既にハイライト済みの語の内側は原理的に見えない**(実測: 直すべき140枚のうち**拾えるのは139枚**。
        `shadowstrike` は「プレパレーション」が既に `term-hl` なので、残りテキストに
        中核語「プレパレーションカウンター」が現れない)。**規則の正しさは別途カード単位で確認する**
      - ⚠️ **この検査は「用語や訳を足したのに `build:cards` を回していない」も捕まえる**
        (`data/tl-*.json` の陳腐化検査と同型の穴が、カードページ側にも開いていた)
      - ⚠️ ⭐ **検査自身に穴がある**(2026-08-30・レビュー S-1)。効果節を
        `<h2>効果（日本語）</h2><p class="cp-effect">` の**字面で拾う**ため、
        **`<p>` に属性が1つ増えるだけで節を1つも走査しないまま緑になる**。
        → 起票案 `docs/design/未採番-用語ハイライトの語形ずれ/起票案_2026-08-30_ハイライト検査が無言で緑になる.md`
        (⭐ **カードページのテンプレートを変えるタスクより前に入れる**)

    - ⚠️ **cronは `validate` を実行しない**ので、**subtypes 側も新セット当日の自動publishは止められない**(#40 と同型)
    - ⚠️ **`terms` に「無い」と判定するときは基語も見る。** `matchedTerms()` は
      **`\b` + キーの前方一致**なので、`preparation counter` は**キー `preparation` で既に拾えている**。
      キーの完全一致だけで数えると**過剰検出**になる
      (⭐ **これは英語ゲート側の話**。2026-08-30以降は**訳文ゲートとの和集合**になった＝次項)
    - ⚠️ ⭐ **「キーはあるのに出ない」型もあった**(2026-08-30に全数調査で確定 → **同日 ✅ 解消**)。
      旧実装の `matchedTerms()` は**英語原文だけ**に `\b`+前方一致でキーを当てていたため、
      ⚠️ **キーが英語に当たらなければ、訳文にどれだけ明確に用語が書かれていても黙って何も出なかった**
      (fail-open)。実在した欠落は **155件 / 140枚**(18キー)＝**2つの型**:

      | 型 | 原因 | 件数 / 枚数 |
      |---|---|---|
      | ① | **英語の語形がずれてキーが当たらない**(下表) | **65 / 64** |
      | ③ | ⭐ **英語原文に該当語が無い**——訳者が英語より手厚くリマインダーを補っている。**英語をゲートにする限り原理的に拾えない** | **90 / 78** |

      | キー | 中核語 | 件数 | 英語の実際の語形 | ずれ方(型①) |
      |---|---|---|---|---|
      | `on hit` | ヒット時 | 39 | `On Champion Hit` / `On Ally Hit` | **語中に別の語が挿入**され連続一致しない |
      | `on kill` | キル時 | 4 | `On Ally Kill` | 同上 |
      | `glimpse` | グリンプス | 9 | `glimpsing` | ⚠️ **e脱落**で前方一致にならない(`banished` 型の活用は当たる) |

      - ⭐ **症状は「訳文が同じなのにカードによって出たり出なかったり」だった。**
        `astra-sight`(出た)と `cometfall`(出なかった)は**リマインダー文が同一**で、
        英語に `glimpse` が別途あるかだけが違った
      - ✅ ⭐ **2026-08-30に案C′で解消**(設計書 `docs/design/未採番-用語ハイライトの語形ずれ/`。
        ⚠️ **未pushなので本番にはまだ出ていない**)。**`matchedTerms()` の判定は
        「英語ゲート」から「英語 ∪ 訳文」の和集合になった**——英語にキーが当たらなくても、
        **中核語(`jp` の「（」より前)が和訳の効果文に現れれば採る**
      - ⚠️ ⭐ **その結果、中核語が「用語を採るかどうかのゲート」そのものになった。**
        `data/translations.js` の `terms` で **`jp` の字面を変えると、その用語の解説とハイライトが
        まるごと消えうる**(型③の90件は**訳文の一致だけ**が根拠なので、逃げ道が無い)。
        ⚠️ **訳語を言い換えるときは `terms` の `jp` も同時に直す**
      - ⚠️ **自カード名は同じ長さの空白で潰してから訳文を探している**(「雪のフラクタル」→ `fractal` のような
        名前由来の誤検出を防ぐため)。⭐ **カード名由来の語を用語として拾わない**のは仕様
      - ⚠️ ⭐ **中核語が同じキーを2つ足すと、同じ見出しが2行並ぶ**(現在134キーはすべてユニークなので起きない)。
        **`terms` にキーを足すときは中核語の重複を確認する**
      - ⚠️ **ハイライトの実装は2ファイルに二重定義されている**(`shared/js/card-detail.js` と
        `scripts/build-card-pages.mjs` に約125行ずつ)。**片方だけ直しても何も止まらない**
        → 起票案 `docs/design/未採番-用語ハイライトの語形ずれ/起票案_2026-08-30_用語ハイライト規則の二重定義.md`
      - ⚠️ ⭐ **`check-terminology.mjs` はこの型を原理的に検出できない。**
        同ツールは**キーが英語に当たったもの**だけを見るので、当たらない155件は永久に出てこない
        (⭐ **検出経路になったのは上表の `validate` 3つ目**)。
        しかも**既存報告11件は全数が誤検出**(`starcall`×10 は `starcalling` キーで正常動作・
        `charge`×1 はカード名由来)＝**探す方向が逆を向いている**
      - ⚠️ **`matchedTerms()` は該当キーを全部返す。** 「このキーが当たるからこのキーの中核語で
        判定される」という単独キー前提の推論は誤り。中核語は**長い順**に当たるので、
        `Starcalling` は `starcall`(スターコール)ではなく `starcalling`(スターコーリング)が優先される
      - ⚠️ ⭐ **2026-08-26版のこの節は「`starcall` の中核語ずれで10枚が無反応」と書いていたが誤りだった**
        (2026-08-30に実測し取り下げ: `docs/design/未採番-スターコーリングのハイライト欠落/取り下げ_2026-08-30.md`)。
        ⭐ 原因は **`check-terminology.mjs` の報告を実画面で裏取りせずに症状として書いたこと**。
        報告は「疑い」であって症状ではない——**起票前に `grep -c 'term-hl">…'` を1行打つ**
- `npm run build:cards` — カード個別ページ・セット別ページ・`sitemap.xml` を生成
  - ⚠️ **出力に「日付依存」が1つだけある**(#34のシーズン禁止)。`data/seasonal-banlist.json` の
    `effectiveFrom` / `effectiveTo` を跨ぐ日に、**対象カードのページだけ**表示が変わる。
    「出力は決定的であること」への**意図的な例外**で、毎日変わるわけではない
    (境界日以外は同日2回実行してもバイト一致する)。境界日は**日次cronが翌朝までに自動追従**する
  - ⚠️ **手動実行すると、タスクと無関係な差分が混ざり得る**(2026-08-17・#71で実測)。
    `build:cards` は**実行時点の公式API応答**をそのまま使うため、前夜のcron以降にAPI側が動くと
    **新カードの追加**だけでなく、**既存カードの `editions` 配列の並び替え・画像URLの入れ替え**が
    無関係なカードページ・セットページの差分として現れる(#71では**31ファイル**=カード25+セット6)。
    ⭐ **無関係な差分はタスクのコミットに混ぜず、翌朝のcronに任せる**
    - ⭐ **スナップショットキャッシュが有効期限内(既定60分)なら、ドリフトは構造的に混入しない**
      (2026-08-18・#73で実測)。`build:cards` は `tmp/api-cache/cards-snapshot.json` を再利用するため、
      **直前に取得済みならAPIを叩かない＝実行時ドリフトが入る余地が無い**。#71 が混入したのは
      キャッシュが期限切れで取り直しになったため。⭐ **翻訳追加のように「APIを見に行く必要がない」作業では、
      先にキャッシュを温めてから `build:cards` を回すとドリフト混入を避けられる**
    (cronが同じ内容を自動コミット・pushするので、放置してよい。#71では混ぜてレビューM7で差し戻しになった)
    - ⚠️ ⭐ **ドリフトの検査は「変更ファイル数」「ファイル名の集合差」では不十分**
      (2026-08-26・未採番(フレーバー表示経路)で**不合格1回分のコスト**として現実化した)。
      **1つのファイルの中に、タスク由来の差分とドリフトが同居しうる**ため、
      **外側(495ファイル以外に出たか)しか見ない検査は原理的に見落とす**:

      | 検査 | 検出できる | ⚠️ 見落とす |
      |---|---|---|
      | ❌ 変更ファイル名の集合を突き合わせる | 対象**外**に出たドリフト | **対象ファイルの内側に同居するドリフト** |
      | ✅ 対象ファイル**それぞれの差分の中身**から、タスクが変える節を除去してなお残る差分を見る | 内側・外側の両方 | — |

      ```bash
      # 例: フレーバー節を除去してなお差分が残るファイルを数える(作業ツリーを変更しない)
      git show --name-only --format= <commit> -- cards | while read f; do
        diff -q <(git show "<commit>^:$f" | perl -0pe 's|<section class="cp-block"><h2>見出し</h2>.*?</section>||s') \
                <(git show "<commit>:$f"  | perl -0pe 's|<section class="cp-block"><h2>見出し</h2>.*?</section>||s') \
          >/dev/null || echo "$f"
      done
      ```

      - ⭐ **なぜ内側に入るのか**: 親コミットの生成物は**前回cron時点のAPI応答**で作られている。
        再生成に使うスナップショットはそれより新しいので、
        **その間にAPI側が動いたページと、タスクが触るページが重なった分だけ同居する**
      - ⚠️ **cronが止まっている期間は必ずこれが起きる**(時点の差が開き続けるため)
    - ✅ **`build:cards` の出力がページ単位で非決定的だった問題は解消した**
      (2026-08-26判明 → 2026-08-30実装。⚠️ **未pushなので本番にはまだ出ていない**)。
      かつては `newestFirst()` が `release_date` **だけ**で比較し**同着の第2キーを持たなかった**ため、
      公式APIが `editions` を並べ替えた日に **代表画像・`og:image`・収録セット表の行順**が
      中身と無関係に変わった(⭐ **#71 の「無関係な差分」の発生源はこれ**)。
      今は **`editionOrder()`(`build-card-pages.mjs`)の5段の全順序**で決着する:
      **①所属セットの発売日 降順 → ②`meta.sets` の並び → ③レアリティ → ④カード番号(数値考慮) → ⑤版slug**
      - ⚠️ ⭐ **第2キー以降を削ると非決定に戻る。整形目的で簡略化しないこと**
        (実測: キー⑤を潰すだけで7ファイルが非決定に戻る)。⚠️ **守っているのはコメント4行だけで
        自動検査はまだ無い**——気づける経路は「cronがノイズコミットを作り始めること」だけで、
        **API由来のドリフトと区別が付かない**(起票案 `起票案_2026-08-30_決定性の自動検査.md`)
      - ⚠️ **「同じキャッシュで2回回してバイト一致」は決定性の証明にならない**
        (入力配列が同じなら同じ結果になるのは当然)。**入力の並び順を変えても一致すること**まで見る
        (⭐ **逆順は置換の1つに過ぎないのでシャッフルも併用する**)
      - ⚠️ **ブラウザ側(`shared/js/card-i18n.js` の `firstEdition()` 等)は直っていない**。
        **同じカードの代表画像が、トップの検索タイル・詳細モーダル・デッキ構築と
        カードページとで食い違う(487枚)**(起票案 `起票案_2026-08-30_ブラウザ側の代表画像.md`)
      - 記録は `docs/design/未採番-版順序の非決定性/`
- **シーズン禁止(Seasonal Banlist・#34)は `data/seasonal-banlist.json` で手動管理する**。
  公式APIは季節禁止を持たない(対象カードの `legality` は `null`・`/banlist` 等のエンドポイントも無い)ため、
  **cronでは自動更新できない**。判定は `shared/js/card-i18n.js` の `seasonalBanState()` 1箇所で、
  ブラウザ5面(トップ・詳細モーダル・カードページ・デッキ構築のタイルと適合判定)とビルドが共用する
  - **恒久禁止(B&R)とは公式が明言する別のリスト**。`bannedFormats()` に混ぜない・合算しない
  - 更新手順: **新シーズンを `seasons` に1ブロック追記し、同時に前シーズンの `effectiveTo`
    (＝新シーズン開始日の前日)を埋める**。公式の告知は各メインセット発売の1週間前
  - ⚠️ **`effectiveTo: null` は「終了日未定＝まだ有効」。埋め忘れると禁止が永久に残る。**
    とくに**次シーズンの禁止が0枚だと追記するブロックが無く、更新の機会そのものが来ない**
    (公式は0枚もあり得ると明言)。→ **次のメインセットの発売日が判明した時点で `effectiveTo` を埋める**
    - この失敗は**安全側**である(使えるカードを「使えない」と表示する)。逆方向(禁止の見落とし)は
      大会での失格につながるため、**`null` をfail-openにしてはいけない**
  - ロールバックは **`seasons` を `[]` にしてpushするだけ**(表示・判定が全部消える。コード修正より速い)
  - ⚠️ **消費側が2026-08-09に1つ増えた**(#56)。ブラウザ5面+ビルドに加えて **`npm run dashboard` の
    運用ヘルス**が `effectiveTo: null` の件数を見る(**2件以上で警告**=前シーズンの終了日の埋め忘れ検知)。
    ⭐ これで「`effectiveTo` を埋め忘れると禁止が永久に残る」問題に**初めて検出経路ができた**
  - 詳細は `docs/design/34-シーズン禁止/`
- `node scripts/gen-tl-json.mjs` — 翻訳データを**ブラウザ用のJSONに変換**する(#22フェーズ2)。
  `data/tl/*.js`(人間が訳を書く原本)から `data/tl-names.json`(名前)と
  `data/tl-effects.json`(効果・フレーバー)を生成する。**生成物はコミットする**。オフラインで実行できる
  - **訳を追記・修正したら必ず実行する**。トップ・デッキ構築・大会詳細ページは
    `data/tl/*.js` を**実行時に読まなくなった**ため、再生成しないと**新しい訳が本番に出ない**
  - ✅ 忘れても **`npm run validate` が exit 1 で落ちる**(生成物と `data/tl/*.js` の一致を検査する)。
    「気づけない」問題にはならない(#27 の索引も #29 でcron自動再生成になり、同じ懸念は解消済み)
  - ⚠️ **`data/tl-names.json` の消費側が2026-08-09に1つ増えた**(#56)。ブラウザ/ビルドに加えて
    **`npm run dashboard`** が slug→日本語名の解決に読む。壊れると**ダッシュボードの人気カード・
    人気チャンピオンが slug のまま**になる(`(未訳)` 付きで表示され落ちはしない=fail-open)
  - ⚠️ **新しい `data/tl/*.js` を足すとき、`index.html` の編集は不要になった**(#22フェーズ2)。
    以前は `index.html` の `<script src="data/…">` の並びが読み込み順の出所だったが、
    同フェーズで `<script>` を撤去したため、**`scripts/lib/page-i18n.mjs` は
    `data/tl/` を「ファイル名の昇順」で読む**ようになった(`load-i18n.mjs` と同じ規則に統一)。
    手順は「訳ファイルを作る → `gen-tl-json.mjs` → コミット」だけ
  - ⚠️ **翻訳データを実行時に読むページは無くなった。** トップ・デッキ構築・大会詳細ページ(426件)は
    生成JSONを `fetch` する。カード個別ページ・セットページは**ビルド時に訳を埋め込み済み**
    (`/cards/<slug>/` `/sets/<set>/` は訳データを一切読み込まない)
- `node scripts/gen-card-meta-index.mjs` — 日本語検索(JPモード)の**メタ索引**(#27・#43・#45)
  `data/card-meta-index.json` を生成(引数なし・**ネットワーク必須**・生成物はコミットする)。
  `tmp/api-cache/cards-snapshot.json` があれば再利用し、スナップショット未収録のフリップ面22件は
  `/cards/:slug` で個別取得する。
  - ⚠️ **この索引はJPモードの「取得前フィルタ」だけのものではない**(2026-08-02〜)。
    **絞り込み・並び替え・総件数の3つ**がこれ1ファイルに依存している:

    | キー | 用途 | 壊れると |
    |---|---|---|
    | `m[slug][0..5]` | 取得前の絞り込み(#27) | 候補から漏れる → **取得後フィルタでは救えない** |
    | `m[slug][6]`=`[level,power,life,cost_memory]` / `[7]`=prefix別の最小レアリティ | JPモードの並び替え(#43) | 並ばない・末尾に寄る(注記は出る) |
    | `f`={裏面slug:表面slug} | 候補を表面へ畳む(#45)／**裏面一致の注記と裏面画像で開く判定**(#46) | **「全N件」が行数と合わなくなる**(注記は出る)／**「🔄 裏: ◯◯」が出なくなり、検索語がタイルのどこにも見えなくなる**(無言) |

    - ⚠️ **`m` のエントリに9要素目を足さないこと**。`card-search.js` の `sortKeyOf()` が
      **`entry.length < 8` で旧形式を判定している**ため干渉する。増やすなら**トップレベルの別キー**にする
      (`f` がその例)
    - ✅ **`npm run validate` が索引の内部整合を検査する**(#43・#45)。8要素/`entry[7]`と`entry[4]`の長さ一致/
      値域/`f` の自己参照・**連鎖**・`m` との整合/数値4項目の全滅。⚠️ **cronは `validate` を実行しない**ので、
      cronログに痕跡を残すのは**生成スクリプトの完走サマリ2行**(並び替えキーの件数・フリップ面の件数)
    - ⭐ 消費側は**すべて fail-open**(索引が古い・欠ける・旧形式でも落ちない)。
      並び替えは「不明」を末尾に回し、畳み漏れ・過剰包含・取得失敗は**取得後に数えて注記を出す**
  - ✅ **日次cronが毎日自動で再生成する**(#29・2026-07-27〜)。**禁止改定・再録による腐りは24時間以内に回復する**ため、
    手動実行が要るのは**訳を追記して即日反映したいとき**だけ(待てるなら翌朝のcronに任せてよい)
    - 索引は「候補を絞る」役で最終判断は取得後の `matchesActiveFilters` だが、
      **候補から外れたカードはそもそも取得されない**ため、腐った索引は取得後フィルタでは救えない
      (詳細は `docs/design/27-JP検索の絞り込み埋もれ/` の「保証の範囲」)。だからcronで鮮度を保つ
  - ⚠️ **出力は決定的でなければならない**(タイムスタンプを入れない・列挙値はソートしてからトークン化する)。
    中身が変わらない日に差分が出ると、cronが毎日ノイズコミットを作る
  - 個別取得に失敗したslugは**索引から除外して続行する**(スクリプトは exit 0 で完走)。
    ここで落とすと後述の `continue-on-error` によりjobが緑のまま索引だけ更新されなくなり、
    綴りミス等で恒久化すると**永久に気づけない**(#29 のレビュー指摘)
- `npm run build:tournaments` — 大会デッキ(#14)のスキャン+ページ生成。
  sitemapへ反映するには **`build:tournaments` → `build:cards` の順**に実行する
  - `node scripts/build-tournament-pages.mjs --no-scan` で**APIを叩かず既存データからページのみ再生成**できる
    (`--limit N` でスキャン件数の制限も可)。ページ生成ロジックを変えたときはこちらを使う
    - ⚠️ `--no-scan` でも `data/tournaments/index.json` の `updatedAt` は**毎回**現在時刻に書き換わる
      (`scan`・`events` は不変)。**このタイムスタンプだけの差分はコミットに含めない**
      (`git checkout -- data/tournaments/index.json` で戻す)。日次cronの自動コミットと混ざるため
  - `node scripts/build-tournament-pages.mjs --refresh` で**収録済みの全大会を取り直す**
    (`events/<id>.json` を上書き)。保存項目を増やしたときに使う。既知idだけを引き直すため
    全id再スキャン(約12,000リクエスト)より軽い
  - **`build()` は `tournaments/` を事前削除しない**。ページ構成を変えた場合は
    `rm -rf tournaments/` してから実行しないと旧構成のファイルが残留する
  - ⚠️ **大会は「チーム戦(3v3)」がある**(2026-08-14・#65〜)。個人戦とは**APIの応答形が違う**:

    | 項目 | 個人戦 | チーム戦 |
    |---|---|---|
    | イベント本体 | (なし) | **`teamSize: 3`** / `teams` を持つ＝**これが唯一の判別子** |
    | `/events/<id>/teams` | ⚠️ **400** `Not a team event.` | 200 `[{name, finalPlacement, players:[{id,slot}]}]` |
    | `/standings` の行 | `id`・`hasSubmittedDecklist` を持つ | ⚠️ **`name`(チーム名)しか無い**(`id` が**無い**) |
    | `/decklists` | — | **完全に同じ**(⭐ デッキはチーム戦でも**プレイヤーid単位**) |

    - ⚠️ **`/teams` はチーム戦のときだけ呼ぶ。** `fetchJson()` は404以外を3回リトライして**例外を投げる**ため、
      個人戦(13,962件)で呼ぶと壊れる
    - ⚠️ **`standings[].id` を前提に書くと `Player #undefined` が並ぶ**(#65 の元の症状)。
      ⭐ フェイルセーフとして **`/teams` が空・`standings` に `name` が無いときは pending に落として公開しない**
    - ⭐ **`--only <id>`**(#65) — 指定idだけを取り込む**ローカル検証専用**の引数。
      **カテゴリ許可リストだけを無視**し、上端探索・pending再チェック・`index.scan` の更新はしない。
      ⚠️ **cronからは絶対に呼ばない**(許可リストを迂回するため)
    - 実測(2026-08-14・id 46500〜60529 の全14,030件走査): チーム戦は**68件**で全て `team-standard-3v3`・`teamSize:3`。
      うち**取り込み対象カテゴリは `#59730`(ascent)の1件だけ**で、残り67件は `regular`。
      完了48件のうち**デッキリストがあるのは4件**(→ 対象拡大は **#67**)
  - ⚠️ **大会の掲載は最大およそ1日遅れる**(2026-08-14に問い合わせで判明)。cronは1日1回なので、
    **大会の完了(`dateCompleted`)がcronの直後だと、載るのは翌朝**になる
    (実例: Grand Archive Gauntlet 2026 は完了 8/14 08:48 JST・当日cronは 03:56 JST ＝**約5時間差で翌日回し**)。
    ⭐ **未確定の大会は `data/tournaments/pending.json` に積まれ毎日再チェックされる**ので、
    「載っていない」ときは**まずここを見る**(障害ではない)
  - ⚠️ **絞り込みの「選択肢」は自動、「表示名・色・並び順」は手動**(2026-08-14・#67の調査で確認)。
    大会一覧の カテゴリ / フォーマット / 開催国 / シーズン は**掲載データから機械的に選択肢を作る**
    (`[...new Set(events.map(e => e.category))]` 等)ので、新しい値の大会が1件でも載れば**選択肢は勝手に増える**。
    ⚠️ **だが表示は定数表**(`CATEGORY_JP` / `TOURNAMENT_FORMAT_JP` / `COUNTRY_JP` / `SEASONS`)で、
    **未知の値はフォールバックする**:

    | 箇所 | 未対応のまま新しい値が載ると |
    |---|---|
    | 選択肢名・列の表示 | **素の英字**のまま(例: `regular` / `team-standard-3v3`) |
    | 種別バッジ | ⚠️ `catInfo()` が **`cls:"store"`** に落ちる＝**Store Championship と同じ金色**で見分けが付かない |
    | 種別ソート | `CAT_RANK[c] ?? -1` ＝ **常に最下位** |

    ⭐ **新カテゴリ・新フォーマットを載せるときは、定数表とバッジCSSの追加まで含めて1組**で考える
    (#65 で `team-standard-3v3` のラベルを追加したのがこの実例)
  - 順位表の属性玉(#15)は `scripts/lib/element-orbs.json`(コミット済み)を読むだけ。
    生成スクリプトは**どちらも手動実行専用**で、日次cronのビルドから呼んではいけない
    (画像処理に sharp が要るため。スナップショットを読むだけの `gen-card-meta-index.mjs` は事情が違い、cronに載っている):
    - `node scripts/gen-element-orbs.mjs` — 公式カード画像を取得して32px WebPに切り出し
      `scripts/lib/element-orbs.json` を出力(**ネットワーク+sharp が必要**)
    - `node scripts/gen-element-orbs-css.mjs` — 上記JSONから `shared/css/element-orbs.css`
      を出力(#19のカード検索がブラウザから使う。**JSONを読むだけでオフライン実行できる**)
    - ⚠️ **新しい属性が増えたときは2つとも実行し、出力(JSON・CSS)を両方コミットする**
  - GitHub Actions `build-tournaments.yml` の日次cronが **毎日 03:00 JST に稼働中**(2026-07-22〜)。
    新規大会がなくても `data/tournaments/index.json` の `scan.maxId`(次回スキャン位置)が
    毎日進むため、**1日1件の自動コミットが発生するのは正常**(差分2行)
    - ⚠️ **`data/tournaments/index.json` の消費側が2026-08-09に1つ増えた**(#56)。
      **`npm run dashboard` の運用ヘルス**が `updatedAt` を読み、**48時間より古いと警告**を出す。
      ⭐ これで**cronが止まったことに気づく経路ができた**(従来はコミットが来ないことに人が気づくしかなかった)
    - ⚠️ **IndexNow通知(#38)は 2026-07-29 の導入以来一度も成功していない**(既知・調査済み)。
      ✅ ただし **2026-08-12 の #62 で「cronが赤くなる」問題だけは解消した**(既知403を警告化)。
      通知が飛ばない状態そのものは変わらず、原因の本命は `pages.dev` の構造的制約で、
      解決は **#28(独自ドメイン)の費用判断待ち**
      - ⭐ **#62 以降のcronは緑が既定**。既知403(`UserForbiddedToAccessSite`)を受けても
        **警告に留めて exit 0** し、**実行ページに黄色の注釈 `IndexNow未通知` を残す**。
        ⚠️ **これは host が `*.pages.dev` のときだけ**で、**独自ドメインへ移ると自動で解除される**
        (撤去し忘れを構造で防ぐ設計。⚠️ **簡略化して落とさないこと**)
      - ⚠️ **赤を見たら必ず中を開く**。#62 以降、**赤は「既知403以外の何か」**を意味する
        (400/422/JSONでない403/errorCode違いは従来どおり exit 1)
      - ⚠️ **緑も「通知できた」を意味しない**(2026-08-11 全数調査)。緑の日は
        **①通知対象0件でAPIを呼んでいない** か **②既知403を警告化した**(黄色注釈あり)のどちらか。
        ⭐ **区別するにはrunのログを開く**——`通知対象がありません(送信しません)` か
        `⚠ 403 UserForbiddedToAccessSite …` か
      - ⚠️ **`data/tournaments/index.json` だけが変わる日は通知対象URLが0件**(新規大会が無い日)。
        ⭐ **IndexNow関連の検証を「翌朝のcronで確認」と書くときは、
        通知対象がある日でないと経路そのものを通らない**ことに注意する(#62 C-4 で踏んだ)
      - ⭐ **IndexNow APIの実測(2026-08-11)**: **正しいキー→403 `UserForbiddedToAccessSite`** /
        **誤ったキー→202** / URLがhost外→422 `InvalidRequestParameters`。
        ⚠️ **403は「キーの誤り」の信号ではない**ので、**キーファイルが壊れても応答では永久に気づけない**
        (#38設計書の「403で気づける」という前提は誤りだった)
      - ✅ **キーファイルの健全性は送信直前に自前で検査する**(#62 §5-2・2026-08-12〜)。
        `/<KEY>.txt` を**キャッシュ回避クエリ付きで**取得し、200かつ本文が `KEY` と一致しなければ
        **送信せず exit 1**。⚠️ **クエリを付けるのは検査の取得側だけ**——
        **IndexNowへ渡す `keyLocation` に付けると本番の通知が壊れる**
        (fetch例外は fail-open で送信へ進む＝一時的な断で赤くしない)
  - **cronがコミットする範囲**(#30・2026-07-27〜): <!-- CRON-ADD:START -->
    `data/tournaments` `tournaments` `sitemap.xml` `cards` `sets` `data/card-meta-index.json`
    `data/featured-sets.json` **`index.html`**<!-- CRON-ADD:END -->
    - ⚠️ **`index.html`(トップページ)が入っている**のを忘れない(2026-08-15・#60の確認で判明。
      それまでCLAUDE.mdもREADMEも列挙から漏らしていた)。トップの静的リンクは
      `build:cards` がAPIから生成するので、**新セットが出た日はトップも自動publishされる**
    - ⭐ 正は**ワークフローの `git add` 行**(`.github/workflows/build-tournaments.yml`)。
      **列挙をここに書き写す運用なので、ワークフローを変えたらこの2箇所(CLAUDE.md・README.md)も直す**
      - ✅ **2026-08-16〜、`npm run validate` が3箇所(ワークフロー・README・CLAUDE.md)の集合一致を検査する**(#70)。
        食い違うと **exit 1**。⚠️ **cronは `validate` を実行しない**が、`git add` 行を変えるのは**常に人**なので
        検出経路として成立する(レビューの必須基準 M3 が `validate` の exit 0 を要求する)
        - ⚠️ **これで `build-tournaments.yml` の消費側が3つになった**(cron本体・README・CLAUDE.md → +`validate`)。
          ⭐ **`git add` は1行で書く**——**分割・削除すると cron は正常に動くのに `validate` が exit 1** で落ちる
          (`git add 行が1行ではありません（N行）`)。整形目的で折り返さないこと
        - 検査の仕様・**横展開するときのひな形要件**(マーカーは1組・行頭に置かない等)は
          `docs/design/70-cron列挙のドリフト防止/ドリフト防止_設計.md`
      - ⚠️ 上の列挙は**HTMLコメントのマーカーで囲ってある**(GitHubの描画では見えない)。
        **節を書き換えるときもマーカーを消さない・1組だけにする**(消しても2組目を作っても exit 1)。
        ⚠️ **マーカーを行頭に置かない**——CommonMarkのHTMLブロック(type 2)に入り、
        **その行のMarkdownが死ぬ**(実測: 同じ行のインラインコードが `<code>` にならず生のバッククォートで出た)。
        **行の途中か前行の末尾**に置く
    - 以前は **`sitemap.xml` だけ**をコミットし、カードページ2,240枚・セットページ56枚は生成して捨てていた。
      そのため**新セットが出た日に sitemap が存在しないページを指して404**になる状態だった
    - ✅ おかげで**新カードのページ・sitemapエントリ・訳の埋め込みは翌朝までに自動で揃う**
      (訳を足したあと `build:cards` を手で回さなくても、カード個別ページが追いつく)
    - ⚠️ **新セットが出た日は数百〜数千ファイルがレビューなしで自動publishされる**(ユーザー承認済みの運用)。
      戻すときは `git revert <自動コミット>`(新規追加ファイルも消えることを実地検証済み。
      手順は `docs/design/30-cron生成物の同期/` の「ロールバック手順」)
    - ⚠️ **生成ロジック(`build-card-pages.mjs`・`scripts/lib/page-i18n.mjs` 等)を変えたら、生成物も再生成してコミットする。**
      忘れると**翌朝のcronがその変更を反映した全ページを自動でpushする**(上の `--no-scan` の注意書きと同根)
    - `data/featured-sets.json` は公式API `/featured-sets` の応答のコピーで、**取得失敗時のフォールバック専用**。
      **手で編集しない**(取得できた日だけcronが更新する)。これが無いとfetch失敗時にセットページ35枚から
      ロゴと `og:image` が消え、`cards/index.html` の全セットが「その他」に潰れた状態で自動publishされる
    - 索引の再生成ステップだけは **`continue-on-error: true`**。ここでjob失敗にすると後段のコミットに到達せず、
      **その日の大会データの取り込みごと失われる**ため
    - ✅ 副産物として、**シーズン禁止(#34)の境界日にカードページが自動で切り替わる**
      (`effectiveFrom` / `effectiveTo` を跨いだ翌朝のcronが対象カードのページだけ再生成してpushする)
- `npm run db:migrate:local` — **空のローカルD1を初期化する**(`migrations/*.sql` をファイル名順に流す)
  - ⚠️ **適用済みのDBに対しては1本目の `table users already exists` で停止する。これは正常**
    (`|| exit 1` による fail-fast)。⭐ **停止を「本番DBが壊れた」と読み違えて `DROP TABLE` で
    作り直そうとしないこと**——`migrations/` は `CREATE TABLE`/`ALTER TABLE ADD COLUMN` しか無く、
    **失敗しても何も壊れない**(#60の影響度評価)。作り直すなら `.wrangler/state/v3/d1` を消す
  - ⭐ **glob ループなので、`migrations/` にファイルを足しても `package.json` の追記は要らない**(#60)
    - ⚠️ **その代わり「ファイル名順＝実行順」になった**(2026-08-15〜の新しい依存)。
      展開はシェルの**辞書順**なので、**4桁ゼロ埋めの連番(`0005_…`)を必ず守る**こと。
      `5_foo.sql` のように桁を崩すと**`0001` より前に流れて壊れる**。
      (以前は `package.json` に順番を明示列挙していたので、ファイル名は順序に関与しなかった)
  - ⭐ **ローカルD1を壊さずに「空のDBへの適用」を検証できる**(#60で確立・レビューも同手順):

    ```bash
    mkdir -p tmp/<役割>/d1
    for f in migrations/*.sql; do npx wrangler d1 execute DB --local --persist-to tmp/<役割>/d1 --file="$f" || exit 1; done
    ```

    ⭐ `--persist-to` は `--local` 併用が前提。**既存の `.wrangler/state` に一切触れない**ので、
    ログイン済みユーザーのデッキを持つローカルDBを保ったまま初期化経路を試せる。
    ⚠️ 出来上がるテーブルには **wrangler の内部表 `_cf_METADATA` が混ざる**(件数を数えるときの注意)
  - ⚠️ **`npm run db:migrate:remote` は「一括適用はできない」と案内して exit 1 するだけ**(#60・2026-08-15〜)。
    **wrangler を起動しないので本番D1には一切アクセスしない**。
    (2026-08-10〜08-15は `0001` から全部流す作りで、`0001_init.sql` に `IF NOT EXISTS` が無いため
    `table users already exists` で必ず失敗していた＝**壊れているのに使えるように見える**状態だった)
  - ⚠️ **`wrangler d1 migrations apply` は使えない**(#60で実測)。適用済み判定に使う `d1_migrations` の
    **記録が空**なので、打つと **0001 から流して同じエラーになる**。移行するには本番D1へ
    「適用済み」を1回だけ手で `INSERT` するブートストラップが要る
    → ⭐ **次にスキーマ変更(0005)が必要になった機会に、ユーザー立ち会いで移行する**
    (根拠と手順は `docs/design/60-D1マイグレーション手順の是正/` §2)
  - **本番D1への適用は単体実行する**:

    ```bash
    npx wrangler d1 execute DB --remote --file=migrations/000N_xxx.sql
    ```

    (`CLOUDFLARE_API_TOKEN`(=`~/.cloudflare-token`)と `CLOUDFLARE_ACCOUNT_ID` を渡す。
    `wrangler login` は済んでいない＝`whoami` は「not authenticated」を返すが、トークン経由で通る)
  - ⚠️ **⭐ 必ず「本番D1に適用 → push」の順**。逆順にすると、コードだけ本番に出た数分間
    **新しい列を読むクエリが `no such column` で落ちる**(#4 では `/api/decks` の `SELECT d.format`＝
    デッキ一覧が開けなくなる)。**列の追加は既存クエリを壊さない**ので、先に適用しておくのは安全
  - 適用後は `SELECT <新列>, COUNT(*) FROM <表> GROUP BY <新列>;` で**既存行が既定値に倒れたか**を確認する
- ⚠️ **デッキ構築ツールは「フォーマット」を持つ**(2026-08-10・#4〜)。
  `decks.format` = `STANDARD` | `PANTHEON` の2値(既定 `STANDARD`。**既存65デッキは全て `STANDARD`**)。
  **スタンダード決め打ちの前提でコードを足さないこと**:

  | フォーマット | ゾーン(`deck_cards.board`) | メイン同名 | 特有の要件 |
  |---|---|---|---|
  | `STANDARD` | `material` / `main` / **`side`** / `maybe` | 4枚 | サイドは15pt制 |
  | `PANTHEON` | `material` / `main` / **`pantheon`** / `maybe` | **1枚**(シングルトン) | **サイドボードなし**・Boon(Lesser/Greater)各1枚 |

  - ⭐ **判定の出所は `app.js` の `formatIssues()` 1箇所**。統計の「フォーマット適合」と
    タブ帯直上の警告バナー(`#deck-warn`)が**同じ関数を呼ぶ**。⚠️ **判定を足したら
    `warnReasons()` の理由ラベルも足す**(足し忘れてもバナーは出る＝安全側だが理由が空になる)
  - ⚠️ **「違反」と「不足」は別物**(#58/#59・2026-08-11〜)。`formatIssues()`＝**違反**(禁止・枚数制限超過)、
    `formatShortfalls()`＝**不足**(Lv0チャンピオン未設定・メイン60枚未満・パンテオンのマテリアル12枚未満・Boon未設定)。
    統計の適合行は **⚠️不適合 / 🚧構築中 / ✅使用可能 の3状態**で、⚠️と🚧は同時に出る
    - ⚠️ **`formatShortfalls()` は `warnReasons()` に足さない**。上の「判定を足したら理由ラベルも足す」規約は
      **違反(`formatIssues`)だけの話**で、不足を混ぜると**カードを1枚入れた瞬間からバナーが常時点灯する**
    - ⚠️ **マテリアルの下限はフォーマットで違う**。スタンダードは「**最大**12枚」＝**下限なし**(8枚でも合法＝`✅ 使用可能`)、
      パンテオンは「**ちょうど**12枚」。メインは**両方とも最低60枚・上限なし**(61枚以上を赤にしない)
    - ⚠️ **`FORMAT_RULES` に3つ目のフォーマットを足すときは、先に `rule.boons` が兼ねている意味を分離する**。
      現状 `boons` は「Boonを使うか」に加えて「**マテリアルがちょうど12枚か**」の判定にも流用されている
      (2件しか無く両者が一致するため今は正しい)。分離せずに足すと**片方だけ違う組み合わせで静かに間違える**
    - ⚠️ Lv0チャンピオンの判定は **`types` に `CHAMPION` を含み `level === 0`**(実データ31枚)。
      **厳密等価で書く**——`!card.level` や `Number(card.level) === 0` は **`level: null` を0と誤判定する**
      (`lu-bu-indomitable-titan` が実在。`type=CHAMPION` 検索に混ざって返るがチャンピオンではない)
  - ⚠️ **`maybe`(検討中)は常に判定対象外**。非アクティブなゾーン(パンテオンでの `side` 等)も
    **カードを消さずに残す**(掃除経路)が、統計・適合の対象からは外す
  - Boonカード47枚は `types` が `LESSER BOON` / `GREATER BOON` で**パンテオン専用**
    (`legality` の STANDARD/DRAFT が limit 0)。`isMaterialCard()` には該当しないので、
    **ゾーン制限は `isBoonCard()` で別途見ている**
  - 詳細は `docs/design/4-パンテオン構築対応/`

## 公式API(api.gatcg.com)の注意(2026-08-05〜)

- ⚠️ **数値項目でソートするとページングが壊れる**(#44)。`sort=level|power|life|cost_memory` は
  同点時の第2ソートキーが無く、**同点行がページ境界に当たると1枚が2ページに重複し、
  代わりに別の1枚が一度も返ってこない**(実測: 絞り込みなしのlevel順で124件中**4枚が欠落**/
  `type=champion`併用で125件中2枚)。**サイト側の複合指定では直せない**
  (`sort=power,name` は `collector_number` にフォールバックして無視される)
  - ⭐ **`sort=collector_number` は全2,240件で安定**(重複0・欠落0。絞り込み併用でも実測済み)。
    **数値順が要るときは安定キーで全件取ってからローカルで並べる**
    (`docs/design/44-数値ソートのページング/`)
  - ✅ **2026-08-06に実装・本番反映済み**(`7250f16c`)。`shared/js/card-search.js` の数値ソートは
    **APIに `sort=collector_number` を送って絞り込み結果を全件取り、除外・並び替え・ページングを
    すべてローカルで行う**。トップとデッキ構築の両方に効く
    - 本番ブラウザ実測: 初回は**絞り込みなしで19.0秒(45リクエスト・並列6)/ `type=champion` で1.6秒**。
      ⭐ **絞り込みを併用するほど速い**。並び替えの項目・方向の変更は**0リクエスト**(集合を絞り込みキーで保持)
    - ⚠️ **数値ソートを触るときは `type=champion` + レベル順を必ず検証に含める**(欠落の再現条件)。
      期待値は **124件**(チャンピオン125枚のうち1枚はレベルを持たず、#39 の除外規則で除かれる)
  - ⚠️ ただしこの安定性は**実測でありAPIの保証ではない**。取得後に**ユニーク数と `total_cards` を
    突き合わせる**自衛を入れること(実装済み。食い違うと件数表示に注記が出る＝**出たら異常の信号**)
- ⚠️ **単発の応答が約1.8秒と遅い。逐次リクエストを積むと簡単に数十秒になる**
  (実例: #44 以前の `probeNonNullCount` は二分探索14回を**逐次**で投げて**22.7秒**かかっていた。
  この関数は #44 で削除済み)。**並列12以上は頭打ち**(API側で律速)。
  ⭐ **待ちが数秒を超えるなら進捗表示を出す**
  - 並列度は **6を採用**(#44)。node実測では6と12の差が1.4〜4.1秒と測定ごとにぶれ、
    **ブラウザ実測があるのは6だけ**(19.0〜20.7秒)。相手は制御できない外部APIなので、
    **上げるならブラウザで実測してから**(#50)
- ⚠️ 検索応答は**1件あたり展開約10KB**で、**86%が `editions` + `result_editions`**。
  `result_editions` は `card.editions || card.result_editions` という**フォールバック専用で実際には使われない**
  (消費側5箇所すべてこの形)ため、まとめて保持するときは捨ててよい
- ⚠️ **キーワードやルールの定義を返すエンドポイントは存在しない**(2026-08-18・#73で実測:
  `/keywords` `/rules` `/glossary` はいずれも **404**)。新メカニクスの用語解説を書くときは
  **カードの効果文とリマインダー文から読み取れることしか根拠にできない**
  → ⭐ **裏の取れない仕様(カウントの範囲・上限を超えた場合の挙動など)を断定して書かないこと**
- ⚠️ ⭐ **`legality` は「発売前」と「制限なし」で形が違う**(2026-08-27に実測)。
  この3状態を知らないと、**発売日の差分を「大量解禁」と読み違える**:

  | `legality` の値 | 意味 | サイトの表示 |
  |---|---|---|
  | `null` | 制限なし(既存カードの通常状態) | なし |
  | **`{}`(空オブジェクト)** | **制限なし**(⭐ 発売済みの新カードはこちらになる) | なし |
  | `{"STANDARD":{"limit":0}}` 等 | 使用禁止 | 🚫 使用禁止：スタンダード |

  - ⚠️ ⭐ **未発売のカードは `{"STANDARD":{"limit":0}}` を返す**ため、サイトでは**「使用禁止」と表示される**。
    **発売日にAPIが `{}` へ変わり、禁止表示が一斉に外れる**
    (実例: PRD の `release_date` は 2026-08-21。**PRD系230枚**が同日に禁止表示を失った)
  - ⚠️ **これを恒久禁止(B&R)の解除と誤認しないこと。** 見分け方は
    **対象が特定セットに固まっているか**(230枚すべてPRD系だった)と、**そのセットの `release_date`**
  - ⚠️ **日次cronが動いていれば発売日に自動publishされる**ので人の目に触れない。
    **cronが止まっている期間に手で回すと、この差分がまとめて出てくる**

- ⭐ **カードは `rule` フィールドを持つ**(公式裁定・エラッタ)。形は
  `[{title, date_added, description}]` で、**2026-08-18時点で444枚・595件**
  (`title` は `ERRATA` 113件・**空 474件**・複合8件)
  - ⚠️ **`effect` はエラッタ前の印刷文のまま**。エラッタは `rule` に別途載るだけで**本文には反映されない**
    (実例: `blightheart-penitent` は「条件を満たすなら効果が起きる」→「そもそも起動できない」に
    変わっているが `effect` は旧文面)。**訳文が正確でも挙動が違う**ことがある
  - ⚠️ **表示経路が片方しかない**: 静的カードページ(`build-card-pages.mjs`)は出すが、
    **詳細モーダル(`shared/js/card-detail.js`)は取得しているのに描画していない** → **#74**
- ⭐ **フレーバーテキストの置き場は2箇所ある**(2026-08-20実測・2,494枚)。
  **トップレベル `card.flavor`(614枚)** と **版ごとの `editions[].flavor`**で、
  ⚠️ **586枚は版側にしか持たない**。`card.flavor` だけを見る実装は**そのカードで何も表示しない**
  - ⚠️ **空白1文字(`" "`)を持つカードが実在する**(`virgil-altered-future` / `zinn-volnia-abbess`)。
    truthy なので `||` では弾けず、**空の枠だけが描画される**。**trim してから判定する**
  - ⚠️ **版によって文面が違うカードが110枚ある。** `editions` の**配列順は公式API側で入れ替わる**ため
    (#71)、**「最初の非空を採る」と出力が非決定的になる**(実測: **65枚で結果が変わる**＝
    中身が変わらない日に生成物の差分が出て**cronが毎日ノイズコミットを作る**)。
    ⭐ **初出(`set.release_date` 昇順・`1970-01-01` は未設定なので末尾・同着は `edition.slug`)に固定する**
  - ✅ **取り出しは `shared/js/card-i18n.js` の `flavorOf(card, t)` 1箇所に集約済み**
    (2026-08-21実装・2026-08-26承認。⚠️ **未pushなので本番にはまだ出ていない**)。
    優先順は **和訳 > `card.flavor` > `editions[].flavor`(初出の版)**、空判定は trim 後、戻り値は `""` に統一。
    消費側は**静的カードページ・詳細モーダル・`backFace()` の3箇所**で、⭐ **新しい表示面を足すときは
    自前で `card.flavor` を読まず `flavorOf()` を呼ぶ**(片方だけ直すと「画面によって別のフレーバーが出る」
    という、より分かりにくい不具合になる)
  - ✅ **ページ全体の非決定性も解消した**(2026-08-30。⚠️ **未pushなので本番にはまだ出ていない**)。
    `build-card-pages.mjs` の `newestFirst()` は `release_date` だけで並べ**同着に第2キーが無かった**ため、
    版テーブルと **`og:image`(代表画像)が `editions` の配列順に依存していた**
    (⭐ **#71 で観測された「無関係な差分」の正体**)。今は **`editionOrder()` の5段の全順序**で決着する
    (詳細と ⚠️ **簡略化するなの警告**は「開発コマンド」の `build:cards` の節)。
    → `docs/design/未採番-版順序の非決定性/`
  - 設計・検証の記録は `docs/design/未採番-フレーバー表示経路/`

## 環境の注意

- devcontainer(node:20-bookworm)で動作。メモリー(`/home/node/.claude/`)は
  コンテナ再構築で消えるため、恒久的な運用ルールは本ファイルに書く
- ⚠️ **Claude Code の hooks はこのリポジトリでは使っていない**(`.claude/settings.json` は**存在しないのが正**)。
  2026-08-16に4セッションかけて実測済み——**同じことを再実測しないこと**。記録は
  `docs/design/70-cron列挙のドリフト防止/実測メモ_hook.md`(発火が確認できた設定の全文つき)
  - ⭕ できる: `if:` でツール名+パスを絞る / **exit 2 + stderr** と `additionalContext` はモデルに届く
  - ❌ できない: `systemMessage` はモデルに届かない / 単一 `/` の絶対パスは一致しない /
    **Bash(`sed -i` 等)経由の編集は捕まらない**
  - ⚠️ **同じ定義でも発火しない起動が実在した**＝**失敗が無言**。「フックがあるから安心」と書かない
  - ⚠️ 設定変更は**セッション再起動まで効かない**うえ、**変えたセッションでは以後フックが止まる**
    (元に戻しても復活しない)。**1回の起動で試せるのは1回分**
- ⚠️ **公式APIの効果文と語句を突き合わせるときは、必ず太字・斜体マークを除去してから比較する**
  (2026-08-18・#73で**同一セッション中に2回**踏んだ)。効果文は
  **`put a **buff** counter on that ally.`** のように**キーワードだけが太字**になるため、
  `"buff counter"` の素の文字列一致は**語間の `**` に阻まれて0件を返す**。
  ⚠️ **「既訳に無い＝新キーワード」という判定がこれで丸ごと狂う**(#73初版は `buff counter` を
  新キーワードと誤判定し、既訳126枚・183回の実績を見落とした)。日本語側でも同じ
  (`**カスケード**するたびに` は `カスケードするたびに` で検索しても出ない):

  ```js
  const clean = s => String(s || "").replace(/\*+/g, " ").replace(/\s+/g, " ").toLowerCase();
  ```

- ⚠️ **Markdownに `<!-- … -->` のマーカーを埋めるときは行頭に置かない**(#70で実際に踏んだ)。
  CommonMarkのHTMLブロック(type 2)に入り、**その行のMarkdownが死ぬ**(インラインコードが生のバッククォートで出る)。
  **行の途中か前行の末尾**に置く。⭐ マーカー文字列自体は描画に出ないままなので、
  **「マーカーが見えないこと」の確認だけでは気づけない**——`<code>` の数が導入前と一致することまで見る
- CSPが `style-src 'self'` のためHTMLのstyle属性は使えない。動的スタイルは
  `data-*` 属性+CSSOM適用で行う(既存の.meter・統計タブの棒グラフと同方式)
- ⚠️ ⭐ **トップのスマホ幅(`@media (max-width: 640px)`)には、外すと静かに壊れる対策が3つ入っている**
  (2026-08-28・未採番「スマホの絞り込み導線」。⚠️ **未pushなので本番にはまだ出ていない**)。
  **絞り込みFAB＋ボトムシートは、この3つが噛み合って初めて動く**:

  | 対策 | 外すとどうなるか |
  |---|---|
  | `html, body { overflow-x: **clip** }`(❌ `hidden` に戻さない) | スクロールコンテナ化で**子孫の `position: sticky` が死ぬ**(元の不具合) |
  | `.controls { position: **static** }`(スマホ幅のみ) | 条件バーが画面外に出ず、**FABが永久に出ない**(⭐ 実装は完成して見えるのに動かない) |
  | `.controls { backdrop-filter: **none** }`(スマホ幅のみ) | `fixed` の**包含ブロック**ができ、**シートが画面下端に来ない**(bottom が 844→-1249) |

  - ⭐ **3つとも style.css に理由コメント付きで別ブロックとして置いてある。整形目的でまとめない**
  - ⭐ 検証は「動いたか」ではなく **CSSOM で対策を実行時に外して壊れることを見る**(破壊試験)。
    「動いたから合格」では、対策が効いているのか偶然動いているのか区別できない
  - ⚠️ FABの退避量は **`--printbar-h` に印刷バーの実測高さを書く**(固定値にしない。実測 60〜79px で環境依存)
  - 詳細は `docs/design/未採番-UI刷新の検討/スマホ絞り込み導線_設計.md` §2-3(F1〜F5)
- ⚠️ **カード画像に重ねるバッジは `.badges-bl`(左下)の縦積みに入れる。座標を分けて増やさない**(#35)。
  **カード画像の上端2隅には常に情報がある**(左上=コスト・右上=属性シンボル)ため、そこに置くと絵柄が読めなくなる。
  また座標を分けると**幅次第で必ず横に重なる**(実例: 🎨 の文言に版prefixが入って幅が倍になり左下バッジと衝突)。
  実装は `shared/css/card-badges.css` の1箇所で、**トップとデッキ構築の両方から読む**
  - この失敗は #25・#26・#35 と**3回繰り返している**。新しいバッジを足すときは必ず縦積みに合流させる
  - ⚠️ 共通CSSに `<button>` を置くときは **`font-family: inherit` を明示する**。
    読み込む側に `button { font: inherit }` があるかどうかで見た目が変わるため(デッキ構築にはあり・トップには無い)
  - ⚠️ **`card-badges.css` の中身は「全部が画像の上」ではない。** `.flip-hit`(#46・裏面一致の注記)は
    **カード本文に出る行**で、両画面に出るという理由だけで同居している。バッジと混同しないこと
  - ⚠️ **共用CSSに本文側の要素を足すときは、両画面で「横paddingの出どころ」を確かめる**(#46)。
    トップは**親**の `.card-body { padding: 10px 12px 14px }` が効くが、デッキ構築の `.result` は
    横paddingを持たず **`.rname` が自前で持つ**。共用ルールを `padding: 0` で書くと
    **デッキ構築だけ8pxはみ出す**(`tools/deck-builder/style.css` の `.result .flip-hit { padding: 0 8px }` で解決済み)
    - ⭐ 検証は**「下の要素と重ならないこと」では不足**。はみ出していても重ならないので通ってしまう。
      **近接要素と左端が揃うこと**を、要素の矩形ではなく `Range.getBoundingClientRect()`(テキストの実描画位置)で測る
- **NortonのHTTPS検査があるためヘッドレスブラウザは証明書エラーになる**
  (api.gatcg.com へのfetchが「Failed to fetch」で空画面になる)。Playwrightは
  `launch({ args: ["--ignore-certificate-errors"] })` + context `ignoreHTTPSErrors: true` で回避
- ⭐ **Playwrightを使う前に `npm run setup:env` を1回打つ**(#9・2026-08-09〜)。
  npmパッケージ・Chromium本体・**Chromiumが要求する共有ライブラリ14個**をまとめて用意する。
  導入済みなら**1秒未満でスキップ**するので、毎回打ってよい
  - ⚠️ **旧記述「Chromium本体は再構築でも残る」は誤りだった**(2026-08-08の再構築で実測)。
    `~/.cache/ms-playwright` は **overlayfs上なのでコンテナ再構築で消える**
  - ⚠️ **`npm install` を打つと Playwright は毎回消える。** `package.json` に入れていない
    (本番Pagesビルドで Chromium一式294MBのDLが走るリスクを避けるため)ので、
    **extraneous として prune される**。消えたら `npm run setup:env` で戻す
  - ⚠️ 共有ライブラリが欠けると **`libnspr4.so: cannot open shared object file` で `launch` が落ちる**。
    `setup.sh` は `ldd` で不足を数えており、**不足があるとスキップせず自動で埋める**。
    それでも残る場合だけ診断表示が `npx playwright install-deps chromium` を案内する
  - 環境全体の復旧手順・何が消えて何が残るかは **`docs/dev-setup.md`** に集約(#9)
- **デッキ構築ツールはDiscord OAuthログインが前提**のため、ヘッドレスでは検索パネルまで到達できない
  (`#boot-status` が「読み込み中…」のまま)。⭐ **検証手順(#51で確立・以後これを標準とする)**:
  `#view-editor` の `hidden` を外し、**空のデッキ状態を用意してから**ページ本来の `searchCtl` と
  イベントハンドラをそのまま動かす(検索経路はトップと同一モジュール)
  - ⚠️ **空のデッキ状態の用意を飛ばすと `deckData.cards` が null で落ちる**
    (`検索に失敗しました(Cannot read properties of null (reading 'cards'))`)
  - ⚠️ **この手順で担保できるのは「デッキの中身に依存しない描画」まで。**
    適合判定バッジ・所持枚数などデッキ内容を読む表示は**未検証として報告する**
    (合成した状態で「通った」と書くと、実ログイン時の不具合を見落とす)
- 宣伝画像などの生成に使う `python3-pil`・`fonts-noto-cjk` はaptパッケージのため**コンテナ再構築で消える**
  (`sudo apt-get install -y python3-pil fonts-noto-cjk` で再導入)
- **Cloudflare Pagesは `/foo.html` を `/foo` へ308リダイレクトする**(拡張子トリム)。
  `_headers` のパスに `.html` だけを書くと、**ヘッダはリダイレクト応答にしか付かず本文には効かない**。
  拡張子あり・なしの**両方を登録する**こと(実例: `_headers` の `decks.html` / `decks`)。
  この挙動は `npm run dev` では再現しないため、**リリース後に `curl -sI` で実物を確認する**
  (`_headers` 自体もローカルサーバーは解釈しない)
  - ⚠️ **この308は「本番反映の確認」の測定側も壊す**(2026-08-10・#4のpushで実際に踏んだ)。
    `curl -s https://…/tools/deck-builder/index.html | grep -c '…'` は**本文が0バイト**で返るため
    **必ず0件**になり、「反映されていない」と誤読する(実測: 素のcurl **0バイト** / `-L` 付き **22,073バイト**)。
    ⭐ **本文をgrepして反映を確かめるときは、末尾スラッシュのURL(`/tools/deck-builder/`)を使うか `-L` を付ける。**
    ヘッダだけ見る `curl -sI` では気づけない(308も200も「応答はある」ため)
- ⚠️ **CSPは「HTMLのmeta」と「`_headers`」の2箇所にあり、両方を直さないと効かない**(2026-08-09・#56)。
  Pagesは**一致した全ルールのCSPを重複して送り、ブラウザは積集合で評価する**ため、
  **片方が緩んでも、もう片方が拒否すればブロックされる**
  (実例: #56 で `tools/print/index.html` の meta だけ緩め、`_headers` の `/tools/print/*` を
  据え置いた → beacon.min.js が `requestfailed` reason `csp`・**0バイト**でブロックされた)
  - ⭐ **外部オリジンを1つ許可するときは、`grep -n "<対象パス>" _headers` を必ず打つ**。
    そのパス専用のルールがあれば、そこにも同じ変更が要る
  - ⚠️ **`connect-src` を書き忘れない。** 未指定だと `default-src 'self'` にフォールバックするので、
    `script-src` だけ直しても**スクリプトは読めるが送信先が塞がったまま**になる
  - ⚠️ **検証は `wrangler pages dev` で行う。`npm run dev` は `_headers` を解釈しないので、
    直っていなくても合格に見える**(#56 で実際にこれで1往復差し戻した)
- **存在しないパスは `404.html` が404で返る**(2026-07-23〜・#23)。それ以前は `404.html` が無く、
  Pagesが `index.html` を**200で**返すフォールバックだったため、**入れ子パス**(`/cards/xxx/` 等)では
  CSS/JSの**相対参照が解決できず画面が壊れていた**(soft 404)。`404.html` は**JSを読み込まず**、
  リンク・CSSは**絶対パスで書く**(任意のパスで配信されるため相対パスだと壊れる)。
  ⚠️ **この挙動も `npm run dev` では再現しない**ので、変更時は本番で `curl -sI` を確認する
- `_headers` の `/shared/vendor/*` は `max-age=31536000, immutable`(#22)。
  **ベンダーライブラリを差し替えるときは必ずファイル名を変える**(そのままだと更新が届かない)
- ⚠️ **ファイルを削除しても、素のURLは最大7日間200を返し続ける**
  (2026-08-04・#10 → **2026-08-13・#64 で再検証し追認**)。削除済みファイルの応答は
  `cache-control: public, s-maxage=604800` で、**デプロイではエッジキャッシュが無効化されない**。
  #64 の実測では削除後30分(20秒×90回)すべて 200 / `cf-cache-status: HIT` / `age` が単調増加
  (**404の観測回数 0/90**。#10 の 0/24 を再現)
  - ⭐ **応答ヘッダは「現存」と「削除済み」で別物になる。これを混同したのが #64 の発端**
    (2026-08-13・**3セッションが独立に**同一エッジで実測):

    | 測る対象 | `cache-control` | `cf-cache-status` / `age` |
    |---|---|---|
    | **現存**するファイル(`robots.txt`・`sitemap.xml`・カードページ・共有CSS等) | `public, max-age=0, must-revalidate` | **付かない** |
    | **削除済み**のファイル(エッジに残ったコピー) | **`public, s-maxage=604800`** | **HIT** / `age` あり |

    - ⚠️ **現存ファイルのヘッダを見て「キャッシュ挙動が変わった」と判断してはいけない。**
      2026-08-12 に6パスを測って `max-age=0, must-revalidate` しか出なかったのは
      **測った6つが全て現存ファイルだったから**で、#10 の記録と**矛盾していなかった**
      (⭐ ヘッダだけでは削除時の挙動を判定できない＝**実地に消して測るしかない**)
    - ⚠️ **`age` はpush時刻より前を指す**(残っているのは**削除前に**キャッシュされたコピーのため)。
      #64 では `age` の逆算が**削除pushの46分前**＝そのファイルが最初に200になった瞬間を指した
    - ⭐ **「削除しても残るか」を検証するなら、削除前に素のURLへアクセスしてキャッシュに載せる。**
      一度もアクセスされていないファイルはエッジに載っていないので、消せば404になって当然で、
      **再現にならない**(この条件を欠くと「404だった＝挙動が変わった」と誤結論する)
  - ⭐ **更新と削除で「正しい確認方法」が逆になる**。更新は素のURLが正(エッジが取り直すので
    待てば揃う)だが、**削除はキャッシュ回避クエリ(`?cb=…`)が正**。素のURLは「まだ古いコピーが
    生きている」ことしか言わない
    - ⚠️ **`?cb=` は「常に最新が見える」という意味ではない**(2026-08-14・#65のpushで実測)。
      **素のURLが36秒後に新版になったのに、`?cb=` 付きは76秒後まで旧版**を返した(**40秒間、逆転**)。
      ⭐ **更新の確認で `?cb=` を単独の根拠にしない**——この40秒だけを見ると「未反映」と誤読する。
      **両方を並べて、全数一致するまで反復する**(削除確認で `?cb=` が正、という上の規則は変わらない)
  - ⭐ **新規追加は最も確実な到達マーカー**(2026-08-08・#53/#54実測)。新規URLにはエッジの
    古いコピーが存在しないため、**素のURLの 404→200 でデプロイ到達を即確定できる**。
    コミットに新規ファイルが含まれるなら、更新ファイルの新旧入れ替わりを待つよりこちらを先に見る
    - ⚠️ **新規ファイルが1つも無いコミットではこの手が使えない**(2026-08-11・#58/#59・
      2026-08-15・#60/#69で該当)。その場合は**反復測定しかない**。
      実測では **20秒間隔×18回(約6分)が全数一致**で確定できた。
      ⭐ **旧識別子が消えたこと**(`grep -c MAIN_MAX` が0)も併せて見ると、部分反映と区別しやすい
      - ⭐ #60/#69 は**約2分・計8回**で確定した(**1回目だけ旧版、以降7回一致**)。
        ドキュメント/設定だけの小さなコミットなら**6分もかからない**
  - ⚠️ **更新の伝播はpush直後、リクエストごとに新旧が入れ替わる**(#51・#52・#53/#54・
    **#60/#69**で4回連続実測。5回中2回旧版のような混在が数分続く)。
    **push直後の1回の測定を結論にせず、2〜3分after再測定して全数一致を確認する**
    - ⚠️ 2026-08-15(#60/#69)は**まさに1回目だけが旧版**だった。
      ⭐ **「1回測って旧版」は未反映の証拠にならない**——この1回で判断すると誤報になる
  - **削除が成功したかの判定**: ① `?cb=` 付きで **404** ② **同じコミットの別の変更**が
    素のURLで反映済み(＝デプロイ自体は届いている) — この2つが揃えば成功と確定してよい
  - ⚠️ **検証項目に「削除したURLが404になること」を書くときは、期待値を
    「キャッシュ回避で404・素のURLは最大7日残る」と書く**。「push後に404」と書くと
    実際の反映時間と桁が違い(8分 vs 7日)、達成できない項目になる
  - `*.pages.dev` はユーザーのゾーンではないため、**ダッシュボードからのキャッシュパージは
    使えない可能性が高い**(カスタムドメイン未設定。未検証)。基本は**待ち**
