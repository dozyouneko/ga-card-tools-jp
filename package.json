{
  "name": "ga-card-tools-jp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Grand Archive TCG 日本語カードDB（非公式）— 静的サイト＋翻訳ツールチェーン",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "scaffold": "node scripts/scaffold.mjs",
    "validate": "node scripts/validate.mjs",
    "pages:dev": "wrangler pages dev . --local",
    "db:migrate:local": "wrangler d1 execute DB --local --file=migrations/0001_init.sql && wrangler d1 execute DB --local --file=migrations/0002_deck_card_art.sql && wrangler d1 execute DB --local --file=migrations/0003_deck_thumb.sql",
    "db:migrate:remote": "wrangler d1 execute DB --remote --file=migrations/0001_init.sql && wrangler d1 execute DB --remote --file=migrations/0002_deck_card_art.sql && wrangler d1 execute DB --remote --file=migrations/0003_deck_thumb.sql"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "wrangler": "4.86.0"
  }
}
