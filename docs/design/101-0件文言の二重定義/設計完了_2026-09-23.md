**対象: 「0件文言の二重定義」**

# 設計完了 — #101 検索結果0件の文言の二重定義（2026-09-23）

- 設計書: **`docs/design/101-0件文言の二重定義/0件文言の二重定義_設計.md`**（1.0）
- issue: **#101**（`enhancement` / `小タスク` / `着手可` → **`実装待ち`**）

## 1. ⭐ 起票案の主張を実測し直した（**4点のうち2点は前提が失効していた**）

| 起票案 §3 | 実測（2026-09-23） |
|---|---|
| #1 戻り値の形が違う（難所） | ⚠️ ⭐ **実質ゼロ**。トップの `el.loadMore.hidden` は**全経路で `!info.hasMore` と一致**する（`element-and` も `hasMore: false` を返す）。デッキ構築はすでに `!tab.hasMore`＝**等価** |
| #2 注記の語が違う | ✅ **健在**。⭐ **いま画面で食い違っているのはこの1語だけ**（トップ `（日本語テキスト一致・翻訳済みのみ）` / デッキ構築 `（日本語一致・翻訳済みのみ）`。ブラウザで両画面実測） |
| #3 デッキ構築にレアリティ絞り込みが無い | ❌ **失効**。**チップ9個（値 1〜9）が存在する** |
| #3 ページサイズ 40 vs 24 | ✅ 健在。⚠️ ⭐ **ただし手当ては不要**——`jpChecked` は `card-search.js` が `opts.jpPageSize` から計算しており**自動で追従する**（実測: トップ 40→80→120 / デッキ構築 24→48→72） |
| #4 #43 の評価順 | ✅ **健在**。⭐ **順序が効いていることを示すペアを実測で確定**（`&sort=level` の有無で文言が入れ替わる） |

- ⭐ **0件の分岐は 6 ではなく 7**（終端5＋途中2）。`element-and` の早期returnを入れて**8経路**
- ⭐ 由来の設計書 §7 の実測値（候補 **141**件 / `?q=アリス` **8**件 / 1ページ **40**件）は**今日も再現した**

## 2. 対象範囲

- **変更するファイル4つ**: `shared/js/card-search.js` / `app.js` / `tools/deck-builder/app.js` / `scripts/validate.mjs`
- ⚠️ **生成物は1つも変わらない**（`build:cards` は回さない。出たらAPI由来のドリフトなので混ぜない）

## 3. 判断済み事項（⭐ **未決事項ゼロ**）

| # | 決定 |
|---|---|
| **D1** | 共有先は **`shared/js/card-search.js`**（新規モジュールを作らない＝`<script>` も CSP も触らない） |
| **D2** | 戻り値は **`{ text, showLoadMore }`**。引数は `(info, shown)` で **`shown` は呼び出し側が数える**（共有側は DOM を触らない）。⚠️ **観測上 no-op** だが、将来「文言は出すが『もっと見る』は隠す」分岐を足す穴を塞ぐため2値にする |
| **D3** | 共通化の範囲は**関数まるごと**（0件7分岐＋件数行＋`approxTotal` 2分岐＋`element-and`）。⭐ `approxTotal` も**含める**——両画面で既に同一で、除くと件数行が半端に割れて**いま塞ぐ穴が残る**。`numericSortNote` / `jpSortNote` / `jpDropNote` / `fetchGapNote` / `numericSortLabel` / `ELEMENT_AND_MESSAGE` は**据え置き**（共有関数から直接呼ぶ・`?.` は付けない） |
| **D4** | ⚠️ 呼び出し側は **`?.` ＋ 1行のフォールバック**（push直後に「新しい `app.js` × 古い `card-search.js`」が組み合わさるため。⚠️ 付けないと `TypeError` で件数欄が「検索中…」のまま固まる）。⚠️ **フォールバックに分岐を書かない** |
| **D5** | ⭐ 注記は **「日本語テキスト一致」に統一**（ユーザー決定）。**変わるのはデッキ構築側の1語だけ・トップは無変更** |
| **D6** | デッキ構築の `tab.hasMore` は **`st.showLoadMore` から入れる**（書き込み1・読み出し2で用途はどちらも `el.resultMore.hidden`） |
| **D7** | ⭐ **`npm run validate` に検査を1本足す**（→ §6）。マーカー `SEARCH-STATUS:START` / `:END` で囲んだブロックの**10文字以上の静的断片**（実測 **11個**）が `app.js` / `tools/deck-builder/app.js` に**現れないこと**。**0組/2組以上/逆順・断片9個未満で exit 1**（fail-closed）。成功行 `search status text is single-sourced — 11断片 / 2ファイル走査` |

## 4. 検証項目の要約（詳細は設計書 §7）

| 群 | 内容 |
|---|---|
| **V1〜V5** | ⭐ **0件の全分岐で、トップとデッキ構築の文字列を `===` で突き合わせる**（目視で「同じに見える」では不足）。エレメントANDも含む |
| **V6・V7** | ⭐ `jpChecked` が**ページサイズに追従**（トップ **40→80→120** / デッキ構築 **24→48**）。⚠️ デッキ構築で 40 が出たら共有側に固定してしまっている |
| **V8・V9** | ⭐ **#43 が絞り込み分岐より先に評価される**（`/?q=アリス&set=PRD&sort=level` で #43 の文言／`&sort` 無しで絞り込みの文言） |
| **V10a〜V10h** | 0件でないときの表示を**変えていない**こと。⭐ M1-b の観点（**件数表示・共有URL(#20)・絵柄の追従(#41)・バッジ集計**）を含む。⚠️ **V10c だけが本タスクで変わる唯一の画面上の差** |
| **V11〜V15** | ⭐ **破壊試験7ケース**。V11 は**共有側の1文言を壊すと両画面が同時に変わる**こと（⚠️ **片方だけ変わったら未達**）、V12〜V15 は**検査が本当に効く**こと。⚠️ ⭐ **復旧に `git checkout -- <path>` を使わない**（未追跡ファイルが戻らず破壊が累積する）——**原本をスクラッチへ退避し `cp` で戻し、各ケース後に `git status --short` が空**であることまで報告する |
| **V16〜V19** | `npm run validate` **exit 0**／`grep` で**文言が1ファイルにしか無い**こと（⚠️ 変更前は6行 → 変更後は `shared/js/card-search.js` の3行だけ）／旧い語 `（日本語一致・翻訳済みのみ）` が **0行**／⚠️ `LINEREF` に当たる書き方をしない |

⭐ **デッキ構築ツールの検証はログイン不要でできる**（設計書 §5）。
`page.route()` で `/api/me` と `/api/decks/1` を差し替えて `#edit/1` を開くだけ
（⭐ **本設計の実測は全部この手順で取った**。空のデッキ状態も応答でまかなえるので `deckData` が `null` で落ちない）。

## 5. ⚠️ この工程で担保できないもの

- ⭐ **レビュー合格後に「ユーザーが 3000番のローカルサーバーで実物を操作する工程（②）」が入る**。
  見てもらうことは設計書 §8 に **U1〜U6 の番号つき**で書いた
  - ⚠️ ⭐ **U6（スマホ幅で注記が4文字長くなる）で窮屈だと感じたら、そこで止めて設計に戻す**（`（→ 継続）`）。
    **語を戻すか短縮するかは仕様の判断なので実装側で決めない**
- ⚠️ **サーバーは 3000番で上げる**（`forwardPorts` が `[3000]` の1つだけ。3100 で渡すと**コンテナ内からは 200 に見えるのにユーザーには何も映らない**）

## 6. 未決事項

⭐ **なし。** 設計書 §3 の D1〜D7 ですべて確定している。
⚠️ **判断が要ることが出てきたら実装側で決めず、issueに書いて設計担当の回答を待つこと。**
