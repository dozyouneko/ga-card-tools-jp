# リードセッション構想（design/dev/review を1つの窓口から回す）

**状態: 検討のみ・未着手。** 実装は GitHub アカウント復旧後に判断する。

| 版 | 日付 | 内容 |
| --- | --- | --- |
| 初版 | 2026-08-19 | ユーザーとの検討結果を記録（実装は保留） |

## 1. 背景と目的

現行は **ターミナルを2つ**使い分けている:

- `cc-design`（設計セッション）… ユーザーが要件を相談し、設計書を書かせる
- `cc-sprint`（スプリント進行役）… dev / review をサブエージェントとして交互に呼び、実装〜レビューを回す

これを **「ユーザーが会話するのはリードセッション1つだけ」** にまとめたい、というのが本構想。
リードが `design` / `dev` / `review` の3サブエージェントを直接呼び、
`設計 → 開発 → レビュー` のループを1つの窓口で回す。

```text
ユーザー ── リードセッション（唯一の窓口・進行と監視のみ）
                ├─ Agent(design)  … 要件を設計書に落とす → 実装待ち
                ├─ Agent(dev)     … 実装・コミット・報告  → レビュー待ち
                ├─ Agent(review)  … 操作して合否判定      → 設計確認待ち / 実装待ち
                └─ Agent(design)  … 最終確認             → 承認はユーザー
```

## 2. 実現性

**技術的には可能で、3工程のうち2つ（dev↔review）は `.claude/agents/sprint.md` に実装済み。**
追加で必要なのは「設計のサブエージェント化」と「窓口の一本化」の2点だけ。

## 3. 決まったこと

| # | 決定 | 理由 |
| --- | --- | --- |
| D1 | `sprint.md` を拡張せず **`.claude/agents/lead.md` を新設**する。`.claude/aliases.sh` に `cc-lead` を追加し、既存の `cc-design` / `cc-sprint` は残す | sprint は「実装〜レビュー専用」という単機能が明確で、設計を混ぜると責務が濁る |
| D2 | ⚠️ **入れ子にしない。** lead が design/dev/review を**直接**呼ぶ1段構成にする | `lead → sprint → dev` の2段にすると、ストップ条件がユーザーまで伝わらず「止まったつもりが止まっていない」状態になる |
| D3 | 要件ヒアリングは**リードがユーザーと直接**行い、確定後に design へブリーフとして渡す。design にやらせるのは設計書の執筆（API調査・検証項目・モックHTML）だけ | サブエージェントは呼び出し中に質問できず、対話が「1往復＝1回の呼び出し」に分断されるため |

## 4. 未決

- **最終承認をユーザーが行うか、design サブエージェントに任せるか**
  → ⭐ **ユーザーが行う案を推奨**。design に承認させると起票者と承認者が同じになり、
  レビューの独立性と「`設計確認待ち` に溜めて人が見る」という安全弁が消える

## 5. 設計をループに入れるときの注意（開発↔レビューとは難易度が違う）

1. **設計は本質的に対話工程** — dev/review は「issueを読んで作業して報告」で完結するが、
   設計は途中で仕様判断が必ず出る → §3 D3 で回避する
2. **起票者＝承認者になる** — §4 のとおり承認はユーザーに残す
3. **リードのコンテキストが溜まる** — 記録の正は issueコメントと `docs/design/` に置いたまま、
   リードは「どのissueがどのラベルか」だけを持つ。サブエージェントの戻り値を要約して次に渡さない
   （思い込みの混入防止という現行ルールがそのまま効く）

## 6. 着手条件（⚠️ 前提が1つ壊れている）

⚠️ **GitHub アカウントが停止中のため、今は回せない。**

```text
$ gh issue list
HTTP 403: Sorry. Your account was suspended
```

現行の役割分担は **GitHub Issues のラベルとコメントが唯一のハンドオフ媒体**で、
停止中はサブエージェントが工程完了を記録できず、受け渡しが成立しない。
（2026-08-19 に GitHub Support へ用途説明を返信済み・審査待ち）

- **暫定案（採らない）**: ハンドオフを `docs/design/<issue番号>-<タスク名>/handoff.md` に
  置き換える（ラベル＝状態行、コメント＝追記）。`docs/` は本番配信外でpushも承認不要なので運用は回るが、
  **issueに戻すときの移行コストが発生する**ため見送る
- **復旧までの過ごし方**: `cc-design` 単独で設計書を書き溜める期間に充てる

## 7. 実装するときの差分（`sprint.md` に対して）

- `Agent(subagent_type: "design")` の呼び出しを2箇所追加（起票時・最終確認時）
- ストップ条件に「**要件が曖昧なときはサブエージェントを呼ばずユーザーに聞く**」を追加
- 「**最終承認はユーザーが行う**」を明記（`push待ち` ラベルの扱いは現行どおり）
- 直列実行（`run_in_background: false` で1つずつ）は現行どおり — 作業ツリーが1つしかないため変えられない
