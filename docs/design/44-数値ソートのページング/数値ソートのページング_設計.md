# 数値ソートのページング根治 — 設計書（issue #44）

## 改版履歴

| 版 | 日付 | 内容 | 状態 |
|---|---|---|---|
| 1.0 | 2026-08-05 | 初版。案F（安定キーで全件取得→ローカル整列）を確定 | 実装完了 |
| 1.1 | 2026-08-06 | 実装・レビュー完了を受けて**設計書側の誤りを訂正**（§7 V1・V2 の期待値）。実装時の確認事項5件に回答（§3.3 キャッシュ上限・§5 注記文言・§9 並列度とメモリ）| **承認済み（push待ち）** |

---

## 1. 直す症状

数値項目（`level` / `power` / `life` / `cost_memory`）で並び替えると、**同点行がページ境界に当たったときに
1枚が2ページに重複し、代わりに別の1枚が一度も返ってこない**。

利用者から見える形: 「全 124 件」と表示しながら**120枚しか出ない**。**4枚のカードが検索から永久に見えない**。

⚠️ **#39 が持ち込んだ不具合ではない**（素の昇順ページングの性質）。詳細は issue #44 本文。

---

## 2. 調査で確定した事実（すべて 2026-08-05 の実測）

### 2.1 バグは今も再現する

`sort=level&order=ASC` で `page_size=50` → 重複4種、`page_size=24` → 重複2種。**起票時と同じslug**。

### 2.2 ⭐ 安定した全順序のページングキーが存在する

| クエリ | `sort=collector_number` | `sort=level` |
|---|---|---|
| 絞り込みなし（2,240件） | ✅ **2240/2240**（45リクエストで捲り切り） | ⚠️ 重複4種 |
| `element=WIND`（294件） | ✅ 294/294 | ✅ 294/294（境界に当たらず） |
| `type=champion`（125件） | ✅ 125/125 | ⚠️ **125取得/123ユニーク＝2枚欠落** |
| `class=warrior`（299件） | ✅ 299/299 | ✅ 299/299 |

→ **崩れるのは数値項目でソートしたときだけ。** 安定キーで捲れば完全な候補集合が取れる。

### 2.3 ⭐⭐ 現行の実装は「すでに」非常に遅い

`probeNonNullCount()`（非nullの境界を二分探索する仕掛け）は**逐次14リクエスト**で、**実測22.7秒**かかる。

```
単発の応答時間        1.79秒（page_size=50）/ 1.59秒（page_size=1）
現行の二分探索        14リクエスト逐次 = 22.7秒   ← 数値ソートの初回はここで待たされている
全件取得（並列6）     45リクエスト     = 20.7秒
全件取得（並列12）    45リクエスト     = 16.6秒   ← 並列12以上は頭打ち（API側で律速）
```

⭐ **つまり「全件取得」は現行より遅くない。むしろ速い。**

### 2.4 応答は軽い / ただしメモリは重い

- 50件で **32KB(gzip)** ・全2,240件で **1.4MB(gzip)**
- ⚠️ ただし**展開後は50件で496KB＝全件で21.7MB**。内訳は **`editions` 43% + `result_editions` 43%**
- ✅ `result_editions` は `card.editions || card.result_editions` という**フォールバック専用**で、
  検索応答には `editions` が必ず入るため**使われない**（`card-i18n.js` / `card-detail.js` / `card-search.js` の5箇所すべてこの形）
  → **取得時に捨てて良い。これだけで43%減る**
- ⚠️ **`editions` は捨てられない**。`cardImages()` が全版を回し、`flipEdition()` が
  `configuration === "flip"` を全版から探し、エキスパンション絞り込みが全版の `set.prefix` を見る

---

## 3. 対処: 案F — 安定キーで全件取得 → ローカルで整列

### 3.1 やること

数値ソートのときだけ、取得と並び替えを次に置き換える:

1. `sort=collector_number&order=ASC&page_size=50` で、**絞り込み結果の全ページ**を取得する
   （1ページ目の `total_cards` からページ数を決め、2ページ目以降は**並列6**で取る）
2. 取得時に各カードから **`result_editions` を捨てる**（§2.4）
3. `isNullish(field, card)` で**その項目を持たないカードを除外**する（#39 の既存規則をそのまま使う）
4. **全順序**で整列する（キー比較 → 同点は `slug`。昇順・降順とも）
5. ページングは**ローカルの `slice`**

### 3.2 ⭐ メタ索引は使わない

当初は `data/card-meta-index.json` の `m[slug][6]` を使う案だったが、**不要**と判明した。
**取得したカードオブジェクト自身が `level` / `power` / `life` / `cost_memory` を持っている**ため。

これにより:

- ✅ **索引の消費側が増えない**（CLAUDE.md の「壊れたときの症状」の表を**更新しなくてよい**）
- ✅ 索引の鮮度に依存しない。**自分が取ったデータだけで完結する**
- ✅ 検算が同じ応答の中で閉じる（ユニーク数 vs `total_cards`）

### 3.3 ⭐ 絞り込みキーでキャッシュする → 並び替えの変更が無料になる

取得した集合は **`filterParams()` の文字列をキーに保持**する（現行の `nCache` と同じキー）。
並び替えの項目・方向を変えても**再取得は起きない**（ローカルで並べ直すだけ）。

現行は並び替えを変えるたびに `countNonNull()` のキャッシュミスで**22.7秒の二分探索が再発**しうる。

### 3.4 消えるコード

| 消すもの | 行数の目安 | 理由 |
|---|---|---|
| `probeNonNullCount()` | 約30行 | 件数Nは取得した集合を数えれば分かる |
| `countNonNull()` / `nCache` | 約15行 | 同上 |
| `fetchNumericPage()` | 約35行 | **降順の位置演算（ASCページを逆から読む）がまるごと不要**。降順はローカルの比較で済む |

差し引き **約80行減って約40行増える**。

---

## 4. リクエスト数と体感（実測ベース）

| 場面 | 現行 | 案F |
|---|---|---|
| 初回・絞り込みなし | probe 14逐次 **22.7秒** + 1ページ | 45並列 **16.6秒** / 1.4MB |
| 初回・`element=WIND`（294件） | probe 14逐次 22.7秒 + 1ページ | 6リクエスト **約3秒** / 0.19MB |
| 2ページ目以降（もっと見る） | 毎回1リクエスト 約1.8秒 | **0リクエスト（即時）** |
| 並び替えの項目・方向の変更 | 最悪 22.7秒（キャッシュミス時） | **0リクエスト（即時）** |
| 正しさ | ⚠️ 欠落あり | ✅ 完全 |

⭐ **絞り込みを併用するほど速くなる。** 絞り込みなしの初回だけが最も重く、それでも現行より速い。

### ⚠️ 16.6秒は「速くなった」だけで「速い」わけではない

**進捗表示を必ず入れる**（現行の `onStart` の「検索中…」だけでは無言で16秒待つことになる）。
`読み込み中 12/45` のように**取得済みページ数を出す**こと。

---

## 5. 自衛（fail-open）

⚠️ **`collector_number` の安定性は実測であって、APIが保証しているものではない。**

- 全件取得後に**ユニーク数と `total_cards` を突き合わせる**。食い違ったら
  **注記を出して続行する**（黙って落とさない・エラーにもしない）
- 取得したページ数が想定より多い場合の**暴走止め**を入れる（`MAX_PAGES = 60`。2,240件÷50=45が上限のため）
- 途中のリクエストが失敗したら**その検索全体を失敗させる**（部分的な集合で並べると
  「順位は正しいが歯抜け」という**最も気づきにくい壊れ方**になるため。⚠️ ここだけは fail-fast）

---

## 6. 副作用として直るもの・変わる表示

- ✅ 「全 124 件（レベルを持つカードのみ）」が**実際に124枚出る**ようになる（現在は120枚）
- ✅ **AND絞り込みの総件数が正確になる。** 現行はページ内で `matchesAndFilters` を後から
  間引くため件数が概数（`approxTotal`）だが、案Fは全件に対して間引ける
  → **数値ソート時は `approxTotal: false` にしてよい**
  ⚠️ 非数値ソートの挙動は**変えない**（本issueの範囲外）
- ⚠️ 「もっと見る」で**ページごとの表示件数が揃う**ようになる（現行はAND間引きで不揃い）

---

## 7. 検証項目

期待値は**画面に出る最終形**で書く。

| # | 項目 | 期待値 |
|---|---|---|
| V1 | 欠落の解消 | トップで**レベル降順**を選び最後まで「もっと見る」→ **「124 件を表示（レベルを持つカードのみ）」**（めくっている途中は `50 件を表示 / 全 124 件（レベルを持つカードのみ）`）。現行は120件 |
| V2 | 再現条件そのもの | **`type=champion` + レベル順** で、**124件**が出る（現行は123件）。⚠️ この組み合わせを必ず含める |
| V3 | 昇順と降順の対称性 | レベル昇順の全件と降順の全件が、**集合として一致**し、**降順＝昇順の完全な逆順**であること |
| V4 | 同点の決定性 | 同じ検索を2回実行し、**同点カードの並びまで一致**する（同点は slug 昇順／降順で反転） |
| V5 | 4項目すべて | `level` / `power` / `life` / `cost_memory` × 昇順・降順の**8通り**で V1・V3 を確認 |
| V6 | 2画面 | **トップ（PAGE_SIZE=50）とデッキ構築（24）の両方**で V1・V2 を確認。⚠️ ページサイズ違いが再現条件 |
| V7 | 絞り込み併用 | `element=WIND` / `class=warrior` / エキスパンション絞り込みと併用しても件数が合う |
| V8 | 並び替えの切替が無料 | 絞り込みを変えずに項目・方向を変えたとき、**ネットワークリクエストが0件**（DevToolsで確認） |
| V9 | 進捗表示 | 絞り込みなしで数値ソートを選ぶと、**取得済みページ数が画面に出る**（無言で待たされない） |
| V10 | 不整合の注記 | ユニーク数と `total_cards` を意図的に食い違わせて、**注記が出て結果は表示される**（落ちない） |
| V11 | 取得失敗 | 途中のリクエストを失敗させると、**エラーとして表に出る**（歯抜けの結果を表示しない） |
| V12 | メモリ | 絞り込みなしで `power` 降順を実行し、**タブのメモリ使用量を実測して記録する**（目安: 展開12MB前後） |

### ⚠️ 1.1 で訂正した期待値（誤っていたのは実装ではなく**この設計書**）

開発担当・レビュー担当・設計担当の**3者が独立に実測して一致**した。実装は指示どおりで差し戻しは不要。

**(a) V1「124 件を表示 / 全 124 件」は、この画面では原理的に出ない**

`app.js:169` の `updateSearchStatus()` が `info.total > shown` のときだけ `/ 全 N 件` を付ける**既存実装**
（#44 以前からの全モード共通）。全件を出し切ると `total === shown` になるため必ず消える。
ここを変えると §8「非数値ソートの挙動は変えない」に反するため、**変えない**のが正しい。
元の症状（「全124件と言いながら120枚」）が消えたことは、**めくっている途中の
`50 件を表示 / 全 124 件`** で画面から確認できる。

**(b) V2「125件すべて」は誤り。正しくは124件**

| クエリ | `total_cards` | ユニーク | `level` 非null |
|---|---|---|---|
| `type=CHAMPION` | 125 | 125 | **124** |

チャンピオン125枚のうち**1枚はレベルを持たない**ため、§3.1-3 が「そのまま使う」と指示した
#39 の除外規則（`isNullish`）で除かれる。125 は §2.2 の測定値（`sort=level` で捲ったときの
取得125／ユニーク123）を**除外規則を通す前のまま**期待値に持ち込んだもの。実際の改善は **123枚 → 124枚**。

⚠️ **教訓**: 検証項目の期待値は「画面に出る最終形」で書くと決めていた（§7 冒頭）のに、
**調査段階の中間値をそのまま持ち込んだ**。中間値を期待値に使うときは、
**その後段で何が起きるか（今回は除外規則）を通してから書く**。

---

## 8. 実装の範囲

- [x] `shared/js/card-search.js` — §3 の置き換え（`probeNonNullCount` / `countNonNull` / `nCache` / `fetchNumericPage` を削除）
- [x] 進捗表示の受け渡し（`opts.onProgress`）と、トップ・デッキ構築の両呼び出し元での表示
- [x] 不整合・取得失敗の注記（§5）

いずれも `7250f16c`（`shared/js/card-search.js` / `app.js` / `tools/deck-builder/app.js`・+148/-100）で完了。

### 対象外

- 非数値ソート（`name` / `rarity`）の経路。**触らない**（安定していることを実測済み）
- JPモードの経路。**触らない**（すでにローカル整列で正しい）
- 公式APIへのフィードバック（issue #44 の案D）。やるなら別issue

---

## 9. 設計担当への確認事項

1. ⚠️ **45リクエストを連続で投げたときのレート制限は未検証**。実装時に**連続実行して429が出ないか確認**すること。
   出る場合は並列度を下げる（並列6でも20.7秒＝現行より速い）
2. **メモリ12MB前後**（V12）が低スペック端末で問題になるなら、`editions` 以外の未使用項目
   （`effect_html` / `effect_raw` / `references` / `referenced_by`）も落とす余地がある。
   ⚠️ ただし**詳細モーダルの表示に使っていないかの確認が要る**ので、まず実測してから判断する

### 1.1 での回答（実測で解決した）

| # | 結果 |
|---|---|
| 1 | ✅ **429は出なかった**（並列6・12とも）。→ 並列度を下げる必要なし。並列度の判断は §10-5 |
| 2 | ✅ **`usedJSHeapSize` = 9.5MB**（レベル降順直後・レビュー担当実測）で目安12MBを下回った。
→ **`effect_html` 等の追加削除は不要**。詳細モーダルの参照調査も**当面やらない**（やらずに済んだ） |

---

## 10. 実装・レビュー後の判断（1.1・設計担当）

対象コミット `7250f16c`。レビュー合格（必須基準 M1〜M7 すべて ✅）。以下は開発・レビューからの確認事項への回答。

| # | 論点 | 判断 |
|---|---|---|
| 1 | 全件を出し切ると `/ 全 N 件` が消える既存挙動を変えるか | ❌ **変えない・派生issueも起こさない**。`124 件を表示` は全件を出し切った状態を過不足なく伝えており、誤りではない。変えると**非数値ソートを含む全モードの表示変更**になり §8 の範囲外。訂正するのは設計書の期待値のほう（§7 の (a)） |
| 2 | V2 の期待値 125 → 124 の読み替え | ✅ **承認**。設計担当も独立に実測して一致（§7 の (b)）。**推測で仕様を変えず実測を持って来た開発・レビューの判断が正しい** |
| 3 | `ALL_CACHE_MAX = 4`（設計書に無い追加） | ✅ **承認・値も4のまま**。§3.3 が上限を書いていなかったのは**設計の抜け**で、`MAX_PAGES` と同じ「暴走止め」の系統。実測で最大の集合（絞り込みなし）が9.5MBなので、現実の絞り込み後の集合が4つ並んでも問題にならない。⚠️ ただし**挿入順（FIFO）で捨てるため、最も高価な「絞り込みなしの集合」が再利用されていても4回の絞り込み変更で捨てられる**（再取得は約20秒）。改善はS指摘として別issueへ |
| 4 | 不整合の注記 `（APIの申告は N 件ですが M 件しか取得できませんでした）` | ✅ **承認**。既存の `jpDropNote` と語調・全角括弧が揃っており、**「出たら異常」の信号**として何が起きたかが一意に読める |
| 5 | 並列度 6 か 12 か | ✅ **6を維持**。理由: ①**ブラウザ実測が取れているのは6だけ**（20.4〜20.7秒。設計書 §2.3 の並列6=20.7秒と一致）で、12の16.6秒は**node実測**。開発担当のnode実測では 18.6 vs 17.2 と**差が1.4秒しかなく**、どちらが再現するか不明 ②差は最大4秒で、**進捗表示が入った初回の絞り込みなしだけ**に効く ③相手は**自分たちが制御できない外部API**。上げるなら**ブラウザで並列12を実測してから**（別issue） |

⚠️ 3 と 5 は**「今のままで合格」と「改善の余地あり」が両立**する項目。合格を妨げないものとして
`#50` に切り出し、**#44 の範囲はここで閉じる**。
