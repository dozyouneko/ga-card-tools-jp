# 運営ダッシュボード（ローカル専用・非公開）設計書 — issue #56

## 改版履歴

| 版 | 日付 | 内容 | 状態 |
| --- | --- | --- | --- |
| 1.0 | 2026-08-09 | 初版。ユーザー判断（ローカル専用・段階導入・印刷ビーコン同梱）を確定し、フェーズ1の全仕様と検証項目を定義 | 実装済み（`089fb9ad` / `9dee0a38` / `5cad0e98` / `b7b78c0d`） |
| 1.1 | 2026-08-09 | レビュー指摘と実データ実測を反映。⚠️ **1.0 の誤り3件を訂正**: ①**§8 の対象は2ファイル**（`_headers` の `/tools/print/*` を落としていた。CSPは積集合で効くため片方だけでは無効＝差し戻しの原因） ②**PVの換算に `sampleInterval` を掛けてはいけない**（`count` は既に重み付け済み。掛けると約10倍に過大評価） ③**ビーコンの位置は `</head>` 直前**（既存2,751ページに合わせる）。あわせて **V17 を `wrangler pages dev` 前提に改版**・**V11b（`<details>` 展開時）を追加**・**畳み込み規則11を追加**・推奨指摘(S)の採否を確定 | **実装待ち**（§11.2 の A〜H） |

**画面モック**: `docs/design/56-運営ダッシュボード/ダッシュボード_モック.html`
（ブラウザで開ける自己完結HTML。**PC幅と375px幅を同一ページで同時に確認できる**＝2つの iframe に
同じマークアップを流し込むので、メディアクエリが実幅どおりに効く）

---

## 1. 背景と目的

このサイトには「運営状況を見る場所」が無い。Cloudflare のダッシュボードで閲覧数の概要は見られるが、
**D1（登録者・デッキ）とは別画面**で、両者を突き合わせた全体像を1枚で見る手段が存在しない。

目的は**運営判断のための1枚**を用意すること。具体的には次の3問に即答できる状態にする。

1. 人は増えているか（登録者・アクティブ）
2. 作った機能は使われているか（デッキ構築・版指定・印刷・大会ページ…）
3. どこから来て、何を見ているか（流入元・パス別閲覧）

## 2. ユーザー決定事項（2026-08-09）

| 論点 | 決定 | 却下した案と理由 |
| --- | --- | --- |
| 提供形態 | **ローカル専用**（`npm run dashboard`） | 本番 `/admin/` は公開サイトに管理面を増やし、認証不備がそのまま情報漏洩になる |
| 機能利用の計測 | **段階導入**。フェーズ1は既存データのみ（**本番コード無改造**）、フェーズ2でイベント計測 | 最初からイベント計測を入れると本番変更とレビューを伴い、着手が重い |
| 印刷ツールのビーコン欠測 | **本issueに含める** | 別issueにすると、ダッシュボード初日から印刷ツールだけ欠測表示になる |

## 3. 事前調査で確定した事実（2026-08-09 実測）

### 3.1 取得できるもの・できないもの

| 指標 | 出どころ | 状態 |
| --- | --- | --- |
| 登録者数・アクティブ・デッキ | D1 `users` / `sessions` / `decks` / `deck_cards` | ✅ すでに蓄積されている |
| 閲覧数（パス・参照元・国・端末） | Web Analytics GraphQL | ⚠️ **トークン権限が不足**（§3.3） |
| 機能の使用数（検索実行・絞り込み・PDF生成・X投稿…） | — | ❌ **どこにも記録が無い** → フェーズ2 |

### 3.2 Web Analytics の識別子（実測値）

`GET /accounts/{account}/rum/site_info/list` の応答より:

| 項目 | 値 |
| --- | --- |
| account tag | `53dcf4e7f02ea5e504977816a68865f5` |
| **site tag**（GraphQL の絞り込みに使う） | `27d8cfee93a54d88932ee8790fe4f9a9` |
| site token（HTMLのビーコンに書く値） | `79f367803da7466d9b8edd3476edb554` |
| host | `ga-card-tools-jp.pages.dev` |

⚠️ **site tag と site token は別物。** GraphQL の `filter.siteTag` に渡すのは **site tag** のほう。
HTMLに埋まっている `79f3…` を渡しても結果は0件になる（エラーにならないので気づきにくい）。

### 3.3 トークン権限が足りない（着手前にユーザーの手作業が必要）

現行の `~/.cloudflare-token` で RUM を引くと **`not authorized for that account`** が返る（実測）。

- 必要な権限: **Account → Account Analytics → Read**
- 現行トークンの権限（`docs/dev-setup.md` 記載）: Account/D1:Edit、Account/Cloudflare Pages:Edit、
  Account/Account Settings:Read、User/User Details:Read → **Analytics が無い**
- 追加手順: Cloudflare ダッシュボード > My Profile > API Tokens > 該当トークン > **Edit** >
  Permissions に `Account` `Account Analytics` `Read` を1行追加して保存 → `~/.cloudflare-token` は**変更不要**
- ⚠️ **`Edit` と `Roll` を取り違えないこと。** 同じメニューに並んでいるが役割が逆:

  | 操作 | 権限 | トークンの値 | 結果 |
  | --- | --- | --- | --- |
  | **Edit** | 変えられる | **変わらない** | ⭕ これを使う。`~/.cloudflare-token` はそのまま |
  | Roll | 変わらない | **再発行される** | ❌ 権限は増えず、**既存の値が無効化**され wrangler も RUM も全部落ちる |

  誤って Roll した場合は、表示された新しい値で `~/.cloudflare-token` を上書きし
  （`chmod 600` を維持）、`docs/dev-setup.md` のバックアップ手順で取り直す
- トークンは**ユーザー所有**（My Profile 配下）。Account 配下の "Account API Tokens" ではない
- 権限行を足すとき、**Account リソースの指定は既存行と同じアカウント**にする
  （別アカウントを選ぶと権限が増えたのに `not authorized` のままになる）
- ⚠️ **`docs/dev-setup.md` の権限一覧も同時に更新すること**（消費側が増えたケース。§9-3）

なお **GraphQL のイントロスペクションはこの権限が無くても通る**（実測）。スキーマ確認だけなら先行できる。

### 3.4 RUM のデータ仕様（イントロスペクションで確定）

`rumPageloadEventsAdaptiveGroups` で使える語彙（実測）:

- **dimensions**: `bot` `countryName` `date` `datetimeHour` `deviceType` `navigationType`
  `refererHost` `refererPath` `refererScheme` `requestHost` `requestPath` `requestScheme`
  `siteTag` `userAgentBrowser` `userAgentOS` ほか
- **metrics**: `count` / `sum { visits }` / `avg { sampleInterval }`

保持期間（Cloudflare公式ドキュメント）: **生データは7日**、以降は**約10%に集約**、遡れるのは**約6か月**。
→ ⭐ **長期推移は手元に日次スナップショットを溜めないと永久に失われる**（§6）。

### 3.5 ビーコンの設置状況（全3,217 HTML を実測）

| 対象 | 件数 | ビーコン | 判定 |
| --- | --- | --- | --- |
| カード個別・セット・大会詳細・トップ・デッキ構築・用語解説 | 2,751 | あり | ✅ 正常 |
| `tournaments/*/decks.html` | 447 | なし | ✅ **正常**。ページではなく順位表から `fetch` して読む**HTML断片**。ビーコンを入れると二重計上になる |
| `tools/print/index.html` | 1 | なし | ❌ **欠測**。独立したページなのに未設置。CSPも `script-src 'self'` のままで許可していない |
| `404.html` | 1 | なし | ⚠️ **意図的**（#23 の方針でJSを一切読み込まない）。404の発生数は測れない — 本issueでは**変更しない** |
| `docs/design/**/*.html`（モック） | 17 | なし | ✅ 正常（本番配信対象外） |

## 4. 全体構成

```text
npm run dashboard                  取得 → HTML生成 → ローカルサーバー起動(3200)
npm run dashboard -- --no-fetch    取得せず、最新スナップショットから再生成（オフライン可）
npm run dashboard -- --no-serve    生成のみ（サーバーを立てない）
```

```text
scripts/dashboard/
├─ index.mjs          エントリ。引数処理・実行順の制御・サーバー起動
├─ collect-d1.mjs     D1（本番）から集計。SELECT のみ
├─ collect-rum.mjs    Web Analytics GraphQL から集計
├─ collect-health.mjs 運用ヘルス。ローカルファイルと git のみ（ネットワーク不要）
└─ render.mjs         スナップショット → 自己完結HTML

tmp/dashboard/                     ⚠️ 出力先はここだけ
├─ index.html                      生成物
└─ snapshots/YYYY-MM-DD.json       日次スナップショット（UTC日付・同日は上書き）
```

### 4.1 ⚠️ 出力先とコミット禁止（最重要）

**このリポジトリは公開されている。** 登録者数・閲覧数・人気カードは運営情報であり、
`docs/` に置くと（本番配信されなくても）**GitHub上で誰でも読める**。

- 数値の入った成果物は **`tmp/dashboard/` 配下にのみ**書き出す（`tmp/` は `.gitignore` 済み）
- **`data/` `docs/` に数値を書き出してはいけない**
- コミットしてよいのは `scripts/dashboard/` のコード・`package.json` のスクリプト定義・本設計書だけ

### 4.2 ポート

| 役割 | 静的プレビュー | Pages Functions | **ダッシュボード** |
| --- | --- | --- | --- |
| 開発担当 | 3000 | 8788 | — |
| レビュー担当 | 3100 | 8888 | — |
| **本ツール** | — | — | **3200** |

既存の役割別ポート割り当てと衝突しない値を選んだ。`PORT` 環境変数で上書き可能にする。

### 4.3 失敗時の方針 — **fail-open（1つ欠けても全体を落とさない）**

3つの収集元（D1・RUM・ヘルス）は**独立して失敗し得る**。1つの失敗で全体を落とすと、
権限追加前は**何も見られない**ことになる。

- 各セクションは `{ ok: true, data }` または `{ ok: false, reason }` を返す
- 失敗したセクションだけ画面に**理由と対処手順**を出し、他は通常表示する
- プロセスは **exit 0** で完走する（`--strict` 指定時のみ、1つでも失敗したら exit 1）

## 5. データ取得の仕様

### 5.1 D1（本番）

```bash
CLOUDFLARE_API_TOKEN=$(cat ~/.cloudflare-token) \
CLOUDFLARE_ACCOUNT_ID=53dcf4e7f02ea5e504977816a68865f5 \
npx wrangler d1 execute DB --remote --json --command "<SQL>"
```

- ⚠️ **`--remote` は本番DBを見る。SELECT 以外を書いてはいけない。**
  実装には INSERT/UPDATE/DELETE/DROP/ALTER を**一切含めない**（検証項目 V14 で機械的に確認する）
- **1クエリ = 1実行**とする。1ファイルに複数ステートメントをまとめて `--file` で流すほうが速いが、
  `--json` の結果配列とクエリの対応付けが崩れたときに**静かに取り違える**。11本×往復数百msは許容範囲なので確実さを取る
- ⚠️ `wrangler d1 execute --remote --json` の**出力の形**は未確認（調査時に実行がブロックされたため）。
  開発担当は**最初に Q1 を1本だけ流し、応答JSONの構造を issue に貼ってから**残りを実装すること

| # | 指標 | SQL |
| --- | --- | --- |
| Q1 | 登録者総数 | `SELECT COUNT(*) AS c FROM users;` |
| Q2 | 新規登録の日次推移（60日） | `SELECT date(created_at) AS d, COUNT(*) AS c FROM users WHERE created_at >= datetime('now','-60 days') GROUP BY d ORDER BY d;` |
| Q3 | アクティブ利用者 | `SELECT (SELECT COUNT(DISTINCT user_id) FROM sessions WHERE last_seen_at >= datetime('now','-7 days')) AS a7, (SELECT COUNT(DISTINCT user_id) FROM sessions WHERE last_seen_at >= datetime('now','-30 days')) AS a30, (SELECT COUNT(*) FROM sessions) AS live_sessions;` |
| Q4 | デッキ全体 | `SELECT COUNT(*) AS decks, SUM(CASE WHEN is_public=1 THEN 1 ELSE 0 END) AS public_decks, SUM(CASE WHEN thumb_image IS NOT NULL THEN 1 ELSE 0 END) AS with_thumb, COUNT(DISTINCT user_id) AS owners FROM decks;` |
| Q5 | デッキ作成の日次推移（60日） | `SELECT date(created_at) AS d, COUNT(*) AS c FROM decks WHERE created_at >= datetime('now','-60 days') GROUP BY d ORDER BY d;` |
| Q6 | 直近の更新 | `SELECT SUM(CASE WHEN updated_at >= datetime('now','-7 days') THEN 1 ELSE 0 END) AS u7, SUM(CASE WHEN updated_at >= datetime('now','-30 days') THEN 1 ELSE 0 END) AS u30 FROM decks;` |
| Q7 | 1人あたりデッキ数の分布 | `SELECT n, COUNT(*) AS users FROM (SELECT user_id, COUNT(*) AS n FROM decks GROUP BY user_id) GROUP BY n ORDER BY n;` |
| Q8 | board別の枚数 | `SELECT board, SUM(qty) AS qty, COUNT(*) AS rows_ FROM deck_cards GROUP BY board ORDER BY qty DESC;` |
| Q9 | 版指定（イラスト選択）の利用率 | `SELECT COUNT(*) AS rows_, SUM(CASE WHEN art_image IS NOT NULL THEN 1 ELSE 0 END) AS with_art, COUNT(DISTINCT CASE WHEN art_image IS NOT NULL THEN deck_id END) AS decks_with_art FROM deck_cards;` |
| Q10 | 人気チャンピオン top10 | `SELECT champion_slug AS slug, COUNT(*) AS c FROM decks WHERE champion_slug IS NOT NULL GROUP BY slug ORDER BY c DESC LIMIT 10;` |
| Q11 | 人気カード top20 | `SELECT card_slug AS slug, SUM(qty) AS qty, COUNT(DISTINCT deck_id) AS decks FROM deck_cards GROUP BY slug ORDER BY qty DESC LIMIT 20;` |

**slug → 日本語名**は `data/tl-names.json`（既存の生成物）で解決する。
未収録の slug は **slug をそのまま表示し、末尾に `(未訳)` を付ける**（黙って空欄にしない）。

#### ⚠️ Q3 の限界を画面に明記する

`sessions` は TTL 30日で、しかも**ログインのたびに期限切れ行を全ユーザー分まとめて削除する**
（`functions/_lib/session.js` の `createSession`）。したがって:

- `a30` は「直近30日に来た人」の**下限**でしかない
- **31日以上前の活動履歴は D1 から復元できない** — これが §6 のスナップショットを必須にする理由

画面には `アクティブ（30日）19人 ⓘ` と出し、ⓘ に上記の注記を付ける。

### 5.2 Web Analytics（GraphQL）

エンドポイント `https://api.cloudflare.com/client/v4/graphql`、
`filter` に `siteTag: "27d8cfee93a54d88932ee8790fe4f9a9"` と期間、そして **`bot: 0`** を必ず含める。

| # | 取得内容 | dimensions | 期間 | limit |
| --- | --- | --- | --- | --- |
| R1 | 日次の閲覧数・訪問数 | `date` | 30日 | 100 |
| R2 | パス別 | `requestPath` | 30日 | 500 |
| R3 | 参照元 | `refererHost` | 30日 | 20 |
| R4 | 端末種別 | `deviceType` | 30日 | 10 |
| R5 | 国別 | `countryName` | 30日 | 20 |

- ⭐ **閲覧数（PV）= `count` をそのまま使う。`avg(sampleInterval)` を掛けてはいけない。**
  （1.0 の「`count × avg(sampleInterval)`」は**誤りだった**。2026-08-09 の実測で訂正）
  - **根拠（権限追加後に実測）**: ① 同一期間・同一フィルタで **日別にグループ化した総 `count` と、
    パス別にグループ化した総 `count` が完全一致**した ② そのとき全行の `count` が **`sampleInterval` の
    倍数に丸まっていた**（`sampleInterval` は 10〜13）③ 一方、期間を7日に縮めると `sampleInterval` が
    小さくなり `count` は倍数でない値を返した
    → **`count` は「サンプル行数 × sampleInterval」で既に重み付けされた推定値**。
    もう一度掛けると**約10倍に過大評価**する（1.0 のままなら桁を間違えた数字が出ていた）
  - `sum { visits }` は本サイトの実測では**全行で `count` と同値**だった。
    ⚠️ **PVと訪問数を別々のKPIとして並べると同じ数字が2つ出る**ので、画面では
    **閲覧数（`count`）のみを出し、訪問数は出さない**
  - `avg(sampleInterval)` は表示しないが**取得はする**。⭐ **1 を超えていたら「サンプリング中（概算値）」
    のバッジをグラフ見出しに出す**（`sampleInterval = 1` の期間は実測値そのもの）
  - V4b（Cloudflareダッシュボードとの ±5% 突合）は**引き続き実施する**。上記は換算式の誤りを
    正しただけで、ダッシュボード表示との一致を確認したわけではない
- **日付は UTC で扱う**。JSTに直すと日次グラフの境界が1日ずれて日次cronの痕跡と読み違える。
  画面のグラフ見出しに **`（UTC基準）`** と明記する（取得日時のほうはJSTで表示する）

#### パスの畳み込み規則（**上から順に評価し、最初に一致したものを採用**）

カード個別ページだけで2,240本あるため、生パスのまま並べても機能の使われ方は読めない。

| 順 | 判定 | カテゴリ表示名 |
| --- | --- | --- |
| 1 | `/` に完全一致 | トップ（カードDB） |
| 2 | `/cards/` に完全一致 | カード一覧 |
| 3 | `/cards/<何か>/` | カード個別ページ |
| 4 | `/sets/` に完全一致 | セット一覧 |
| 5 | `/sets/<何か>/` | セット別ページ |
| 6 | `/tools/deck-builder/` で始まる | デッキ構築ツール |
| 7 | `/tools/print/` で始まる | 印刷PDF生成 |
| 8 | `/tools/glossary/` で始まる | 用語解説 |
| 9 | `/tournaments/` に完全一致 | 大会一覧 |
| 10 | `/tournaments/<何か>/` | 大会詳細 |
| 11 | `/tournaments/<何か>/decks` または `/tournaments/<何か>/decks.html` | 大会詳細（**規則10に合流**） |
| 12 | `/decks` または `/decks.html` | デッキ一覧 |
| 13 | 上記以外 | **その他** |

⚠️ **規則11は 1.0 に無く、実データで見つけて追加した**（2026-08-09）。大会デッキの断片 `decks.html` は
`fetch` で読む前提なのに、**RUM に実際のパスとして記録されていた**（本番で `/…/decks.html` は
`/…/decks` へ **308** される＝拡張子トリムのため、両方の形を書く）。
⚠️ **この断片にはビーコンが無く（ローカル・本番の実配信の両方で確認済み）、なぜ記録されたかは未解明。**
件数はごく僅かなので実害は無いが、**「断片が閲覧されている」と読み違えないよう大会詳細に合流させる**。

⭐ **畳み込み規則は実データで検証済み**（2026-08-09・30日・25パス）。「その他」は**閾値5%を大きく下回り**、
中身は上記 `decks.html` の1種のみだった。規則11を足した現在は**その他が0件**になる見込み。

- ⭐ **「その他」が総PVの5%を超えたら、その行に ⚠️ バッジを出し、生パスの内訳を `<details>` で展開できるようにする。**
  畳み漏れ＝**新しいページが増えたのに規則が追いついていない**合図であり、放置すると
  そのページの利用が永久に見えなくなる（`meta.sets` の追記漏れと同じ形の失敗）
- **カード個別ページは畳んだうえで、別表で slug 別 top20 も出す**（どのカードが読まれているかは翻訳の優先順位に直結する）。
  slug → 日本語名の解決は §5.1 と同じ

### 5.3 運用ヘルス（ローカルのみ・ネットワーク不要）

CLAUDE.md に繰り返し出てくる「静かに壊れる」箇所のうち、**既存の検出経路が無いもの**を3つだけ載せる。

| 項目 | 出どころ | ⚠️ の条件 |
| --- | --- | --- |
| 日次cronの最終更新 | `data/tournaments/index.json` の `updatedAt` | **48時間**より古い |
| シーズン禁止の終了日 | `data/seasonal-banlist.json` の `seasons[].effectiveTo` | `null` が**2件以上**（1件は現行シーズンで正常） |
| 未pushコミット | `git rev-list --count origin/main..HEAD` と `git diff --stat origin/main..HEAD -- . ':(exclude)docs/*'` | 後者が**非空**（＝本番配信物が未pushで溜まっている） |

⚠️ `meta.sets` の新セット追記漏れ（#40）は**載せない**。`npm run validate` が既に exit 1 で検出しており、
二重に持つと片方の更新漏れで嘘をつく。

## 6. スナップショット

`tmp/dashboard/snapshots/YYYY-MM-DD.json`（**UTC日付**）に、その日取得した全数値をそのまま保存する。

- 同日に複数回実行したら**上書き**（1日1ファイル）
- HTML生成時に `snapshots/` を**全部読み**、累計推移グラフ（登録者数・デッキ数）を描く
- `--no-fetch` はネットワークに出ず、既存スナップショットの**最新1件**で画面を作る
- 存在意義: **RUMの6か月制限**（§3.4）と **`sessions` の30日制限**（§5.1）を超える履歴は、ここにしか残らない

⚠️ `tmp/` は `/workspaces` のバインドマウント上なので**コンテナ再構築では消えない**が、
**PC故障では消える**（`docs/dev-setup.md` のシナリオB）。長期の履歴が惜しくなったら
バックアップ対象に加えることを別途検討する（本issueでは扱わない）。

## 7. 画面仕様

配色・余白の詳細はモック（`ダッシュボード_モック.html`）が正。ここでは構成と決定事項のみ記す。

| # | セクション | 内容 |
| --- | --- | --- |
| 0 | ヘッダ | タイトル / 取得日時（**JST**）/ 各データ源の状態バッジ（D1・閲覧・ヘルス） |
| 1 | KPIタイル ×4 | 登録者数 / 保存デッキ数 / 閲覧数（7日）/ アクティブ利用者（7日）。各タイルに**前週比**を併記 |
| 2 | 利用者 | 新規登録の日次グラフ（60日）/ アクティブ 7日・30日 / 1人あたりデッキ数の分布 |
| 3 | デッキ | 総数・公開率・所有者数 / board別枚数 / **版指定の利用率**・サムネ指定率 / 人気チャンピオン top10 / 人気カード top20 |
| 4 | 閲覧 | 日次グラフ（30日・UTC）/ **機能カテゴリ別**（畳み込み）/ カード個別ページ top20 / 参照元 top10 / 端末比 / 国別 top10 |
| 5 | 運用ヘルス | §5.3 の3項目 |
| 6 | フッタ | 取得元と**制約の注記**（RUMサンプリング・UTC基準・sessions30日・印刷ツールの計測開始日） |

### 7.1 決定事項

1. **外部ライブラリを使わない。** グラフはCSSの棒（`div` の幅）だけで描く。
   ローカル生成HTMLなのでCSPの制約下になく、**インライン `style` を使ってよい**
   （本番の `style-src 'self'` 制約はこのファイルには掛からない。本番のコードとは混ぜないこと）
2. **単一の自己完結HTML**（外部CSS・JS・画像を参照しない）。ファイル1つコピーすれば後から単体で開ける
3. **レスポンシブ対応する。** ローカルサーバーは同一LAN内のスマホからも開けるため、
   **PC幅と375px幅の両方**を成立させる（検証項目 V9）
4. **配色はOSのライト/ダーク設定に追従**する（`prefers-color-scheme`）
5. 数値は**3桁区切り**、率は**小数第1位**まで。0件は空欄ではなく **`0`** と書く

### 7.2 前週比の出し方

`users.created_at` / `decks.created_at` から直接計算する（スナップショットが1日分しか無くても出る）。
閲覧数の前週比は RUM の「直近7日」と「その前の7日」を比較する。

- 増加は `+6（+14.6%）`、減少は `-3（-5.2%）`、比較対象が0のときは **`—`**（`+∞%` と書かない）

### 7.3 印刷ツールの計測開始日を注記する

ビーコン設置（§8）以前の期間は `/tools/print/` の閲覧数が **0 として返る**。
グラフ上は「使われていない機能」に見えてしまうため、フッタと機能カテゴリ表に
**`※ 印刷PDF生成は YYYY-MM-DD 計測開始`** を出す（日付は実装時のpush日を手で入れる）。

## 8. 印刷ツールのビーコン設置（本番変更・**push 承認が要るのはここだけ**）

⚠️ **1.0 の重大な誤り（2026-08-09 のレビュー指摘で判明・1.1 で訂正）**

1.0 は対象を `tools/print/index.html` の1ファイルと書き、「他の本番ファイルは触らない」と明記していた。
**これが誤りで、印刷ツールのCSPは2箇所にある**:

| 場所 | 効き方 |
| --- | --- |
| `tools/print/index.html` の `<meta http-equiv="Content-Security-Policy">` | ページ内のポリシー |
| **`_headers` の `/tools/print/*`** | **HTTPヘッダのポリシー** |

⭐ **Pagesは一致した全ルールのCSPを重複して送り、ブラウザは積集合で評価する。**
片方が `script-src 'self'` のままだと、もう片方を緩めても **beacon.min.js はブロックされる**
（実測: `requestfailed` reason `csp` / `transferSize 0`）。

→ **変更対象は2ファイル。push 承認の対象も2ファイル。**
⚠️ **片方だけ push すると、ブロックされたままか、ヘッダだけ緩んだ中途半端な状態になる。**

### 8.1 `tools/print/index.html`

1. meta CSP の `script-src 'self'` → `script-src 'self' https://static.cloudflareinsights.com`
2. meta CSP に `connect-src 'self' https://cloudflareinsights.com` を追加
3. **`</head>` の直前**に、既存ページと**同一の**ビーコンを追加:

```html
<!-- Cloudflare Web Analytics -->
<script defer src='https://static.cloudflareinsights.com/beacon.min.js' data-cf-beacon='{"token": "79f367803da7466d9b8edd3476edb554"}'></script>
<!-- End Cloudflare Web Analytics -->
```

- ⚠️ **`index.html` の書き方に合わせること**（`defer` + `src`）。`site_info` API が返す snippet は
  `type='module'` 形式だが、**このサイトの既存2,751ページはすべて `defer` 形式**。混在させない
- ⚠️ **位置は `</head>` 直前**（1.0 は `</body>` 直前と書いていたが**誤り**）。
  既存2,751ページはすべて `</head>` 直前に置いている（`index.html` 26行目・
  `cards/dungeon-guide/index.html` 26行目・`tools/deck-builder/index.html` 30行目で実測）。
  `defer` なので動作に差は無いが、**1ファイルだけ違う置き方をすると後から grep したときに漏れる**
- ⚠️ **`data-cf-beacon` に渡すのは site token（`79f3…`）。** site tag（`27d8…`）ではない（§3.2）
- ⚠️ **`404.html` には入れない**（#23 の「JSを読み込まない」方針を維持する）
- ⚠️ **`tournaments/*/decks.html` にも入れない**（HTML断片。二重計上になる）
### 8.2 `_headers` の `/tools/print/*`

同じ2点を**ヘッダ側にも**適用する。

1. `script-src 'self'` → `script-src 'self' https://static.cloudflareinsights.com`
2. `connect-src 'self' https://cloudflareinsights.com` を追加
   （⚠️ 未指定だと `default-src 'self'` にフォールバックし、`script-src` だけ直しても
   **ビーコンの送信先が塞がったまま**になる）
3. 直前のコメント「印刷PDFツール: 外部通信なし。厳格な self-only CSP。」を**事実に合わせて訂正**し、
   **CSPが2箇所にあること・検証は `wrangler pages dev` で行うこと**を注記として残す

### 8.3 コミットと push の扱い

- **§8 の変更（2ファイル）は独立したコミットにする**（本番配信物なので push 判断を分けられるようにする）
- ⚠️ **`_headers` と `tools/print/index.html` は必ず同じコミットに入れる**（片方だけ push できないようにする）

### 8.4 push 当日のチェックリスト（⚠️ 忘れると静かにずれる）

| # | やること | 忘れると |
| --- | --- | --- |
| 1 | `render.mjs` の **`PRINT_BEACON_SINCE` を実際の push 日に直す**（§7.3） | 「印刷PDF生成 ※ … 計測開始」の日付がずれ、**0件の意味を読み違える** |
| 2 | push 後に `curl -sI 'https://ga-card-tools-jp.pages.dev/tools/print/?cb=$RANDOM'` でCSPヘッダを確認 | `_headers` はローカルでは `wrangler pages dev` までしか確認できない |
| 3 | 実ブラウザで `/tools/print/` を開き、`beacon.min.js` が 200 で読まれることを確認 | 本番のCSPが想定と違っても気づけない |
| 4 | ⚠️ **反映確認は2〜3分あけて2回**（push直後はリクエストごとに新旧が入れ替わる） | 1回の測定で「直っていない」と誤判断する |

## 9. 検証項目

⚠️ 期待値は**画面に出る最終形**で書いている。件数だけでなく**順序・文言・位置**まで一致すること。

### 9.1 基本動作

| # | 手順 | 期待値 |
| --- | --- | --- |
| V1 | `npm run dashboard` を引数なしで実行 | exit 0 で完走。`tmp/dashboard/index.html` が生成され、`http://localhost:3200/` で表示される。Ctrl-C で停止できる |
| V2 | V1の直後に `git status --short` | **1行も出力されない**（生成物が `tmp/` 配下だけであることの確認） |
| V3 | `npm run dashboard -- --no-fetch` をネットワーク遮断状態で実行 | exit 0。最新スナップショットの数値で画面が出て、ヘッダの取得日時が**スナップショットの日付**になる |
| V14 | `grep -riE "insert|update|delete|drop|alter" scripts/dashboard/` | **SQLとしての一致が0件**（コメント・変数名の `updated_at` 等は可。本番DBへの書き込みが無いことの機械的確認） |

### 9.2 D1セクション

| # | 手順 | 期待値 |
| --- | --- | --- |
| V4 | KPIタイル「登録者数」の値と、`wrangler d1 execute DB --remote --command "SELECT COUNT(*) FROM users;"` の結果を比較 | **完全一致** |
| V5 | 人気チャンピオン top10 を見る | **日本語名**で1位から降順。`tl-names.json` に無い slug は `spirit-of-fire (未訳)` の形で slug をそのまま表示 |
| V6 | 人気カード top20 を見る | 「日本語名 / 採用デッキ数 / 総枚数」の3列。**総枚数の降順**で20行（データが20種未満ならその件数） |
| V7 | アクティブ利用者の ⓘ を開く | 「`sessions` は30日で失効し、ログイン時に期限切れ行が削除されるため、31日以上前の活動は復元できない」旨が読める |

### 9.3 閲覧セクション

| # | 手順 | 期待値 |
| --- | --- | --- |
| V8 | **トークンに Analytics 権限が無い状態**で実行 | ⭐ **exit 0 で完走**。閲覧セクションに `⚠️ 閲覧数を取得できませんでした（Account Analytics: Read が必要）` と**§3.3の追加手順**が出る。**利用者・デッキ・運用ヘルスの3セクションは通常どおり数値が出る** |
| V4b | 権限追加後、Cloudflareダッシュボードの同一期間（直近7日）のPVと画面のPVを比較 | **±5%以内で一致**。外れる場合は §5.2 の換算をやめ、`count` の生値＋サンプリング注記に切り替えて再報告する |
| V9 | 機能カテゴリ別の表を見る | `/cards/spirit-of-fire/` が「カード個別ページ」に、`/tools/print/` が「印刷PDF生成」に集計されている。**「その他」が総PVの5%超のとき**、その行に ⚠️ バッジが付き、`<details>` を開くと生パスの内訳が出る |
| V10 | 日次グラフの見出し | **`（UTC基準）`** と明記されている。一方ヘッダの取得日時は **JST**（`2026-08-09 13:24 JST` の形） |

### 9.4 表示・レイアウト

| # | 手順 | 期待値 |
| --- | --- | --- |
| V11 | 幅375pxで表示 | **横スクロールが発生しない**（`document.documentElement.scrollWidth <= 375`）。KPIタイルが**2列×2段**に折り返す。人気カード表は**表単位で横スクロール**し、ページ本体はスクロールしない |
| V11b | 幅375pxで、**画面上のすべての `<details>` を開いた状態**でも測る（「畳み漏れの内訳」を含む） | ⭐ **開いた状態でも `document.documentElement.scrollWidth <= 375`**。1.0 は「既定の描画」しか読めない書き方で、**展開時に 500px まで広がる不具合を見逃した**（レビューの参考指摘 S1-4）。`.cols` のグリッドアイテムに `min-width: 0` が無く、min-content が入れ子テーブルに引っ張られるのが原因の候補 |
| V12 | 最長の日本語カード名 `アトモス・アーマー Type-Hermes`（21文字。実データの最長）を含む行をPC幅・375px幅の両方で確認 | 名前が折り返しても**数値列の右端が揃う**。⭐ 判定は要素の矩形ではなく **`Range.getBoundingClientRect()`（テキストの実描画位置）**で測り、**右端の差が±1px以内**であること |
| V13 | 登録者0人・デッキ0件のデータ（スナップショットを手で用意）で描画 | 例外にならず、各セクションに **`データがありません`** と出る。前週比は `—`（`+∞%` や `NaN` を出さない） |

### 9.5 運用ヘルス／本番変更

| # | 手順 | 期待値 |
| --- | --- | --- |
| V15 | `data/tournaments/index.json` の `updatedAt` を3日前に書き換えて実行（**確認後に `git checkout` で戻す**） | 「日次cronの最終更新」に ⚠️ が付き、経過時間が表示される |
| V16 | 未pushコミットに `docs/` 以外が含まれる状態で実行 | 「未pushコミット」に ⚠️ と **`本番配信物 N件`** が表示される |
| V17 | ⚠️ **必ず `npx wrangler pages dev . --local --port <ポート>` で配信**し、`/tools/print/` を開いて Console と Network を見る | **CSP違反が出ない**。Network に `beacon.min.js` が **status 200** で現れる（`requestfailed` でない）。⭐ **同じ手順で `/`（トップ）を測り、結果が一致すること**（対照） |
| V17b | `curl -sI http://localhost:<ポート>/tools/print/ \| grep -i content-security` | **2つのポリシーが重複して返り、両方が `static.cloudflareinsights.com` を許可**していること（`_headers` と meta の積集合で効くため） |
| V18 | `tools/print/index.html` と `_headers` の差分 | `tools/print/index.html` は**CSP 2箇所とビーコン1ブロックのみ**（`defer` 形式・token `79f367803da7466d9b8edd3476edb554`・**`</head>` 直前**）。`_headers` は `/tools/print/*` の**CSP 1行とコメント**のみ。**2ファイルが同一コミット**にある |

⚠️ **V17 を `npm run dev`（`scripts/serve.mjs`）で実施してはいけない。**
静的サーバーは **`_headers` を解釈しない**ため、`_headers` 側がブロックしたままでも
**meta CSP だけが効いた状態＝合格に見える**。1.0 ではここを「ローカルで開き」としか書かなかったため、
**実際に1往復目の差し戻しを招いた**（CLAUDE.md にも同じ注意が既に書かれている）。

## 10. フェーズ2（イベント計測）— 方針のみ

本issueでは**実装しない**。フェーズ1の完了後、**別issueで詳細設計**する。現時点の方針:

- `POST /api/stat` に `{ e: "<イベント名>", v?: "<値>" }` を送り、D1 に
  **日付×イベント名×値の集計行として UPSERT** する（**生ログを貯めない**＝プライバシーと容量の両立）
- 計測候補: 検索実行 / JPモード切替 / 絞り込み種別 / 並び替え項目 / 共有URL生成 / 詳細モーダル /
  印刷トレイ追加 / PDF生成 / 統計タブ / X投稿 / デッキ保存
- 検討が要る点: bot・連打による水増し、D1の書き込み量（無料枠10万/日）、
  CSP（`connect-src 'self'` なので**追加許可は不要**）、計測を嫌う利用者への配慮

## 11. 未決事項

### 11.1 1.0 の未決事項 — **すべて解決済み**

| # | 内容 | 結果 |
| --- | --- | --- |
| 1 | トークンへの `Account Analytics: Read` 追加 | ✅ **完了**（2026-08-09・ユーザー作業）。実データ取得を確認 |
| 2 | `wrangler d1 execute --remote --json` の出力構造 | ✅ **回答済み**（配列で返り `[0].results` が行配列。`--json` 時の stdout は純粋なJSON） |
| 3 | `count` の換算 | ✅ **解決。§5.2 のとおり 1.0 の式は誤りで、1.1 で訂正**（`sampleInterval` を掛けない） |
| 4 | `docs/dev-setup.md` の権限一覧追記 | ✅ 完了 |

### 11.2 1.1 で追加した実装項目（次の周回で対応する）

| # | 内容 | 由来 |
| --- | --- | --- |
| A | **`_headers` の `/tools/print/*` を §8.2 のとおりにする** | レビュー M1-1（対応済み・**設計書側の追認**） |
| B | **ビーコンを `</head>` 直前へ移す**（§8.1-3） | 開発担当の確認事項1 |
| C | **375px で `<details>` 展開時の横スクロールを解消**（V11b） | レビュー S1-4 |
| D | **PVの換算から `sampleInterval` の乗算を外す**（§5.2）。`sampleInterval > 1` のときだけ「概算値」バッジ | 1.1 の実測 |
| E | **畳み込み規則11を追加**（§5.2） | 1.1 の実測 |
| F | **`serve()` のパス検査を `OUT_DIR + path.sep` で比較**する | レビュー S1-2 |
| G | **60日グラフの基準日を `snapshot.fetchedAt` に統一**する（`--no-fetch` 時のずれ） | レビュー S1-3 |
| H | **V4b・V9 を実データで実施**し、RUMクエリ本体（`filter`・`orderBy` の名前）が通ることを確認 | 権限追加により実施可能になった |

**対応しないと決めたもの**:

- **S3-1（`git fetch` せず `origin/main..HEAD` を見る）** → **直さない**。多めに出る方向＝
  push を促す安全側で、`git fetch` を副作用として走らせるほうが害が大きい。
  代わりに**画面に「`origin/main` が古いと多めに出ます」の注記を添える**
- **V4 の独立突合**（`wrangler d1 execute --remote` の直実行）→ **現状で可とする**。
  KPIは `COUNT(*)` を変換せずに流すだけで、SQL文字列の一致と値の伝播はレビューで確認済み。
  ⚠️ ユーザーが自分で1コマンド叩けば30秒で確認できるため、**必要なら随時**
