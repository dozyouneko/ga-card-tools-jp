**対象: 「アクセント色の直値」**（→ 完了）

# 承認 — #105 アクセント色の直値をトークン参照に置き換える（2026-09-24）

- 設計書: `docs/design/105-アクセント色の直値/アクセント色の直値_設計.md`（→ **1.1** に改版）
- 実装: **`74afb970`**（置き換え34箇所＋生成物の再生成＋検査2本）／**`8af353c3`**（検査ブロック冒頭コメントの追記）
- レビュー: **合格**（2026-09-24・M1〜M7 すべて ✅。⭐ **V1〜V9 と破壊試験 12ケースをレビュー担当が自分で測り直した**）
- ⭐ **実物確認（設計書 §8 の U1〜U12）: 実施済み**（ユーザー・2026-09-24 → §2）
- ⚠️ ⭐ **クローズはしない。** ラベルを **`設計確認待ち` → `push待ち`** に付け替える
  （**本番公開の可否はユーザーの判断**。push が済んだらラベルを外してクローズする）
- ⚠️ ⭐ **1行目は `（→ 完了）`**（＝**このタスクはこの承認で終わり**）。
  ⭐ **論点6件のうち実装の変更が要るものは1つも無く、残作業はすべて #118 と本書の改版で引き取った**（→ §3）

---

## 1. ⭐ 承認したのはユーザー（設計担当は記録係）

⭐ **2026-09-24、ユーザーが実物を操作したうえで承認した**（リードが直接聞き取り）。
本書はその**記録**であって、⚠️ **設計担当が可否を判断したものではない。**

## 2. ⭐ 実物確認（U1〜U12）の結果 — **全部OK**

⭐ サーバーは **`npx wrangler pages dev . --local --port 3000`**（ユーザーが**実ログイン**して確認）。

| 項目 | 結果 |
|---|---|
| U1・U2・U4〜U9・U11・U12 | ✅ **OK**（⭐ **「変わっていないこと」を見てもらう工程**で、変化なし） |
| ⚠️ **U3** | ⭐ **飛ばした**——**項目自体が無効**（条件バーは半透明ではなく不透明。→ §3 の論点2） |
| ⚠️ **U10** | ✅ **OK**。⭐ **個人戦の `/tournaments/50047/` で確認**（→ §3 の論点3） |

⭐ **U9（種別バッジ4色）が今回いちばん効いた確認**——`--yes`（緑）と `--accent-3`（桃）は
**今回 `tournaments.css` の `:root` に新設したトークン**で、**4色の濃さがそろっていること**を見てもらった。

---

## 3. ⭐ 論点6件の処理

⚠️ ⭐ **6件とも「実装を差し戻す理由」にはならなかった。** 内訳は
**設計書側の誤り・不足が4件（論点1・2・3・5）／派生の判断が1件（論点4）／記録の抜けが1件（論点6）**。

### 論点1 — `CSS_GENERATORS` が 2 ではなく 3 だった → ⭕ **実装（3件）を採る。設計書を直した**

| | |
|---|---|
| **結論** | ⭐ **3件が正。** 設計書 §6-1 の実測（「該当は2件だけ」）と V2-d の期待文字列を **1.1 で直した** |
| **理由** | ⭐ **`scripts/gen-element-orbs-css.mjs` は実際に `shared/css/element-orbs.css` を書き出している。** **2件で実装すると、この1本だけ「消せば黙る」の穴が残る**＝**迷子検査の目的そのものが達成されない** |
| ⭐ **なぜ設計が落としたか** | 書き出しが **`writeFileSync(OUT, css)`** で、**パスが離れた行の `const OUT = …` にある**。⚠️ ⭐ **`writeFileSync(… ".css" …)` を「1行の中で」探すと落ちる**。⭐ **判定はファイル単位**（`writeFileSync` を呼び、かつ `".css"` のリテラルを持つ `.mjs`）が正しい |
| **実害** | ⭕ **無い**（`element-orbs.css` のトークン色リテラルは0件。`gen-element-orbs-css.mjs` の `rgba(0,0,0,.55)` はどのトークンとも一致しない＝黒はトークンに無い） |
| ⚠️ **教訓** | ⭐ **「実測した」と書いてある数字でも、探し方が1行単位だと落ちる。** **設計書に実測値を書くときは、探し方（何を何単位で見たか）も一緒に書く** |

⭐ **実測で追認（2026-09-24・設計担当）**: 成功行は `no token-colored rgb() literals — 0件（11ファイル + 生成元3ファイル走査 / :root出所 4）`。

### 論点2 — U3 / V7-c の文面（`.controls` は不透明） → ⭕ **設計書を訂正（U3 は無効化）**

- ⭐ **実測**: `.controls` の算出背景色は **`rgb(22,26,34)`（不透明）**。
  `style.css` には `.controls` の背景宣言が**2つ**あり、後ろの **`.controls { background: #161a22 }`** が
  **同詳細度・後出しで必ず勝つ**
- ⚠️ ⭐ **この状態は置き換え前から同じ**で、**本タスクは勝っているほうの宣言を1バイトも動かしていない**（レビューが before ツリーで追認済み）
- ⭐ **処理**: **設計書 1.1 で V7-c に訂正を入れ、U3 は取り消し線＋無効の理由を書いた**。
  ⚠️ ⭐ **実物確認でも飛ばした**——**そのまま渡すとユーザーは存在しない半透明を探すことになる**
- ⚠️ ⭐ **副作用として「死んでいる宣言」が1つ残った**（#105 でトークン参照にした宣言が画面に出ない）。→ **論点4**

### 論点3 — U10 を個人戦の大会に → ⭕ **設計書に明記（`/tournaments/50047/`）**

- ⭐ **実測**: `.rank-badge` は **チーム戦 `59730` に 0件 / 個人戦 `50047` に 7件**。
  ⚠️ **チーム戦のモーダルの見出しは `<span class="deck-player">名前（チーム・N位）</span>` の形**（#65 §6-6 の仕様どおり）
- ⭐ **処理**: **設計書 1.1 の U10 に「個人戦の大会・例 `/tournaments/50047/`」と、チーム戦には出ない理由を書いた**
- ⚠️ ⭐ **教訓**: **確認項目に「任意の◯◯」と書かない。** ⭐ **その要素が出る条件を満たす個体を1つ名指しする**
  （本件は**設計書が大会を指定していなかった**ために、レビューと実物確認の両方で拾い直しが要った）

### 論点4 — `style.css` の `.controls` の hex 直書き → ⭕ **派生 #118 に含める（別項目として）**

- ⚠️ ⭐ **`#161a22` はトークンと同じ色ではない**（`--bg` は `#0f1115` / `--panel` は `#1b1f28`）。
  ⭐ **HeroUI 案Aで「面を一段明るくして階層を作る」ために足した中間色**で、**対応するトークンが存在しない**
  （`.site-header { background: #191d25 }` も同型）
- ⭐ **だから「hex がトークンと同じ色」（§9-1 の10箇所）とは少し違う型**だが、
  ⭐ **`style.css:30` の `#14171d`（トークンに無い中間色）と**まったく同じ判断**を要する**ので、
  **#118 の中の別項目（§1-3）として一緒に決める**
- ⭐ **判断の選択肢を #118 に書いた**: **(a) 勝っているほうをトークン化**（`tokens.css` に新設）／
  **(b) 負けている宣言を消す**／**(c) 両方残す**
- ⚠️ ⭐ **#105 では直さない**——**見た目を変えないリファクタという本タスクの性質を超える**（(a) はトークンの新設、(b) は宣言の削除）

### 論点5 — 検査 (iii) がスペース区切り記法を見ていない → ⭕ **設計書に限界として明記＋ #118 で塞ぐ**

⭐ **設計担当が独立に実測して再現させた（2026-09-24）**——拾う正規表現は `rgba?\( 数 , 数 , 数 [,)]` の形:

| 書き方 | 検査 |
|---|---|
| `rgba(217,164,65,.14)` / `RGBA(217, 164, 65, .14)` | ⭕ **拾う** |
| ⚠️ **`rgb(217 164 65 / 14%)`** / **`rgb(217 164 65)`** | ❌ ⭐ **素通り** |
| `color(srgb 0.85098 0.643137 0.254902)` | ❌ 素通り |

| | 結論 |
|---|---|
| **記載** | ⭐ **設計書 §6-4 に限界⑥として追記した**（1.1）。⚠️ **レビューの指摘どおり、1.0 の「限界」の表に挙がっていなかった** |
| **塞ぐか** | ⭐ **塞ぐ。ただし #105 では塞がない**（→ §6-5 に判断を書いた） |
| **理由（塞ぐ）** | ⚠️ ⭐ **スペース区切りは DevTools の色表示や新しく書くCSSで自然に出てくる形**＝**この検査が本来いちばん止めたい「これから書かれる直値」の書き方そのもの**。⭐ **「いま実体が0件だから放置」は、再発防止の検査としては筋が通らない** |
| **理由（#105 では塞がない）** | ⭐ **漏れている実体は0件**（既存の直値はすべてカンマ記法）。⚠️ **実害が無い状態で実装をもう1往復させる理由が無い**。⭐ **#118 の設計の主題はもともと「(iii) の視野をどこまで広げるか」**なので、**記法と hex は同じ設計判断の中にある** |
| **どこまで** | ⭐ **スペース区切りの整数表記まで**（`rgb(r g b)` / `rgb(r g b / a%)`）。⚠️ **`color(srgb 0.85 …)` の小数表記は広げない**——⭐ **`getComputedStyle()` の出力形であって人が手で書く形ではなく**、広げると丸めの判定が要って検査が一段複雑になる |

### 論点6 — 新検査2本を `CLAUDE.md` に書く → ⭕ **本承認と同じコミットで書いた（設計担当が）**

- ⚠️ ⭐ **開発担当が書かなかった判断は正しい**（`CLAUDE.md` は**ルート直下＝そのまま本番配信される**ので、担当パス外）。
  ⭐ **設計担当の担当パス**なので、**この承認と同じコミットで入れた**
- **書いた場所**: 「開発コマンド」の `npm run validate` の節、**既存の (i)(ii) と同じブロック**
  （⭐ **`validate.mjs` でも同じブロックなので、記述も分けない**）。⭐ **「検査は2本」→「検査は4本」**に直した
- **書いた内容**（既存の #101/#114・#97・#93・#108 と同じ粒度）:
  **成功行2本の期待値** / **直し方（`color-mix`）と、なぜそれ以外に手が無いか** /
  **生成CSS 3枚は生成元も走査する（迷子検査つき）** / **3枚目を数え落とした理由** /
  **生成元はコメントを除去しない（#117 の罠）＝規約が1つ増える** /
  **(iv) が `var(--X, 既定値)` を見ない理由（JS が書く3変数）** /
  **成功行の数字が動くのは正常** / ⚠️ **限界3つ（hex・スペース区切り記法・事後の腐り）と #118 への行き先**

---

## 4. ⭐ 承認の理由

| # | 根拠 |
|---|---|
| 1 | ⭐ **見た目が1画素も変わっていないことが、3通りで示されている**——**宣言値の文字列一致 34/34** / **正規化した算出色の一致 34/34** / **要素の画素のバイト一致**。⭐ **レビューが before/after の2ツリーを並走させて自分で測り直した**（⚠️ **配信ルートの切り替わりを「既知の差」で検算済み**） |
| 2 | ⭐ **空回りしていないことが陰性対照で示されている**——**V5: 着手前のツリーでは 34箇所すべてが追従しない**（0/34）。⭐ **「動いたから合格」ではない** |
| 3 | ⭐ **足した検査2本が、破壊試験12ケースで「壊すと必ず鳴る」ことを示している**（レビューが独立に3ケース追加）。⭐ **両辺が別ファイル**（`CSS_GENERATORS` ↔ 実走査／`var()` ↔ `:root`） |
| 4 | ⭐ **生成物の同期が最強の形で確認されている**——`git archive HEAD` を作業ツリー外に展開して再生成し、**`diff -rq` が0件**（457×2ページ＋一覧＋CSS＋JS が**バイト一致**）。⚠️ ⭐ **「ファイル名の集合」ではなく中身での照合**なので、**内側に同居するAPIドリフトも原理的に0** |
| 5 | ⭐ **ユーザーが実ログインで U1〜U12 を確認し「全部OK」**（U3 は無効・U10 は個人戦で） |
| 6 | ⭐ **破壊試験の復旧に `git checkout` を使っていない**（#108 の累積を避け、`cp` で戻して毎回 `git status --short` が空であることを確認） |

## 5. ⭐ レビューの推奨指摘（S）の扱い

| # | 指摘 | 扱い |
|---|---|---|
| **S3-1** | 検査 (iii) がスペース区切り記法を見ていない | ⭕ **設計書に限界⑥として明記＋ #118 で塞ぐ**（→ 論点5） |
| **S1-1** | `.controls` の hex 直書きが、トークン参照にした宣言を殺している | ⭕ **#118 の §1-3 に別項目として入れた**（→ 論点4） |
| **S1-2** | `CSS_GENERATORS` は「生成元 → CSS 1枚」の対応。**1つの生成元が違う `:root` の CSS を2枚書き出す**ようになると片方の基準でしか照合されない | ⭐ **起票しない。** **現状そのような生成元は無く**、⚠️ **起きてもいない構造を先回りして検査すると、検査のほうが先に腐る**。⭐ **迷子検査が「増えた側」にも効く**（レビュー追加R2で実証）ので、**新しい生成元が現れた時点で必ず人が `CSS_GENERATORS` を触る**＝**そのときに気づける** |
| **S1-3** | 新検査2本が `CLAUDE.md` に無い | ⭕ **この承認と同じコミットで書いた**（→ 論点6） |

## 6. ⭐ 派生issueの要否

| | |
|---|---|
| **#118 を起票した** | ⭐ **`docs/design/105-アクセント色の直値/派生起票案_2026-09-24_hex直書きのトークン化.md` を投函**（`enhancement` / `着手可` / `小タスク`）。**論点4（S1-1）と論点5（S3-1）もこの issue に合流させた** |
| ⚠️ **投函前の実測** | ⭐ **CLAUDE.md の規約どおり、投函の直前に4項目を測り直して再現させた**。⚠️ ⭐ **起票案 1.0 の数字は1箇所が実態と違っていた**——**腐ったフォールバックは「1つ」ではなく 3トークン / 15箇所**（`--border` ×10・`--panel-2` ×4・`--panel` ×1）。⭐ **`--border` は値が古いのではなく形が変わっている**（hex → `rgba(255,255,255,.11)`）ので、**フォールバックを検査する案は hex 同士の比較では足りない**。⭐ **起票案を実測値で書き直してから投函した** |
| **ほかに起票したもの** | ⭐ **無い**（S1-2 は上記のとおり見送り） |

## 7. ⚠️ 残っている確認（⭐ **承認を妨げないもの**）

| # | 内容 | 行き先 |
|---|---|---|
| 1 | **`color-mix` 非対応ブラウザでの落ち方**は未検証（設計書 §4-2 の実測に依拠） | ⭐ **追わない。** トップ・デッキ構築・大会ページ・カードページは**すでに全部 `color-mix` を必須にしている**（実測）ので、**本タスクは新しい依存を1つも増やしていない** |
| 2 | **本番（Pages）での確認**は未実施 | ⭐ **ユーザーの push 判断のあと**（`push待ち`） |

## 8. ⭐ 次の状態

- ラベル: **`設計確認待ち` を外し `push待ち` を付ける**（⚠️ **クローズしない**）
- ⚠️ ⭐ **`push待ち` の担当はセッションではなくユーザー**。⭐ **どのセッションもこの issue に着手しない**
