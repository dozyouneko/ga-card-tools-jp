# docs/design/ の構成

設計成果物は**対処内容(タスク)ごとのサブフォルダ**で管理する。

```text
docs/design/
├── <issue番号>-<タスク名>/   ← タスクごとの設計書・モック
│   ├── ○○_設計.md            (改版履歴表つき。実装済み/実装待ちを明記)
│   └── ○○_モック.html        (必要な場合のみ。自己完結HTML)
└── リリース確認/              ← リリース時の確認資料(日付プレフィックスつき)
    └── YYYY-MM-DD_○○.md
```

## 現在のフォルダ

| フォルダ | 対処内容 |
|---|---|
| `12-デッキ統計タブ/` | #12 デッキ構築ツールの統計タブ(設計書第8版+モックv6) |
| `16-フォーマット検索/` | #16 カード検索のフォーマット絞り込み |
| `リリース確認/` | 本番スモークテスト チェックリスト等 |

## 今後のタスク

### P26新カード3枚の和訳対応(未着手・issue未採番)

2026-07-18のAPI実測(全2,240件)で判明した、7/16以降にAPIへ追加されたP26(2026プロモ)の新カード3枚:

| カード名 | タイプ | セット/番号 |
|---|---|---|
| Dinah, Lost Spirit | CHAMPION | P26 #009 |
| Transcendental Rite | REGALIA/ITEM | P26 #000 |
| Tristan, Ascendant Shadow | CHAMPION | P26 #001 |

現状調査(2026-07-18):

- `data/tl/` に3枚のslugは存在しない(p26.jsも未作成。既存P26カードは再録元セットのtlファイルでカバーされているが、この3枚は新規カードのため受け皿がない)
- 個別カードページ `cards/<slug>/` も3枚とも未生成(既存P26カードのfluffy-shopkeep等は生成済み)
- `sets/p26/index.html`(7/13生成)にも3枚は未掲載
- sitemap.xml(7/13生成)にも未掲載

作業範囲(想定): ①和訳データ追加(置き場所は設計時に決定) ②カードページ生成(scaffold) ③sets/p26ページ更新 ④sitemap再生成

### バックログ(既存設計書の「見送り」項目)

- コストカーブ表示: `12-デッキ統計タブ/統計タブ_設計.md` 参照(集計関数に1ブロック追加で対応可)
- 「専用カード」絞り込み: `16-フォーマット検索/フォーマット検索_設計.md` 参照(サーバー側で表現不可のため見送り。優先度低)

## 運用メモ

- 設計はVSCode拡張側セッション、実装はターミナル側セッションが担当(詳細はリポジトリ直下のCLAUDE.md)
- `docs/` 配下は本番配信対象外(`functions/docs/[[path]].js` が全パス404を返す)
- `tmp/` はgitignoreのため設計成果物を置かない
