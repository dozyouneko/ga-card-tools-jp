**対象: 「preferredArtIndexの二重定義」**（→ 完了）

# 承認 — #97 `preferredArtIndex()` の一本化 ＋ デッキ構築の詳細モーダルが版の絞り込みに追従しない件（2026-09-24）

- 設計書: `docs/design/97-preferredArtIndexの二重定義/preferredArtIndexの二重定義_設計.md`（→ **1.1** に改版）
- 実装: **`e2ccf291`**（4ファイル・生成物の差分0）
- レビュー: **合格**（2026-09-24・M1〜M7 すべて ✅・**破壊試験 B-1〜B-5 をレビュー担当が自分で壊して測り直し**）
- ⭐ **実物確認（設計書 §10 の U1〜U6）: 実施済み**（ユーザー・2026-09-24。→ §2）
- ⚠️ ⭐ **クローズはしない。** ラベルを **`設計確認待ち` → `push待ち`** に付け替える
  （**本番公開の可否はユーザーの判断**。push が済んだらラベルを外してクローズする）
- ⭐ **1行目は `（→ 完了）`** ＝ **このタスクはこの承認で終わり**。残っているのはユーザーの push 判断だけ。
  ⚠️ **確認事項から出た派生3件は「#97 の続き」ではなく別issueへ出した**ので `（→ 継続）` にはしない（→ §4・§5）

---

## 1. ⭐ 承認したのはユーザー（設計担当は記録係）

⭐ **2026-09-24、ユーザーが実物を操作したうえで承認した**（リードが直接聞き取り）。原文:

> **「U1〜U6まで確認しすべてOKです。」**

本書はその**記録**であって、⚠️ **設計担当が可否を判断したものではない。**

## 2. ⭐ 実物確認（U1〜U6）の結果 — **全項目 OK**

| # | 見たところ | 結果 |
|---|---|---|
| **U1** | デッキ構築・**エキスパンション**で絞って 🔍 → 結果タイルを開く（**本タスクの本体**） | ✅ **OK** |
| **U2** | デッキ構築・**レアリティ**で絞って 🔍 → 結果タイルを開く | ✅ **OK** |
| **U3** | U2 のあと **🔍 を押さずに**左ペインを変え、同じタイルを開く（＝タブの条件で開く） | ✅ **OK** |
| ⭐ **U4** | **デッキに入っているカード**（ゾーンのタイル）を開く | ✅ **OK**（→ §5） |
| **U5** | 検索タブを2つ作り、別々の条件で**行き来**する | ✅ **OK** |
| **U6** | トップで絞り込み → タイル → **`＋🖨️`** → 印刷ページ（回帰） | ✅ **OK** |

⭐ **U1・U2 が OK ＝ 直したかった症状（デッキ構築のモーダルが版の絞り込みに追従しない）が実物で直っている。**
⭐ **U3 が OK ＝ 「検索結果のタブ化」の V22（左ペインの「いま」で絵柄が決まる）が再発していない。**

## 3. ⭐ 承認の中身 — 何をもって「設計の意図を満たした」と見たか

⚠️ **動作の再確認はしていない**（レビュー担当の判定コメントに証跡がある）。設計担当が見たのは**設計意図との適合**:

| 設計の意図 | 満たされたと見た根拠 |
|---|---|
| **規則が本当に1本になったか**（単に「2箇所とも動く」ではなく） | ⭐ **破壊試験 B-1** — 共有ファイルの**1行を反転すると、トップとデッキ構築の計8点（タイル・モーダル × 4条件）が同時に**変わり、**取り残しゼロ**。⭐ **これが「1本である」ことの決定的な証拠**で、レビュー担当が**自分で壊して**再現した |
| **条件の取り出しも寄せたか** | B-2 で `rarities` を落とすと rarity 側4箇所だけが崩れ、set 側は無傷＝**キーごとに独立して効いている**（⚠️ ただし**検査は届いていない**＝§4-A） |
| **変えてはいけない経路を変えていないか**（§1 の A・C） | V4-a / V4-b / V4-c が全て不変。⭐ **V4-a は「検索結果のモーダルを開いた直後」に測っており、`detailArtCond` が持ち越されないことまで見えている**。⭐ **U4（実物）でもユーザーが違和感を示さなかった** |
| **検査自身が効いているか** | B-3〜B-5 が全て exit 1、復旧後に exit 0 に戻るところまで確認済み（#110 で確立した破壊試験の流儀） |
| **スコープ** | 変更4ファイル＝設計書 §12 と完全一致。`shared/js/card-detail.js` 無変更・生成物の差分0・`docs/` 無変更（§9 の「直さない」どおり） |

⭐ **逸脱3点は3点とも採用する**（レビューの判定に同意）:

| # | 逸脱 | 採否 | 設計担当の見解 |
|---|---|---|---|
| **1** | 設計 §5 の「式を2か所に直書き」をやめ、`app.js` に薄いラッパー `artIndexOf(imgs)` を置いた | ⭕ **採用**（⭐ **設計より良い**） | ⚠️ **設計書の書き方のほうが誤っていた**——同じ式を2か所に書くのは**本タスクの趣旨（二重定義の解消）に逆行する**。⭐ ラッパーは規則を持たず共有関数を呼ぶだけで、B-1 で**トップのタイルとモーダルが同時に変わる**ことまで実測されている |
| **2** | `app.js` の `const { setPrefixes } = …` を削除（設計書に記載なし） | ⭕ **採用** | 旧 `preferredArtIndex()` が唯一の利用者。残すほうが誤り（未使用の束縛）。`grep` でヒット0を確認済み |
| **3** | 行動フィクスチャ6組を `set`→F2 / `rarity`→F3・F6 / 共通→F1・F4・F5 に振り分けた | ⭕ **採用** | 設計 §6-2 の **fail-closed**（「そのキーだけで先頭以外が選ばれる」ケースを各キーが持つ）を機械的に言うために必要。**内容・期待値・合計6組は設計どおり**で、B-5 で実際に fail-closed が効いた |

## 4. ⚠️ ⭐ 推奨指摘（S）の扱い — **2件とも受け、#116 として起票した**

⭐ **設計担当が自分で再現させてから起票した**（CLAUDE.md の規約。測定は 2026-09-24・原本は退避して `cp` で復旧・各ケース後に `git status --short` が空であることを確認）。

### A. S3-1 — ⚠️ ⭐ **`artCondOf()` に検査が届いていない（無言の fail-open）**

`artCondOf()` のループから `rarities` だけを落とす（`ART_COND` は無傷なので (b) の双方向照合は通る）と:

| 測ったもの | 結果 |
|---|---|
| `npm run validate` | ⚠️ ⭐ **exit 0**。成功行は `… ／条件キー 2項目（set / rarity）／行動フィクスチャ 6組 …` と**何事も無いかのように出る** |
| `artCondOf({rarity:["8"]})` | ⚠️ **`{"prefixes":[]}`** |
| `preferredArtIndex(imgs, artCondOf(els))` | ⚠️ **0**（本来 **1**） |
| `preferredArtIndex(imgs, {rarities:["8"]})` | **1**（⭐ **フィクスチャが叩いているのはこちらだけ**なので緑になる） |

- **画面では4箇所が同時に壊れる**（レビューの B-2 が実測）のに、**検査は何も言わない**
- ⚠️ ⭐ **これは #93 が問題にしたのと同じ型**（「選んでも1件も落ちないのに警告も注記も出ない」）。
  ⭐ **このリポジトリが自分で「無言の fail-open は検査にする」と決めた型なので、推奨止まりにしない**
- ⭐ **設計の落ち度として受ける**——§6-2 の (c) が対象を `preferredArtIndex()` だけに定めたのが原因で、実装は設計どおり

### B. S1-1 — 定義の一意性検査が**アロー関数形を拾わない**

`tools/deck-builder/app.js` の末尾に `const preferredArtIndex = (imgs, cond) => 0;` を足しても **exit 0**、
しかも成功行は **`定義 1ファイル`** のまま（実測）。⚠️ **2つ目の定義が実在するのに「1本」と報告する。**

- ⭐ **`function` 形での再定義・改名・削除は今でも exit 1**（B-3 / B-4）なので、**穴は「書き方を変えたコピー」に限る**
- ⭐ **A と同じ検査ブロックなので1つのissueにまとめた**（⚠️ **B だけ直して A を残さないこと**——**画面が壊れるのは A**）

### S1-2（コード品質）

指摘なし。⭐ **設計担当からも追加なし。**

## 5. ⭐ P1（U4 の対象）の扱い — **新規起票しない。#107 に追記した**

設計書 §10 は「**U4 で違和感が出たら §2-3 の別件として派生起票する**」と事前に決めていた。
⭐ **ユーザーは U4 を OK と答え、違和感を示さなかった**。⚠️ **事前に決めた基準に従い、新規issueは立てない。**

⚠️ ⭐ **ただし「測って分かっている食い違い」を記録なしにはしない**ので、**症状が最も近い #107 にコメントで追記した**
（#114 の U3 を #102 に取り込んだのと同じ扱い）:

- 症状: デッキゾーン／共有画面の**タイルは保存した版（`art_image`）**なのに、**モーダルは先頭の版**で開く（3者が独立に再現済み）
- ⚠️ ⭐ **#107 の本体（`firstEdition()` の非決定性）を直してもこの食い違いは残る**——原因が別なので、
  **#107 に着手する人が「どの面がどの版を出すか」を1つの方針で決められるように**、同じ場所に置いた
- ⚠️ ⭐ **#97 で直さなかったのは正しい**。ここに検索条件を流し込むと
  **デッキのカードの詳細が左ペインの検索条件で別の絵柄になる**という**新しい不具合**になる（設計書 §2-2・V4 がこれを守っている）

## 6. ⭐ P2 の扱い — **#117 として起票した**

`scripts/validate.mjs` の `stripJs` が行コメント内の `data/tl/*.js` の `/*` を誤って対にし、
**`app.js` 271行（約30%）＋ `tools/deck-builder/app.js` 338行（約10%）＝ 合計 609行**を検査から消している。

- ⭐ **設計書 §2-4 より穴は大きかった**（承認時の再実測で**デッキ構築側にも同型の穴**が見つかった。→ 設計書 1.1 §2-4）
- ⚠️ ⭐ **消える範囲に `app.js` の `GA_CARD_SEARCH.create({ … })` の options が丸ごと入っている**＝
  **検査①が `onRemoveOne:` を探すべき唯一の場所を、検査が見ていない**
- ⭐ **再現の形**: 同じ1行を**範囲の内に置くと exit 0**（成功行は `渡しているのは 0ファイル（なし）`）／**範囲の外に置くと exit 1**
- ⭐ **今日は誤判定していない**（25ファイルで生テキストと `stripJs` 後の判定が全件一致）。
  ⚠️ **これを本文に書いたのは #115 で確立した書き方**——「いま何が起きるか／何は起きないか」を書かないと、**着手者が再現できず腐る**

## 7. 派生issueの要否（まとめ）

| # | 内容 | 判断 |
|---|---|---|
| **S3-1 ＋ S1-1** | #97 で足した検査の射程の穴2つ | ⭐ **#116 として起票**（`enhancement` / `着手可` / `小タスク`） |
| **P2** | `stripJs` の誤対応で609行が検査から消えている | ⭐ **#117 として起票**（`bug` / `着手可` / `小タスク`） |
| **P1** | デッキゾーンのタイルとモーダルで版が食い違う | ⭐ **起票しない**。**#107 にコメントで追記**（理由は §5） |
| **P3** | ブラウザ側の代表画像の非決定性（487枚） | ⭐ **既存 #107 の範囲**。本タスクからは何もしない |

## 8. ⚠️ この後

1. ラベルを **`設計確認待ち` → `push待ち`** に付け替える（⚠️ **クローズしない**）
2. **push はユーザーの判断**。⚠️ **`e2ccf291` は `app.js` / `shared/` / `tools/` / `scripts/` を含む＝本番配信物**なので、
   `docs/` 限定の例外には当たらない
3. ⚠️ ⭐ **push するときは「一緒に出るもの」を必ず提示する**（`git diff --stat <本番位置>..HEAD -- . ':(exclude)docs/*'`）——
   `push待ち` が他にも溜まっている
