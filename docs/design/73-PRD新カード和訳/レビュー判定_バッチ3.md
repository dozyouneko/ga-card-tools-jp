# #73 レビュー判定: バッチ3（34枚）— ⭐ 全254枚完了

⚠️ **本来はissue #73 のコメント。GitHubアカウント停止（`gh` が403）のため、代替としてこのファイルに記録する。**
復旧後、この本文をそのまま
`gh issue comment 73 --body-file docs/design/73-PRD新カード和訳/レビュー判定_バッチ3.md` で投稿する。
⚠️ ラベル操作は未実施（本来は `レビュー待ち` → **`設計確認待ち`**）。

- 判定日: 2026-08-19
- レビュー対象コミット: **`9b472a88`**（実装本体・49ファイル）／`6dac7a76`（報告のみ・ドキュメント）
- 参照した設計書: `docs/design/73-PRD新カード和訳/PRD新カード和訳_設計.md` **第8版**（§2-2〜§2-8 / §13）
- 証跡: `tmp/review/73/`（gitignore）

---

## 判定: **合格**

必須基準 M1〜M7 はすべて充足。⭐ **開発担当の報告値はすべて、レビュー担当が独立に再測定して一致を確認した**
（報告の引き写しではない）。推奨基準の指摘 3件は合格を妨げない。

| 基準 | 結果 | 証跡 |
|---|:--:|---|
| **M1** 設計適合 | ✅ | §13-3 の V1〜V6 全項目を再測定して一致（下の「検証項目との対応」） |
| **M2** 実駆動 | ✅ | Playwright で **PC 1280px / 375px の両方**を操作。`tmp/review/73/prdp-{pc,375}.png` `b3-modal-terms-{pc,375}.png` `card-{virelai,fulgurite}-{pc,375}.png` `set-prdp-{pc,375}.png` |
| **M3** 回帰 | ✅ | `npm run validate` **exit 0**（`tmp/review/73/validate.log`）。主要7画面を両幅で巡回し **HTTP 200 / pageerror 0 / 横スクロール 0** |
| **M4** 生成物の同期 | ✅ | validate の `generated JSON up to date`。`cards/` `sets/` 全HTMLを走査して**旧英語名のリンク文言 0件**。索引 `2516slug` |
| **M5** 静かな劣化がない | ✅ | 本コミットは検査を追加していない。既存検査の「両辺が別源」であることを確認（下記 M5 の欄） |
| **M6** 公開安全性 | ✅ | 49ファイルすべて `M`（追加・削除・リネーム 0）。`_headers` `404.html` `functions/` に変更なし。秘匿情報の混入なし |
| **M7** スコープ | ✅ | 変更は `data/tl/` `data/` 生成物 `cards/` `sets/` のみ。`data/translations.js` **差分0**・`TRANSLATION.md` 無変更 |

---

## 設計書 §13-3 の検証項目との対応（すべてレビュー担当が再測定）

### V1. 未訳数（`node scripts/scaffold.mjs <prefix>` を7prefix分・**公式APIに直接問い合わせ**）

| prefix | 期待 | **レビュー実測** | 判定 |
|---|---:|---:|:--:|
| PRD | 0 | **0**（fetched 232） | ✅ |
| PRD 1st | 0 | **0**（fetched 27） | ✅ |
| PRDG | 0 | **0**（fetched 21） | ✅ |
| PRDP | 0 | **0**（fetched 14） | ✅ |
| PRDSD | 0 | **0**（fetched 61） | ✅ |
| PRDDP | 0 | **0**（fetched 39） | ✅ |
| PRDEVP | 0 | **0**（fetched 33） | ✅ |
| **ユニーク残** | **0** | **0** | ⭐ **全254枚の翻訳完了を確認** |

証跡: `tmp/review/73/scaffold.log`。⭐ スナップショットではなく**API現物**に対して0。

### V2. ファイルごとのエントリ数（`grep -cE '^  "[a-z0-9-]+": \{'`）

| ファイル | 期待 | **レビュー実測** | 判定 |
|---|---:|---:|:--:|
| `data/tl/prdp.js` | 14 | **14** | ✅ |
| `data/tl/prdsd.js` | 11 | **11** | ✅ |
| `data/tl/prddp.js` | 10 | **10** | ✅ |
| `data/tl/prd-1st.js` | 8 | **8** | ✅ |
| `data/tl/prd.js` | 211（不変） | **211** | ✅ |

⭐ さらに、`9b472a88` で**追記された34slug**を抽出して §13-5 の全件表と `diff` → **完全一致（差分0）**。
⚠️ **`prd.js` にカードは1枚も追加されていない**ことを diff で確認（変更は `fulgurite-coordinator` の
`name` / `effect` 各1行＋冒頭コメント2行のみ）。

### V3. 生成物の整合

| 項目 | 期待 | **レビュー実測** | 判定 |
|---|---|---|:--:|
| `npm run validate` | exit 0 | **exit 0**（`all checks passed.`） | ✅ |
| 索引の行 | `2516slug` | **`card-meta-index sort keys OK — 2516slug / level 133 / power 1006 / life 858 / cost_memory 515 / flip 22`** | ✅ |
| `check-terminology.mjs "PRD 1st"` | 0件 | **0件**（対象27件） | ✅ |
| `check-terminology.mjs PRDP` | 0件 | **0件**（対象14件） | ✅ |
| `check-terminology.mjs PRDSD` | 0件 | **0件**（対象61件） | ✅ |
| `check-terminology.mjs PRDDP` | 0件 | **0件**（対象38件） | ✅ |

⭐ `check-terminology.mjs` は**実行時に公式APIから英語原文を取得**しており、日本語側（ローカル）と
**別源で突き合わせている**ことをレビュー実行時のログ（`英語原文を取得: N件`）で確認した。

### V4. `data/translations.js` の差分

- `git show --name-only 9b472a88 | grep translations.js` → **0件**（コミットに含まれていない）✅
- 作業ツリーも clean（`git status --short` が空）✅
- ⭐ **`terms` を1件も足していないのに「レベルロック」の解説が出る**ことを V5-3 で実駆動確認した

### V5. 画面表示（レビュー担当が自分で操作・**PC 1280px と 375px の両方**）

| # | 期待 | **レビュー実測（PC / 375px）** | 判定 |
|---|---|---|:--:|
| 1 | トップで `.asphodel/paradise: Pantheon（PRDP）` に絞り **全14件**・英語のままのタイル0 | 選択肢名 **`.asphodel/paradise: Pantheon（PRDP）`**。ステータス行 **「14 件を表示」**、タイル **14**、カード名を全数走査して **ASCIIのみの名前 0件**、**`未翻訳` バッジ 0件**、**横スクロール 0**。両幅で同一結果 | ✅ |
| 2 | `cards/fulgurite-coordinator/` の `<title>` | **`フルグライトの指揮者 \| Fulgurite Coordinator - Grand Archive 日本語カードDB`**（期待値と一字一句一致。curl と Playwright の両方で確認） | ✅ |
| 3 | `lesser-boon-of-provocation` で「レベルロック」がハイライト＋用語解説 | `.term-hl` に **`レベルロック`**（`color: rgb(110,168,254)` / `font-weight: 600`）。見出し **「この効果に出てくる用語」** の下に **`レベルロック（Level Locked N）`＋「あなたのチャンピオンの基本レベルがN以上でなければ、このカードは起動できない。」**。両幅で同一 | ✅ |

- 実測した14件の表示名（両幅で同一・順序も同一）: 絆の大いなる恩恵 / 群れの大いなる恩恵 / プロクシアの大いなる恩恵 /
  ローゼンの大いなる恩恵 / エリュシオンの血の小さな恩恵 / 群れの小さな恩恵 / フラクタルの小さな恩恵 /
  滋養の小さな恩恵 / 浸透の小さな恩恵 / 挑発の小さな恩恵 / プロクシアの小さな恩恵 / 避難の小さな恩恵 /
  剣聖の小さな恩恵 / ヴィレライの小さな恩恵
- `requestfailed` は `cloudflareinsights.com/cdn-cgi/rum` のみ（localhost では常に出る既知のもの）。**pageerror は0件**
- 証跡: `tmp/review/73/prdp-pc.png` `prdp-375.png` `b3-modal-terms-pc.png` `b3-modal-terms-375.png` `v5.log`

### V6. 意図しない差分が無いこと

| 内訳 | 期待 | **レビュー実測** | 判定 |
|---|---:|---:|:--:|
| カードページ | 36 | **36**（`comm` で照合し、訳34 ＋ `fulgurite-coordinator` ＋ `elysian-test-subject` の**過不足なし**） | ✅ |
| セットページ | 5 | **5**（`prd` `prd-1st` `prdp` `prdsd` `prddp`） | ✅ |
| データ | 8 | **8** | ✅ |
| **計** | **49** | **49**（全て `M`。**追加・削除・リネームは0**） | ✅ |
| `sets/evp/` | 差分なし | **差分なし**（＝APIドリフト混入なし） | ✅ |

⭐ **セットページ5枚の差分を「タグ単位の語差分」まで分解して全数点検した**（`tmp/review/73/` の作業スクリプト）:

- `sets/prd/` … **`フルグライト・コーディネーター` → `フルグライトの指揮者` の1箇所のみ**
- `sets/prd-1st/` `sets/prdp/` `sets/prdsd/` `sets/prddp/` … **追加された訳名と `cp-en-inline` の挿入のみ**
  （`sets/prdsd/` は上記に加えて `fulgurite-coordinator` の改名1箇所。同セットにも収録されているため正常）
- ⭐ **無関係な語の書き換え・並び替え・画像URLの入れ替えは1件も無かった**（#71 型のAPIドリフトなし）

`cards/elysian-test-subject/index.html` の波及も1行で、「関連カード」欄の
**`Gencode Womb` → `ジェンコードの胎`** のみであることを確認した。

#### セットページの訳済み件数

| ファイル | 期待 | **レビュー実測 `cp-en-inline`** | `<tr>` −ヘッダ1 | 判定 |
|---|---:|---:|---:|:--:|
| `sets/prdp/` | 14 | **14** | 14 | ✅ |
| `sets/prdsd/` | 61 | **61** | 61 | ✅ |
| `sets/prddp/` | 39 | **39** | 39 | ✅ |
| `sets/prd-1st/` | 27 | **27** | 27 | ✅ |
| `sets/prd/` | 232（不変） | **232** | 232 | ✅ |

⭐ **PRD系の全セットページで「訳済み ＝ 総カード行」**を確認した。

---

## ⭐ フレーバー取りこぼしの独立検証（⚠️ バッチ2の差し戻し理由）

開発担当の「**非nullは17枚、うち `virgil-altered-future` は両側とも空白のみで実質16枚を16/16訳出**」という
主張を、**2つの独立した入力**で検証した。

### ① スナップショット（`tmp/api-cache/cards-snapshot.json` / 取得 2026-08-18T14:49:02Z）

### ② ⭐ **公式APIから34枚を新規に取り直して**（`GET /cards/<slug>` × 34・成功34/失敗0）

| 指標 | 期待（報告） | **① スナップショット** | **② 公式API再取得** | 判定 |
|---|---:|---:|---:|:--:|
| 対象カード | 34 | 34 | 34 | ✅ |
| `flavor` が非null（両側のどちらか） | 17 | **17** | **17** | ✅ |
| `trim()` 後に本文が残る＝実体あり | 16 | **16** | **16** | ✅ |
| 実体16枚のslug集合 | — | **①と②で完全一致** | 同左 | ✅ |
| `data/tl-effects.json` の `f` に訳が入っている | 16/16 | **16/16**（欠落0） | — | ✅ |
| 実体が無いのに訳が入っている（過剰） | 0 | **0** | — | ✅ |
| 34枚の効果文の訳（`e`） | 34/34 | **34/34**（英語原文が空のカードは0枚） | — | ✅ |

- ⚠️ 判定に使った式は設計書の指示どおり **`card.flavor || (card.editions||[]).map(e=>e.flavor).find(Boolean)`** と、
  比較用の **`trim()` 版**の**両方**。両者の差は `virgil-altered-future` の1枚だけで、
  **その1枚は `card.flavor` も `editions[0].flavor` も `" "`（半角スペース1つ）**であることを実データで確認した
  → ⭐ **開発担当の主張は正しい。取りこぼしは0件。**
- ⚠️ `cards/virgil-altered-future/index.html` の `<p class="cp-flavor"> </p>` は
  **`9b472a88^` の時点で既に同一**（`git show` で確認）＝本コミットが作った劣化ではない。**スコープ外の判断は妥当**

---

## 訳文の品質確認（レビュー担当が原文と突き合わせ）

### Boonカード12枚 — §2-8 のパターン適合

⭐ **`data/tl/` 全ファイルを機械走査**して `(greater|lesser)-boon-of-*` を全数検査した:

- **Greater 17枚がすべて「◯◯の大いなる恩恵」／Lesser 44枚がすべて「◯◯の小さな恩恵」＝ 違反 0件**
- ⚠️ **`Proxia` は Greater / Lesser の2枚とも「プロクシア」で一致**（設計書の最重要指示）✅
- `Rosen` → **ローゼン**（既訳 `rdop.js`「ローゼンの小さな恩恵」と一致）✅
- `Flock` → **群れ**（バッチ1「群れの大いなる恩恵」と一致）✅
- `Fractals` → **フラクタル**（§2-7）✅ ／ `Elysian Blood` → **エリュシオンの血**（§2-2）✅

### ⭐ `Coordinator` 3枚の統一

| slug | ファイル | 訳名 | 判定 |
|---|---|---|:--:|
| `fulgurite-coordinator` | `prd.js` | **フルグライトの指揮者** | ✅ |
| `rumble-coordinator` | `prd.js` | 乱闘の指揮者 | ✅ |
| `surged-coordinator` | `prdsd.js` | **奔流の指揮者** | ✅ |

⭐ **リポジトリ全体を grep して、旧訳「フルグライト・コーディネーター」が残っているのは
`data/tl/prd.js` の改名理由を書いたコメント1行だけ**であることを確認した（生成物・HTMLには0件）。

### §2-8 のその他の指定

| slug | 指定 | 実装 | 判定 |
|---|---|---|:--:|
| `shieldroid` | Droid＝カタカナ音写 | **シールドロイド** | ✅ |
| `veltech-presidential-card` | §2-6 ブランド＋「・」＋カタカナ | **ヴェルテック・プレジデンシャルカード** | ✅ |
| `jovian-hilt-x-ultra` | §2-6「・Xウルトラ」 | **ジョビアン・ヒルト・Xウルトラ** | ✅ |
| `virgil-altered-future` | `Virgil → ウェルギリウス` | **ウェルギリウス、改変された未来** | ✅ |
| `agnis-signet` / `banner-of-ares` | 既訳 アグニ／アレス | **アグニの印章** / **アレスの軍旗** | ✅ |
| `magus-initiate` | 魔導士＋秘儀参入者 | **魔導士の秘儀参入者** | ✅ |

### 効果文の原文突き合わせ（抜粋10枚を全文照合）

`greater-boon-of-proxia` `lesser-boon-of-refuge` `lesser-boon-of-provocation` `lesser-boon-of-nourishment`
`greater-boon-of-rosen` `lesser-boon-of-elysian-blood` `virgil-altered-future` `crimson-vein`
`surged-coordinator` `capacitance-x-psycho` を英語原文と1文ずつ突き合わせ、
**訳抜け・意味の反転・太字マークの欠落は0件**だった。

- `**boon**` → `**恩恵**`、`**First Boon**` → `**ファーストブーン**`（既訳 `greater-boon-of-luxera` に一致）、
  `[REST]` → `[レスト]`（半角）、`***Deluge 3*** —` → `**デリュージ3**——` など、
  §2-3・§13 の記号規則をいずれも守っている

---

## M5（静かな劣化がない）の確認内容

本コミットは**検査ロジックを1行も追加していない**（訳データと生成物のみ）。既存の検出経路について、
「両辺が同源で常に通る」形になっていないことを次のとおり確認した:

| 検出経路 | 両辺 | 確認方法 |
|---|---|---|
| `validate` の `generated JSON up to date` | **`data/tl/*.js` から再計算した値** vs **コミット済みの `tl-*.json` の実ファイル** | `scripts/validate.mjs:86-106` を読み、`buildTlJson()` の結果と `readFileSync` の中身を**バイト比較**していることを確認 |
| `check-terminology.mjs` | **実行時に公式APIから取得した英語原文** vs **ローカルの日本語訳** | レビュー実行時のログ `英語原文を取得: 27/14/61/38件` で、ネットワーク取得が実際に走っていることを確認 |
| `card-meta-index` の内部整合 | 索引ファイル自身の構造検査 | validate が `2516slug` を報告して exit 0 |

⚠️ 既存の弱点（フレーバー抽出式が空白文字列を truthy として拾う）は**今回のデータでは実害0**であることを
上の独立検証で確認済み。恒久対処は S3-1 として下に記す。

---

## 推奨基準の指摘（合格を妨げない）

### S1-1 `非戦闘ダメージ` / `スロースピード` — セット内に食い違いが残る

- **該当**: `data/tl/prd-1st.js`（`capacitance-x-psycho`）／`data/tl/prdsd.js` ほか
- **レビュー実測**（コーパス全体）: `スロースピード` **13** vs `低速` **1**（`prd.js` の `divine-comedy`）／
  `非戦闘ダメージ` **16** vs `戦闘以外のダメージ` **2**（うち1件が `prd.js` の `extinguishing-synchron`）
- 開発担当は 5-B で**多数派に倒したことを開示**しており、判断としては妥当。
  ただし結果として**同一セット（PRD系）の中に2通りの表記が残っている**
- ⚠️ **既存訳の書き換えは開発担当のスコープ外**なので、**#12（翻訳精度向上）で扱うかどうかは設計担当の判断**とする
  （レビュー担当は起票しない）

### S1-2 `virgil-altered-future` のカードページに空のフレーバー段落が出る

- **該当**: `cards/virgil-altered-future/index.html` の `<p class="cp-flavor"> </p>`
- **`9b472a88^` と完全に同一**＝**本コミットが作った劣化ではない**ことを確認済み。触っていない判断は正しい
- 原因は公式APIの `flavor` が `" "` であること。恒久対処は S3-1 と同じissueで拾えるとよい

### S3-1 フレーバー抽出式が空白文字列を truthy として拾う（fail-open の穴）

- 開発担当の報告 6-C に同意する。**今回は実害0**（両側とも空白のため結果が変わらない）だが、
  「**トップレベルが空白・edition側に本文がある**」カードが将来出ると**本文を無言で落とす**
- ⭐ 症状が「エラーではなく、フレーバーが出ないだけ」なので**気づく経路が無い**。
  フォローアップissue（消費側の棚卸し）に含めるかは設計担当の判断

---

## 設計担当への確認事項（合否には含めない）

⚠️ いずれも**仕様判断**であり、レビュー担当が決めるものではない。承認時に扱いを決めてほしい。

1. **6-A `Surged Coordinator` → 「奔流の指揮者」**（開発担当提起）
   §2-8 は「◯◯の指揮者」までしか指定しておらず、`Surged` は委任範囲。既訳 `Surge → 奔流` に倒した判断は
   規約どおりだが、**本カードは電気的なサージ（`static` カウンター）**で、原文リマインダーも
   「アーケインエレメントのユニットが〜スタティックカウンターを取り除き」となっている。
   ⭐ **レビュー担当としては「合否を左右しない」と判断した**（既訳の先例に従っており、規約違反ではないため）。
   語感を優先して差し替えるなら設計担当の指示が要る
2. **6-B `Program cards`**（`virgil-altered-future`）— 実データに `PROGRAM` サブタイプが存在しない。
   「プログラムカード」という音写でよいか
3. **6-D の迷った訳名10件** — いずれも既訳の先例に当てて決めており、**規約違反は見つからなかった**。
   採用の可否は設計担当の判断
4. **S1-1 の表記ゆれ**（上記）を #12 に回すか
5. **S3-1 / S1-2** をフォローアップissue（消費側の棚卸し）に含めるか

---

## 残っている確認（push しないと確認できない項目）

⚠️ **push は要求しない。** 合否は**それ以外の項目で判定した**。

| 項目 | 状況 |
|---|---|
| 本番URLでの反映確認 | **未実施**。アカウント停止で push できない。⭐ 本コミットは**新規追加ファイルが0**（全49件が更新）なので、公開後の到達確認は**反復測定しかない**（CLAUDE.md「20秒間隔×18回」の手順） |
| デッキ構築ツールでの表示 | **未実施**。Discord OAuth が前提。⭐ ただし読む生成JSONはトップと同一（`tl-names.json` / `tl-effects.json`）で、**トップ・カード個別ページ・セットページの3経路で訳の表示を確認済み**。`/tools/deck-builder/` の**素の到達（200・pageerror 0・横スクロール 0）は両幅で確認した** |
| issueへの反映・ラベル付け替え | **未実施**（`gh` が403）。本ファイルが代替 |

---

## レビューで実行したコマンド（証跡）

```text
npm run validate                                     → exit 0 / 2516slug   （tmp/review/73/validate.log）
node scripts/scaffold.mjs "<7 prefixes>"             → untranslated 全て 0 （tmp/review/73/scaffold.log）
node scripts/check-terminology.mjs "PRD 1st"|PRDP|PRDSD|PRDDP → いずれも0件
git show --name-status 9b472a88                      → 49件すべて M
公式API GET /cards/<slug> × 34                        → flavor 非null 17 / 実体 16
Playwright（PC 1280px / 375px）                       → tmp/review/73/*.png
PORT=3100 npm run dev                                → ⭐ 検証後に停止済み
```

⚠️ レビュー担当は**作業ツリーを一切変更していない**（`git status --short` は空。
`scratch_ref_*` は gitignore 対象で、レビュー後に自分が作った分だけ削除した）。

---

## 次の工程

- **合格**につき、本来は `レビュー待ち` → **`設計確認待ち`**（最終承認とクローズは設計担当）
- ⚠️ **`closes #73` を含むコミットが未pushで溜まっている**。公開の可否はユーザー判断。
  push 時は `git pull --rebase origin main` を先に実行すること
