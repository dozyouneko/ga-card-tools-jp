# ⚠️ issue反映待ち（2026-08-19・GitHubアカウント停止のため未反映）

## 何が起きたか

**2026-08-19 の作業中に GitHub アカウント `dozyouneko` が停止された**。
同日の作業開始時（`c5777923` のpush）までは正常だったが、その後に発生。

```text
gh api user     → 403 {"message": "Sorry. Your account was suspended"}
git ls-remote   → remote: Your account is suspended. …403
gh auth status  → The token in /home/node/.config/gh/hosts.yml is invalid.
```

⚠️ **`gh` のトークン不良ではない。`git` も同じ 403 を返すためアカウント側の問題**
（トークンを再発行しても解決しない）。→ **ユーザーが <https://support.github.com> で異議申立てをする**。

## 影響

| できること | できないこと |
|---|---|
| ローカルのコミット・生成・検証・Playwright実駆動 | **push**（本番公開が完全に止まる） |
| 設計書の更新 | **issueのコメント・ラベル操作・閲覧**（＝役割間ハンドオフの記録） |

⭐ **作業内容は失われていない。** ユニット1の実装は `283804cb` にコミット済み・作業ツリーはclean。

## 復旧後に実行すること（この順で）

### 1. レビュー担当の判定コメント（ユニット1・合格）を投稿する

```bash
gh issue comment 73 --body-file tmp/review/73/judgement-283804cb.md
```

⚠️ **`tmp/` はgitignoreなので、この本文はコミットされていない。**
消えていた場合は、下の「判定の要旨」から再構成するか、レビューをやり直す。

### 2. 設計担当の承認コメントを投稿し、ラベルを戻す

本ファイルの「設計確認の結果」節を本文にする。

```bash
gh issue edit 73 --remove-label レビュー待ち --add-label 実装待ち   # ユニット2へ
```

### 3. 新規issueを起票する（設計担当の役割・下の「派生issue」節）

⭐ **2026-08-20 更新: 派生issueは設計書まで書き上げた。起票時はこの2件になる（3件ではない）。**

| 起票する内容 | 設計書 | 備考 |
|---|---|---|
| 手書き辞書の抜けと、その検出経路 | `docs/design/未採番-辞書の抜けと検出経路/` | ⚠️ **下の「派生issue」節の①は②に包含されていた**ので統合した。⚠️ **骨子の数字は過少**（subtypes は5種/68枚ではなく**10種/94枚**・terms は2種ではなく**15種**） |
| `editions[].flavor` のフレーバーが表示されない | `docs/design/未採番-フレーバー表示経路/` | 骨子の実測値（726/473/2）は再現。⚠️ ただし**修正案はそのままでは採れない**（版で文面が違う110枚があり、配列順で拾うと出力が非決定的になる） |

⚠️ **起票したら設計フォルダを `<issue番号>-…` にリネームし、設計書冒頭の注記を消す。**
リネーム後は次も直す:

```bash
grep -rn "未採番" docs/design/ CLAUDE.md   # 参照元の洗い出し（設計書冒頭・相互参照・本ファイル）
```

⚠️ **ラベルは `実装待ち`**（設計は完了している）。状態ラベルは `bug` / `着手可`、
規模は 辞書＝`小タスク` / フレーバー＝**要見積り**（生成物475ファイル）。

### 4. push の可否をユーザーに確認する

⚠️ **未pushコミットが溜まっている。** 日次cron（03:00 JST）もアカウント停止中は動かないため、
復旧後は `git pull --rebase origin main` で取り込んでから送ること。

---

## ユニット1（サブバッチA・44枚）の記録

- **実装コミット: `283804cb`**（`data/tl/prd.js` 35→79エントリ・純追記）
- **レビュー判定: 合格**（M1〜M7すべて充足）

### 判定の要旨（レビュー担当が独自に再測定した値）

| 検証 | 期待（設計書§10 A完了列） | 実測 |
|---|---|---|
| `scaffold.mjs` untranslated | PRD 132 / 1st 13 / G 0 / P 12 / SD 29 / DP 7 / EVP 0 | 全一致（ユニーク残 166） |
| `data/tl/prd.js` エントリ数 | 79（他は 3/2/1/3） | 全一致 |
| `npm run validate` | exit 0・`2350slug` | 一致 |
| `check-terminology.mjs PRD` | 0件 | 0件 |
| `cp-en-inline` | prd 100 / prdsd 32 | 一致（prd-1st 14・evp 22 据え置き） |
| 差分ファイル | HTML 48＋データ4 | 52（カード44＋波及2＋セット2＋データ4） |
| `data/translations.js` | **差分0** | **0** |

レビュー担当が追加で入れた検証: 追記slugが§11-Aの44枚と完全一致（diffゼロ）/ 索引の1,693slug差分は
**辞書idのシフトのみで意味変化0件** / 全2,494カードページ＋全セットページを走査して未反映0件 /
新規追加・削除・リネーム0。実駆動は PC1280・375px の両方で pageerror 0。

---

## 設計確認の結果: ✅ 承認（ユニット1完了・ユニット2へ）

### 承認の理由

設計書 §10 の V1〜V6 がすべて期待値と一致し、**レビュー担当が申告に依存せず独自に再測定**している
（issueが読めなかったため、むしろ独立性が高い状態で判定された）。訳文品質も §2-7 の命名ルールに適合し、
**Droid複合語5枚はユーザー確定訳と一字一句一致**。⭐ **APIドリフトの混入0件**
（`GA_SNAPSHOT_MAX_AGE_MIN=0` でスナップショットを固定した効果。#71 の再発防止が機能した）。

### 開発担当の確認事項への回答

| # | 確認事項 | 回答 |
|---|---|---|
| 6-A | 44枚を1ターンで書けた | ⭐ **B・C・Dも44枚単位で1ターンずつ**。B+C+Dの一括はしない（開発担当の判断を採用） |
| 6-B | 迷った11枚の訳語 | **全て承認**。既訳の先例に当てて決めており妥当 |
| 6-C | 固有名詞3語 | **確定**（設計書§2-7に追記・第6版）: `DisCorp`→**ディスコープ** / `Higher Plane`→**上位界** / `Disonian`→**ディソニア人** |
| 6-D | `Core Fractal` の衝突 | **「コアフラクタル」で確定**（§2-7の例外）。⚠️ バッチ1の効果文は**直さない**。なお同カードは §11-**B**（報告の「C」は読み違い） |
| 6-E | `subtypes` 辞書の欠落 | **バッチ2ではやらない**（§10 V4「`translations.js` 差分0」を維持）。**別issueに切り出す**（下記） |

### レビューの推奨指摘(S)の扱い

| 指摘 | 扱い |
|---|---|
| S1-1 `Core Fractal` の食い違い | ⭐ **採用**。6-D のとおり「コアフラクタル」で確定し設計書に明記した |
| S1-2 `DisCorp` の新造語・subtypes欠落 | ⭐ **採用**。訳語を確定し、subtypes は別issueへ |
| S1-3 `［クラスボーナス］` の全角/半角 | **対応しない**。`prd.js` 内は一貫しており表示も正常。コーパス全体の統一は #12（翻訳精度向上）の範疇 |

---

## 派生issue（復旧後に起票する）

### 新規: `translations.js` の `subtypes` にPRDの新サブタイプ5種が無く、68枚が英字表示になる

- **ラベル**: `bug` / `小タスク` / `着手可`
- **本文の骨子**:
  - `data/translations.js` の `subtypes` に **`DISCORP`(20枚) / `VELTECH`(31枚) / `RESONATOR`(9枚) /
    `KITCHEN`(6枚) / `PHONE`(2枚)** が無く、カードページ・詳細モーダルのサブタイプ行が**素の英字**で出る
  - ⚠️ **#40（`meta.sets` の追記漏れ）と同じ型**の抜け。「新セットが来たときに手で足す必要がある辞書」が
    `meta.sets` 以外にもあることが判明した
  - fail-openで破綻はしない（英字のまま出るだけ）
  - ⭐ **訳語は #73 で確定済み**: `DISCORP`→ディスコープ / `RESONATOR`→レゾネーター
    （`VELTECH`→ヴェルテック は §2-7 のブランド表記表）
  - ⭐ **検出経路が無い**点も併せて検討する（`npm run validate` に「実データのsubtypesが辞書に全部あるか」
    の検査を足せば #40 と同じく人を止められる）

---

# ユニット2（サブバッチB/C/D・132枚）の記録

## コミット

| コミット | 内容 |
|---|---|
| `a61b2abc` | サブバッチB（44枚）→ `prd.js` 79→123 |
| `5228daec` | サブバッチC（44枚）→ 123→167 |
| `0a071894` | サブバッチD（44枚）→ 167→**211**・索引と全カードページを再生成 |
| `d61a24ab` | 実装報告（issueの代替） |
| `62f2110a` | レビュー判定（**不合格 M1-1**） |
| `ef2a77db` | 差し戻し対応（フレーバー2枚） |
| `1a199120` | 実装報告の追記 |
| `19240c45` | 再判定（**合格**） |

## 経過: 1往復で合格

**不合格の理由は1件のみ**（M1-1）。`cato-meadows-channeler` と `fran-carmine-spark` の
**フレーバー2枚が未訳のまま本番HTMLに英語で出ていた**。

⭐ **構造上の取りこぼしで、手順の不備ではない**: この2枚だけフレーバーが
`editions[].flavor` ではなく**カードのトップレベル `flavor`** にあり（`editions[].flavor` は両方 `null`）、
edition側だけを見る手順では**構造的に見えない**。

- ⚠️ **全2,494枚のうち「トップレベル `flavor` のみ」は411枚**＝レアな例外ではない
- ⭐ ただし**PRD系7prefixではこの2枚だけ**で、どちらも **PRD と EVP（Event Packs・2023年）の両方に収録**
  （＝`sets/evp/` が差分に出た2枚と同一）
- ⭐ **バッチ3の残り34枚に同じ構造は0枚**（設計担当・開発担当・レビュー担当が独立に確認）

## 設計確認の結果: ✅ 承認（バッチ2完了）

### 承認の理由（設計担当が独自に再測定した結果）

| 設計意図 | 実測 |
|---|---|
| **PRD本編の全訳完了** | `sets/prd/` の `cp-en-inline` = **232 / 232枚** ✅ |
| `data/tl/prd.js` | **211**エントリ ✅ |
| 未訳数（§10 D完了列） | PRD **0** / 1st 5 / G 0 / P 12 / SD 10 / DP 7 / EVP 0・ユニーク残 **34** ＝ **全項目一致** ✅ |
| `data/translations.js` 差分 | **0**（`terms` 追加なし） ✅ |
| 第6版の確定訳5語 | ディスコープ4 / レゾネーター3 / 上位界3 / ディソニア人1 / コアフラクタル5 ＝ すべて反映 ✅ |
| §2-7 の例外（`Core Fractal`） | 「コアのフラクタル」の使用 **0件** ＝ 例外が正しく適用されている ✅ |
| フレーバー | 原文98 / 訳出**98** / 欠落**0** ✅ |

レビュー担当が**申告に依存せず全数で再測定**し、差し戻し1件を的確に検出した点も評価する。
とくに再判定で、訳文が**既存邦訳の転載でない**ことを
「英語 sorrow 由来の『悲しみ』（邦訳は伊語 dolore 由来の『苦しみ』）」「英語 "we were happy" 由来の『私たちが』（伊語原文に主語なし）」
という**2つの積極的根拠**で示したのは、著作権配慮の検証として的確。

### 開発担当の確認事項への回答（ユニット2・6件すべて承認）

| # | 確認事項 | 回答 |
|---|---|---|
| 6-A | 固有名詞19語 | **全て承認**。⭐ 懸念された「`Tower of Dis`→ディスの塔」と「`DisCorp`→ディスコープ」は**どちらも `Dis`→ディスで一貫しており分かれていない**。`Forese` を『神曲』の男性ではなく**カードの `her` に合わせた**判断も正しい（カードが正） |
| 6-B | `blood counter` / `training counter` が `terms` 未登録 | **バッチ2ではやらないで正解**。下記フォローアップissueに含める |
| 6-C | `Phone` / `Kitchen` の訳 | **承認**（フォンのアイテム／キッチンのアイテム）。フォローアップissueの `subtypes` 訳語にそのまま使う |
| 6-D | `TRANSLATION.md` の `Retort` | **実データ側が正**（`terms` と既訳**21件すべてが「リトルト」**・TRANSLATION.md 77行目だけが「リトート」）。⚠️ **`TRANSLATION.md` はルート直下＝本番配信対象**なので、修正はフォローアップissueで行う |
| 6-E | `Rhesus Eradication` → 「Rh因子の根絶」 | **承認**。`ハーヴェスターMk II`・`バッテリーコアX` の先例があり逸脱ではない。「レサス因子」は医学的に不自然 |
| 6-F | 迷った10枚 | **全て承認**。⭐ `Vel-ocity Punch → ヴェル・オシティ・パンチ`（VelTech の言葉遊びを中黒で再現）は §2-6 の意図に沿う良い処理 |

### レビューの推奨指摘(S)の扱い

| 指摘 | 扱い |
|---|---|
| S1-1 `1つ選ぶ—` のダッシュ（コーパスは `——` が16件） | **対応しない**。§2-3「ダッシュは原文のまま」に従った選択で開発担当が開示済み。表記統一は #12 の範疇 |
| S1-2 `TRANSLATION.md` の `Retort` | ⭐ **採用** → フォローアップissue |
| S1-3 `subtypes` 辞書の欠落 | ⭐ **採用** → フォローアップissue |
| S3-1 フレーバー未訳を止める自動検査が無い | ⭐ **採用** → フォローアップissue（⚠️ 既訳コーパスは711枚中612枚が未訳なので**全体対象の検査は常時赤になる**。対象を絞る設計が要る） |

---

# 派生issue（復旧後に起票する）※ユニット1の案を統合・拡大した最終版

## 新規: PRDで増えた辞書項目の追加と、「緑のまま抜ける」検出経路の手当て

- **ラベル**: `bug` / `小タスク` / `着手可`
- **背景**: #73 のバッチ2（PRD本編176枚の和訳）で、**辞書の抜けが3種類**見つかった。
  いずれも fail-open で破綻はしないが、**`npm run validate` が緑のまま通る**ため気づけない（#40 と同型）

### やること

1. **`data/translations.js` の `subtypes` に5種を追加**（PRDで新登場。**68枚**が英字表示のまま）
   | キー | 枚数 | 訳語（#73で確定済み） |
   |---|---:|---|
   | `DISCORP` | 20 | ディスコープ |
   | `VELTECH` | 31 | ヴェルテック |
   | `RESONATOR` | 9 | レゾネーター |
   | `KITCHEN` | 6 | キッチン |
   | `PHONE` | 2 | フォン |
2. **`terms` に2種を追加**（既訳に用例はあるが辞書に無く、**ハイライトも用語解説も出ない**）
   - `blood counter`（ブラッドカウンター・2枚）／`training counter`（トレーニングカウンター・1枚）
3. **`TRANSLATION.md:77` の `Retort → リトート` を「リトルト」に修正**
   （`terms` と既訳21件すべてが「リトルト」。⚠️ **ルート直下＝本番配信対象**なのでpushはユーザー判断）
4. **検出経路を足す**（設計判断が要る）
   - ⭐ **`validate` に「実データに出る `subtypes` が辞書に全部あるか」の検査**を足す（#40 と同じく人を止められる）
   - ⚠️ **フレーバー未訳の検査は設計が難しい**。既訳コーパスは **711枚中612枚が未訳**のため、
     全体対象にすると**常時赤**になる。**対象セット限定にするか、警告に留める**かの判断が要る
5. ⭐ **消費側の棚卸し**: 今回の取りこぼしは
   **「`editions[].flavor` だけを見て `card.flavor` を見なかった」**ことが原因。
   **全2,494枚中411枚がトップレベル `flavor` のみ**を持つため、
   **同じ見方をしている実装が他に無いか**を確認する（正しい形は
   `card.flavor || (card.editions||[]).map(e=>e.flavor).find(Boolean)`）

---

# バッチ3（34枚）の記録 ＝ #73 完了

## コミット

| コミット | 内容 |
|---|---|
| `9b472a88` | 実装本体（49ファイル）。メッセージに **`closes #73`** を含む |
| `6dac7a76` | 実装報告 |
| `0c5a4556` | レビュー判定（**合格**・M1〜M7すべて充足） |

## 設計確認の結果: ✅ 承認 — **#73 完了（全254枚の翻訳が終わった）**

### 承認の理由（設計担当が独自に再測定）

| 設計意図 | 実測 |
|---|---|
| **PRD系7prefixの未訳が0** | PRD 0 / 1st 0 / G 0 / P 0 / SD 0 / DP 0 / EVP 0 ＝ **合計0** ⭐ |
| 翻訳済みカード総数 | 2,482 → **2,516枚** |
| `prd.js` が不変 | **211**（バッチ3でカードを足していない）✅ |
| §2-8 の `Coordinator` 統一 | フルグライト**の指揮者** / 乱闘**の指揮者** / 奔流**の指揮者** ＝ 3枚統一 ✅ |
| §2-8 の Boonパターン | **12/12 適合・違反0**。`Proxia` も2枚とも「プロクシア」で一致 ✅ |
| `data/translations.js` | 差分 **0** ✅ |

レビュー担当が **`scaffold.mjs` を公式APIに直接問い合わせて7prefix分**確認し、フレーバーを
**スナップショットとAPI再取得の2系統**で数え直した点を評価する（バッチ2の差し戻し理由を的確に潰している）。

### 開発担当の確認事項への回答

| # | 確認事項 | 回答 |
|---|---|---|
| 6-A | `Surged Coordinator` →「奔流の指揮者」 | **承認**。セット内の既訳 `Surge → 奔流` に倒した判断は一貫性の観点で正しい。電気的サージの含意は効果文の「スタティックカウンター」で伝わる |
| 6-B | `Program cards` →「プログラムカード」 | **承認**（`PROGRAM` サブタイプは実データに存在しないため辞書との整合を気にしなくてよい） |
| 6-C | フレーバー抽出式が空白を truthy で拾う | ⭐ **重要な指摘。採用**（下記の新規issueへ。⚠️ **設計書§13に書いた式の欠陥**＝設計側の誤り） |
| 6-D | 迷った訳名10件 | **全て承認** |
| §5 | `prd.js` の変更が1行でなく2行 | **承認**。効果文中の自己参照も直すのが `TRANSLATION.md` の規約どおりで、設計書の「1行」という記述のほうが不正確だった |

### レビューの推奨指摘(S)の扱い

| 指摘 | 扱い |
|---|---|
| S1-1 `非戦闘ダメージ`(16) vs `戦闘以外のダメージ`(2) 等の表記ゆれ | **対応しない**。多数派に倒した判断は妥当。コーパス全体の統一は **#12** の範疇 |
| S1-2 `virgil-altered-future` の空フレーバー枠 | ⭐ **採用**。本コミット由来でないという切り分けも正しい → 新規issueへ |
| S3-1 フレーバー抽出式の空白問題 | ⭐ **採用** → 新規issueへ（6-C と同一） |

---

# 派生issue その2（復旧後に起票する）※設計確認中に設計担当が発見

## 新規: フレーバーテキストが `editions[].flavor` にあるカード473枚で、原文が一切表示されない

- **ラベル**: `bug` / `着手可`（規模は要見積り）
- **⚠️ #73 のスコープ外**（和訳ではなく**表示経路**の問題）。PRD以外の既存カードにも及ぶ

### 症状（2026-08-19 実測）

カードページ（`scripts/build-card-pages.mjs:368`）も詳細モーダル（`shared/js/card-detail.js:237`）も
**`(t && t.flavor) || card.flavor`** で、**`editions[].flavor` を一切見ていない**。

| 状態 | 枚数 |
|---|---:|
| 正常に表示 | 726 |
| ⚠️ **原文フレーバーが存在するのに一切表示されない** | **473** |
| ⚠️ **空の `<p class="cp-flavor"> </p>` が描画される** | **2**（`zinn-volnia-abbess` / `virgil-altered-future`） |

- ⭐ **同じ「未訳」でも表示が割れる**: フレーバーがトップレベルにあれば英語原文が出るのに、
  `editions[]` 側にしか無いと**何も出ない**
- ⚠️ **和訳を入れると出るようになる**ため露見しにくい。#73 バッチ2で96枚のフレーバーが
  表示されるようになったのは**訳を入れたからで、経路が直ったわけではない**
- 実例: `zinn-volnia-abbess` は `card.flavor` が `" "`（空白）で `editions[0].flavor` に
  `“If the price of brilliance is to burn, then so be it.”` があるが、**空の枠だけが出ている**

### 直し方（両方の消費側を同じ形に）

```js
const pick = v => (typeof v === "string" && v.trim()) ? v : "";
const flavor = pick(t && t.flavor) || pick(card.flavor)
  || (card.editions || []).map(e => pick(e.flavor)).find(Boolean) || "";
```

⚠️ **空白のみを falsy として扱う**こと（`||` だけでは `" "` を拾って空の枠が出る）。
⚠️ **消費側は2箇所**（`build-card-pages.mjs` / `card-detail.js`）。**両方直さないと画面で食い違う**。

### ⚠️ 影響の見積りが要る

修正すると**473枚のカードページに英語フレーバーが新たに出る**（＝生成物の差分が473ファイル）。
**日本語サイトに英語原文を出すことの是非**を先に決める必要がある（出さない選択もあり得る）。
⭐ その場合でも **空の `<p>` 2枚は直すべき**。
