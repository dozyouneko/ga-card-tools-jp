⚠️ **本来はissue #73 のコメント。GitHubアカウント停止（`gh` が403）のため代替としてこのファイルに記録する。**
復旧後、この本文をそのまま `gh issue comment 73 --body-file docs/design/73-PRD新カード和訳/レビュー判定_ユニット2.md` で投稿し、
`gh issue edit 73 --remove-label レビュー待ち --add-label 実装待ち` を実行してください。

---

## 判定: 不合格（M1）

対象コミット: `a61b2abc`（サブバッチB） / `5228daec`（C） / `0a071894`（D・生成物再生成） / `d61a24ab`（報告・docsのみ）
レビュー日: 2026-08-19 ／ 差し戻し回数: **1往復目**（2往復まで）
評価基準: `.claude/agents/review.md` M1〜M7、設計書**第6版** §2-2〜§2-7 / §9 / §10 / §11-B・C・D
（⚠️ §4・§5 はバッチ1用のため参照していません）

⭐ **不合格の理由は1件だけ**（在庫132枚のうち**フレーバー訳が2枚欠落**）で、**重大度は「軽微」**です。
それ以外の V1〜V6・M2〜M7 は**すべて自分で再測定して一致**しました。修正は2枚の `flavor:` を足して
`gen-tl-json.mjs` → `build:cards` を回すだけで、**訳文・命名・生成物の設計適合そのものに問題はありません**。

---

### 必須基準

| 基準 | 結果 | 証跡 |
|---|---|---|
| **M1 設計適合** | ❌ | §10 V1〜V6 は**全項目一致**（下表）。しかし設計書 §2-7「フレーバーの注意」が全数訳出を前提としている**バッチ2のフレーバー98枚**のうち、ユニット2の担当分 **74枚に対し訳出は72枚**で、**2枚が英語のまま本番HTMLに出る**（M1-1） |
| M2 実駆動 | ✅ | `PORT=3100 npm run dev` + Playwright 1.61.1。**PC 1280 と 375px の両方**で V5-6/V5-7 を再現。`tmp/review/73/pc-charge-static.png` `375-charge-static.png` `pc-top-prd.png` `375-top-prd.png` `pc-modal.png` `375-modal.png` `pc-flavor-cato-meadows-channeler.png` `pc-flavor-fran-carmine-spark.png`。**pageerror 0 / 横スクロール 0**（scrollW=clientW を両幅で実測） |
| M3 回帰 | ✅ | `npm run validate` **exit 0**（`all checks passed.` / `tmp/review/73/validate.log`）。主要画面 `/`・詳細モーダル（`#gacd-modal`）・`/cards/<slug>/`・`/tools/deck-builder/`・`/tournaments/`・`/sets/prd/` がすべて **200**・pageerror 0 |
| M4 生成物の同期 | ✅ | **全2,494カードページ＋全セットページを走査**し、未反映（関連カードのリンク表示名が英語のまま／`CARDNAME` 残存／title未反映）は **0件**。索引 **2482slug**。⭐ **翌朝のcronが拾う差分は残っていない** |
| M5 静かな劣化がない | ✅ | 検査ロジックの追加・変更なし（`scripts/` の差分 0）。索引は**辞書idのシフトのみ**で**意味が変わった既存slug 0件**（`d` 247→249・新規slug **132**・消失 0・8要素でないエントリ 0）。⚠️ ただし**今回の欠落を既存の検査が誰も止められない**点は参考として後述 |
| M6 公開安全性 | ✅ | 差分144件は**すべて既存ファイルの変更**（追加・削除・リネーム **0**）＝**公開URLの増減なし**。`_headers` `404.html` `functions/` `scripts/` `sitemap.xml` `package.json` に差分なし。秘匿情報なし（検出された `data-cf-beacon` は全ページ既存の公開site token・文脈行） |
| M7 スコープ | ✅ | `data/tl/prd.js` は**純追記（削除行 0 / +1104行）**。追記slug **132枚が §11-B/C/D の一覧と完全一致**（`diff` 差分0）。`data/translations.js` の差分 **0**。`sets/evp/` は設計どおりの正当な差分。**APIドリフト混入 0** |

---

### ❌ M1-1 バッチ2のフレーバー2枚が未訳のまま本番HTMLに出る

- **該当**: `data/tl/prd.js` の `cato-meadows-channeler` / `fran-carmine-spark`
  （生成物への現れ: `cards/cato-meadows-channeler/index.html` / `cards/fran-carmine-spark/index.html`）
- **再現**:

  ```bash
  PORT=3100 npm run dev
  # ブラウザ（またはcurl）で下記を開き「フレーバーテキスト」欄を見る
  curl -s http://localhost:3100/cards/cato-meadows-channeler/ | grep -o '<p class="cp-flavor">[^<]*'
  curl -s http://localhost:3100/cards/fran-carmine-spark/  | grep -o '<p class="cp-flavor">[^<]*'
  ```

- **期待**: 日本語のフレーバー訳が出る。設計書 §2-7「⚠️ フレーバーの注意（バッチ2の実データで確認済み）」は
  バッチ2の **98枚のフレーバーを全数検査**したうえで「**原文から自分で訳す**」「（署名も）**通常どおり訳す**」と
  指示しており、**98枚すべてが訳出対象**。同じPRDの兄弟カード97枚は日本語で出ている
- **実測**: 英語のまま出る。
  - `cato-meadows-channeler` → `“Now, I am master of myself.”`
  - `fran-carmine-spark` → `“There is no greater sorrow than to recall in misery the time when we were happy.”`
  - 網羅計測: **サブバッチA = 原文flavorあり24枚 / 訳出24枚（欠落0）**、
    **サブバッチB+C+D = 原文flavorあり74枚 / 訳出72枚（欠落2）**。24+74 = **98**＝設計書§2-7の全数と一致
- **重大度**: **軽微**（動作はするが基準に届かない。落ちない・他カードに波及しない）
- **根拠**: 設計書 §2-7「フレーバーの注意」／§10 V5「画面表示（**最終形での期待値**）」／
  `.claude/agents/review.md` M1「期待値は**画面に出る最終形**（件数だけでなく**文言**・順位・位置まで）で照合する」

#### ⭐ 原因の見立て（修正時の注意・同じ取りこぼしを防ぐため）

**この2枚だけ、フレーバーが `editions[].flavor` ではなくカードの トップレベル `flavor` にある**（実測）:

| slug | `flavor`（トップレベル） | `editions[PRD].flavor` | `editions[EVP].flavor` |
|---|---|---|---|
| `cato-meadows-channeler` | **あり** | `null` | `null` |
| `fran-carmine-spark` | **あり** | `null` | `null` |

- 残り72枚は `editions[].flavor` 側にある。**edition だけを見る手順だとこの2枚は構造的に見えない**
- ⚠️ **この2枚は設計書 §10 V6 が「`sets/evp/` に差分が出る正当な理由」として名指ししている2枚と同一**
- ⚠️ **どちらもダンテ『神曲』からの引用**（煉獄篇 第27歌 ウェルギリウスの最後の言葉／地獄篇 第5歌 フランチェスカ）。
  §2-7 のとおり**既存邦訳（寿岳文章訳・平川祐弘訳など）を転載せず、印刷された英文から自分で訳す**こと。
  ⚠️ 著作権配慮で**意図的に飛ばした**のであれば、それは §2-7 の指示と食い違うので
  **実装報告の §3（残っている確認）か §5（逸脱と根拠）に明記して設計担当の判断を仰いでください**（無記載のため今回は欠落として扱いました）
- 修正後は **`gen-tl-json.mjs` → `gen-card-meta-index.mjs` → `build:cards`** まで回して生成物を同期させてください
  （フレーバーはカードページに埋め込まれるため、回さないと**翌朝のcronが拾って自動publishします**＝M4）

---

### 設計書 §10 の検証項目との対応（**すべてレビュー担当が独自に再測定**）

⚠️ 開発担当の報告値は参照せず、コマンドを打ち直した結果です。

| # | 期待値（D完了） | 実測 | 判定 |
|---|---|---|---|
| V1 PRD | 0 | **0**（fetched 232） | ✅ |
| V1 PRD 1st | 5 | **5**（fetched 27） | ✅ |
| V1 PRDG | 0 | **0**（fetched 21） | ✅ |
| V1 PRDP | 12 | **12**（fetched 14） | ✅ |
| V1 PRDSD | 10 | **10**（fetched 61） | ✅ |
| V1 PRDDP | 7 | **7**（fetched 39） | ✅ |
| V1 PRDEVP | 0 | **0**（fetched 33） | ✅ |
| V1 ユニーク残 | 34 | **34**（7prefixの未訳slugの和集合を実算出） | ✅ |
| V2 `data/tl/prd.js` | 79→123→167→**211** | **79 / 123 / 167 / 211**（各コミット時点で `git show` して計数） | ✅ |
| V2 他4ファイル | 3 / 2 / 1 / 3 | prd-1st **3** / prdp **2** / prdsd **1** / prddp **3**（増減なし） | ✅ |
| V3 `npm run validate` | exit 0 | **exit 0**・`all checks passed.` | ✅ |
| V3 索引の行（D完了） | `2482slug` | `card-meta-index sort keys OK — **2482slug** / level 133 / power 999 / life 854 / cost_memory 501 / flip 22` | ✅ |
| V3 索引（B・C中間） | `2350slug` のまま | **B・Cのコミットに `card-meta-index.json` が含まれない**（差分は `tl/prd.js`＋`tl-names.json`＋`tl-effects.json` の3件のみ）＝§9-3どおり | ✅ |
| V3 `check-terminology.mjs PRD` | 0件 | `対象 232件 / 日英とも訳文がある 232件 / 用語ハイライトが効かない疑い **0件**` | ✅ |
| **V4 `data/translations.js` 差分** | **0** | **0**（`git diff --stat 82587616 0a071894 -- data/translations.js` が空。`terms` 追加 0件） | ✅ |
| V5-6 `cards/charge-static/` | スタティックカウンターがハイライト＋用語解説 | `.term-hl` = **`スタティックカウンター`**（`rgb(110,168,254)` / `font-weight:600`）。用語欄に `スタティックカウンター（static counter）…` の解説。⭐ **`terms` を1件も足していないのに効いている**（V4の裏づけ）。PC・375px とも同一 | ✅ |
| V5-7 トップ `.asphodel/paradise（PRD）` | 全232件・英語のままのタイル0 | 選択肢 `.asphodel/paradise（PRD）` を選択 → **「全 232 件」**。「もっと見る」を4回押して**232タイル全部**を展開し全名称を走査 → **英語のみの名前 0件**。件数の食い違い注記なし。**PC・375px の両方で同結果** | ✅ |
| V6 HTML差分 | 140（カード132＋波及4＋セット4） | **140**（カードページ**136**＝対象132＋波及4、セット4） | ✅ |
| V6 対象132枚の一致 | §11-B/C/D と一致 | **完全一致**（差分に出たカードページから§11一覧を引くと余り0・不足0） | ✅ |
| V6 波及4枚 | `aenean-crystallization` `augustine-votary-of-yore` `elysian-test-subject` `powercell` | **一致**。4枚とも **`1 1`（1行の増減）**で、内容は関連カード欄のリンク表示名のみ（`Core Fractal→コアフラクタル` ×2 / `Epicurean Institute→美食研究所` / `Cell Production→セルの生産`ほか）。⭐ **「用語欄が増える」型の波及 0件**（`terms` 未追加のため＝設計どおり） | ✅ |
| V6 セット4枚 | `sets/prd/` `sets/prd-1st/` `sets/prdsd/` `sets/evp/` | **一致**。⭐ `sets/evp/` は設計どおりの正当差分 | ✅ |
| V6 データ4件 | `tl/prd.js` `tl-names.json` `tl-effects.json` `card-meta-index.json` | **この4件のみ** | ✅ |
| V6 上記以外 | 0 | **0**（144件すべて `M`＝変更。**追加・削除・リネーム 0**） | ✅ |
| V6 `cp-en-inline` | prd **232** / prd-1st **22** / prdsd **51** / evp **24** | **232 / 22 / 51 / 24**（`sets/prd/` は 232/232 ＝ **PRD本編は名前・効果の訳出完了**） | ✅ |

#### レビュー担当が追加で入れた検証

| 観点 | 結果 |
|---|---|
| 索引の意味変化 | 辞書展開して旧新を比較 → **意味が変わった既存slug 0件** / 新規 **132** / 消失 **0** / `f` 22→22 / **8要素でないエントリ 0** |
| 効果訳の構造一致 | 132枚すべてで **原文と訳文の段落数が一致**（不一致0）・**箇条書き `•` の数が一致**（不一致0） |
| 効果訳の有無 | `tl-effects.json` に132枚すべて収録（欠落0）。原文に効果がある/ない と訳の有無が**過不足0** |
| フレーバー訳の有無 | ⚠️ **欠落2件**（上記 M1-1）。それ以外は過不足0 |
| 重複収録の振り分け | `data/tl/*.js` 全体の重複slugは**既知3件のみ**（`astra-sight` / `stalwart-shieldmate` / `umbra-sight`）＝**今回の132枚で新たな重複なし** |
| 生の英語マーカー残存 | 追記ブロック内の `CARDNAME` **0** / `[REST]` **0**（`prd.js` 全体でも0）/ `Cascade` **0**。`[レスト]` 8件は定訳どおり |
| 詳細モーダル | `ディスの塔` を検索して `#gacd-modal` を開き、効果訳・`ディスコープ`・`.term-hl`（シージアブル／耐久カウンター）・用語解説の表示を PC・375px で確認 |

---

### 訳文の品質（§2-6・§2-7 の命名ルール適合）— **問題なし**

| ルール | 対象 | 実測 |
|---|---|---|
| **第6版の確定訳5語** | — | `DisCorp→`**ディスコープ**（2箇所）/ `Resonator→`**レゾネーター**（2箇所）/ `Higher Plane→`**上位界**（1箇所）/ `Core Fractal→`**コアフラクタル**（3箇所）。⭐ **`Core Fractal` は §2-7 の「◯◯のフラクタル」ではなく例外どおり「コアフラクタル」**。`Disonian` はサブバッチA側の語のためユニット2に出現なし（0件で正） |
| 表記ゆれ | — | `ディスコルプ` `ディスコーポ` `レゾネータ`（長音なし）`高位界` `コアのフラクタル` いずれも **0件** |
| `... Fractal →「◯◯のフラクタル」` | 3枚（＋例外1） | 分岐のフラクタル／潤いのフラクタル／潮汐のフラクタル（＋`core-fractal`＝コアフラクタル） |
| `... Synchron →「◯◯のシンクロン」` | 3枚 | 蒸発のシンクロン／鎮火のシンクロン／扇動のシンクロン |
| `... Bladehand →「◯◯の剣使い」` | 2枚 | 燃え立つ剣使い／荒れ狂う剣使い（A の「泉の剣使い」と合わせ3枚） |
| `... X Ultra →「・Xウルトラ」` | 2枚 | ブローチ・Xウルトラ／チャージャー・Xウルトラ |
| `Cell / Powercell` | — | セルの生産／セル反応炉（「セル」で統一） |
| ブランドカタカナ表記 | — | `ヴェルテック` 7箇所。表記ゆれ 0 |
| 効果訳の忠実さ | 抜き取り10枚 | `vascular-collapse` `tower-of-dis` `where-futures-stir` `leran-pastoral-hymns` `zena-echo-weaver` `business-card` `blood-surge` `epicurean-institute` `acheron-express-officer` `rolling-chorus` を原文と逐語照合 → **条件・数値・段落構成・リマインダー文に欠落・誤読なし**。`CARDNAME` の「」置換・`［クラスボーナス］`・`[パワー]`/`[ライフ]` も既訳準拠 |

---

### 推奨基準の指摘（**合格を妨げないもの**／対応要否は設計担当の判断）

- **S1-1: `1つ選ぶ—` のダッシュが既訳コーパスと違う**（`vascular-collapse` 1件）。
  コーパスの先例は **`1つ選ぶ——`（二倍ダッシュ）が16件**、`—`（1つ）は今回が初。
  ただし設計書 §2-3 は「区切りのダッシュ `—` は**原文の形をそのまま残す**」と定めており、
  開発担当は報告 §5-4 でこの選択を**開示済み**です。**どちらが正かは設計判断**（規約の適用範囲が
  Cascade限定か全体かが §2-3 から読み取れません）
- **S1-2: `TRANSLATION.md` の `Retort → リトート` が実データと食い違う**（開発担当 6-D と同件・レビューでも再現）。
  実測: `data/tl/*.js` + `translations.js` に **リトルト 21件 / リトート 0件**、`TRANSLATION.md:77` だけが「リトート」。
  今回の `リトルト2` `リトルト3` は**実データ側に合わせた妥当な判断**です
- **S1-3: `subtypes` 辞書の欠落は今回も可視**。詳細モーダルの `ディスの塔` で
  `サブタイプ TAMER（テイマー）・SIEGEABLE（攻城可能）・**DISCORP**・SPIRE（尖塔）` と**素の英字**が出ます。
  ⭐ ユニット1の設計確認で**別issueに切り出す**と決定済みのため、ここでは再掲のみ（fail-openで破綻なし）

### 参考（基準に紐づかないので合否に含めていません）

- ⚠️ **今回の M1-1 を止められる検査が1つも無い**。`npm run validate`（exit 0）・
  `check-terminology.mjs PRD`（0件）・`cp-en-inline`（232/232）は**いずれも名前と効果しか見ておらず、
  フレーバーの訳抜けを検出しません**。#40 の `meta.sets` と同型の「緑のまま抜ける」経路です。
  ⭐ `validate` に「訳済みカードで**原文にflavorがあるのに訳が無い件数**」を出す検査を足せば人を止められます
  （⚠️ 既訳コーパス全体では **711枚中612枚**がフレーバー未訳なので、**全体ではなく#73の対象セットに限る**か
  警告に留める設計が要ります＝設計担当の判断事項）

### 設計担当への確認事項（**合否には含めていません**）

開発担当が報告 §6 に挙げた6件（**6-A** 固有名詞19語 / **6-B** `blood counter`・`training counter` の `terms` 未登録 /
**6-C** `Phone item`・`Kitchen item` / **6-D** `TRANSLATION.md` の `Retort` / **6-E** `Rh因子の根絶` /
**6-F** 迷った10枚）は**すべて仕様判断**のため、レビューでは合否の材料にしていません。
レビューで再現・確認できた事実だけ上の S1-2・S1-3 に整理しました。

### 残っている確認

- **本番（push後）の反映確認** — ⚠️ **アカウント停止でpushできないため実施不可**。合否は**それ以外**で判定しています。
  差分に新規ファイルが1つも無い（144件すべて変更）ため、復旧後の反映確認は
  **反復測定（20秒間隔で全数一致するまで）**が必要です（CLAUDE.md の既定手順・新規URLの到達マーカーは使えません）
- **デッキ構築ツールのデッキ内容依存の表示**（適合判定バッジ・所持枚数）— 未検証。
  今回の変更は訳データのみで、検索経路はトップと同一モジュール（`shared/js/card-search.js`）のため、
  トップでの全232件確認で代替しました

---

### 次のアクション（開発担当）

1. `cato-meadows-channeler` / `fran-carmine-spark` の `flavor:` を `data/tl/prd.js` に追記
   （⚠️ **既存邦訳を転載せず英文から自分で訳す**。フレーバーは **トップレベル `flavor`** にあります）
2. `node scripts/gen-tl-json.mjs` → `GA_SNAPSHOT_MAX_AGE_MIN=0 npm run build:cards` → `npm run validate`
   （⭐ **`gen-card-meta-index.mjs` は不要**。索引はフレーバーを持たないため `2482slug` から変わりません）
3. 差分が **`data/tl/prd.js` / `tl-effects.json` ＋ カードページ2枚**に収まることを確認してコミット
   （⚠️ 名前は変えないので `tl-names.json` は動かないはずです。動いたら想定外なので報告してください）
4. 実装報告に追記して `レビュー待ち` へ

