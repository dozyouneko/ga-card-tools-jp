# #73 実装報告: バッチ2 ユニット2（サブバッチB → C → D・132枚）

⚠️ **本来は issue #73 のコメント。GitHubアカウント停止（`gh` が403）のため、代替としてこのファイルに記録する。**
経緯と復旧手順は同フォルダの `issue反映待ち_2026-08-19.md`。復旧後、この本文をそのまま
`gh issue comment 73 --body-file docs/design/73-PRD新カード和訳/実装報告_ユニット2.md` で投稿する。

- 実施日: 2026-08-19
- 参照した設計書: `docs/design/73-PRD新カード和訳/PRD新カード和訳_設計.md` **第6版**（§2-2〜§2-7 / §9 / §10 / §11-B・C・D）
- ⚠️ ラベル操作は未実施（`gh` が使えないため）。本来は `実装待ち` → `レビュー待ち`

---

## 1. 実施内容

設計書 §9-3「ユニット2」の手順どおり、**44枚単位で3サブバッチに分けて**実装した。
⭐ **全コマンドを `GA_SNAPSHOT_MAX_AGE_MIN=0` で実行**し、スナップショット
（`tmp/api-cache/cards-snapshot.json` / 取得 `2026-08-18T14:49:02Z`）を固定した＝
**#71 の実行時APIドリフトは構造的に混入していない**（後述 V6 で実測確認）。

| # | 手順 | コミット |
|---|---|---|
| 1 | サブバッチB（§11-B の44枚）を `data/tl/prd.js` 末尾に追記 → `gen-tl-json.mjs` → `validate` | **`a61b2abc`** |
| 2 | サブバッチC（§11-C の44枚）を同様に追記 → `gen-tl-json.mjs` → `validate` | **`5228daec`** |
| 3 | サブバッチD（§11-D の44枚）を追記 → `gen-tl-json.mjs` → `gen-card-meta-index.mjs` → `build:cards` → `validate` | **`0a071894`** |

- **push: していない**（アカウント停止中。かつ本番公開の可否は常にユーザー判断）
- 追記はすべて **`data/tl/prd.js` の末尾への純追記**。既存行の移動・書き換えはしていない
  （ファイル冒頭のコメントに各サブバッチ1行ずつ、計3行だけ追記）
- ⚠️ B・C のコミットで `gen-card-meta-index.mjs` / `build:cards` を回していないのは設計書 §9-3 の指示どおり
- 追記した132枚の slug が §11-B/C/D の一覧と**完全一致すること**を diff で機械確認済み（3サブバッチとも差分ゼロ）

---

## 2. 検証結果（設計書 §10 の V1〜V6 に番号で対応）

### V1. 未訳数（`node scripts/scaffold.mjs <prefix>`）

| prefix | 期待（D完了） | **実測** | 判定 |
|---|---:|---:|:--:|
| PRD | 0 | **0** | ✅ |
| PRD 1st | 5 | **5** | ✅ |
| PRDG | 0 | **0** | ✅ |
| PRDP | 12 | **12** | ✅ |
| PRDSD | 10 | **10** | ✅ |
| PRDDP | 7 | **7** | ✅ |
| PRDEVP | 0 | **0** | ✅ |
| **ユニーク残** | 34 | **34** | ✅ |

（ユニーク残は `scratch_ref_prd1st` / `prdp` / `prdsd` / `prddp` の slug を重複排除して計数＝34）

### V2. `data/tl/prd.js` の収録エントリ数

| 段階 | 期待 | **実測** | 判定 |
|---|---:|---:|:--:|
| B完了 | 123 | **123** | ✅ |
| C完了 | 167 | **167** | ✅ |
| **D完了** | **211** | **211** | ✅ |

他4ファイルは**1枚も増えていない**（`prd-1st.js` 3 / `prdp.js` 2 / `prdsd.js` 1 / `prddp.js` 3 のまま）✅

### V3. 生成物の整合

| 項目 | 期待 | **実測** | 判定 |
|---|---|---|:--:|
| `npm run validate` | exit 0 | **exit 0**（B・C・Dの3コミットすべて） | ✅ |
| `node scripts/check-terminology.mjs PRD` | 0件 | **0件**（対象232件が日英とも訳あり） | ✅ |
| 索引の行（B・C完了時） | `2350slug` のまま | **`2350slug`** | ✅ |
| 索引の行（D完了時） | **`2482slug`** | **`2482slug`**（level 133 / power 999 / life 854 / cost_memory 501 / flip 22） | ✅ |
| `gen-tl-json.mjs` の出力 | — | `tl-names.json` **2482件** / `tl-effects.json` 効果2465件・フレーバー99件 | — |

### V4. ⭐ `data/translations.js` に差分が出ないこと

**差分0**（`git diff --stat data/translations.js` が空）✅
**`terms` は1件も追加していない。** 新規に出た語（`blood counter` / `training counter`）は
どちらも**既訳コーパスに用例が2件ずつある**ため、既存表記をそのまま踏襲した（§5 参照）。

### V5. 画面表示（ユニット2の項目6・7）

`npm run setup:env`（Playwright 1.61.1 / chromium-1228 / 不足ライブラリ0）→ `npm run dev`（ポート3000）で実駆動。

| # | 検証項目 | **実測** | 判定 |
|---|---|---|:--:|
| 6 | `cards/charge-static/` で「スタティックカウンター」がハイライト＋用語解説 | `<span class="term-hl">スタティックカウンター` が**1箇所**（`color: rgb(110,168,254)` / `font-weight: 600`）。「このカードの用語」欄に **`スタティックカウンター（static counter）`** と解説文が出る。⭐ **`terms` を1件も追加していないのに効いている**（V4の裏づけ） | ✅ |
| 7 | トップで `.asphodel/paradise（PRD）` を選び全232件・英語のままのタイル0 | **「全 232 件」**表示。「もっと見る」で**232タイル全部を展開**し、カード名を全数走査 → **英語のみの名前は0件**。件数の食い違い注記も無し | ✅ |

追加で行った実駆動（報告の透明性のため）:

- **375px（モバイル幅）**で `vascular-collapse` / `tower-of-dis` / `sift` / `velocity-punch` /
  `zena-echo-weaver` / `rhesus-eradication` の6枚を開き、**pageerror 0・横スクロール0**を確認
- モーダル選択肢（`vascular-collapse`）の `• …` 3行、`［レベル9以上］`・`［レベル15以上］` の表示崩れなし
- `tower-of-dis` で **シージアブル・耐久カウンター**、`velocity-punch` で **サクリファイス・軽減不能・メモリー** が
  それぞれハイライトされることを確認（既訳 `terms` の流用が効いている）
- ⚠️ 唯一のconsoleエラーは Cloudflare beacon の CORS（`cloudflareinsights.com/cdn-cgi/rum`）で、
  **localhost では常に出る既知のもの**。pageerror は0件

### V6. 意図しない差分が無いこと

**差分ファイル 144 = HTML 140 ＋ データ4**（期待どおり）✅

| 内訳 | 期待 | **実測** | 判定 |
|---|---:|---:|:--:|
| カードページ（対象132枚） | 132 | **132**（§11-B/C/D の一覧と `comm` で突合し、過不足0） | ✅ |
| 波及ページ | 4 | **4**：`aenean-crystallization` / `augustine-votary-of-yore` / `elysian-test-subject` / `powercell` | ✅ |
| セットページ | 4 | **4**：`sets/prd/` `sets/prd-1st/` `sets/prdsd/` **`sets/evp/`** | ✅ |
| データ | 4 | **4**：`data/tl/prd.js` / `tl-names.json` / `tl-effects.json` / `card-meta-index.json` | ✅ |
| 上記以外 | 0 | **0**（新規追加・削除・リネームも0） | ✅ |

- 波及4件は**すべて `1	1`（1行の増減）** で、内容は「関連カード欄のリンク先表示名が英語→日本語」のみ。
  実物: `Core Fractal → コアフラクタル`（2件）／`Epicurean Institute → 美食研究所`／
  `Cell Production → セルの生産` と `Sordelle, Unmoored Exception → ソルデル、繋がれざる例外`（`powercell`）
- ⭐ **`sets/evp/` が出るのは設計どおり**（`cato-meadows-channeler`・`fran-carmine-spark` がEVPにも収録）。
  APIドリフトではないので戻していない
- ⭐ 設計書のとおり、**バッチ1で13枚出た「用語欄が増える」型の波及は0件**（`terms` を足していないため）

#### セットページの訳済み件数（`cp-en-inline`）

| ファイル | 期待（D完了） | **実測** | 総カード行 | 判定 |
|---|---:|---:|---:|:--:|
| `sets/prd/` | **232** | **232** | 232 | ✅ **PRD本編は全訳完了** |
| `sets/prd-1st/` | 22 | **22** | 27 | ✅ |
| `sets/prdsd/` | 51 | **51** | 61 | ✅ |
| `sets/evp/` | 24 | **24** | 24 | ✅ |

---

## 3. 実行できなかった検証・残っている確認

| 項目 | 状況 |
|---|---|
| **本番（push後）の確認** | ⚠️ **未実施。アカウント停止でpushできない**。ローカル実機まで |
| **デッキ構築ツール上の表示** | 未実施。今回の変更は訳データのみで、検索経路はトップと同一モジュール（`shared/js/card-search.js`）のため、トップでの全232件確認で代替した |
| **詳細モーダルでの表示** | 未実施（トップのタイル名と静的カードページで確認）。⚠️ モーダルは `rule`（公式裁定）を描画しない既知の問題があるが、これは **#74** の範囲で今回のスコープ外 |
| **`gh` によるissue報告・ラベル付け替え** | ⚠️ **実施不可**。本ファイルで代替（冒頭に明記） |
| `data/tl/prd.js` の**訳文そのものの品質** | 機械検証（用語ハイライト0件・表記ゆれ）は通っているが、**訳語の妥当性は人のレビュー対象**。§5 に判断した点を列挙した |

---

## 4. 差分が設計書の想定と食い違った点

**食い違いは0件。** V1〜V6 のすべての期待値が実測と一致した。

⭐ 設計書の「差分の期待ファイル数＝HTML 140＋データ4」は、**波及4枚・セット4枚の内訳まで含めて完全一致**した。

念のための補足: `scripts/scaffold.mjs` は**スナップショットを使わず公式APIを直接叩く**（V1の計測用）。
ただし**生成物には一切関与しない**（読み取り専用で `scratch_ref_*` に出力するだけ・`.gitignore` 済み）ため、
これがドリフト源になることはない。生成側（`gen-card-meta-index.mjs` / `build:cards`）は
`GA_SNAPSHOT_MAX_AGE_MIN=0` で固定したスナップショット（2026-08-18T14:49:02Z）を再利用しており、
`build:cards` のログにも `有効期限なし` と出ている。

---

## 5. 設計書からの逸脱と根拠

### 5-1. §2-7 の確定訳は判断し直していない（逸脱なし）

`DisCorp → ディスコープ`（`acheron-express-officer` の効果文・`tower-of-dis` の効果文）、
`Resonator → レゾネーター`（`fanclub-leader` ほか）、`Higher Plane → 上位界`（`golden-measure-patisserie`）、
`Core Fractal → コアフラクタル`（`core-fractal` / `bifurcating-fractal` / `cryogenic-ritual`）、
`VelTech → ヴェルテック`、`... Fractal → ◯◯のフラクタル`、`... Bladehand → ◯◯の剣使い`、
`... Synchron → ◯◯のシンクロン`、`... X Ultra → ・Xウルトラ`、`Cell/Powercell → セル／パワーセル` は
すべて表どおりに適用した。⚠️ **バッチ1と `aenean-crystallization` の既存の訳には触れていない。**

### 5-2. 既訳コーパス／`terms` を優先し、TRANSLATION.md の表記を採らなかった点（1件）

| 語 | 採用 | 不採用 | 根拠 |
|---|---|---|---|
| `Retort N` | **リトルト2 / リトルト3** | ~~リトート~~ | `translations.js` の `terms.retort` が **`リトルト（Retort N）`**、既訳コーパスも **リトルト17件 / リトート0件**。TRANSLATION.md の表記のほうが実データと食い違っている（⚠️ **TRANSLATION.md 側の修正が要るかもしれない＝6-D で確認** ） |

### 5-3. `terms` 未登録だが既訳コーパスに先例がある語（そのまま踏襲・`terms` は追加せず）

| 語 | 採用訳 | 先例 |
|---|---|---|
| `blood counter` | **ブラッドカウンター** | 既訳コーパスに2件 |
| `training counter` | **トレーニングカウンター** | 既訳コーパスに2件（「ドゥクスの大いなる恩恵」） |
| `Command Automaton` | **コマンド：オートマトン** | 既訳コーパスに2件（`terms."command chessman"` と同型） |

⚠️ どちらのカウンターも `terms` に無いのでハイライトは付かないが、**既訳と同じ挙動**なので新たな不整合ではない。
`terms` の追加は §10 V4 で禁じられているため行っていない（追加の要否は 6-B で確認）。

### 5-4. 記号の扱い（§2-3 の流儀を prd.js 内で一貫させた）

- `vascular-collapse` のモード選択は原文が `Choose one—` / `• …` のため、**`1つ選ぶ—` ＋ `•`** で訳出した。
  ⚠️ **既訳コーパスの先例は `・`（中黒）** だが、`prd.js` はバッチ1のカスケードで一貫して **`•`** を使っており、
  §2-3 の「箇条書き記号 `•` はそのまま残す」に合わせた（ファイル内一貫性を優先）
- `Deluge N —` は既訳コーパスの **`デリュージN——`** に合わせた（`current-groover` / `soaked-slash`）
- `Siegeable` のリマインダーは既訳の **`*（**シージアブル**——…）*`** 形を採用（`epicurean-institute` / `tower-of-dis`）
- `［クラスボーナス］`（全角）は `prd.js` の既存40箇所に合わせた。⚠️ レビュー S1-3 の判断（対応しない）に従っている

### 5-5. フレーバーの著作権配慮（§2-7 の注意）

`sift` のフレーバーは**ダンテ『神曲』地獄篇 第1歌の遊泳者の比喩**。既存邦訳（寿岳文章訳・平川祐弘訳など）は
一切参照せず、**カードに印刷された英文からのみ訳出**した。
`edge-of-tomorrow` の署名 `— Lorraine Allard` は**ゲーム内のチャンピオン**なので通常どおり訳した
（⚠️ 既訳コーパスに「フレーバーの署名」の先例が無く、`flavor` に `\n` を使う前例も0件だったため、
**`……」——ロレイン・アラード` と全角ダッシュで1行に収めた**）。

---

## 6. 設計担当への確認事項

⚠️ いずれも**実装側では決めきれない・先例が無い**項目。判断が要るものは勝手に確定していない。

### 6-A. §2-7 に載っていない固有名詞の訳（19語）— 実装側の判断で書いた。承認 or 差し替えの判断がほしい

**世界観・地名**

| English | 採用訳 | 出現 | 判断の根拠 |
|---|---|---|---|
| `Acheron`（Acheron Express） | **アケロン急送** | `acheron-express-officer` | 『神曲』アケロン川。DisCorpの実データが**パワーセルの配送業**（`delivery-droid` `production-crawldroid` のフレーバー）なので Express＝急送と解した |
| `the Meadows` | **メドウズ**（地名としてカタカナ） | `blightheart-thaumaturge` / `cato-meadows-channeler` | 区名（`Bedlam Borough → ベドラム区`）と同じ扱い。⚠️ **`.asphodel`＝アスフォデルの野との関係を訳に出していない**（断定を避けた） |
| `Cerberus' Maw` | **「ケルベロスの顎」** | `aquaveil-ambusher`（フレーバー） | 区名。カギ括弧で固有名であることを示した |
| `Lower Plane` | **下位界** | `lurching-rogue`（フレーバー） | §2-7 の `Higher Plane → 上位界` の対 |
| `Dis`（Tower of Dis） | **ディスの塔** | `tower-of-dis` | ⚠️ **`DisCorp → ディスコープ` と読みが分かれる**（ディス／ディスコープ）。原語も `Dis` と `DisCorp` で別語なので分けたが、**統一すべきなら指示がほしい**（『神曲』の定訳は「ディーテの市」で、これも候補） |
| `Harmonia` | **ハルモニア** | `charge-static`（フレーバー） | 神名のラテン読み。`Aenean → アエネアン` と同じ流儀 |

**人物名（既訳コーパスに前例0件。ラテン語・イタリア語読みで統一した）**

| English | 採用訳 | | English | 採用訳 |
|---|---|---|---|---|
| `Camil` | カミル | | `Piccarda` | ピッカルダ |
| `Cato` | カトー | | `Plutus` | プルートス |
| `Forese` | フォレーゼ | | `Sordelle` | ソルデル |
| `Fran` | フラン | | `Statius` | スタティウス |
| `Judas` | ユダ | | `ZENA` | ゼナ |
| `Leran` | レラン | | `Beatrice`（Dr.） | ベアトリーチェ博士 |
| `Lorraine Allard` | ロレイン・アラード | | `Virgil` | ウェルギリウス（§2-2 の確定訳を踏襲） |

- ⚠️ `Forese` は『神曲』では**男性**（フォレーゼ・ドナーティ）だが、カードの効果文・フレーバーは
  **`her` / 女性一人称**で書かれている。**カード側の設定を正として訳した**（`belted-tune` のフレーバー「フォレーゼの言葉」）
- ⚠️ `Sordelle` は『神曲』の `Sordello`（ソルデッロ）と綴りが違うため、**原綴どおり「ソルデル」**にした

### 6-B. `terms` に足すべき語が2つある（今回は足していない）

`blood counter`（ブラッドカウンター・2枚: `perfusive-envelopment` / `varicose-amplification`）と
`training counter`（トレーニングカウンター・1枚: `where-futures-stir`）は**既訳コーパスに先例があるだけで
`terms` には無い**ため、**ハイライトも用語解説も出ない**。

⭐ §10 V4 の「バッチ2では `terms` を1件も追加しない」に従って**足していない**が、
**バッチ3または別issueで足すかどうかの判断がほしい**（`subtypes` 辞書の欠落と同種の抜けで、fail-openで破綻はしない）。

### 6-C. 効果文に出るサブタイプで、辞書にも §2-7 にも無い語（2つ）

| English | 採用訳 | 出現 |
|---|---|---|
| `Phone item` | **フォンのアイテム** | `business-card` |
| `Kitchen item` | **キッチンのアイテム** | `golden-measure-patisserie` |

`DisCorp → ディスコープ` / `Resonator → レゾネーター` と同じく**カタカナで音写**した。
⭐ 承認いただければ、**別issue（`subtypes` 辞書の欠落）の訳語としてそのまま使える**。

### 6-D. `TRANSLATION.md` の `Retort → リトート` は実データと食い違っている

`terms.retort` も既訳コーパス（17件）も **リトルト**。TRANSLATION.md だけが「リトート」。
今回は**実データ側に合わせた**が、⚠️ **TRANSLATION.md の修正**（＝バッチ3完了時の追記作業に含めるか）は
設計担当の判断がほしい。

### 6-E. カード名にラテン文字を残した1枚

`Rhesus Eradication` → **「Rh因子の根絶」**。医学用語（Rh因子）の定訳がラテン文字を含むため。
既訳に `ハーヴェスターMk II` の先例、`prd.js` に `バッテリーコアX` `ブローチ・Xウルトラ` があるので
逸脱ではないと判断したが、⚠️ **全カタカナ（「レサス因子の根絶」等）に寄せるべきなら指示がほしい**。

### 6-F. 訳語に迷った10枚（既訳の先例に当てて決めたもの・差し替え可）

| slug | English | 採用訳 | 迷った点 |
|---|---|---|---|
| `charge-static` | Charge Static | スタティック充電 | 「充電」（既訳 `Charged Hunter → 充電された狩人`）と用語「スタティックカウンター」の橋渡し |
| `fanned-synchron` | Fanned Synchron | 扇動のシンクロン | `fan`（扇ぐ）と 扇動 を掛けた。フレーバー「風は炎を煽るばかり」に合わせた |
| `lacunarity-guide` | Lacunarity Guide | 空隙の案内人 | `lacunarity` はフラクタル解析の専門語（空隙性）。長いので「空隙」に短縮 |
| `martial-flowstate` | Martial Flowstate | 武技の無我境 | `flow state` の定訳が無く「ゾーン」「フロー状態」だと現代語すぎる |
| `powerforged-burst` | Powerforged Burst | 動力鍛造の炸裂 | バッチ1の `Cellforger Droid → セル鍛造ドロイド` に合わせた |
| `uncanny-realization` | Uncanny Realization | 不気味な気づき | `realization` を「実現」でなく「気づき」と解した（フレーバーが Virgil との再会の驚き） |
| `velocity-punch` | Vel-ocity Punch | ヴェル・オシティ・パンチ | ⚠️ 原文の `Vel-ocity` は **VelTech の言葉遊び**。§2-6 に従い全カタカナにし、中黒で区切りを再現した |
| `where-futures-stir` | Where Futures Stir | 未来が芽吹く場所 | `stir`（動き出す）を「芽吹く」と意訳（TRIALサブタイプ＝育成ギミック） |
| `wicked-gildbreaker` | Wicked Gildbreaker | 邪悪な黄金崩し | `gild`（金メッキ）＋ インフルエンス参照＝富の否定 |
| `sift` | Sift | 選り分け | 「篩う」だと固い。効果（デッキ1枚を墓地→1枚ドロー）に合わせた |

---

## 7. 次の工程

- ⚠️ **バッチ3（残り34枚）が未着手**なので `closes #73` は書いていない
- 未pushコミット: **6件**（うち今回の3件）。⚠️ **push はユーザーの判断待ち**（かつアカウント復旧待ち）
- 起動したローカルサーバー（`npm run dev` / ポート3000）は**停止済み**
