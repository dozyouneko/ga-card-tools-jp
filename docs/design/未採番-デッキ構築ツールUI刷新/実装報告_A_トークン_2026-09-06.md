# 実装報告 — A：共有トークン `shared/css/tokens.css`（2026-09-06）

- 対象設計書: [`左ペイン化_設計.md`](左ペイン化_設計.md) **§3（A）のみ**
- 担当: 開発（実装）担当セッション
- ⚠️ **GitHubアカウント停止中のため `gh` が使えない**（`gh auth login` 未ログイン状態を
  `npm run setup:env` で確認済み）。issueコメントの代わりに**この文書が「実装→レビュー／設計」の報告経路**。
- ⚠️ **§4（B）・§5（C）・§7（D）には手を付けていない**（別の周で実施）。

---

## 1. 実施内容

| # | ステップ | 触ったファイル |
|---|---|---|
| 1 | `shared/css/tokens.css` を新規作成（`:root` 1つだけ・セレクタを増やさない） | ⭐ 新規 |
| 2 | `<link rel="stylesheet" href="/shared/css/tokens.css">` を4ページに追加（**各ページCSSより前・絶対パス**） | `index.html` / `404.html` / `tools/deck-builder/index.html` / `tools/glossary/index.html` |
| 3 | `:root { … }` を削除（集約先を示すコメントに置き換え） | `style.css` / `tools/deck-builder/style.css` / `tools/glossary/style.css` |
| 4 | HeroUI 節の直値をトークン参照へ／`.search-top input, .search-top select` に同じトークンを適用 | `style.css` / `tools/deck-builder/style.css` |
| 5 | `validate` に検査2本を追加（設計 §3-5） | `scripts/validate.mjs` |

- **コミット: `b41d6411`**（1コミット・9ファイル・+197 / −51）
  - `git status --short` で**自分が触っていないパスの変更が無いこと**を確認してからステージした
    （`git add -A` は不使用。ステージしたのは上表の9ファイルのみ）
- **push: していない**（⚠️ 本番配信物 `style.css` `tools/**` `shared/**` `scripts/**` を変えるため、
  公開の可否はユーザーの判断。設計書 §10-5 の段取りどおり「レビュー合格 → 3000番で実物確認 → 承認 → 公開判断」）

### トークンの中身（設計 §3-2 の表どおり・逸脱なし）

配色13（`--bg` `--panel` `--panel-2` `--border` `--text` `--muted` `--accent` `--accent-2`
`--accent-3` `--yes` `--no` `--danger` `--discord`）＋ 形1（`--radius`）＋
HeroUI 6（`--radius-control` `--control-pad` `--control-fs` `--control-lh` `--ring-offset` `--panel-hi`）＝ **20トークン**。

### 検査2本の仕様（`scripts/validate.mjs`）

| # | 内容 | 実装 |
|---|---|---|
| (i) | `shared/css/**.css` の `var(--…)` が全部 `tokens.css` に定義されているか | 未定義があれば **exit 1**（`ファイル — 変数名` を全部列挙） |
| (ii) | `:root` を持つCSSの集合が `ROOT_OWNERS` と一致するか | **増えても減っても exit 1**（extra / missing を別々に列挙） |

- 走査範囲: リポジトリ全体の `*.css` から **`node_modules` `.git` `tmp` `docs` `.wrangler` `shared/vendor` を除外**
- ⚠️ **両方とも `/* … */` を除去してから走査する**（設計 §3-5 の指摘どおり）
- ⚠️ `:root` は `/:root\b[^{};]*\{/` で判定（`:root, html {` のようなセレクタリストも数え、
  宣言をまたがないよう `[^{};]*` で歯止め）
- ⚠️ fail-closed: 走査対象0ファイル・`tokens.css` が読めない・`tokens.css` に定義0 のいずれも **exit 1**
- 成功行（設計指定の文言どおり）:
  - `shared css tokens in sync — 68 トークン参照 / 未定義 0`
  - `:root owners in sync — 4ファイル`

---

## 2. 検証結果（設計 §9-2 の V1〜V7）

⚠️ すべて**実測値**。証跡は `tmp/dev/`（gitignore）に置いた（`before.json` / `after.json` /
`after8788.json` / 各PNG）。⚠️ **他セッションの作業物があるので `tmp/` は消していない**。

| # | 期待値 | 実測 | 判定 |
|---|---|---|---|
| **V1** | `npm run validate` が **exit 0** ＋ 新しい成功行2本 | `EXIT=0`。`shared css tokens in sync — 68 トークン参照 / 未定義 0` と `:root owners in sync — 4ファイル` の2行を確認。既存の成功行（`meta.sets` 63件・`no line-number refs … 46ファイル走査`・`card term highlights … 2495 ページ / 2517 節` 等）も全部出る | ✅ |
| **V2** | 検査(i)の破壊試験 | `shared/css/card-badges.css` に `.__v2_probe { color: var(--nonexistent-token-v2); }` を1行足す → **exit 1**（`SHARED CSS TOKENS (shared/css/tokens.css): shared/css/tokens.css に定義が無いトークン参照: 1件 / shared/css/card-badges.css — --nonexistent-token-v2`）。ファイルを戻すと **exit 0**（`68 トークン参照 / 未定義 0`） | ✅ |
| **V3** | 検査(ii)の破壊試験 | **両方向**を試した。①**増えた**: `tools/glossary/style.css` に `:root { --v3-probe: 1px; }` → **exit 1**（`許可リストに無いのに :root を持つCSS: 1件 / tools/glossary/style.css`）。②**減った**: `tools/print/style.css` から `:root` ブロックを削除 → **exit 1**（`許可リストにあるのに :root を持たないCSS: 1件 / tools/print/style.css`）。どちらも戻すと **exit 0** | ✅ |
| **V3-b** | コメント誤検出と `docs/` のモックCSSで落ちないこと | ①`shared/css/card-detail.css` と `shared/css/filter-chips.css` はどちらも `grep -c ':root'` が **1**（＝注意書きの文中にある）。対照実験: **素の `/:root/` 一致だと両方 true**、コメント除去＋`{` 必須だと**両方 false**（＝この2枚は owners に混ざらない）。②`docs/…/__probe_mock.css`（`:root { --mock-probe: 1px; }` ＋ `var(--mock-nonexistent)`）を一時的に置いて `validate` → **exit 0**（＝ `docs/` は (i) にも (ii) にも掛からない）。既存の `モック_PC左ペイン_2026-09-02.css` は「コメント除去後も `:root {` を持つ」ため、**docs/ を除外していなければ確実に落ちる**ことも確認済み。③プローブは削除し `git status --short` が自分の担当パスのみになることを確認 | ✅ |
| **V4** | トップ（`/`）が**変わらないこと** | ⭐ **`git show HEAD:index.html` / `HEAD:style.css` から移設前のドキュメントルートを作って3010番で並走させ、3000番（移設後）と突き合わせた**（CSSOMでの再現ではなく実物どうしの比較）。① `getComputedStyle(document.documentElement)` の既存11トークンが**全部一致**（`--bg #0f1115` / `--panel #1b1f28` / `--panel-2 #232834` / `--border rgba(255,255,255,.11)` / `--text #e7e9ee` / `--muted #9aa2b1` / `--accent #d9a441` / `--accent-2 #6ea8fe` / `--accent-3 #e07bd0` / `--yes #2f9e6b` / `--no #7a6320` / `--radius 14px`）。②要素の実効値も**完全一致**（`.controls input` = 角丸10px / padding 10px 14px / font-size 14px / line-height 20px / bg rgb(27,31,40) / border rgba(255,255,255,.11)、`.card` = bg rgb(27,31,40) / border rgba(255,255,255,.09) / 角丸14px / box-shadow rgba(255,255,255,0.043) 0px 1px 0px 0px inset、`.fgroup` = bg rgb(35,40,52) / 角丸10px、`.site-header` = bg rgb(25,29,37)、矩形も x/y/w/h まで一致）。③**スクリーンショットのピクセル差 0**（`.controls` 要素 320×927 → 差分0・bbox=None、ページ上部 1400×460 → 差分0・bbox=None） | ✅ |
| **V5** | `404.html` が**入れ子パス**で絶対パスCSSを解決できること | ⚠️ `npm run dev` は `404.html` を返さないため **`npx wrangler pages dev . --local --port 8788`** で確認。`/cards/xxx/yyy`（3階層）と `/nope`（1階層）の**両方**で **HTTP 404**・`document.styleSheets` が `…/shared/css/tokens.css` と `…/style.css` の2枚・**CSSの応答は 200/200**・`requestfailed` **0件**。トークンも解決している（`--bg #0f1115` / `--panel #1b1f28` / `--border rgba(255,255,255,.11)` / `--radius 14px`）。`.site-header` の `border-bottom-color rgba(255,255,255,.11)`、`.brand-mark` の bg rgb(217,164,65) / 角丸8px / 36px も期待どおり | ✅ |
| **V6** | `tools/glossary/` が **6宣言ぶん変わる**（変わらないことではない） | ① 使用箇所の実測: `--panel` **3**（27・35・53行）/ `--border` **3**（27・34・54行）＝ **計6宣言**。`--radius` **0**・`--panel-2` **0**（定義のみ未使用）＝設計 §3-4 と一致。② 実効値: `--panel #171a21 → #1b1f28` / `--panel-2 #1f232c → #232834` / `--border #2a2f3a → rgba(255,255,255,.11)`。③ 要素: `.notice` と `.search` が bg `rgb(23,26,33) → rgb(27,31,40)`・border `rgb(42,47,58) → rgba(255,255,255,.11)`。④ **目視1回**（1280×1000のスクリーンショットを新旧で撮り比べ）: **面が一段明るく・枠線が半透明**になり、地色（`--bg`）とレイアウト・文字は不変。ピクセル差は 485,299px（37.9%）で **bbox (280,183)-(1000,1000)＝`.app` のカード領域に限定**。代表点の実測: エントリ内側 `(23,26,33)→(27,31,40)`、エントリ上枠 `(42,47,58)→(52,55,63)`、本文地 `(15,17,21)→(15,17,21)`（不変） | ✅ |
| **V7** | デッキ構築の入力欄が 角丸10px・padding 10px 14px・font-size .875rem（＝トップと一致） | #51 の手順（`#view-editor` の `hidden` を外す）で `.search-top` を描画。`.search-top input` = **border-radius 10px / padding 10px 14px / font-size 14px（＝.875rem）/ line-height 20px / bg rgb(27,31,40) / border rgba(255,255,255,.11)**。移設前は 8px / 9px 12px / 14.4px / normal / rgb(23,26,33) / rgb(42,47,58)。**トップの `.controls input` の実測値と1項目も違わない**。`.search-top select` も同じ（`line-height` だけ Chrome のUA既定で `normal` のまま＝トップの `.controls select` も同じく `normal` なので**ここも一致**）。⚠️ 高さの変化: input **36→42px**、select **38→40px**（トップの `.controls input` は 42px なので一致した）。⭐ **`wrangler pages dev`（8788・`_headers` の CSP `style-src 'self'` が効く実物）でも同じ値**で、CSP由来のコンソールエラー **0件** | ✅ |

### 補足（V1〜V7 以外に確認したこと）

- **CSP**: `_headers` に `/shared/css/*` の個別ルールは無く（`immutable` は `/shared/vendor/*` だけ）、
  `tokens.css` は `Content-Type: text/css` / `Cache-Control: public, max-age=0, must-revalidate` で **200**。
  4ページとも `style-src 'self'` の下で読めている（同一オリジン）。⭐ **`_headers` の変更は不要**（設計 §3-3 の想定どおり）。
- **`document.styleSheets` の順序**: 4ページとも `tokens.css` が**先頭**（glossary の `cssResponses` は
  ネットワークの応答順で `style.css` が先に返っているが、`styleSheets` の順＝カスケードの順は
  `tokens.css → style.css` で正しい）。

---

## 3. 実行できなかった検証・残っている確認

| # | 項目 | 状況・代替手段 |
|---|---|---|
| 1 | **デッキ構築のログイン後の画面**（編集画面の実データ・カード詳細モーダル・結果モーダル・統計タブ） | ⚠️ Discord ログインが前提で到達できない。#51 の手順で **`.search-top` の描画までしか確認していない**。⭐ `--panel` / `--panel-2` / `--border` / `--radius` は**デッキ構築の CSS 全体で 18 / 21 / 43 / 3 箇所**、加えて共有CSS（`filter-chips.css` は `--panel` 1 / `--panel-2` 1 / `--border` 5、`card-detail.css` は `--panel` 1 / `--panel-2` 4 / `--border` 10）で使われるので、**ログイン後の面もすべて同じ方向（面が一段明るく・枠線が半透明・角丸 12→14px）に変わる**。⭐ **設計書 §9-4（R5）どおり「確認できた範囲」を明記する**——**実データの画面は未確認** |
| 2 | **本番（`*.pages.dev`）での確認** | **未push**のため不可能。push後に `curl -sI` で `/shared/css/tokens.css` の 200 を確認する必要がある（⚠️ 新規ファイルなのでエッジに古いコピーが無く、**404→200 で到達を即確定できる**＝最も確実な到達マーカー） |
| 3 | **V8〜V21** | ⭐ **B・C・D の検証項目なので今回の範囲外**（設計 §3 だけを実装したため） |
| 4 | **スマホ幅（640px以下）での入力欄** | `@media (max-width: 640px)` の `.search-top input, … { font-size: 16px }` は**この節より後**にあるので iOS のズーム防止は生きている（CSSの並びで確認）。⚠️ **実機幅での目視はしていない**（B の V12 で 375px を測る予定なので、そこで併せて確認できる） |

---

## 4. 差分が設計書の想定と食い違った点

**食い違いは無かった。** 設計書が「意図した変更」と書いた項目は全部そのとおりに出た
（デッキ構築・glossary の `--panel` / `--panel-2` / `--border`、デッキ構築の `--radius` 12→14px、
glossary の6宣言、`.search-top input` の3項目）。

意図内と判断した**追加の差分**が2つあるので内訳を書く:

| # | 差分 | 意図内と判断した根拠 |
|---|---|---|
| 1 | **デッキ構築で `--accent-3` と `--no` が、glossary で `--accent-3` `--yes` `--no` `--danger` `--discord` `--radius` `--radius-control` … が新たに定義される**（移設前は未定義） | ⭐ **見た目は1pxも変わらない**。消費側（`shared/css/card-detail.css`）は `var(--accent-3, #e07bd0)` / `var(--yes, #2f9e6b)` / `var(--no, #7a6320)` と**フォールバック付きで書かれており、値がトークンと同一**のため。⭐ むしろ「フォールバックで辛うじて正しかった」状態が**本当に定義された**状態になる（設計 §3-2 が `--accent-3` を「現状は `style.css` だけが持つ」・`--danger` `--discord` を「現状はデッキ構築だけが持つ」として**1本に集約すると決めている**ため意図内） |
| 2 | **トップで `--danger` `--discord` が新たに定義される** | 同上。`style.css` はこの2つを1箇所も参照していないので実効差分ゼロ（実測: トップのピクセル差 0） |

---

## 5. 設計書からの逸脱と根拠

| # | 逸脱 | 根拠 |
|---|---|---|
| 1 | ⚠️ **`.search-top input, .search-top select` に `line-height: var(--control-lh)` も当てた**（設計 §3-2 の括弧書きは「現状は角丸8px・padding 9px 12px・font-size .9rem」の**3項目だけ**を挙げている） | 設計の本文は「**同じトークンを当てる**」で、コントロール用トークンは4つ（`--radius-control` / `--control-pad` / `--control-fs` / `--control-lh`）。V7 の期待値が「**＝トップと一致**」であり、トップの `.controls input` は `line-height: 1.25rem` を持つため、**当てないとトップと一致しない**（移設前は `normal`）。⚠️ **副作用として input の高さが 36→42px になる**が、**トップの `.controls input` が 42px** なので「揃える」目的には合致する。→ ⭐ **§6-1 で確認を求める** |
| 2 | **`.search-top input:focus, .search-top select:focus` の `outline-offset: 1px` は据え置き**（`--ring-offset` を当てていない） | 設計 §3-2 が名指ししたのは **`.search-top input, .search-top select` の1ルールだけ**で、`:focus` は別セレクタ。⭐ **名指しされていないルールへ広げない**のがスコープの線引きとして安全と判断（デッキ構築のフォーカスは `outline: 2px solid var(--accent-2)` と**色まで自前指定**でトップと設計が違うため、片方だけ揃えると中途半端になる）。→ ⭐ **§6-2 で確認を求める** |
| 3 | **`.search-top .btn`（リセット／検索）は据え置き**（角丸8px / padding 9px 16px / font-size .9rem のまま） | 同上。トップは `.controls button` にもトークンを当てているが、設計 §3-2 が名指ししたのは `input, select` のみ。→ ⭐ **§6-3 で確認を求める** |
| 4 | 検査(ii)の判定を **`:root {` の素の一致ではなく `/:root\b[^{};]*\{/`** にした | 設計 §3-5 は「**`:root` の直後に `{` が続く形だけを数える**か、**`/* … */` を除去してから走査する**」と選択肢を示している。⭐ **両方やった**（コメント除去 ＋ `{` 必須）ほうが安全で、`:root, html { }` のようなセレクタリストも取りこぼさない |
| 5 | 検査の走査範囲を「許可リストのファイル」ではなく **リポジトリ全体の `*.css`（除外リスト方式）** にした | 「増えても減っても exit 1」を成立させるには**新しく増えたCSS**も見る必要があるため。除外は `node_modules` `.git` `tmp` `docs` `.wrangler` `shared/vendor` の6つで、`docs/` と `shared/vendor/` は設計・既存規約が明記している除外 |

⭐ **既存表記・`TRANSLATION.md` との照合**: 本タスクは**訳文・辞書・生成物を一切触っていない**
（`data/**` `cards/**` `sets/**` に差分なし）ので、`TRANSLATION.md` の対象外。
`npm run build:cards` / `gen-tl-json.mjs` / `gen-card-meta-index.mjs` の**再生成も不要**
（生成ロジックにも生成物の入力にも触れていない。`npm run validate` が
`generated JSON up to date` / `card term highlights up to date — 2495 ページ / 2517 節` で緑）。

---

## 6. 設計担当への確認事項（⚠️ 実装側では決めなかったもの）

| # | 確認したいこと | いま入っている状態 | 変える場合のコスト |
|---|---|---|---|
| **1** | ⭐ **`.search-top input, .search-top select` に `line-height` まで当ててよいか**（§5-1） | **当てている**（`normal` → `1.25rem`。input の高さ 36→42px でトップと一致） | 1行削るだけ（`line-height: var(--control-lh)` を外す）。⚠️ 外すと高さがトップと 6px 違うままになる |
| **2** | **`.search-top input:focus` の `outline-offset` を `--ring-offset`（2px）に揃えるか**（§5-2） | **据え置き**（1px・`outline` の色もデッキ構築独自の `var(--accent-2)`） | 1行。⚠️ ただし「色は独自・間隔だけ共通」というちぐはぐな状態になるので、**揃えるならフォーカス表現ごと設計してほしい** |
| **3** | **`.search-top .btn`（リセット／検索）にもコントロール用トークンを当てるか**（§5-3） | **据え置き**（角丸8px・padding 9px 16px・font-size .9rem） | 1ルール。⚠️ ⭐ **B（左ペイン化）で `.row-actions` のボタンを横並びにする回に一緒に決めたほうが、見た目の確認が1回で済む** |
| **4** | ⚠️ **`--radius` がデッキ構築で 12→14px になる箇所は3つ**（`tools/deck-builder/style.css` の 125・409・656行）。⭐ **ログイン後にしか見えない面が含まれる**ので、`3000` 番での実物確認のときに**ログインして目視してほしい**（設計 §9-4・R5） | 変更済み（未確認） | — |
| **5** | ⚠️ **本タスクは本番配信物を変える**（`style.css` `tools/**` `shared/**` `scripts/validate.mjs`）。**pushしていない**ので、公開の可否はユーザー判断（設計 §10-5） | 未push（`b41d6411` がローカルに1件） | — |

---

## 付録: 再現手順

```bash
npm run setup:env                 # Playwright（導入済みなら1秒未満でスキップ）
npm run validate                  # V1（exit 0 と成功行2本）
npm run dev                       # 3000番（V4・V6・V7）
npx wrangler pages dev . --local --port 8788   # V5（404.html は npm run dev では返らない）
```

- ブラウザは `chromium.launch({ args: ["--ignore-certificate-errors"] })` ＋
  context `ignoreHTTPSErrors: true`（NortonのHTTPS検査対策）
- V4 の移設前の再現: `git show HEAD~1:index.html` / `HEAD~1:style.css` を別ドキュメントルートに置き、
  残りは作業ツリーへの symlink で 3010 番に並走させる（⚠️ **`git stash` / `git checkout` は使わない**——
  作業ツリーを他セッションと共有しているため）
- ⚠️ 使ったサーバー（3000 / 8788 / 3010）は**待受ポートから PID を引いて個別に停止済み**
  （`pkill -f "scripts/serve.mjs"` は使っていない）。証跡は `tmp/dev/` に残してある
