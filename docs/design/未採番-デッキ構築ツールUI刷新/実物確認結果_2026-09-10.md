# ⭐ ユーザーの実物確認の結果（2026-09-10）— 設計書 1.13 §12-9

⚠️ **この文書は「ユーザーが実物を触った結果」の記録**であって、設計でも判定でもない。
⭐ **§12-9 の7ステップに対する回答がここで確定した**ので、次は**公開の可否の判断**だけが残る。

| 項目 | 値 |
|---|---|
| 実施日 | **2026-09-10** |
| 環境 | ローカル静的サーバー **3000番**（`PORT=3000 node scripts/serve.mjs`） |
| 対象 | HEAD（左ペイン化7単位が全部入った状態）。⚠️ **本番 `27b32ee8` にはまだ出ていない** |
| トップ | `http://localhost:3000/`（実物） |
| デッキ構築 | ⚠️ **静的サーバーではログインできない**ため [`モック_F_アクションバー_2026-09-07.html`](モック_F_アクションバー_2026-09-07.html) 経由（⭐ **表示モード＝現状 / F＝現状**＝候補CSSを注入せず**実装そのもの**を動かす。⭐ **ページ＝デッキ構築**に切り替える） |

## 1. ⭐ 総評

⭐ **ユーザーの評価は「おおむねOK」。** 差し戻しの指摘は無い。

## 2. ⭐ 【判断】4件の回答（＝これで確定）

| # | 内容 | ⭐ **回答** | 結果 |
|---|---|---|---|
| **1-2** | 初期表示（scrollY=0）でボタン行がヘッダぶん約122px 隠れる | ⭐ **(A) このまま**（推奨案） | **実装変更なし**。⭐ **左ペインの高さの再設計（半日〜1日）は起こさない** |
| **4-3** | 861〜1199px で「選択中の条件」が出ず、選んだ内容がバッジの数字でしか見えない | ⭐ **(A) このまま**（推奨案） | **実装変更なし**。⭐ **スマホ表示をコンパクトに保つ狙いを優先** |
| **5-1** | トップのトグルの font-size が 640px以下だけ 16px（既存挙動） | ⭐ **(A) このまま**（推奨案） | **実装変更なし**。⚠️ **16px は iOS の入力欄ズーム防止の値**なので、揃える方向自体が別途の判断 |
| **7-1** | iPad 実機での入力欄タップ時のズーム有無 | ⚠️ ⭐ **保留（確認する実機が無いため、確認せずに進める）** | **実装変更なし**。→ §3 |

⭐ **4件とも実装を変えない**ので、**追加のコミットは発生しない**（＝レビュー・承認をやり直す対象が無い）。

## 3. ⚠️ 7-1（iPad のズーム）の扱い — **起票しない**

⭐ **保留は「症状が未確認」であって「症状がある」ではない。**

- ⚠️ **起票案は作らない。** 設計書 §13 の8と同じ理由で、
  **本人が症状を確認していない起票案を増やさない**（⭐ **放置すると腐る**＝待ち行列 §4 の型）
- ⭐ **直す場合のコストは変わらず1行・30分**（入力欄の `font-size: 16px` の帯を 1199px まで広げるだけ。
  ⚠️ **副作用として 641〜1199px の入力欄が少し大きくなる**）
- ⭐ **引き金**: **ユーザーが iPad で実際にズームを踏んだとき**。そのとき初めて起票する
- ⚠️ **いまの実装は「1199px以下でも入力欄は 14px」**である。⭐ **G-3a の前から同じ**で、
  **今回の変更が持ち込んだ後退ではない**（設計書 §14-3 の4）

## 4. ⭐ 次にやること

1. ⭐ **本タスクの最終承認**（実物確認が終わったので、残る工程はこれと公開だけ）
2. ⚠️ ⭐ **公開の可否の判断（ユーザー）** — 材料は [`承認_F-b_2026-09-08.md`](承認_F-b_2026-09-08.md) §5。
   ⚠️ ⭐ **本タスク以外も同じバッチで出る**（**日本語モードの0件表示**・**行番号参照の検査の一連**）。
   ⭐ **2026-09-10 の「大会データの週1更新」（`587f40c6`・106ファイル）も同じバッチに乗る**
   （⚠️ **経路は `CONFIRM=publish npm run deploy:prod` の1つだけ**＝GitHub 停止中）

---

## 5. ✅ ⭐ 本番公開まで完了（2026-09-10・`ecb2b51d`）

⭐ **ユーザーの明示的な指示のもと、経路②（`CONFIRM=publish npm run deploy:prod`）で公開した。**

| 項目 | 実測 |
|---|---|
| デプロイ | ステージング **4,319ファイル**（`git archive HEAD`・追跡分のみ）／**168ファイルをアップロード**（4,133は再利用）・**14.28秒**。`Compiled Worker successfully` |
| ⚠️ **危険物** | `/tmp/dashboard/index.html`・`/node_modules/…`・`/.wrangler/state`・`/tmp/deploy-stage/…`・`/docs/…` とも **404**（すべて正） |
| 到達確認 | ⭐ **新規の `/shared/css/tokens.css` が 200**（＝**エッジに古いコピーが無い到達マーカー**）。素のURLと `?cb=` を並べて **3回とも全数一致**（トップの `.pane-actions` 5／`MOBILE_MQ` 1199／デッキ構築の `.row-actions` 4／用語集の tokens.css／**P26「収録 61 枚」**／sitemap 3,018 URL） |
| 本番ブラウザ | **1440px**: `.pane-actions` が `sticky` で表示・⭐ **リセットは1つだけ**／**900px**: **FAB が出る**・⭐ **リセットの重複0**／トークン（`--panel: #1b1f28`）が**トップ・デッキ構築・用語集**に適用／`/tournaments/` 454行。⚠️ **コンソールエラーは `/api/me` の401のみ**（未ログインのため＝正常） |

⚠️ **同じバッチで「日本語モードの0件表示」「行番号参照の検査の一連」「2026-09-10 の大会データ週1更新（`587f40c6`）」も公開された**（合計 **122ファイル・+1,884/−418**）。

⚠️ ⭐ **残るのは GitHub への push だけ**（＝ `origin/main` が古い。**未公開ではない**）。
