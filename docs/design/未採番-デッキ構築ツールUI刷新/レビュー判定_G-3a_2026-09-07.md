**対象: 「デッキ構築の左ペイン化」** — この宣言は 2026-09-11 に遡って追記した（索引の転記ドリフト防止）

# レビュー判定 — G-3a：トップページを 1199px 以下でスマホ表示に統一（2026-09-07）

⚠️ **GitHubアカウント停止中のため `gh issue comment` が使えない。** 本ファイルが判定コメントの代替。

| | |
|---|---|
| 設計書 | [`左ペイン化_設計.md`](左ペイン化_設計.md) **版 1.8** の **§14-3（分割表・`MOBILE_MQ` の5使用箇所）/ §14-5（影響範囲）/ §14-6（冒頭の必須条件3点・V43・V45〜V49・M1-b）** |
| 対象コミット | **`9de581a8`**（`style.css` / `app.js` の**2ファイルのみ**） |
| 実装報告 | [`実装報告_G-3a_トップのスマホ表示統一_2026-09-07.md`](実装報告_G-3a_%E3%83%88%E3%83%83%E3%83%97%E3%81%AE%E3%82%B9%E3%83%9E%E3%83%9B%E8%A1%A8%E7%A4%BA%E7%B5%B1%E4%B8%80_2026-09-07.md)（`5c7db364`） |
| 前の周 | **A〜E**（承認済み）・**G-2**（承認 `213ce62e`・実装 `609af054`）＝**再判定していない** |
| 証跡 | `tmp/review/G-3a-トップのスマホ表示統一/`（gitignore・測定JSON8本＋検証スクリプト15本＋スクリーンショット26枚） |
| 検証環境 | **3100**（after＝作業ツリー＝`9de581a8`）と **3101**（before＝`git archive 213ce62e` を `tmp/review/.../before/` へ展開し、**そこの `serve.mjs`** を起動）の2本並走 ＋ Playwright 1.61.1 / chromium（`--ignore-certificate-errors` / `ignoreHTTPSErrors: true`）。⚠️ **3000番（ユーザーの実物確認用）には一切触れていない** |
| 取り違えの検算 | ⭐ **本測定の前に既知の差で検算した**: `/style.css` の `レスポンシブ①` が after **4件** / before **0件**、`/app.js` の `max-width: 1199px` が after **1件** / before **0件**（before は `max-width: 640px` が1件）。**2本が別のツリーを配信していることを確認してから測っている** |

---

## 判定: **合格**

⭐ **報告の申告を根拠にせず、V43・V45〜V49 と M1-b をすべてレビュー担当自身で再実施した。**
⭐ **破壊試験3本も、報告とは独立の方法で（`.controls` ではなく自前の sticky プローブを body 直下に挿して）やり直した。**
⭐ さらに **報告に無い検証を6本足した**（①**2ブロックの宣言の重複を静的に全数照合** ②**逸脱1の必要性を CSSOM で外して実測** ③**`404.html` への波及**（`/style.css` を共有している）④**シートを開いたときの重なり順**（4幅） ⑤**主要5画面の before/after 全数比較**（4幅） ⑥**印刷バーと FAB の退避**）。**すべて期待どおり**。

| 基準 | 結果 | 証跡 |
|---|---|---|
| **M1** 設計適合 | ✅ | **V43・V45・V46・V47・V48・V49(a)(b)(c)(d)** の**全番号**を独立に再実施し、§14-6 の期待値と一致（→ §1）。⭐ 期待値は**画面に出る最終形**で照合した（列数の実値・`grid-template-columns` の実数値・件数文字列・`role`/`aria-modal` の実値・フォーカスが乗った要素名・シート下端の実測余白）。⚠️ 必須条件2点（**5使用箇所を1つずつ**・**900px と 1199px の2点**）も満たしている |
| **M1-b** 既存条件との対称性 | ✅ **適用した・欠落なし** | ⭐ **「新しくスマホ表示になった帯（900 / 1199px）」と「従来からスマホ表示の帯（375 / 640px）」で4つの副作用を突き合わせ、全項目が一致**（→ §3）。**絵柄の追従**（タイル `<img>.src` ＝ `dataset.artUrl` ＝ 🎨バッジの有無が6枚とも一致）／**共有URL(#20)**（`?element=NORM&element=FIRE` が付き、**900px の別タブで開くと同じ画面が復元**・絵柄まで一致。⭐ 添字ではなく `NORM`/`FIRE` の安定キー）／**バッジ集計**（グループ `fbadge`＝`2`・FAB バッジ＝`2`・トグル＝`▾2`）／**件数表示**（`50 件を表示 / 全 1270 件`）。⭐ **1199→1200→1199 の往復でも全項目が不変**（変わるのは `#selected-filters` の `display: none↔block` だけ＝1200px以上でだけ出す既存仕様） |
| **M2** 実駆動 | ✅ | Playwright で **375 / 640 / 641 / 768 / 900 / 1024 / 1199 / 1200 / 1280 px の9幅**を自分で操作。**PC幅（1200/1280）と 375px 幅の両方**あり。スクリーンショット **26枚**（`tmp/review/G-3a-トップのスマホ表示統一/shots/`・before/after × 5幅 × closed/open/scrolled）。⭐ **FAB は `display` を根拠にせず、`is-hidden` の有無・`elementFromPoint` が FAB 自身を返すこと・Playwright の実クリックが通ることの3点**で判定（→ §1 V45） |
| **M3** 回帰 | ✅ | `npm run validate` **exit 0**（`all checks passed.`／`no line-number refs in code comments — 0件（46ファイル走査）`・`shared css tokens in sync — 68 トークン参照 / 未定義 0`・`:root owners in sync — 4ファイル` を含む。**終了コードを単独実行で確認**）。**トップ・カード詳細モーダル・カード個別ページ・デッキ構築・大会一覧**を **375 / 900 / 1199 / 1280px** で開いて before/after を全数比較し **16通りすべて差分0**（`m3.json`）。⭐ **カード詳細モーダルは実際に開いて矩形まで確認**（`.gacd-modal` が開き、`.gacd-body` は 900/1199/1280 で `max-width` 900px を保つ＝全画面シートに落ちていない）。`pageerror` **0件** |
| **M4** 生成物の同期 | ✅ | 生成ロジックは**無変更**（`scripts/` に差分なし）・訳の追加なし＝`gen-tl-json.mjs` / `build:cards` 不要。⭐ **`style.css` / `app.js` の中身が生成物へ埋め込まれる経路が無いことを実測**（`cards/` `sets/` `tournaments/` に `レスポンシブ` / `MOBILE_MQ` の出現 **0件**）。`git diff --name-only 213ce62e..9de581a8` は **`app.js` と `style.css` の2件だけ**＝翌朝のcronが拾う差分は発生しない |
| **M5** 静かな劣化がない | ✅ | ⭐ **破壊試験3本をレビュー担当が独立にやり直した**（→ §2）。**3つとも新しい帯（900 / 1199px）で load-bearing** であり、**戻すと復帰する**ことまで確認した＝「動いたから合格」ではない。⚠️ **G-3a は新しい検査（validate 等）を足していない**ので、破壊試験の対象は他に無い。⭐ **CSS の 1199 と `MOBILE_MQ` の 1199 を結び付けているのは依然コメントだけ**だが、**設計はその受け皿として `E-add-1` を別単位に切り出している**ため G-3a の不合格理由にはしていない（→ **S-1**・§6） |
| **M6** 公開安全性 | ✅ | **配信ファイルの新規・削除 0**（変更2件はどちらも既に公開済みのファイル）。`_headers` / `404.html` / `functions/` / `wrangler.toml` **無変更**＝Pages Functions・キャッシュ・CSPの前提に触れていない（⭐ **そのため 8888 の wrangler 実行は不要と判断した**）。差分に秘匿情報なし（`token`/`secret`/`password`/`api key`/秘密鍵の出現 0）・外部オリジンの追加なし。⚠️ **`style.css` は `404.html` も読む**が、波及は `overflow-x: visible → clip` の1点だけで**幾何は 4幅すべて差分0**（→ §4-3） |
| **M7** スコープ | ✅ | 変更は **`style.css` と `app.js` の2ファイルのみ**（`git -c core.quotepath=false show --name-only`）。§14-3 の分割表と**移設先が完全一致**することを、**2ブロックのセレクタ集合を機械的に抽出して照合**（→ §4-1）。⭐ **`tools/deck-builder/` `shared/` には1バイトも触っていない**＝G-3b は未着手のまま。逸脱2件は報告に明記されており、**逸脱1は妥当と判断**（→ §4-2） |

---

## 1. 設計書の検証項目との対応（⭐ すべてレビュー担当自身の実測値）

### V43 — 境界が1つだけになったこと

**9幅で「スマホ表示のマーカー集合」を測った**（`geom-after.json`）。

| 幅 | `body` | `body` の列 | `.controls` position | backdrop-filter | `html` overflow-x | `.filter-toggle` | `#filter-row` | `#filter-sheet` position | `#filter-fab` | `#results` |
|---|---|---|---|---|---|---|---|---|---|---|
| 375 | block | none | static | none | clip | block | none | **fixed** | flex | contents |
| 640 | block | none | static | none | clip | block | none | **fixed** | flex | contents |
| **641** | block | none | static | none | clip | block | none | **fixed** | flex | contents |
| **768** | block | none | static | none | clip | block | none | **fixed** | flex | contents |
| **900** | block | none | static | none | clip | block | none | **fixed** | flex | contents |
| **1024** | block | none | static | none | clip | block | none | **fixed** | flex | contents |
| **1199** | block | none | static | none | clip | block | none | **fixed** | flex | contents |
| **1200** | **grid** | **320px 880px** | static | **blur(6px)** | **visible** | **none** | **grid** | **static** | **none** | **block** |
| 1280 | **grid** | **320px 960px** | static | **blur(6px)** | **visible** | **none** | **grid** | **static** | **none** | **block** |

⭐ **641〜1199px の5幅は 375 / 640px と 10項目すべてが一致**。**1200 / 1280px は左ペインの形ただ1つ**。
⚠️ **中間の形（上部パネル＝`.controls` が sticky で `#filter-row` が grid のまま）はどの幅にも出ていない**。→ **✅ 合格**

### V45 — トップの FAB が 1199px以下で出ること（⭐ 最重要）

⚠️ **CSS の `display` は根拠にしていない**（`is-hidden` が付いたままでも `display: inline-flex` のままだから）。

| 幅 | スクロール前 `is-hidden` | 1600px スクロール後 `is-hidden` | `elementFromPoint(FAB中心)` | FAB自身か | 矩形 / 右余白 / 下余白 | **実クリック** | 傍証 `matchMedia("(max-width:640px)")` |
|---|---|---|---|---|---|---|---|
| **900** | `true` | ⭐ **`false`** | `SPAN#filter-fab-label` | ✅ | `[784,734,100,50]` / 16px / 16px | ✅ **通った**（シートが開いた） | ⭐ **`false`** |
| **1199** | `true` | ⭐ **`false`** | `SPAN#filter-fab-label` | ✅ | `[1083,734,100,50]` / 16px / 16px | ✅ **通った** | ⭐ **`false`** |

⭐ **旧閾値（640px）では出ないはずの幅で、当たり判定つきで押せている**＝`updateFabVisibility()` の閾値が実際に 1199 になっている。→ **✅ 合格**

### V46 — ボトムシートが画面下端に来ること

| 幅 | `#filter-sheet` position | 矩形 | **下端の余白** | visibility | シート中央の `elementFromPoint` | ベール |
|---|---|---|---|---|---|---|
| **900** | **fixed** | `[0,192,900,608]` | ⭐ **0.00px** | visible | シート内（`DIV.filter-sheet-head`） | opacity 1 / pointer-events auto |
| **1199** | **fixed** | `[0,192,1199,608]` | ⭐ **0.00px** | visible | シート内 | opacity 1 / pointer-events auto |

→ **✅ 合格**（`backdrop-filter` の罠は §2 の破壊試験で「外すと下端に来なくなる」ことまで確認）

### V47 — 寸法まわりが 641〜1199px に漏れていないこと

⭐ **900 / 1199px で before(3101) と after(3100) を照合。寸法44項目が両幅とも不一致0。**

| 見た項目 | 900px（before → after） | 1199px（before → after） |
|---|---|---|
| ⭐ **カード一覧の列数** | **4列 → 4列** | **5列 → 5列** |
| ⭐ `.grid` の `grid-template-columns` の実値 | `201px ×4` → 同じ | `217.391 / 217.406 / 217.391 / 217.406 / 217.391px` → 同じ |
| `.grid` の padding / gap | `16px 24px 48px` / `16px` → 同じ | 同左 |
| ⭐ **入力欄の font-size**（`#q` `#qtext` `#order` `#reset`） | **14px → 14px** | **14px → 14px** |
| ⭐ **`.controls` の padding** | **`16px 24px` → `16px 24px`** | 同左 |
| ⭐ **モーダルの角丸 / max-width / min-height** | **16px / 900px / 0px → 同じ** | 同左 |
| `.modal-close` / `.card-name` / `.card-add` / `.status` / `.site-footer` / `.setlinks` / `.tray-*` / `.qty*` / ヘッダ7項目 | すべて一致 | すべて一致 |
| ⚠️ `.filter-toggle` の font-size / padding | **14px / `10px 14px` → 変化なし** | 同左 |

⭐ **§14-3 が警告した「帯ごと広げるとカード一覧が 4列 → 2列」は起きていない**（4列/5列を維持）。
⭐ **導線以外に出た差分は下流8要素の縦ずれだけ**（900px: **−174.00px** / 1199px: **−124.00px**・幅と高さは1pxも変わらず）＝**条件バーが畳まれた分そのまま**。→ **✅ 合格**

⚠️ ⭐ **設計 §14-3 が予告した「`.filter-toggle` が 641〜1199px で 16px になる」は起きなかった**（HeroUI節の `.controls button`(0,1,1) が `.filter-toggle`(0,1,0) に勝つため 14px のまま）。**想定より変化が小さい方向**なので V47 の合否には影響しない（→ §6 の確認事項）。

### V48 — 640px以下が1pxも変わらないこと（⚠️ 必達）

| 幅 | 幾何・計算値の差分（closed / open） | ⭐ 挙動の差分 |
|---|---|---|
| **375** | **0件 / 0件** | ⭐ **0件** |
| **640** | **0件 / 0件** | ⭐ **0件** |
| **1200** | **0件 / 0件** | （帯外・トグル非表示） |
| **1280** | **0件 / 0件** | （同上） |

- **幾何・計算値**＝**47セレクタ × 37プロパティ ＋ 矩形 ＋ メタ12項目**を、**折りたたみ状態と開いた状態の2状態**で照合。
- ⭐ **挙動**＝FAB の出現（`is-hidden` / `elementFromPoint` / 実クリック）・シートの下端余白・`role`/`aria-modal`/`body.no-scroll`・**ホイールで背面が動かないこと**・フォーカストラップ（Tab4回＋Shift+Tab）・Esc・**1200px へ広げると閉じること**・別入口（インラインのトグル）・ベールでの閉じ・横あふれ の**全シーケンスを before/after で走らせて差分0**（`func-sp-before.json` / `func-sp-after.json`）。
- ⭐ **1200 / 1280px も差分0**＝左ペイン側は無傷。→ **✅ 合格**

### V49 — `MOBILE_MQ` の残り4使用箇所（⚠️ **4つを別々に判定**）

| 使用箇所 | 900px | 1199px | レビュー担当の実測 |
|---|---|---|---|
| **(a) `openFilterSheet()`** | ✅ | ✅ | シートを開くと `#filter-sheet` に **`role="dialog"` / `aria-modal="true"`**、`body` に **`no-scroll`**（`overflow: hidden`）。FAB の `aria-expanded="true"`。⭐ **ホイールで 500px 送っても `scrollY` は 1600 のまま**＝背面が固定されている |
| **(b) Tab のフォーカストラップ** | ✅ | ✅ | シート内の**最後のフォーカス可能要素**から **Tab 4回**：`BUTTON#filter-sheet-close` → `SUMMARY` → `BUTTON` → `BUTTON.on` で**毎回シート内**。**先頭から Shift+Tab** で **`BUTTON#filter-apply`（シート内の末尾）へ巡回**。⭐ **背面へ抜けない**（`inSheet` が5回とも `true`） |
| **(c) Escape で閉じる** | ✅ | ✅ | Esc で `filters-open` が外れ、**`role` / `aria-modal` が `null` に戻り**、`no-scroll` も解除。⭐ **フォーカスは開いた側（`BUTTON#filter-fab`）へ戻る** |
| **(d) `MOBILE_MQ` の `change`** | ✅ | ✅ | シートを開いたまま **1200px へ広げると閉じる**（`filters-open:false` / `role:null` / `no-scroll` 解除）。同時に **FAB は `is-hidden` かつ `display:none`** になる |

→ **✅ 4つとも合格**（⚠️ まとめて「動いた」ではなく、上の4行を別々の測定として取得している）

---

## 2. 破壊試験（⭐ レビュー担当が独立に再実施）

⚠️ **「動いたから合格」では、対策が効いているのか偶然動いているのか区別できない**（CLAUDE.md）。
⚠️ **CSP `style-src 'self'` のため `<style>` 注入は無言で無視される**ので、**`/style.css` のシートに `insertRule` で `!important` を被せて実行時に外した**（ファイルは変更していない）。
⭐ **報告とは独立の方法を採った**——対策①は `.controls` ではなく **`position: sticky; top: 0` の自前プローブを `body` 直下に挿して**測っている（`.controls` は今 `static` なので、`.controls` で測ると対策②と混ざる）。

| 外した対策 | 期待される破綻 | **900px** | **1199px** | 戻すと | 判定 |
|---|---|---|---|---|---|
| `html, body { overflow-x: clip }` → **`hidden`** | スクロールコンテナ化で**子孫の `position: sticky` が死ぬ** | sticky プローブの `top` が **0 → −1200** | **0 → −1200** | **0 に復帰** | ✅ **load-bearing** |
| `.controls { position: static }` → **`sticky`** | 条件バーが画面外に出ず **FAB が永久に出ない** | `.controls` の `top` が **−1494.06 → 0**、**FAB の `is-hidden` が `false` → `true`** | **−1522.16 → 0**、同上 | **両方とも復帰** | ✅ **load-bearing** |
| `.controls { backdrop-filter: none }` → **`blur(6px)`** | `fixed` の包含ブロックができ**シートが画面下端に来ない** | シート `bottom` が **800 → −1368.06**（下端との差 **0 → 2168.06px**） | **800 → −1396.16**（差 **0 → 2196.16px**） | **800 / 差0 に復帰** | ✅ **load-bearing** |

⭐ **3つとも「新しい帯でも効いている」ことが数値で確定**した。⭐ **戻して復帰することまで見ているので、測定の副作用ではない。**

---

## 3. M1-b — 既存の帯で効いている副作用が、新しい帯でも効くか

⭐ **「新しくスマホ表示になった帯」と「従来からスマホ表示の帯」の対称性**で見た（G-3a は絞り込み条件を足す単位ではないが、**帯を足す単位**なので同じ形で当てはめた）。
エレメント **NORM ＋ FIRE** を選んで測定（`m1b.json`）。

| 副作用 | 1199px（新しい帯） | 1200px（左ペイン） | 1199px（戻し） | ⭐ 900px の**別タブ**で共有URLを開いた |
|---|---|---|---|---|
| **共有URL(#20)** | `/?element=NORM&element=FIRE` | 同じ | 同じ | ⭐ **同じURL・`checked` が `["NORM","FIRE"]` で復元** |
| **バッジ集計** | グループ `fbadge`=**2**（可視）・FAB=**2**・トグル=`▾2` | 同じ | 同じ | ⭐ **FAB=2 / トグル=▾2** |
| **件数表示** | `50 件を表示 / 全 1270 件` | 同じ | 同じ | ⭐ **同じ** |
| **絵柄の追従** | 先頭6枚の `<img>.src` ＝ `dataset.artUrl` ＝ 🎨バッジの有無 | **一致** | **一致** | ⭐ **一致**（`fcng06fi6r.jpg` / `fiszdne4n3.jpg`(🎨) / `2l8lbewemh.jpg`(🎨) / …） |
| 選択中の条件 | `選択中の条件 ノーム NORM✕火 FIRE✕すべて解除`（`display:none`） | **同じ文言**（`display:block`） | `display:none` に戻る | — |
| AND/OR | `["OR","OR","OR","OR","OR"]` | 同じ | 同じ | — |

⭐ **1199→1200→1199 の往復で変わるのは `#selected-filters` の `display` だけ**＝**1200px 以上でだけ出す既存仕様どおり**（E の Q-E2 で確定済み）。
⚠️ **「新しい帯では効かない副作用」は1つも見つからなかった。** → **✅ M1-b の欠落なし**

---

## 4. スコープと逸脱の検証

### 4-1. ⭐ 2ブロックの分割が §14-3 の表と一致しているか（静的に全数照合）

`style.css` の2ブロックから **セレクタ＋プロパティの対**を機械抽出して比較した。

| | 宣言数 | セレクタ |
|---|---|---|
| **①`@media (max-width: 1199px)`（絞り込みの導線）** | **126** | `html` `body` `.controls` `.filter-toggle` `.filter-row` `.controls.filters-open .filter-row` `.fgroup + .fgroup` `.filter-rest` `.controls.filters-open .filter-rest` `.controls.filters-open .filter-rest select` `#filter-sheet` `.controls.filters-open #filter-sheet` `#filter-sheet-grab` `#filter-sheet-head` `#filter-sheet-title` `#filter-sheet-close` `#filter-sheet-foot` `#filter-apply` `#filter-veil` `.controls.filters-open ~ #filter-veil` `#filter-fab` `#filter-fab.is-hidden` `#filter-fab-badge` `#filter-fab-badge[hidden]` |
| **②`@media (max-width: 640px)`（スマホの寸法）** | **78** | ヘッダ系・`.controls{padding}`・`.status`・`.site-footer`・`.setlinks`・入力欄の16px・`#q/#qtext`・`#order/#reset`・タップ領域・`.grid` の2列・`.card-name*`・`.modal-body`・`.modal-close`・`.tray-options` |

- ⭐ **①のセレクタ集合は §14-3 の「1199px以下へ移す」欄と完全一致**（`.filter-rest { margin-top }` を除く＝逸脱1）。
- ⭐ **同じ「セレクタ＋プロパティ」の対は0件**＝**2ブロックの並び順に依存しない**（`.controls` は①が `position`/`backdrop-filter`、②が `padding` で衝突しない）。報告の逸脱3の主張を独立に確認した。

### 4-2. ⭐ 逸脱1（`.filter-rest { margin-top: 10px }` も 1199 側へ移した）— **妥当と判断する**

**CSSOM で `margin-top: 0 !important` を被せて実測**した（`leftover.mjs`）:

| 幅 | 通常 | `margin-top` を外すと |
|---|---|---|
| 640 | `.filter-rest` の `margin-top` **10px** / 直前の `.fgroup` との間隔 **10px** | 間隔 **0px** |
| **900** | **10px** / **10px** | ⭐ **0px（直前の `.fgroup` に密着）** |
| **1199** | **10px** / **10px** | ⭐ **0px（同上）** |

- ⭐ **報告の根拠（グリッドの `gap` はブロックレイアウトでは効かない／`.fgroup + .fgroup { margin-top: 8px }` と対になる行）が実測で裏付けられた。**
- ⚠️ **640px 以下への影響はゼロ**（同じ規則が広い帯に移っただけ＝V48 が差分0）。
- ⭐ **仕様の変更ではなく、分割表の列挙の取りこぼしを埋めたもの**と判断する（§14-3 は「`.filter-rest` の開閉時グリッド」としか書いていないが、**この1行が無いと 641〜1199px の見た目だけが ≤640px と違ってしまい、「スマホ表示に統一」という単位の目的に反する**）。**設計担当が違う意図なら §14-3 の表に1行足すだけで済む**（→ §6）。

### 4-3. ⭐ `404.html` への波及（報告に無い・レビューが追加）

⚠️ **`/style.css` を読むのはトップだけではない**（`404.html` も読む。`tools/*` は各自の `style.css`）。

| 幅 | `404.html` の before/after 差分 |
|---|---|
| 640 | **0件** |
| **900** | ⭐ **`html`/`body` の `overflow-x` が `visible → clip` の1点のみ**（ヘッダ・タグライン・ブランド・本文・フッタの矩形/padding/font-size は**すべて一致**・`scrollWidth == clientWidth`） |
| **1199** | 同上 |
| 1200 | **0件** |

⭐ **`clip` はスクロールコンテナを作らないので `sticky` を壊さない**（設計 §14-3 の対策①の趣旨そのもの）＝**害はない**。⚠️ ただし**報告は「トップの style.css」と書いており、共有先の存在に触れていない**ので **参考**として記録する（→ §5）。

### 4-4. その他のスコープ確認

- `git -c core.quotepath=false show --name-only --format= 9de581a8` → **`app.js` / `style.css` の2件のみ**。
- `git diff --name-only 213ce62e..9de581a8` → 同じ2件（＝前工程のコミット以降、担当外のパスに手が入っていない）。
- 差分に `token` / `secret` / `password` / `api key` / 秘密鍵の文字列 **0件**。
- ⭐ **`app.js` の非コメント差分は1行だけ**（`MOBILE_MQ` の `640` → `1199`）。他はコメントの追記。

---

## 5. 参考（合否には含めない・レビューの観測）

| # | 内容 |
|---|---|
| 1 | ⭐ **印刷バーと FAB の退避は新しい帯でも効く**。印刷リストに1枚入れて 1600px スクロールした状態で、`--printbar-h` は **640 / 900 / 1199px とも実測 60px**、**FAB の下端 724 / バーの上端 740 ＝ 16px の隙間**（重なり 0）。⭐ **3幅で完全に同じ値**＝退避量が固定値ではなく実測値で動いている |
| 2 | ⭐ **シートを開いたときの重なり順も3幅で同一**。`#filter-veil`(z-index 17) / `#filter-sheet`(18) で、**ヘッダの中心を叩くとベールが返る**（375 / 640 / 900 / 1199px の4幅とも同じ）＝背面が触れない |
| 3 | ⭐ **1199px ではシート内の入力欄が非常に横長になる**（`.filter-rest` が `1fr 1fr` なので選択肢が **583.5px ×2**・トグルは **852〜1175px**）。⚠️ **左ペイン化の狙い G1「検索入力が画面幅に比例して間延びしない」とは逆方向**だが、**これは 1.5 でユーザーがモックを見て選んだ形**であり、G-3a の実装上の欠陥ではない。⭐ **実物確認（3000番）で必ず触ってもらう項目**（設計 §12-9 の4） |
| 4 | ⚠️ **G-3b が未着手なので、いま 641〜1199px は「トップ＝スマホ表示・デッキ構築＝上部パネル」の食い違いが残る**。⭐ **実測でもデッキ構築の `.filter-toggle` は 900 / 1199px で `display: none`**（＝まだ 860px 境界）。**§14-5 の想定どおりで、G-3a の不合格理由にはしていない** |
| 5 | ⭐ **`404.html` への波及**（§4-3）。害は無いが、**次に `style.css` の帯を触る人が「トップだけ」と思い込まないよう**記録しておく |

---

## 6. 推奨基準の指摘（合格を妨げない）

| ID | 指摘 | レビューの見立て |
|---|---|---|
| **S-1** | ⭐ **CSS の `@media (max-width: 1199px)`（レスポンシブ①）と `app.js` の `MOBILE_MQ = 1199` を結び付けているのは、依然としてコメント2か所だけ**。⚠️ **片方だけ戻すと、`validate` は緑・1200px以上の見た目も無変化のまま、641〜1199px で FAB が永久に出ない**（＝設計 §14-6 が「無言の失敗」と名指しした型が、帯が広がった分だけ被害範囲を増した）。⚠️ ⭐ **`E-add-1` の検査②は `PANE_MIN_WIDTH` と CSS の `min-width` しか見ないので、この対はいまの仕様では拾えない**（規則2「`max-width` はすべて `PANE_MIN_WIDTH` 未満」は、CSS を 640 に戻しても通ってしまう） | ⭐ **`E-add-1` の検査②に「トップの `MOBILE_MQ` ＝ そのページCSSの `max-width` の最大値 ＝ `PANE_MIN_WIDTH` − 1」を1本足すのが最小の手当て**と考える（**両辺が別ファイル＝CSSとJSなので空回りしない**）。⚠️ **G-3a で足すのは M7 違反**（設計は E-add-1 を別単位に切っている）なので**指摘に留める**。**採否は設計担当の判断** |
| **S-2** | `.filter-toggle` の文字サイズが **≤640px で 16px・641〜1199px で 14px** と境界で変わる（HeroUI節の `.controls button` が勝つため）。⭐ **G 以前からある挙動で G-3a は変えていない** | ⭐ **揃えるかどうかは設計担当の判断**。⚠️ **ボタンなので iOS のズーム対象ではなく実害は無い** |

⚠️ **S-1・S-2 とも「気になること」ではなく基準IDに紐づけてあるが、必須基準の欠落ではないので合否には含めていない。**

---

## 7. 実行できなかった検証・残っている確認

| # | 項目 | 状況 |
|---|---|---|
| 1 | **実機（iOS / iPadOS / Android）** | ❌ 未実施（環境なし）。ヘッドレス Chromium のビューポート指定で代替。⚠️ ⭐ **iPad 縦（768px CSS幅）は今回「スマホ表示」の帯に入るが、入力欄は 14px のまま**なので **Safari のフォーカス時ズームが起きうる**。⭐ **ただし G-3a の前も 768px の入力欄は 14px だったので、これは回帰ではなく既存の状態**（→ §6 の設計担当への確認事項1） |
| 2 | **本番（Cloudflare Pages）での確認** | ❌ 未実施（**push していない**）。⭐ **`_headers` / `404.html` / `functions/` / キャッシュに触れていないので、本番でしか確かめられない項目は今回発生していない**＝**push を要求しない**（合否は残り全部で確定させた） |
| 3 | **`prefers-reduced-motion`** | ❌ 未実施。既存ブロックは無変更（`#filter-sheet` / `#filter-veil` / `#filter-fab` を対象にしたまま）で、**帯の指定を持たないので新しい帯にもそのまま効く**はずだが、実駆動では確かめていない |
| 4 | **クラシックなスクロールバー環境**（15〜17px） | ❌ 未検証（ヘッドレスはオーバーレイ）。⭐ **ユーザーの実物確認（3000番）で見てもらうのが確実** |
| 5 | **V44 / V50 / V51 / V52 / V53 / V54 / V55** | ⭐ **G-2（判定済み）・G-3b・E-add-1 の担当なので今回の範囲外** |

---

## 8. 設計担当への確認事項

⭐ **合否を左右するものは無い**（＝`設計確認待ち` へ**合格**として送る）。

| # | 内容 | レビューの見立て |
|---|---|---|
| 1 | ⭐ **641〜1199px の入力欄を 16px にするか**（開発の確認事項1）。⚠️ **iPad 縦（768px）が新しく「スマホ表示」の帯に入った**ので、**フォーカス時ズームの当事者が増えた**。⭐ **ただし 16px を 1199px まで広げると `.controls` の見た目が PC 幅で変わる**ため、**寸法側の設計判断**である | ⭐ **回帰ではない**（G-3a 前も 768px は 14px）ので**急がなくてよい**が、「スマホ表示に統一」と言い切る以上、**どちらに倒すかを設計書に1行残す**のが望ましい |
| 2 | ⭐ **`.filter-toggle` の 16px は起きなかった**（§14-3 の予告が外れた）。**§14-3 の注記と V47 の「16pxだけは変わってよい」を訂正するか** | ⭐ **訂正する価値はある**（次に読む人が「変わるはず」と思って探す）。**承認の根拠には影響しない** |
| 3 | ⭐ **逸脱1（`.filter-rest { margin-top: 10px }` の移設）を §14-3 の分割表に取り込むか** | ⭐ **取り込むのが正しい**と考える（§4-2 で必要性を実測した）。**戻す必要は無い** |
| 4 | ⭐ **S-1（`MOBILE_MQ` と CSS 1199 の対を `E-add-1` の検査②に含めるか）** | ⭐ **含める価値はある**。**`E-add-1` はまだ `実装待ち` なので、いま仕様を1行足すのが最も安い** |

---

## 9. ⭐ 次の単位（G-3b）への申し送り

| # | 内容 |
|---|---|
| 1 | ⭐ **G-3b は「デッキ構築の折りたたみトグルの帯を 860 → 1199px に広げるだけ」**（§14-4 でユーザーが確定）。⚠️ **FAB＋ボトムシートは範囲外**——起票案に切り出し済み |
| 2 | ⚠️ ⭐ **デッキ構築側も「帯ごと広げてはいけない」**（§14-3）。**移すのは `.search-top .filter-toggle` / `.filter-row { display:none }` と `.filters-open` の開放 / `.filter-row select` の全幅 / `.filter-row .fgroup` の全幅 の4つだけ**で、**`.artgrid` の2列・入力欄の16px・`.modal-body` の全画面シート化は 640px / 620px に残す**。⭐ **本レビューが使った「44の寸法プロパティ × 2幅の before/after 照合」がそのまま使える**（スクリプトは `tmp/review/G-3a-トップのスマホ表示統一/geom.mjs` と `diff.mjs`。**セレクタ配列を差し替えるだけ**） |
| 3 | ⚠️ ⭐ **デッキ構築に `MOBILE_MQ` 相当は無い**（FAB が無いので）。**CSS だけで完結するはず**だが、**`PANE_MIN_WIDTH`（1200）と新しい `max-width: 1199px` が重ならないこと**は必ず測る（E-add-1 の検査②の規則2に相当） |
| 4 | ⚠️ **デッキ構築はログインが要る**ので、到達手順は **srcdoc ハーネス**（設計 §9-1 / モック説明 §2）。⭐ **`.filter-toggle` の `display` は 900 / 1199px でいま `none`**（＝860px 境界のまま）なので、**G-3b の差分はここからの純増として読める** |
| 5 | ⚠️ **before/after 並走は展開先の `serve.mjs` を起動**し、⭐ **本測定の前に「既知の差」で検算してから測る**（本レビューは `/style.css` と `/app.js` の文字列で検算した） |
| 6 | ⚠️ **3000番はユーザーの実物確認用。止めない。** 本レビューは **3100 / 3101** を使い、**待受ポートから PID を引いて個別に停止**した |
| 7 | ⭐ **G-3a と G-3b が揃ってはじめて §14-5 の表が成立する**（いまは 641〜1199px でトップとデッキ構築の見え方が食い違う）。**ユーザーの実物確認は G-3b の後にまとめて行うのが自然** |
