**対象: 「デッキ構築の左ペイン化」** — この宣言は 2026-09-11 に遡って追記した（索引の転記ドリフト防止）

# レビュー判定 — G-2：左ペインの境界を 1200px に統一（2026-09-07）

⚠️ **GitHubアカウント停止中のため `gh issue comment` が使えない。** 本ファイルが判定コメントの代替。

| | |
|---|---|
| 設計書 | [`左ペイン化_設計.md`](左ペイン化_設計.md) **版 1.7** の **§14-1 G-2 / §14-2（実測）/ §14-3（置換チェックリスト）/ §14-6（V44・V51・V52・V53）/ §12-8（V42）** |
| 対象コミット | **`609af054`**（`app.js` / `shared/js/card-search.js` / `tools/deck-builder/{app.js,index.html,style.css}`） |
| 実装報告 | [`実装報告_G-2_境界1200_2026-09-07.md`](実装報告_G-2_%E5%A2%83%E7%95%8C1200_2026-09-07.md)（`ca72af8d`） |
| 前の周 | **A**（承認 `7abc072d`）・**B・C**（`52bd2685`）・**D**（`1856aa24`）・**E**（`543ab9d9`）＝**再判定していない** |
| 証跡 | `tmp/review/G-2-境界1200/`（gitignore・測定JSON10本＋検証スクリプト9本＋スクリーンショット16枚） |
| 検証環境 | **3100**（変更後＝作業ツリー）と **3101**（変更前＝`git archive 609af054^` を別ディレクトリへ展開し、**そこの `serve.mjs`** を起動）の2本並走 ＋ Playwright 1.61.1 / chromium-1228（`--ignore-certificate-errors` / `ignoreHTTPSErrors: true`）。⚠️ **3000番（ユーザーの実物確認用）には一切触れていない** |

---

## 判定: **合格**

⭐ **報告の申告を根拠にせず、V42・V44・V51・V52・V53 を全部レビュー担当自身で再実施した。**
⭐ さらに **報告に無い検証を4本足した**（①**破壊試験**：CSS閾値だけ戻すと無言の劣化帯が再現すること
②**帯の外の全数比較**（7幅×80項目）③**M1-b をレアリティ絞り込みで新旧の帯を突き合わせ**
④**トップの回帰**（6幅×29項目））。**すべて期待どおり**。

| 基準 | 結果 | 証跡 |
|---|---|---|
| **M1** 設計適合 | ✅ | **V42・V44・V51・V52・V53 の5項目すべて**をレビュー担当が独立に再実施し、§14-6 / §12-8 の期待値と一致（→ §1）。⭐ **設計 §14-2 の実測表（1279→142px / 1240→135px / 1200→129px・いずれも6列・`.stats-pane` 880px・横あふれ0）を、今回のコードで数値まで再現**（→ §1 V44）。⚠️ **V43 は G-3a/G-3b の担当なので合否に含めていない**（リード・実装報告の申し送りどおり） |
| **M1-b** 既存条件との対称性 | ✅ **適用した・欠落なし** | ⭐ **「新しく左ペインになった帯（1240px）」と「従来から左ペインの帯（1280px）」でレアリティ絞り込みの副作用3つを突き合わせ、全項目がバイト一致**（→ §3）。**絵柄の追従**（`<img>.src`・`dataset.artUrl`・🎨バッジの3面とも `WuXuRE0rl3.jpg`(PRDSD) → `q2Owup2V4N.jpg`(PRD 1st)）／**バッジ集計**（`0`+`hidden` → `1`+可視）／**件数表示**（`7 件` → `4 件` → `4 件を表示（レベルを持つカードのみ）`）。⭐ **数値ソート（レベル順）に変えても PRD 1st の絵柄のまま**＝`result_editions` に依存していない（V19 の趣旨）。⚠️ **共有URL(#20) はデッキ構築に存在しない**ので3つで正しい（設計 §9-3 と同じ理由） |
| **M2** 実駆動 | ✅ | Playwright で **375 / 640 / 900 / 1024 / 1199 / 1200 / 1240 / 1279 / 1280 / 1440 px の10幅**を自分で操作（デッキ構築へは srcdoc ハーネス＝設計 §9-1・モック説明 §2）。**PC幅と375px幅の両方**あり。スクリーンショット16枚（`tmp/review/G-2-境界1200/shots/`）。⭐ **✕の押下は `elementFromPoint` が自分自身を返すことを確かめたうえで実クリック**している |
| **M3** 回帰 | ✅ | `npm run validate` **exit 0**（`all checks passed.`／`shared css tokens in sync — 68 トークン参照 / 未定義 0`・`:root owners in sync — 4ファイル`・`no line-number refs in code comments — 0件（46ファイル走査）` を含む）。**トップ・カード詳細モーダル・カード個別ページ・セットページ・大会一覧・デッキ構築**を **1400px と 375px** で開いて `pageerror` **0件** / `requestfailed` **0件**。⭐ **カード詳細モーダルは実際に開いて中身まで確認**（`#gacd-modal` `display:block`・見出し「アエラ、西風の使徒」・画像3枚。1400/375 とも） |
| **M4** 生成物の同期 | ✅ | ⭐ **挙動を変える差分は2行だけ**（下の M7）。`scripts/` `data/` `cards/` `sets/` `tournaments/` `sitemap.xml` に**1件も差分が無い**（`git diff --name-only 543ab9d9..HEAD`）。生成ロジック無変更・訳の追加なし＝`gen-tl-json.mjs` / `build:cards` 不要。⭐ **`shared/js/card-search.js` と `app.js` の変更は非コメント行0**なので、生成物への埋め込み経路も動かない |
| **M5** 静かな劣化がない | ✅ | ⭐ **破壊試験を実施した**（→ §2）。**1240px で CSS の `@media (min-width:1200px)` だけを 1280px に戻すと、`#s-selected-filters` が `display:none` に落ちる一方でアコーディオン（JS＝1200）は活性のまま**＝**設計 §14-3 が警告した「無言の劣化帯」がその場で再現**した。⭐ **いま出荷されるコードにはこの帯が無い**ことを、同じ測定で対照して確認している（＝V51/V53 の検査は空回りしていない）。⚠️ G-2 は新しい検査（validate 等）を足していないので、破壊試験の対象は他に無い。⭐ **閾値のずれを止める自動検査が無い**点は **S-1** として指摘（合否には含めない——設計 §14-3 自体がコメントでの担保しか求めていない） |
| **M6** 公開安全性 | ✅ | **新規・削除の配信ファイル 0**（追加は `docs/` の報告1件のみ＝本番配信対象外）。`_headers` / `404.html` / `functions/` / `wrangler.toml` **無変更**＝Pages Functions・キャッシュ・CSPの前提に触れていない（⭐ **そのため 8888 の wrangler 実行は不要と判断した**）。差分に秘匿情報なし・外部オリジンの追加なし |
| **M7** スコープ | ✅ | 変更は設計 §14-3 のチェックリストどおりの**5ファイルのみ**。⭐ **コメントを機械的に除去して比較したところ、挙動を変える差分は次の2行だけ**（→ §4）:<br>`tools/deck-builder/style.css`: `@media (min-width: 1280px)` → `(min-width: 1200px)`<br>`tools/deck-builder/app.js`: `const PANE_MIN_WIDTH = 1280;` → `1200;`<br>`index.html` は**コメント除去後に完全一致**、`app.js`（トップ）と `shared/js/card-search.js` は**非コメント行の差分 0**。⭐ **G-3a / G-3b / F / E-add-1 には一切手が付いていない** |

---

## 1. 設計書の検証項目との対応（⭐ すべてレビュー担当自身の実測値）

到達手順は設計 §9-1 の srcdoc ハーネス（実データのデッキ＝マテリアル12／メイン60／サイド9・900px高）。

| # | 期待値 | 実測 | 合否 |
|---|---|---|---|
| **V42** | ⭐ **境界の直下・直上**。1199 / 1200 でレイアウトが**それぞれ1つの形にだけ落ちる**（中間の壊れた形が無い） | ⭐ **切り替わる項目が同じ境界でまとめて反転**した。**1199**: `#view-editor` `display:block` / `grid-template-columns:none` ／ `#search-top` `position:static`・幅1199・`border-right:0px`・`.row` は `flex-direction:row` ／ `#result-modal` `left:0px` ／ `#s-selected-filters` `display:none` ／ アコーディオン**不活性** ＝ **1カラムの形ただ1つ**。**1200**: `display:grid` / `320px 880px` ／ `sticky`・幅320・`border-right:1px`・`.row` は `column` ／ `left:320px` ／ `display:block` ／ アコーディオン**活性** ＝ **左ペインの形ただ1つ**。⚠️ **混ざった形（片方だけ切り替わった状態）はどの項目にも出ていない**。⭐ 併せて **1200〜1279 の3幅すべてで 1280px と同じ形**になることも確認 | ✅ |
| **V44** | 1200〜1279px で 左ペイン320・右列が残り・メイン**6列**・`.stats-pane` に横あふれ無し・`.editor-bar` 1行。⚠️ **最長デッキ名でも横あふれ無し** | 下の表。**1200/1240/1279 とも 6列**・`.stats-pane` は **幅880 / left 320 / scrollWidth−clientWidth = 0**・`documentElement` の横あふれ **0**・`.editor-bar` **57.47px（1行）**。**最長デッキ名（36文字）**では `.editor-bar` **101.94px（2行）**になるが、**1200 / 1279 とも 6列・横あふれ 0・`.stats-pane` あふれ 0**（`m2-after-long.json`） | ✅ |
| **V51** | `PANE_MIN_WIDTH` と `#s-selected-filters` の CSS 閾値が**どちらも 1200**（**1199 で効かず・1200 で効く**を実測） | **1199**: `#s-selected-filters` の実効 `display` = **none**（矩形 0×0。⚠️ **`.has-selection` クラス自体は付いている**＝共有モジュールは動いており、出す/出さないを決めているのは CSS だけ）／アコーディオン**不活性**（2群を順に開いて両方 `open`）。**1200 / 1240 / 1279**: `display` = **block**（矩形 **287×73.63**）／アコーディオン**活性**（1群目が自動で閉じる）。⭐ **2つが同じ 1200 で同時に反転**し、片方だけの帯は存在しない。⭐ **CSSOM 上で `min-width:1200px` のメディアルールは 1本だけ**（破壊試験で計数）＝CSS側は閾値を1箇所しか持たない | ✅ |
| **V52** | `#view-login` / `#view-decks` / `#view-deck` の top / left / width が **G の前と一致**（1200px以上と1199px以下の両方） | ⭐ **10幅すべて（375〜1440）で before と after が完全一致**。**2つの状態で測った**: ①編集画面を開いた状態＝3ビューとも `hidden` かつ `display:none`（全幅） ②**`#view-editor` を `hidden` にして `#view-decks` を出した状態**（V10 が心配した経路）＝`#view-editor` は **1200px以上でも `display:none`**（`#view-editor[hidden]` のルールが効いている）・`#view-decks` は **top 120.64 / left 0 / width＝画面幅**。⚠️ **帯の内側（1200/1240/1279）でも3ビューの差分は 0**（→ §2 の表） | ✅ |
| **V53** | **1200px で「選択中の条件」の✕が押せる／1199px では出ない**。CSS の閾値と `PANE_MIN_WIDTH` の**両方が 1200** | ⭐ **押せるかどうかではなく「✕が実際に再検索を起こすところまで」確かめた**。エレメント`FIRE` ＋ タイプ`ALLY` を選び 🔍検索 →`24 件を表示 / 全 124 件`：<br>**1199**: `display:none`・✕の矩形 **0×0**・`elementFromPoint` の対象なし → **クリック不能**・api.gatcg.com への追加リクエスト **0**・件数 `全 124 件` のまま（＝G前と同じ挙動）。<br>**1200 / 1240 / 1279**: `display:block`・✕の矩形 **80.91×27.69**・**`elementFromPoint(中心)` が✕自身を返す** → **実クリックで api.gatcg.com へ +21リクエスト**・件数が **`全 124 件` → `全 744 件`** に更新・**モーダルは開いたまま**（`hidden:false`）。⭐ **3幅とも同一値**。⭐ **「左ペインは出るのに選択中の条件だけ出ない帯」は存在しない** | ✅ |

### V44 の実測値（900px高・実データのデッキ）

| 画面幅 | `#view-editor` の列 | メイン列数 | タイル幅 | `.stats-pane`（幅 / left / あふれ） | ページ横あふれ | `.editor-bar` |
|---|---|---|---|---|---|---|
| 1199（帯の外・1カラム） | `none` | 8列 | 133.375px | 880 / 0 / **0** | **0** | 57.47px |
| **1200**（境界） | `320px 880px` | **6列** | **128.656px** | **880 / 320 / 0** | **0** | 57.47px |
| **1240** | `320px 920px` | **6列** | **135.328px** | 880 / 320 / **0** | **0** | 57.47px |
| **1279** | `320px 959px` | **6列** | **141.828px** | 880 / 320 / **0** | **0** | 57.47px |
| 1280（従来から左ペイン） | `320px 960px` | 6列 | 142px | 880 / 320 / 0 | 0 | 57.47px |
| 1440 | `320px 1120px` | 7列 | 142.844px | 880 / 320 / 0 | 0 | 57.47px |

⭐ **設計 §14-2 の承認の根拠（「列は落ちず6列を保つ」「320+880=1200 でちょうど収まり横あふれ0」）が、今回のコードでそのまま成立している。**
⚠️ **1200〜1279px で 8列→6列になるのは意図した変更**（§14-7 でユーザーが承認済み）なので、劣化として扱っていない。

---

## 2. ⭐ レビューが追加した検証① — 帯の外に副作用が1つも漏れていないこと（全数比較）

⚠️ **「変更後が期待どおり」だけでは足りない。** 3101（変更前 `609af054^ = 543ab9d9`）に**同じ測定スクリプト**を流し、
**1幅あたり80項目**（レイアウト・アコーディオンの実効・選択中の条件の矩形と当たり判定・統計パネル・他3ビュー×2状態・
入力欄の `font-size`・`.artgrid` の列数・モーダルの角丸/最大幅・`.filter-toggle` の表示と `font-size`）を突き合わせた。

| 画面幅 | 差分 | 読み方 |
|---|---|---|
| **375 / 640 / 900 / 1024 / 1199** | ⭐ **0件** | **帯の外（狭い側）に漏れなし**。G-3a/G-3b の担当範囲は手つかず＝正 |
| **1200 / 1240 / 1279** | **23件** | ⭐ **すべて「上部パネル → 左ペイン」の意図した差**（`viewEditorDisplay` `block→grid` ／ `searchTopPos` `static→sticky` ／ `resultModalLeft` `0px→320px` ／ `selectedFilters` `none→block` ／ `accordionActive` `False→True` ／ `mainCols` `8→6`（1279 は `9→6`） ／ `.stats-pane` の `left 0→320` など）。⚠️ **他3ビューの矩形は帯の内側でも差分 0** |
| **1280 / 1440** | ⭐ **0件** | **帯の外（広い側）に漏れなし**。従来から左ペインだった幅は1pxも動いていない |

⭐ **取り違えの検算を先にやってから本測定に入った**（レビューEの申し送り）:

| 測ったもの | before(3101) | after(3100) |
|---|---|---|
| 配信された `style.css` の `min-width: 1280px` 出現数（curl） | **1** | **0** |
| ⭐ **ブラウザで測った左ペインの発生境界** | **1280px**（1199〜1279 は `display:block`） | **1200px**（1200 から `display:grid`） |

⚠️ **「配信物の文字列」だけでなく「ブラウザ上の挙動の境界」でも別物であることを確認**しているので、
「全項目に差なし」の偽の緑ではない。

### トップページの回帰（コメントしか変えていないが JS を触ったため）

**375 / 640 / 900 / 1199 / 1200 / 1280 px の6幅 × 29項目**（アコーディオンの実効・`#selected-filters` の矩形・
`.controls` の `position`/`padding`/`backdrop-filter`・`.grid` の列数・FAB・シート・`html` の `overflow-x`・
入力欄の `font-size`・リセットの可視状態）を before/after で比較 → ⭐ **差分 0件**・`pageerror` **0件**。
⚠️ **トップの境界は 1200px のまま**（1199＝アコーディオン不活性・`#selected-filters` 非表示／1200＝活性・表示）で、
**`MOBILE_MQ` はまだ 640px**（＝G-3a が未着手であることの裏取り。正）。

---

## 3. ⭐ レビューが追加した検証② — M1-b（新しい帯で副作用が同じに効くか）

⚠️ **G-2 は絞り込み条件を足す変更ではないが、「1200〜1279px が新しく左ペインになった」＝
その帯で初めて『選択中の条件』とアコーディオンが効き始める**。
⭐ **そこで「新しい帯（1240px）」と「従来からの帯（1280px）」で、レアリティ絞り込みの副作用3つを突き合わせた。**

カード名 `lorraine` に固定し、レアリティ **CSR（7）** で絞り、さらに並び替えを**レベル順**に変えた。

| 副作用 | 1240px（新しい帯） | 1280px（従来の帯） | 判定 |
|---|---|---|---|
| **絵柄の追従** | 既定 `WuXuRE0rl3.jpg`（🎨 4・PRDSD） → CSR で `q2Owup2V4N.jpg`（🎨 4・**PRD 1st**） → レベル順でも `MDhADlH3Zk.jpg`（🎨 11・**PRD 1st**）。⭐ **`<img>.src` と `dataset.artUrl`（印刷リストが読む面）が同じ値で追従** | **完全に同じ値** | ✅ 一致 |
| **バッジ集計** | `#s-filter-badge` `0`＋`hidden` → **`1`＋可視** | 同じ | ✅ 一致 |
| **件数表示** | `7 件を表示` → `4 件を表示` → `4 件を表示（レベルを持つカードのみ）` | 同じ | ✅ 一致 |
| 共有URL(#20) | — | — | ⭐ **デッキ構築に存在しないので対象外**（設計 §9-3） |

⭐ **新しい帯と従来の帯でバイト一致**＝「既存の同種の条件で効いている副作用が、新しい帯では効かない」型は無い。

⚠️ **参考（合否に含めない）**: 別カード `aella-zephyrs-hand` で同じ手順を踏むと、レベル順に変えた瞬間に
`該当するカードがありません` になる。⭐ **これは #39 の仕様どおり**（`data/card-meta-index.json` の
`m["aella-zephyrs-hand"][6]` は `[null, 2, 3, null]` ＝ **level を持たない**ので数値ソートから除かれる）。
⭐ **変更前（3101）の1280pxでも同じ結果**なので、G-2 とは無関係。

---

## 4. ⭐ レビューが追加した検証③ — 破壊試験（M5）

⚠️ **「1200 で効いた」だけでは、閾値が2つとも動いたのか、片方が偶然効いていたのか区別できない。**
そこで **1240px で CSSOM から `@media (min-width: 1200px)` を `1280px` に戻し**（JS の `PANE_MIN_WIDTH` は 1200 のまま）、
設計 §14-3 が警告した状態を**その場で作れるか**を試した。

| 状態 | `#view-editor` | `#s-selected-filters` の実効 `display` | アコーディオン |
|---|---|---|---|
| ⭐ **出荷されるコード（1240px）** | `grid` | **`block`**（✕が押せる） | **活性** |
| ⚠️ **CSS 閾値だけ 1280 に戻したとき** | `block` | **`none`**（クラスは `has-selection` のまま＝**JSは動いているのに見えない**） | **活性のまま** |

⭐ **「左ペインが無いのにアコーディオンだけ効き、選んだ条件がどこにも見えない」帯が再現した。**
⇒ **V51 / V53 の検査は空回りしていない**（＝失敗する形をちゃんと検出できる測り方になっている）。
⭐ 同時に **CSSOM 上で `min-width:1200px` のメディアルールが 1本しか無い**ことも数えており、
**CSS 側が閾値を1箇所しか持たない**（＝`#s-selected-filters` が左ペイン一式と同じブロックの中にある）ことの裏取りになっている。

---

## 5. 静的な確認（コミットの中身）

| # | 見たこと | 結果 |
|---|---|---|
| 1 | 変更ファイル | `app.js` / `shared/js/card-search.js` / `tools/deck-builder/{app.js,index.html,style.css}` ＝ **設計 §14-3 のチェックリストと一致**。`git diff --name-only 543ab9d9..HEAD` の `docs/` 以外も同じ5本（**スコープ外なし**） |
| 2 | ⭐ **コメントを機械的に除去した後の実差分** | **2行だけ**（`@media` の条件文／`PANE_MIN_WIDTH`）。`index.html` はコメント除去後に**完全一致**、`app.js`（トップ）と `shared/js/card-search.js` は**非コメント行の差分 0**。⭐ **CSS閾値と JS閾値が同一コミットで同時に動いていることが、差分の形そのもので示されている** |
| 3 | `grep -rn "1280\|1279"`（vendor / docs / 生成物を除く） | 残るのは `tools/deck-builder/style.css` の**履歴注記コメント1行だけ**（「§4-4 の 1280px は 1.5 のユーザー判断で 1200px に上書きされた」）。⭐ **閾値ではないことが文面から分かる**ので、次に grep する人が実装漏れと誤読しない |
| 4 | `#s-selected-filters` の表示ルールの位置 | `#s-selected-filters.has-selection { display: block }` は `style.css` の **`@media (min-width: 1200px)` ブロックの内側**にある（同ブロックは左ペイン一式と `#result-modal { left: 320px }` を含む）。**既定の `display:none` はブロックの外**。⭐ **CSS側に閾値は1つしか無い** |
| 5 | `npm run validate` | **exit 0**（`all checks passed.`） |

---

## 6. 推奨基準の指摘（⚠️ **合格を妨げない**）

| ID | 指摘 | 対応要否 |
|---|---|---|
| **S-1** | ⚠️ ⭐ **CSS閾値と `PANE_MIN_WIDTH` のずれを止める自動検査が無い。** いまは両方のコメントに「同時に動かす」と書いてあるだけで、**片方だけ動かしても `npm run validate` は緑のまま**。⭐ **§4 の破壊試験でそのときの症状（E が無言で効かなくなる帯）を実際に再現できているので、リスクは机上のものではない**。⭐ 検査の形は素直で、**`tools/deck-builder/style.css` の `@media (min-width: N px)` と `app.js` の `const PANE_MIN_WIDTH = N` の N が一致するか**を1本見るだけで足りる（同種の検査は `validate` に既に多数ある） | ⭐ **設計担当の判断待ち**。開発担当の確認事項2と同じ内容で、**`E-add-1` に相乗りできる**という見立ても妥当だと考える。⚠️ **合否には含めない**——設計 §14-3 自体がコメントでの担保しか求めていないため |
| **S-2** | 設計 **§14-5 の影響範囲の表**が「1200〜1279px のデッキ構築（現状）＝ 8列」と書いているが、レビューの実測では **1200px=8列 / 1240px=8列 / 1279px=**⭐ **9列**（ペインが無い状態の列数は幅に比例して増えるため）。⭐ **G 後の値（6列）は表のとおり**で、**承認の前提（§14-2）は崩れていない** | ⭐ **設計担当の判断待ち**（表の精度の話）。開発担当の確認事項1と同じ観測 |

⭐ **S1（コード品質）・S2（応答性）・S3（fail-open）の観点では指摘なし**:
style属性の追加なし（CSPの `style-src 'self'` に抵触しない）／バッジの追加なし（`.badges-bl` の縦積みに関係しない）／
公式APIへの逐次リクエストを増やしていない（✕1回につき1検索のまま＝設計 §11-7）／
索引・生成JSONの読み方を変えていないので fail-open の性質も不変。
⭐ **コメントの書き換え4件（実装報告 §6）はいずれも「数字を置換すると文意が誤りになる」箇所の是正**で、
`review.md` の S1（周囲の慣習・行番号参照を書かない）にも適合している（`validate` の行番号参照検査も緑）。

---

## 7. ⭐ 未検証として残るもの（⚠️ 合否に含めていない）

| # | 項目 | 状況 |
|---|---|---|
| 1 | **実ログインでの確認** | できていない（Discord OAuth）。srcdoc ハーネスで差し替えたのは `/api/me` と `/api/decks/<id>` だけで、カード・絵柄・チップ157種・`renderZones()`・`.editor-bar` の高さは本物。⭐ **G-2 は閾値しか変えていない**ので、デッキの中身に依存する表示（適合判定バッジ・所持枚数・omnidex提出用コピー・デッキ画像出力）は**そもそも今回の変更の影響を受けない**が、**実ログインでは未検証**である（設計 §9-4） |
| 2 | **クラシックなスクロールバー環境**（15〜17px） | 未検証（ヘッドレスはオーバーレイ）。設計 §14-2 の注記どおり `.stats-pane` は `max-width` なので**縮むだけであふれない**見込み。⭐ **ユーザーの実物確認（3000番・§12-9 の3）で見てもらうのが確実** |
| 3 | **V43・V45〜V50・V32〜V40・V54** | ⭐ **G-3a / G-3b / F / E-add-1 の担当なので今回の範囲外**。⚠️ **したがって 641〜1199px の「上部パネル」は今回まだ残っているのが正**（実測でも 900 / 1024 / 1199px は before と完全一致）。**V43 は G-3a / G-3b が入るまで成立しない** |
| 4 | **push が要る確認** | ⭐ **無し**。`_headers` / `404.html` / `functions/` / キャッシュに触れていないので、本番でしか確かめられない項目は今回発生していない |

---

## 8. 設計担当への確認事項

⭐ **合否を左右するものは無い**（＝`設計確認待ち` へは合格として送る）。次の2件は **S-1 / S-2 と同じ内容**で、
**開発担当が §7 に挙げた確認事項をレビューとしても妥当と判断した**もの。

| # | 内容 | レビューの見立て |
|---|---|---|
| 1 | **閾値のずれを止める自動検査を足すか**（S-1） | ⭐ **足す価値はある**と考える（破壊試験で症状を再現できた＝実在するリスク）。⚠️ **ただし本タスクの設計 §14-3 はコメントでの担保しか求めていない**ので、**G-2 の不合格理由にはしていない**。`E-add-1` への相乗りか、独立の小単位かは設計担当の判断 |
| 2 | **§14-5 の表の「1200〜1279px 現状＝8列」の精度**（S-2） | ⭐ **直すかどうかは設計担当の判断**。⚠️ **承認の根拠（§14-2）は G 後の値で書かれており、そちらは実測で再現できている**ので、承認をやり直す必要は無いと考える |

---

## 9. ⭐ 次の単位（G-3a）への申し送り

| # | 内容 |
|---|---|
| 1 | ⚠️ ⭐ **G-3a は最も壊しやすい単位。** 本レビューが取った **「1幅あたり80項目 × 10幅を before/after で全数比較」** の形をそのまま使えば、§14-3 の「帯ごと広げてはいけない」（寸法まわりが 641〜1199px へ漏れる）を**数値で検出できる**。⭐ 測定スクリプトは `tmp/review/G-2-境界1200/measure2.mjs`（`.artgrid` の列数・入力欄の `font-size`・`.controls` の padding・モーダルの角丸/最大幅を既に含んでいる） |
| 2 | ⭐ **トップ側の測定は `tmp/review/G-2-境界1200/top.mjs`**（FAB のクラス・シートの `position`・`html` の `overflow-x`・`.grid` の列数・リセットの可視状態を測る）。⚠️ **G-2 の時点では 6幅すべて差分0・`MOBILE_MQ` は 640px のまま**なので、**G-3a の差分はこの基準線からの純増として読める** |
| 3 | ⚠️ ⭐ **`MOBILE_MQ` を 1199 にしたら、CSS だけでなく §14-3 の表の5つの使用箇所すべてを実駆動で確かめること**（とくに `updateFabVisibility()`——**変え忘れると CSS が直っていても FAB が永久に出ない**）。⭐ **900px と 1199px の2点**で見るのが設計 §14-6 V45 の要求 |
| 4 | ⭐ **srcdoc ハーネスの親は CSPメタを持たないページにする**（`tmp/review/G-2-境界1200/harness.html` がそのまま使える）。⚠️ `/404.html` を親にすると `base-uri 'none'` で `<base>` が無言で無視される |
| 5 | ⚠️ **before/after 並走は展開先の `serve.mjs` を起動**し、⭐ **「配信物の文字列」と「ブラウザ上の挙動の境界」の両方で取り違えを検算してから本測定に入る** |
| 6 | ⚠️ **3000番はユーザーの実物確認用。止めない。** 本レビューは 3100 / 3101 を使い、**待受ポートから PID を引いて個別に停止**した |
