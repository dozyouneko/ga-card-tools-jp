**対象: 「デッキ構築の左ペイン化」** — この宣言は 2026-09-11 に遡って追記した（索引の転記ドリフト防止）

# レビュー判定 — G-3b：デッキ構築を 1199px 以下でスマホ表示に統一（2026-09-07）

⚠️ **GitHubアカウント停止中のため `gh issue comment` が使えない。** 本ファイルが判定コメントの代替。

| | |
|---|---|
| 設計書 | [`左ペイン化_設計.md`](左ペイン化_設計.md) **版 1.9** の **§14-3（分割表・帯ごと広げるなの警告）/ §14-4（デッキ構築のスマホ表示の定義）/ §14-6（G-3b の必須条件4点・V50・V43/V52/V53 相当・M1-b）** |
| 対象コミット | **`5312e104`**（`tools/deck-builder/style.css` の**1ファイルのみ**・`+18 / -6`） |
| 実装報告 | [`実装報告_G-3b_デッキ構築のスマホ表示統一_2026-09-07.md`](実装報告_G-3b_%E3%83%87%E3%83%83%E3%82%AD%E6%A7%8B%E7%AF%89%E3%81%AE%E3%82%B9%E3%83%9E%E3%83%9B%E8%A1%A8%E7%A4%BA%E7%B5%B1%E4%B8%80_2026-09-07.md)（`1590a722`） |
| 前の周 | **A〜E**（承認済み）・**G-2**（承認 `213ce62e`）・**G-3a**（承認 `c8f2afa2`）＝**再判定していない** |
| 証跡 | `tmp/review/G-3b-デッキ構築のスマホ表示統一/`（gitignore・測定JSON10本＋自作スクリプト7本＋スクリーンショット6枚） |
| 検証環境 | **3100**（after＝作業ツリー＝`5312e104`）と **3101**（before＝`git archive 5312e104^` を `tmp/review/.../before/` へ展開し、**そこの `serve.mjs`** を起動）の2本並走 ＋ Playwright / chromium（`--ignore-certificate-errors` / `ignoreHTTPSErrors: true`）。⚠️ **3000番（ユーザーの実物確認用）には一切触れていない**（測定後に 3100 / 3101 のみ待受ポートから PID を引いて個別停止） |
| 取り違えの検算 | ⭐ **本測定の前に既知の差で検算した**: `/tools/deck-builder/style.css` の `max-width: 1199px` が after **1件** / before **0件**、`sha256` も相異（after `8a4e75d9…` / before `13fb92a2…`）。**2本が別のツリーを配信していることを確認してから測っている**（`00-検算.txt`） |

---

## 判定: **合格**

⭐ **報告の申告を根拠にせず、V50 の7小項目・V43/V52/V53 相当・M1-b をすべてレビュー担当自身で再実施した。**
⭐ **閾値スイープは自分で組み直し、報告の18幅より細かい23幅**（境界の直前直後 **639/640/641・859/860/861・1198/1199/1200/1201** を含む）で測った。
⭐ **破壊試験は報告と同じ1本を独立にやり直したうえ、報告に無い2本目を足した**
——**「帯ごと広げる」を CSSOM で実際に再現し、設計 §14-3 が警告した `1199×900・角丸0・2列・16px` が本当に起きること**を示した
（＝**分割が load-bearing であることの証明**。⚠️ **「起きなかった」だけでは、分割が効いているのか偶然かを区別できない**）。
⭐ さらに **報告に無い検証を4本足した**（①**3ビューを実際に開いた状態での before/after 比較**＝dev の確認事項②への回答
②**resize ではなく各幅で新規ロードした検算** ③**寸法系の測定でモーダルを実際に開いて実寸を採る**
④**主要5画面 × 2幅のスモーク**）。**すべて期待どおり**。

| 基準 | 結果 | 証跡 |
|---|---|---|
| **M1** 設計適合 | ✅ | **V50-a〜g の全小項目**＋**V43 / V52 / V53 相当**を独立に再実施し、§14-6 の期待値と一致（→ §1）。⭐ 期待値は**画面に出る最終形**で照合した（`display` の実値・矩形の実数値・`aria-expanded`・トグルのラベル文字列 `絞り込み ▾/▲`・バッジの文字列 `"2"`・`grid-template-columns` の実数値・モーダルの実寸）。⚠️ **G-3b の必須条件4点**（帯ごと広げない／閾値の非重複を人が測る／JS 無変更／純増として測る）も**4つとも満たしている** |
| **M1-b** 既存条件との対称性 | ✅ **適用した・欠落なし** | ⭐ G-3b は**絞り込み条件を増やさず「出し方」だけを変える**単位なので、設計が M1-b として指定した**「帯をまたいだときに選択が消えないこと」**で判定した。**1199 → 1200 → 1199 の往復**で**チップ（`NORM`/`ANOMALY`）・セレクト・バッジ件数がすべて不変**（`m1b.json`・→ §3）。⭐ 併せて **1200px で「選択中の条件」の ✕ を実クリックして実際に効くこと**（`NORM` が消え、バッジが `2`→`1`、残りが `ANOMALY` のみ）＝**E（承認済み）が新しい帯の隣で無言死していない**ことまで確認した。⚠️ **861〜1199px で「選択中の条件」が出ないのは M ではない**——**従来からスマホ表示の ≤860px でも同じ**（既存の同種の条件でも効いていない＝新規要望）。⭐ ただし**利用者から見た変化なので実物確認の観点に挙げた**（→ §6 参考1） |
| **M2** 実駆動 | ✅ | Playwright で **375 / 480 / 620 / 639 / 640 / 641 / 700 / 768 / 859 / 860 / 861 / 900 / 1000 / 1024 / 1100 / 1198 / 1199 / 1200 / 1201 / 1240 / 1279 / 1280 / 1440 px の23幅**を自分で操作。**PC幅（1200/1280/1440）と 375px 幅の両方**あり。スクリーンショット **6枚**（`shots/`・375/900/1199/1200 の closed と 900/1199 の open）。⭐ **トグルは `display` を根拠にせず、`elementFromPoint` がトグル自身を返すこと・Playwright の実クリックが通ることの2点**で判定（バッジも同様に `elementFromPoint` で確認）。⚠️ **before では実クリックが `NOT_CLICKABLE`（3秒タイムアウト）**＝純増であることをクリックの成否でも裏取りした |
| **M3** 回帰 | ✅ | `npm run validate` **exit 0**（`all checks passed.`／`no line-number refs in code comments — 0件（46ファイル走査）`・`shared css tokens in sync — 68 トークン参照 / 未定義 0`・`:root owners in sync — 4ファイル` を含む。**終了コードを単独実行で確認**）。**トップ・カード個別ページ・セットページ・大会一覧・デッキ構築**を **1280px と 375px** で開いて全数確認＝**10通りすべて status 200 / 主要要素あり / 横あふれ 0 / `pageerror` 0 / 自オリジンの取得失敗 0**（`smoke.json`）。カード個別ページは中身まで確認（`h1`＝`ダンジョンの案内人Dungeon Guide`・`.cp-effect` あり） |
| **M4** 生成物の同期 | ✅ | 生成ロジックは**無変更**（`scripts/` に差分なし）・訳の追加なし＝`gen-tl-json.mjs` / `build:cards` 不要。⭐ **`tools/deck-builder/style.css` が生成物へ埋め込まれる経路が無いことを実測**（生成スクリプトからの参照 **0件**・`cards/` `sets/` `tournaments/` からの参照 **0件**）。変更は1ファイルのみ＝**翌朝のcronが拾う差分は発生しない** |
| **M5** 静かな劣化がない | ✅ | ⭐ **破壊試験2本**（→ §2）。**①新設ブロックの削除**で 900px のトグルが `block→none`・`.filter-row` が `none→grid`（144.19px）に戻る＝**5本のルールが load-bearing**。**②「帯ごと広げる」の再現**で `1199×900 / 角丸 0px / max-width 100% / .artgrid 2列 / 入力欄 16px` が実際に発生＝**設計の警告は実在し、現在の分割はそれを能動的に避けている**。⚠️ **G-3b は新しい検査（validate 等）を足していない**ので、破壊試験の対象は他に無い。⭐ **CSS の 1199 と `PANE_MIN_WIDTH` の 1200 を結び付けているのは依然コメントだけ**だが、**設計はその受け皿として `E-add-1` を次の単位に切り出している**ため G-3b の不合格理由にはしていない（→ **S-2**・§5） |
| **M6** 公開安全性 | ✅ | **配信ファイルの新規・削除 0**（`git show --name-status` は `M` 1件のみ＝既に公開済みのファイルの変更）。`_headers` / `404.html` / `functions/` / `wrangler.toml` **無変更**＝Pages Functions・キャッシュ・CSPの前提に触れていない（⭐ **そのため 8888 の wrangler 実行は不要と判断した**）。`_headers` に `deck-builder` 専用規則は無い。差分に秘匿情報なし（`token`/`secret`/`key`/`password` の出現 0）・外部オリジンの追加なし・**style属性の追加なし**（CSP `style-src 'self'` に抵触しない） |
| **M7** スコープ | ✅ | 変更は **`tools/deck-builder/style.css` の1ファイルのみ**（`git -c core.quotepath=false show --name-only`）。⭐ **移設先が §14-3 の分割表「デッキ構築 / 1199px以下へ移す」列の4項目とちょうど一致**し、**据え置き列（`.gtile .ctrl-strip` / 入力欄16px / `.artgrid` 2列 / `.modal-body` 全画面シート）は 860px 側に残っている**ことを、**寸法系10幅の before/after 全一致（差分0）で実測裏取り**した（→ §1 V50-g）。⚠️ **JS は1バイトも触っていない**（`PANE_MIN_WIDTH = 1200` は `app.js:594` のまま）＝設計 §14-6 の必須条件3のとおりで、**`E-add-1` の検査③の対象は増えていない**。⭐ `shared/` にも未着手 |

---

## 1. 設計書の検証項目との対応（⭐ すべてレビュー担当自身の実測値）

### V50-f — ⭐ 閾値の非重複（**23幅スイープ**・`sweep-after.json`）

⚠️ ⭐ **`E-add-1`（自動検査）はまだ無いので人が測る**、という設計の指示どおり自分でスイープした。
⭐ **報告の18幅に対し、境界の直前直後（639/640/641・859/860/861・1198/1199/1200/1201）を足した23幅**で測っている。

| 幅 | `#s-toggle` の `display` | 見えるか | `elementFromPoint` がトグル自身 | `#s-filter-row` | `#view-editor` の列 | 横あふれ |
|---|---|---|---|---|---|---|
| 375 / 480 / 620 / **639** / **640** / **641** / 700 / 768 / **859** / **860** / **861** / 900 / 1000 / 1024 / 1100 / **1198** / **1199** | **`block`** | **true** | **true**（`s-toggle`） | `none`（畳まれている） | `none`（1カラム） | **0** |
| **1200** / **1201** / 1240 / 1279 / 1280 / 1440 | **`none`** | false | — | `grid` | **`320px 880px`** 〜 `320px 1120px`（左ペイン） | **0** |

⭐ **どの幅でも「トグル」か「左ペイン」の**片方だけ**が成立する。重なりも空白も無い。**
⭐ **境界は 1199 ↔ 1200 のちょうど1px**で、`PANE_MIN_WIDTH = 1200`（JS・未変更）と**過不足なく接している**。
`pageerror` **0件**・**全23幅で横あふれ 0**。→ **✅ 合格**

⭐ **resize 限定の artifact でないことも別に確かめた**（`fresh.json`）。**8幅で「その幅のまま新規に読み込む」**方式でも
上表と**完全に同じ結果**（375/640/860/861/900/1199 は `block`＋hit=true、1200/1280 は `none`）。

### V50-a〜e — 折りたたみの機能（**実クリック**・`func-after.json` / `func-before.json`）

⚠️ **`display` だけを根拠にしていない。** Playwright の**実クリック**と `elementFromPoint` で見た。

| 小項目 | 期待値 | **900px の実測（after）** | **1199px の実測（after）** | 判定 |
|---|---|---|---|---|
| **a** トグルが出る | 表示され押せる | `display: block` / 矩形 `852×49.59` / `elementFromPoint`＝**`s-toggle` 自身** | 同左（`block` / hit=true） | ✅ |
| **b** `.filter-row` が畳まれる | 既定で非表示 | `display: none` / 高さ **0** | `none` / **0** | ✅ |
| **c** トグルで開閉できる | 押すと開き、再度押すと閉じる | 閉 `aria-expanded=false` / `絞り込み ▾` → **実クリック OK** → 開 `aria-expanded=true` / `絞り込み ▲` / `row=flex` / 高さ **409.84** → **実クリック OK** → **初期状態へ完全復帰** | 同左（開の高さ 409.84・閉で復帰） | ✅ |
| **d** 選択件数バッジが出る | バッジが出る | チップ2つ選択で `hidden=false` / テキスト **`"2"`** / 矩形 `24.02×16` / `elementFromPoint`＝**バッジ自身** | 同左 | ✅ |
| **e** ⭐ **純増として測る** | before は 900/1199 とも `display:none` | **before: `display: none` / `elementFromPoint` が拾わない / 実クリックが `NOT_CLICKABLE`（3秒タイムアウト）**・`.filter-row` は `grid`（144.19px） | **before: `none` / `NOT_CLICKABLE`**・`.filter-row` は `grid`（103.97px） | ✅ |

⭐ **e は「CSS で消えている」だけでなく「実際にクリックが届かない」ことまで確認**したので、純増であることに疑いが無い。

### V50-g — ⭐ 帯ごと広げていないこと（**モーダルを実際に開いて実寸**・`dims-*.json`）

⚠️ **`hidden` のままでは矩形が 0 になるので、`#result-modal` / `#art-modal` を実際に開いて測った。**

| 幅 | `.artgrid` 列数 | 入力欄 `#s-name` | `.modal-body` 角丸 | `.modal-body` max-width | `.modal-body` 実寸 | `.gtile .ctrl-strip` opacity |
|---|---|---|---|---|---|---|
| 375 / 620 / 640 / 860 | 2 | 16px | 0px | 100% | 幅いっぱい×900 | **1** |
| **861 / 900 / 1024 / 1199 / 1200 / 1280** | **3** | **14px** | **16px** | **860px** | **860×106.88** | **0** |

⭐ **before / after の差分は全10幅・全項目で 0 件**（`dims` の全キー照合）。
⚠️ ⭐ **設計 §14-6 の必須条件1が警告した「1199px でモーダルが 1199×800・角丸0」は起きていない**
——**1199px でも `860×106.88 / 角丸 16px / max-width 860px`**。`.artgrid` も **3列**のまま、入力欄も **14px** のまま、
`.gtile .ctrl-strip` の常時表示（860px の項目）も **861px 以上へ漏れていない**。→ **✅ 合格**

### V43 相当 / V48 相当 — 中間の形が無いこと・既存境界が1pxも変わらないこと

**39セレクタ × 34プロパティ ＋ 矩形 を 19幅で before/after 採取し、全キー再帰比較した**（`regress-*.json`）。

| 幅 | 差分件数 | 意味 |
|---|---|---|
| **375 / 480 / 620 / 639 / 640 / 641 / 768 / 859 / 860** | **0** | ✅ **640px以下も、620/640/860 の既存境界も1pxも変わっていない** |
| 861 / 900 / 1024 / 1100 / 1198 / 1199 | 70 | ⭐ **新しい帯（意図どおり）** |
| **1200 / 1201 / 1280 / 1440** | **0** | ✅ **左ペイン帯に一切影響していない** |

⭐ **差分が出るのは 861〜1199px の帯だけ**で、**内訳も検分した**（900px）:
`#s-toggle`（`none→block` ほか）・`#s-filter-row`（`grid→none`）・`#s-set` / `#s-format`（全幅化）・
`#s-g-*` 5グループ（全幅化）＝**移した4項目そのもの**と、
**それに伴う `html` / `body` / `#view-editor` / `#search-top` の高さ**（`-94.59px`）だけ。
⭐ **下流の `.deck-pane` / `.deck-tabs` / `.gtile` / `.gtile .ctrl-strip` / `.row-actions` / `.result-sort` / `.site-foot` は
`_rect` の y が一律 `-94.59px` ずれるだけで、幅・高さ・スタイルは1つも変わっていない**（＝畳んだ分だけ上に詰まった結果）。
⚠️ **中間の形（上部パネル＝`.filter-row` が `grid` で常時展開）はどの幅にも出ていない。**

> ⭐ **報告の記述の微修正（合否に影響なし）**: 報告は `.gtile .ctrl-strip` を「1件も差分が無い」と書いているが、
> **`opacity` は不変でも `_rect` の y は上記のとおり動く**。**実質（常時表示が漏れていないこと）は報告のとおり**で、
> 指摘ではなく**事実の精度の補足**として残す。

### V52 相当 — 他3ビュー（⭐ **`hidden` のままではなく実際に開いて測った**）

⚠️ ⭐ **dev の確認事項②「`hidden` のままの計算値比較しかできていない。やり直す必要があるか」への回答＝
レビュー担当が実施済み。追加のやり直しは不要。**

`#view-login` / `#view-decks` / `#view-deck` を**1つずつ `hidden` を外して実際に開き**（他ビューは隠す）、
**6幅（375 / 640 / 900 / 1199 / 1200 / 1280）× 3ビュー = 18通り**で before/after を比較（`views-*.json`）。
デッキ一覧は `openDeckList()` を実行して**スタブの実データで描画させた**状態で測っている。

| 確認 | 結果 |
|---|---|
| 実際に開いているか | ✅ `hidden=false` / **矩形が非0**（例: 900px で `[0,120.64,900,463.92]`）/ 子要素あり（login 1・decks 2・deck 6） |
| **before/after の差分** | ⭐ **18通りすべて 0 件**（`display`/`position`/`top`/`left`/`width`/`maxWidth`/`margin`/`padding`/`fontSize`/`gridTemplateColumns`/`flexDirection`/`gap`/`textAlign` ＋ 矩形 ＋ 直下6要素の矩形） |
| 横あふれ | ✅ 全18通りで **0** |

→ **✅ 合格**（⭐ **変更が `.search-top` 配下だけで3ビューはそれを含まない**、という報告の見立ては**実測でも裏付けられた**）

### V53 相当 — 「選択中の条件」と `PANE_MIN_WIDTH` の閾値が揃っていること

| 幅 | `#s-selected-filters` の実効 `display` | 矩形 | ✕ が押せるか |
|---|---|---|---|
| **1199** | **`none`** | `[0,0]` | — |
| **1200** | **`block`** | **`287×107.31`** | ✅ **実クリックで効いた**（`NORM` が消え、バッジ `2→1`、残り `ANOMALY`） |

⭐ **CSS 側の閾値（`#s-selected-filters.has-selection` が `@media (min-width: 1200px)` の中）と
`PANE_MIN_WIDTH = 1200` が揃っている**ことを実測で確認。⚠️ **「左ペインは出るのに選択中の条件だけ出ない帯」は無い。**

> ⭐ **スイープ表で 1200px の `#s-selected-filters` が `none` に見えるのは正常**——
> **`.has-selection` クラス（＝条件を1つ以上選んでいる）が付いて初めて表示される**設計で、
> スイープは無選択状態だったため。**選択した状態で測ると `block` になる**（上表）。

---

## 2. ⭐ 破壊試験（レビュー担当が独立に実施・`break.json`）

⚠️ **「動いたから合格」では、対策が効いているのか偶然動いているのか区別できない。**

### 破壊試験1 — 新設ブロックが load-bearing であること（報告と同じ主旨・独立に再実施）

900px で **CSSOM から `@media (max-width: 1199px)` を探して削除**した（`conditionText` の空白を除いて一致するものだけ）。

| 項目 | 削除前 | **削除後** |
|---|---|---|
| 削除できたメディアルール | — | **`(max-width: 1199px)` / 内側ルール 5本**（＝新設ブロックの中身と一致） |
| `#s-toggle` の `display` | `block` | ⚠️ **`none`（トグルが消える）** |
| `#s-filter-row` の `display` | `none`（畳まれている） | ⚠️ **`grid`（元の上部パネルに戻る・高さ 144.19px）** |

### 破壊試験2 — ⭐ **報告に無い。設計の警告が実在することの証明**

⚠️ ⭐ **設計 §14-3 は「帯ごと広げると 1199px でモーダルが 1199×800・角丸0 になる」と警告している。**
**「起きなかった」だけでは、分割が効いているのか、そもそも起きない話なのかを区別できない。**
そこで **`@media (max-width: 860px)` の `mediaText` を実行時に `(max-width: 1199px)` へ書き換え**、
**禁じられた「帯ごと広げる」を実際に再現**した。

| 幅 | 項目 | **出荷された実装（as shipped）** | ⚠️ **帯ごと広げた場合** |
|---|---|---|---|
| **1199** | `.modal-body` 角丸 | **16px** | ⚠️ **0px** |
| | `.modal-body` max-width | **860px** | ⚠️ **100%** |
| | `.modal-body` 実寸 | **860×106.88** | ⚠️ **1199×900**（＝全画面シート） |
| | `.artgrid` 列数 | **3** | ⚠️ **2** |
| | 入力欄 `#s-name` | **14px** | ⚠️ **16px** |
| **900** | 同上 | **860×106.88 / 16px / 3列 / 14px** | ⚠️ **900×900 / 0px / 2列 / 16px** |

⭐ **設計の警告は実在し、現在の分割はそれを能動的に避けている。**
（⚠️ 設計は `1199×800` と記録しているが、これは測定時のビューポート高の違いで、**高さが画面いっぱいになる**という実質は一致する。）

---

## 3. M1-b — 帯をまたいだときに選択が消えないこと（`m1b.json`）

1199px でトグルを**実クリックで開き**、チップ2つ（`NORM` / `ANOMALY`）＋エキスパンションのセレクトを選んで往復させた。

| 状態 | 選択チップ | セレクト | バッジ | トグル | `#view-editor` | 選択中の条件 |
|---|---|---|---|---|---|---|
| 1199px（選択直後） | `["NORM","ANOMALY"]` | `"0"` | `"2"`（表示） | `block` | 1カラム | `none` |
| **1200px（左ペインへ）** | **`["NORM","ANOMALY"]`** | **`"0"`** | **`"2"`** | `none` | `320px 880px` | **`block`・`287×107.31`・✕押せる** |
| 1200px（**✕を実クリック**） | `["ANOMALY"]` | `"0"` | **`"1"`** | `none` | `320px 880px` | `block`・`287×73.63` |
| **1199px（戻り）** | **`["ANOMALY"]`** | **`"0"`** | **`"1"`** | `block` | 1カラム | `none` |

✅ **チップの選択・セレクト・バッジの件数が往復で完全に保たれる。**
✅ ⭐ **1200px で E（✕・すべて解除）が実際に効く**——**新しい帯の隣で E が無言死していない。**
`pageerror` **0件**・横あふれ **全状態で 0**。

⚠️ **M1-b の4観点のうち「絵柄の追従」「共有URL(#20)」は本単位の対象外**と判断した。
⭐ **根拠: G-3b は絞り込み条件を1つも増やしていない**（＝新しい版レベルの条件が無い）ため、
**「新しく足した条件で副作用が効かない」という M1-b の型に当てはまるものが存在しない**。
本単位で対称性を問うべきなのは**帯をまたいだときの状態保持**であり、設計 §14-6 の M1-b もそう指定している。

---

## 4. 実行できなかった検証・残っている確認

| # | 内容 | 状態 |
|---|---|---|
| 1 | ⚠️ **実ログインでのデッキ構築** | srcdoc ハーネス（`/api/me` と `/api/decks/<id>` だけスタブ）で代替。**カード・絞り込みチップ・`renderZones()` は本物**。⚠️ **所持枚数バッジ・適合判定など実アカウントのデータに依存する表示は未検証**（本単位は `.search-top` 配下の `display` しか触らず、`regress` でも該当要素に差分が無いため影響は考えにくいが**断定はしない**） |
| 2 | ⚠️ **カード画像（api.gatcg.com）** | ヘッドレスでは外部画像が読めずタイルが空枠で写る。⭐ **本単位はレイアウトのみで画像の有無に依存しない**ため判定に影響しない |
| 3 | ⚠️ **実機（iPad 等）での確認** | 未実施。⭐ **本単位は入力欄の font-size を一切動かしていない**（1199px でも 14px のまま＝§14-3 補足4 の方針どおり・実測で確認）ので、**G-3b の前後でズーム挙動は変わらない** |
| 4 | ⚠️ **本番での確認** | **未push**のため未実施。⚠️ **push は要求しない**（本番公開はユーザー判断）。⭐ 本単位に **push しないと確認できない項目は無い**（`_headers` / Functions / キャッシュに無関係）ため、**合否はそれ以外で判定した** |

---

## 5. 推奨基準の指摘（合格を妨げないもの）

### S-1（軽微・**S1 コード品質**）新設ブロック内のコメントの「上／下」が逆

- **該当**: `tools/deck-builder/style.css` の新設ブロック内、`.search-top .filter-toggle` の直前のコメント
- **内容**: 「`font-size: 16px` はトグル自身の見た目（**上**の寸法系の16pxとは別ルール）」とあるが、
  その寸法系（`.search-top input, .search-top select, .search-top .btn { font-size: 16px }`）は
  **同じコミットで新設ブロックの「下」へ移動している**（860px ブロックの中）。
  ⭐ **同じブロックの見出しコメントは「**下**の 860px / 620px / 640px に残す」と正しく書けている**ので、**内部で食い違っている**
- **なぜ拾ったか**: ⭐ **CLAUDE.md が繰り返し警告する「ずれるのはいつもドキュメント側」の型**で、
  次にこの帯を触る人が**上を探して見つからない**。1語の修正で済む
- **対応要否は設計担当の判断待ち**（⚠️ レビューは直さない）

### S-2（軽微・**S3 fail-open / 検出経路**）閾値の非重複を守っているのは今もコメントだけ

- **内容**: `style.css` の `1199` と `app.js` の `PANE_MIN_WIDTH = 1200` を結び付けているのは**コメント2箇所だけ**で、
  **片方だけ動かしても `npm run validate` は緑のまま**（＝§7-4 が警告した「無言の劣化帯」を止める自動検査が無い）
- **不合格にしない理由**: ⭐ **設計がその受け皿を `E-add-1`（次の単位）として明示的に切り出しており**、
  §14-6 は **「G-3b の時点ではまだ自動検査が無い＝人が測る」と明記**している。
  **今回はレビュー担当が23幅スイープで実測して重なりも空白も無いことを確認した**ので、**現時点の劣化は無い**
- ⭐ **`E-add-1` への申し送り**: **本判定の23幅スイープ（`sweep.mjs`）がそのまま検査②の受け入れテストになる**。
  ⚠️ **検査を足したら「わざと片方だけずらして exit 1 になること」を必ず確認すること**（M5）

### 参考（基準に紐づかない・合否に含めない）

1. ⭐ **861〜1199px の利用者は、絞り込み中の条件が「バッジの数字」でしか見えなくなる**。
   従来この帯は条件が常時展開されていた。⚠️ **これは劣化ではなく §14-4 で承認された「スマホ表示」の定義そのもの**で、
   **≤860px は以前からこの挙動**（＝M1-b の「既存の同種の条件でも効いていない」に該当し M ではない）。
   ⭐ **ただし利用者から見た変化ではあるので、§12-9 の実物確認で 900px 前後を触ってもらう観点に挙げておく**

---

## 6. 設計担当への確認事項（⚠️ **合否には影響しない**）

| # | 内容 | レビューの見解 |
|---|---|---|
| 1 | dev 確認事項①「640px 境界でトグルだけ font-size が変わる現象はデッキ構築側に存在しない。§12-9 の項目10 はトップだけの話としてよいか」 | ⭐ **実測で dev の主張を追認した**——`#s-toggle` の `font-size` は **375〜1280px の全10幅・before/after とも 16px で不変**（`dims-*.json`）。`.filter-toggle` は `.btn` を持たないため `.search-top .btn { font-size: 16px }` と競合しない。⭐ **「トップだけの話」として扱ってよい**というのがレビューの見立てだが、**§12-9 の項目をどう畳むかは設計判断** |
| 2 | dev 確認事項②「3ビューは `hidden` のままの比較しかできていない。V52 をやり直す必要があるか」 | ✅ ⭐ **レビュー担当が実施済み＝やり直し不要**。3ビューを**実際に開いた状態**で 6幅 × 3ビュー = **18通り、差分0**（→ §1 V52 相当）。**未検証のまま合格にしていない** |
| 3 | dev 確認事項③「次は `E-add-1` の理解でよいか」 | ⭐ **設計判断**。レビューからは **S-2 の申し送り**（23幅スイープを受け入れテストに流用・**わざとずらして落ちることの確認を忘れない**）を添える |

---

## 7. 証跡一覧（`tmp/review/G-3b-デッキ構築のスマホ表示統一/`・gitignore）

| ファイル | 内容 |
|---|---|
| `00-検算.txt` | ⭐ **取り違えの検算**（after=1 / before=0・sha256 相異） |
| `01-進捗メモ.txt` | 区切りごとの進捗（セッション中断対策） |
| `lib.mjs` | ハーネス共通（srcdoc・編集画面到達・iframe 実行） |
| `sweep.mjs` / `sweep-after.json` | **23幅の閾値スイープ**（V50-f・V43 相当） |
| `func.mjs` / `func-after.json` / `func-before.json` | **実クリックの開閉・バッジ**（V50-a〜e） |
| `dims.mjs` / `dims-after.json` / `dims-before.json` | **寸法系10幅・モーダルを開いて実寸**（V50-g） |
| `regress.mjs` / `regress-after.json` / `regress-before.json` / `regress-diff.json` | **39セレクタ×34プロパティ×19幅の全数比較**（V48 相当） |
| `break.mjs` / `break.json` | **破壊試験2本**（新ブロック削除・帯ごと広げるの再現） |
| `m1b.mjs` / `m1b.json` | **M1-b の往復＋V53 の ✕ 実クリック** |
| `views.mjs` / `views-after.json` / `views-before.json` | **3ビューを実際に開いた比較**（V52 相当） |
| `fresh.mjs` / `fresh.json` | **各幅で新規ロードした検算**（resize artifact でないこと） |
| `smoke.mjs` / `smoke.json` | **主要5画面 × 2幅のスモーク**（M3） |
| `shots/*.png` | 375 / 900 / 1199 / 1200 の closed、900 / 1199 の open（**6枚**） |
| `before/` | `git archive 5312e104^` の展開（3101 で配信） |

---

## 8. 次の単位への申し送り

- ⭐ **`E-add-1`（UI規約の自動検査2本＋検査③）が次**。**S-2 のとおり本判定の 23幅スイープが検査②の受け入れテストに使える**
- ⚠️ ⭐ **検査を足したら「わざと壊して exit 1 になること」を必ず確認する**（M5）。
  ⚠️ **検査の両辺が同源だと常に通る**——`style.css` の `1199` と `app.js` の `1200` は**別ファイルなので同源ではない**が、
  **どちらか片方だけを読んで自分自身と比べる作りにしない**こと
- ⭐ **ユーザーの実物確認（3000番）で見てもらう観点**として、§5 参考1（861〜1199px で条件がバッジの数字でしか見えない）と
  設計 §14-2 の「1279px で 9列 → 6列」を挙げる
