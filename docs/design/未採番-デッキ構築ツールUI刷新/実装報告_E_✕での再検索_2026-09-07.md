**対象: 「デッキ構築の左ペイン化」** — この宣言は 2026-09-11 に遡って追記した（索引の転記ドリフト防止）

# 実装報告 — E：「選択中の条件」の✕・すべて解除でも再検索する（2026-09-07）

⚠️ **GitHubアカウント停止中のため `gh issue comment` が使えない。** 本ファイルが
`実装 → レビュー／設計` の報告経路（issueコメントの代替）。

| | |
|---|---|
| 設計書 | [`左ペイン化_設計.md`](左ペイン化_設計.md) **版 1.6** の **§11 全体（11-1〜11-8）** |
| 実装単位 | ⭐ **§8-1 の表の「単位1 ＝ E」だけ**。⚠️ **G / F には手を付けていない**（別コミットで実施予定） |
| 検証項目 | **V24〜V31**（§11-8） |
| 前の周 | **A**（`b41d6411` / 承認 `7abc072d`）、**B・C**（`0adb564d` / 承認 `52bd2685`）、**D**（`8c87be9c` / 承認 `1856aa24`） |

---

## 1. 実施内容

### 1-1. コミット（⚠️ **push はしていない**）

| コミット | 範囲 | 変更ファイル |
|---|---|---|
| **`a8420674`** | **§11 全体（E）** | `shared/js/card-search.js` / `tools/deck-builder/app.js` |

- ⚠️ **push は未実施**（本番公開の可否はユーザー判断）。コミット直後の `git status --short` は
  **本報告ファイルを除いてクリーン**。
- 担当パス外には触っていない（⚠️ `docs/` 配下は設計担当が触ったばかりなので、本報告ファイル以外は一切変更していない）:

  ```
  $ git -c core.quotepath=false show --name-only --format= a8420674
  shared/js/card-search.js
  tools/deck-builder/app.js
  ```

- ⭐ **トップ（`index.html` / `app.js` / `style.css`）は1行も触っていない**（設計 §11-3）。

### 1-2. 何をしたか（差分は2ファイル・3か所）

| # | ファイル | 変更 |
|---|---|---|
| **①** | `shared/js/card-search.js` | `createSelectedFilters()` に **`onRemoveOne` オプションを追加**（省略時は完全な no-op）。`chipFor()` の click ハンドラを **`item.input.click(); onRemoveOne();`** に変更（＝群側の `onChange` が走った**後**に1回だけ呼ぶ）。冒頭の使い方コメントと `createSelectedFilters` の直前に **⚠️「トップには渡さないこと（✕1回で2回検索になる）」の注意書き**を残した（設計 §11-4 の指示） |
| **②** | `tools/deck-builder/app.js` | `createSelectedFilters()` の呼び出しを **`onChange: () => { updateFilterBadge(); rerunIfResultOpen(); }`**、**`onRemoveOne: () => rerunIfResultOpen()`** に変更 |
| **③** | `tools/deck-builder/app.js` | **`rerunIfResultOpen()`** を新設（`if (!el.resultModal.hidden) runSearch(true);`）。⚠️ 設計 §11-6 のコメント（閉じているときに呼ぶと「✕のたびに結果モーダルが開く」／**並び替えで外したガードを戻すものではない**）をそのまま残した |

- ⚠️ **却下された2案（`setValues()` 経由に寄せる／群側 `onChange` に再検索を足す）は採っていない**（設計 §11-5）。
- ⚠️ **チップ本体を押したときに検索しない既存挙動（Q5）は据え置き**。群側の `onChange`（`updateFilterBadge` のみ）は無変更。

### 1-3. 検証環境

- Playwright 1.61.1（`--ignore-certificate-errors` ＋ context `ignoreHTTPSErrors: true`）。`npm run setup:env` 実行済み。
- ⚠️ ⭐ **3000番はユーザーの実物確認用に動いていたため触っていない。** 検証用サーバーは
  **3010番（変更後＝作業ツリー）** と **3011番（変更前＝`git archive HEAD` を別ディレクトリに展開し、そこの `serve.mjs` を起動）** の2本。
  ⭐ **CLAUDE.md の「`serve.mjs` は `import.meta.url` から配信ルートを決める＝`cd` では切り替わらない」に従い、展開先の `serve.mjs` を起動した**（両サーバーの起動ログの `Serving <path>` が別パスであること・`grep -c onRemoveOne` が **0 / 6** であることで before/after の取り違えが無いことを確認）。
- デッキ構築ツールへの到達は ⭐ **srcdoc ハーネス方式**（[`モック説明_左ペイン化_2026-09-06.md`](モック説明_左ペイン化_2026-09-06.md) §2）。
  本物の `/tools/deck-builder/` を `fetch` し、`<base href="/tools/deck-builder/">` と
  `モック_左ペイン化_2026-09-05.stub.js` を差し込んで `iframe.srcdoc` に入れ、`/api/me` と `/api/decks/mock` だけを差し替えた。
  ⭐ **チップ157種・検索経路・結果タイル・絵柄バッジはすべて本物**。
- ⭐ **検索の回数は `searchCtl.run` を包んで数えた**（`window.__runs`）。API のリクエスト数も併記するが、
  ⚠️ **`api.gatcg.com` の生のリクエスト数は結果タイルの遅延読み込み画像を含むため単独の根拠にしない**
  （実測: チップ本体クリックで `runs=0` なのに画像だけで 37 リクエストが流れた）。**画像 URL（`/cards/images/`）を除いた数**を併記する。
- 検証スクリプトは `tmp/dev-E/`（gitignore）。⚠️ **3010 / 3011 のサーバーは検証後に待受ポートから PID を引いて個別に停止した**（`pkill` は使っていない）。
- 測定幅は **1400×950**（左ペインが出る幅。「選択中の条件」はそこにしか出ない）。

---

## 2. 検証結果（設計書 §11-8 の V24〜V31 に対応）

| # | 何を | 期待値 | 結果 | 実測 |
|---|---|---|---|---|
| **V24** | ⚠️ ⭐ **トップの回帰（リクエスト数）** | ✕1回で検索1回・すべて解除も1回。**変更前と同数** | ✅ **合格** | **変更前(3011) / 変更後(3010) が完全一致**。✕1回 → `searchCtl.run` **1回**（生リクエスト 34 も一致）／すべて解除（2件選択） → **1回**（生 27 も一致）。`pageerror` 0件 |
| **V25** | モーダルを開いたまま✕ | 結果がその場で更新・モーダルは開いたまま・件数が新条件と一致 | ✅ **合格** | エレメント2件（OR）で `全 1270 件` → ✕1回で **`全 336 件`**・`modalHidden=false`・`runs=1`・チップ 2→1 |
| **V26** | モーダルを閉じたまま✕ | モーダルは開かない・チップが消えバッジが1減る・**API検索は走らない** | ✅ **合格** | `modalHidden` は **true のまま**・チップ 2→1・バッジ `2`→`1`・**`runs=0`**・API 増分 **0** |
| **V27** | すべて解除（3件選択・モーダルを開いた状態） | **検索は1回だけ**・全群が空になる | ✅ **合格** | `runs=1`（1回だけ）・チップ 3→0・バッジ `hidden`・チェック済み 0・件数 `全 1655 件` → `全 2495 件` |
| **V28** | ⚠️ **チップ本体を押す**（Q5 の据え置き） | **検索が走らない**（バッジと「選択中の条件」だけ更新） | ✅ **合格** | **モーダルを閉じた状態**: `runs=0` / API増分 0。**モーダルを開いた状態でも** `runs=0` / **画像を除く API 増分 0**（件数表示も `全 2495 件` のまま不変）。バッジは `0`→`2`・チップ 0→2 |
| **V29** | ⭐ **絵柄の追従**（M1-b） | レアリティで絞った状態から✕で外すと、タイルの絵柄が既定の版に戻る | ✅ **合格** | `aella-zephyrs-hand`（名前 `aella` で1件に固定して比較）: CSR(7)選択時 `…/kT45RijQzg.jpg`・🎨バッジ `2・RDO 1st` → ✕で外すと `…/OX21mHMuGf.jpg`・🎨バッジ **`2・RDO`**（＝既定の版へ復帰）。⭐ 同一 slug で画像 URL と版 prefix の両方が変化 |
| **V30** | **バッジ集計**（M1-b） | ✕・すべて解除でバッジが正しく減る／0で `hidden` | ✅ **合格** | ✕: `2`→`1`（開・閉どちらでも）。すべて解除: `3`→**`hidden=true`**。⭐ 併せて `#s-selected-filters` の `.has-selection` が外れ `display:none` になることも確認 |
| **V31** | **件数表示**（M1-b） | ✕の後の「全N件」が、同じ条件で🔍検索したときと**一致** | ✅ **合格** | ✕直後 `24 件を表示 / 全 336 件` → そのまま🔍検索 `24 件を表示 / 全 336 件`（**文字列まで一致**）。⚠️ 取得ギャップの注記は出ていない |

### 2-1. 参考（V24〜V31 の外だが、E で壊しうるので見た）

| 何を | 結果 |
|---|---|
| **リセットボタン**（モーダルを開いた状態） | ⭐ **従来どおり再検索しない**（`runs=0`・API増分 0・件数表示は不変）。`resetSearchForm()` は群の `reset()` を呼ぶだけで `createSelectedFilters` の `onChange` を通らないため |
| `npm run validate` | ⭐ **exit 0**（`no line-number refs in code comments — 0件（46ファイル走査）` / `shared css tokens in sync` / `:root owners in sync — 4ファイル` を含む全項目 `all checks passed.`） |
| `node --check` | `shared/js/card-search.js` / `tools/deck-builder/app.js` ともに構文 OK |
| ブラウザのエラー | `pageerror` **0件**。`console.error` は Cloudflare RUM ビーコン（`cloudflareinsights.com` への CORS）だけで、**ローカル配信固有・本タスクと無関係** |

---

## 3. 実行できなかった検証と代替手段・残っている確認

| # | 項目 | 状況 |
|---|---|---|
| **R1** | ⚠️ **実ログインでの確認** | **未実施**。デッキ構築は Discord OAuth が前提で、`/api/me` と `/api/decks/mock` をスタブに差し替えた**合成状態**で確認した。⭐ ただし E が触るのは**検索パネル側の経路だけ**で、デッキの中身を読まない（`rerunIfResultOpen()` が見るのは `el.resultModal.hidden` のみ）ため、影響は小さいと考える |
| **R2** | ⚠️ **デッキの中身に依存する表示**（§9-4・R5） | 検索結果タイルの**所持枚数バッジ・適合判定**は合成デッキ（実データの大会デッキ）での描画までしか見ていない。**「通った」とは書かない** |
| **R3** | **1279px 以下（狭い幅）での挙動** | **未検証**（V24〜V31 に項目が無い）。⭐ そこでは `#s-selected-filters` が `display:none` で✕自体が押せないため、E の経路に入らないと考えられるが、**実測はしていない** |
| **R4** | **本番（push 後）の確認** | **未実施**（push していないため）。⚠️ push はユーザー判断 |
| **R5** | ⭐ **「モーダルが閉じていても開いてほしい」かどうか** | 設計 §11-6 のとおり **`if` 1行で切り替えられる形**にしてある。⭐ **ユーザーの実物確認（3000番）で見てもらう項目**（→ §12-9） |

---

## 4. 差分が設計書の想定と食い違った点

**無し。** 設計 §11-4 の API 形（`onChange` / `onRemoveOne` の2本）・§11-6 の `rerunIfResultOpen()`・
§11-2 の表（トップは据え置き）と、実測がすべて一致した。

⭐ **設計 §11-2 の「トップは既に✕で1回検索する」という読みも実測で裏が取れた**
（変更前 3011 の測定で `runs=1`）。⚠️ **もし `onRemoveOne` をトップにも渡していれば 2 になっていた**ので、
この実測は「渡していないこと」の証跡でもある。

---

## 5. 設計書からの逸脱と根拠

**逸脱なし。**

- ⚠️ **却下2案は実装していない**（§11-5）。
- ⚠️ **デバウンスは入れていない**（§11-7・✕1回につき1リクエスト）。
- ⚠️ **トップは1行も触っていない**（§11-3）。
- コメントに **`<file>.js:<行番号>` 形式は書いていない**（`validate` の該当検査も緑）。
- ⭐ 設計 §11-4 が明示的に求めた **「トップに渡すと2回検索する」旨のコメント**は、
  `shared/js/card-search.js` の `createSelectedFilters` の直前と `chipFor()` の click ハンドラ直上の**2か所**に残した。

---

## 6. 設計担当への確認事項

| # | 確認事項 | 補足 |
|---|---|---|
| **Q-E1** | ⭐ **「すべて解除」がモーダルを開いた状態のときは、条件が空の検索（＝全2495件）を1回投げる**。これは意図どおりか | 実測: `全 1655 件` → `全 2495 件`。設計 §11-4 の表は `onChange` を `updateFilterBadge(); rerunIfResultOpen()` に変えると書いてあるので**設計どおりに実装した**が、⚠️ **絞り込みなしの全件検索は重い**（トップの実測で 45 リクエスト・約19秒の経路に入りうる）。**モーダルが開いていても「すべて解除」だけは投げない**という選択肢もあるため、念のため確認したい（⭐ 変えるなら `onChange` から `rerunIfResultOpen()` を外す1行） |
| **Q-E2** | **1279px 以下での扱い**（R3） | 「選択中の条件」自体が出ないため E は無効化されるはずだが、**G-2 で境界が 1200px に動いた後も同じ**という理解でよいか（E の実装は幅を見ていない） |

⚠️ **上の2件はいずれも「実装せず報告して止める」対象と判断した**（仕様の判断が要るため）。
⭐ **現状のコードは設計書どおり**なので、回答が「そのままでよい」ならこのままレビューへ進める。

---

## 7. 次の単位

⭐ **設計 §8-1 の表で残るのは 2〜6（G-2 / G-3a / G-3b / F-a / F-b）**。本コミットは **E のみ**で、
**G・F には一切触れていない**（`tools/deck-builder/style.css` / `index.html` / トップの3ファイルは無変更）。
