**対象: 「デッキ構築の左ペイン化」** — この宣言は 2026-09-11 に遡って追記した（索引の転記ドリフト防止）

# 実装報告：E-add-1（UI規約の自動検査3本）— 2026-09-08

対象: 設計書 [`左ペイン化_設計.md`](左ペイン化_設計.md)（1.10）**§8-1 の単位5 = `E-add-1`**（**§11-9**）
検証項目: **V54 / V55 / V56**　⚠️ **F-a / F-b には手を付けていない**

---

## 1. 実施内容

| 項目 | 内容 |
|---|---|
| **コミット** | ⭐ **`bd94bee9`**（`未採番(デッキ構築の左ペイン化): E-add-1 — UI規約の自動検査3本を validate に追加`） |
| **push** | ⚠️ **していない**（`scripts/` も `style.css` も本番配信物。公開の可否はユーザー判断） |
| 変更ファイル | `scripts/validate.mjs`（+233行）／ `style.css`（+2行＝マーカー1組のみ） |
| 実行時の挙動 | ⭐ **変えていない**（CSSの追加分はコメント2行だけ・JSは無変更） |

### 実装した3本（設計 §11-9 の表のとおり）

| 検査 | 片側 | もう片側 | 落ち方 |
|---|---|---|---|
| **①** | `app.js` / `shared/js` / `tools` / `functions` の **コードの実態**（オプションとして `onRemoveOne` を渡しているか＝コメント除去後の `onRemoveOne` + `:` の一致） | `validate.mjs` の許可リスト **`REMOVEONE_ALLOW`** | ⭐ **双方向**。許可外が渡しても、許可リストのページが渡さなくなっても **exit 1** |
| **②** | ページCSSの **`@media` の prelude の `min-width` / `max-width`**（コメント除去後） | ページJSの **`PANE_MIN_WIDTH`** | `min-width` の集合が `PANE_MIN_WIDTH` ただ1つと一致しない／`max-width` に閾値以上がある で **exit 1** |
| **③** | `style.css` の **マーカー `MOBILE-BAND:START` の直後に現れる最初の `@media` の `max-width`** | `app.js` の **`matchMedia("(max-width: …)")`** | 値の不一致・マーカー0組/2組・直後が `@media` でない・迷子のマーカー で **exit 1** |

- ⭐ **同源にならない形にしてある**（①はコードと許可リスト、②③はCSSとJSで別ファイル・別言語）。
- ⭐ **fail-closed**: 走査対象0ファイル・`UI_PAGES` のファイルが読めない・`PANE_MIN_WIDTH` の定義が1つでない・
  `matchMedia(max-width)` の値が複数ある・マーカーがコメントの外にある — いずれも **exit 1**。
- ⭐ **エラーメッセージには規則そのもの**を書いた（「渡してよいのは群側の `onChange` が再検索しないページだけ」／
  「閾値は2か所にあり必ず同じ値」／「ずれても CSS 上は display が変わらないので直って見える」＋
  **「検査を消して通さないこと」**）。
- ③の走査対象は **`UI_PAGES`（トップ・デッキ構築）**。⭐ **デッキ構築を含めてある**ので、
  将来デッキ構築に JS 側の帯を足したら、覚えていなくてもマーカーが要求される（設計 §11-9 の要求どおり）。

### マーカー（`style.css`・1組だけ）

```css
/* MOBILE-BAND:START (max-width) — ⚠️ app.js の MOBILE_MQ と同じ値にすること。
   ⚠️ このマーカーは validate が読む。消さない・2組にしない・行を分けない。 */
@media (max-width: 1199px) {
```

設計 §11-9「マーカーの形」の規約1〜5をそのまま満たす（`@media` の直前の行・単独のCSSコメント・入れ子なし・1組だけ）。

---

## 2. 検証結果（設計書の検証項目に対応）

| 項目 | 内容 | 結果 |
|---|---|---|
| **V54** | 検査①が「トップに `onRemoveOne` を渡す」を止める（＋デッキ構築から消しても止める） | ✅ **合格**（破壊試験 D1-1・D1-2 とも exit 1／戻すと exit 0） |
| **V55** | 検査②が CSS と JS の閾値のずれを止める | ✅ **合格**（D2-1〜D2-4 の4通りすべて exit 1／戻すと exit 0） |
| **V56** | 検査③がマーカー直後の帯と `MOBILE_MQ` のずれを止める | ✅ **合格**（D3-1〜D3-7 の7通りすべて exit 1／戻すと exit 0） |

### ⭐ 破壊試験の一覧（本体）

ハーネス: `tmp/dev-E-add-1/destroy.py`（各ケースで **壊す → `node scripts/validate.mjs` → `git checkout --` で戻す**）／
生ログ: `tmp/dev-E-add-1/destroy-results.json`

| # | 項目 | 壊し方 | exit | 出たエラー（先頭） |
|---|---|---|---|---|
| D1-1 | V54 | トップ `app.js` の `createSelectedFilters({…})` にも `onRemoveOne` を渡す | **1** | 許可リストに無いのに onRemoveOne を渡しているページ: 1件 |
| D1-2 | V54 | `tools/deck-builder/app.js` から `onRemoveOne` の行を削除 | **1** | 許可リストにあるのに onRemoveOne を渡していないページ: 1件 |
| D2-1 | V55 | `style.css` の `@media (min-width: 1200px)` だけ **1280** に戻す | **1** | トップ: … min-width が [1280] で、app.js の PANE_MIN_WIDTH（1200）ただ1つと一致しません |
| D2-2 | V55 | `app.js` の `PANE_MIN_WIDTH` だけ **1280** にする | **1** | トップ: … min-width が [1200] で、… PANE_MIN_WIDTH（1280）… |
| D2-3 | V55 | デッキ構築CSSに **2つ目の `min-width`（900px）** を足す | **1** | デッキ構築: … min-width が [900, 1200] で … ただ1つと一致しません |
| D2-4 | V55 | デッキ構築CSSの `max-width: 860px` を **1250px**（＝閾値以上）にする | **1** | … max-width [1250] が PANE_MIN_WIDTH（1200）以上です — 帯が重なります |
| **D3-1** | V56 | ⭐ **マーカー側の帯だけ 640 に戻す**（⚠️ 素朴な検査なら通るケース） | **1** | トップ: マーカー直後の帯は max-width 640px ですが、app.js の matchMedia は 1199px です |
| D3-2 | V56 | `app.js` の `MOBILE_MQ` だけ 640 に戻す | **1** | トップ: マーカー直後の帯は max-width 1199px ですが、… matchMedia は 640px です |
| D3-3 | V56 | マーカーを **CSS側だけ**消す | **1** | マーカー MOBILE-BAND:START が 0組です — ちょうど1組でなければ… |
| D3-4 | V56 | マーカー文字列を **`validate.mjs` 側だけ**変える | **1** | マーカー MOBILE-BAND-2:START が 0組です — … |
| D3-5 | V56 | マーカーを **2組**にする | **1** | マーカー MOBILE-BAND:START が 2組です — … |
| D3-6 | V56 | マーカーと `@media` の**間に宣言を挟む** | **1** | マーカーの直後が @media ではありません — … 間に別の宣言やコメントを挟まない |
| D3-7 | V56 | マーカーを**別ページのCSS（デッキ構築）にも置く** | **1** | matchMedia(max-width) が無いのにマーカーが 1組あります ／ 要求されていないCSSにあります: 1件 |
| — | 復旧 | 全ケースとも `git checkout --` で復旧 | **0** | 成功行3本が出る（下記） |

⭐ **D3-1 が「素朴な形なら通る」ことの裏取り**: コメント除去後の `style.css` の `@media` は
`[max-width:1199, max-width:640, prefers-reduced-motion, max-width:1199, min-width:1200]` で、
**`max-width: 1199px` が2ブロックある**（②絞り込み導線 と ②`#results { display: contents }`）。
D3-1 で導線側を 640 にしても **集合には 1199 が残る**ため、「`MOBILE_MQ` の値が集合に含まれるか」だけの検査は
**通ってしまう**。マーカーで名指ししているので落ちる。

### ⭐ `npm run validate` の成功行3本（`tmp/dev-E-add-1/validate-after.log`）

```text
onRemoveOne wiring in sync — 渡しているのは 1ファイル（tools/deck-builder/app.js）／25ファイル走査
pane threshold in sync — 2ページ（トップ 1200px / デッキ構築 1200px）
mobile band marker in sync — 1組（style.css 1199px）
```

（`npm run validate` は **exit 0 / `all checks passed.`**。既存の成功行はすべて据え置きで、
`no line-number refs in code comments — 0件（46ファイル走査）` も緑のまま＝**コメントに行番号参照を書いていない**。）

---

## 3. 実行できなかった検証・残っている確認

| 項目 | 状況 |
|---|---|
| ブラウザ実駆動（Playwright） | ⭐ **不要と判断**。本単位は `validate.mjs` の検査追加＋CSSコメント2行のみで、**配信されるCSSの規則も画面も1つも変わらない**（`@media` の値・宣言は無変更）。⚠️ 念のため `git diff` で `style.css` の変更が**コメント2行だけ**であることを確認済み |
| G-3b の23幅スイープの再作成 | ⚠️ **実施していない**。`tmp/` は gitignore で残っておらず、かつ**幾何は1pxも変えていない**ため受け入れの対象が無い（②の受け入れは破壊試験 D2-1〜D2-4 で代替した） |
| cron | ⚠️ **cron は `validate` を実行しない**ので、この3本が守るのは**人が編集するときだけ**（既存の検査群と同じ制約・設計 §11-9 の前提どおり） |

---

## 4. 差分が設計の想定と食い違った点

**なし。** 変更は `scripts/validate.mjs`（検査3本）と `style.css`（マーカー1組）だけで、
コミット前に `git status --short` で他セッションのパス（`docs/` 等）の巻き込みが無いことを確認した。
⚠️ **`app.js` / `tools/deck-builder/app.js` は破壊試験で一時的に書き換えたが、全ケース `git checkout --` で復旧済み**
（最終の `git status --short` は空）。

---

## 5. 設計書からの逸脱と根拠

⭐ **逸脱1件（判断が要る性質のものではなく、設計 §11-9 の要求どおりに実装するための範囲確定）**:

| # | 設計の記述 | 実装 | 根拠 |
|---|---|---|---|
| 1 | 検査③「**`matchMedia("(max-width: …)")` を持つページを探し**、見つかったページにマーカー付きの帯を要求する」 | ⭐ **走査対象を `UI_PAGES`（トップ・デッキ構築）の2ページに限定**した | ⚠️ **リポジトリ全体を素で走査すると `tournaments/tournaments.js` の `matchMedia("(max-width:640px)")` が引っかかる**（順位表の列を畳むためのもので、絞り込み導線とは別の規約）。これにマーカーを要求すると **`tournaments.css` を触る必要が生じ、設計の「マーカーは1組だけ」「触るのは `style.css`」と両立しない**。⭐ **デッキ構築を走査対象に含めてある**ので、設計が挙げた「将来デッキ構築に JS 側の帯を足したらマーカーも要る」は**覚えていなくても効く**（＝“いま無いこと”を前提に埋め込んでいない）。⚠️ **`tournaments` にも同じ規約を広げるべきか**は §6 で確認を求める |

- ①の走査範囲から **`scripts/` を外した**のは逸脱ではなく必然（ビルドスクリプトは絞り込みUIを組み立てず、
  **`validate.mjs` 自身が識別子を文字列で持つため自己検出になる**）。コードにその理由をコメントで残した。
- 既存表記との照合: 成功行の形（`… in sync — N…`）・エラー見出しの大文字表記・
  「fail-closed／両辺が同源にならないこと」の注意書きは、既存の `CRON-ADD` 検査・`LINEREF_ROOTS` 検査・
  `ROOT_OWNERS` 検査に揃えた。`TRANSLATION.md` に関わる変更は無い。

---

## 6. 設計担当への確認事項

| # | 確認したいこと | 実装側の見立て |
|---|---|---|
| **Q-A1** | ⚠️ **`tournaments/tournaments.js` の `matchMedia("(max-width:640px)")`（＋生成元 `scripts/build-tournament-pages.mjs`）にも、同じ「マーカーで帯を名指しする」規約を広げるか。** いまは検査③の対象外 | ⭐ **広げないのが今回の範囲**（設計の「マーカーは1組だけ」に従った）。広げるなら**別単位**（`tournaments.css` と生成スクリプトの両方を触るため）。⚠️ **実装側では決めない** |
| **Q-A2** | 検査①の**消しどき**（設計 §13 の「タブ化で非対称が消えたら検査も消す」）を、`validate.mjs` のコメント以外にどこかへ残すか | いまは `validate.mjs` の冒頭コメントに「タブ化で消えたら検査①も消す（設計 §13）」と書いてある。**待ち行列側にも1行要るなら設計担当の判断で** |

---

## 7. レビュー担当への引き継ぎ

- **コミット**: `bd94bee9`（`git show bd94bee9` で全差分。生成物の再生成は**不要**＝生成ロジックに触れていない）
- **再現手順**: `npm run validate` → **exit 0**・成功行3本（§2）。
  破壊試験は `python3 tmp/dev-E-add-1/destroy.py`（⚠️ **作業ツリーを一時的に書き換えて `git checkout --` で戻す**ので、
  レビュー担当は**同じ手順を自分で走らせず**、個別ケースを手で試すか、`tmp/dev-E-add-1/destroy-results.json` を証跡として見るのが安全）
- ⭐ **見どころは D3-1**（マーカー側の帯だけ 640 に戻す）。**素朴な集合一致の検査なら通る**ケースで、
  ここが exit 1 になることがこの単位の成否を分ける
- ⚠️ **サーバーは 3000番（ユーザーの実物確認用）が上がっている。止めないこと**（この単位では新規に上げていない）
