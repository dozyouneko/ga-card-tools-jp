**状態: 生きている**（最終確認 2026-09-04）— 未起票。⭐ **設計書はまだ無い**（＝`実装待ち` を付けない）。GitHub復旧後に `gh issue create`（`enhancement` / `着手可`。⚠️ **`小タスク` を付けない**）／ 索引: [`待ち行列と復旧手順.md`](../待ち行列/待ち行列と復旧手順.md)

# issueの起票案 — デッキ構築ツールを左ペイン化し、検索条件をカードDBと統一する

⭐ **呼び名: 「デッキ構築の左ペイン化」**（⚠️ **かつて索引で「新A」と呼んでいたもの**。記号をやめた経緯は索引 §4-4）

⚠️ **これは設計書ではなく「issueの下書き」。** GitHubアカウント停止中で `gh issue create` が打てないため、本文をここに用意する。

- 出所: [`ユーザー要望メモ_2026-09-04.md`](ユーザー要望メモ_2026-09-04.md) の「デッキ構築ツールUI刷新」
- ⭐ **同じ見出しから2件に分けた**。もう1件は **「検索結果のタブ化」**（[`起票案_2026-09-04_検索結果のタブ化.md`](起票案_2026-09-04_検索結果のタブ化.md)）。分割の理由は §7

> ## ⚠️ 置き場の注意（起票時にやること）
>
> ⭐ **起票したら本ファイルを `docs/design/<本issue番号>-<タスク名>/` へ移し、
> 索引 §1 のリンク修正を同じコミットで行う**（分けると `npm run validate` が
> `索引に載っていない起票案` で exit 1 になる）。
> ⚠️ **`ユーザー要望メモ_2026-09-04.md` は他の4件の起票案からも参照されている。**
> 移すならリンク5本を同時に直す。

---

## 1. 要望（ユーザー・2026-09-04）

> - カードDBと同じように検索条件は左ペインに
> - 検索条件はカードDBと統一する
> - UIはカードDBと同じようにHeroUIを利用する
> - omnidex提出用コピーやなども左ペインに納められるなら納める

⭐ **これは「新しい見た目を発明する」タスクではない。**
トップページ（`index.html`）で **2026-09-03 に設計・実装・レビュー・ユーザーの実物確認・承認まで
完了している見た目**を、デッキ構築ツールへ**移植する**タスクである。

---

## 2. ⭐ 前提: トップ側で決着済みの判断（**蒸し返さない**）

⚠️ **次はすべて 2026-09-03〜09-04 に決着済み。** 設計工程で再検討しないこと。
出所は [`PC左ペイン_設計.md`](../未採番-UI刷新の検討/PC左ペイン_設計.md) §3〜§5・§11〜§13 と
[`承認_2026-09-03.md`](../未採番-UI刷新の検討/承認_2026-09-03.md)。

| # | 決着済みの判断 | 値 |
|---|---|---|
| P1 | 検索入力の置き場 | ⭐ **まるごと左ペイン**（案1） |
| P2 | ペイン幅 | **320px**（280pxと列数が同じ・360pxは1列余分に減る） |
| P3 | ブレークポイント | **1200px**。それ未満は現状の上部バーのまま |
| P4 | 組み方 | ⭐ **`body` を2カラムGridにするCSSのみ**。`display: contents` と `#filter-row` の中身に触らない |
| P5 | HeroUI の採り方 | ⭐ **案A-しっかり**（角丸・フォーカスリング・余白・行高・文字サイズ）。⚠️ **React・Tailwind・ビルド工程は入れない＝CSSの値と考え方だけ借りる** |
| P6 | 配色 | ❌ **置き換えない**。金 `#d9a441` はサイトの識別子 |
| P7 | 影 | ⚠️ **HeroUIの影スケールは暗色地では効かない**（黒・不透明度0.02〜0.08）。⭐ **面の明度差＋内側1pxハイライト＋区切り線の半透明化**で翻案する |
| P8 | 絞り込みの開閉 | ⭐ **アコーディオン（1つ開いたら他は閉じる）** |
| P9 | 選択中の条件の見せ方 | ⭐ **案A（左ペイン最上部のブロック）のみ**。⚠️ **案C（見出しに項目名）は実装しない** |
| P10 | ヘッダ | ⭐ **固定する**（アプリシェル化） |

⚠️ ⭐ **P5 は設計担当の推奨（案A-最小）より一段踏み込んだユーザー判断**。
「最小でよいのでは」と設計工程で差し戻さないこと。

---

## 3. ⚠️ トップ側の対象外だった理由が、本タスクの中身

[`起票案_2026-09-02_PC左ペインとHeroUI案Aの採否.md`](../未採番-UI刷新の検討/起票案_2026-09-02_PC左ペインとHeroUI案Aの採否.md) §4 の **S1**:

> ⭐ **今回はトップページ（`index.html`）のみ。デッキ構築ツールは対象外**
> — デッキ構築はDiscordログイン必須でヘッドレス検証に制約があり、規模と検証コストが跳ね上がる

⭐ **本タスクはこの S1 を意図的に解除するもの**であって、決着済みの判断を覆すものではない。
⚠️ **「検証コストが跳ね上がる」という理由のほうは解除されていない**（→ §6）。

---

## 4. 現状（2026-09-04 実測）

### 4-1. デッキ構築ツールは**トップとCSSを共有していない**

| 項目 | 実測 |
|---|---|
| 読むCSS | `tools/deck-builder/style.css`（**724行**・自前）＋ `shared/css/` の4本（`card-badges` / `filter-chips` / `element-orbs` / `card-detail`） |
| ⚠️ ルート `style.css` | ⭐ **読んでいない**（読むのは `index.html` と `404.html` の2つだけ） |
| 検索条件のDOM | `#search-top` → `.row`（`#s-name` `#s-text`）＋ `#s-filter-row`（4群＋`#s-format`＋`#s-set`）＋ `.row`（`#s-reset` `#s-search`） |
| 検索結果 | ⚠️ ⭐ **モーダル `#result-modal`**（並び替え `#s-sort` / `#s-order` もモーダルの中） |
| omnidex等の導線 | `#ed-copy-text`（📤 omnidex提出用コピー）・`#ed-copy-link`・`#ed-image` が**デッキ見出し行**にある |

⚠️ ⭐ **`:root` のトークンを直すだけではデッキ構築に届かない。**
トップの HeroUI 化は `style.css` に入っているので、**同等の見た目にするには
`tools/deck-builder/style.css` 側にも同じ値を入れる必要がある**。
⭐ **ここが本タスク最大の「二重定義」リスク**（→ §5 の Q1）。

### 4-2. ⭐ 検索条件の差分は**配線の差**であって、モジュールの差ではない

`shared/js/card-search.js` は両ページが共用している。差は**呼び出し側の引数だけ**:

| 条件 | トップ（`app.js`） | デッキ構築（`tools/deck-builder/app.js`） |
|---|---|---|
| エレメント / クラス / タイプ / サブタイプ | あり | あり |
| **レアリティ**（`g-rarity`） | ⭐ **あり** | ❌ **無し** |
| **チップ絞り込み欄**（`search: true`） | ⭐ **4群すべてに指定** | ❌ **どの群にも指定なし** |
| 選択中の条件（`#selected-filters`） | ⭐ **あり** | ❌ **無し** |
| アコーディオン | ⭐ **あり**（`app.js` に実装＝トップ専用） | ❌ **無し** |
| フォーマット / エキスパンション | あり | あり |
| 並び替え・昇降順 | 絞り込み欄の中 | ⚠️ **結果モーダルの中** |

- ⭐ **レアリティは共有モジュール側が「渡されないページ」を織り込み済み**
  （`card-search.js` の注記: `els.rarity` を持たないページでは `valuesOf(null)=[]` で常に true ＝ **no-op**）。
  **つまり足す側の作業は配線だけで、モジュールの改造は要らない見込み**
  （⚠️ **見込みであって実測ではない**。設計工程で確かめる）
- ⚠️ **アコーディオンは意図的にトップ専用**にしてある
  （`PC左ペイン_設計.md` §12-1: `shared/js/card-search.js` に書くと**デッキ構築も勝手にアコーディオンになる**＝当時のS1違反）。
  ⭐ **本タスクではそれが「望ましい挙動」に変わる**ので、**共有側へ上げるか両方に置くかを設計工程で決める**

---

## 5. 設計工程で決めること

> ### ✅ ⭐ Q1〜Q6 は 2026-09-05 に決着した（**未決は Q2 の1件だけ**）
>
> 判断材料・実測・却下した案は
> [`検討メモ_左ペイン化Q1-Q6_2026-09-05.md`](検討メモ_左ペイン化Q1-Q6_2026-09-05.md) にある。
> ⭐ **設計書を起こす人は、下の表の「決定」列だけ読めば進められる。**
> ⚠️ **Q2 だけはモックをユーザーに見せてから決める**（左ペインの縦の構成そのものが変わるため）。

| # | 論点 | メモ | ✅ 決定（2026-09-05） |
|---|---|---|---|
| **Q1** | ⚠️ ⭐ **HeroUI のトークンを2ファイルに二重定義するのか、共有CSSに切り出すのか** | ⭐ **本タスクで最も重い判断**。二重定義は**片方だけ直すと画面で見た目が食い違う**（既に `card-badges.css` と用語ハイライトで2回踏んでいる型）。⚠️ **共有化するなら `404.html` への波及も見る**（ルート `style.css` を読むため） | ✅ ⭐ **`shared/css/tokens.css` に切り出す**（二重定義にしない）。⭐ **HeroUI の「ルール」はセレクタが違い共有できないので「値」をトークン化する**（`--radius-control` 等）。⚠️ **範囲は `index.html` / `404.html` / `tools/deck-builder/` / ⭐ `tools/glossary/` の4枚**（glossary を巻き込むのはユーザー判断）。⭐ **`validate` に乖離検査を2本足す** |
| **Q2** | 左ペインに何を入れるか | ⭐ **トップと違い、デッキ構築には「デッキ見出し行」がある**。omnidex提出用コピー・共有リンク・デッキ画像出力を左ペインへ入れるかはユーザー要望（「納められるなら」＝**条件付き**）。⚠️ **入れると縦が伸びて検索条件が押し下がる**ので、モックで見せて決める | ⚠️ ⭐ **唯一の未決＝モック待ち**。設計担当の推奨は **omnidex提出用コピー・共有リンク・デッキ画像出力の3ボタンだけを、ペイン最下部へ**（デッキ名・公開・フォーマット・保存状態は `.editor-bar` に残す）。⭐ **3案（最下部／最上部／移さない）を並べて提示する** |
| **Q3** | ブレークポイント | P3 は 1200px だが、⚠️ **デッキ構築の右側はカードタイル一覧ではなく「デッキのゾーン表示」**。⭐ **「最低4列を保てること」という P3 の判断規則がそのまま使えるとは限らない**ので、実測して決める | ✅ ⭐ **1280px**（⚠️ **トップの 1200px とは違う**）。⭐ **判断規則はトップと同じ「右列が設計幅を割らないこと」**で、デッキ構築ではそれが **`.stats-pane` の 880px**（損益分岐 1248px）。⚠️ **ゾーン表示は拘束にならない**（右列580px＝vw900pxで4列に届く） |
| **Q4** | 並び替え・昇降順の置き場 | 現状は結果モーダルの中。⚠️ **「検索結果のタブ化」と真正面から重なる**ので、**同じ設計工程で決める**（→ §7） | ✅ ⭐ **左ペインへ移す**（トップでは `sort`/`order` が左ペイン内かつ URL共有(#20)の条件の一部）。⭐ **「並び替えは検索条件の一部」と設計書に明記し、タブ化へ申し送る**（タブは条件のスナップショット／タブ切替で左ペインを戻す） |
| **Q5** | 検索条件を統一するときの「統一の程度」 | レアリティ・チップ絞り込み欄・選択中の条件・アコーディオンを**全部入れるのか、一部か**。⭐ **全部入れるのが要望の素直な読み**だが、規模が増える | ✅ ⭐ **4つとも全部入れる**（レアリティ／チップ絞り込み欄／選択中の条件／アコーディオン）。⚠️ **「検索の起動方法」だけは統一しない**（🔍ボタンと Enter のまま）。⭐ **「選択中の条件」とアコーディオンは移植せず `shared/js/card-search.js` へオプトインで上げる**（移植は3つ目の二重定義） |
| **Q6** | デッキ構築の**未ログイン画面**（`#view-login`）と**閲覧専用画面**（`#view-deck`）の扱い | ⚠️ **左ペインは編集画面（`#view-editor`）にしか意味が無い**。ほかの3ビューでレイアウトが崩れないことを設計に書く | ✅ ⭐ **`#view-editor` の内側だけを2カラムGridにし、`.editor-bar` を `position: sticky`**。⚠️ **`body` にクラスを付け外ししない**（トップの `body` 直下Grid は in-flow が3 vs 7 で、実測すると `#view-login` の高さが **0px** になる） |

---

## 6. ⚠️ 壊してはいけないもの・検証の制約

| # | 内容 |
|---|---|
| **R1** | ⚠️ **`shared/css/filter-chips.css` はトップとデッキ構築の両方が読む**。⭐ **本タスクは「向こうに出る」側が対象なので、トップ側の見た目が変わらないことを必ず確認する** |
| **R2** | ⚠️ **`shared/js/card-search.js` の `fillChips()` が `#s-filter-row` の中身の参照を握っている**。動かすと絞り込みが動かなくなる |
| **R3** | CSP `style-src 'self'`。動的スタイルは **`data-*` 属性＋CSSOM** |
| **R4** | ⚠️ ⭐ **デッキ構築はDiscord OAuthログインが前提でヘッドレスでは検索パネルに到達できない。** 検証は #51 で確立した手順（`#view-editor` の `hidden` を外し、**空のデッキ状態を用意してから** `searchCtl` を動かす）を使う。⚠️ **空のデッキ状態の用意を飛ばすと `deckData.cards` が null で落ちる** |
| **R5** | ⚠️ ⭐ **R4 の手順で担保できるのは「デッキの中身に依存しない描画」まで。** 適合判定バッジ・所持枚数などは**未検証として報告する** |
| **R6** | ⚠️ ⭐ **レビュー合格後、承認の前にユーザーがローカルサーバーで実物を操作する工程が入る**（2026-09-02のユーザー指示）。⭐ **渡すサーバーは 3000番**（`forwardPorts` が `[3000]` の1つだけで、他ポートはコンテナ外から届かない） |

---

## 7. ⚠️ なぜメモの1見出しを2件に分けたか

**本件（デッキ構築の左ペイン化）＝ 見た目の移植** / **[検索結果のタブ化](起票案_2026-09-04_検索結果のタブ化.md)＝ 操作モデルの変更**。

| | デッキ構築の左ペイン化 | 検索結果のタブ化 |
|---|---|---|
| 性質 | ⭐ **決着済みの設計を移す**（P1〜P10） | ⭐ **新しい相互作用を設計する**（モーダル廃止・履歴・タブの生成と破棄） |
| 未決の量 | 少ない（Q1〜Q6） | 多い（タブの上限・永続化・状態管理） |
| 失敗の仕方 | CSSの回帰（見れば分かる） | 状態管理のバグ（**見ただけでは分からない**） |
| 依存 | 単独で完結する | ⭐ **サイドボードプランメモの前提**になる |

⭐ **分けないと、CSSの回帰と状態管理のバグが1回のレビューに同居する**。
⚠️ **ただし「同じ見た目を二度設計する」ことは避ける**（HeroUI採否をPC左ペインと同じ設計書で決めた教訓）
——**この2件は同じ設計工程で1つのモックを共有し、実装とレビューだけ分ける**ことを推奨する。
⭐ **Q4（並び替えの置き場）はどちらの側でもなく「共有するモック」で決める**。

⚠️ **`小タスク` を付けない。** レイアウト構造の変更・トークンの二重定義判断（Q1）・
ログイン必須ページの検証制約（R4）が同居するため。

---

## 8. 完了の目安（素案・⚠️ 正式な検証項目は設計書で起こす）

1. 1200px以上で、検索入力と絞り込みが**横に間延びした帯ではなくなっている**（実測値で示す）
2. 絞り込みを開いても**デッキ表示がファーストビューから消えない**
3. **トップページの見た目が変わっていない**（R1）
4. **375px幅の挙動が変わっていない**
5. `#view-login` / `#view-decks` / `#view-deck` のレイアウトが崩れていない（Q6）
6. `npm run validate` が exit 0
7. ⭐ **ユーザーがローカルサーバー（3000番）で実物を操作し、結論を出している**（R6）

---

## 9. 参考

- [`PC左ペイン_設計.md`](../未採番-UI刷新の検討/PC左ペイン_設計.md) — §3 決めたこと / §5 HeroUI技術評価 / §11 ヘッダ固定 / §12 アコーディオン
- [`デッキ構築の検索同等化_設計.md`](../31-デッキ構築の検索をカードDBと同等に/デッキ構築の検索同等化_設計.md) — #31。⭐ **「カードDBと同等にする」は今回が2度目**
- [`スマホ絞り込み導線_設計.md`](../未採番-UI刷新の検討/スマホ絞り込み導線_設計.md) — §2-3 F1〜F5（⚠️ 外すと静かに壊れる対策）
- `CLAUDE.md`「環境の注意」— 共有CSSの横paddingの罠・CSP・ポート
