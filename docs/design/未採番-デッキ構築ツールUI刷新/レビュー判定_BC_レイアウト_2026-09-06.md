# レビュー判定 — B（レイアウト＝左ペイン化）／C（並び替えを左ペインへ）（2026-09-06）

⚠️ **GitHubアカウント停止中で `gh issue comment` が使えない**ため、issueコメントの代わりにこのファイルへ書く。

| | |
|---|---|
| 対象コミット | **`0adb564d`**（`tools/deck-builder/` の3ファイル・105挿入 20削除） |
| 実装報告 | [`実装報告_BC_レイアウト_2026-09-06.md`](実装報告_BC_レイアウト_2026-09-06.md)（`3beadb97`） |
| 設計書 | [`左ペイン化_設計.md`](左ペイン化_設計.md) **版 1.1** の **§4（B）・§4-5・§5（C）** |
| 対象外 | **§7（D）は未実装**のため **V13・V15・V19〜V21 は判定しない**。**A（共有トークン）は承認済み**（`7abc072d`）につき再判定しない |
| 証跡 | `tmp/review/未採番-左ペイン化BC/`（Playwright スクリプト15本 + スクリーンショット10枚。gitignore対象） |

---

## 判定: ⭐ **合格**

⚠️ **ただし設計担当の判断が要る項目が4件ある**（→ §5）。**合否には含めていない**。

| 基準 | 結果 | 証跡 |
|---|---|---|
| **M1** 設計適合 | ✅ | §4 の V8〜V18（対象9項目）すべてに合否。⚠️ **V9・V16 は設計書の記述側が腐っており、実装は正しい**（§4-2・§4-6 に検算） |
| **M1-b** 既存条件との対称性 | ✅ | **絵柄の追従・バッジ集計・件数表示**を並び替え経由で実測（§3）。⚠️ **共有URL(#20)はデッキ構築に存在しないため対象外**（設計 §9-3 と同じ） |
| **M2** 実駆動 | ✅ | レビュー担当自身が **1280 / 1279 / 1366 / 1440 / 1920 / 375px** で操作。スクリーンショット10枚（§6） |
| **M3** 回帰 | ✅ | `npm run validate` **exit 0**（全緑）。トップ50タイル / カード詳細モーダル / カード個別ページ / デッキ構築 / 大会一覧454行 が**すべて描画・JSエラー0件** |
| **M4** 生成物の同期 | ✅ | 生成ロジック（`scripts/**`・`data/tl/**`）に**変更なし**（差分は `tools/deck-builder/` の3ファイルのみ）＝再生成の対象が無い |
| **M5** 静かな劣化がない | ✅ | ⭐ **破壊試験5本を独立に実施**し、対策4つがすべて**外すと壊れる**ことを確認（§2） |
| **M6** 公開安全性 | ✅ | **新規ファイル0**（公開面は増えない）。`_headers` / `404.html` 無変更。秘匿情報の混入なし。CSP違反 **0件**（§2-4） |
| **M7** スコープ | ✅ | 変更は `tools/deck-builder/{app.js,index.html,style.css}` の3ファイルのみ。**§7（D）は1行も入っていない**（`shared/js/card-search.js`・トップの `app.js`/`style.css` は無変更） |

---

## 1. 前提の検算（⚠️ 測る前に必ずやる）

`scripts/serve.mjs` は配信ルートを `import.meta.url` から決めるので `cd` では切り替わらない。
**before 側のツリーにある `serve.mjs` を起動**した（A の周でレビューが踏んだ罠）:

```bash
git archive 0adb564d^ | tar -x -C tmp/review/未採番-左ペイン化BC/before
PORT=3100 node scripts/serve.mjs                                        # after
PORT=3120 node tmp/review/未採番-左ペイン化BC/before/scripts/serve.mjs   # before
```

| 検算 | 結果 |
|---|---|
| `curl :3100/tools/deck-builder/ \| grep -c row-actions` | **2** |
| `curl :3120/tools/deck-builder/ \| grep -c row-actions` | **0** |
| ⭐ **実際に差を検出したか** | **した**（V9 で after/before に 12ノードの増加と Δtop 65px の一様なずれ・§4-2） |

⚠️ ポートは **3100 / 3120**（開発担当の 3000 / 3010 とずらした）。**終了時に個別に停止**。

---

## 2. ⭐ 破壊試験（レビューが独自に足したもの）

⚠️ **「動いたから合格」では、対策が効いているのか偶然動いているのか区別できない。**
CSSOM で対策を実行時に外し、**壊れることを見た**（作業ツリーは変更していない）。

| # | 外したもの | 結果 | 判定 |
|---|---|---|---|
| **D1** | ⭐ **`#view-editor[hidden] { display: none }`** | ⚠️ **`#view-editor` が `display: grid` のまま残り、`#view-deck` が `top 121 → 851`（+730px）** へ押し出される（1440/1280 の両方・148要素に波及）。⭐ **デッキ一覧では編集画面がまるごと一覧の下に描画される**（`destruct_hidden_removed.png`）。1279px では発生しない | ⭐ **この1行は効いている**（報告の主張を独立に再現） |
| **D2** | `#search-top > .row-actions { flex-direction: row }` | ⚠️ **リセット/検索が縦積みに戻る**（`flex-direction: column`・reset 139×42 → 287×42、ペイン 639 → 691px） | ✅ 効いている |
| **D3** | `#s-name, #s-text { flex: 0 0 auto }` | ⚠️ **`#s-name` の高さが 42 → 240px**（`flex-basis: 240px` が高さとして効く）・**ペイン 639 → 843px** | ✅ 効いている（コメントの主張どおり） |
| **D4** | （外さずに測定）`--editor-bar-h` の追従 | 通常名 **57px** / 最長名36文字 **102px**。⭐ **`#search-top` の `top` と `max-height` が 57px/843px ↔ 102px/798px で完全に追従**。1920pxへ resize すると 102 → **57px** に戻る | ✅ **固定値ではない**ことを実測 |
| **D5** | （測定）**CSP違反** | `securitypolicyviolation` を捕捉 → ⭐ **0件**（`style-src 'self'` 下で `documentElement.style.setProperty()` は通る） | ✅ |
| **D6** | （測定）**375px でのボタン font-size** | ⚠️ 実装コメントが「`.search-top .btn` の節は `@media (max-width: 860px)` より**前**に置くこと」と主張 → 実測 **`#s-reset` / `#s-search` / `#s-order` とも 16px**（iOSのフォーカス時ズーム防止が生きている） | ✅ 主張どおり |

---

## 3. ⭐ M1-b（既存の同種の条件で効いている副作用）

C は**並び替えの置き場と再検索条件を変えている**ので、**並び替え経由で既存の副作用が壊れていないか**を見た。
⚠️ **共有URL(#20)はデッキ構築に存在しない**ので3つ（設計 §9-3 と同じ扱い）。

**手順**: タイプ=`CHAMPION` ＋ エキスパンション=`Re:Collection Shadowdancer（ReC-SHD）` で検索 → 並び替えを **レベル順**に変更 → **昇降順ボタン**を押す。
各段階でタイル4件の `dataset.artUrl` を公式APIの `editions` と突き合わせた（`m1b5.mjs`）。

| # | 副作用 | 実測 | 判定 |
|---|---|---|---|
| **絵柄の追従** | 4件すべて `artSet = **ReC-SHD**`（＝選んだ版の絵）。⭐ **うち3件は `editions[0]` ではない**（`spirit-of-wind`=ALC初出 / `tristan-underhanded`・`tristan-hired-blade`=MRC初出）＝**フォールバックではなく本当に追従している**。⭐ **レベル順に変えても・降順にしても4件とも同じ `artUrl` のまま**（崩れ **0件**）。`<img>` の `src` も `dataset.artUrl` と一致（4/4） | ✅ |
| **バッジ集計** | `#s-filter-badge` = **1**（タイプ群1件）が、**検索後・並び替え変更後・昇降順変更後**とも **1 / hidden=false** のまま。⭐ 4群を1つずつ選ぶと **4** に加算され、リセットで **0**（V17） | ✅ |
| **件数表示** | 版なし: `24 件を表示 / 全 134 件` → 版あり: `4 件を表示` → レベル順: `4 件を表示（レベルを持つカードのみ）` → 降順: 同左。⭐ **注記（概算・不一致）は出ていない**＝異常の信号なし | ✅ |
| — | ~~共有URL（#20）~~ | ⭐ **デッキ構築に仕組みが存在しない**ため対象外 | — |

⚠️ **`preferredArtIndex()` がレアリティを見ていない件は D の周の指摘**（設計 §7-1）。
**本コミットの範囲では「エキスパンションでの追従」が既存の同種条件**であり、それは壊れていない。

---

## 4. 設計書 §9-2 の検証項目との対応（⭐ レビューが独立に再実施）

### 4-1. V8 — 1280px の幾何 ✅

| 対象 | 期待値 | 実測 |
|---|---|---|
| 左ペイン `#search-top` | left 0 / 320px | **top 178 / left 0 / 320×639** |
| 右列 `.deck-tabs` / `.deck-pane` | left 320 / 960px | **left 320 / 960px**（tabs 178・pane 229） |
| メインデッキの列数 | 6列 | **6列**（grid 912px） |
| `.stats-pane` | 880px | ⭐ **880px**（統計タブを開いて実測。left 320・`max-width: 880px` に到達） |
| 横スクロール | 出ない | `scrollWidth` **1280** ＝ `innerWidth` **1280** |

⭐ **1366 / 1920px も測った**: 右列 1046px / **7列**、1600px / **11列**（設計 §4-4 のモック実測と一致）。

### 4-2. V9 — 1279px の幾何 ✅（⚠️ ただし設計書の文言は文字どおりには成立しない）

**B（レイアウト）は 1279px で不活性**であることを先に確定させた:
`#search-top` は **left 0 / 幅1279 / `position: static`**、`.editor-bar` も **全幅・static**、メイン **9列**（＝before と同じ）。

そのうえで **`#view-editor` の全ノードを before と突き合わせた**（`v9.mjs`）:

| 分類 | 1279px | 375px | 中身 |
|---|---|---|---|
| **完全一致** | **916** | **916** | — |
| **縦にずれただけ** | 43 | 43 | ⭐ **Δtop が全要素で 65px（375pxでは 62〜63px）で一定**＝並び替え行1行ぶん |
| **寸法が変わった** | **3** | **3** | `#view-editor` と `#search-top` が**ちょうど 65px / 63px 高くなった** ＋ `.row-actions` 行が **43→42px（375pxでは 46→42px）**＝§4-5 のボタンのトークン化 |
| **after にのみ存在** | 12 | 12 | `.result-sort` の label / `#s-sort` / option×6 / `#s-order` ＝ **C の新規ノード9個**（残り3件は挿入による添字ずれ） |
| **消えた** | **0** | **0** | — |

⭐ **V9 の括弧書き（`#search-top` が left 0・全幅、`.editor-bar` 全幅）は満たしている。**
⚠️ 一方、見出しの「**現状と完全一致**」は**同じ設計書 1.1 の §4-5（ボタンのトークン化）と §5（DOM追加）と両立しない**。
⭐ **これは実装の不備ではなく設計書の内部矛盾**なので M1 の欠落とは扱わない（→ §5-1）。

### 4-3. V10 — 他3ビュー ✅（⭐ 破壊試験つき）

**`#view-login` / `#view-decks` / `#view-deck` × 1440 / 1280 / 1279 / 375px ＝ 12通り**を before と全ノード突き合わせ:

| | 結果 |
|---|---|
| 差分 | ⭐ **0**（`view-login` 27要素 / `view-decks` 31要素 / `view-deck` 150要素） |
| `#view-editor` の `display` | 3ビューとも **`none`**（after / before とも） |

⭐ **`#view-editor[hidden] { display: none }` を外すと壊れることまで確認**（→ §2 の D1）。
**報告が主張する「実在の不具合を1件検出した」は、レビュー側で独立に再現できた。**

### 4-4. V11 — `.editor-bar` の sticky と z-index ✅

| 対象 | 実測 |
|---|---|
| 1500px スクロール後 | `.editor-bar` の **top = 0**（sticky）／`#search-top` の **top = 57**（＝`--editor-bar-h`） |
| z-index | `.editor-bar` **6** < `#result-modal` **60** ＝ `.gacd-modal` **60** < `#tile-menu` **70** |
| 実際の重なり | 結果モーダル表示中に**バー中央を `elementFromPoint`** → **`DIV.modal-backdrop`** が返る（＝モーダルが上） |

### 4-5. V12 — 375px ✅

| 対象 | 実測 |
|---|---|
| `#search-top` | **left 0 / 幅375** |
| 絞り込み | `#s-filter-row` は `display: none` → `#s-toggle` を押すと **`flex`** |
| 並び替え | `.result-sort` は **left 24 / right 351**（`#s-sort` left 80–right 218・`#s-order` left 226–right 305） |
| はみ出し | ⭐ **`right > innerWidth` の要素 0件**（トグルを開いた後も0件）。`scrollWidth` **375** ＝ `innerWidth` **375** |

### 4-6. V16 — 並び替え（Q4）⭐ **再検索は ✅ / 件数の期待値は設計書側が腐っている**

**(a) 再検索**: **モーダルを閉じた状態から** 6項目 × 昇順/降順 ＝ **12通りすべて**で
`#result-count` が「検索中…」になり結果モーダルが再表示された（**12 / 12**）。
⚠️ **APIリクエスト数では測っていない**（#44 の設計どおり並び替えは0リクエストになりうるため。報告 §4-4 の指摘は妥当）。

**(b) 件数の独立検算**:

| 測定 | after(3100) | before(3120) |
|---|---|---|
| `type=CHAMPION` ＋ 名前順 | **全 134 件** | ⭐ **全 134 件** |
| `type=CHAMPION` ＋ レベル順 | **全 133 件（レベルを持つカードのみ）** | ⭐ **全 133 件** |

⭐ **「もっと見る」を押し切って全slugを取り、レベル順で消える1枚を特定した**（`v16c.mjs`）:

```
missing: ["lu-bu-indomitable-titan"]
```

- ⭐ **差1枚の根拠が完全に裏付けられた**。`lu-bu-indomitable-titan` は
  スナップショット実測で **`types = ["UNIQUE","ALLY"]` / `level = null`**（＝CLAUDE.md が言う
  「`type=CHAMPION` 検索に混ざって返るがチャンピオンではない」1枚）で、#39 の除外規則で落ちる。
- ⭐ **before ツリーでも同じ 134 / 133** ＝ **本コミットの回帰ではない**。
  設計書の「124件」は**総数が 125 → 134 に増えた分だけ古い**。
- → **V16 は合格**。⚠️ **設計書の数字の書き換えは設計担当の判断**（→ §5-4）。

### 4-7. V14 — 4群すべて開く＋「すべて表示」 ✅

| 対象 | 実測 |
|---|---|
| チップ数 | エレメント **13** / クラス **9** / タイプ **13** / **サブタイプ 157** |
| `max-height` | 4群とも **200px**・`overflow-y: auto` |
| 「すべて表示」後 | ⭐ **サブタイプだけ `max-height: none`**（`:has()` が効いている）。他3群は 200px のまま |
| ペイン内スクロール | `scrollHeight 5060` / `clientHeight 843` / `overflow-y: auto` |
| ページ全体 | ⭐ **横に伸びない**（`scrollWidth` 1280 ＝ `innerWidth`） |
| 🔍検索の到達性 | ペインを最下部までスクロールすると **ペインの矩形内**（pane 178–1021 / search 955–997） |

### 4-8. V17 — リセット ✅

| 項目 | リセット前 | リセット後 |
|---|---|---|
| 名前 / テキスト | `fire` / `counter` | **空 / 空** |
| フォーマット / エキスパンション | `STANDARD:LEGAL` / `0` | **空 / 空** |
| 4群の選択数 | 1 / 1 / 1 / 1（バッジ **4**） | **0 / 0 / 0 / 0**（バッジ **0**） |
| `#s-sort` | `power` | ⭐ **`name`（名前順）** |
| `#s-order` | `DESC`「▼ 降順」 | ⭐ **`ASC`「▲ 昇順」** |

### 4-9. V18 — パンテオンのデッキ ✅

**1280px と 1279px で完全に同一**の5ゾーン（バッジ「🏛 パンテオン」）:

| ゾーン | 見出し |
|---|---|
| マテリアルデッキ | 12 / 12 |
| メインデッキ | 60 / 60以上 |
| パンテオン | Lesser 1/1・Greater 1/1 |
| 検討中 | 2枚 |
| **サイドボード** | ⭐ **「サイドボード（このフォーマットでは使いません）」2枚** |

⚠️ **R5（設計 §9-4）の制約はそのまま**: 実ログインでの保存・PATCH 経路は未検証（スタブが握りつぶすため）。
⭐ ただしゾーン描画・適合表示はスタブの**実データ**（マテリアル12・メイン60）で描かれている。

### 4-10. M3（回帰）の実測

| 画面 | 実測 |
|---|---|
| `npm run validate` | ⭐ **exit 0**（`shared css tokens in sync — 68 / 未定義 0`・`:root owners in sync — 4ファイル` を含む全緑） |
| トップ（`/`） | 検索欄あり・タイル **50件**描画 |
| カード詳細モーダル | タイルをクリック → `.gacd-modal` が開く（「自己放棄」） |
| カード個別ページ | `/cards/tristan-shadowdancer/` — h1 と `.cp-block` **6節** |
| デッキ構築 | 1280 / 1279 / 375px とも編集画面が描画（カード40枚） |
| 大会一覧 | `/tournaments/` — **454行** |
| **JSエラー** | ⭐ **0件**（cloudflareinsights のCORSは公開環境専用のビーコンで、本変更と無関係） |

---

## 5. ⚠️ 設計担当への確認事項（⭐ **合否には含めていない**）

⭐ **報告 §6 の4件はいずれも妥当**で、レビュー側でも実測して裏を取った。**レビューが仕様を決めるべきではない**ので、そのまま上げる。

### 5-1. V9 の期待値「現状と完全一致」を書き換えるか（報告 §6 に無い・**レビューの追加**）

**同じ設計書 1.1 の中で矛盾している。** §4-5（`.search-top .btn` のトークン化）は**全幅**でボタン寸法を変え、
§5（`.result-sort` の移設）は**全幅**でDOMを1行増やすので、**V9 が文字どおり成立することは原理的にありえない**。

- ⭐ 提案: **「B（レイアウト）由来の差が0であること」**＝ `#search-top` が left 0・全幅・`position: static`、
  `.editor-bar` 全幅、メイン9列——**と書き換える**（今回はこの形で合格判定した）。
- ⚠️ **このまま残すと、次に §4-5 相当の変更が入るたびに「V9 が落ちた」と誤読される。**

### 5-2. ⚠️ ⭐ `#s-sort` だけ見た目が揃わない（報告 §6-2）— **レビューも実測で追認**

`.result-sort select`（結果ダイアログ用の古い宣言）が `.search-top select` と**同詳細度（0,2,0）で後勝ち**するため、
**左ペインの中で1つだけ小さく、地の色も違う**:

| | 角丸 | padding | font-size | 高さ | 背景色 |
|---|---|---|---|---|---|
| `#s-format` / `#s-set` | 10px | 10px 14px | 14px | **40px** | `rgb(27,31,40)`（`--panel`） |
| `#s-name` / `#s-reset` / `#s-search` / **`#s-order`** | 10px | 10px 14px | 14px | **42px** | `rgb(27,31,40)` |
| ⚠️ **`#s-sort`** | **8px** | **6px 10px** | **13.6px** | ⚠️ **32px** | ⚠️ **`rgb(35,40,52)`（`--panel-2`）** |
| （参考）トップの `#f-sort` | 10px | 10px 14px | 14px | 40px | — |

- ⚠️ **375px でも揃っていない**（`#s-sort` **35px** / 他 **42px**）。
- ⭐ **スクリーンショットで明確に見える**（`pc1280.png` の「並び替え」行）。
- ⭐ **実装が触らなかったのは正しい**（§4-5 が名指ししたのは `.search-top .btn` と `outline-offset` の2つだけ＝M7 を守った）。
  **どちらに倒すかは設計判断**: (a) `.result-sort select` の旧宣言を消してトークンに揃える／(b) このままにする。

### 5-3. ⚠️ ⭐ 結果モーダルが左ペインを覆い、「結果を見ながら並び替える」ができない（報告 §6-3）— **レビューも再現**

⭐ **レビュー中に偶然踏んだ**（Playwright が `#s-reset` をクリックできず 30秒でタイムアウトし、
`<div data-close class="modal-backdrop"> … intercepts pointer events` と報告した）。
そのうえで意図的に再現した:

```
並び替えを power に変更 → モーダルが開く（modalHidden: false）
elementFromPoint(#s-sort 中央)  → DIV.modal-backdrop
elementFromPoint(#s-order 中央) → DIV.modal-backdrop
elementFromPoint(#s-reset 中央) → DIV.modal-backdrop
```

- **変更前**は並び替えが結果ダイアログの中にあり、**結果を見ながら**変えられた。
- **変更後**は「モーダルを閉じる → ペインで変える → またモーダルが開く」の往復になる。
- ⚠️ ⭐ **これは Q4（ユーザー決定）から直接出る帰結**であり、**実装の不備ではない**。
  設計 §5-3・§10 の申し送り（タブ化でモーダルが消えたら再検討）が効くまでの間、この往復が残る。
  ⭐ **許容してよいかを設計担当に判断してほしい**（レビューでは決めない）。

### 5-4. V16 の期待値「124件」の更新（報告 §6-4）

**134 / 133（差 1 ＝ `lu-bu-indomitable-titan`）が現在の実測で、変更前ツリーでも同じ**（§4-6 で独立検算済み）。
⭐ **報告の提案（絶対数ではなく「総件数 − レベル順の件数 ＝ 1」という差で書く）は妥当**と考える。
⚠️ **書き換えの可否は設計担当の判断**。

### 5-5. `#view-editor[hidden]` を設計書 §4-1 のCSSに反映するか（報告 §6-1）

⭐ **反映すべき**というのがレビューの意見（§2 の D1 で「外すと壊れる」ことを実証した）。
§4-1 のコードブロックをそのまま写すと**必ず V10 が落ちる**ので、設計書側に1行足しておかないと次に同じ穴を掘る。
⚠️ ただし**設計書の編集は設計担当の仕事**なので、ここでは直していない。

---

## 6. 参考（⚠️ 合否には含めない・基準IDに紐づかないもの）

| # | 内容 |
|---|---|
| **参考1** | ⭐ `#s-order` は `class="btn btn-sm"` のままだが、新しい `.search-top .btn`（0,2,0）が `.btn-sm`（0,1,0）に勝つため **`btn-sm` は事実上無効**になり 42px で描画される。**見た目としては他のボタンと揃うので望ましい**が、**クラスと実際の見た目が食い違っている**。`btn-sm` を外すかどうかは D の周でまとめて見るのが自然 |
| **参考2** | `.result-sort` は `margin: 6px 0 8px` を保ったままペインに入っている。1280px の `gap: 8px` と二重にかかるが、実測では破綻していない（ペイン内の縦間隔がわずかに広いだけ） |
| **参考3** | ⭐ **報告 §4-4 の「APIリクエスト数で再検索を判定しない」は良い記録**。レビューでも同じ判定手段（`onStart` ＝ `#result-count` の「検索中…」＋モーダル表示）を使い、12/12 を確認した |
| **参考4** | 実装コメントが「`.search-top .btn` の節は `@media (max-width: 860px)` より前に置くこと」と主張しており、**375px で 16px が維持されていることを実測で確認した**（§2 の D6）。⭐ **コメントが検証可能な主張になっている**のは良い |

---

## 7. 次の工程

1. ⭐ **設計担当が §5 の5件を判断する**（V9 の文言・`#s-sort` の見た目・モーダルの往復・V16 の数字・`#view-editor[hidden]` の設計書反映）
2. ⭐ **ユーザーの実物確認**（設計 §9-5）。⚠️ **3000番で上げる**（3100/3300 はコンテナ外に届かない）
3. 承認 → **公開の可否はユーザーが判断**（⚠️ **未push**）

---

## 付録: 証跡

`tmp/review/未採番-左ペイン化BC/`（gitignore対象）

| ファイル | 中身 |
|---|---|
| `harness.mjs` | Playwright 共通（`--ignore-certificate-errors` ＋ `ignoreHTTPSErrors`、スタブの `addInitScript`、`frameElement.dataset` の getter） |
| `v8.mjs` / `v9.mjs` | V8・V9（1280/1279/1366/1920/375 の幾何、before との全ノード突き合わせ） |
| `v10.mjs` / `v10b.mjs` | V10（12通り）＋ **`#view-editor[hidden]` の破壊試験** |
| `v11_14.mjs` | V8の `.stats-pane`・V11（sticky/z-index）・V14（4群＋すべて表示） |
| `v12_17.mjs` / `v17_18.mjs` | V12（375px）・V17（リセット）・V18（パンテオン） |
| `v16.mjs` / `v16b.mjs` / `v16c.mjs` | V16（12通りの再検索・件数の before/after・**消える1枚の特定**） |
| `m1b3.mjs` / `m1b5.mjs` | **M1-b**（絵柄の追従・バッジ集計・件数表示） |
| `destruct.mjs` | 破壊試験 D2〜D6（`.row-actions` / `flex:0 0 auto` / `--editor-bar-h` / CSP / 375pxのfont-size） |
| `m3_s62.mjs` | M3（主要5画面の回帰）・§5-2 の寸法・§5-3 のモーダル遮蔽 |
| `pc1280.png` / `pc1279.png` / `pc1280_before.png` | 1280px の左ペイン（after）／1279px（1カラム）／before の1280px |
| `sp375.png` | 375px（並び替えが検索パネル内・はみ出しなし） |
| `pc1280_scrolled.png` / `pc1280_groups_open.png` / `pc1280_stats.png` | sticky・4群すべて開く・統計タブ（`.stats-pane` 880px） |
| `pantheon1280.png` | パンテオンの5ゾーン |
| ⭐ `destruct_hidden_removed.png` | **破壊試験 D1**：`#view-editor[hidden]` を外すと**デッキ一覧の下に編集画面がまるごと出る** |
| `m3_top.png` / `m3_carddetail.png` | トップ・カード詳細モーダル |
| `s63_modal_covers_pane.png` | §5-3：結果モーダルが左ペインを覆っている状態 |
