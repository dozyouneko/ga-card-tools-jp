# 実装報告 — G-3b（デッキ構築を 1199px 以下でスマホ表示に統一）

- **対象**: `docs/design/未採番-デッキ構築ツールUI刷新/左ペイン化_設計.md`（**1.9**）§8-1 の**単位4 = G-3b** のみ
- **日付**: 2026-09-07
- **担当**: 開発（実装）
- ⚠️ **E-add-1 / F-a / F-b には手を付けていない**（設計の指示どおり）

---

## 1. 実施内容

| # | ステップ | 内容 |
|---|---|---|
| 1 | 設計の読み込み | §14-3 の分割表（作業指示）／§14-4（スマホ表示の定義）／§14-6 の **G-3b 必須条件4点** |
| 2 | 実装 | `tools/deck-builder/style.css` に **`@media (max-width: 1199px)` を新設**し、**絞り込みの導線4つだけ**を `@media (max-width: 860px)` から移した |
| 3 | 検証 | before/after を**別ツリー・別ポートで並走**させ、Playwright + srcdoc ハーネスで実駆動測定 |
| 4 | `npm run validate` | **exit 0**（`all checks passed.`） |

### コミット

| 項目 | 値 |
|---|---|
| **コミットハッシュ** | **`5312e10460e9db327eb7298ee46d150494dad08f`**（短縮 `5312e104`） |
| 変更ファイル | **`tools/deck-builder/style.css` の1ファイルのみ**（`+18 / -6`・実質21行） |
| 担当パス外の混入 | **なし**（`git -c core.quotepath=false show --name-only` で確認） |
| **push** | ⚠️ **していない**（本番公開はユーザー判断） |

### 差分の中身（4つだけを移した）

新設した `@media (max-width: 1199px)` に入れたのは、**§14-3 の分割表の「1199px以下へ移す」列の4項目**だけ:

1. `.search-top .filter-toggle`（ルールごと）
2. `.search-top .filter-row { display: none }` と `.search-top.filters-open .filter-row { display: flex }`
3. `.search-top .filter-row select` の全幅（`flex: 1 1 100%; width: 100%; min-width: 0`）
4. `.search-top .filter-row .fgroup` の全幅（`flex: 1 1 100%`）

**`@media (max-width: 860px)` に据え置いたもの**（＝帯ごと広げていない）:
`.gtile .ctrl-strip { opacity: 1 }` ／ **`.search-top input, select, .btn { font-size: 16px }`** ／
**`.artgrid { 2列 }`** ／ **`.modal-body` の全画面シート化** ／ `.resultgrid` ／ `.modal-close`。
`@media (max-width: 620px)` / `(max-width: 640px)` は**一切触っていない**。

⭐ 新ブロックは `@media (max-width: 860px)` の**直前**に置いた。基底の
`.search-top .filter-toggle { display: none }` と `.search-top .filter-row { display: grid }` より
**後ろ**にある必要があり、860px ブロックとは**現在1つも競合していない**（上の据え置き一覧と重ならない）。

---

## 2. 検証結果（設計の検証項目に対応づけ）

### 検証環境

| 項目 | 内容 |
|---|---|
| 手順 | **before/after 並走**。`git archive HEAD~1` を `tmp/dev-G-3b/before/` へ展開し、**そのツリーの `serve.mjs`** を **3011** で、現ツリーを **3010** で起動（⚠️ `cd` では配信ルートが切り替わらないため） |
| ⭐ **取り違えの検算** | 本測定の前に**既知の差**で検算した: `curl .../tools/deck-builder/style.css \| grep -c "max-width: 1199px"` が **3010 → 1 / 3011 → 0**。⭐ **before と after が同一になる偽の緑ではない**ことを先に確定させた |
| 到達手段 | ⚠️ Discord ログインが前提のため **srcdoc ハーネス**（`tmp/review/G-2-境界1200/harness.html` を流用）。`/api/me` と `/api/decks/<id>` だけをスタブし、**カード・絵柄・チップ・`renderZones()` は本物** |
| ブラウザ | Playwright Chromium（`--ignore-certificate-errors` + `ignoreHTTPSErrors`） |
| 証跡 | `tmp/dev-G-3b/`（`geom-before.json` / `geom-after.json` / `diff.txt` / `func-*.json` / `extra.json` / `v50-table.txt` / スクショ5枚） |

### V50 — デッキ構築の折りたたみ（G-3b）★本単位の本体

| 小項目 | 期待値 | 実測 | 判定 |
|---|---|---|---|
| **V50-a** 900 / 1199px でトグルが出る | `.filter-toggle` が表示される | **900px: `display: none` → `block`**、矩形 `852×49.59`、`elementFromPoint` が **`s-toggle` 自身**を返す。**1199px も同じ**（`block`・押せる位置にある）。⭐ **`display` だけを根拠にしていない** | ✅ |
| **V50-b** `.filter-row` が畳まれる | 既定で非表示 | **900 / 1199px とも `grid`（高さ 144.2 / 104px） → `none`（高さ 0）** | ✅ |
| **V50-c** トグルで開閉できる | 押すと開き、もう一度押すと閉じる | **900px**: 閉 `aria-expanded=false` / `row=none` / ラベル `絞り込み ▾` → **押下 → 開** `aria-expanded=true` / `row=flex` / 高さ **409.8px** / ラベル `絞り込み ▲` → **再押下 → 閉**（初期状態へ完全復帰）。**1199px も同一**。⚠️ **before では2回とも `NOT_CLICKABLE`（`display:none` でクリック不能）** | ✅ |
| **V50-d** 選択件数バッジが出る | バッジが出る | 900px でチップを2つ（`NORM` / `FIRE`）選択 → `#s-filter-badge` は **`hidden=false` / テキスト `"2"` / 矩形 `24×16`**、`elementFromPoint` が **`s-filter-badge` 自身**を返す。スクショ `shot-after-900-open.png` に「絞り込み ▲ ②」が写っている | ✅ |
| **V50-e** ⭐ **純増として測る** | 変更前は 900 / 1199 とも `display:none` | **before 実測: 900px `none` / 1199px `none`**（§14-6 の「出発点」と一致）→ after で `block`。⭐ **純増であることを before 実測で確認済み** | ✅ |
| **V50-f** ⚠️ **`PANE_MIN_WIDTH`(1200) と `max-width:1199px` が重ならない** | 1200px でトグルが出ない／1199px で左ペインが出ない | **下の専用表**（全数一致） | ✅ |
| **V50-g** ⚠️ 寸法系が 900 / 1199px で G-3b の前と一致 | `.artgrid` 列数・入力欄 font-size・モーダルの角丸／最大幅 | **下の専用表**（全部一致） | ✅ |

#### V50-f — ⭐ **境界の非重複（18幅スイープ・すべて after 実測）**

| 幅 | トグル（display / 実際に見えるか） | `#view-editor` | `#s-filter-row` | 横あふれ |
|---|---|---|---|---|
| 375 / 480 / 620 / 640 / 641 / 700 / 768 / 860 / 861 / 900 / 1000 / 1024 / 1100 / **1199** | **`block` / true** | **1カラム**（`block`） | `none`（畳まれている） | **0** |
| **1200** / 1201 / 1280 / 1440 | **`none` / false** | **左ペイン**（`grid` / `320px 880px`） | `grid` | **0** |

⭐ **どの幅でも「トグル」か「左ペイン」の片方だけ**が成立する。**重なりも空白もない**。
境界は **1199 ↔ 1200 のちょうど1px**で、`PANE_MIN_WIDTH = 1200`（JS・未変更）と過不足なく接している。
往復（1199 → 1200 → 1199）でも同じ状態に戻る。

#### V50-g — ⭐ **寸法系が漏れていないこと（before / after 全一致）**

| 幅 | `.artgrid` 列数 | `#s-name` font-size | `.modal-body` 角丸 | `.modal-body` max-width | `.modal-body` 実寸 | `.gtile .ctrl-strip` opacity |
|---|---|---|---|---|---|---|
| **375** | 2 / **2** | 16px / **16px** | 0px / **0px** | 100% / **100%** | 375×900 / **375×900** | 1 / **1** |
| **640** | 2 / **2** | 16px / **16px** | 0px / **0px** | 100% / **100%** | 640×900 / **640×900** | 1 / **1** |
| **860** | 2 / **2** | 16px / **16px** | 0px / **0px** | 100% / **100%** | 860×900 / **860×900** | 1 / **1** |
| **861** | 3 / **3** | 14px / **14px** | 16px / **16px** | 860px / **860px** | 860×106.88 / **860×106.88** | 0 / **0** |
| **900** | 3 / **3** | 14px / **14px** | 16px / **16px** | 860px / **860px** | 860×106.88 / **860×106.88** | 0 / **0** |
| **1024** | 3 / **3** | 14px / **14px** | 16px / **16px** | 860px / **860px** | 860×106.88 / **860×106.88** | 0 / **0** |
| **1199** | 3 / **3** | 14px / **14px** | **16px / 16px** | **860px / 860px** | **860×106.88 / 860×106.88** | 0 / **0** |
| **1200** | 3 / **3** | 14px / **14px** | 16px / **16px** | 860px / **860px** | 860×106.88 / **860×106.88** | 0 / **0** |
| **1280** | 3 / **3** | 14px / **14px** | 16px / **16px** | 860px / **860px** | 860×106.88 / **860×106.88** | 0 / **0** |

⚠️ ⭐ **設計 §14-6 の必須条件1が警告した「1199px でモーダルが 1199×800・角丸0」は起きていない**
（1199px でも **860×106.88 / 角丸 16px / max-width 860px**）。`.artgrid` も 1199px で **3列**のまま。
`.gtile .ctrl-strip` の常時表示（860px の項目）も 861px 以上へ漏れていない。

### V48 相当 — ⭐ **640px以下・620/860 の既存境界が1pxも変わらないこと**

`geom.mjs` で **12幅 × 30セレクタ × 34プロパティ + 矩形**を before/after で採り、**全キー再帰比較**した結果:

| 幅 | 差分件数 | 意味 |
|---|---|---|
| **375** | **0** | ✅ スマホ据え置き |
| **620** | **0** | ✅ 620px 境界（`.deck-tabs` / `.stats-pane` / `.statrow`）据え置き |
| **640** | **0** | ✅ 640px 境界（`.list-controls` / `.editor-bar`）据え置き |
| **641** | **0** | ✅ |
| **768** | **0** | ✅ |
| **860** | **0** | ✅ **860px 境界の内側は完全に同一** |
| **861** | 88 | ⭐ **ここから新しい帯**（意図どおり） |
| **900** | 91 | ⭐ 同上 |
| **1024** | 89 | ⭐ 同上 |
| **1199** | 88 | ⭐ 同上 |
| **1200** | **0** | ✅ **左ペイン帯に一切影響していない** |
| **1280** | **0** | ✅ |

⭐ **差分が出るのは 861〜1199px の帯だけ**で、その内訳も
`.filter-toggle` / `#s-filter-row` / `#s-filter-row .fgroup` / `#s-filter-row select` / `#s-set`（＝`.filter-row` 内のセレクト）と、
**それに伴う `html` / `body` / `#view-editor` / `#search-top` の高さと、下に続く `.deck-pane` / `.gtile` の `top`** だけ。
**`.artgrid` / `.modal-body` / `.modal-close` / `#result-grid` / `#art-grid` / `#view-login` / `#view-decks` / `#view-deck` / `.gtile .ctrl-strip` は1件も差分が無い。**

### V43 相当 — 中間の形（上部パネル）がどの幅にも出ないこと

上の18幅スイープのとおり、**641 / 768 / 900 / 1024 / 1199 を含む全幅で「スマホ表示」か「左ペイン表示」のどちらかに落ちる**。
⚠️ **before は 861〜1199px が「上部パネル（全幅グリッド）」だった**（`#s-filter-row` が `grid` で常時展開）——これが本単位で消えた。

### V52 相当 — 他3ビュー

`#view-login` / `#view-decks` / `#view-deck` は **全12幅で差分0**（上の再帰比較に含まれている）。
⚠️ **制約**: 3ビューはハーネスでは `hidden` のままなので**矩形は 0 で採れており、「計算値が同一」までしか言えない**（→ §3）。

### M1-b — 帯をまたいだときに選択が消えていないこと

1199px でチップ2つ（`NORM` / `FIRE`）＋エキスパンションのセレクトを選び、**1199 → 1200 → 1199** と往復:

| 状態 | 選択チップ | セレクト | バッジ | トグル | `#view-editor` | `#s-selected-filters` |
|---|---|---|---|---|---|---|
| 1199px（選択直後） | `["NORM","FIRE"]` | `"0"` | `"2"`（表示） | `block` | 1カラム | `none` |
| **1200px（左ペインへ）** | **`["NORM","FIRE"]`** | **`"0"`** | **`"2"`** | `none` | 左ペイン | **`block`・矩形 `287×107.3`** |
| **1199px（戻り）** | **`["NORM","FIRE"]`** | **`"0"`** | **`"2"`** | `block` | 1カラム | `none` |

✅ **チップの選択・セレクト・バッジの件数がすべて保たれる。**
⭐ 併せて **V53 の性質（`#s-selected-filters` が 1200px で出て 1199px で出ない）が壊れていない**ことも同じ往復で確認した。

### 追加検証 — ⭐ 破壊試験（新ブロックが実際に効いていることの証明）

900px で **CSSOM から `@media (max-width: 1199px)` のルール5本を実行時に削除**した:

| 項目 | 削除前 | **削除後** |
|---|---|---|
| `#s-toggle` の `display` | `block` | ⚠️ **`none`（トグルが消える）** |
| `#s-filter-row` の `display` | `none`（畳まれている） | ⚠️ **`grid`（元の上部パネルに戻る）** |

⭐ **「動いたから合格」ではなく、対策を外すと壊れることまで確認した**（`shot-after-900-breaktest.png`）。
削除できたルールは **5本**（＝新設ブロックの中身と一致）。

### 例外・エラー

**全測定（12幅 × 2ツリー + 機能4本 + スイープ + 破壊試験）で `pageerror` は 0 件。**
横あふれ（`scrollWidth - clientWidth`）も**全18幅で 0**。

### `npm run validate`

**exit 0 / `all checks passed.`**（`no line-number refs in code comments — 0件（46ファイル走査）`・
`:root owners in sync — 4ファイル`・`shared css tokens in sync — 68 トークン参照 / 未定義 0` を含む）

---

## 3. 実行できなかった検証と代替手段・残っている確認

| # | 内容 | 代替 / 状態 |
|---|---|---|
| 1 | ⚠️ **実ログインでのデッキ構築** | srcdoc ハーネス（`/api/me` と `/api/decks/<id>` だけスタブ）で代替。**カード・絵柄・チップ157種・`renderZones()` は本物**。⚠️ **所持枚数バッジ・適合判定など「実アカウントのデータに依存する表示」は未検証**（本単位は絞り込みパネルの `display` しか触らないため影響は考えにくいが、断定はしない） |
| 2 | ⚠️ **`#view-login` / `#view-decks` / `#view-deck` の実描画** | 3ビューは `hidden` のままなので**矩形は 0 / 計算値のみの比較**。⚠️ **「実際にそのビューを開いた状態」での確認は未実施**（V52 を厳密にやるなら route を切り替える必要がある）。⭐ ただし変更は `.search-top` 配下のみで、3ビューはいずれも `.search-top` を含まない |
| 3 | ⚠️ **実機（iPad 等）での確認** | 未実施。⭐ 本単位では**入力欄の font-size を一切動かしていない**（1199px でも 14px のまま＝ §14-3 補足4 の方針どおり）ので、**G-3b の前後でズーム挙動は変わらない** |
| 4 | ⚠️ **本番での確認** | **未push**のため未実施 |
| 5 | ⭐ **`E-add-1`（閾値の自動検査）** | **本単位の次**なので**まだ無い**。⚠️ **`PANE_MIN_WIDTH` と 1199 の非重複は人が測った**（V50-f の18幅スイープ）。⭐ **`E-add-1` が入ったら自動検査へ引き継がれる** |

---

## 4. 差分が設計書の想定と食い違った点

**食い違いなし。** 設計 §14-3 の分割表・§14-6 の必須条件4点と、実測がすべて一致した。

⭐ 1点だけ**設計の予告より穏やかだった**ので記録する:

| 項目 | 設計の予告 | 実測 |
|---|---|---|
| `.filter-toggle` の font-size | （G-3a のトップ側では「16px になる予告が外れて 14px のままだった」＝ §14-3 補足1・2） | ⭐ **デッキ構築では 861〜1199px でも font-size に差分が出なかった**（before / after とも同じ値）。理由は **`.filter-toggle` が `.btn` クラスを持たない**ため `.search-top .btn { font-size: 16px }`（≤860）と競合せず、**基底の `button { font: inherit }` の値と新ルールの 16px が一致している**から。⭐ **つまりトップで問題になった「640px 境界でトグルだけ 14→16px に変わる」現象は、デッキ構築では起きない** |

---

## 5. 設計書からの逸脱と根拠

**逸脱なし。**

- 移したのは §14-3 の分割表「デッキ構築 / 1199px以下へ移す」列の**4項目ちょうど**。**足しても引いてもいない**
- ⚠️ **JS は1行も触っていない**（設計 §14-6 の必須条件3「CSS だけで完結するはず」のとおり）。
  `PANE_MIN_WIDTH = 1200` も `tools/deck-builder/app.js` も**未変更**。
  ⭐ **したがって `E-add-1` の検査③の対象は増えていない**
- ⭐ **既存表記との照合**: 新ブロックのコメントは既存の左ペイン化ブロックと同じ書式（`左ペイン化_設計 §…` 参照・⚠️/⭐ の使い分け・**行番号での参照はしない**）に揃えた
- ⚠️ **`TRANSLATION.md` は無関係**（訳データ・生成物に変更なし）。生成物の再生成も不要（CSSのみ）

---

## 6. 設計担当への確認事項

| # | 内容 | 判断が要るか |
|---|---|---|
| 1 | ⭐ **報告のみ**: §14-3 補足1・2 が指摘した「640px 境界でトグルだけ font-size が変わる」既存挙動は、**デッキ構築側には存在しない**（§4 の表）。⭐ **§12-9 の項目10（ユーザーに見てもらう）はトップだけの話**として扱ってよいか | ⭐ **設計判断**（ユーザーに見せる項目の範囲） |
| 2 | ⭐ **報告のみ**: `#view-login` / `#view-decks` / `#view-deck` は **`hidden` のままの計算値比較**しかできていない（§3 の2）。**V52 を「ビューを開いた状態で」やり直す必要があるか** | ⭐ **設計判断**（レビューで厳密にやるなら route 切り替えの手順が要る） |
| 3 | ⭐ **確認**: 本単位で **§8-1 の単位4 は完了**。次は **`E-add-1`（閾値の自動検査）** の理解でよいか（⚠️ **F より前**という §8-1 の着手順） | ⭐ **設計判断** |

⚠️ **仕様に関わる判断は一切していない。** 迷った点（トグルの font-size をトップと揃えるか等）は
**実装せずこの表に上げている**。

---

## 付録: 再現手順

```bash
# before ツリーを展開して別ポートで並走させる（⚠️ cd では配信ルートが切り替わらない）
mkdir -p tmp/dev-G-3b/before && git archive 5312e104^ | tar -x -C tmp/dev-G-3b/before
cp tmp/review/G-2-境界1200/harness.html tmp/dev-G-3b/harness.html
mkdir -p tmp/dev-G-3b/before/tmp/dev-G-3b && cp tmp/dev-G-3b/harness.html tmp/dev-G-3b/before/tmp/dev-G-3b/

PORT=3010 node scripts/serve.mjs &                      # after（現ツリー）
(cd tmp/dev-G-3b/before && PORT=3011 node scripts/serve.mjs &)   # before

# ⭐ 本測定の前に「既知の差」で取り違えを検算する（3010=1 / 3011=0 でなければ中止）
curl -s http://127.0.0.1:3010/tools/deck-builder/style.css | grep -c "max-width: 1199px"
curl -s http://127.0.0.1:3011/tools/deck-builder/style.css | grep -c "max-width: 1199px"

node tmp/dev-G-3b/geom.mjs before > tmp/dev-G-3b/geom-before.json
node tmp/dev-G-3b/geom.mjs after  > tmp/dev-G-3b/geom-after.json
node tmp/dev-G-3b/diff.mjs        > tmp/dev-G-3b/diff.txt
node tmp/dev-G-3b/func.mjs before > tmp/dev-G-3b/func-before.json
node tmp/dev-G-3b/func.mjs after  > tmp/dev-G-3b/func-after.json
node tmp/dev-G-3b/extra.mjs       > tmp/dev-G-3b/extra.json
```

⚠️ **サーバーを止めるときは待受ポートから PID を引く**（`pkill -f "scripts/serve.mjs"` は
他セッションのサーバーと自分のシェルまで巻き添えにする）:

```bash
PID=$(ss -ltnp 2>/dev/null | grep ':3010' | grep -oP 'pid=\K[0-9]+' | head -1); [ -n "$PID" ] && kill "$PID"
```
