**対象: 「ハイライト検査の無言緑」** — この宣言は 2026-09-11 に遡って追記した（索引の転記ドリフト防止）

# 実装報告 — 用語ハイライト検査が「節を1つも走査できないまま緑」になるのを止める

⚠️ **これは issueコメントの代替**（GitHubアカウント停止中で `gh issue comment` が打てないため、
ファイルに残す）。復旧して採番されたら、次のコマンドで**そのまま投函する**:

```bash
gh issue comment <n> --body-file docs/design/未採番-ハイライト検査の無言緑/実装報告_2026-09-02.md
```

- 日付: **2026-09-02**
- 実装コミット: **`63856769`**（親 `f4d8ef60`）
- push: ❌ **していない**（⚠️ `CLAUDE.md` を含むため `docs/` だけの push 例外に乗らない。設計書 §6-4）
- 設計書: `docs/design/未採番-ハイライト検査の無言緑/ハイライト検査の無言緑_設計.md`
- 次の担当: **レビュー担当**（ラベル相当: **`レビュー待ち`**）

---

## 1. 実施内容

### 1-1. コミット

| コミット | 内容 | 変更ファイル |
|---|---|---|
| **`63856769`** | 用語ハイライト検査に不変条件 (a)(b)(c) を足す ＋ `CLAUDE.md` の記述更新 | **`scripts/validate.mjs`** / **`CLAUDE.md`** の**2ファイルのみ** |

⭐ **担当パス外の混入なし**（`git -c core.quotepath=false show --name-only --format= 63856769` の出力が
上記2ファイルだけであることを確認）。⭐ **生成物は0ファイル**（V12）。

### 1-2. 変更の中身（設計書 §3-0 の5箇所にそのまま対応）

| # | 変更 | 実装 |
|:--:|---|---|
| ① | `SECTION` の直後に見出し計数用の正規表現 | `const JP_H2 = /<h2>効果（日本語）<\/h2>/g;`（⚠️ **`SECTION` から切り出さず別に書いた**。同源だと常に一致して検査自体が新しい無言の緑になるため、その旨をコメントに残した） |
| ② | `scan()` の戻り値 | `[...hits]` → **`{ hits: [...hits], sections }`**。ハイライト判定のロジック（`visibleText` / `cores` の突き合わせ）は**1文字も変えていない** |
| ③ | ワーカー内（`pages++` の直後） | `h`（見出し数）と `sections` を数え、**(a)** `s === 0` → `noSection` / **(b)** `else if (s !== h)` → `partial` に積む。⚠️ **`else if`**（D5）。`sections += s` の右辺は `await` を含まない同期値なので既存コメントの `+=` の罠は踏まない |
| ④ | 集計と出力 | **(c)→(a)→(b)→既存(missed)** の順に `bad` へ積む（⚠️ **`else if` で連ねない**＝併発を抑制しない）。成功行を `` `card term highlights up to date — ${pages} ページ / ${sections} 節` `` に |
| ⑤ | 誘導行 | 3本目を **`if (noSection.length || partial.length)`** の条件付きで追加（D6）。既存2本は不変 |

- ⚠️ **`noSection` / `partial` も `missed` と同じく `sort()` した**（並列8で読むため順序が不定）
- ⚠️ **期待値のハードコードは一切していない**（D7）。(a)(c) は **0との比較**、(b) は
  **同じHTMLを2通りに数えた値どうしの突き合わせ**のみ

### 1-3. `CLAUDE.md`（設計書 §4-3 の文面をそのまま適用）

1. 検出経路の表の成功行を `card term highlights up to date — 2495 ページ / **2517 節**` に更新
2. 「⚠️ ⭐ **検査自身に穴がある**」の箇条書きを、**塞いだ旨＋3つの不変条件＋保守制約＋残る限界**の
   記述（設計書 §4-3 (2)）に置き換え

---

## 2. 検証結果（設計書 §5 の **V0〜V13** に対応）

⭐ **全14項目とも設計書の期待値に一致**（文面・エラー行数・例示スラッグ・誘導行の本数まで）。
⚠️ 破壊試験は §5-0 の手順どおり**1つずつ**行い、**次に進む前に必ず復元**した。

| # | 結果 | 実測 |
|---|:--:|---|
| **V0**<br>（着手前） | ⭕ | ⭐ **実装前のコードで V2 の壊し方をして `exit 0` / `card term highlights up to date — 2495 ページ`**。**症状が現に存在することを実装前に確認**（節が取れず、同時に `term-hl` を剥がしたのに緑） |
| **V1** | ⭕ | `exit 0`。末尾は設計書の順・文面どおり:<br>`cron git add paths in sync — 8パス（README.md / CLAUDE.md）`<br>`issue drafts in sync — 起票案11件（生きている6 / 完了4 / 取り下げ1）`<br>**`card term highlights up to date — 2495 ページ / 2517 節`**<br>（空行）`all checks passed.` |
| **V2**<br>⭐主症状 | ⭕ | `exit 1`。エラー行**ちょうど2行**: `- 効果（日本語）の節を1つも走査できないページ: 1ページ` / `-   例: abnegation`。**誘導行3本**（3本目が出る）。⚠️ **「ハイライトされていない用語」は出ない** |
| **V3** | ⭕ | `exit 1`。**2行**・V2 と同一文面（`例: abnegation`） |
| **V4** | ⭕ | `exit 1`。**2行**: `2495ページ` / `例: abnegation / absolving-flames / academy-attendant / academy-guide / accelerate`（**例は5件まで**） |
| **V5**<br>⭐(b) | ⭕ | `exit 1`。**2行**: `効果（日本語）の見出しの数と走査できた節の数が違うページ: 1ページ` / `例: beseeched-fatestone（節1 / 見出し2）`。⚠️ **(a) は出ない**（D5 のとおり） |
| **V6**<br>⭐(c) | ⭕ | `cores` を一時的に空にして `exit 1`。エラー行**ちょうど1行**: `用語辞書から中核語を1つも取り出せません（data/translations.js の terms を確認してください）`。**誘導行2本**（3本目は出ない）。⚠️ **「0件だから全部一致」で緑にならない** |
| **V7** | ⭕ | `exit 1`。エラー行**ちょうど4行**をこの順で: 節0(1ページ)→`例: abnegation`→用語(1ページ)→`例: absolving-flames（クラスボーナス）`。**抑制していない** |
| **V8** | ⭕ | `exit 1`。**2行**: `日本語効果文にあるのにハイライトされていない用語: 1ページ` / `例: abnegation（ネゲート）`。⚠️ ⭐ **誘導行は2本のまま**（3本目が**出ない**＝D6・既存の見た目が1文字も変わらない） |
| **V9** | ⭕ | 変更前生成物（`93c2348c` の `cards/`・2,498ディレクトリ）に対して `exit 1`。**2行**: `139ページ` / `例: aenean-frozen-shunt（マテリアライズ） / anathemas-end（ヒット時） / andronika-eternal-herald（リザーブ） / aphotic-ruin（レベル） / apostle-of-the-woods（レベル）`。⭐ **(a)(b) のエラーは0**＝**新検査が過去の生成物を false red にしない** |
| **V10** | ⭕ | ⭐ **実装前後を同一セッションで測って比較**（下記 2-1）。増分は**計測誤差内** |
| **V11** | ⭕ | `git status --short` が **`M CLAUDE.md` と `M scripts/validate.mjs` の2件だけ**（`cards/` の差分**0件**）。`npm run validate` は **exit 0 で V1 と完全に同一の出力** |
| **V12** | ⭕ | `git status --porcelain cards sets sitemap.xml data index.html` が**空**。`git diff --stat` は `CLAUDE.md`(+20/-6 相当) と `scripts/validate.mjs` の**2ファイルのみ** |
| **V13** | ⭕ | 設計書指定の3本の `grep -c` が **1 / 1 / 0**（設計時点 0 / 1 / 1）。⭐ **2本目が 1 のまま**＝記述を増やしていない。参考: `README.md` の `card term highlights` は **0件**（設計書 §4 の想定どおり不変） |

### 2-1. V10 の詳細（実行時間）

⚠️ 最初の3回計測で **2664 / 2173 / 2126 ms** と1回目が設計書の帯（2.0〜2.6秒）をわずかに超えたため、
**原因を切り分けた**。直前に V9 で 2,498ファイルを `tar` 展開しており、**1回目だけページキャッシュが冷えていた**。
⭐ **実装前後を同一条件で並べて測り直した**:

| | run1 | run2 | run3 | run4 |
|---|---|---|---|---|
| **実装前**（`git show HEAD:scripts/validate.mjs`） | 2114 | 2116 | 2118 | 2180 ms |
| **実装後** | 2154 | 2113 | 2102 | 2141 ms |

⭐ **増分は計測誤差内**（実装後の中央値はむしろわずかに小さい）＝設計書の想定どおり。
帯を外れた1回は**新カウンタではなく直前の大量I/Oが原因**と確定した。

### 2-2. 破壊試験の復元（§5-0 の手順どおり）

| 対象 | 復元方法 | 結果 |
|---|---|---|
| `cards/abnegation/index.html`（V0・V2・V3・V7・V8） | `cp` で退避 → 改変 → `cp` で復元 | ⭕ |
| `cards/beseeched-fatestone/index.html`（V5） | 同上 | ⭕ |
| `cards/absolving-flames/index.html`（V7） | 同上 | ⭕ |
| `cards/*/index.html` **全2,495件**（V4） | `git checkout -- cards/` | ⭕（直後に `git status --short` が1件＝`validate.mjs` のみ） |
| `scripts/validate.mjs` の一時改変（V6 の `cores`・V9 の `cardsDir`） | 実装版を `tmp/dev/…/validate.mjs.impl` に退避しておき `cp` で復元 | ⭕ |

⚠️ **V11 で全復元を確認済み**。作業物は `tmp/dev/ハイライト検査の無言緑/`（gitignore）に置いた。

---

## 3. 実行できなかった検証・残っている確認

⭐ **設計書 §5 の V0〜V13 は全項目実行できた**（未実施・代替手段で済ませた項目は**無い**）。

残るのは**push後の確認のみ**で、これは本タスクの検証対象外:

- ⚠️ `npm run validate` は**開発者のローカルでしか動かない**（⚠️ **cronは `validate` を実行しない**）。
  したがって **push後の本番での挙動確認は原理的に存在しない**（ブラウザの表示・生成物は0ファイル変更）
- ⭐ **Playwright 実駆動は不要と判断した**。本変更は**ビルド時検査スクリプトのみ**で、
  画面・生成物・Functions のいずれにも影響しない（V12 で裏取り済み）

---

## 4. 差分が設計書の想定と食い違った点

⭐ **なし。** 設計書 §5 の期待値（文面・行数・例示スラッグ・誘導行の本数）と**全項目一致**した。

参考までに、設計書と実測で**数字が異なるが想定内**のものを挙げる:

| 項目 | 設計書 | 実測 | 判断 |
|---|---|---|---|
| 設計時 HEAD | `d433777c` | 実装時の親は **`f4d8ef60`** | ⭕ **意図内**。`f4d8ef60` は設計書そのものを追加したコミットで、`scripts/` `cards/` は不変 |
| V9 の対象ディレクトリ数 | 記載なし | **2,498**（現在は 2,495） | ⭕ **意図内**。`93c2348c` 時点の生成物なので現在と件数が違うのは当然。⭐ **それでも (a)(b) が0件**だったことが重要（false red なし） |

---

## 5. 設計書からの逸脱と根拠

⭐ **逸脱なし。** 設計書 §3 の仕様どおりに実装した。以下は**設計書が明示的に禁じた案を採らなかった**ことの確認:

| 設計書が禁じた案 | 実装 |
|---|---|
| ❌ `2495` / `2517` / `134` を定数で書く（D7） | **書いていない**。(a)(c) は0との比較、(b) は2通りの計数の突き合わせ |
| ❌ `SECTION` を緩めて属性を許す（§3-6） | **緩めていない**。`SECTION` は**1文字も変えていない** |
| ❌ `SECTION` から見出しを切り出して (b) の右辺にする（§3-1） | **別の正規表現 `JP_H2` を書いた**（同源回避の理由をコード内コメントに明記） |
| ❌ (a) と (b) を同時に報告する（D5） | **`else if`** にした（V5 で (a) が出ないことを実測） |
| ❌ 3本目の誘導行を無条件に出す（D6） | **条件付き**にした（V8 で出ないことを実測） |
| ❌ 英語原文の節も数える / HTMLパーサ導入 / `sets/**` も対象 | **いずれもしていない** |

既存表記との照合:

- ⭐ **既存の失敗時の出力は1文字も変わっていない**（V8 の2行＋誘導行2本が変更前と同一）
- ⭐ 追加行は既存の書式に合わせた（`  - ` の箇条書き、`  例: ` の先頭は**半角スペース2つ**、
  誘導行のインデントは**半角スペース4つ**、1本目だけ `  → `）
- ⭐ 成功行は既存の `<名前> up to date — <数>` の形（`generated JSON up to date` 等）を踏襲
- ⚠️ `TRANSLATION.md` は本変更と無関係（訳データ・用語辞書の**中身は変更していない**）

---

## 6. 設計担当への確認事項

⭐ **判断が必要になった項目は無い。** 設計書は「実装をブロックする未決事項なし」で、実際にそのとおりだった。

申し送りとして2点だけ記録する（**いずれも設計書に既出で、判断を求めるものではない**）:

1. ⭐ **本タスクは保守制約を意図的に持ち込んだ**（設計書 §1-2・§6-2）。
   今後 `scripts/build-card-pages.mjs` の `effectSections()` を変えるコミットは、
   **同じコミットで `scripts/validate.mjs` の `SECTION` を直さないと `validate` が赤くなる**。
   ⚠️ **「うるさいから緩める」方向の変更が後から入らないよう**、`CLAUDE.md` にもその旨を書いた
2. ⚠️ **承認時の作業（設計書 §6-2）は実装コミットに含めていない**（D10）。
   起票案の状態行 `生きている` → `完了` と、`docs/design/待ち行列/待ち行列と復旧手順.md` の
   件数見出し（**生きている 6→5 / 完了 4→5**）は**設計担当が承認時に同じコミットで**直す。
   ⚠️ **片方だけ直すと `issue drafts in sync` 検査が exit 1 になる**（＝正しい動作）。
   ⭐ その結果 **V1 の期待行が `…（生きている5 / 完了5 / 取り下げ1）` に変わる**点も設計書 §6-2 のとおり

---

## 7. 次の担当

**レビュー担当**（ラベル相当: **`レビュー待ち`**）

- 対象コミット: **`63856769`**
- ⚠️ **未pushのまま**（`CLAUDE.md` を含むため `docs/` の push 例外に乗らない）。
  レビュー・承認まで済んだら **`push待ち` 相当**として止め、**本番公開の可否はユーザーが判断する**
- ⭐ レビューで再現しやすい2点:
  - **V0**（実装前のコードでの無言の緑）は `git stash` 等を使わずとも
    `git show 63856769^:scripts/validate.mjs` を一時ファイルに出して確認できる
    （⚠️ **レビュー担当は作業ツリーを変更しない**規約があるため、`cards/` を壊す破壊試験を行う場合は
    `cp` 退避→復元を徹底し、`git checkout` は使わないこと）
  - **V13** は⚠️ **新しい成功行が古い成功行を文字列として含む**ため、素の `grep -c` では増減しない。
    設計書 §5 の3本の grep（期待値 **1 / 1 / 0**）で見ること
