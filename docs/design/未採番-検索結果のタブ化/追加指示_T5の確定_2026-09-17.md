**対象: 「検索結果のタブ化」**

# 追加指示 — T5（最後の実装単位）の確定と、T4 の S 指摘の裁定（2026-09-17）

⚠️ **これは issueコメントの代替である。** GitHubアカウント停止中で `gh issue comment` が打てないため、ここに書く。
復旧して採番されたら、本文をそのまま
`gh issue comment <n> --body-file docs/design/未採番-検索結果のタブ化/追加指示_T5の確定_2026-09-17.md` で投函する。

| | |
|---|---|
| 受けた工程 | **T4＝合格・差し戻し0回**（`55402b29`。実装は `f3c4e8a5` / `533ff220`） |
| 設計書 | ⭐ **[`検索結果のタブ化_設計.md`](検索結果のタブ化_設計.md) を 1.2 → 1.3 に改版した**（この指示の正は設計書側） |
| 裁定の対象 | レビュー判定 T4 の **S-1〜S-5** ／ 実装報告 T4 §6 の**4件** |
| 状態 | **実装中**のまま（起票案の1行目は変えていない）。⚠️ **派生起票案を1件足したので索引は再生成した** |
| 次の担当 | **開発担当**（ラベル相当 **`実装待ち`**）——⭐ **単位 T5（本タスク最後の実装）** |

⚠️ **レビューの判定（合格）は覆していない。** ⚠️ **承認はしない**（最終承認はユーザー）。

---

## 1. 裁定の一覧

| # | 論点 | ⭐ 決定 | 理由 |
|---|---|---|---|
| **S-1** | ⚠️ `app.js` に **消したはずの `:nth-child(n+7)` を指すコメントが2行残っている** | ⭐ **T5 の作業に含める（必須）**。設計書 **§8-4-3-1**・検証 **V17-e** | ① ⚠️ ⭐ **§8-4-3 が明示的に禁じた「数値指定への書き戻し」を誘発する**——**読んだ人は「CSS 側に `n+7` があるのに見当たらない＝追随できていない」と読む** ② ⭐ **`style.css` 側からは語が消えているので、片側だけが古い**＝**CLAUDE.md が繰り返し警告する「写しが増えると、検査の無い側から黙って古くなる」型そのもの** ③ **コメント2行なので安い** |
| **S-2** | ⭐ **検索タブ表示中はデッキ・統計の面も `hidden`**（＝**見えている面は常に1つ**）という T4 の実装判断 | ⭐ **追認し、§8-4-1 に6項目目として明文化した**。検証 **V18-c** | ① ⭐ **ARIA の tabs パターンから出る**（§8-4-1 の4「固定タブ表示中はパネルを `hidden`」の逆向きで、**同じ規則**） ② ⚠️ ⭐ **2面が同時に立つとページが `min-height: 100vh` の2つ分に伸び、§8-7 の送り先がずれる** ③ ⭐ **レビューが副作用を実測済み**（統計 → 検索 → 投入 → 統計で**中身が更新される**＝鮮度は落ちない）。⚠️ ⭐ **明文化しないと T5 で「整理のつもりで」戻される**（1.1 §11-6 と同じ型） |
| **S-3** ＋ 実装報告 §6 の2 | **並び替えでもページがタブ帯まで飛ぶ**（375px で `scrollY 0 → 677`）／**`behavior: "smooth"` にするか** | ⭐ **T5 では変えない。§10-5 の7としてユーザーの実物確認に回す**（⭐ **感触と `smooth` の要否をまとめて聞く**） | ⭐ **設計違反ではない**（§2-3 で並び替えは検索が走る＝§8-7 の「検索のたびに送る」に素直に当てはまる）。⚠️ **「飛ぶのが嫌か」は実物でしか分からない**。⭐ **直すとしても1行**なので、**聞いてから決めるほうが安い**。⚠️ **即時のままのほうが検証は決定的**（`smooth` だと送り先の実測が待ち時間に依存する） |
| **S-4** ＋ 実装報告 §6 の1 | 左ペインの「選択中の条件」の背景が**アクセント色の直値**のまま | ⭐ **本件では直さず、派生起票案にした**（[`派生起票案_2026-09-17_アクセント色の直値.md`](派生起票案_2026-09-17_アクセント色の直値.md)・呼び名「**アクセント色の直値**」）。設計書 **§11-10** | ⚠️ ⭐ **起票前に実測したら、範囲は報告の「デッキ構築の2か所」ではなく `rgba(217,164,65,…)` が 4ファイル10か所**だった（トップ2 / `shared/css/filter-chips.css` 2 / デッキ構築2 / `tournaments/tournaments.css` 4）＝ ⚠️ **`shared/` と別ページに跨るので V21 の範囲外**で、T5 に混ぜられない。⭐ **T4 で箱だけ追従にしたので、同じ画面に「追従する箱」と「追従しない選択中の条件」が並んでいる**ことも書いた |
| **S-5** | 持ち主タブを × した後 `#result-cond-title` の文字列だけ残る（箱ごと `hidden`） | ⭐ **直さない**（記録として §11 に残す必要も無いと判断）。⚠️ **T5 の作業に入れない** | ⭐ **画面に出ず、次に表示するときは必ず書き直される**。⭐ **V17-b の「`hidden` に頼らない」は `.cond-more` の文言についての裁定**（**出る可能性のある文言**が対象）で、**ここまでは求めていない**——レビューの判断に同意する |
| 実装報告 §6 の4 | **T5 で `min-height: 100vh` の再確認が要るか** | ⭐ **不要**（⚠️ **T5 は §8-2 に触らないので、空パネルの高さの前提は変わらない**）。⭐ **ただし V14-b で「送り先がずれないこと」は1回測る** | ⭐ **§8-2 を「何もしない」と確定したので、この心配の前提が消えた**（下の「T5 に残る作業」） |

---

## 2. ⭐ T5 に残る作業（⚠️ **点検した結果、実質1つしか無い**）

⚠️ ⭐ **1.0 で T5 に置いた3つのうち、2つは既に成立している。**
**無い作業を指示すると dev が推測で触る**ので、**「何もしない」と明記した**（設計書 §8-2・§8-3 の節）:

| 節 | 1.0 の想定 | ⭐ 1.3 の確定 |
|---|---|---|
| **§8-1** `preferredArtIndex()` の引数化 | 左ペインを「いま」読むので、続き・裏のタブの絵柄が狂う | ⭐ **残っている＝T5 の本体**（V22） |
| **§8-2** 件数の描画先の引数化 | `el.resultGrid.childElementCount` を読んでいる | ⭐ **何もしない**——**`#result-grid` に入っているのは常に表示中のタブのタイルだけ**（裏は holder へ退避）＝**定義上そのタブの件数**。**T3・T4 のレビューが V25・V25-b で実測済み** |
| **§8-3** `updateElementWarn()` の呼び分け | 表示中のタブのときだけ呼ぶ | ⭐ **何もしない**——**`onResults` の `mine()` ガードの内側から呼んでいる**＝要求そのもの |

⚠️ ⭐ **`mine()` が「裏のタブに届いた結果を捨てて `loaded=false` に戻す」挙動は仕様として確定した**
（§11-6 ③の「引数化したら捨てる必要が無くなる」は**引数化しないので発生しない**）。
**代償はタブ切り替えの競合時に1回余分に取りに行くことだけ**で、**DOM 外の holder へ描くほうが §5-1-1 と衝突して危険**。

### ⭐ 次に開発担当がやること（正は設計書 **§9-3**）

| # | やること | 設計書 | 検証 |
|---|---|---|---|
| **1** | ⭐ **`preferredArtIndex()` をそのタブの条件（prefix・レアリティ）で決める**（**`makeSearchCtl(tab)` の `onResults` から `tab.cond` を渡す**。⚠️ **左ペイン（`el.sSet` / `el.sGRarity`）を読む経路を1つも残さない**） | **§8-1・§8-1-1** | ⭐ **V22**（⚠️ **期待値は §10-3**） |
| **2** | ⚠️ **`app.js` の `:nth-child(n+7)` のコメント2行を消す**（⚠️ **`COND_FOLD_AT` / `COND_FOLD_SHOW` の値と畳みの境界は変えない**） | **§8-4-3-1** | **V17-e** |
| **3** | ⚠️ **§8-2・§8-3 は触らず、確認だけ** | §8-2・§8-3 の節 | **V14-b・V25-c** |

⭐ **報告に必ず書くこと**:

- ⭐ **V22 の4つ全部**（①もっと見るの続きが全部 `PRD` ②タブを行き来してからの続き ③**レアリティ**でも追従 ④絞り込み無しは先頭のまま）。
  ⚠️ ⭐ **①は「T4 時点では `PRD` と `PRD 1st` が混在する」ことが実測済み**なので、**直った差が出るはず**。**出なければ直っていない。**
- **V17-e**: ⭐ **`grep -rn 'nth-child' tools/deck-builder/` が 0件**（⚠️ **フォルダ全体**。2026-09-17 時点ではコメント1件がヒットする）。
- **V14-b・V25-c**: ⭐ **「何もしない」ことの確認**（⚠️ **おかしければコードを触らず報告して止まる**＝仕様判断は設計担当）。
- **V7-c**（🎨 の選択が往復で戻らない）を**再測する**——⚠️ **絵柄の決め方を触る単位なので、ここが一番壊しやすい**。
- **V19**: ⚠️ ⭐ **before/after は `rj-v19b.mjs` の形で**（`v19.mjs` は現行 Chrome で走査0件＝空回りする。§10-1）。

⚠️ **やらないこと**: **トップの `app.js` の `preferredArtIndex()`（二重定義の解消は別タスク＝§11-2）／`shared/`／`scripts/`**（**触ると V21 で不合格**）／
**S-4 の色の直値**（⭐ 派生起票案へ）／**`behavior: "smooth"`**（§10-5 の7で聞いてから）／**push**（§11-8）。

---

## 3. ⭐ T5 の後に残るもの（⚠️ **実装はもう無い**）

1. ⭐ **ユーザーの実物確認**（§10-5。⚠️ ⭐ **1.3 で全面更新した**——**1.0 の記述はモーダル前提だった**）。
   - ⚠️ ⭐ **上げ方は `npx wrangler pages dev . --local --port 3000`**（**3000番以外はユーザーのブラウザに届かない**）。
     ⭐ **実ログインできる**（`.dev.vars` と Discord 側の `http://localhost:3000/...` 登録は済んでいる）。⭐ **`--local` なので本番D1に触らない。**
   - 見てもらうのは **8項目**（タブの体感・✕の食い違い・375px の横送り・復元の待ち時間・**実ログインでの保存**・**2タブの枚数一致**・
     ⭐ **並び替えで飛ぶ感触と `smooth` の要否**・⭐ **3面の行き来と統計の鮮度**）。
   - ⚠️ ⭐ **ここで新しい症状が出たら「継続」で受けて設計へ戻す**（描画・スクロール系は複数回になりうる）。
2. **ユーザーの承認**（⚠️ **設計担当は承認しない**）。
3. ⭐ **公開**（§11-8・**2026-09-17 ユーザー決定**: **タブ化の完成後に、大会データの週1更新と同時**）。
   ⚠️ **公開の可否を聞くときは「一緒に出るもの」を必ず提示する**（`git diff --stat <本番位置>..HEAD -- . ':(exclude)docs/*'`）。

---

## 4. 設計書のどこを見ればよいか（⭐ **1.3 の変更点**）

| 節 | 何が変わったか |
|---|---|
| **改版履歴** | 1.3 の行 |
| ⭐ **§8-1・§8-1-1・§8-1-2** | **T5 の本体を「実装できる手順」に**（引数の渡し方・⚠️ **prefix のまま保存されている点**・⚠️ **レアリティを落とさない**・⚠️ **トップには触らない**） |
| ⭐ **§8-2・§8-3 の節（書き換え）** | ⚠️ ⭐ **「T5 では何もしない」と確定**（なぜ不要か・代わりに何を確認するか） |
| **§8-4-1** | **6項目目＝面は常に1つ**（S-2 の追認） |
| **§8-4-3-1（新設）** | **取り残されたコメント2行**（S-1） |
| **§9 の単位表・§9-3（新設）** | **T5 の中身3点と「やらないこと」** |
| **§10-2** | **V14-b・V17-e・V18-c・V25-c を追加**（⚠️ ⭐ **既存番号は動かしていない**） |
| ⭐ **§10-3 V22** | ⭐ **期待値を画面に出る最終形で具体化**（4つとも合格が要る） |
| ⭐ **§10-5（全面更新）** | **上げ方（3000番・実ログイン）と、見てもらう8項目** |
| **§11-10・§11-11（新設）** | **S-4 は派生起票案へ**／**T4 が作った3つの合流点を崩さない** |

---

## 5. ⚠️ ユーザーへの確認事項

- **裁定の中には無い**（⭐ **ユーザー決定（§2）も公開方針（§11-8）も1つも覆していない**）。
- ⭐ **ただし §10-5 の7は「ユーザーに聞く」項目そのもの**（**並び替えでページが飛ぶ感触・`behavior: "smooth"` にするか**）。
  ⚠️ **T5 の完了後・実物確認のときにまとめて聞く**（**いま先に聞く必要は無い**）。

---

**次の担当: 開発担当（ラベル相当 `実装待ち`）**
