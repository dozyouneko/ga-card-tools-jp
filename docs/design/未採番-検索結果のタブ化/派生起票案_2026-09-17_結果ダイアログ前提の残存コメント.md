**状態: 生きている**（最終確認 2026-09-21）— 呼び名「結果ダイアログ前提の残存コメント」。⭐ **設計書はまだ無い**＝ハンドオフラベル（`実装待ち`）は付けない。ラベル（`enhancement` / `着手可` / `小タスク`）／ 索引: [`待ち行列と復旧手順.md`](../待ち行列/待ち行列と復旧手順.md) — ✅ **#106 として起票済み**（2026-09-21・投函前に実測して再現を確認）

# 派生issueの起票案（復旧後に `gh issue create` する）— T4 で消えた「結果ダイアログ」を指すコメントが `app.js` に残っている

⭐ **呼び名: 「結果ダイアログ前提の残存コメント」**（親タスク: [「検索結果のタブ化」](../未採番-デッキ構築ツールUI刷新/起票案_2026-09-04_検索結果のタブ化.md)）

## 症状

`tools/deck-builder/app.js` の **`searchStatusText()` の直前2行のコメント**が
**「結果ダイアログの件数表示。」**と書いている。⚠️ **結果ダイアログは「検索結果のタブ化」の T4（`f3c4e8a5`・`533ff220`）で撤去済み**で、
件数は**タブの結果パネル**に出る。**存在しない画面部品を指すコメント**が残っている。

## 直し方

- コメント1行の文言を、**いまの描画先（結果パネル）**に合わせて書き換えるだけ。⭐ **挙動は変わらない**
- ⚠️ **行番号では参照しない**（`validate` の検査に引っかかる＋腐る）。**関数名で書く**

## ⭐ なぜ親タスクで直さなかったか

- **全単位（T1〜T6）がレビュー合格し、ユーザーの実物確認も終わった**あとに出てきた指摘のため、
  ここでコードを1行変えると**誰もレビューしていない差分**になる（→ [`承認_2026-09-17.md`](承認_2026-09-17.md) §4-1）
- ⚠️ **コメント1行のために設計→実装→レビューを回し直す価値は無い**ので、
  ⭐ **次にデッキ構築の `app.js` を触る回に同梱する**のが安い

## 同種の取り残し

- ⭐ **T4 の同型の取り残しは、`updateElementWarn()` 側だけ T5（§8-4-3-1）で消えている**。
  ⚠️ **本件を消すと「同じ型が2件あって片方だけ直した」状態が解消する**

## 検証項目

| # | 見るもの | 期待 |
|---|---|---|
| V1 | `grep -n '結果ダイアログ' tools/deck-builder/app.js` | **0件** |
| V2 | `npm run validate` | exit 0 |
| V3 | デッキ構築の検索で件数表示 | **変化なし**（コメントのみの変更） |
