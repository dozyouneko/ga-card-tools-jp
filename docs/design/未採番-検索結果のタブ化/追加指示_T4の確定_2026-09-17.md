**対象: 「検索結果のタブ化」**

# 追加指示 — T4 の確定と、T3 までの確認事項の裁定（2026-09-17）

⚠️ **これは issueコメントの代替である。** GitHubアカウント停止中で `gh issue comment` が打てないため、ここに書く。
復旧して採番されたら、本文をそのまま
`gh issue comment <n> --body-file docs/design/未採番-検索結果のタブ化/追加指示_T4の確定_2026-09-17.md` で投函する。

| | |
|---|---|
| 受けた工程 | **T1・T2 の再判定＝合格**（`d765c53f`）／**T3＝合格・差し戻し0回**（`3addcd11`） |
| 設計書 | ⭐ **[`検索結果のタブ化_設計.md`](検索結果のタブ化_設計.md) を 1.1 → 1.2 に改版した**（この指示の正は設計書側） |
| 裁定の対象 | 実装報告 T3 §6 の**4件**／レビュー判定 T3 の **S-1〜S-5**／再判定 T1・T2 の **S-1・S-2** |
| 状態 | **実装中**のまま（起票案の1行目は変えていない） |
| 次の担当 | **開発担当**（ラベル相当 **`実装待ち`**）——⭐ **単位 T4** |

⚠️ **レビューの判定（T1・T2 再判定＝合格／T3＝合格）は覆していない。** ⚠️ **承認はしない**（最終承認はユーザー）。

---

## 1. 裁定の一覧

### 1-1. 実装報告 T3 §6 の確認事項（4件）

| # | 論点 | ⭐ 決定 | 理由 |
|---|---|---|---|
| **1** | 「↩ この条件を左ペインに読み込む」ボタンの要否 | ⭐ **足さない（確定）**。設計書 **§6-1 の1** に「**次の単位で足さない**」と明記した | ⭐ **あのボタンはモックで U1「独立」案のときだけ出していた物**で、**ユーザーは「連動」を選んだ**（§2-2 U1）。**タブを押せば左ペインがその条件に戻る**ので役目が重なる。⚠️ **足すと 375px で箱の見出し行が1行増え、S-3 の高さ（211px）がさらに伸びる** |
| **2** | T4 で結果パネルを右列へ移すときの条件の箱の扱い | ⭐ **箱はパネルの一部として一緒に動かす**（**箱だけ別に出し分けない**）。**§8-4-1 の4「固定タブ表示中はパネルを `hidden`」に箱も含まれる**。設計書 **§8-4-2 の5** | ⭐ **T3 が作った「箱・件数・タイルは `activeSearchTab` 1つで決まる」を崩さない**（レビュー §5・§6 の S-6 が良い点として挙げている構造）。⚠️ **箱だけ残るとデッキ面の上に「検索3 の条件」が浮く** |
| **3** | 「並び」を「並び替え」に揃えるか | ⭐ **「並び」のまま追認**（設計書 **§6-1 の2**） | ⚠️ **2列の表の1列目は `max-content`** なので、**項目名を伸ばすと値の幅が削れて折り返しが増える＝375px の箱が高くなる**（S-3 に直に効く）。⭐ **左ペインと綴りが違うのはこの行だけの意図的な例外**と明記した |
| **4** | 値が1つのとき「（いずれか）」を添えない | ⭐ **追認し、§6 に明文化した**（**§6-1 の3**） | ⭐ **1つしか選んでいなければ AND と OR で結果が変わらない**（意味の無い注記が常時出るのを避ける）。⚠️ **§6 冒頭の「項目名に添える」は複数値の文脈である**と書き足した——**次の単位で読み違えないため** |

### 1-2. レビュー判定 T3 の S 指摘

| # | 論点 | ⭐ 決定 | 理由 |
|---|---|---|---|
| **S-1** | 畳めないタブで `▾ すべて表示（ほか -1 項目）` が組み立てられている | ⭐ **T4 の作業に含める**（**§8-4-2 の2**・検証 **V17-b**）。⭐ **`foldable` でないときは文言を組み立てない**（⚠️ **0 下限で誤魔化さない・`hidden` に頼らない**） | ⚠️ ⭐ **T4 はまさに `hidden` の扱いを作り直す単位**なので、**1つ間違えた瞬間に画面に出る**。⭐ **出ない値を持たせないほうが、`hidden` の正しさに依存しないぶん強い** |
| **S-2** | ⭐ **CSS の `:nth-child(n+7)` と JS の `COND_FOLD_SHOW = 3` の対に自動検査が無い** | ⭐ **検査は足さない。⚠️ 代わりに重複そのものを消す**——**JS が畳む行に印を付け、CSS から行数を無くす**（設計書 **§8-4-3**・検証 **V17-c**）。⚠️ **消せないと判断したときだけ検査を足す**（**両辺別ファイル・`K === SHOW*2+1`・破壊試験必須**）。⭐ **どちらを採ったかは報告に書く** | ① ⭐ **CLAUDE.md の検査①②③は「重複が避けられない対」に入れたもの**（**CSS の `@media` と JS の `matchMedia` は言語をまたぐので2回書くしかない**／別ページ間の配線）。⚠️ ⭐ **ここは `renderCondBox()` が行を自分で作っているので重複を避けられる**——**消せる重複に検査を足すと、重複と検査の2つを保守することになる** ② ⭐ **症状の性質も①②③と違う**: ①②③は**完全に無言**（リクエストが倍・帯が出ない）。**ここは「畳んだ行数と『ほか N』が1つずれる」＝画面に出ている**（⚠️ **気づきにくいだけで無言ではない**）。⭐ **無言でない不整合には、検査より「起こせない形」のほうが効く** |
| **S-3** | ⚠️ **375px の畳んだ高さは上限が無い**（ふつう **108px** / 最悪 **211px**。⚠️ **実装報告の 137px は再現しない**） | ⭐ **レビューの実測値を設計書に取り込んだ**（**§6-2** に表・**§8-7-1** に T4 への帰結）。⭐ **送り先の基準を「タブ帯の上端」に固定**し、**箱やタイルを基準にしない**と明記。検証 **V15-b** | ⚠️ ⭐ **「畳めば高さは一定」という前提で T4 の寸法を決めると、条件の数で 100px 以上ずれる**。⭐ **`min-height: 100vh` は維持でよい**（箱が高いほどページは長くなるので、送れなくなる側には倒れない） |
| **S-4** | 箱の背景 `rgba(217,164,65,.08)` のハードコード | ⭐ **T4 で `color-mix(in srgb, var(--accent) 8%, transparent)` に寄せる**（**§8-4-2 の4**・検証 **V17-d**）。⚠️ ⭐ **`shared/css/tokens.css` に新しいトークンは足さない** | ⚠️ ⭐ **`shared/` を触ると V21 で不合格**であり、**ページCSSに `:root` を書くと `npm run validate` が exit 1**（CLAUDE.md の検査(ii)）＝**この2つの規約の隙間で成立する直し方は `color-mix` だけ**。⚠️ **検証は目視では不足**（ハードコードでも今日は一致して見える）＝**`--accent` を差し替える破壊試験で見る** |
| **S-5** | 実装報告の「V3 は未検証（実クリックが阻まれる）」が不正確 | ⭐ **工程記録は訂正しない**（⚠️ **投函済みのissueコメントの代替なので後から書き換えない**）。⭐ **設計書 §10-1 に「Escape を挟めば帯の全経路を測れる」と書いて上書きした**（**§11-9** に方針も明記） | ⚠️ ⭐ **「測れない」と報告に残ると、次の周でも測られない**。⭐ **次の周の担当が読むのは設計書**なので、**訂正は設計書側に置くのが届く** |
| **S-6** | 良かった点（辞書を新設していない・`renderCondBox()` の呼び出しを1か所に集約・`orderLabel()` の共用・箱はスナップショット） | ⭐ **設計書 §6-1 の末尾に「以後の単位でも崩さない」として残した** | ⚠️ ⭐ **「なぜそう書いたか」が残っていないと、次の単位で整理のつもりで戻される**（1.1 の §11-6 と同じ型） |

### 1-3. 再判定 T1・T2 の S 指摘（**T4 宛て**）

| # | 論点 | ⭐ 決定 |
|---|---|---|
| **S-1** | 裏のタブを × した後 `#result-count` に古い件数が残る | ⭐ **T4 の作業**（**§8-4-2 の1**・検証 **V25-b**）。**タイルを消すのと同じ場所で件数と「もっと見る」も空にする**。⚠️ ⭐ **モーダルの裏だった間は見えなかったが、右列に常時出ると「0枚しか無いのに『18 件を表示』」が見える** |
| **S-2** | `aria-controls` / `role="tabpanel"` | ⭐ **裁定 F（1.1）のとおり T4**。**§8-4-1 の4項目に、レビュー T3 の持ち越し（`.cond-more` の `aria-controls`）を5項目目として足した**。検証 **V18-b** |

### 1-4. ⭐ 公開方針（**2026-09-17・ユーザー決定**）

- ⭐ **本番公開はタブ化を完成させてから、大会データの週1更新と同時に行う**（**今日は公開しない**）。→ 設計書 **§11-8**。
- ⭐ **理由**: **経路②は `git archive HEAD` から作る**ので、**作りかけのタブ化が必ず同乗する**。
- ⚠️ ⭐ **＝T4・T5 が終わるまで、週1更新の公開そのものが待たされる**。**公開の可否を聞くときは「一緒に出るもの」を必ず提示する**:

  ```bash
  git diff --stat <本番位置>..HEAD -- . ':(exclude)docs/*'
  ```

---

## 2. 次に開発担当がやること（⭐ **単位 T4**。正は設計書 **§9-2**）

| # | やること | 設計書 | 検証 |
|---|---|---|---|
| **1** | ⭐ **結果モーダルを撤去し、結果パネル（⚠️ **条件の箱を含む**）を `#view-editor` の右列へ移す** | §8-4・§8-4-2 の5 | V13〜V16 |
| **2** | ⚠️ **`el.resultModal` を4か所から外す** | §8-4 | 回帰（Escape・カード詳細の `onAfterClose`） |
| **3** | ⭐ **ARIA を仕上げる**（`role="tabpanel"` / `aria-controls` / ⭐ **`aria-labelledby` は表示中タブに追従** / 固定タブ表示中は `hidden` / ⭐ **`.cond-more` の `aria-controls`**） | §8-4-1 | **V18-b** |
| **4** | ⭐ **タブを × したら件数と「もっと見る」も空にする** | §8-4-2 の1 | **V25-b** |
| **5** | ⭐ **畳めないときは「ほか N 項目」の文言を組み立てない** | §8-4-2 の2 | **V17-b** |
| **6** | ⭐ **畳みの閾値の重複を消す**（CSS から行数を無くす） | **§8-4-3** | **V17-c** |
| **7** | **箱の背景を `--accent` 追従に**（⚠️ `shared/` と `:root` を触らない） | §8-4-2 の4 | **V17-d** |
| **8** | **`min-height` とスクロール送り**（⚠️ ⭐ **送り先はタブ帯の上端。箱・タイルを基準にしない**） | §8-7・**§8-7-1** | V15・V16・**V15-b** |

⭐ **報告に必ず書くこと**:

- **V25-b**: × のあと `#result-count` が**空**であること（⚠️ **「18 件を表示」が残っていたら不合格**）。
- **V17-b**: 畳めないタブで `ほか -1 項目` の**文字列がどこにも組み立てられていない**こと（⚠️ **`hidden` だから見えない、で済ませない**）。
- **V17-c**: ⭐ **`grep -n 'nth-child' tools/deck-builder/style.css` の結果**（0件が期待。⚠️ **検査を足す方を選んだなら、破壊試験で exit 1 になる実測**）。
- **V17-d**: ⭐ **`--accent` を差し替えると箱の背景も変わること**（破壊試験。⚠️ **目視の色合わせでは不足**）。
- **V15-b**: **375px・10項目・長い値**で**畳んだ高さ**と、**送り先がタブ帯で一定**であること（⚠️ **137px は再現しない前提で測る**）。
- **V19**: ⚠️ ⭐ **before/after は `rj-v19b.mjs` の形で測る**（**前任者の `v19.mjs` は現行 Chrome で走査 0 件＝空回りしていた**。設計書 §10-1）。
- ⚠️ **V3・V3-b は「モーダルに阻まれて測れない」と書かない**（⭐ **T4 ではモーダルが無くなるので、そもそも回り道が要らなくなる**）。

⚠️ **やらないこと**: **T5（§8-1〜8-3＝絵柄の追従・件数の描画先・エレメント警告）に入らない**／
**「↩ この条件を左ペインに読み込む」ボタンを足さない**／**畳みの境界（4→5項目）を変えない**／**push しない**（§11-8）。

---

## 3. 設計書のどこを見ればよいか（⭐ **1.2 の変更点**）

| 節 | 何が変わったか |
|---|---|
| **改版履歴** | 1.2 の行（⭐ **なぜ変えたかを記載**） |
| ⭐ **§6-1（新設）** | T3 の裁定4件（↩ボタン・「並び」・（いずれか）・モック由来の文言）＋ **辞書を新設しない作りを崩さない** |
| ⚠️ **§6-2（新設）** | ⭐ **375px の実測値の正**（108 / **211** / 510px）。⚠️ **実装報告の 137px は再現しない** |
| ⭐ **§8-4-2（新設）** | ⭐ **T4 でまとめて受ける持ち越し5件**（古い件数・`ほか -1 項目`・重複解消・背景色・箱はパネルの一部） |
| ⭐ **§8-4-3（新設）** | ⭐ **S-2 の裁定**——**検査ではなく重複を消す**（⚠️ 消せないときの代替と、そのときの要件） |
| **§8-4-1** | **5項目目（`.cond-more` の `aria-controls`）を追加** |
| ⚠️ **§8-7-1（新設）** | **条件の箱の高さは可変**＝**送り先の基準はタブ帯の上端**・**`min-height: 100vh` は維持**・**畳みの既定を維持** |
| **§9 の単位表 / §9-2（新設）** | **T4 の中身を8項目に確定** |
| **§10-1** | ⭐ **Escape を挟めば帯の全経路を測れる**（S-5）／⚠️ **`v19.mjs` の空回りと `rj-v19b.mjs` を使うこと** |
| **§10-2** | **V15-b・V17-b・V17-c・V17-d・V18-b・V25-b を追加**。⚠️ ⭐ **既存の V番号は動かしていない**（実装報告・レビュー判定が番号で参照しているため） |
| **§11-8・§11-9（新設）** | ⭐ **公開方針（2026-09-17 ユーザー決定）**／⚠️ **工程記録は書き換えず、食い違いは設計書で上書きする** |

---

## 4. ⚠️ ユーザーへの確認事項

- **なし。** ⭐ **ユーザー決定（§2-1・§2-2・§2-3）は1つも覆していない。**
  上の裁定はすべて、**決定が書いていなかった隙間を埋めたもの**である。
- ⚠️ ⭐ **残っているユーザー工程は2つ**: **§10-5 の実物確認**（レビュー合格後・承認の前・**3000番**）と、**§11-8 の公開判断**。
  ⭐ **どちらも T5 まで終わってからまとめて行う**（⚠️ **T4 の時点で承認を求めない**）。

---

**次の担当: 開発担当（ラベル相当 `実装待ち`）**
