**対象: 「検索結果のタブ化」**

# 設計完了 — 検索結果のタブ化（2026-09-16）

⚠️ **これは issueコメントの代替**（GitHubアカウント停止中で `gh issue comment` が打てないため、ファイルに残す）。
復旧して採番されたら、本文をそのまま `gh issue comment <n> --body-file docs/design/未採番-検索結果のタブ化/設計完了_2026-09-16.md` で投函する。

- 日付: **2026-09-16**
- 設計の起点 HEAD: **`adee6324`**（⚠️ `tools/deck-builder/` は**本番 `ecb2b51d` ではなく「デッキ構築の操作不具合2件」を含む**＝未公開の実装の上に載る）
- 次の担当: ⭐ **開発担当**（ラベル相当: **`実装待ち`**）——**単位 T1 から**

---

## 1. 設計書のパス

- ⭐ **正**: [`検索結果のタブ化_設計.md`](検索結果のタブ化_設計.md)（**1.0**）
- 起票案: [`../未採番-デッキ構築ツールUI刷新/起票案_2026-09-04_検索結果のタブ化.md`](../未採番-デッキ構築ツールUI刷新/起票案_2026-09-04_検索結果のタブ化.md)（⚠️ **リンク元が9ファイルあるので移していない**）
- モックと実測: [`モック説明_検索結果のタブ化_2026-09-15.md`](モック説明_検索結果のタブ化_2026-09-15.md)（⭐ **実測値の正はこちら**）

## 2. 対象範囲

| 単位 | 中身 | 変えるファイル | 設計書 |
|---|---|---|---|
| **T1** | タブ帯（固定2つ＋区切り＋横スクロールする検索タブの箱）・上限5・連番・× ・キーボード | `index.html`・`style.css`・`app.js` | §4 |
| **T2** | タブごとの検索コントローラ・条件のスナップショット・localStorage・リロード復元 | `app.js` | §5・§7 |
| **T3** | 条件の箱（2列の表・4項目超で畳む） | `app.js`・`style.css` | §6 |
| **T4** | ⭐ **結果モーダルの撤去**・結果パネルの移設・`min-height`・スクロール送り | `index.html`・`style.css`・`app.js` | §8-4・§8-7 |
| **T5** | 既存コードの手当てと `validate` | `app.js`・**`scripts/validate.mjs`** | §8-1〜8-3・8-5・8-6 |

⚠️ **範囲外**: トップページ（ルートの `index.html` / `style.css` / `app.js`）・`shared/`・閲覧専用画面（`#view-deck`）。**触ったら V21 で不合格**。

## 3. 検証項目の要約（⚠️ **正は設計書 §10**）

- **V1〜V21**（本体）: タブの生成・× ・上限5と連番・**カード投入（起票案 R1）**・並び替えのその場更新・**✕で再検索しないこと（V10）**・**P1（V11）**・リロード復元・375px・`validate` exit 0・範囲外を触っていないこと。
- ⚠️ ⭐ **M1-b（V22〜V25）**: **絵柄の追従 / 共有URL（⭐ デッキ構築には無い＝対象外） / バッジ集計 / 件数表示**。
  **設計書に書いてあるので、欠けたら M1 の欠落＝不合格**。
- **ヘッドレスで担保できないもの**: デッキの中身に依存する表示（§10-4）と**タッチの感触**（§10-5 の3）。

## 4. 判断済み事項（⚠️ **実装側で覆さない**）

| # | 決定 | 出所 |
|---|---|---|
| ① | タブはリロード後も残る（**localStorage**・D1は使わない＝**マイグレーション不要**） | ユーザー 2026-09-15 |
| ② | **上限5**・**連番は超えても続ける** | 同上 |
| ③ | **PC・スマホともタブ**（結果モーダルは廃止） | 同上 |
| **U1** | **連動**（タブを開くと左ペインがその条件に戻る／並び替えは表示中のタブをその場で更新） | ユーザー 2026-09-16 |
| **U2〜U7** | デッキごと／全部閉じたら検索1へ／リロード後は最後のタブだけ再検索／新しい検索へ切り替える／**チップでは検索しない**／**固定タブは絵文字だけにしない** | 同上 |
| **⭐ 追加** | ⚠️ **「選択中の条件」の✕・すべて解除では再検索しない**（＝モック2026-09-15版から覆った）。**食い違いの注記も出さない** | ⭐ **ユーザーの指摘・2026-09-16** |
| **P1** | 別の起票案にしない（本件の §7 が直す）。⚠️ **本件の公開まで本番に残る** | ユーザー 2026-09-16 |

## 5. ⚠️ ⭐ 開発担当がとくに気をつけること

1. ⭐ **`onRemoveOne` を渡すページが 0 になるので、`npm run validate` の検査①が exit 1 になる。**
   **`scripts/validate.mjs` の許可リストを空にする変更を、同じコミットに入れる**（設計書 §8-6）。
   ⚠️ **検査ごと消さないこと**（消すと後から誰かが渡しても止まらない）。
2. ⭐ **`preferredArtIndex()` の引数化を忘れない**（§8-1）。忘れると **P5 と同じ「絵柄が追従しない」不具合が再発する**＝ M1-b の V22 で不合格。
3. ⚠️ **結果モーダルを消したら `el.resultModal` を4か所から外す**（§8-4）。1つでも残ると例外か `body` の `overflow` が戻らない。
4. ⚠️ **`shared/js/card-search.js` は無改修で足りる**（`els` は値の読み出しにしか使われていない）。⭐ **触らずに済ませる**。
5. ⚠️ **モックの `inject.js` を写さない**（設計検討用の部品。⭐ **✕の挙動は 2026-09-16 に覆っている**）。

## 6. 未決事項

- **なし**（⭐ **U1〜U7・P1 がすべて決着した**）。
- ⚠️ ⭐ **ただし「実物確認（§10-5）」は未実施**。**レビュー合格後・承認の前にユーザーが 3000番で操作する工程が入る**。
  ⭐ **タブの体感・✕の食い違い・タッチの感触は、この工程でしか判定できない**。
