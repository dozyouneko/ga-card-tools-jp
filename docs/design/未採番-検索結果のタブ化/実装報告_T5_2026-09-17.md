**対象: 「検索結果のタブ化」**

# 実装報告 — T5（本タスク最後の実装単位）2026-09-17

⚠️ **これは issueコメントの代替である。** GitHubアカウント停止中で `gh issue comment` が打てないため、ここに書く。
復旧して採番されたら、本文をそのまま
`gh issue comment <n> --body-file docs/design/未採番-検索結果のタブ化/実装報告_T5_2026-09-17.md` で投函する。

| | |
|---|---|
| 受けた指示 | [`追加指示_T5の確定_2026-09-17.md`](追加指示_T5の確定_2026-09-17.md)（設計書 **1.3** が正） |
| 設計書 | [`検索結果のタブ化_設計.md`](検索結果のタブ化_設計.md) **§8-1 / §8-1-1 / §8-1-2 / §8-2・§8-3 / §8-4-1 の6 / §8-4-3-1 / §9-3 / §10-3 V22** |
| コミット | **`6f56a040`**（本体）・**`395d61e6`**（コメントの是正） |
| push | ⚠️ **していない**（§11-8・公開はユーザー判断） |
| 変更ファイル | ⭐ **`tools/deck-builder/app.js` の1つだけ**（`git diff --name-only c94bac53..HEAD`） |
| 次の担当 | **レビュー担当（ラベル相当 `レビュー待ち`）** |

---

## 1. 実施内容

### ステップ

| # | やったこと | コミット |
|---|---|---|
| 1 | ⭐ **V22 の「直る前」を先に実測して記録した**（§5 の表・⚠️ 直す前を測らないと「直った差」を示せない） | （計測のみ） |
| 2 | ⭐ **`preferredArtIndex()` の引数化**（§8-1・§8-1-1 の手順1〜4） | `6f56a040` |
| 3 | **`app.js` に取り残された `:nth-child(n+7)` のコメント2行を是正**（§8-4-3-1） | `6f56a040` |
| 4 | **`updateElementWarn()` のコメントの前提を T4 後の実態に直した**（⚠️ **コードは1行も変えていない**） | `395d61e6` |
| 5 | V22 ①〜④ の再実測＋回帰（V1〜V25-c）＋ `npm run validate` | （検証） |

### 変更の中身（`6f56a040`）

| # | 変更 | 中身 |
|---|---|---|
| 1 | ⭐ **`artCondOf(cond)` を新設**（`condEls()` の直後） | 条件のスナップショット（クエリ文字列）から **`prefixes`（`setIndexOf()` → `setPrefixes()`）と `rarities`（`rarity` の全値）** を取り出す。⭐ **添字は保存しない**＝`condEls()` と同じ引き方（§8-1-1 の3） |
| 2 | ⭐ **`preferredArtIndex(imgs, artCond)`** | ⚠️ **`el.sSet` / `el.sGRarity` を読まなくなった**。判定規則（prefix ∧ rarity、無ければ `imgs[0]`）は**そのまま** |
| 3 | **`appendResults(cards, info, artCond)`** | 受け取った条件を各タイルの `preferredArtIndex()` に渡す |
| 4 | ⭐ **`makeSearchCtl(tab)` が作成時に `const artCond = artCondOf(tab.cond)` を固定** | `els: condEls(tab.cond)` と**同じ固定の仕方**。⚠️ `tab.cond` が変わるのは `updateActiveSearchTab()` だけで、そこは `tab.ctl` も作り直すので**必ず一致する**（`mine()` を通った後の値を素直に渡すのと等価） |
| 5 | **畳みの閾値のコメント** | `:nth-child(n+7)` への参照を削除し、「閾値を持つのは JS のこの2つだけ・CSS に数値を書き戻さないこと」に書き換え。⚠️ **`COND_FOLD_AT = 4` / `COND_FOLD_SHOW = 3` の値は変えていない** |

⚠️ **トップの `app.js` の `preferredArtIndex()` は1文字も触っていない**（二重定義の解消は §11-2 の別タスク）。
⭐ デッキ構築側の関数コメントに「**トップ側は引数化していない**」と明記した（片方だけ直したことが次の人に伝わるように）。

---

## 2. 検証結果（設計書 §10 の番号に対応）

⭐ **到達手順は #51 の標準手順**（`/tools/deck-builder/#edit/<id>` へ `/api/me`・`/api/decks`・`/api/decks/<id>`・
`/api/decks/<id>/cards`（POST/PATCH/DELETE）を stub して**ログイン済み・デッキを開いた状態**を作る）。
⭐ **デッキの中身が要る V6・V7・V7-b も stub で実測した**（§10-4 のとおり「未検証」にしていない）。
スクリプトと証跡は `tmp/dev/検索結果のタブ化/`（⚠️ **他セッションの `tmp/review/…` には触っていない**）。

### 2-1. ⭐ T5 の主目標

| # | 項目 | 実測 | 判定 |
|---|---|---|---|
| **V22 ①** | PRD で検索 → 🔍を押さず左ペインだけ **PRD 1st** →「もっと見る」 | 1ページ目 `24 件を表示 / 全 232 件`、もっと見る後 **48枚**。🎨バッジの内訳 **`{PRD:12, (バッジ無し):36}`**＝**PRD 以外 0件** | ⭐ **合格** |
| **V22 ②** | PRD と PRD 1st の2タブを**行き来してから**それぞれで「もっと見る」 | 検索1 の続き24枚 `{PRD:8, (バッジ無し):16}`（PRD 以外 **0**）／検索2 の続き3枚 `{PRD 1st:3}`（PRD 1st 以外 **0**） | ⭐ **合格** |
| **V22 ③** | ⚠️ **レアリティ**（P5 の再発確認）: SR(4) で絞って検索 → 🔍を押さず左ペインだけ **コモン(1)** →「もっと見る」 | 1ページ目の🎨持ち18枚の**表示中レアリティが全部 `4`**／続きの🎨持ち20枚も**全部 `4`**（SR版を持つのに SR で出ていないもの **0件**） | ⭐ **合格** |
| **V22 ④** | 絞り込み無し（`imgs[0]` のまま） | `sword` 24件・🎨持ち15枚の **`dataset.artUrl` が全部 `imgs[0].url` と一致**（ずれ **0件**） | ⭐ **合格** |
| **V17-e** | 取り残されたコメントが消えたこと | ⭐ **`grep -rn 'nth-child' tools/deck-builder/` → 0件**（フォルダ全体・grep の exit が 1）。⭐ **畳みの境界は不変**: 3行/4行=畳まない・**5行=`▾ すべて表示（ほか 2 項目）`**・**8行=`（ほか 5 項目）`** | ⭐ **合格** |
| **V14-b** | 375px でエレメントAND 0件確定（§8-3 の確認・⚠️ **コードは触らない**） | ①件数欄に `ELEMENT_AND_MESSAGE`（`この組み合わせに該当するカードはありません。…`）②エレメント群が `open`・警告 `.fwarn` が `hidden:false`・**`search-top.filters-open = true`／`s-toggle aria-expanded=true`／ラベル `絞り込み ▲`** ③⭐ **送り先は `band.getBoundingClientRect().top === 0`**（左ペインが 302px → **1210px** に伸びてもタブ帯の上端のまま） | ⭐ **合格（何もしないで正しい）** |
| **V25-c** | 裏のタブで検索中にタブを切り替える（§8-2 の確認） | 切替後の表示中タブの件数 `24 件を表示 / 全 27 件`・grid 24＝**裏の結果は混ざらない**。戻すと **`/cards/search` が +1**（取り直す＝仕様どおり） | ⭐ **合格（何もしないで正しい）** |
| **V18-c** | ⭐ 見えている面は常にちょうど1つ（§8-4-1 の6） | 検索タブ表示中 / 統計表示中 / デッキ表示中の**どれでも可視は1**（`ed-pane-deck` `ed-pane-stats` `ed-pane-search` の可視数＝**1**）。⭐ **統計の鮮度**: デッキ面で ＋ を押して枚数を増やした後に統計へ戻ると中身が更新されている（`カードがありません` にならない） | ⭐ **合格** |

### 2-2. 回帰

| # | 実測 | 判定 |
|---|---|---|
| **V1** | 2回検索でタブ2つ。検索1を押しても **`/cards/search` 増分 0**・grid 24・`aria-selected=true` は1つ | ⭕ |
| **V2** | 固定タブ（🃏デッキ・📊統計）に `.tab-close` は **0個** | ⭕ |
| **V3** | 選択中の検索4を × → `["検索2","検索4*","検索5"]`＝**右隣へ移る**・`aria-selected=true` は1つ | ⭕ |
| **V3-b** | 🃏デッキ を見ている間に裏の検索タブを × → **`ed-tab-deck` が `true` のまま**・検索タブは1つも選択されない・`ed-pane-search` は `hidden` のまま | ⭕ |
| **V4** | 6回検索 → タブ5つ・いちばん古いものが閉じる・トースト `タブは5つまでです — いちばん古い「検索2」を閉じました`・**連番は続く**（検索4〜検索8） | ⭕ |
| **V5** | 全部閉じてから検索 → ラベルは **`検索1`** | ⭕ |
| **V6** | 検索2 で ＋メイン → トースト `✅ 「熟達の剣豪」をメインデッキに追加 — 1 / 60以上`・バッジ `1枚`・追加行が `main:1` のステッパー | ⭕ |
| **V7** | 検索1 へ切り替え → 同カードのバッジ `1枚`（`hidden` でない）・`main:1` のステッパー。⭐ **`/cards/search` 増分 0** | ⭕ |
| **V7-b** | 🃏デッキ の ＋ で2枚にしてから検索1 へ戻す → バッジ `2枚`・`main:2` | ⭕ |
| **V7-c** | 🎨 で `ALCSD → RDOA` に切り替え → 別タブへ行って戻す → **`art` / `artUrl` / `src` の3つとも切り替えた版のまま** | ⭕ ⭐ |
| **V8/V9** | （T3・T4 で確認済み。今回は触っていない経路） | — |
| **V10** | 「選択中の条件」の ✕ と **すべて解除**: **`/cards/search` 増分 0**・タブ数不変・grid 不変・件数不変・**条件の箱のテキスト不変**。チップとバッジだけが減る | ⭕ |
| **V11（P1）** | `sword` 27件のまま「もっと見る」→ **27枚すべて sword**（`notSword: 0`） | ⭕ |
| **V12** | リロード → タブ3つが残り、**`/cards/search` 増分 1**（最後に見ていたタブだけ）。別タブを押すと +1 | ⭕ |
| **V12-b** | 📊統計 を見た状態でリロード → **統計タブが開く**・検索タブ3つは帯に残る・⭐ **`/cards/search` 増分 0** | ⭕ |
| **V13** | 絞り込み `type=champion` + レベル順 → **`読み込み中 1/3 ページ…`** が出る。⭐ **`#result-count` は `#ed-pane-search` の中**＝タブの中に出ている。最終 `24 件を表示 / 全 133 件（レベルを持つカードのみ）` | ⭕ |
| **V14** | JP `剣豪` → `1 件を表示（日本語一致・翻訳済みのみ）`／0件文言も従来どおり／⭐ **裏面一致の注記（#46）**: `リス` で **`🔄 裏:滑空するリス`** が出て、そのタイルは**裏面で開く** | ⭕ |
| **V15** | 375px で3回検索 → 毎回 **`scrollY 677` / `bandTop 0`**・**横あふれ 0**・固定2つは左に残り（`deckLeft 16` 不変）検索タブだけ横に送れる | ⭕ |
| **V15-b** | 375px・多項目（9行）で検索 → 箱は**畳まれて 174px**（少項目 86px）・`▾ すべて表示（ほか 6 項目）`・**横あふれ 0**・⭐ **送り先は少項目のときと同じ `scrollY 677`** | ⭕（※§4） |
| **V16** | 1440px・タブ帯が画面の上半分（`top 178 / 高さ900`）→ 検索しても **`scrollY 0` のまま** | ⭕ |
| **V17** | 条件8項目 → 3項目＋`▾ すべて表示（ほか 5 項目）`。開くと `▴ 畳む`・`aria-expanded=true`。開閉はタブごとに残る | ⭕ |
| **V17-b** | 2項目のタブ → **`moreText` が空文字**（`▾ すべて表示（ほか -1 項目）` を**組み立てていない**）・ボタンは `hidden` | ⭕ |
| **V17-c** | `grep -n 'nth-child' tools/deck-builder/style.css` **0件**・境界不変（上の V17-e と同じ実測） | ⭕ |
| **V17-d** | ⭐ **破壊試験**: `--accent` を `rgb(0,255,0)` に差し替えると箱の背景が `color(srgb 0.851 0.643 0.255 / 0.08)` → **`color(srgb 0 1 0 / 0.08)`** に変わる | ⭕ |
| **V18** | ←/→ でタブ移動（`aria-selected=true` は常に1つ）・Delete で検索タブが閉じる（2→1） | ⭕ |
| **V18-b** | `ed-pane-search` に `role="tabpanel"`／検索タブの `aria-controls` は **`ed-pane-search`**（`result-grid` ではない）／⭐ **`aria-labelledby` がタブを移ると `ed-tab-search-1` → `ed-tab-search-2` に切り替わる**／固定タブ表示中はパネル `hidden`／`.cond-more` に `aria-controls="result-cond-list"` | ⭕ |
| **V19** | ⚠️ **閲覧専用画面（`#view-deck`）が1pxも変わらない**。⭐ **`rj-v19b.mjs` の形**で before（`c94bac53` を `tmp/dev/…/before-tree` に `git archive` → **そこの `serve.mjs` を 3011 で起動**）と after（3000）を比較 → **1440px・375px とも `IDENTICAL`**（走査ルール **旧473 / 新473**＝空回りしていない） | ⭕ |
| **V20** | `npm run validate` **exit 0**。⭐ **検査① は `onRemoveOne wiring in sync — 渡しているのは 0ファイル（なし）／25ファイル走査`**（§8-6 のとおり「0ファイル」が正） | ⭕ |
| **V21** | `git -c core.quotepath=false diff --name-only c94bac53..HEAD` → **`tools/deck-builder/app.js` の1行だけ**。⚠️ **トップ（ルートの `index.html` / `style.css` / `app.js`）・`shared/`・`scripts/` は出ない** | ⭕ |
| **V23** | ⭐ **確認したうえで対象外**: デッキ構築の `location.hash` は `#edit/<id>` / `#deck/<id>`（デッキの経路）だけで、**検索条件を URL に書く経路は無い**。`🔗 共有` はデッキ一覧のデッキURLのコピー | ⭕（対象外） |
| **V24** | 「選択中の条件」の ✕ でバッジ **3 → 2**、すべて解除で **2 → (hidden)**。⭐ **再検索していないのに数は動く** | ⭕ |
| **V25** | 件数はタブごと（検索1 `24 件を表示 / 全 27 件` / 検索2 `22 件を表示`）。裏のタブの件数は混ざらない | ⭕ |
| **V25-b** | 持ち主タブを × → `#result-count` が **空文字**・「もっと見る」が `hidden`・grid 0・条件の箱も `hidden` | ⭕ |

⭐ **全スクリプトで `pageerror` は 0件**。

---

## 3. 実行できなかった検証と代替手段・残っている確認

| # | 項目 | 扱い |
|---|---|---|
| 1 | **実ログインでの保存の往復** | ⚠️ **ヘッドレスでは不可**（Discord にログインできるのはユーザーの手だけ）。⭐ **§10-5 の5** でユーザーの実物確認に委ねる |
| 2 | **375px のタッチの感触**（タブの横送り・切り替えの体感） | ⭐ **§10-5 の1・3**。ヘッドレスでは `scrollLeft` が動くことまでしか測れない |
| 3 | **並び替えでページが飛ぶ感触・`behavior: "smooth"` の要否** | ⭐ **§10-5 の7**（⚠️ **T5 では変えないと裁定済み**。⭐ 実測では 375px で `scrollY 0 → 677`＝T4 時点と同じ） |
| 4 | **V8 / V9**（並び替えのその場更新・タブ0個での並び替え） | ⚠️ **今回は再測していない**（T3・T4 で合格済みで、T5 は `preferredArtIndex()` の引数化とコメントだけ＝**`updateActiveSearchTab()` の経路には触っていない**）。⭐ ただし **`makeSearchCtl()` を作り直す経路**は `artCond` の固定に関わるので、**V22 ②（タブの行き来と続き）で同じ固定の仕方を実測している** |

⚠️ **「デッキの中身に依存するので未検証」は1件も出していない**（§10-4）。

---

## 4. 差分が設計書の想定と食い違った点

| # | 食い違い | 内訳と、意図内と判断した根拠 |
|---|---|---|
| 1 | **V15-b の箱の高さ** — 設計書は「**211px 前後**（⚠️ 137px にはならない）」、実測は **174px**（9行・畳んだ状態） | ⭐ **条件の項目数が違うため**（§10-3 の想定は10項目、今回組めたのは**9行**）。⚠️ **判定の本体は「箱の高さが変わっても送り先が変わらないこと」**で、そちらは **少項目 86px のときと同じ `scrollY 677`** で一致した。⭐ **`137px`（畳まれていない少項目の高さ）とは明確に別の値**なので、「畳まれている」ことの確認としては成立している |
| 2 | **V22 ① の「48枚すべて PRD」** — 実測の内訳は `{PRD:12, (バッジ無し):36}` | ⭐ **`(バッジ無し)` は版が1つしかないカード**（🎨ボタンが出ない＝選びようがない）。**`preferredArtIndex()` は `imgs[0]` を返すのが正**で、`PRD 1st` の版を持たない。⚠️ **判定は「🎨バッジが PRD 以外のものが0件」**で行った（T4 時点は同じ数え方で **2件**あった＝§5） |
| 3 | **V14-b の `scrollY`** — 通常の検索は `677`、V14-b は `1585` | ⭐ **左ペインが警告で 302px → 1210px に伸びるため**。⚠️ **要件は「送り先がタブ帯の上端のままずれない」**で、**`bandTop === 0`** は両方で一致した（＝**ドキュメント座標が動いただけで、見え方は同じ**） |

---

## 5. ⭐ V22 が「直った差」（⚠️ 直す前を先に測ってある）

⭐ **同じスクリプト（`tmp/dev/検索結果のタブ化/v22full.mjs`）を、`c94bac53`（T4 完了時点）と `395d61e6`（T5 後）で実行した。**

| 条件 | T4 時点（`c94bac53`） | ⭐ T5 後（`395d61e6`） |
|---|---|---|
| **① もっと見るの続き** | ⚠️ **`{PRD:10, PRD 1st:2}` の混在**。`augustine-votary-of-yore` と `camil-basked-abundance` の2枚が**左ペインの「いま」（PRD 1st）に従っていた** | ⭐ **`{PRD:12}`＝PRD 以外 0件** |
| **② 裏のタブ**（新しく追加された続きだけを数える） | `PRD 以外 0` / `PRD 1st 以外 0`（⭐ **この経路は U1 の連動で左ペインがタブの条件に戻っているため、もともと一致していた**） | ⭐ **同じく 0 / 0**（回帰していない） |
| **③ レアリティ**（P5 の再発） | ⚠️ **続きの🎨持ち20枚の表示中レアリティが `{1:4, 2:1, 4:15}`**＝**SR版を持つのに SR で出ていないもの 5件**（例 `assassins-ripper` が `DOA Alter(r1)`、`beastbond-boots` が `DOA Alter(r2)`） | ⭐ **`{4:20}`＝該当 0件** |
| **④ 絞り込み無し** | `imgs[0]` でないもの 0件 | ⭐ **同じく 0件**（回帰していない） |

⚠️ ⭐ **①だけでなく③でも差が出ている**のが要点——**prefix だけ直して満足していない**（§8-1-1 の4）。

---

## 6. 設計書からの逸脱と根拠

| # | 逸脱 | 根拠 |
|---|---|---|
| 1 | ⭐ **`tab.cond` を `onResults` の中で読むのではなく、`makeSearchCtl()` の作成時に `artCondOf(tab.cond)` として固定した** | ⚠️ 設計書 §8-1-1 の2 は「`onResults` から `tab.cond` を渡す（`activeSearchTab` を関数の中で読みに行かない）」。⭐ **意図（＝左ペインも `activeSearchTab` も見ない）はそのまま満たしている**うえ、**`els: condEls(tab.cond)` と同じ固定の仕方に揃う**。⚠️ **`tab.cond` が変わるのは `updateActiveSearchTab()` だけで、そこは `tab.ctl` も作り直す**ので両者は**必ず一致する**（`grep -n '\.cond *=' tools/deck-builder/app.js` → 1か所のみ）。⭐ 逆に毎回 `tab.cond` を読むと、**差し替え前のコントローラの結果が後から届いたときに新しい条件で絵柄を決めてしまう**（`mine()` で捨てられる想定だが、固定しておくほうが壊れない） |
| 2 | **`updateElementWarn()` のコメントを是正した**（`395d61e6`） | ⭐ **追加指示 §2-3 が「コメントの是正は範囲内」と明記**。⚠️ **コードは1行も変えていない**（`git show 395d61e6` はコメント行だけ） |

### 既存表記・`TRANSLATION.md` との照合

- ⭐ **画面に出る文言は1文字も増減していない**（今回の変更は関数シグネチャとコメントだけ）。`TRANSLATION.md` の照合対象なし。
- ⭐ **`GA_CARD_SEARCH.setIndexOf()` / `setPrefixes()` は既存の共有モジュールをそのまま使った**（`shared/` は無改修＝V21）。

---

## 7. ⚠️ 設計担当への確認事項

| # | 事項 | 判断してほしいこと |
|---|---|---|
| **1** | ⚠️ **`searchStatusText()` のコメントにも「結果ダイアログの件数表示」という T4 前の前提が残っている**（`app.js`。⚠️ **`updateElementWarn()` と同じ型の取り残し＝S-1 と同じ**） | ⭐ **今回は触っていない**——追加指示が範囲内と認めたのは `updateElementWarn()` のコメントだけで、**§8-2 は「何もしない」と確定している**ため（勝手に広げると V21 の対象になる）。⭐ **コメント1行なので T5 のコミットに足すか、派生起票案にするかを裁定してほしい** |
| **2** | ⭐ **`preferredArtIndex()` の二重定義**（トップ側は引数化していない）。**同じカードの代表画像が、トップの検索タイルとデッキ構築とで食い違う余地が残る** | ⭐ **§11-2 のとおり別タスクと理解している**（今回はデッキ構築側の関数コメントに「トップ側は引数化していない」と明記しただけ）。⚠️ **この T5 で片側だけが直ったので、派生起票案「ブラウザ側の代表画像」の前提が1つ変わっている**（＝**もう「両方とも左ペインを見る」ではない**）ことを起票案に反映するかどうか |
| **3** | ⚠️ **V8 / V9 を再測していない**（§3 の4） | ⭐ **T5 は `updateActiveSearchTab()` の経路に触っていない**ので回帰なしと判断したが、**レビューが必須と考えるなら測り直す** |

---

## 8. ⭐ 次にやること（T5 で実装は終わり）

1. **レビュー担当の判定**（⚠️ **V22 の4つが本単位の合否**＝§8-1-2）。
2. ⭐ **ユーザーの実物確認**（§10-5）。⚠️ ⭐ **上げ方は `npx wrangler pages dev . --local --port 3000`**
   （**3000番以外はユーザーのブラウザに届かない**）。⚠️ **開発担当が検証で使った 3000/3011 のサーバーは止めてある**。
3. **ユーザーの承認 → 公開**（§11-8・⚠️ **大会データの週1更新と同時**。⚠️ **push はしていない**）。

---

**次の担当: レビュー担当（ラベル相当 `レビュー待ち`）**
