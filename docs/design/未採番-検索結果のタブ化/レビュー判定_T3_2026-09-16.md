**対象: 「検索結果のタブ化」**

# レビュー判定 — 単位 T3（条件の箱・2026-09-16）

⚠️ **これは issueコメントの代替である。** GitHubアカウント停止中で `gh issue comment` が打てないため、ここに書く。
復旧して採番されたら、本文をそのまま
`gh issue comment <n> --body-file docs/design/未採番-検索結果のタブ化/レビュー判定_T3_2026-09-16.md` で投函する。

⚠️ **実装したのは T1・T2 を書いたセッションとは別の開発セッション**であり、⭐ **レビューもコンテキストを共有していない**。
判断材料は**設計書 1.1 §6・実装報告・コミットの中身・自分で操作した結果**だけである。

| | |
|---|---|
| 対象コミット | ⭐ **`66efc355`**（実装）／`48dac3ba`（実装報告）。前工程は **`d765c53f`**（T1・T2 の再判定＝合格） |
| 設計書（正） | [`検索結果のタブ化_設計.md`](検索結果のタブ化_設計.md) **1.1** の **§6** |
| 実装報告 | [`実装報告_T3_2026-09-16.md`](実装報告_T3_2026-09-16.md) |
| 今周の範囲 | **T3（§6）だけ**。⚠️ **T4**（モーダル撤去・パネル移設・`min-height`・スクロール送り・ARIA・前周の S-1）と **T5**（V22 の絵柄）は**不合格理由にしていない** |
| 証跡 | `tmp/review/未採番-検索結果のタブ化/`（gitignore。⭐ **今回追加したものは `rj3-` 接頭辞**。前任者・前周の作業物は消していない） |
| 差し戻し回数 | **0回**（この単位では初回） |

---

## 判定: **合格**

⭐ **設計 §6 の要求（2列の表・文言規則・畳み・タブごとの開閉・タブの `title`・丸ピルにしない）は全部満たしている。**
⚠️ ⭐ **境界のオフバイワンも無い**——**4行までは畳まず、5行から畳む**（＝「4項目を超えたら」）を自分で実測した。

⭐ **とくに良いのは「辞書を新設しなかった」判断**である。チップと `<select>` の表示名を実行時に引くので、
⚠️ ⭐ **`meta.rarities` / `meta.sets` に新しい値が増えても素の英字にならない**ことを、
**チップを1つ足して実測**して確かめた（§4-3）。**CLAUDE.md が繰り返し踏んでいる「手で足す辞書」を増やしていない。**

---

## 1. 必須基準

| 基準 | 結果 | 証跡 |
|---|---|---|
| **M1** 設計適合 | ✅ | 設計 §6 の**全要求**に合否を付けた（§3）。今周の番号付き項目 **V17** は畳み・タブごとの開閉・リロード復元まで一致（§4-1）。⭐ **M1-b も §5 で個別に判断**——欠落なし |
| **M2** 実駆動 | ✅ | レビュー担当が自分で操作。**1440px と 375px の両方**。Playwright 1.61.1 / `--ignore-certificate-errors` ＋ `ignoreHTTPSErrors`。静的プレビュー **3100番**（規定どおり）。⚠️ **検証後に停止済み**（3100・3111 とも）。スクリーンショット10枚 |
| **M3** 回帰 | ✅ | `npm run validate` → **exit 0**（§2）。主要画面: **トップ**（`sword` で **27件**・`.cond-box` の漏れ **0個**）／**カード詳細モーダル**（`#gacd-modal` に「熟達の剣豪」）／**カード個別ページ**／**大会一覧 454行**／**デッキ構築**（🃏デッキ・📊統計・検索タブ）— **pageerror 0件**。⭐ 回帰 **V1・V2・V3・V4・V5・V7・V7-b・V7-c・V10・V12・V12-b・V18・V19・V21・V25** を再測して全一致（§4-4） |
| **M4** 生成物の同期 | ✅ | 生成ロジック（`scripts/`・`data/tl/`）に変更なし。差分は `tools/deck-builder/` の3ファイルのみ。`validate` の `generated JSON up to date` / `card-meta-index sort keys OK` / `card term highlights up to date` はいずれも緑 |
| **M5** 静かな劣化がない | ✅ | ⭐ **①辞書を増やしていないことを実測で検算**（新しいチップを足す → 箱がその日本語名で出る・§4-3）。⭐ **②畳みの判定は `class` ではなく `getComputedStyle(dt).display` で数えた**（CSS の `:nth-child` で隠すので、クラスだけ見ると「畳んだつもりで見えている」を検出できない）。⭐ **③V19 の before/after は「既知の差」で検算**（旧 `result-cond` 0件 / 新 4件）。⚠️ **新しい検査は足していない**ので「両辺が同源」の問題は無い。⚠️ **CSS と JS の対の自動検査が無い**点は **S-2** に記録（現状は一致しており劣化していない） |
| **M6** 公開安全性 | ✅ | 本番配信物の**追加・削除 0**（`git show --name-status` は `M` 3件のみ）。**`style=` 属性の追加 0件**（CSP `style-src 'self'`）。`_headers`・`404.html` 無改修。秘匿情報なし。⭐ 参照しているCSS変数（`--accent` / `--accent-2` / `--muted` / `--text`）は **`shared/css/tokens.css` に定義済み**（`validate` の `shared css tokens in sync` も緑） |
| **M7** スコープ | ✅ | `git -c core.quotepath=false diff --name-only d765c53f..HEAD` = `tools/deck-builder/{app.js,index.html,style.css}` ＋ 実装報告の1ファイル。**トップ（ルートの `index.html`/`style.css`/`app.js`）・`shared/`・`scripts/` は出ない**。⭐ **新しい `@media` は足されていない**（実測: **旧6本 / 新6本**・prelude の集合も完全一致＝`validate` の検査②③に手を付けていない）。⭐ 新設クラス `.cond-box` / `.cond-title` / `.cond-list` / `.cond-more` は**製品コードのどこにも既存の同名が無い**（`docs/` のモックは `mk-` 接頭辞で別物）。作業ツリーは `git status --short` が空 |

⭐ **M1〜M7 すべて満たしているので合格。**

---

## 2. `npm run validate`（V20）

```
onRemoveOne wiring in sync — 渡しているのは 0ファイル（なし）／25ファイル走査
pane threshold in sync — 2ページ（トップ 1200px / デッキ構築 1200px）
mobile band marker in sync — 1組（style.css 1199px ↔ app.js MOBILE_MQ 1199px）
shared css tokens in sync — 68 トークン参照 / 未定義 0
:root owners in sync — 4ファイル

all checks passed.
EXIT=0
```

- ⭐ **検査①の「0ファイル」は設計 §8-5-1・§8-6 のとおり**（T2 で前倒し済み）。⚠️ **回帰ではない。**
- ⭐ **検査②が緑のまま**＝ `@media` を足していないことの裏付けにもなっている。

---

## 3. 設計 §6 の要求ごとの合否

| # | 設計 §6 の要求 | 実測 | 合否 |
|---|---|---|---|
| **6-1** | 「検索N の条件」＋**2列の表**（項目名｜値） | `#result-cond-title` が `検索1 の条件`。`<dl>` が `grid-template-columns: max-content minmax(0,1fr)` の2列。**箱の `border-radius` は 10px**（⚠️ **丸ピルなら 999px 級**＝**丸ピルにしていない**） | ✅ |
| **6-2** | 値は**チップの日本語名** | `ノーム` / `火` / `アノマリー・アサシン・クレリック` / `新レアリティ（NR）`（⭐ §4-3 の追加チップも追従） | ✅ |
| **6-3** | 複数値は「・」 | `アノマリー・アサシン・クレリック` | ✅ |
| **6-4** | **AND は「かつ」** | `エレメント（すべて）｜ノーム かつ 火` | ✅ |
| **6-5** | 項目名に**（いずれか）／（すべて）** | OR → `クラス（いずれか）`／AND → `エレメント（すべて）`。⚠️ **値が1つのときは添えない**（実装の判断。§6 の文は「複数値は…つなぎ、項目名に…添える」なので**複数値の文脈と読める**＝**逸脱とまでは言えない**）。→ **§7 の確認事項3** | ✅ |
| **6-6** | フォーマット・エキスパンションは**選択肢の表示名** | `フォーマット｜スタンダードで使用可`（`<select>` の実表示名と一致）／`エキスパンション｜.asphodel/paradise 1st Ed.（PRD 1st）`（⭐ **保存は prefix `set=PRD 1st` のまま**＝#20 の安定キーを壊していない） | ✅ |
| **6-7** | **「並び」は常に最後の行** | 全ケース（3行・4行・5行・6行・8行・10行）で最終行。値は `名前順 ▲ 昇順` / `レベル順 ▼ 降順`。⭐ **昇降順の文言は `orderLabel()` をボタンと共用**＝「ボタンは▲昇順・箱は▼降順」の食い違いが構造的に起きない | ✅ |
| **6-8** | 絞り込み無しで **「絞り込み｜なし（全カード）」** | `[["絞り込み","なし（全カード）"],["並び","名前順 ▲ 昇順"]]`・`dd` に `cond-none` クラス | ✅ |
| **6-9** | ⭐ **4項目を超えたら3項目だけ見せて畳む** | ⭐ **3行=畳まない / 4行=畳まない / 5行=畳む / 6行=畳む**（§4-1）＝**オフバイワン無し** | ✅ |
| **6-10** | 「▾ すべて表示（ほか N 項目）」 | 5行 → `▾ すべて表示（ほか 2 項目）`／8行 → `▾ すべて表示（ほか 5 項目）`（**V17 の期待値と一致**）。開くと `▴ 畳む`・`aria-expanded` も切り替わる | ✅ |
| **6-11** | **開閉はタブごとに覚える** | 検索1を開く → 検索2 → 検索1 で**開いたまま**／畳む → 往復で**畳んだまま**。⭐ **`localStorage` にも `open:true/false` が入り、リロードで復元**（§4-1） | ✅ |
| **6-12** | タブの `title` に条件の1行要約 | `エレメント（すべて）: ノーム かつ 火 / クラス（いずれか）: … / 並び: レベル順 ▼ 降順`。⭐ **並び替えのその場更新でも追従**（`syncSearchTabSelection()` で書いているため） | ✅ |
| **6-13** | ⚠️ ⭐ **丸ピルにしない**（375px で結果が 583px まで押し下がる） | **2列の表**（`<dl>`・角丸10px）。**375px の畳んだ高さは通常 108px / 最悪 211px**＝**丸ピル実測の 470px よりはるかに小さい**（§4-2） | ✅ |

⭐ **`condFromForm()` が作りうるパラメータ（`q` / `qtext` / 5群 ＋ `*_op` / `format` / `set` / `sort` / `order`）は、
`condRows()` が1つ残らず行にしている**——**箱に出ない条件が無い**ことをコードと実測の両方で確認した。

---

## 4. 実測の詳細

到達手順は **#51 の標準手順の実ログイン相当**（`/api/me`・`/api/decks`・`/api/decks/<id>`・`/api/decks/<id>/cards` だけを
`context.route` で差し替え、`#edit/REVIEW1` へ遷移して**ページ本来の `openEditor()` → 左ペイン → 🔍検索 → タブ**を動かした）。
⚠️ **内部変数は書き換えていない**（`renderCondBox()` を直接呼ぶような測り方はしていない）。

### 4-1. V17（畳みと開閉）

| 測ったこと | 実測 | 証跡 |
|---|---|---|
| **境界** | `3行: n=3 visible=3 folded=false moreHidden=true`／⭐ **`4行: n=4 visible=4 folded=false moreHidden=true`**／⭐ **`5行: n=5 visible=3 folded=true "▾ すべて表示（ほか 2 項目）"`**／`6行: visible=3 "ほか 3 項目"` | `rj3-cond.mjs`・`rj3-fold-5rows.png` |
| **V17 本体（8項目）** | `n=8 / visible=3 / "▾ すべて表示（ほか 5 項目）" / aria-expanded=false` → 開くと `visible=8 / "▴ 畳む" / aria-expanded=true` | `rj3-fold-per-tab.mjs`・`rj3-v17-folded.png`・`rj3-v17-open.png` |
| **タブごとの開閉** | 検索1(8行)を開く → 検索2(2行) → 検索1 で **`folded=false visible=8`**／畳んでから往復で **`folded=true visible=3`** | 同上 |
| **リロード復元** | 保存 `{"n":1,…,"open":true}` → リロード後 **`folded=false visible=8 aria=true`**（⭐ `open:false` の回は畳んだ状態で復元） | `rj3-v17-reload-open.png` |

⚠️ **畳みの判定は `class` ではなく `getComputedStyle(dt).display !== "none"` で数えている**
（CSS の `:nth-child(n+7)` で隠す実装なので、クラスだけ見ると「畳んだつもりで見えている」を検出できない）。

### 4-2. 375px（⭐ **自分で測り直した**）

| ケース | 箱の矩形 | 行数/表示 | 横あふれ |
|---|---|---|---|
| 1440px・ふつう（4行） | **810 × 137** | 4/4 | — |
| **375px・ふつう**（3行） | **343 × 108** | 3/3 | ⭐ **`documentElement` 0 / `body` 0 / `.cond-list` 0** |
| **375px・最悪**（10行・長い値） | ⚠️ **343 × 211**（畳んだ） | 10/3 | ⭐ **0 / 0 / 0** |
| 375px・最悪（開いた） | 343 × 510 | 10/10 | ⭐ **0 / 0 / 0** |
| 1440px・最悪（開いた） | 810 × 288 | 10/10 | ⭐ **0 / 0 / 0** |

- ⭐ **角丸は全ケース 10px**（丸ピルではない）。⭐ **`minmax(0,1fr)` が効いていて長い値でも横あふれ 0**。
- ⚠️ ⭐ **実装報告 §4 の「最悪ケースでも畳んだ 137px」は再現しなかった**（実測 **211px**）。
  **畳んでも先頭3行は折り返すので、畳んだ高さは値の長さで伸びる**。→ **S-3**（⚠️ **設計 §6 に数値要件は無いので不合格理由にはしない**）。
- 参考: 375px・ふつうの条件で、**結果タイルの先頭はパネル上端から 206px**（丸ピル実測の 583px と比べる位置）。

### 4-3. ⭐ 辞書を新設していないことの検算（M5）

**`fillChips()` が作るのと同じ形のチップ**（`<input value="NEWRARITY"><span>新レアリティ（NR）</span><em>NEWRARITY</em>`）を
レアリティ群に1つ足して選択 → 検索。

- 結果: **箱は `レアリティ｜新レアリティ（NR）`**。⭐ **`meta.rarities` / `meta.sets` に値が増えても、辞書を足さずに日本語名が出る。**
- ⚠️ **どこにも無い値**（保存が古い・選択肢が消えた）は **素のキーのまま出る＝fail-open**（`rj3-failopen.mjs`）:
  `エレメント（すべて）｜ZZZUNKNOWN かつ 火` / `レアリティ｜99` / `フォーマット｜NOSUCHFORMAT` / `エキスパンション｜NOSUCHPREFIX` / `並び｜nosuchsort ▼ 降順`。
  **pageerror 0件**・箱は開閉とも正常に動く。

### 4-4. 回帰（T1・T2 の挙動を壊していないこと）

| # | 実測 | 合否 |
|---|---|---|
| **V1** | `fire`→検索1(22件) / `water`→検索2(18件)。検索1へ戻して **22枚復元・左ペインが `fire` に戻る・`/cards/search` 2→2** | ✅ |
| **V2** | 固定タブの × = 0 / 検索タブ = 2（`aria-label` も正） | ✅ |
| **V3** | ⭐ **レビューが実測した**（⚠️ 実装報告 §3 は「未検証」と書いているが、**Escape で結果ダイアログを閉じてから × を押せば測れる**）。\[検索1..検索5] で**検索3（選択中・まん中）× → 検索4（右隣）**／**右端 × → 検索4（左隣）**／**全部 × → 🃏デッキ**（区切り線も消える） | ✅ |
| **V4** | 6回検索で \[検索2..検索6]・トースト「タブは5つまでです — いちばん古い「検索1」を閉じました」 | ✅ |
| **V5** | 全部閉じてから検索 → \[検索1]（⭐ **箱も `検索1 の条件` に戻る**） | ✅ |
| **V7** | 検索2 で ＋メイン → 検索1 で **`1枚`・`main=1`**・`/cards/search` 2→2 | ✅ |
| **V7-b** | 🃏デッキ のゾーンで ＋ → 検索1 が **`2枚`・`main=2`**／0まで減らすと **`0枚`・素の4ボタン** | ✅ |
| **V7-c** | 🎨 を `3・ALCSD`→`3・RDOA` に切替 → タブ往復後も **badge・`data-art-url`・`img.src` が一致** | ✅ |
| **V10** | ✕・すべて解除で **`/cards/search` 1→1**・タブ増えず・件数/タイル/先頭slug 同一。⭐ **条件の箱も1文字も変わらない**（箱は**タブのスナップショット**であってフォームの現在値ではない＝§2-3 の決定どおり・`rj3-v10-box.png`） | ✅ |
| **V12** | リロード → \[検索1,検索2]・選択=検索1・**`/cards/search` 1回だけ**・検索2を押すとそこで2回目 | ✅ |
| **V12-b** | 📊統計 を見た状態でリロード → 統計が `aria-selected=true`・検索タブは帯に残る・**`/cards/search` 0回**・**箱は `hidden`** | ✅ |
| **V18** | → で `📊統計 → 検索2 → … → 検索6 → 🃏デッキ → 📊統計`（各段階で `aria-selected=true` はちょうど1個）／Delete は検索タブで 5→4・固定タブで 4→4 | ✅ |
| **V19** | ⭐ **before/after の実測比較**（`22e1deba` を別ディレクトリへ `git archive` で展開し、**そこの `serve.mjs` を 3111 で起動**。⚠️ **既知の差で検算**: 旧 `result-cond` 0件 / 新 4件）。**1440px・375px とも `bandHTML`・子要素数・3要素の矩形・20個の computed style が IDENTICAL**（走査ルール 旧443 / 新**471**）。⭐ **`#view-deck` の中に `.cond-box` は 0個** | ✅ |
| **V21** | 差分は `tools/deck-builder/` の3ファイル＋報告のみ。**トップ・`shared/` は出ない**・`style=` 0件 | ✅ |
| **V25** | 検索2=18件 / 検索1=22件 で混ざらない（⭐ **箱のタイトルも `検索2 の条件` ⇄ `検索1 の条件` と一致して動く**） | ✅ |
| 番号なし | **トップ**（`sword` 27件・`.cond-box` 0個）・カード詳細モーダル・カード個別ページ・大会一覧454行 — **pageerror 0件** | ✅ |

⚠️ **すべての実行で `pageerror` は 0件。**

---

## 5. ⚠️ ⭐ M1-b（既存の同種の条件で効いている副作用）の判断

⚠️ **「参考」に落として合格にしない**ため、個別に判断した。**T3 は「表示する条件の種類」を増やす変更**なので、
**箱に出ない条件・箱と食い違う表示**が無いかを見た。

| # | 副作用 | 判断 |
|---|---|---|
| **条件の網羅** | ⭐ **`condFromForm()` が作りうる全パラメータが `condRows()` に1行ずつある**（コード＋実測）。⚠️ **「絞り込めるのに箱に出ない条件」は無い** | ✅ |
| **絵柄の追従（V22）** | ⚠️ **T5 まで残る既知の穴**（設計 §10-3 が「T5 が終わってなお混在なら M1-b の欠落＝不合格」と期限を明記）。**今周の不合格理由にしない** | 対象外 |
| **共有URL（V23）** | ⭐ **対象外**（デッキ構築に検索条件のURL共有が無い）。⚠️ ⭐ **ただし箱は `set` を prefix（`PRD 1st`）のまま保存し、表示名は実行時に引いている**＝**安定キーの要件を壊していない**ことを実測で確認した | ✅ |
| **バッジ集計（V24）** | ✅ ✕・すべて解除・チップ操作でバッジは増減する（§4-4 V10 と対） | ✅ |
| **件数表示（V25）** | ✅ タブごとに正しい。⭐ **箱・件数・タイルの3つが常に同じタブを指す**（`activeSearchTab` 1つで決まる） | ✅ |
| ⭐ **新しい値への追従** | ✅ **辞書を新設していないので `meta` の追加に自動で追従する**（§4-3 で実測）。⚠️ **「手で足す辞書」を増やしていない**のは、この単位で**むしろ穴を1つ減らしている** | ✅ |

---

## 6. 推奨基準の指摘（⚠️ **合格を妨げない**）

### **S-1: 隠れているときの「▾ すべて表示（ほか -1 項目）」**

- **実測**: 2行（畳めない）のタブで、`#result-cond-more` は `hidden=true` だが **`textContent` は `▾ すべて表示（ほか -1 項目）`**。
  3行のときは `ほか 0 項目`。⭐ **ボタンが `hidden` なので今は誰にも見えない**（`aria-expanded` も `false`）。
- ⚠️ ⭐ **T4 で結果パネルを右列へ移すときに `hidden` の扱いを1つ間違えると、そのまま画面に出る**。
  ⭐ **`foldable` でないときは文言を組み立てない**か、`rows.length - COND_FOLD_SHOW` を 0 下限にするのが安い。
- **今周の判断**: **表示されない文字列**なので、設計 §6 の要求（「▾ すべて表示（ほか N 項目）」）は満たしている＝**不合格理由にしない**。

### **S-2: CSS の `:nth-child(n+7)` と JS の `COND_FOLD_SHOW = 3` の対に自動検査が無い**

- **該当**: `tools/deck-builder/style.css` の `.cond-box.is-folded .cond-list > :nth-child(n+7)` と
  `tools/deck-builder/app.js` の `COND_FOLD_SHOW`。**片方だけ動かすと「N と実際に隠れている行数がずれる」**。
- ⭐ **両側にコメントがあり、現状は一致している**（実測で `visible=3` と `ほか N` が整合）。⚠️ **`npm run validate` はこの対を見ていない**（`grep` で0件）。
- ⚠️ ⭐ **CLAUDE.md の検査①②③（`onRemoveOne` / 左ペイン閾値 / スマホ帯）は、まさにこの型を検査にした前例**である。
  ⭐ **検査を足すかどうかは設計担当の判断**（レビューは起票しない）。⚠️ **足すなら「両辺が別ファイル」なので空回りはしない**。

### **S-3: 実装報告の「375px の最悪ケースでも畳んだ 137px」は再現しない**

- **実測**: 375px・10項目・値が長いケースで **畳んだ状態 211px**（ふつうの条件は 108px）。
  **畳んでも先頭3行は折り返すので、畳んだ高さは上限を持たない。**
- ⭐ **設計 §6 に数値要件は無く、丸ピル実測の 470px よりはるかに小さい**ので**不合格理由にしない**。
- ⚠️ ⭐ **ただし T4 の `min-height`・スクロール送り（§8-7）の設計に効く数字**なので、**T4 の設計で 211px を前提に置くこと**。

### **S-4: 箱の背景色が `rgba(217, 164, 65, .08)` のハードコード**

- `--accent` は `#d9a441`＝`rgb(217,164,65)` なので**いまは一致している**が、**トークンを変えても箱だけ追従しない**。
- ⭐ `color-mix(in srgb, var(--accent) 8%, transparent)` 等に寄せられる。⚠️ `validate` の `shared css tokens` はページCSSを見ないので**気づけない**。

### **S-5: 実装報告の「V3 は未検証（実クリックが阻まれる）」は正確でない**

- ⚠️ **結果ダイアログが帯を覆うのは事実**だが、**Escape で閉じてから × を押せば全経路を実測できる**
  （⭐ **レビューが実際に測って ✅**・§4-4 の V3。T1・T2 の再判定でも同じ手順で測っている）。
- ⭐ **今周は T3 が帯に触れていないので合否に影響しない**が、⚠️ **「測れない」と書くと次の周でも測られない**ので、
  **報告には「Escape を挟めば測れる」と書き直してほしい**。

### **S-6（良かった点・記録として残す）**

- ⭐ **辞書を新設せず DOM から引いた判断**（§4-3 で実測）。**CLAUDE.md の「手で足す辞書」を1つも増やしていない。**
- ⭐ **`renderCondBox()` の呼び出しを `syncSearchTabSelection()` の1か所に集約した**のは、
  §5-1-1（`restoreTiles()` の直後だけに置くな）で学んだ型をそのまま避けている。**T4 でも崩さないこと。**
- ⭐ **`orderLabel()` を `setSearchOrder()` と共用**したので、「ボタンは▲昇順・箱は▼降順」の食い違いが構造的に起きない。
- ⭐ **箱がフォームの現在値ではなく「タブのスナップショット」を表す**（V10 で実測）。
  **タイル・件数と常に同じタブを指す**ので、T1・T2 が直した P1 の考え方と一貫している。

### **⚠️ 持ち越し（今周の不合格理由にしていないもの）**

| # | 内容 | いつ |
|---|---|---|
| 前周の **S-1** | 裏のタブを × した後 `#result-count` に古い件数が残る | **T4** |
| 前周の **S-2** | `aria-controls` が `result-grid` のまま・`#result-grid` に `role="tabpanel"` が無い | **T4**（設計 §8-4-1） |
| **V22** | 絵柄の追従 | **T5**（⚠️ **T5 が終わってなお混在なら不合格**） |
| 新規 | ⭐ **`.cond-more` に `aria-controls`（`result-cond-list` を指す）が無い** | **T4 の ARIA と一緒に見るとよい**（S-2 と同じ場所） |

---

## 7. 設計担当への確認事項（⚠️ **レビューでは決めていない**）

⭐ **実装報告 §6 の4件はそのまま設計担当の判断待ち**である。⚠️ ⭐ **4件とも合否には影響しない**と判断した（理由を添える）:

| # | 確認事項 | ⭐ レビューの見立て（**決定ではない**） |
|---|---|---|
| **1** | 「↩ この条件を左ペインに読み込む」ボタンの要否 | ⭐ **設計 §6 に無いので、不実装は逸脱ではない**（＝合否に影響しない）。⚠️ 足すなら**375px で箱の見出し行が1行増える**（S-3 の 211px がさらに伸びる） |
| **2** | T4 で結果パネルを右列へ移すときの箱の扱い | ⭐ **「パネルの一部」として一緒に出し入れするのが、いまの実装と整合する**（箱・件数・タイルが `activeSearchTab` 1つで決まる）。⚠️ **T4 の設計に明記してほしい** |
| **3** | 「並び」を「並び替え」に揃えるか | ⭐ **設計 §6 の本文が「『並び』は常に最後の行」と書いている**ので、現状が設計どおり。⚠️ 揃えるなら §6 の本文も直す |
| **4** | 値が1つのとき「（いずれか）」を添えない挙動 | ⭐ **§6 の文は複数値の文脈と読めるので、現状を逸脱とは判定しなかった**。⚠️ **どちらが正かを §6 に1文足してほしい**（次の単位で読み違えないため） |

⚠️ **上記に加えて、S-2（畳みの閾値の自動検査を足すか）も設計担当の判断**を仰ぎたい。**レビューは起票しない。**

---

**次の担当: 合格＝`設計確認待ち`**（設計担当。⚠️ **最終承認とクローズ、および §10-5 のユーザー実物確認の段取りは設計担当が行う**）
