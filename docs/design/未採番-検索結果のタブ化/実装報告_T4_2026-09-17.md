**対象: 「検索結果のタブ化」**

# 実装報告 — 単位 T4（結果モーダルの撤去とパネルの移設・2026-09-17）

⚠️ **これは issueコメントの代替である。** GitHubアカウント停止中で `gh issue comment` が打てないため、ここに書く。
復旧して採番されたら、本文をそのまま
`gh issue comment <n> --body-file docs/design/未採番-検索結果のタブ化/実装報告_T4_2026-09-17.md` で投函する。

| | |
|---|---|
| 対象コミット | ⭐ **`f3c4e8a5`**（T4 本体）＋ **`533ff220`**（検証中に見つけた ARIA の後始末）。⚠️ **push していない** |
| 起点 | `3addcd11`（T3 合格）／指示は [`追加指示_T4の確定_2026-09-17.md`](追加指示_T4の確定_2026-09-17.md) |
| 設計書（正） | [`検索結果のタブ化_設計.md`](検索結果のタブ化_設計.md) **1.2** の **§9-2（8項目）** |
| 今周の範囲 | **T4 のみ**。⚠️ **T5（§8-1〜8-3＝絵柄の追従 V22・件数の描画先・エレメント警告）には入っていない** |
| 証跡 | `tmp/dev/検索結果のタブ化/`（gitignore。⚠️ レビュー担当の `tmp/review/…` は**読んで複写しただけ**で変更・削除していない） |

⚠️ **セッション上限で1度落ちたが、作業は失われていない**（落ちた時点の変更をそのままコミットし、続きから検証した）。

---

## 1. 実施内容

### 1-1. 設計 §9-2 の8項目

| # | やること | 実装 |
|---|---|---|
| **1** | 結果パネルを右列へ移す | `#result-modal` を**撤去**し、`#view-editor` の末尾に **`#ed-pane-search`（`.result-pane`）** を新設。中身の並びは**旧モーダルのまま**（条件の箱 → 件数 → グリッド → もっと見る）。1200px以上では既存の `#view-editor > * { grid-column: 2 }` で**自動的に右列**に入る（実測 `gridColumn: "2"`）。⭐ **条件の箱はパネルの一部として一緒に動かした**（§8-4-2 の5） |
| **2** | `el.resultModal` を4か所から外す | `closeModal()` の配列／`data-close` の登録リスト／Escape／カード詳細の `onAfterClose` の**4か所すべて**。⭐ **`el.resultModal` と `el.resultTitle` を `el` から削除**（`resultTitle` は T3 時点で**どこからも使われていなかった**＝旧モーダルの見出し。パネルの名前は `aria-labelledby` がタブから取る） |
| **3** | ARIA を仕上げる | パネルに **`role="tabpanel"`**／検索タブの `aria-controls` を **`ed-pane-search`**（旧 `result-grid`）へ／**`aria-labelledby` は表示中の検索タブのボタン id に追従**／**固定タブ表示中はパネルを `hidden`**／**`.cond-more` に `aria-controls="result-cond-list"`** |
| **4** | × で件数と「もっと見る」も空に | `clearResultPane()` を新設し、**タイルを捨てるのと同じ場所**（`closeSearchTab()` の `wasActive` 分岐・`clearSearchTabs()`）で呼ぶ |
| **5** | 畳めないときは文言を作らない | `!foldable` のとき `textContent = ""`。⚠️ **`hidden` に頼らず・0 下限で誤魔化さず「持たせない」**（§8-4-2 の2） |
| **6** | ⭐ **畳みの閾値の重複を消す** | **§2 に別項**（⭐ **消した**——検査は足していない） |
| **7** | 箱の背景を `--accent` 追従に | `color-mix(in srgb, var(--accent) 8%, transparent)`。⚠️ **`shared/` は触っていない・ページCSSに `:root` を書いていない**（`validate` の `:root owners in sync — 4ファイル` は変わらず） |
| **8** | `min-height` とスクロール送り | `.result-pane { min-height: 100vh }`（実測 `minHeight: "900px"`）。⭐ **`sendPageToTabs()` を新設し、検索の開始時（`onStart(reset)`）に呼ぶ**——**旧 `openModal(el.resultModal)` の置き換え** |

### ⭐ 1-2. 「面は3つのうち1つだけ」にした（⚠️ 設計に明示が無い部分の実装判断）

設計は **§8-4-1 の4「固定タブ表示中は結果パネルを `hidden`」**しか書いていないが、**逆向き**（検索タブ表示中にデッキ／統計の面をどうするか）は書かれていない。
⭐ **検索タブを選んだらデッキ・統計の面を `hidden` にした**（＝面は常に1つ）。根拠:

- ⭐ **§8-7 が結果パネルに `min-height: 100vh` を要求している**——**デッキ面を残したままだとページはもともと長く、この要求の前提（「検索中はパネルが空でページが短い」）が成り立たない**。
- ⭐ **§2-1 の③「PC・スマホとも**タブ**」**（タブは面を入れ替えるもの）。残すと右列が「デッキの下に検索結果が続く長いページ」になる。
- ⭐ 実装は `setupTabs()` に **`syncPanes(show)`** を1つ足しただけ（⚠️ **`select()` は変えていない**＝**閲覧専用画面（`#view-deck`）の `setupTabs` はこの関数を呼ばないので1pxも変わらない**＝V19 で実測）。

### ⭐ 1-3. スクロール送りの作り（§8-7-1）

```
送り先 = タブ帯の上端（getBoundingClientRect().top）− 編集バーが sticky ならその高さ
```

- ⚠️ ⭐ **条件の箱・結果タイルを基準にしていない**（箱の高さは可変＝§6-2）。**定数も持たせていない。**
- ⭐ **1200px以上で「タブ帯が画面の上半分に見えている」ときは動かさない**（§8-7・V16）。
- ⭐ **編集バーのぶんの逃げは `position === "sticky"` を実測して決める**（⚠️ **画面幅で分岐しない**——CSS 側で sticky になる条件が動いても追従する）。
- ⚠️ **`behavior: "smooth"` は使っていない**（既定＝即時）。§8-7 に指定が無く、即時なら送り先を実測で確定できるため（→ §6 の確認事項2）。

### 1-4. 旧モーダルの名前が残っている箇所（⚠️ **意図的な残置**）

進行役から指摘された「`app.js` に `resultModal` が1件・`index.html` に `result-modal` が1件」は、**どちらもコメントの中の履歴の記述**である（**コードは0件**）:

| ファイル | 行 | 中身 |
|---|---|---|
| `app.js` | 1643 | `// ⚠️ 旧 openModal(el.resultModal) の置き換え…`（`sendPageToTabs()` が何の後継かを書いた行） |
| `index.html` | 221 | `<!-- …⭐ 旧 #result-modal の中身をそのまま右列へ移した。…`（パネルの由来） |
| `style.css` | 333 / 1007 | 同上（`.result-pane` の由来と、**撤去した `#result-modal { left: 320px }` がなぜ不要になったか**） |

⭐ **残した理由**: `#result-modal { left: 320px }` は「左ペインをベールが覆う」という**過去の不具合の手当て**で、**器ごと消えたことを書き残さないと、次に似た症状を見た人が同じ手当てを足しに来る**。
⚠️ **消し忘れではない。** `el.resultModal` の**参照・`openModal`/`closeModal` の呼び出しは0件**（§3 の表に実測）。

### 1-5. ⚠️ 設計に無い小さな追加（375px の横 padding）

旧 `.modal-body` は **860px以下で `padding: 20px 16px` の全画面シート**だった。`.result-pane` を `.deck-pane` と同じ **24px** のままにすると、
**375px で結果タイルと条件の箱が左右 8px ずつ細くなる**（＝移設しただけで見た目が縮む）。
⭐ **既存の `@media (max-width: 620px)` に `.result-pane { padding: 16px 16px 48px }` を足して `.stats-pane` と揃えた**（⚠️ **新しい `@media` は作っていない**＝`validate` の検査②に触れない）。
- ⭐ これで **375px の箱の実測がレビューの値と一致した**（畳んで **211px**・開いて **510px**。§3 の V15-b）。

### 1-6. push の有無

⚠️ **していない**（§11-8 の公開方針どおり。本番公開は T5 完了後・大会データの週1更新と同時・ユーザー判断）。

---

## 2. ⭐ S-2（畳みの閾値の重複）をどちらで解決したか

### ⭐ **重複を消した**（＝検査は足していない）。設計 §8-4-3 の第1案。

| | before（T3） | after（T4） |
|---|---|---|
| `style.css` | `.cond-box.is-folded .cond-list > :nth-child(n+7) { display: none }` | `.cond-box.is-folded .cond-list > .cond-extra { display: none }` |
| `app.js` | `COND_FOLD_SHOW = 3`（**CSS の 7 と対**） | `COND_FOLD_SHOW = 3` から **`i >= COND_FOLD_SHOW` の行に `.cond-extra` を付ける** |

- ⭐ **`grep -n 'nth-child' tools/deck-builder/style.css` → 0件**（コメントに書いた説明文からも `nth-child` の語を外した。⚠️ **語が残ると grep が 1 件になり、検証が「消えていない」と読める**）。
- ⭐ **CSS 側に行数が現れなくなった**＝**片方だけ動かしてずれる余地が無い**（⚠️ `COND_FOLD_SHOW` を変えれば印の付く行がそのまま動く）。
- ⭐ **畳みの境界は変えていない**（3行・4行=畳まない／5行=`ほか 2 項目`／8行=`ほか 5 項目`。§3 の V17-c で実測）。
- ⭐ **印の数と実際に隠れている数が一致すること**も実測した（`印の数 10 / 実際に隠れている数 10 / 全要素 16`）。

⚠️ **検査（`K === SHOW*2+1` の突き合わせ）は足していない**ので、破壊試験の報告は無い（＝§8-4-3 の「消せたときは検査を足さない」に該当）。

---

## 3. 検証結果（設計 §10 の番号に対応）

⭐ **到達手順は #51 の標準手順**（`/api/me`・`/api/decks`・`/api/decks/<id>`・`/api/decks/<id>/cards` だけを `context.route` で差し替え、
`#edit/DEV1` へ遷移して**ページ本来の `openEditor()` → 左ペイン → 🔍検索 → タブ**を動かす。⚠️ **内部変数は書き換えていない**）。
Playwright 1.61.1 / `launch({ args: ["--ignore-certificate-errors"] })` ＋ context `ignoreHTTPSErrors: true`。静的プレビュー **3000番**（⚠️ **検証後に停止済み**）。
⭐ **ハーネスはレビュー担当の `harness2.mjs` / `rj-*.mjs` を自分のフォルダへ複写して流用**（BASE と deckId だけ変更）。

### 3-1. 今周の主目標

| # | 項目 | 結果 | 実測 |
|---|---|---|---|
| **V13** | 数値ソート・絞り込みなしで**進捗表示がタブの中に出る** | ✅ | `検索中…` → **`読み込み中 1/50 ページ…` → `7/50` → `13/50` …** → `24 件を表示 / 全 133 件（レベルを持つカードのみ）`。**全部 `#ed-pane-search` の中**（`inPane=true`・`paneHidden=false`）。50リクエスト |
| **V14** | 日本語モード | ✅ | 「ドラゴン」→ `1 件を表示（日本語一致・翻訳済みのみ）`／0件文言も従来どおり（`日本語テキストに一致する翻訳済みカードが見つかりませんでした…`）。⭐ **裏面一致の注記（#46）も実測**——「滑空するリス」（裏面）で **`🔄 裏:滑空するリス` がパネルの中に出て、左端がカード名と一致**（Range 実測で **353 = 353**。⚠️ 矩形でなくテキストの実描画位置で測った） |
| **V15** | 375px で検索を3回 | ✅ | **3回とも `scrollY 0 → 677`・送り後の `bandTop = 0`**（同じ位置）。⭐ **固定2つは動かず検索タブだけ横に送れる**（`scrollLeft 249 → 0`・`deckX` は 16 のまま・`fade-l`↔`fade-r` が入れ替わる）。**横あふれ 0**（`documentElement.scrollWidth = innerWidth = 375`） |
| **V15-b** | ⭐ **375px・条件10項目・長い値** | ✅ | **畳んだ箱 211px**（⭐ **レビューの実測と一致。137px にはならない**）・**開いて 510px**・**横あふれ 0**。⭐ **箱が 86px → 211px と 125px 変わっても、送り後のタブ帯の位置は `bandTop = 0` で同じ**（⚠️ `scrollY` の絶対値は左ペインの高さが変わるので違う。**基準はタブ帯**） |
| **V16** | 1200px / 1440px で検索 | ✅ | **帯が上半分（`bandTop=178`・画面の半分=450）にあるとき `scrollY 0 → 0`＝動かさない**。⭐ 帯が画面外（`scrollY=2000`）のときだけ送る（`bandTop -1822 → 57`＝編集バーのぶん手前で止まる） |
| **V17-c** | ⭐ **畳みの重複が消えた** | ✅ | **`grep -n 'nth-child' tools/deck-builder/style.css` → 0件**。境界は不変（3/4=畳まない・5=`ほか 2 項目`・8=`ほか 5 項目`）。**印 10 ＝ 実際に隠れている 10** |
| **V17-b** | ⭐ **`ほか -1 項目` を組み立てない** | ✅ | 2行のタブで **`#result-cond-more` の `textContent` が `""`**。⭐ **`#view-editor` の全要素を走査して `/ほか\s*-?\d+\s*項目/` に当たる文字列 0件**（⚠️ `hidden` だから見えない、ではない） |
| **V17-d** | ⭐ **背景が `--accent` に追従**（破壊試験） | ✅ | `--accent` を `#00ff00` に差し替えると箱の背景が **`color(srgb 0.85 0.64 0.25 / 0.08)` → `color(srgb 0 1 0 / 0.08)`** に変化し、戻すと元に戻る。⚠️ **参考: 左ペインの「選択中の条件」は直値のままで追従しない**（＝**本件の範囲外**。→ §6 の確認事項1） |
| **V18-b** | ⭐ **ARIA の仕上げ** | ✅ | パネル `role="tabpanel"`／検索タブの `aria-controls` は **2つとも `ed-pane-search`**／**`aria-labelledby` が `ed-tab-search-2` → `ed-tab-search-1` と追従**／**固定タブ表示中は `hidden: true`**／`.cond-more` の `aria-controls="result-cond-list"` |
| **V25-b** | ⭐ **× のあとの件数** | ✅ | **裏のタブ（統計を見ている間）を × → `#result-count` が `""`・「もっと見る」`hidden`・タイル 0**／**選択中の最後の1つを × → 同じく `""`**（⚠️ `18 件を表示` は残らない） |
| **§8-4 の跡片づけ** | `el.resultModal` の4か所 | ✅ | **コード上の参照 0件**（残るのはコメント4行の履歴記述＝§1-4）。`#result-modal` は**DOM に存在しない**（`resultModal: false`） |
| **`body { overflow: hidden }`** | 検索では起きない | ✅ | 検索後 `body.style.overflow = ""`・`getComputedStyle` は `visible`。⭐ **モーダル側は従来どおり**——omnidex を開くと `hidden`、**Escape でも × でも `""` に戻る**（`closeModal()` の配列から外した影響が無いことの実測） |
| **カード詳細** | `onAfterClose` の回帰 | ✅ | 開くと `overflow: hidden` → Escape で閉じて `""`。⭐ **その後もう一度 Escape を押しても例外なし**（旧コードは `el.resultModal.hidden` を読んでいた） |

### 3-2. 回帰

| # | 結果 | 実測 |
|---|---|---|
| **V1** | ✅ | 2回検索でタブ2つ・検索1へ戻しても `/cards/search` は **2→2**（件数 22 / 18 が入れ替わる） |
| **V2** | ✅ | 固定タブの × は 0個・検索タブは2個（`aria-label="「検索1」を閉じる"`） |
| **V3** | ✅ | ⭐ **モーダルが無くなったので実クリックで全経路を測れた**——まん中を × → **右隣（検索4）**／右端を × → **左隣**／全部閉じる → **デッキタブ `aria-selected=true`**。⚠️ **T3 報告の「実クリックが阻まれる」は T4 で解消**（設計 §10-1 の上書きどおり） |
| **V3-b** | ✅ | 🃏デッキ を見ている間に裏の検索タブを × → **`ed-tab-deck` は true のまま・パネルは `hidden` のまま・ほかの検索タブは選択されない**。**最後の1つでも同じ**。`/cards/search` 増えず |
| **V4** | ✅ | 6回検索でタブ5つ・最古が閉じる・トースト・連番は `検索2〜検索6` |
| **V5** | ✅ | 全部閉じると区切り線も条件の箱も消え、再検索で **「検索1」** に戻る |
| **V7 / V7-b / V7-c** | ✅ | 別タブ投入後に `1枚`・`− 1 ＋`（`/cards/search` 増えず）／ゾーン増減経由でも `2枚`→`0枚`／🎨 で選んだ版（`RDOA`）は往復後も維持 |
| **V8 / V9** | ✅ | 並び替えでタブは増えず箱の「並び」とタブの `title` が同時に変わる／タブ0個で並び替えると新しいタブができる（`9 件を表示（コストを持つカードのみ）`） |
| **V10 / V24** | ✅ | ✕・チップ操作で **`/cards/search` 1→1**・タブ増えず・件数/タイル/先頭slug すべて同一。バッジは 2→1 と動く |
| **V11（P1）** | ✅ | `sword` で結果を出したまま左ペインを `of` に書き換えて「もっと見る」→ **27件すべて sword**・2ページ目URLは `name=sword`。⭐ **左ペインと結果パネルは横に並び重ならない**（`pane [0,320] / panel [320,1440]`・ペイン上の `elementFromPoint` は `s-name`） |
| **V12 / V12-b** | ✅ | 保存から2タブ復元し最後のタブだけ再検索（1回）・**条件の箱の開閉もタブごとに復元**（`open:false`→畳んだ／`open:true`→開いた）／統計で復元すると **`/cards/search` 0回**・パネルは `hidden` |
| **V17** | ✅ | 8項目で **3項目＋「▾ すべて表示（ほか 5 項目）」**・開閉はタブごとに残る |
| **V18** | ✅ | →で 検索2〜検索6→🃏デッキ→📊統計 と巡回し `aria-selected` は常に1つ。Delete は検索タブで閉じ（5→4）、固定タブでは何も起きない |
| **V19** | ✅ | ⭐ **`rj-v19b.mjs` の形で before(3010=`3addcd11`)/after(3000) を比較 → 1440px・375px とも `IDENTICAL`**。⚠️ **走査ルールは 旧471 / 新473 で 0 ではない**（＝前任の空回りではない）。当たったセレクタは `.deck-tabs` 系のみで**新規ルールは1つも当たっていない**。⚠️ **原本の `if(!same) for… else` は「ぶら下がり else」で一致時に詳細が1行も出なかったので、自分の複写ではそこだけ直した**（判定ロジックは変えていない） |
| **V20** | ✅ | `npm run validate` **exit 0**。`onRemoveOne wiring in sync — 渡しているのは 0ファイル`（⚠️ **設計どおり。回帰ではない**）・`pane threshold in sync — 2ページ`・`mobile band marker in sync — 1組`・`:root owners in sync — 4ファイル` |
| **V21** | ✅ | `git -c core.quotepath=false diff --name-only 3addcd11..HEAD` = `tools/deck-builder/{app.js,index.html,style.css}` ＋ 設計書2件。**トップ・`shared/` は出ない** |
| **V25** | ✅ | 件数はタブごと（検索2=18 / 検索1=22）で混ざらない |
| **主要画面のスモーク** | ✅ | トップ（`sword` 27件）・カード個別（`/cards/adept-swordmaster/`）・大会一覧（455行）とも **pageerror 0件** |

- ⭐ **すべての実行で `pageerror` 0件。**

### 3-3. ⚠️ 測定が空回りしていないことの確認

- **V19**: 本測定の前に**既知の差で検算**した（`curl` で `id="result-modal"` が **3010=1 / 3000=0**、`id="ed-pane-search"` が **3000=1**）。⭐ **同じツリーを2回測る「偽の緑」ではない**。
- **omnidex モーダルが開かない件**: 最初、空のデッキでは `#ed-copy-text` を押してもモーダルが開かず「T4 で壊したか」を疑った。
  ⭐ **before（3010＝T3）でも同じ**であることを確認し（**対照実験**）、**カードを1枚入れれば開く**ことも確認した＝**本件の影響ではない**。
- **V17-b**: `hidden` を見ずに **`textContent` を直接読み、さらに `#view-editor` 全体を正規表現で走査**した。
- **V17-d**: 目視の色合わせではなく **`--accent` を差し替える破壊試験**で確かめた。

---

## 4. 設計書の想定と食い違った差分

| # | 食い違い | 内訳と根拠 |
|---|---|---|
| 1 | **375px の箱の高さがレビューの実測と一致した**（畳んで 211px・開いて 510px） | ⭐ **一致させるために §1-5 の padding を揃えた**。⚠️ **揃える前は 327px 幅（24px padding）で開いたときだけ 529px** だった（19px 高い）。**幅が狭いほど折り返しが増える**という §6-2 の性質どおり |
| 2 | **V15-b の「送り先が同じ位置」の読み方** | ⚠️ **`scrollY` の絶対値は一致しない**（左ペインの高さが条件の数で変わるため、タブ帯のページ上の位置そのものが動く）。⭐ **設計が求めているのは「送り先の基準が箱ではないこと」**なので、**送り後の `bandTop`（＝画面上のタブ帯の位置）が一定であること**で示した（86px の箱でも 211px の箱でも **`bandTop = 0`**） |
| 3 | **`el.resultTitle`（旧モーダルの見出し「検索結果」）を復活させていない** | T3 時点で**どこからも参照されていなかった**。設計 §3・§9-2 のパネルの構成要素にも**入っていない**（条件の箱・件数・グリッド・もっと見るの4つ）。⭐ **パネルの名前は `aria-labelledby`（タブのボタン）が持つ**ので、見出しが無くても支援技術には名前が届く |

---

## 5. 設計書からの逸脱と根拠

| # | 逸脱 | 根拠 |
|---|---|---|
| 1 | ⭐ **検索タブ表示中はデッキ・統計の面も `hidden` にした**（設計は逆向きしか書いていない） | **§1-2 に理由**（`min-height: 100vh` の前提・③「タブ」の意味）。⚠️ **判断が要るなら差し戻してほしい**（→ §6 の確認事項3） |
| 2 | **`@media (max-width: 620px)` に `.result-pane` の padding を1行足した** | §1-5。**移設で見た目が縮むのを防ぐため**。⚠️ **新しい `@media` は作っていない**（`validate` の検査②は `min-width` の集合と `max-width < 1200` を見るので影響なし） |
| 3 | **スクロール送りは `behavior: "smooth"` を使わず即時** | §8-7 に指定が無い。⭐ **即時なら送り先を実測で確定できる**（`behavior:"smooth"` だと検証が待ち時間に依存する）。→ §6 の確認事項2 |
| 4 | **旧モーダルの名前をコメントに残した**（`result-modal` / `resultModal` 各1〜2件） | §1-4。⚠️ **コードの参照は0件**。⭐ **`#result-modal { left: 320px }` を撤去した理由を残さないと、同じ手当てが将来足し直される** |
| 5 | **`aria-labelledby` は表示中の検索タブが無いとき属性ごと外す**（設計は「切り替える」までしか書いていない） | ⚠️ **閉じたタブのボタン id を指したままだと、DOM に無い要素を指す idref になる**（実測で `ed-tab-search-6` が残っていた）。パネルは `hidden` なので画面には出ないが、**宙に浮いた参照を残す理由が無い**（コミット `533ff220`） |

⚠️ **`TRANSLATION.md`・既存表記との照合**: **新しい文言は1つも増やしていない**（T4 は器の移設と後始末で、画面に出る日本語は T3 のまま）。

---

## 6. 設計担当への確認事項（⚠️ 実装側では決めていない）

| # | 確認事項 |
|---|---|
| **1** | ⭐ **左ペインの「選択中の条件」の背景も `rgba(217,164,65,.08)` の直値のまま**である（V17-d の参考で実測。`#s-selected-filters.has-selection` と `#s-selected-filters-list button:hover` の2か所）。**S-4 と同じ型の指摘が残っている**が、**本件（検索結果のタブ化）の範囲外**と判断して触っていない。⚠️ **別タスクとして起票するかは設計担当の判断**（⭐ **起票は設計担当のみ**なので、こちらでは何もしていない） |
| **2** | **スクロール送りを「なめらか（`behavior: "smooth"`）」にするか。** いまは即時。⭐ **ユーザーの実物確認（§10-5 の3）で感触を見てもらうのがよいのでは**（⚠️ T4 の時点で決めなくても、T5 の周で1行で変えられる） |
| **3** | ⭐ **§1-2 の実装判断（検索タブ表示中はデッキ・統計の面も `hidden`）の追認をお願いしたい。** 設計 §8-4-1 は「固定タブ表示中はパネルを `hidden`」しか書いておらず、逆向きは書かれていない |
| **4** | ⚠️ **T5 で `min-height: 100vh` の再確認が要るか。** いまは検索中でもページが 100vh 以上あるので送りは効くが、**T5 で件数の描画先を引数化（§8-2）したときに空パネルの高さの前提が変わらないか**は T5 の周で見たほうがよい |

---

## 7. 残っている確認

- **T5（§8-1〜8-3）**: ⚠️ **V22（絵柄の追従）は T5 まで既知の穴**（設計 §10-3 が期限を明記）。本件では触れていない。
- **§10-5 のユーザー実物確認**（レビュー合格後・承認の前・**3000番**）: ⭐ **T4 で「結果が右列に出る」形が初めて見えるので、1・3・6 の項目はここで効く**。
- **§11-8 の公開判断**: ⚠️ **T5 まで終わってからまとめて**（今日は公開しない）。

---

**次の担当: レビュー担当（ラベル相当 `レビュー待ち`）**

- ⭐ **対象コミットは `f3c4e8a5` と `533ff220`**（⚠️ **未push**）。差分は `tools/deck-builder/` の3ファイルだけ。
- ⚠️ **`validate` の検査①が「0ファイル」なのは設計どおり**（§8-6）。**回帰と読まないこと。**
- ⭐ **証跡スクリプトは `tmp/dev/検索結果のタブ化/`**（`t4-core.mjs`・`t4-scroll.mjs`・`t4-v15b.mjs`・`t4-v17cd.mjs`・`t4-v13v14.mjs`・`t4-v14b.mjs`・`t4-modals.mjs`・`t4-smoke.mjs`・`v19b.mjs`・`regress.mjs`・`v3.mjs`・`v7.mjs`・`v10v24.mjs`・`v11v9.mjs`・`v12.mjs`・`v12b.mjs`・`v5.mjs`・`v8.mjs`・`v17.mjs`・`vfold-boundary.mjs`、出力は `out-*.txt`）。**流用してよい。**
  ⚠️ **`v19b.mjs` は before 側に 3010番のサーバーが要る**（`git archive <T3のコミット> | tar -x -C <別ディレクトリ>` してから**そのツリーの `serve.mjs`** を `PORT=3010` で起動する。⚠️ `cd` では配信ルートが変わらない）。
