**対象: 「検索結果のタブ化」**

# レビュー判定 — T5（本タスク最後の実装単位）2026-09-17

⚠️ **これは issueコメントの代替である。** GitHubアカウント停止中で `gh issue comment` が打てないため、ここに書く。
復旧して採番されたら、本文をそのまま
`gh issue comment <n> --body-file docs/design/未採番-検索結果のタブ化/レビュー判定_T5_2026-09-17.md` で投函する。

⭐ **この判定は、実装した開発担当とは別のセッション（レビュー担当）が、コンテキストを共有しない状態で行った。**
判断材料は**設計書 1.3・追加指示・コミットの中身・自分で操作した実測**だけで、
⚠️ **実装報告の「検証済み」は合格の根拠にしていない**（V22 は before/after を自分で測り直した）。

| | |
|---|---|
| 対象コミット | **`6f56a040`**（本体＋コメント是正）／**`395d61e6`**（コメントのみ）／`b3b3392d`（報告）。起点 `c94bac53` |
| 設計書 | [`検索結果のタブ化_設計.md`](検索結果のタブ化_設計.md) **1.3**（§8-1 / §8-1-1 / §8-1-2 / §8-2・§8-3 / §8-4-1 の6 / §8-4-3-1 / §9-3 / §10-2 / §10-3） |
| 指示 | [`追加指示_T5の確定_2026-09-17.md`](追加指示_T5の確定_2026-09-17.md) |
| 報告 | [`実装報告_T5_2026-09-17.md`](実装報告_T5_2026-09-17.md) |
| 差し戻し回数 | **0回**（T5 は1周目で合格） |
| 証跡 | `tmp/review/未採番-検索結果のタブ化/t5/`（gitignore。スクリプト・JSON・スクリーンショット） |

---

## 判定: ⭐ **合格**

| 基準 | 結果 | 証跡 |
|---|---|---|
| **M1 設計適合** | ✅ | ⭐ **V22 の4条件すべて合格**（§10-3。before/after で差が出ている＝下表）。V1〜V25-c の全番号に合否を付けた（§2）。⭐ **M1-b の4つ（絵柄の追従 V22 / 共有URL V23 / バッジ集計 V24 / 件数表示 V25）はすべて実測済み**——**「参考」に落としたものは1件も無い** |
| **M2 実駆動** | ✅ | ⭐ **レビュー担当自身が 3100 番のサーバーに Playwright で接続して操作**（PC 1440px・**375px** の両方）。`t5/t5-375-v15.png` `t5-375-v15b.png` `t5-375-v14b.png` `t5-pc-1440.png` `t5-v7c.png` `t5-v18c-stats.png` `t5-v13.png` `t5-v12b-stats.png` `t5-smoke-top-detail.png` `t5-smoke-tournaments.png` |
| **M3 回帰** | ✅ | `npm run validate` **exit 0**（§2 の V20）。主要画面のスモーク（`t5/smoke.json`）＝トップ27件・**カード詳細モーダル**（`#gacd-modal` が開き「熟達の剣豪」）・カード個別ページ（節5）・大会一覧（454行）・デッキ構築（全項目）。⭐ **`pageerror` は全スクリプトで 0件** |
| **M4 生成物の同期** | ✅ | ⭐ **生成ロジックに触れていない**（変更は `tools/deck-builder/app.js` のみ＝実行時コード）。訳の追加も無し。`validate` の `generated JSON up to date` / `card term highlights up to date — 2495 ページ / 2517 節` が緑 |
| **M5 静かな劣化がない** | ✅ | ⭐ **T5 は検査を足していない**ので「両辺が同源で空回り」は発生しない。⚠️ ⭐ **代わりに、この単位が持ち込みうる無言の劣化＝「固定した `artCond` が古くなる」を構造と実測の両方で潰した**（§3）。⭐ **測定側の空回りも潰した**——before/after の2サーバーを**既知の差**（`app.js` の `artCondOf` が 旧0件 / 新3件）で検算してから本測定した。V19 の走査ルールも **旧473/新473**（0件ではない） |
| **M6 公開安全性** | ✅ | `git diff --name-status c94bac53..HEAD` は **`M tools/deck-builder/app.js` と `A` 設計書1枚だけ**（新規の配信物・削除は無し）。`_headers` / `404.html` / `functions/` は**0ファイル**。差分に秘匿情報の混入なし（`token|secret|password|api_key|.dev.vars` で 0ヒット） |
| **M7 スコープ** | ✅ | **V21**: 変更は `tools/deck-builder/app.js` 1ファイル。⭐ **トップの `app.js` / `index.html` / `style.css` / `shared/` / `scripts/` は差分 0行**（`git diff c94bac53..HEAD -- app.js index.html style.css shared scripts | wc -l` → **0**）。`#view-deck` は V19 で 1px も変わらないことを確認 |

---

## 1. ⭐ V22（本単位の合否・§10-3）— **4条件すべて合格**

⭐ **測り方**: `t5/t5-v22.mjs`。⚠️ **判定はページの実装に依存させない**——
**`api.gatcg.com` の応答を Playwright 側で捕まえて `slug → editions[{url, prefix, rarity}]` を自前で作り、
タイルの `dataset.artUrl` を突き合わせた**（🎨バッジの文字列だけを数える方法より強い）。
⭐ **before は `c94bac53` を `t5/before-tree` へ `git archive` で展開し、そこの `serve.mjs` を 3111 で起動**した
（⚠️ `cd` では配信ルートが変わらないため。⭐ **起動後に「既知の差」で検算済み**）。

| # | 条件 | before（`c94bac53`） | ⭐ after（`395d61e6`） | 判定 |
|---|---|---|---|---|
| **①** | PRD で検索（`24 件を表示 / 全 232 件`）→ **🔍を押さず**左ペインだけ **PRD 1st** →「もっと見る」（48枚） | ⚠️ **🎨バッジ `{PRD:10, PRD 1st:2}`**／**`dataset.artUrl` の版 `{PRD:46, PRD 1st:2}`**（`augustine-votary-of-yore` / `camil-basked-abundance`） | ⭐ **🎨バッジ `{PRD:12, バッジ無し:36}`**／⭐ **`dataset.artUrl` の版が 48枚すべて `PRD`**（PRD 以外 **0件**） | ✅ |
| **②** | PRD と PRD 1st の2タブを**行き来してから**それぞれ「もっと見る」 | 続き `PRD 以外 0` / `PRD 1st 以外 0` | ⭐ **同じ**（タブ1の続き24枚に PRD 以外 0・タブ2の続き3枚は全部 `PRD 1st`）＝**回帰なし** | ✅ |
| **③** | ⚠️ ⭐ **レアリティ**（P5 の再発確認）: SR(4) で検索 → **🔍を押さず**左ペインだけ **コモン(1)** →「もっと見る」 | ⚠️ **続きの🎨持ち20枚の表示中レアリティが `{1:4, 2:1, 4:15}`**＝**SR版を持つのに SR で出ていないもの 5件**（`assassins-ripper` `bauble-of-abundance` `beastbond-boots` `beastbond-ears` `beastbond-paws`） | ⭐ **1ページ目18枚・続き20枚とも `{4:...}` のみ**＝**該当 0件** | ✅ |
| **④** | 絞り込み無し（`sword` 24件） | `imgs[0]` でないもの 0件 | ⭐ **同じく 0件**＝**回帰なし** | ✅ |

⚠️ ⭐ **①だけでなく③でも差が出ている**——**prefix だけ直して満足していないこと**を、レビュー側でも独立に確認した（§8-1-1 の4）。
⭐ **①の `バッジ無し:36` は版が1つしかないカード**で、そこも `dataset.artUrl` を自前マップで引いて **PRD であること**を確かめた
（＝**バッジの数え方に逃げ道を作っていない**）。

**→ §8-1-2 の「T5 が終わってなお混在するなら M1-b の欠落＝不合格」という期限つきの穴は、ここで閉じた。**

---

## 2. 設計書の検証項目との対応（§10-2・§10-3）

⚠️ **「未検証」は1件も無い。**

| # | 期待 | 実測 | 証跡 | 判定 |
|---|---|---|---|---|
| **V1** | タブ2つ・前の結果が残り再検索しない | 検索1→検索2 でタブ2つ。検索1 を押しても **`/cards/search` 増分 0**・`24 件を表示 / 全 27 件`・grid 24・`aria-selected=true` は1つ | `core.json` | ⭕ |
| **V2** | 固定タブに × が無い | `🃏 デッキ` / `📊 統計` とも `.tab-close` **0個**（帯直下にも0）。検索タブ側は1個 | `core2.json` | ⭕ |
| **V3** | 選択中を × → 右隣へ | 検索3 を選んで × → `["検索1","検索2","検索4*"]`・`aria-selected=true` は1つ | `core.json` | ⭕ |
| **V3-b** | デッキを見ている間に裏を × | `ed-tab-deck` が **true のまま**・検索タブは1つも選択されない・`ed-pane-search` は `hidden` のまま。**最後の1つを × しても同じ** | `core.json` | ⭕ |
| **V4** | 6回検索でタブ5つ・最古が閉じる・連番継続 | `["検索2"…"検索6"]`・トースト **`タブは5つまでです — いちばん古い「検索1」を閉じました`** | `core.json` | ⭕ |
| **V5** | 全部閉じてから検索で `検索1` | `["検索1"]` | `core.json` | ⭕ |
| **V6** | 投入でトースト・バッジ・追加行 | ＋メイン → **`✅ 「熟達の剣豪」をメインデッキに追加 — 1 / 60以上`**・`1枚`（`hidden` でない）・`− 1 ＋` のステッパー | `deck.json` | ⭕ |
| **V7** | 別タブを開くとバッジが反映・取得しない | 検索1 へ切り替え → `1枚`・`main` ステッパー・⭐ **`/cards/search` 増分 0** | `deck.json` | ⭕ |
| **V7-b** | 固定タブ経由でも反映 | 🃏デッキ で ＋ → 検索1 へ戻す → **`2枚`** | `deck.json` | ⭕ |
| **V7-c** | 🎨 の選択が往復で戻らない | `archon-broadsword` を `ALCSD → RDOA` に切替 → **別の検索タブ往復**でも **🃏デッキ 経由**でも `artUrl` / `badge` / `src` の3つとも **RDOA のまま**。⭐ **同じカードの別タブ側は ALCSD のまま独立** | `v7c.json` / `t5-v7c.png` | ⭕ ⭐ |
| **V8** | 並び替えでタブは増えず並べ替わる | タブ1つのまま・先頭 `adept-swordmaster → warriors-longsword`・条件の箱の「並び」が `名前順 ▲ 昇順 → ▼ 降順` | `stale.json` C | ⭕ ⭐ |
| **V9** | タブ0個で並び替え → 新タブ | `[] → ["検索1*"]`（1つできる） | `stale.json` B | ⭕ ⭐ |
| **V10** | ✕・すべて解除で再検索しない | ⭐ **`/cards/search` 増分 0**・件数 `3 件を表示` 不変・grid 3 不変・**条件の箱の行が完全に不変**・タブ数不変 | `misc2.json` | ⭕ |
| **V11（P1）** | 左ペインを書き換えて「もっと見る」でも混ざらない | `sword` 27件のまま **27枚すべて sword**（`notSword: 0`） | `misc.json` | ⭕ |
| **V12** | リロードで最後のタブだけ再検索 | タブ3つ復元・**`/cards/search` 1回**。別タブを押すと **+1** | `misc.json` | ⭕ |
| **V12-b** | 統計を見た状態でリロード | 統計タブが選択・検索タブ3つは帯に残る・⭐ **`/cards/search` 0回**・パネル `hidden` | `misc.json` / `t5-v12b-stats.png` | ⭕ |
| **V13** | 数値順の進捗がタブの中に出る | **`読み込み中 1/3 ページ…`** を観測・最終 `24 件を表示 / 全 133 件（レベルを持つカードのみ）`・⭐ **`#result-count` は `#ed-pane-search` の中** | `misc.json` / `t5-v13.png` | ⭕ |
| **V14** | JP の文言・裏面一致の注記 | `剣豪` → `1 件を表示（日本語一致・翻訳済みのみ）`／`リス` → **`🔄 裏:滑空するリス`** が出てそのタイルは **`🔄 裏面`**（裏面で開く） | `misc.json` | ⭕ |
| **V14-b** | 375px エレメントAND 0件・送り先がずれない | ①件数欄に `ELEMENT_AND_MESSAGE` ②群が `open`・`.fwarn` が可視・`filters-open`・`aria-expanded=true`・`絞り込み ▲` ③⭐ **左ペインが 302 → 1210px に伸びても `bandTop === 0`**（`scrollY 1585`） | `mobile.json` / `t5-375-v14b.png` | ⭕ |
| **V15** | 375px で毎回タブ帯まで送る・横あふれ0 | 3回とも **`scrollY 677` / `bandTop 0` / 横あふれ 0**。固定タブは `deckLeft 16` のまま帯だけ横に送れる | `mobile.json` / `t5-375-v15.png` | ⭕ |
| **V15-b** | 多項目でも畳まれ、送り先が変わらない | 9行で **`cond-box is-folded`・`▾ すべて表示（ほか 6 項目）`・見えている行 3**・横あふれ 0・⭐ **送り先は少項目と同じ `scrollY 677`** | `mobile.json` / `t5-375-v15b.png` | ⭕（※S-4） |
| **V16** | 1440px・帯が上半分ならページを動かさない | `scrollY 0` のまま（`bandTop 178`） | `mobile.json` | ⭕ |
| **V17** | 8項目 → 3項目＋「ほか5項目」・開閉がタブごと | 8行 → 見えている行3・**`▾ すべて表示（ほか 5 項目）`**。開くと **`▴ 畳む`・`aria-expanded=true`・8行**。⭐ **検索2（2行・畳まない）へ移って戻すと、検索1 は開いたまま** | `v17.json` | ⭕ |
| **V17-b** | 畳めないタブは文言を組み立てない | 2行・4行とも **`moreText` が空文字**（`ほか -1 項目` / `ほか 0 項目` をどこにも作っていない）・ボタンは `hidden` | `core2.json` | ⭕ |
| **V17-c** | `style.css` に行数が現れない | `grep -n 'nth-child' tools/deck-builder/style.css` **0件** | 下の V17-e と同コマンド | ⭕ |
| **V17-d** | 箱の背景が `--accent` に追従（破壊試験） | `--accent` を `rgb(0,255,0)` に差し替えると `color(srgb 0.851 0.643 0.255 / 0.08)` → **`color(srgb 0 1 0 / 0.08)`** | `mobile.json` | ⭕ |
| **V17-e** | ⭐ フォルダ全体で `nth-child` 0件・畳みの境界不変 | ⭐ **`grep -rn 'nth-child' tools/deck-builder/` → 0件**（exit 1）。⭐ **境界は不変**: **3行/4行＝畳まない（`moreText` 空）／5行＝`ほか 2 項目`／8行＝`ほか 5 項目`**（どれも見えている行は3） | `core2.json` | ⭕ |
| **V18** | ←/→・Delete | `検索4* → 検索2*`（→で移動）・Delete で `検索2` が閉じて3→…・`aria-selected=true` は常に1つ | `core.json` | ⭕ |
| **V18-b** | ARIA の仕上げ | パネルに `role="tabpanel"`／検索タブの `aria-controls` は全部 **`ed-pane-search`**（`result-grid` ではない）／⭐ **`aria-labelledby` が `ed-tab-search-3` → `ed-tab-search-4` に切り替わる**／統計表示中は `hidden: true`／`.cond-more` に `aria-controls="result-cond-list"` | `core.json` | ⭕ |
| **V18-c** | ⭐ 見えている面は常に1つ・統計の鮮度 | 検索 / 統計 / デッキ のどれでも **可視は 1**（`ed-pane-deck` `ed-pane-stats` `ed-pane-search`）。⭐ **鮮度**: デッキ面で ＋ → 統計へ戻すと **`メインデッキ 2枚` → `3枚`**（`WIND（風）2 → 3` も追従） | `deck.json` / `t5-v18c-stats.png` | ⭕ |
| **V19** | `#view-deck` が1pxも変わらない | ⭐ **`rj-v19b.mjs` の形**（before=3111 / after=3100）で **1440px・375px とも `IDENTICAL`**。⚠️ **走査ルール 旧473/新473**＝空回りしていない | `v19.txt` | ⭕ |
| **V20** | `validate` exit 0・検査①が0ファイル | **exit 0**。⭐ **`onRemoveOne wiring in sync — 渡しているのは 0ファイル（なし）／25ファイル走査`**（§8-6 のとおり）。`:root owners in sync — 4ファイル`・`no line-number refs in code comments — 0件（47ファイル走査）` も緑 | §M3 | ⭕ |
| **V21** | 範囲外を触っていない | `git diff --name-only c94bac53..HEAD` → **`tools/deck-builder/app.js`** と設計書1枚のみ。**トップ・`shared/`・`scripts/` は差分0行** | §M7 | ⭕ |
| **V22** | ⭐ **絵柄の追従（4条件）** | ⭐ **§1 の表**（before/after で差を確認） | `v22-before.json` / `v22-after.json` | ⭕ ⭐ |
| **V23** | 共有URL＝対象外（確認したうえで） | ⭐ **確認した**: `tools/deck-builder/app.js` が `location.hash` に書くのは **`#edit/<id>` / `#deck/<id>` の5か所だけ**で、`history.pushState/replaceState` は無く、`applyUrlQuery` はコメント内の言及のみ（実体はトップ側）＝**検索条件をURLに書く経路が存在しない** | `grep` | ⭕（対象外） |
| **V24** | バッジ集計が再検索せずに動く | ✕1つで **`3 → 2`**・チップ 4 → 3、すべて解除で **`0`＋`hidden`**・チップ 0。⭐ **その間 `/cards/search` は 0回** | `misc2.json` | ⭕ |
| **V25** | 件数がタブごと・混ざらない | 検索1 `24 件を表示 / 全 27 件`（grid 24）／検索2 `4 件を表示`（grid 4）。切り替えでそれぞれの値に戻る | `core.json` | ⭕ |
| **V25-b** | 持ち主タブを × → 件数も空 | ⭐ **`#result-count` が空文字**・「もっと見る」`hidden`・grid 0・条件の箱も `hidden`。**🃏デッキ を見ている間に × した場合も、最後の1つを選択中に × した場合も同じ** | `core2.json` | ⭕ |
| **V25-c** | 裏のタブの結果が混ざらない・戻すと取り直す | ⭐ **復元経路で「未読み込みのタブ」を作り**、検索1 を押した **50ms 後**に検索2 へ戻す → 表示は `4 件を表示`（dagger）のまま・grid 4・条件の箱も dagger（**混ざらない**）。⭐ 検索1 へ戻すと **`/cards/search` が +1**（取り直す＝仕様どおり） | `misc2.json` | ⭕ |

### ⚠️ V14-b・V25-c は「何もしない」ことの確認（§8-2・§8-3）

⭐ **コードが変わっていないことを字面で確かめた**——`searchStatusText()` と `updateElementWarn()` の**関数本体は `c94bac53` とバイト一致**
（`diff <(git show c94bac53:…|awk '/^function X\(/,/^}/') <(awk …)` が両方とも差分なし）。
**`395d61e6` はコメント行だけの差分**（`git show` で確認）。⭐ **動作も T4 から変わっていない**（上の2行）。

---

## 3. ⚠️ ⭐ dev が申告した逸脱1件の妥当性（実装報告 §6 の1）

> `tab.cond` を `onResults` の中で読むのではなく、**`makeSearchCtl()` の作成時に `artCondOf(tab.cond)` として固定した**。

⭐ **妥当と判断する**（設計 §8-1-1 の2 の意図＝「左ペインも `activeSearchTab` も見ない」を満たしている）。
⚠️ **dev の「必ず一致する」という主張を鵜呑みにせず、構造と実測の両方で確かめた**:

| # | 確かめ方 | 結果 |
|---|---|---|
| 1 | ⭐ **`tab.cond` への代入を全数列挙**（`grep -n 'cond' app.js` を代入だけに絞って全数確認） | **代入は3か所だけ**——`addSearchTab()` の生成時・`loadSearchTabs()` の生成時・`updateActiveSearchTab()`（`tab.cond = condFromForm()`）。⭐ **3つとも直後に `tab.ctl = makeSearchCtl(tab)` が走る**＝**固定値が古くなる経路が構造的に無い** |
| 2 | ⭐ **並び替え経路の実測**（PRD で検索 → 🔍を押さず左ペインだけ **PRD 1st** → **昇降順ボタン**） | 条件の箱が **`PRD 1st`** に変わり、**タイルの🎨も `{PRD 1st:19, バッジ無し:5}`**＝**固定値が新しい条件に更新されている**（古ければ `PRD` のままになる）。⭐ **タブは増えず1つのまま**（V8） |
| 3 | ⚠️ ⭐ **逆向きの実測**（PRD で検索 → **昇降順で ctl を作り直した後** → 🔍を押さず左ペインだけ **PRD 1st** →「もっと見る」） | ⭐ **続き24枚の🎨は `{PRD:9, バッジ無し:15}`＝PRD 以外 0件**（条件の箱も `PRD`）。**作り直した後も左ペインを見ていない** |
| 4 | ⭐ **復元経路の実測**（`localStorage` に `set=PRD` と `set=PRD 1st` の2タブを置いてリロード） | タブ1は `{PRD:4, バッジ無し:20}`・タブ2は `{PRD 1st:18, バッジ無し:6}`＝**各タブが自分の条件の版** |

⭐ **`els: condEls(tab.cond)` と同じ固定の仕方に揃っている**点も、コードを読んで確認した（`artCondOf()` は `condEls()` と同じく
`setIndexOf()` で prefix → 添字に戻してから `setPrefixes()` を通す＝**添字を保存していない**・§8-1-1 の3）。

⚠️ ⭐ **dev が「再測していない」と申告した V8 / V9 も、この経路そのものなのでレビュー側で測った**（上の 2 と §2 の V8・V9）。**回帰なし。**

---

## 4. 推奨基準（S）— ⚠️ **合格を妨げない。対応要否は設計担当の判断**

### S-1. ⭐ `searchStatusText()` のコメントにも T4 前の前提が残っている（dev の確認事項1）

- **該当**: `tools/deck-builder/app.js` の `searchStatusText()` の直前2行 — **「結果ダイアログの件数表示。」**（⚠️ **結果ダイアログは T4 で消えた**）。
- ⭐ **S に留めた理由**: **§8-4-3-1 の `nth-child` コメントとは性質が違う**。あちらは
  **§8-4-3 が明示的に禁じた「CSS への数値の書き戻し」を誘発する**（＝**読んだ人に誤った行動をさせる**）ので M として扱われた。
  こちらは**古い名前を指しているだけ**で、誘発する誤った行動が無い。
- ⭐ **dev が黙って範囲を広げずに確認事項へ回した判断は正しい**（追加指示が範囲内と認めたのは `updateElementWarn()` のコメントだけ）。
- ⚠️ ⭐ **ただし放置すると、T5 で1件直して1件残した状態になる**（**同じ型の取り残しが2件あって片方だけ直した**）。
  ⭐ **コメント1行なので、実物確認の前に設計担当が「足す／派生起票案」を決めるのが安い。**

### S-2. ⭐ `preferredArtIndex()` の二重定義の前提が変わった（dev の確認事項2）

- **該当**: ルートの `app.js`（トップ）は `preferredArtIndex(imgs)` のまま **左ペインを読む**／デッキ構築は `preferredArtIndex(imgs, artCond)` で**引数化済み**。
- ⭐ **T5 の裁定どおり触っていないので M ではない**（V21 も緑）。⚠️ **だが派生起票案「ブラウザ側の代表画像」の前提
  （＝「両方とも左ペインを見る」）は、この単位で崩れた**。⭐ **起票案の本文を直すかどうかは設計担当の判断**。
- ⭐ **dev がデッキ構築側の関数コメントに「トップ側は引数化していない」と明記した**のは、片側だけ直した事実を次の人に渡す手当てとして妥当。

### S-3. ⭐ 設計書 §10-3 の V15-b の期待値が、2セッションの実測と噛み合っていない

- **該当**: 設計書 §10-3 V15-b 「**箱は畳まれて 211px 前後**（⚠️ **137px にはならない**）」。
- **実測**: ⚠️ ⭐ **レビュー（9行・短い値）は `137px`**・**dev（9行）は `174px`**——**どちらも 211px ではない**。
  ⭐ **箱の高さは「行数」ではなく「先頭3行が折り返すかどうか」で決まる**（§8-7-1 が自分で書いているとおり **上限が無い**）。
- ⭐ **合否には影響しない**——**判定の本体は「送り先が箱の高さで変わらないこと」**で、そちらは
  **少項目（2行）でも 9行でも `scrollY 677` / `bandTop 0` で一致**した。⭐ **畳まれていることは高さではなく構造で確認した**
  （`cond-box is-folded`・`▾ すべて表示（ほか 6 項目）`・**見えている `dt` が 3**）。
- ⭐ **提案**: ⚠️ **高さの数値を期待値に書かない**（**測るたびに違う値になり、達成できない項目になる**）。
  **「`is-folded` が付き、見えている行が `COND_FOLD_SHOW` 行で、送り先が少項目と同じ」**に書き換えるのが、この設計書の他の項目の書き方に揃う。

### S-4. ⭐ 決定性の自動検査が無い箇所が1つ増えた（参考・起票不要かの判断だけ）

- ⭐ **`artCondOf()` の固定は「`tab.cond` を変える人は `tab.ctl` も作り直す」という規約に依存している**。
  ⚠️ **いまは代入3か所すべてが守っているが、守らせているのはコメント1行だけで自動検査は無い**
  （⭐ **CLAUDE.md の検査①②③と同じ「写しの対」ではない**ので、検査を足すのが適切かは設計判断）。
- ⭐ **破れたときの症状は無言ではない**（**条件の箱と絵柄が食い違う**＝画面に出る）ので、**レビューとしては S に留める**。

---

## 5. ⭐ 次の工程（§10-5）— ⚠️ **ヘッドレスで代替できない項目**

⭐ **T5 が合格したので、次は「ユーザーの実物確認」→ 承認 → 公開**（§10-5・§11-8）。
⚠️ ⭐ **上げ方は `npx wrangler pages dev . --local --port 3000`**（**3000番以外はユーザーのブラウザに届かない**）。

⭐ **8項目のうち、レビューが**どこまで**担保したか**を下に分けた。⚠️ **「×」の行はレビューでは原理的に測れない**:

| § | 見てもらうこと | レビューが担保した範囲 | ⚠️ 実物でしか判定できない部分 |
|---|---|---|---|
| **1** | 5つのタブの行き来の体感 | 構造は担保（`aria-selected=true` は常に1つ・切替で `/cards/search` 0回＝**再取得しない**・V1/V7） | ⚠️ **速さの体感・「いまどのタブか」が分かるか**（△） |
| **2** | ✕・すべて解除で再検索しない | ⭐ **完全に担保**（V10: リクエスト0・件数/grid/条件の箱すべて不変・V24: バッジだけ動く） | ⚠️ ⭐ **左ペインと条件の箱の食い違いが「気になるか」**（ユーザー決定の当否そのもの・△） |
| **3** | 375px でタブを横に送る感触 | `scrollWidth > clientWidth`（送れる）・固定2つは `left 16` に残る・**横あふれ 0** | ⚠️ ⭐ **タッチの慣性・掴みやすさは測れない**（**×**） |
| **4** | リロード後の復元 | ⭐ **担保**（V12: 再検索は最後のタブの1回だけ／V12-b: 統計なら0回） | ⚠️ **待ち時間（絞り込み無しの数値順は約19秒）を許容できるか**（**×**） |
| **5** | ⭐ **実ログインでのカード投入・保存の往復** | ⚠️ ⭐ **stub での往復まで**（`/api/decks/<id>/cards` を Playwright で差し替えている） | ⚠️ ⭐ **本物の D1 への保存・再読み込み後の一致は通っていない**（**×**・§10-4） |
| **6** | 2タブに同じカード → 片方で投入 → もう片方へ | ⭐ **担保**（V7: バッジ `1枚`・取得0／V7-b: 🃏デッキ 経由でも `2枚`） | ⚠️ **実ログインでの枚数一致**（5と同じ理由・**×**） |
| **7** | ⭐ **並び替えで飛ぶ感触・`smooth` の要否** | ⭐ **挙動は実測済み**（375px で **`scrollY 0 → 677` / `bandTop 0`**・タブは増えない） | ⚠️ ⭐ **「並び替えを触っただけで飛ぶ」と感じるか・`smooth` にしたいか**＝**聞くための項目**（**×**） |
| **8** | 3面の行き来と統計の鮮度 | ⭐ **担保**（V18-c: 可視は常に1・統計は `2枚 → 3枚` に更新） | ⚠️ **切り替えのちらつき・体感**（△） |

⚠️ ⭐ **描画・スクロール系はヘッドレスで「直ったこと」を証明できない**（CLAUDE.md）。
⭐ **左ペインの3つの対策（操作不具合2件の F1・F2・案1）が生きていることは構造で確認した**——
**`#search-top` の子孫に `position: sticky` が 0件**／**`.row-actions` が `static`**／**`.pane-scroll` の背景が不透明 `rgb(15,17,21)`**／
**`--pane-top` が生きている（`scrollY 0` で `179px`・スクロール後 `57px`）**／**`#search-top` の `max-height` が `721px → 843px` と追従**／
⭐ **どのスクロール位置でも 🔍検索 が `elementFromPoint` で押せる**（`pane.json`）。
⚠️ **それでも実機の GPU 描画の崩れはこの方法では出ない**ので、**実物確認で必ず触ってもらうこと。**

---

## 6. ⚠️ 設計担当への確認事項

1. **S-1**（`searchStatusText()` のコメント）を **T5 のコミットに足す／派生起票案にする**のどちらにするか。
   ⭐ **どちらでも合否は変わらない**が、⚠️ **「同じ型の取り残しを片方だけ直した」状態で公開することになる**点だけ判断してほしい。
2. **S-2**（`preferredArtIndex()` の二重定義）——⭐ **派生起票案「ブラウザ側の代表画像」の前提が変わった**ので、
   起票案の本文を直すかどうか（⚠️ **レビューは起票しない**）。
3. **S-3**（§10-3 V15-b の期待値）——⭐ **高さの数値を期待値から外すかどうか**。
   ⚠️ **このまま残すと、次に測る人も「211px にならない」と報告することになる**（**2セッション連続で起きた**）。
4. **S-4**——⭐ **`tab.cond` と `tab.ctl` を必ず同時に更新するという規約**に検査を足すかどうか（**起票の要否は設計判断**）。

---

## 7. レビューの環境と後始末

- **配信**: after＝`PORT=3100 npm run dev`／before＝`c94bac53` を `tmp/review/未採番-検索結果のタブ化/t5/before-tree` へ `git archive` で展開し、**そこの `serve.mjs` を 3111** で起動。
  ⭐ **本測定の前に「既知の差」で検算した**（`curl .../tools/deck-builder/app.js | grep -c artCondOf` が **3111→0 / 3100→3**）。
- ⚠️ **作業ツリーは変更していない**（`checkout` / `stash` / `restore` を使っていない。⭐ **コードも直していない**）。
- ⚠️ **他セッションの `tmp/review/…`（`harness2.mjs` / `rj-*` / `t4/`）は読んだだけで、上書きも削除もしていない**
  （`t4/t4r-harness.mjs` は `import` して再利用した）。
- ⭐ **起動したサーバー（3100 / 3111）は判定後に停止した。**

---

**次の担当: 合格＝設計確認待ち / 不合格＝実装待ち → ⭐ 本判定は「合格」なので `設計確認待ち`**
（⚠️ **最終承認とユーザーの実物確認（§10-5）はこの後**。⚠️ **push はしない**）
