**対象: 「検索結果のタブ化」**

# 実装報告（再修正）— 単位 T2 の仕上げ・差し戻し1往復目（2026-09-16）

⚠️ **これは issueコメントの代替である。** GitHubアカウント停止中で `gh issue comment` が打てないため、ここに書く。
復旧して採番されたら、本文をそのまま
`gh issue comment <n> --body-file docs/design/未採番-検索結果のタブ化/実装報告_T1T2_再修正_2026-09-16.md` で投函する。

| | |
|---|---|
| 差し戻し | **1往復目**（[`レビュー判定_T1T2_2026-09-16.md`](レビュー判定_T1T2_2026-09-16.md)・**不合格 M1／M5**） |
| 指示 | [`追加指示_V7と確認事項の裁定_2026-09-16.md`](追加指示_V7と確認事項の裁定_2026-09-16.md)／設計書 **1.1**（§4-2-1・§5-1-1・§5-3-1・§8-5-1・§9-1） |
| 前回の実装 | `360926a0`（+ 報告 `9dce4209`） |
| ⭐ **今回のコミット** | ⭐ **`d75581bf`**（`tools/deck-builder/app.js` ＋ **`scripts/validate.mjs`**） |
| push | ⚠️ **していない**（本番公開はユーザーの判断） |
| 次の担当 | **レビュー担当**（ラベル相当 `レビュー待ち`） |

---

## 1. 指摘への対応表（⭐ **指摘IDごと**）

| 指摘ID | 指摘 | ⭐ 対応 | 検証 |
|---|---|---|---|
| **M1-1 / M5-1** | ⭐ **V7: 別タブで投入したカードが、裏のタブでは「デッキに入っていない」ままになる** | ⭐ **`refreshResultTiles()` を新設**し、**検索タブを表示状態にするたび**に `#result-grid` の `.result` 全部の **「n枚」バッジ（`updateResultBadge`）と追加行（`renderAddRow`）を描き直す**。⚠️ ⭐ **呼ぶのは `selectSearchTab()` から毎回**（`restoreTiles()` の直後**だけ**には置いていない＝§5-1-1 の「2つ目の経路」を塞ぐため）。⚠️ **描き直すのはこの2つだけ**——画像・🎨（版）・🔄・`dataset.artUrl` は触らない。⚠️ **ネットワークを発生させない**（`getCard()` を呼ばず `cardCache` を直接見て、**未取得の slug はそのタイルだけ飛ばす**＝fail-open） | ⭐ **V7・V7-b・V7-c**（§2） |
| **S1** | 固定タブを見ている間に裏の検索タブを×すると、結果ダイアログが勝手に開く | ⭐ **裁定（設計 §4-2-1）どおり「表示中のタブ」＝ `selectedSearchTab`（帯で `aria-selected="true"`）に変更**。`closeSearchTab()` は **`wasActive`（結果パネルの持ち主）ではなく `wasSelected` で行き先を決める**。固定タブを見ている間に裏の検索タブを×しても**見ている面を変えない**（結果パネルを開かない・ほかの検索タブへ移らない・**最後の1つでもデッキタブへ飛ばさない**）。⭐ ×したタブが結果パネルの持ち主だったときは**タイルを捨てて持ち主を空にするだけ** | ⭐ **V3-b**（＋ **V3 の回帰**） |
| **S2** | `aria-controls` が暫定で `result-grid` | ⚠️ **今周は直していない**（裁定 F＝**T4**・設計 §8-4-1 に4項目で明記済み） | — |
| **（裁定 B）** | `rerunIfResultOpen()` の暫定挙動 | ⭐ **関数ごと削除**。`createSelectedFilters` の **`onChange` は `updateFilterBadge()` だけ**・**`onRemoveOne` は渡さない**。⚠️ ⭐ **同じコミットで `scripts/validate.mjs` の検査①の許可リストを空**にした（**検査は消していない**） | ⭐ **V10・V24・V20** |
| **（裁定 C）** | 保存する「最後に見ていたタブ」に固定タブを含めるか | ⭐ **現実装のまま**（戻していない）。復元先が固定タブなら**検索を走らせない** | ⭐ **V12-b** |
| **（裁定 D）** | リロード復元で結果モーダルが開く | ⚠️ **直していない**（**T4 までの暫定として許容**・設計 §5-3-2） | — |
| **（裁定 H）** | V22（絵柄の追従） | ⚠️ **T5 のまま**（`preferredArtIndex()` は未変更）。⭐ **今回の描き直しは絵柄に触らない**ので、T5 で引数化するときの前提（V7-c）は保てている | — |
| **S3・S4** | fail-open が良好・`condGroups()` / `setSearchOrder()` / `mine()` | ⭐ **崩していない**（設計 §11-6・§11-7 の申し送り）。`mine()` はそのまま。`refreshResultTiles()` も同じ流儀で fail-open | §2 の回帰 |

---

## 2. 検証結果

到達手順は ⭐ **レビューと同じ実ログイン相当**——Playwright の `context.route` で
**`/api/me` ／ `/api/decks/<id>` ／ `/api/decks/<id>/cards`（POST・PATCH・DELETE）だけ**を差し替え、
`#edit/<id>` へ遷移して**ページ本来の `openEditor()` → 左ペイン → 🔍検索 → タブ**を動かした。
⚠️ **内部変数は書き換えていない。** 証跡スクリプトとスクリーンショットは `tmp/dev/search-tabs/`（gitignore）。

- 静的プレビュー **3000番**（`npm run dev`・⚠️ **検証後に停止済み**）。
- Playwright 1.61.1 / `launch({ args: ["--ignore-certificate-errors"] })` ＋ context `ignoreHTTPSErrors: true`。

### 2-1. ⭐ 今周の指摘に対応する項目

| # | 期待（設計書 1.1） | 実測 | 合否 |
|---|---|---|---|
| **V6** | 投入でトースト・「n枚」・ステッパー | 検索2 で `＋メイン` → **バッジ「1枚」(hidden=false)・メインが `− 1 ＋`・トースト「✅ 「熟達の剣豪」をメインデッキに追加 — 1 / 60以上」**・デッキ状態 `[{adept-swordmaster, main, qty:1}]`（`v7-tab2-after-add.png`） | ✅ |
| **V7** | ⭐ **検索1 に戻すと「1枚」＋ `− 1 ＋`** | ⭐ **バッジ「1枚」(hidden=false)・メインが `− 1 ＋` のステッパー**。⭐ **`api.gatcg.com/cards/search` は 2→2（1件も増えない）**（`v7-tab1-fresh-badge.png` — ⚠️ **レビューの `v7-tab1-stale-badge.png` と同じ画面**。左のメインデッキ `1 / 60以上` とタイルが一致した） | ✅ |
| **V7-b** | ⭐ **検索1 → 🃏デッキでゾーンの ± → 検索1**（`restoreTiles()` を通らない経路） | 🃏デッキのゾーンで **＋**（ゾーンの枚数 2）→ 検索1 に戻す → ⭐ **バッジ「2枚」・メインが `− 2 ＋`**（`v7b-after-zone-plus.png`） | ✅ |
| **V7-c** | ⭐ **描き直しで 🎨 の選択が戻らない** | 🎨 で `🎨 3・ALCSD` → `🎨 3・RDOA` に切替 → **別タブへ行って戻る** → ⭐ **`🎨 3・RDOA` のまま**・**`data-art-url` 一致**・**`img.src` 一致** | ✅ |
| **V3-b** | ⭐ **🃏デッキ を見ている間に裏の検索タブを×** | 検索2つを作って 🃏デッキ を選択（ダイアログ hidden）→ **裏の検索2（＝結果パネルの持ち主）を ×** → ⭐ **`🃏 デッキ=true` のまま・ダイアログ hidden のまま・検索タブは1つも選択されない・`aria-selected="true"` はデッキの1つだけ** → ⭐ **最後の検索1を × しても同じ**（タブ0個・デッキタブのまま・ダイアログ hidden）。**この間 `/cards/search` は増えない**（`v3b-close-bg-tab.png`） | ✅ |
| **V10** | ⭐ **✕・すべて解除で再検索しない** | 検索後 `/cards/search` **1** → **✕1つ = 1** → **すべて解除 = 1**（⭐ **1件も増えない**）。**タブ [検索1] のまま**・**件数「21 件を表示」のまま**・**タイル 21 枚のまま** | ✅ |
| **V24** | バッジは再検索しなくても増減する | チップ2つ（NORM/FIRE）→ **バッジ「2」(hidden=false)・チップ2個**・⭐ **このとき検索は 0 回**（U6）→ **✕1つ → 「1」・チップ1個** → **すべて解除 → hidden=true・チップ0個** | ✅ |
| **V12-b** | ⭐ **📊統計 を見た状態でリロード** | 保存 `{"nextN":3,"active":"stats","tabs":[検索1,検索2]}` → リロード → ⭐ **`📊 統計=true`・統計面が表示・検索タブ2つは帯に残る**・⭐ **`/cards/search` は 0 回**（`v12b-stats-restore.png`） | ✅ |
| **V20** | ⭐ **検査①の成功行が「0ファイル」** | ⭐ **`onRemoveOne wiring in sync — 渡しているのは 0ファイル（なし）／25ファイル走査`**・`npm run validate` **exit 0**（②③も緑: `pane threshold in sync — 2ページ（トップ 1200px / デッキ構築 1200px）` / `mobile band marker in sync — 1組`） | ✅ |

### 2-2. 回帰（前周で合格していた項目の再測）

| # | 実測 | 合否 |
|---|---|---|
| **V1** | タブ=[検索1,検索2]／検索1を押すと22枚が復元・**検索リクエスト 2→2**・左ペインが `fire` に戻る | ✅ |
| **V2** | 検索タブの ×=2 / 固定タブ内の ×=0 | ✅ |
| **V3** | まん中（検索4）を× → **検索5（右隣）**／右端（検索6）を× → **検索5（左隣）**／全部閉じる → **🃏デッキ**・ダイアログ hidden・区切り線も消える。⭐ **別のケースでも再測**（[検索1,検索2,検索3] の**選択中のまん中**を× → 検索3 が選択） | ✅ |
| **V4** | 6回検索 → [検索2..検索6]（5つ・最古が閉じる・トースト・連番継続） | ✅ |
| **V5** | 全部閉じてから検索 → [検索1] | ✅ |
| **V9** | 検索タブ0個で並び替え → [検索1]・「18 件を表示（コストを持つカードのみ）」 | ✅ |
| **V11** | ⭐ **P1**: `sword`「24 件を表示 / 全 27 件」→ 左ペインを `of` に書き換え →「もっと見る」→ **「27 件を表示」・27枚すべて sword** | ✅ |
| **V12** | リロード → [検索1,検索2]・選択=検索1・**`/cards/search` 1回だけ**・22枚／**検索2を押すとそこで初めて2回目** | ✅ |
| **V18** | → で `📊統計 → 検索1 → 検索2 → 🃏デッキ` と巡回（各段階で `aria-selected=true` は1個）／←で戻る／**Delete で検索タブが閉じ、固定タブでは何も起きない** | ✅ |
| **V19** | ⭐ **閲覧専用画面は1pxも変わらない**——今周追加したCSSセレクタ10本のうち `#view-deck` の中に当たるものは **0本**、帯の DOM はボタン2つ、矩形 `🃏デッキ 103x41 / 📊統計 91x41`、`flex` は既定の `0 1 auto` のまま | ✅ |
| **V21** | `git diff --name-only` = **`tools/deck-builder/app.js` と `scripts/validate.mjs` の2つだけ**。⭐ **トップ（ルートの `index.html`/`style.css`/`app.js`）と `shared/` は出ない**（`scripts/validate.mjs` は設計 1.1 §3 で **T2 の担当ファイル**） | ✅ |
| 回帰（番号なし） | 🃏デッキ⇄📊統計 の切り替え／JP検索「ドラゴン」が1件／Escape でダイアログが閉じる／**エキスパンション ALC の1ページ目は 🎨 が全部 `ALC`**／エレメントAND(FIRE+WATER)の警告が左ペインに出て群が開く／**保存は最近10デッキ分**（12→10）／**`localStorage` の書き込みが例外でも pageerror 0件** | ✅ |

---

## 3. 実行できなかった検証・残っている確認

| # | 項目 | 状態 |
|---|---|---|
| **V8** | 並び替えでタブが増えず表示中が並べ替わる | ⚠️ **今周は未計測**（前周も未計測）。⭐ **レビューが `360926a0` で実測して ✅**（判定 §3）。今周の変更は `#s-sort`/`#s-order` の経路に触っていない（`updateActiveSearchTab()` は無改修） |
| **V13〜V17** | 進捗表示・JPの全項目・375pxの送り・1200pxのスクロール・条件の畳み | **T3・T4 の周の項目**（条件の箱・結果パネルの移設が前提） |
| **V22** | ⭐ **絵柄の追従（もっと見るの続き・裏のタブ）** | ⚠️ **T5 のまま残る既知の穴**（裁定 H）。⭐ **今回の描き直しは絵柄に触らないので、T5 での引数化の妨げにはならない**（V7-c で実測） |
| **V23** | 共有URL | ⭐ **対象外**（デッキ構築に検索条件のURL共有が無い）。保存形式が #20 と同じクエリ文字列・prefix であることは前周に確認済み |
| — | **実物確認（§10-5）** | ⚠️ **未実施**。⭐ **レビュー合格後・承認の前**にユーザーが 3000番で操作する工程。⭐ **裁定 B の前倒しにより、そこで見てもらう ✕ の挙動は最終形**（✕では何も起きない） |

⚠️ **V6・V7・V7-b を「デッキの中身に依存するので未検証」とは書いていない**（設計 1.1 §10-4 のとおり、
`/api/decks/<id>/cards` の stub だけで実測した）。

---

## 4. 設計書の想定と食い違った点

- **なし。** 裁定（A〜H）と設計書 1.1 の §4-2-1・§5-1-1・§5-3-1・§8-5・§8-6 のとおりに実装した。
- ⚠️ 実装上の細部として1点だけ記録しておく: ⭐ **追加行の枚数は `<input value>` にあり `textContent` には出ない**
  （`.cellstep[data-zone="…"] .cs-input`）。**検証スクリプトを「`− 1 ＋` という文字列」で書くと、
  実装が直っていても落ちる**（実際に1回落として気づいた）。**再レビューで測るときは `.cs-input` の `value` を見ること。**

---

## 5. 設計書からの逸脱と根拠

| # | 逸脱 | 根拠 |
|---|---|---|
| 1 | `scripts/validate.mjs` の成功行を `（${passers.join(" / ") || "なし"}）` にした | 許可リストが空だと **`渡しているのは 0ファイル（）`** と括弧が空で出る。⭐ **「なし」と書くほうが、0件が正常であることが読み取れる**。⚠️ **判定ロジックは1文字も変えていない**（双方向の突き合わせはそのまま） |
| 2 | `scripts/validate.mjs` の**コメントを2か所直した** | 旧コメントに ⚠️ **「タブ化で①の非対称が消えたら、検査①も一緒に消す」** と書かれており、**設計 1.1 §8-6 の「検査を消さないこと」と真っ向から矛盾**していた。⭐ **消さない理由（後から誰かが渡しても止まらなくなる）を書き直した**。⚠️ **放置すると、次の周の担当が「消してよい」と読む** |

⚠️ **上記以外は設計書のとおり。** とくに次は**触っていない**:
**`shared/js/card-search.js`（無改修）** ／ **`preferredArtIndex()`（T5）** ／ **`el.resultModal` の跡片づけ4か所（T4）** ／
**`aria-controls` / `role="tabpanel"`（T4）** ／ **リロード復元でモーダルが開くこと（T4 まで暫定）**。

---

## 6. 設計担当への確認事項

- **なし。** ⭐ 前回の確認事項4件は[追加指示](追加指示_V7と確認事項の裁定_2026-09-16.md)で全て裁定され、そのとおりに実装した。
- ⚠️ 1点だけ**申し送り**: ⭐ **`refreshResultTiles()` は T4 以降もそのまま最終形**（設計 §5-1-1 の最後の行）。
  **T4 で結果パネルを右列へ移すとき、`selectSearchTab()` から毎回呼ぶ形を崩さないこと**——
  崩すと V7-b の経路（デッキ面で増減してから検索タブへ戻る）が**無言で古い枚数に戻る**。

---

**次の担当: レビュー担当（ラベル相当 `レビュー待ち`）**
