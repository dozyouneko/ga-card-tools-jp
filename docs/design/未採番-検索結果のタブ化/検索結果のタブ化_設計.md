# デッキ構築ツールの検索結果のタブ化 — 設計書（2026-09-16）

## 改版履歴

| 版 | 日付 | 内容 |
|---|---|---|
| 1.0 | **2026-09-16** | 初版。**モック（2026-09-15）をユーザーが操作し、①B・②上限5・③PC/スマホともタブ に加えて U1〜U7 がすべて決着した状態**で起こした。⭐ **U1 は「連動」だが、ユーザーの指摘により「選択中の条件」の✕・すべて解除では再検索しない**（→ §2-2・§8-5）。⭐ **その結果 `onRemoveOne` の配線が不要になり、`validate` の検査①の期待値が変わる**（→ §8-6） |

- 起票案: [`../未採番-デッキ構築ツールUI刷新/起票案_2026-09-04_検索結果のタブ化.md`](../未採番-デッキ構築ツールUI刷新/起票案_2026-09-04_検索結果のタブ化.md)（⚠️ **リンク元が9ファイルあるので移していない**）
- モックと実測: [`モック説明_検索結果のタブ化_2026-09-15.md`](モック説明_検索結果のタブ化_2026-09-15.md)（⭐ **実測値の正はこちら**。本書は繰り返さない）
- 前提（⚠️ **蒸し返さない**）: [`../未採番-デッキ構築ツールUI刷新/左ペイン化_設計.md`](../未採番-デッキ構築ツールUI刷新/左ペイン化_設計.md)（2026-09-10 公開 `ecb2b51d`）と
  [`../未採番-デッキ構築の操作不具合2件/操作不具合2件_設計.md`](../未採番-デッキ構築の操作不具合2件/操作不具合2件_設計.md)（2026-09-14 承認・⚠️ **未公開**）
- 設計の起点 HEAD: **`adee6324`**（⚠️ `tools/deck-builder/` は**本番 `ecb2b51d` ではなく「操作不具合2件」を含む**）

---

## 1. このタスクで何をするか

**デッキ構築ツールの編集画面（`#view-editor`）で、検索結果を「結果モーダル」から「タブ」に変え、過去の検索結果が残るようにする。**

| # | 決定 | 何をするか |
|---|---|---|
| **T1** | Q7・U2・U3 | ⭐ **タブ帯に「動的タブ」の仕組みを足す**（固定2つ＋横スクロールする検索タブの箱・上限5・連番） |
| **T2** | Q3・U4 | ⭐ **タブごとに検索コントローラを1つ持ち、条件を localStorage に保存する**（⭐ **P1 が直る**） |
| **T3** | Q4 | ⭐ **「検索N の条件」の箱**（2列の表・4項目超で畳む） |
| **T4** | Q8・③ | ⭐ **結果モーダルを廃止する**（PC・スマホともタブ）。跡片づけ4か所 |
| **T5** | §8 | ⭐ **タブ化で壊れる既存の仕掛けの手当て**（絵柄・件数・0件警告・`onRemoveOne`・`validate` 検査①） |

### ⚠️ やらないこと（意図的に外したもの）

| # | 外したもの | 理由 |
|---|---|---|
| 1 | **閲覧専用画面（`#view-deck`）のタブ帯** | ⭐ **検索が無いので検索タブが増えない**。⚠️ **見た目が2種類になる**が、`.deck-tabs` の既存CSSは共用のままにして差を出さない（→ §4-4） |
| 2 | **トップページ（`/`）** | ⚠️ **1行も触らない**。トップの結果はモーダルではない（左ペイン＋結果一覧）ので、そもそも同じ問題が無い |
| 3 | **タブ帯の画面上部への固定（sticky）** | ⚠️ **右列の Grid アイテムなので、そのままでは効かない**（可動範囲が自分の行の高さしか無い）。⭐ **要望が出たら右列の構造から作り直し**（→ §11-3） |
| 4 | **カードの配列の保存** | ⚠️ **禁止改定・新セットで古くなる**／1件約10KB×24枚×5タブで容量を食う（→ §5-2） |
| 5 | **サイドボードプランメモのタブ** | ⭐ **本件が作る「動的タブ」に後から乗せる**（起票案 §5）。⚠️ **基盤だけを別issueに切り出さない** |

---

## 2. ユーザー決定（⚠️ **ここを設計判断で覆さない**）

### 2-1. モックを触る前の決定（2026-09-15）

| # | 決定 |
|---|---|
| ① | **B＝タブはリロード後も残る**（永続化する。⚠️ **D1ではなくブラウザ側**＝§5-2） |
| ② | **上限5**・**連番は上限を超えても続ける** |
| ③ | **PC・スマホとも「タブ」**（＝結果モーダルは廃止） |

### 2-2. モックを触った結果の決定（2026-09-16）

| # | 論点 | 決定 |
|---|---|---|
| **U1** | 左ペインとタブの関係 | ⭐ **連動**（タブを開くと左ペインがそのタブの条件に戻る／並び替え・昇降順は**表示中のタブをその場で更新**する＝タブを増やさない） |
| **U2** | タブを持つ単位 | **デッキごと** |
| **U3** | 全部閉じたときの連番 | **「検索1」に戻す** |
| **U4** | リロード後 | **最後に見ていたタブを開き、そのタブだけ再検索する** |
| **U5** | 新しい検索 | **そのタブへ切り替える** |
| **U6** | チップを押したとき | **検索しない**（🔍検索で反映＝現状どおり） |
| **U7** | 375px の固定タブ | **絵文字だけにしない**（「🃏 デッキ」「📊 統計」のまま） |
| **P1** | 潜在不具合の扱い | ⭐ **別の起票案にしない**——本件の設計（§7）がそのまま直す |

### ⭐ 2-3. ユーザーの指摘（2026-09-16）＝ U1 の修正

> 検索してタブ1を表示させた状態で、検索条件にエレメント：水を選択し、
> 選択中の条件から水の条件を削除すると表示中のタブが再検索されてしまう。
> **選択中の表示条件を削除した際は再検索されない方がよい。**

⭐ **これで規則が1本になった**（⚠️ **モック 2026-09-15 版は✕でも再検索していた＝この決定で覆った**）:

| 操作 | 何が起きるか |
|---|---|
| **🔍検索 / Enter** | ⭐ **新しい検索タブを作る**（これだけがタブを増やす） |
| **並び替え・昇降順** | **表示中の検索タブをその場で更新する**（タブは増やさない） |
| **それ以外の左ペインの編集**（テキスト・チップ・**「選択中の条件」の✕**・**すべて解除**） | ⭐ **何も起きない**（＝下書きを直すだけ。U6 と同じ扱い） |

- ⚠️ **左ペインと「表示中タブの条件の箱」は食い違ったまま残る**。⭐ **注記は出さない**（2026-09-16・ユーザー決定）。
  **カード名を書き換えたときも同じ食い違いが起きており、そこに注記が無いから**（＝✕だけ特別扱いしない）。
- ⭐ **食い違いは「タブを切り替える」と解消する**（U1 連動で左ペインがそのタブの条件に戻る）。

---

## 3. 変更するファイルの一覧

| ファイル | 変更 | 単位 |
|---|---|---|
| `tools/deck-builder/index.html` | タブ帯に検索タブの箱を足す／⭐ **`#result-modal` を撤去**し、結果パネル（条件の箱・件数・グリッド・もっと見る）を `#view-editor` の右列へ移す | T1・T4 |
| `tools/deck-builder/style.css` | 検索タブの箱（横スクロール・端のぼかし）・条件の箱・結果パネルの `min-height` | T1・T3 |
| `tools/deck-builder/app.js` | タブの状態と保存・タブごとの検索コントローラ・条件の文言・跡片づけ | T1〜T5 |
| `scripts/validate.mjs` | ⭐ **検査①の期待値を「0ファイル」へ**（→ §8-6） | T5 |

⚠️ **`shared/js/card-search.js` は変更しない**（⭐ **`els` を値の読み出しにしか使っていないので無改修で足りる**＝§7）。
⚠️ **`shared/css/` も変更しない**（バッジは `.badges-bl` の縦積みのまま＝起票案 R3）。

---

## 4. T1：タブ帯（Q7・U2・U3・U7）

### 4-1. DOM

```text
.deck-tabs[role=tablist]
  ├ #ed-tab-deck  [role=tab] 🃏 デッキ      … 固定・× なし
  ├ #ed-tab-stats [role=tab] 📊 統計        … 固定・× なし
  ├ .tabs-sep                                … 区切り線
  └ .tabs-scroll                             … ⭐ ここだけが横スクロールする
      └ .tab-item ×N
          ├ button[role=tab]   「検索N」
          └ button.tab-close   「×」（aria-label="「検索N」を閉じる"）
```

- ⚠️ **ボタンの中にボタンは置けない**ので、タブ本体と × は**兄弟**にする（`.tab-item` が包む）。
- ⭐ **`role="tablist"` は既存**（起票案 R5）。増やす検索タブにも `role="tab"` / `aria-selected` / `aria-controls` を付ける。
- ⭐ **キーボード**: ←/→ でデッキ・統計・検索タブを順に移動、**Delete で表示中の検索タブを閉じる**（固定2つでは何も起きない）。
- **× の大きさ**: 28px（620px以下は 24px）。

### 4-2. 上限と連番（②・U2・U3）

| 項目 | 決定 |
|---|---|
| 上限 | **5**（PC・スマホとも） |
| 6個目を作ったとき | ⭐ **作った順でいちばん古いタブを閉じる**＋トースト「タブは5つまでです — いちばん古い「検索N」を閉じました」 |
| 連番 | ⭐ **上限を超えても続ける**（検索3〜検索7 のようになる） |
| **全部閉じたとき** | ⭐ **「検索1」に戻す**（U3。取り違える相手がいないため②と矛盾しない） |
| 単位 | ⭐ **デッキごと**（U2。別のデッキを開くと別のタブ列） |
| 新しいタブの位置 | 右端。**帯を横に送って見える位置へ**（⚠️ **ページは縦に動かさない**） |
| 表示中のタブを × | 右隣 → 無ければ左隣 → 無ければ **デッキタブ** |

### 4-3. 横スクロール（③・U7）

- ⭐ **横に送るのは `.tabs-scroll` だけ**（固定2つは動かない）。**続きがある側の端をぼかす**。
- ⚠️ **375px では検索タブが1つしか全部は見えない**（実測はモック説明 §4-1）。**U7 で「絵文字だけ」にはしない**＝**この状態を受け入れる**。
- ⭐ **横スクロールが要るのは約790px 未満**（それ以上は5つとも入る）。

### 4-4. ⚠️ 閲覧専用画面との見た目の差

`#view-deck` の `.deck-tabs` は**固定2つのまま**になる。⭐ **`.tabs-sep` / `.tabs-scroll` は要素が無ければ何も描かない**ので、
**既存の `.deck-tabs` のCSSを分岐させない**（＝閲覧専用側は1pxも変わらない）。

---

## 5. T2：タブが持つものと保存（Q2・Q3・U4）

### 5-1. タブ1つが持つもの

| 項目 | 内容 | 保存する |
|---|---|---|
| `n` | 連番（「検索N」の N） | ✅ |
| `cond` | ⭐ **条件のスナップショット**（§5-2 の形式） | ✅ |
| `open` | 条件の箱を開いているか（4項目超のとき） | ✅ |
| `ctl` | `GA_CARD_SEARCH.create()` のインスタンス | ❌（ページを開いている間だけ） |
| `tiles` | 取得済みの結果タイル（DOM） | ❌（同上） |

- ⭐ **同じページを開いている間は、タブを切り替えても再検索しない**（取得済みのタイルを出し入れする）。

### 5-2. 保存（①B）

- ⭐ **`localStorage`**。キーは **`ga-deckbuilder-search-tabs:v1:<deckId>`**（U2＝デッキごと）。
  - ⚠️ **デッキを消しても保存が残る**ので、**最近の 10 デッキ分だけ残す**（古い順に捨てる）。
- ⭐ **条件の形式はトップの共有URL（#20・`queryString()`）と同じクエリ文字列**
  （例: `q=fire&element=FIRE&element=WATER&element_op=AND&set=PRD&sort=cost_memory&order=DESC`）。
  - ⚠️ ⭐ **エキスパンションは添字ではなく prefix で持つ**（`setKeyOf()`）。**`meta.sets` の並びが変わっても壊れないため**（#20 と同じ理由・CLAUDE.md「新セットの追記」）。
- ⭐ **保存するのは「条件」と「連番」と「最後に見ていたタブ」だけ**（カードの配列は保存しない＝§1 やらないこと 4）。
- ⚠️ **保存できない環境（プライベートウィンドウ等）では、その場限りのタブとして動く**（⭐ **fail-open**。例外を握りつぶして続行する）。

### 5-3. リロード後（U4）

1. 保存からタブを復元して帯に並べる。
2. ⭐ **最後に見ていたタブを開き、そのタブだけ再検索する。**
3. ほかのタブは**押したときに検索する**（それまでリクエストを投げない）。

⚠️ **絞り込みなしの数値順は約19秒かかる**（#44）。⭐ **`onProgress` の進捗表示がタブの中で出ることを検証項目にする**（V13）。

---

## 6. T3：条件の箱（Q4）

- **「検索N の条件」＋2列の表**（項目名｜値）。
  値はチップの**日本語名**、複数値は「・」、**AND は「かつ」**でつなぎ、項目名に**「（いずれか）／（すべて）」**を添える。
  フォーマット・エキスパンションは**選択肢の表示名**。**「並び」は常に最後の行**。絞り込みが何も無いときは **「絞り込み｜なし（全カード）」**。
- ⭐ **4項目を超えたら3項目だけ見せて畳む**（「▾ すべて表示（ほか N 項目）」）。**開閉はタブごとに覚える**。
- タブの `title` に条件の1行要約を入れる。
- ⚠️ ⭐ **丸ピル（「選択中の条件」と同じ見た目）にしない**——**375px で結果が 583px まで押し下がった**（実測はモック説明 §4-2）。

---

## 7. T2：タブごとの検索コントローラ（⭐ **P1 の直し方**）

⭐ **タブごとに `GA_CARD_SEARCH.create()` を1つ作り、`els` に「そのタブの条件を返すだけの物」を渡す。**

- 渡す物が持つのは **`value` / `dataset.dir` / `getValues()` / `getMode()` だけ**。
  ⭐ **共有モジュールは `els` を値の読み出しにしか使っていない**ので、**`shared/js/card-search.js` は無改修**。
- ⭐ **これで「もっと見る」が左ペインのいまの入力を読まなくなる**＝ **P1（本番に出ている潜在不具合）が直る**。

> **P1（再掲）**: 1200px以上で結果を開いたまま左ペインの条件を書き換え、🔍を押さずに「もっと見る」を押すと、
> 前の検索の結果に**新しい条件の2ページ目**が混ざる（`sword` 27件 → `of` に書き換えて「もっと見る」で 334件扱い）。
> **2026-09-10 の左ペイン化で到達可能になった**（モーダルが左ペインを覆わなくなったため）。

⚠️ **検証項目 V11 で「混ざらないこと」を実測する**（⭐ **直ったことの証明はこの1本だけ**）。

---

## 8. T4・T5：既存コードの手当て（⚠️ **1つでも漏らすと静かに壊れる**）

### 8-1. `preferredArtIndex()` を引数化する（⭐ **M1-b「絵柄の追従」**）

⚠️ **`preferredArtIndex()` は左ペインの `#s-set` とレアリティ群を「いま」読む**（`appendResults()` から呼ばれる）。
タブ化すると **「もっと見る」や裏のタブの結果の絵柄が、左ペインのいまの条件で決まる**
＝ **P5（レアリティで絵柄が追従しない）と同じ型の不具合が再発する**。

→ ⭐ **そのタブの条件（prefix とレアリティ）を引数で受け取る形に変える。**

### 8-2. `searchStatusText()` の描画先を引数にする

⚠️ **`el.resultGrid.childElementCount` を読んでいる**（件数表示）。**タブごとの件数**にするため、**数える対象を引数で渡す**。

### 8-3. `updateElementWarn()` は表示中のタブのときだけ

⚠️ **左ペインのエレメント群を開き、スマホではフォーム自体を開く**。
⭐ **U1 が「連動」なので、表示中のタブの結果に対してだけ呼べば食い違わない**（裏のタブの結果では呼ばない）。

### 8-4. ⚠️ 結果モーダルを消したら、`el.resultModal` を次の4か所から外す

| # | 箇所 | 外し忘れるとどうなるか |
|---|---|---|
| 1 | `closeModal()` の「他のモーダルが開いているか」の配列 | `body` の `overflow` が戻らない |
| 2 | Escape キーの処理 | 例外（`el.resultModal` が無い） |
| 3 | カード詳細の `onAfterClose`（`body` の `overflow` を戻す判定） | 同上 |
| 4 | `searchCtl` の `onStart`（`openModal(el.resultModal)`） | 検索のたびに例外 |

⭐ **`body { overflow: hidden }` の固定は検索では起きなくなる**（「操作不具合2件」の案2の論点が消える）。

### 8-5. ⭐ `rerunIfResultOpen()` を削除する（§2-3 の決定）

- ⭐ **関数ごと削除**し、`createSelectedFilters` の **`onChange` は `updateFilterBadge()` だけ**にする。
- ⭐ **`onRemoveOne` は渡さない**（→ §8-6）。
- ⚠️ ⭐ **「無言の劣化になる」という既存コメントの懸念は、ここでは当たらない**——
  **✕を押すとチップが消えてバッジが減る**という視覚的フィードバックがその場にある（元のコメント自身がそう書いている）。
  ⭐ **`#s-sort` / `#s-order` のガードを戻すものでもない**（並び替えは §2-3 のとおり**表示中のタブを更新する**）。
- ⚠️ ⭐ **検索タブが1つも無い状態で並び替えを触ると、新しいタブができる**（＝いまも空条件の検索が走る＝「許容」と書かれている挙動）。**この挙動は変えない**（V9）。

### 8-6. ⚠️ ⭐ `validate` の検査①の期待値が変わる

⭐ **§8-5 で `onRemoveOne` を渡すページが 0 になる**ため、**いまの検査①（「渡しているのは1ファイル」）は exit 1 になる。**

- ⭐ **期待値を「どのページも渡していない（0ファイル）」に変える**（許可リストを空にする）。
  **検査の目的（トップに渡すと1回の✕で検索が2回走る＝リクエストが倍）は、0ファイルでもそのまま守られる。**
- ⚠️ **検査を消さないこと**——消すと**後から誰かが渡しても止まらなくなる**。
- ⚠️ **`shared/js/card-search.js` の `onRemoveOne` オプション自体は残す**（⭐ **サイドボードプランメモが同じ仕組みを使う可能性がある**／触る面積を増やさない）。
- ⚠️ **同じコミットで `scripts/validate.mjs` と `tools/deck-builder/app.js` を直す**（片方だけだと `npm run validate` が exit 1）。

### 8-7. 結果パネルの `min-height` と、検索したときのページ位置

- ⚠️ **検索結果パネルに `min-height: 100vh` が要る**——⭐ **送る先の高さが無いとページを送れない**（検索中はパネルが空でページが短い）。
- **スマホ表示**: ⭐ **検索のたびにタブ帯の位置までページを送る**（送らないと結果が画面外）。
- **1200px以上**: ⭐ **タブ帯が画面の上半分に無いときだけ送る**（左ペインは sticky なのでページのどこからでも検索できる）。

---

## 9. 実装の順序（推奨）

| 単位 | 中身 | ⚠️ 前提 |
|---|---|---|
| **T1** | タブ帯（固定2つ＋区切り＋横スクロールする箱）・上限5・連番・× ・キーボード | — |
| **T2** | タブごとの検索コントローラ・条件のスナップショット・localStorage・リロード復元 | T1 |
| **T3** | 条件の箱（2列の表・畳み） | T2（条件が要る） |
| **T4** | ⭐ **結果モーダルの撤去**（§8-4）・結果パネルの移設・`min-height`・スクロール送り | T1〜T3 |
| **T5** | §8-1〜8-3・8-5・8-6（既存コードの手当てと `validate`） | T4 |

⚠️ ⭐ **T5 を最後に回さない選択肢もある**が、**§8-6 は `app.js` と `validate.mjs` を同時に直す必要がある**ので**必ず1つの単位に収める**。

---

## 10. 検証項目

### 10-1. 到達手順（⚠️ ログインが要る画面）

⭐ **#51 の手順**（`#view-editor` の `hidden` を外し、**空のデッキ状態を用意してから** `searchCtl` を動かす）。
⭐ **デッキの中身が要るものは srcdoc 方式**（`/api/me` と `/api/decks/<id>` だけスタブ。手順は左ペイン化のモック説明 §2）。

### 10-2. 本体

| # | 項目 | 期待 |
|---|---|---|
| **V1** | 続けて2回検索する | ⭐ **タブが2つでき、前の結果が残っている**（1つ目を押しても**再検索しない**） |
| **V2** | 検索タブの × | 閉じる。⭐ **デッキ・統計に × が無い** |
| **V3** | 表示中のタブを × | 右隣 → 無ければ左隣 → 無ければデッキタブ |
| **V4** | 6回検索する | **タブは5つ**・**いちばん古いものが閉じる**・トーストが出る・⭐ **連番は続く** |
| **V5** | 全部閉じてから検索 | ⭐ **「検索1」に戻る**（U3） |
| **V6** | 検索結果からデッキへ投入（起票案 R1） | ⭐ **クリック／タイルメニューとも今までどおり**。トースト・「n枚」バッジ・追加ボタンが出る |
| **V7** | 別タブに同じカードがある状態で投入 | ⭐ **そのタブを開いたときに「n枚」が反映されている**（タブを表示するたびに描き直す） |
| **V8** | タブを表示したまま並び替えを変える | ⭐ **タブは増えず、表示中のタブが並べ替わる**（条件の箱の「並び」も変わる） |
| **V9** | ⚠️ **検索タブが無い状態で並び替えを触る** | **新しいタブができる**（＝現状の「空条件の検索が走る」を踏襲・§8-5） |
| **V10** | ⭐ **「選択中の条件」の✕・すべて解除**（§2-3） | ⚠️ ⭐ **再検索しない**。チップが消えてバッジが減るだけ。**条件の箱は変わらない**・**注記も出さない** |
| **V11** | ⭐ **P1**: 結果を開いたまま左ペインを書き換えて「もっと見る」 | ⭐ **混ざらない**（`sword` 27件のまま・27枚すべて sword） |
| **V12** | リロード | ⭐ **タブが残り、最後に見ていたタブだけ再検索する**（U4）。ほかは押したときに検索 |
| **V13** | 絞り込みなしの数値順で検索 | ⭐ **進捗表示（`読み込み中 n/N ページ…`）がタブの中に出る**（#44） |
| **V14** | 日本語モード（JP）で検索 | 0件文言・裏面一致の注記（#46）がタブの中で今までどおり |
| **V15** | 375px で検索を3回 | タブ帯までページが送られる／固定2つは左に残り検索タブだけ横に送れる／⭐ **横あふれ 0** |
| **V16** | 1200px で検索 | タブ帯が画面の上半分にあれば**ページを動かさない** |
| **V17** | 条件8項目で検索 | ⭐ **3項目＋「▾ すべて表示（ほか5項目）」に畳まれる**。開閉がタブごとに残る |
| **V18** | キーボード ←/→・Delete | タブ移動／検索タブが閉じる。⭐ **`aria-selected` が1つだけ true** |
| **V19** | ⚠️ **閲覧専用画面（`#view-deck`）** | ⭐ **1pxも変わらない**（デッキ・統計の2つだけ） |
| **V20** | `npm run validate` | ⭐ **exit 0**。**検査①の成功行が「0ファイル」になっている**（§8-6） |
| **V21** | ⚠️ **範囲外を触っていないこと** | `git diff --name-only` に **トップ（ルートの `index.html` / `style.css` / `app.js`）と `shared/` が出ない** |

### 10-3. ⚠️ ⭐ M1-b（既存の同種の条件で効いている副作用）— **欠けたら M1 の欠落＝不合格**

| # | 副作用 | 本件での期待 |
|---|---|---|
| **V22** | ⭐ **絵柄の追従** | ⭐ **エキスパンション・レアリティで絞ったタブの結果は、そのタブの条件の絵柄で出る**。⚠️ **「もっと見る」の続き・裏のタブに切り替えた後も同じ**（§8-1） |
| **V23** | **共有URL（#20）** | ⭐ **デッキ構築には検索のURL共有が無い**＝**対象外**（⚠️ **確認したうえで対象外と書くこと**） |
| **V24** | **バッジ集計** | 左ペインの絞り込みバッジの数が、✕・すべて解除・チップ操作で正しく増減する（⚠️ ⭐ **再検索しなくても数は動く**＝V10 と対になる項目） |
| **V25** | **件数表示** | ⭐ **タブごとに正しい**（「N 件を表示 / 全 M 件」。§8-2）。⚠️ **裏のタブの件数が表示中のタブに混ざらない** |

### 10-4. ⚠️ デッキの中身に依存するもの（ヘッドレスで担保できない）

- 適合判定バッジ・所持枚数・追加ボタンの見え方（⭐ **srcdoc 方式で見るか、実物確認に委ねる**）。

### 10-5. ⭐ ユーザーの実物確認で見てもらうもの（②の工程）

⚠️ **レビュー合格後・承認の前**に、**3000番**（⭐ **実ログインなら `npx wrangler pages dev . --local --port 3000`**）で操作してもらう。

1. ⭐ **5つのタブを行き来したときの体感**（切り替えが速いか・どのタブを見ているか分かるか）
2. ⭐ **✕で条件を外しても再検索しないこと**（§2-3 の決定どおりか・**食い違いが気にならないか**）
3. **375px で検索タブを横に送る感触**（⚠️ **iPad 等のタッチはヘッドレスで測れない**）
4. **リロード後の復元**（U4 の再検索が待てる長さか）
5. **実ログインでのカード投入**（枚数・トースト・保存）

---

## 11. 申し送り・派生

| # | 内容 |
|---|---|
| **11-1** | ⭐ **サイドボードプランメモ**は本件の「動的タブ」に乗せる（起票案 §5）。⚠️ **本件の実装で `.tabs-scroll` を検索専用に決め打ちしない** |
| **11-2** | ⚠️ **`preferredArtIndex()` の二重定義**（派生起票案「ブラウザ側の代表画像」）は本件では直さない。⭐ **§8-1 の引数化はデッキ構築側だけ** |
| **11-3** | ⚠️ **タブ帯の sticky 固定**は右列の構造から作り直しになる（§1 やらないこと 3）。要望が出たら別タスク |
| **11-4** | ⭐ **P1 は本件の公開まで本番に残る**（ユーザー決定・§2-2）。⚠️ **本件が先送りになったらそのときに起票する** |
| **11-5** | ⚠️ **モック（2026-09-15版）の `inject.js` を実装が写さないこと**。⭐ **✕の挙動は 2026-09-16 に覆っており、モックは同日に直してある** |
