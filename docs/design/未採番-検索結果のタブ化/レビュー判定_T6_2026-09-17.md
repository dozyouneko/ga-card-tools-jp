**対象: 「検索結果のタブ化」**

# レビュー判定 — T6（検索タブの寄せ方の是正）2026-09-17

⚠️ **これは issueコメントの代替である。** GitHubアカウント停止中で `gh issue comment` が打てないため、ここに書く。
復旧して採番されたら、本文をそのまま
`gh issue comment <n> --body-file docs/design/未採番-検索結果のタブ化/レビュー判定_T6_2026-09-17.md` で投函する。

⭐ **実装者本人ではない別セッション（レビュー担当）による判定である。** 開発担当ともスプリント進行役とも
コンテキストを共有していない。判断材料は **設計書 1.4・工程記録・コミットの中身・自分で動かした結果**だけで、
⚠️ **実装報告の「検証済み」を合格の根拠にしていない**（全項目を自分で測り直した）。

| | |
|---|---|
| **判定** | ⭐ **合格** |
| 対象コミット | ⭐ **`31a98522`**（本体・`tools/deck-builder/app.js` の1関数）／`c09d14a9`（報告）。起点 `4aa4f6d4` |
| 設計書 | [`検索結果のタブ化_設計.md`](検索結果のタブ化_設計.md) **1.4**（§4-3-1〜4-3-3・§9-4・§10 V15-c／V15-d） |
| 証跡 | `tmp/review/未採番-検索結果のタブ化/t6rv-*`（スクリプト13本・スクリーンショット11枚。gitignore） |
| 環境 | after＝**3100**（作業ツリー）／before＝**3111**（`4aa4f6d4` を `git archive` で別ディレクトリへ展開し、**そこの `serve.mjs`** を起動）。Playwright 1.61.1・`--ignore-certificate-errors` ＋ `ignoreHTTPSErrors` |
| **次の担当** | **合格＝設計確認待ち**（⭐ **次はユーザーの2回目の実物確認**。§6 に「レビューが担保した範囲」を表で書いた） |

⚠️ **サーバーは測定後に停止済み**（`ss -ltnp` で 3100 / 3111 とも待受なしを確認）。

---

## 1. 必須基準

| 基準 | 結果 | 証跡 |
|---|---|---|
| **M1 設計適合** | ⭐ **✅** | **V15-c・V15-d が全番号一致**（§2）。**期待値は「画面に出る最終形」で照合**——`scrollLeft` の数値だけでなく、**左端とのズレ（0±1px）／完全に見えているか／📊統計 の右端からの距離／実際に見えているタブのラベル**まで測った |
| **M1-b 既存条件の副作用** | ⭐ **✅** | **絵柄の追従（V22 ①〜④）・共有URL（V23＝確認のうえ対象外）・バッジ集計（V24）・件数表示（V25・V25-b・V25-c）** を全部測った（§3）。⚠️ **T5 で閉じた「期限つきの穴」＝V22 の再発なし** |
| **M2 実駆動** | ⭐ **✅** | **PC幅（1200 / 1440）と 375px の両方**で自分で操作。スクリーンショット: `t6rv-band-before-375.png` / `t6rv-band-after-375.png` / `t6rv-reload-before.png` / `t6rv-reload-after.png` / `t6rv-full-after-375.png` / `t6rv-full-after-1440.png` / `t6rv-smoke-*` |
| **M3 回帰** | ⭐ **✅** | `npm run validate` **exit 0**（成功行は §4）。主要5画面のスモーク（トップ・カード詳細モーダル・カード個別ページ・デッキ構築・大会一覧）が **1440 / 375 とも `pageerror 0`**（§4） |
| **M4 生成物の同期** | ⭐ **✅** | **生成ロジックを1行も触っていない**（差分は `app.js` の1関数のみ）。訳の追加なし＝`gen-tl-json.mjs` 不要。`validate` の `generated JSON up to date` も緑 |
| **M5 静かな劣化がない** | ⭐ **✅** | **検査は足していない**（足していないので「わざと壊して落ちる」対象は無い）。⭐ **代わりに測定側の空回りを2重に潰した**——① **before/after の「既知の差」検算**（§5-1） ② ⭐ **反証テスト**（after の画面で旧式を手で再現すると症状が戻る・§5-2）。⚠️ **「動いたから合格」にしていない** |
| **M6 公開安全性** | ⭐ **✅** | 本番配信物の増減は **`tools/deck-builder/app.js` の更新1件のみ**（`git show --name-status 31a98522`）。**新規・削除なし**。`_headers` / `404.html` / `functions/` は**差分なし**。コミットに秘匿情報なし（`secret|token|password|api_key|PRIVATE KEY` の一致 0件）。⚠️ **push はされていない** |
| **M7 スコープ** | ⭐ **✅** | `git -c core.quotepath=false diff --name-only 4aa4f6d4..HEAD` → **`tools/deck-builder/app.js` と本タスクの報告 `.md` の2つだけ**（15行追加 / 6行削除）。⚠️ **トップ（ルートの `index.html` / `style.css` / `app.js`）・`shared/`・`scripts/`・`#view-deck`・CSS は1件も出ない** |

⚠️ **「未検証」は1件も無い**（ヘッドレスで担保できないものは §6 に「ユーザーにしか判定できない部分」として分けた）。

---

## 2. V15-c / V15-d（T6 の本体）

### 2-1. ⭐ 設計書が禁じた3つを採っていないこと（コードで確認）

| 禁止 | 実測 | 結果 |
|---|---|---|
| ❌ `offsetLeft` の継続使用 | `grep -rn offsetLeft tools/deck-builder/` → **ヒット2件はどちらもコメント行**（`// ⚠️ ⭐ offsetLeft を使わない…`）。**実行されるコードに `offsetLeft` / `offsetWidth` は無い** | ⭕ |
| ❌ `.tabs-scroll` に `position: relative` | **CSS は1文字も変わっていない**（差分に `style.css` が出ない）。実行時に `.tab-item` の **`offsetParent` は `BODY`・`box.offsetLeft = 232`** のまま＝**`position: static` のまま直している** | ⭕ |
| ❌ `element.scrollIntoView()` | `grep -rn scrollIntoView tools/deck-builder/ shared/js/` → **ヒット1件はコメント行のみ** | ⭕ |
| ⚠️ ❌ `behavior: "smooth"` の先取り | `grep -rn smooth tools/deck-builder/` → **0件**。⭐ **即時のまま**＝§10-5-3 の B を先取りしていない | ⭕ |
| ⭐ `updateTabsFade()` を残す | 関数末尾に残っている。**リロード復元後の `fadeR` が false → true に変わった**（右に続きができたので正しい追従・§2-3） | ⭕ |

⭐ 実装は **`box.scrollLeft += tab.item.getBoundingClientRect().left - box.getBoundingClientRect().left;`** の1行＋`updateTabsFade()`。
**クランプのコードは足されていない**（ブラウザ任せ＝設計の決定2）。

### 2-2. ⭐ V15-c — 375px・5タブ（`clientWidth 127 / scrollWidth 472 / maxScroll 345`。before/after 同一）

⚠️ **判定は「左端一致（0±1px）」と「完全に見えている」で行った。px の絶対値は参考。**

#### 経路1: タブを押す（毎回 `scrollLeft = 0` から）

| 押したタブ | ⚠️ before `scrollLeft` / ズレ / 完全に見える / 見えているタブ | ⭐ after `scrollLeft` / ズレ / 完全に見える / 📊統計の右端から |
|---|---|---|
| **検索1** | **195** / **−195** / ❌ / ⚠️ **1つも完全には見えず、画面には 検索3・検索4 が出る** | ⭐ **0** / **0** / ✅ / **17px** |
| 検索2 | 291 / −196 / ❌ / （検索4・検索5） | **95** / **0** / ✅ / **17px** |
| 検索3 | 345 / −154 / ❌ / 検索5 | **191** / **0** / ✅ / **17px** |
| 検索4 | 345 / −59 / ❌ / 検索5 | **286** / **0** / ✅ / **17px** |
| 検索5 | 345 / +37 / ✅（⭐ クランプで偶然合っていただけ） | **345** / +37 / ✅ / 54px（⭐ **クランプ＝仕様**） |

- ⭐ **before は設計書 §4-3-4 の実測値（195 / 291 / 345 / 345 / 345）と完全一致** ＝ **ユーザーが見つけたものと同じ症状を測れている**。
- ⭐ **after は設計書の期待値（0 / 95 / 191 / 286 / 345）と完全一致**。
- ⭐ **📊統計 の右端からの距離が 検索1〜検索4 すべて 17px** ＝ **「統計タブの横に来る」**（ユーザーの指示そのもの）。
- ⭐ **画像**: `t6rv-band-before-375.png` は **📊統計 の右に「検索3」が並び、押した「検索1」が画面から消えている**。
  `t6rv-band-after-375.png` は **📊統計 の右に、選択中（枠と上線つき）の「検索1」が並ぶ**。

#### 経路1b: 同じ操作を `scrollLeft = maxScroll` から（⚠️ 開始位置で飛び先が変わらないこと）

| 押したタブ | before | after |
|---|---|---|
| 検索1 | **232**（⚠️ 0 から押した 195 と違う） | **0**（経路1 と同じ） |
| 検索2 | 328（⚠️ 291 と違う） | **95**（同じ） |
| 検索3 / 4 / 5 | 345 / 345 / 345 | **191 / 286 / 345**（同じ） |

⭐ **「開始位置によって飛び先が変わる」（実物確認結果 §2-1）が before で再現し、after で消えている。**

#### 経路3: キーボード ←/→（📊統計 から → を4回、そのあと ← を1回）

| 操作 | 選択 | before `scrollLeft` / ズレ / 完全に見える | after |
|---|---|---|---|
| → 1回目 | 検索1 | 195 / −195 / ❌ | ⭐ **0 / 0 / ✅** |
| → 2回目 | 検索2 | 291 / −196 / ❌ | **95 / 0 / ✅** |
| → 3回目 | 検索3 | 345 / −154 / ❌ | **191 / 0 / ✅** |
| → 4回目 | 検索4 | 345 / −59 / ❌ | **286 / 0 / ✅** |
| ← 1回 | 検索3 | 345 / −154 / ❌ | **191 / 0 / ✅** |

⭐ `aria-selected="true"` は**どの時点でも常に1つ**。

#### 経路2: 🔍検索で新規タブ（6回目の検索）

| | before | after |
|---|---|---|
| タブ | `検索2,検索3,検索4,検索5,検索6` | **同じ** |
| `scrollLeft` / `maxScroll` | 345 / 345 | **345 / 345** |
| 新しいタブ（検索6） | ズレ +37・完全に見える ✅ | **同じ** |

⭐ **§4-2「新しいタブは右端に作る」と矛盾しない**（クランプで右端に見える）。**before から劣化していない。**

#### 経路5: × で隣のタブへ移る

| 操作 | before | after |
|---|---|---|
| **右端の選択中タブを ×**（左隣へ） | `249` / +37 / ✅ | **同じ**（⭐ **移った先が右端なのでクランプ**＝差が出ないのが正しい場面） |
| **先頭タブを選んで ×**（右隣へ） | ⚠️ `154` / **−154** / ❌ | ⭐ **`0` / 0 / ✅** |

#### ⭐ 経路4: リロード復元（⚠️ **設計が「59px はみ出す」と報告した経路**）

タブ5つ・**左から4番目**を選んでから `reload()`。

| | ⚠️ before | ⭐ after |
|---|---|---|
| リロード**前** | `345` / ズレ **−59** / ❌ | `286` / ズレ **0** / ✅ |
| リロード**後** | `345` / ズレ **−59** / **完全に見える ❌** / 見えているのは 検索5 | ⭐ **`286` / ズレ 0 / ✅ / 見えているのは 検索4** |
| 📊統計の右端から | −42 | ⭐ **17px** |
| 復元 | タブ5つ・`aria-selected=true` は1つ | **同じ** |
| 端のぼかし | `fadeL true / fadeR false` | `fadeL true / fadeR true`（⭐ **右に続きができたので正しい**） |

⭐ **設計が報告した `alignGap −59` が before で再現し、after で 0 になった。**
画像: `t6rv-reload-before.png`（📊統計の右に 検索5）→ `t6rv-reload-after.png`（📊統計の右に選択中の 検索4）。

#### V15-c の判定

| 判定基準（§10 が正） | 結果 |
|---|---|
| 押したタブの左端が箱の左端と一致（0±1px） | ⭐ **検索1〜検索4 で 0**（検索5 は仕様どおりクランプで +37） |
| 押したタブが完全に見えている | ⭐ **5タブとも ✅**（before は 検索5 だけ） |
| **5経路とも同じ結果** | ⭐ **一致**（押す／🔍で新規／キーボード ←→／リロード復元／× で隣へ） |
| before/after で差が出ていること | ⭐ **検索1 が `195`（押したタブが見えない）→ `0`（📊統計のすぐ右）** |

### 2-3. ⭐ V15-d — 1200px / 1440px

| 幅 | 箱 | 5タブを順に押したときの `scrollLeft` | 横あふれ | ページY |
|---|---|---|---|---|
| **1200px** | `clientWidth 616 / scrollWidth 616 / maxScroll 0` | ⭐ **5タブとも `0`（before/after 完全一致）** | **0** | `0 → 0` |
| **1440px** | `clientWidth 856 / scrollWidth 856 / maxScroll 0` | ⭐ **5タブとも `0`（before/after 完全一致）** | **0** | `0 → 0` |

- ⭐ **`maxScroll = 0` なので帯は1pxも横にずれない。**
- **📊統計 へ移っても `scrollLeft 0`・`fadeL / fadeR` とも false・可視パネルはちょうど1**（before/after 同一）。

### 2-4. ⭐ 中間幅での副作用（⚠️ **不合格の理由にしていない。ユーザーに見てもらう材料**）

設計書 §6 が予告した「**すでに完全に見えているタブを押しても左端まで寄る**」を、幅を変えて実測した
（5タブ・`scrollLeft 0` から **検索2** を押す）:

| 幅 | `maxScroll` | before | after | 動く量 |
|---|---|---|---|---|
| **640px** | 116 | `scrollLeft 58`・ズレ +41・完全に見える ✅ | `scrollLeft 99`・**ズレ 0** | ⭐ **41px** |
| **900px** | **0** | `0`・ズレ +99 | **同じ** | ⭐ **0（動かない）** |
| **1100px** | **0** | `0`・ズレ +99 | **同じ** | ⭐ **0（動かない）** |

⭐ **動きが出るのは帯が実際にあふれる幅（実測で 640px 付近まで）だけで、900px 以上では `maxScroll = 0` のため何も動かない。**
⚠️ 設計書 §6 の「およそ 640〜1199px で動きが目立つ」は**実測とややずれる**（900px では動かない）。§7 に確認事項として置いた。

---

## 3. M1-b（既存の同種の条件で効いている副作用）

| # | 副作用 | 実測 | 判定 |
|---|---|---|---|
| **V22 ①** | 絵柄の追従・もっと見るの続き | セット **PRD** で検索（`24 件を表示 / 全 232 件`）→ **🔍を押さずに**左ペインだけ **PRD 1st** に変える → もっと見る → **48枚の 🎨バッジは `{PRD:12, (1版のみ):36}`** ＝ ⭐ **`PRD 1st` は0枚（混在なし）** | ⭕ |
| **V22 ②** | 裏のタブ | PRD のタブと PRD 1st のタブを作り、**2回行き来してから**それぞれで もっと見る → **タブ1（PRD）= `{PRD:12, (1版):36}`／タブ2（PRD 1st）= `{PRD 1st:21, (1版):6}`** ＝ ⭐ **各タブが自分の条件の版のまま** | ⭕ |
| **V22 ③** | ⚠️ レアリティ（P5 の再発確認） | レアリティ `2` で絞って検索 → 1ページ目24枚・続き48枚とも **`img.src` と `dataset.artUrl` が食い違うタイル 0件** | ⭕ |
| **V22 ④** | 絞り込み無し | 🎨の初期版が `imgs[0]` のまま＝**食い違うタイル 0件** | ⭕ |
| **V22 ⚠️** | **V7-c を壊さない** | 3版あるカードの 🎨 を **`ALCSD → RDOA`** に切り替え → **別タブへ行って戻す** → ⭐ **`🎨 3・RDOA` のまま・`dataset.artUrl` も `img.src` も一致**。さらに **🃏デッキ 経由で枚数を変えて戻しても `RDOA` のまま** | ⭕ |
| **V23** | 共有URL（#20） | ⭐ **確認のうえ対象外**——デッキ構築の検索条件はURLに載らない（トップと違い `#f-…` の同期が無い） | ⭕（対象外） |
| **V24** | バッジ集計 | エレメント2＋クラス1で **バッジ `3`（表示）** → 「選択中の条件」の ✕ で **`2`** → **すべて解除** で **`0`・`hidden`**。⭐ **`/cards/search` は増分 0**（再検索せずに数だけ動く＝V10 と対） | ⭕ |
| **V25** | 件数表示 | タブごとに正しい（`24 件を表示 / 全 27 件` ／ `24 件を表示 / 全 30 件`）。裏のタブの件数が混ざらない | ⭕ |

---

## 4. 回帰（自分で測り直したもの）

| # | 実測 | 判定 |
|---|---|---|
| **V1** | 2回検索でタブ2つ。1つ目を押しても **`/cards/search` 増分 0**・grid 24・件数がそのタブのものに戻る・`aria-selected=true` は1つ | ⭕ |
| **V2** | 固定タブ（🃏デッキ・📊統計）の `.tab-close` は **0個**、検索タブは全部 × を持つ | ⭕ |
| **V3** | 選択中の 検索2 を × → `検索1, 検索3*` ＝ **右隣へ移る** | ⭕ |
| **V3-b** | 🃏デッキ を見ている間に裏の検索タブを × → **`ed-tab-deck` の `aria-selected` が true のまま**・パネルは `hidden` のまま・**検索タブの選択数 0**・帯の true は1つ | ⭕ |
| **V4** | 6回検索 → **タブ5つ**・最古が閉じる・トースト `タブは5つまでです — いちばん古い「検索1」を閉じました`・**連番は継続**（検索2〜検索6） | ⭕ |
| **V5** | 全部閉じてから検索 → ラベルは **`検索1`** | ⭕ |
| **V7** | 検索2 で ＋メイン → **検索1 へ切替でバッジ `1枚`（`hidden=false`）・追加行が `＋マテリアル / − [1] ＋ / ＋サイド / ＋検討中` のステッパー**。⭐ **`/cards/search` 増分 0** | ⭕ |
| **V7-b** | 🃏デッキ の ＋ で **メインを 1 → 2** にしてから 検索1 へ戻す → **バッジ `2枚`・ステッパー `[2]`**。⚠️ `activeSearchTab` が変わらない経路でも描き直されている | ⭕ |
| **V7-c** | 上記 V22 の欄のとおり（🎨 の選択がタブ往復・デッキ往復で戻らない） | ⭕ |
| **V10** | 「選択中の条件」の ✕ → **`/cards/search` 増分 0**・件数同一・grid 24→24・**条件の箱のテキスト同一**・タブ 1→1 | ⭕ |
| **V11（P1）** | `sword` 27件のまま左ペインを `shield` に書き換えて もっと見る → **27枚すべて sword**（sword でないもの **0**）・`27 件を表示` | ⭕ |
| **V12** | リロード → **タブ3つが残り `/cards/search` 増分 1**（最後に見ていたタブだけ） | ⭕ |
| **V12-b** | 📊統計 を見た状態でリロード → **統計タブが開く**・検索タブ3つは帯に残る・可視パネル1・⭐ **`/cards/search` 増分 0** | ⭕ |
| **V13** | `type=CHAMPION` + レベル順 → **`読み込み中 1/3 ページ…` が `ed-pane-search` の中**に出る。最終 `24 件を表示 / 全 133 件（レベルを持つカードのみ）` | ⭕ |
| **V14** | JP `リス` → `24 件を表示 / 全 42 件（日本語一致・翻訳済みのみ）`・**裏面一致の注記 `🔄 裏:滑空するリス`** が出る | ⭕ |
| **V14-b** | 375px・エレメント **AND で4つ** → `ELEMENT_AND_MESSAGE` が件数欄に出る・**エレメント群が開く**・**絞り込みが開く**・⭐ **`bandTop 0`**（before/after **完全一致**） | ⭕（※§8 の1） |
| **V15** | 375px で3回検索 → 毎回 **`scrollY 677` / `bandTop 0`**・**横あふれ 0**・固定2つは左に残る（`deckLeft 16` 不変） | ⭕ |
| **V15-b** | 375px 多項目（8行）→ 箱は**畳まれて 137px**（少項目 86px）・見えている行 **3**・`▾ すべて表示（ほか 5 項目）`・開くと `▴ 畳む` / `aria-expanded=true`・⭐ **箱の高さが 86 と 137 で違っても `bandTop` は両方 0**・横あふれ 0。**before/after 完全一致** | ⭕（※§8 の2） |
| **V16** | 1440px / 1200px・タブ帯が画面の上半分（`bandTop 178`）→ 検索しても **`scrollY 0` のまま** | ⭕ |
| **V17** | 条件8項目 → **3項目＋`▾ すべて表示（ほか 5 項目）`** | ⭕ |
| **V17-b** | 2項目のタブ → ⭐ **`textContent` が空文字**（`（ほか -1 項目）` を**組み立てていない**）・ボタンは `hidden` | ⭕ |
| **V17-c / V17-e** | `grep -rn 'nth-child' tools/deck-builder/` → **0件**（フォルダ全体） | ⭕ |
| **V17-d** | ⭐ **破壊試験**: `--accent` を `rgb(0,255,0)` に差し替えると箱の背景が `color(srgb 0.851 0.643 0.255 / 0.08)` → **`color(srgb 0 1 0 / 0.08)`** に変わる＝ハードコードではない | ⭕ |
| **V18** | ←/→ でタブ移動（`aria` true は常に1つ）／Delete で検索タブが閉じる | ⭕ |
| **V18-b** | パネルに `role="tabpanel"`／タブの `aria-controls` は **`ed-pane-search`**／⭐ **`aria-labelledby` がタブを移ると `ed-tab-search-1` → `ed-tab-search-2` に切り替わる**／固定タブ表示中はパネル `hidden`／`.cond-more` に `aria-controls="result-cond-list"` | ⭕ |
| **V18-c** | どの面でも**可視パネルはちょうど1**。⭐ **統計の鮮度**: 統計（`カードがありません`）→ 検索タブでカード投入 → 統計へ戻す → **`メインデッキ 1枚 / エレメント別 WIND（風）1`** に更新されている（before/after 同一） | ⭕ |
| **V19** | ⚠️ **閲覧専用画面（`#view-deck`）が1pxも変わらない**。⭐ **`rj-v19b.mjs` の形**で before(3111) と after(3100) を比較 → **1440px・375px とも `IDENTICAL`**（⭐ **走査ルール 旧473 / 新473** ＝ 空回りしていない） | ⭕ |
| **V20** | `npm run validate` **exit 0**。⭐ 検査① は **`onRemoveOne wiring in sync — 渡しているのは 0ファイル（なし）／25ファイル走査`**（§8-6 のとおり「0ファイル」が正）。`:root owners in sync — 4ファイル` / `pane threshold in sync` / `mobile band marker in sync` も緑 | ⭕ |
| **V21** | 差分は **`tools/deck-builder/app.js` と本タスクの `.md` の2つだけ** | ⭕ |
| **V25-b** | 持ち主タブを × → `#result-count` が **空文字**・「もっと見る」が `hidden`・grid 0・条件の箱も `hidden` | ⭕ |
| **V25-c** | ⭐ **取得中に隣のタブへ切り替える** → 表示中のタブに裏の結果が混ざらない（`7 件を表示` / grid 7）。戻すと **`/cards/search` +1**（取り直す＝仕様） | ⭕ |
| **「操作不具合2件」の回帰** | ⭐ **`#search-top` 子孫の `position: sticky` は 0件**／**`.row-actions` は `static`**／**`.pane-scroll` の背景は `rgb(15, 17, 21)`（不透明）**／**`--pane-top = 179px` が生きていて `#search-top` の `max-height` が `721px`（= 900 − 179）**。⭐ **before/after で完全一致** | ⭕ |
| **主要画面のスモーク（M3）** | 1440 / 375 とも: トップ（タイル27・`27 件を表示`）／**カード詳細モーダル**（`熟達の剣豪 / Adept Swordmaster / meta行7`）／カード個別ページ（`/cards/adept-swordmaster/` の h1 と title）／大会一覧（454行）／デッキ構築。**`pageerror` はすべて 0** | ⭕ |

⭐ **全スクリプトを通して `pageerror` は 0件。**

---

## 5. ⚠️ 測定が空回りしていないことの担保（2重）

### 5-1. before/after の「既知の差」で検算（⚠️ **本測定の前に実施**）

⚠️ before は **`4aa4f6d4` を別ディレクトリへ `git archive` で展開し、そこの `serve.mjs` を 3111 で起動**した
（⚠️ `cd` では `serve.mjs` の配信ルートが変わらない＝**before と after が同一になる偽の緑**を避けるため）。

| 測るもの | before（3111） | after（3100） |
|---|---|---|
| 配信された `app.js` の `tab.item.offsetLeft` | **1** | **0** |
| 同 `getBoundingClientRect().left - box.getBoundingClientRect` | **0** | **1** |

⭐ **両方向で食い違う**ことを確認してから測った。

### 5-2. ⭐ 反証テスト（after の画面で旧式を手で再現する）

after（3100）の 375px・5タブで、**`scrollLeft` を 0 に戻してから旧式の計算をページ内で手作業で実行**した:

| | `scrollLeft` | ズレ | 完全に見える |
|---|---|---|---|
| 現行実装（rect 差） | **0** | **0** | ✅ |
| **旧式を手で再現**（`offsetLeft` 基準） | ⚠️ **195** | **−195** | ❌ |

⭐ **同じ画面・同じタブで症状が戻る**＝ **測定は「たまたま通っている」のではなく、直したのはこの式である**ことの裏取り。
⭐ あわせて **`.tab-item` の `offsetParent` が `BODY`・`box.offsetLeft = 232`** を実測した
（**設計書 §4-3-3 の「232px ずれる」と一致**）。

---

## 6. ⭐ 2回目の実物確認（§10-5-3）— **レビューが担保した範囲 / ユーザーにしか判定できない部分**

⚠️ ⭐ **ユーザーが何を見ればよいかの下敷き。** 上げ方は **`npx wrangler pages dev . --local --port 3000`**
（⚠️ **3000番以外はユーザーのブラウザに届かない**）。

| # | 見てもらうこと | ⭐ レビューが担保した範囲（ヘッドレスで測った） | ⚠️ ユーザーにしか判定できない部分 |
|---|---|---|---|
| **A** | **T6 の直り** | ⭐ **「押したタブの左端が箱の左端に一致（0±1px）」「完全に見えている」を5経路すべてで実測**（§2-2）。⭐ **📊統計 の右端からの距離が 検索1〜4 とも 17px**。⭐ **右端タブはクランプで一番右**（= ユーザー決定2 のとおり）。⭐ **before で症状が再現し after で消えることも実測** | ⚠️ ⭐ **「統計の横に来た」と感じるか**（17px の間隔が意図どおりか）。⚠️ **右端のタブが右端で止まることを、実物でも許容できるか**。⚠️ **指でのスワイプと押下が混ざったときの挙動**（タッチはヘッドレスで測れない） |
| **B** | **アニメーションの要否**（①タブの横送り ②検索・並び替えのページの送り） | ⭐ **①②とも「即時」のままであることをコードと実測で確認**（`smooth` は0件・`scrollLeft` は1フレームで確定）。⭐ **どちらも直すなら1行**という設計の見立ては、実装を読んで妥当と確認した | ⚠️ ⭐ **「パッと動く」ままでよいか / なめらかにしたいか**——**体感なのでヘッドレスでは原理的に判定できない**。⚠️ **中間幅（〜640px 付近）では、すでに見えているタブを押しても 41px 動く**（§2-4）——ここが気になるかも合わせて見てほしい |
| **C-1** | タブの体感（切り替えの速さ・いまどのタブか） | ⭐ **切り替えで `/cards/search` が増えないこと**（V1・V7・増分0）＝**再取得待ちは発生しない**。⭐ **選択中のタブは `aria-selected=true` が常にちょうど1つ**・`.tab-item.on` で枠と上線が付く（`t6rv-band-after-375.png`） | ⚠️ **速い「と感じる」か**・**どのタブを見ているか一目で分かるか** |
| **C-2** | ✕・すべて解除で再検索しないこと／左ペインと条件の箱の食い違い | ⭐ **`/cards/search` 増分 0・件数・タイル・条件の箱がすべて不変**（V10）。⭐ **バッジだけが 3→2→0 と動く**（V24） | ⚠️ ⭐ **「左ペインは変わったのに結果は前のまま」が気にならないか**（注記を出さないのはユーザー決定） |
| **C-3** | 375px で検索タブを横に送る感触 | ⭐ **横あふれ 0・固定2つは左に残る・端のぼかしが正しく付く**（V15・§2-3） | ⚠️ **iPad 等のタッチのスクロール感** |
| **C-4** | リロード後の復元 | ⭐ **タブ5つが復元・選択が1つ・最後に見ていたタブだけ再検索**（V12）。⭐ **T6 で復元直後の寄せ方も直った**（§2-2 経路4） | ⚠️ **再検索の待ち時間が許容できるか**（絞り込み無しの数値順は約19秒＝#44） |
| **C-5** | ⭐ **実ログインでのカード投入・保存の往復** | ⚠️ ⭐ **担保できていない**——ヘッドレスは `/api/decks` を stub しているので、**本物の保存は一度も通っていない** | ⚠️ ⭐ **ここが唯一の判定機会**（枚数・トースト・リロード後に残るか） |
| **C-6** | ⭐ **2つのタブで同じカードの枚数が一致するか** | ⭐ **stub 上では一致を実測**（V7: 検索2 で ＋メイン → 検索1 で `1枚` / `− [1] ＋`。V7-b: デッキ経由で 2枚） | ⚠️ ⭐ **実ログイン（本物のD1）でも同じか**——1往復目の不合格の原因になった経路そのもの |
| **C-7** | 並び替えでページが飛ぶ感触 | ⭐ **送り先は常に「タブ帯の上端」**（`bandTop 0`）で、**条件の箱の高さ（86 / 137px）が変わっても動かない**（V15・V15-b） | ⚠️ **「並び替えを触っただけで飛ぶ」と感じるか**（＝ B に統合） |
| **C-8** | 3面の行き来と統計の鮮度 | ⭐ **可視パネルは常にちょうど1**・**統計 → 投入 → 統計 で中身が更新される**（V18-c） | ⚠️ **行き来したときのちらつき・スクロール位置の感触** |

---

## 7. ⚠️ 設計担当への確認事項（⭐ **レビューは裁定しない。合否には含めていない**）

| # | 事項 | 経緯 |
|---|---|---|
| **1** | **開発担当の確認事項1**: `scrollTabIntoView()` の関数コメントが **6行 → 13行**に増えた（`offsetLeft` を使わない理由・`scrollIntoView()` を使わない理由・クランプをブラウザに任せている理由・5経路が1か所を通ること）。⚠️ **自動検査は無く、守っているのはこのコメントだけ** | ⭐ **レビューの立場**: **合否には影響しない**（M5 は「検査を足したなら破壊試験」を求める基準で、T6 は検査を足していない）。⚠️ ⭐ **ただし「コメントだけが守っている」状態は `.pane-scroll` の背景（F2）と同じ型**で、**将来の整形で消されうる**。**削るか / 検査を足すか / このままかは設計担当の判断**。⭐ 実装そのものは **CSS に依存しない** ので、`position` が変わっても壊れない点は確認済み |
| **2** | **開発担当の確認事項2**: T5 からの持ち越し2件（① `searchStatusText()` のコメントに残る T4 前の前提 ② `preferredArtIndex()` の二重定義）は **T6 でも未着手** | ⭐ **レビューの立場**: **範囲外として正しい**（触っていたら M7 違反になった）。⚠️ **本タスクはこれで最後の単位なので、「持ち越したまま完了するのか / 派生に落とすのか」を承認時に決める必要がある** |
| **3** | **開発担当の確認事項3**: 2回目の実物確認で「なめらかに」となった場合、実装は **T7** になる理解でよいか | ⭐ **レビューの立場**: **仕様の判断なので設計担当へ回す**。⚠️ ⭐ **技術的な注意**: `behavior: "smooth"` を入れると **本判定の `scrollLeft` の実測はすべて取り直し**になる（途中値を返すので `waitForFunction` 待ちの形に変わる）。⭐ **V15-c の判定基準（左端一致・完全に見えている）は落ち着いた後なら同じまま使える** |
| **4** | ⭐ **レビューから1件**: 設計書 §6 の「**中間の幅（およそ 640〜1199px）では動きが目立つ**」は**実測とややずれる** | ⭐ **実測では 900px / 1100px は `maxScroll 0` で1pxも動かない**（§2-4）。**動きが出るのは帯があふれる 640px 付近まで**。⚠️ **2回目の実物確認でユーザーに見てもらう幅の案内に効く**ので、設計書の記述を直すかどうか判断してほしい |

---

## 8. 推奨基準（S）— ⚠️ **合格を妨げない指摘**

| # | 基準 | 指摘 |
|---|---|---|
| **S-1** | **S1 コード品質** | ⭐ **指摘なし**。1行の式＋既存のコメント様式に沿っている。`style` 属性なし（CSP `style-src 'self'`）・CSS 変更なし・バッジの座標にも触れていない |
| **S-2** | **S2 応答性** | ⭐ **指摘なし**。**タブ切り替えで公式APIを叩かない**ことを実測（V1・V7・V10・V12-b とも増分0）。T6 は同期の1行なので待ちを増やさない |
| **S-3** | **S3 fail-open** | ⭐ **ガードは `!tab || !tab.item || !el.edTabsScroll`**。⚠️ 開発担当が `!tab.btn` → `!tab.item` に変えたのは**参照するものとガードを揃える**変更で、`renderSearchTabs()` が両方を必ず同時に代入するため**挙動は変わらない**（差分1行で確認）。⭐ **帯が無い／要素が無い状態でも何もせず戻る** |
| **S-4** | **参考（合否に含めない）** | ⚠️ **V15-b の箱の高さは、設計書の「211px 前後・137px にはならない」と一致しない**（実測 **137px**・8行）。⭐ **ただし高さは条件の値の長さで変わる**（少項目 86px / 本レビュー 137px / 開発担当 174px）ので、**定数ではない**ことは確認できている。⭐ **判定の本体である「箱の高さが変わっても送り先（`bandTop 0`）が変わらない」は成立**。⚠️ **設計書の期待値の書き方（高さの絶対値）を `bandTop` に寄せると、測る人によってぶれなくなる** |
| **S-5** | **参考（合否に含めない）** | ⚠️ **V14-b は「絞り込みパネルを先に自分で開いてから」同じ操作をすると `bandTop 140` になる**（送った後に警告で左ペインが伸びるため）。⭐ **before / after で完全に同一**なので **T6 の回帰ではない**（pre-existing）。⭐ **アプリ自身が警告で絞り込みを開く通常の経路では `bandTop 0`**。⚠️ **§10 の V14-b に「絞り込みを先に開いていない状態で測る」と書き足すと、次の周で測る人が同じ回り道をしない** |

---

## 9. 実行できなかったこと・残っている確認

| # | 項目 | 扱い |
|---|---|---|
| 1 | **実ログインでの保存の往復・2タブでの枚数の一致**（§10-5 の 5・6） | ⚠️ **ヘッドレス不可**（`/api/decks` を stub している）。⭐ **§6 の C-5・C-6 として2回目の実物確認へ** |
| 2 | **タッチの感触・アニメーションの要否** | ⚠️ **原理的に測れない**。⭐ **§6 の A・B・C-3 へ** |
| 3 | ⚠️ **push が要る項目** | ⭐ **無い**（本単位はブラウザ側の1関数で、`_headers` / キャッシュ / Pages Functions に関わらない）。⚠️ **push は要求していない** |

---

**次の担当: 合格＝設計確認待ち / 不合格＝実装待ち** → ⭐ **本判定は合格なので `設計確認待ち`**（⚠️ **次はユーザーの2回目の実物確認 → 承認 → 公開**）。
