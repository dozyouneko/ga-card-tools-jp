# 実装報告 — 日本語モードの0件表示（2026-09-04）

⚠️ **GitHubアカウント停止中のため、issueコメントの代わりにこのファイルを置く。**
復旧後に `gh issue comment <n> --body-file` でそのまま投函する
（索引 [`待ち行列と復旧手順.md`](../待ち行列/待ち行列と復旧手順.md) §2-④）。

⭐ **状態: `レビュー待ち`**（ラベルの代わり。**次はレビュー担当**）

---

## 1. 実施内容

### コミット

| ハッシュ | 内容 |
|---|---|
| ⭐ **`5ebf817c`** | 未採番(日本語モードの0件表示): 絞り込みで0件になったときの文言を正す（⚠️ **レビューの対象はこれ**） |
| `f9da17df` | 本報告ファイル＋設計書の状態行を `レビュー待ち` に付け替え（`docs/` のみ） |

⚠️ **pushはしていない**（本番公開の可否はユーザーの判断）。`wrangler pages deploy` も実行していない。

### 変更ファイル（3件・**これ以外のパスには触っていない**）

| ファイル | 変更 |
|---|---|
| `shared/js/card-search.js` | ⭐ **`info` に読み取り専用の2値を足すだけ**（`jpFiltered` / `jpChecked`）。絞り込みの判定・結果には触っていない |
| `app.js`（トップ） | `updateSearchStatus()` の0件分岐に**新2分岐**（終端）＋**新1分岐**（途中） |
| `tools/deck-builder/app.js` | `searchStatusText()` に**同じ3分岐を同じ順序で**複製 |

### 実装の中身

**`shared/js/card-search.js`**（`run()` 内）:

- `jpFiltered` = `jpSlugs ? hasJpFilters() : false`。⭐ **設計 D3 のとおり `app.js` 側で数え直さない**
  （絞り込みが1つ増えた日に足し忘れると、無言で嘘の文言に戻るため）
- `jpChecked` = `Math.min(from + JP_PAGE_SIZE, jpCand.length)`。JPモードのページング分岐でだけ代入し、
  **非JPモードでは `0` のまま返す**（`jpMatched` 等と同じ扱い）
- ⚠️ `elementAndBlocked()` の早期returnは `jpMode: false` で、`app.js` 側も
  `info.blocked === "element-and"` で先に返すため、**この経路には手を入れていない**（従来どおり）

**`app.js` / `tools/deck-builder/app.js`**（0件時の分岐・**上から評価**）:

| # | 条件 | 文言 |
|---|---|---|
| A（途中・`hasMore`） | `jpMode && jpMatched > 0 && jpFiltered` | ⭐ **新規** 候補 {total} 件のうち {jpChecked} 件を確認しましたが、絞り込み条件に合うカードはまだありません。「もっと見る」で続きを確認できます。 |
| B（途中） | それ以外 | 従来のまま |
| 1（終端） | `jpMode && numericSort && jpMatched > 0` | 従来のまま（#43）。⚠️ **新分岐より先に評価する** |
| 2（終端） | `jpMode && jpMatched > 0 && jpFiltered` | ⭐ **新規** 日本語テキストには一致しましたが、絞り込み条件に合うカードはありませんでした（絞り込みを外すと表示できます）。 |
| 3（終端） | `jpMode && jpMatched > 0`（＝`!jpFiltered`） | ⭐ **新規** 日本語テキストには一致しましたが、カード情報を取得できませんでした（時間をおいて再度お試しください）。 |
| 4（終端） | `jpMode`（＝`jpMatched === 0`） | 従来のまま |
| 5（終端） | 非JPモード | 従来のまま |

- 数字は**3桁区切りを入れていない**（既存の件数表示 `40 件を表示 / 全 141 件` が入れていないため。
  設計 §4-3-2 の「新しい書式を発明しない」に従った）
- ⚠️ **自動追従は入れていない**（設計 §8・案X）。→ **V14**

### 検証環境

- `PORT=3000 npm run dev` ＋ Playwright（`launch({ args: ["--ignore-certificate-errors"] })` ＋
  context `ignoreHTTPSErrors: true`）。**検証後にサーバーは停止済み**
- 検証スクリプトは `tmp/dev/0件表示/`（⚠️ gitignore）:
  `verify.mjs`（トップ）/ `v11.mjs`（破壊試験）/ `deck.mjs`・`deck-a.mjs`（デッキ構築ツール）

---

## 2. 検証結果（**V1〜V14 を全部実施**）

⭐ **すべて合格。「未検証」は無い。**

| # | 条件 | 期待値 | ⭐ 実測 | 判定 |
|---|---|---|---|---|
| **V1** | `/?q=アリス&set=PRD` | 1画面で終端・絞り込み文言 | tiles=0 / もっと見る**非表示** / APIリクエスト**0回**（索引段階で全滅）<br>**「日本語テキストには一致しましたが、絞り込み条件に合うカードはありませんでした（絞り込みを外すと表示できます）。」** | ✅ |
| **V2** | `/?q=の&set=RDOPD&rarity=6` 1画面目 | tiles=0・もっと見る表示・**141／40** | tiles=0 / もっと見る**表示**<br>**「候補 141 件のうち 40 件を確認しましたが、絞り込み条件に合うカードはまだありません。「もっと見る」で続きを確認できます。」** | ✅ |
| **V3** | 同・2画面目／3画面目 | **80／120** に変わる | 2画面目 **80 件** / 3画面目 **120 件**（他は同文） | ✅ |
| **V4** | 同・4画面目（終端） | もっと見る非表示・V1 と同じ文言 | tiles=0 / もっと見る**非表示**<br>**V1 と完全に同じ絞り込み文言** | ✅ |
| **V5** | `/?q=zzzzあい` | ⭐ **変わらない** | **「日本語テキストに一致する翻訳済みカードが見つかりませんでした（未翻訳のカードは日本語検索できません。英語での検索もお試しください）。」**（従来どおり） | ✅ |
| **V6** | `/?q=アリス`・`/?q=の&set=RDOPD` | 件数表示が従来どおり | `8 件を表示（日本語テキスト一致・翻訳済みのみ）` / `40 件を表示 / 全 141 件（日本語テキスト一致・翻訳済みのみ）`<br>⭐ **0件でないときの経路にはコード上も1行も触れていない**（差分で確認） | ✅ |
| **V7** | ⚠️ **デッキ構築ツール**で日本語検索＋エキスパンションで0件 | トップと同じ3分岐 | `q=アリス` ＋ エキスパンション PRD → tiles=0 / もっと見る非表示<br>**「日本語テキストには一致しましたが、絞り込み条件に合うカードはありませんでした（絞り込みを外すと表示できます）。」**＝**トップと同一文言** | ✅ |
| **V8** | ⚠️ **デッキ構築ツール**で日本語一致0 | 従来の文言のまま | `q=zzzzあい` → **従来の文言**（変化なし） | ✅ |
| **V9** | 英語モードで0件 `/?q=zzzzzz` | 従来のまま | **「該当するカードがありません。条件を変えてお試しください。」**（JPモードの分岐が漏れ出していない） | ✅ |
| **V10** | ⭐ **#43 の分岐が先に評価されるか** | #43 の文言が出る | `/?q=アリス&set=PRD&sort=level`（⭐ **絞り込みと数値並び替えを両方掛けた**＝分岐2に横取りされうる条件）<br>→ **「日本語テキストには一致しましたが、レベルを持つカードはありませんでした（並び替えを「名前順」に戻すと表示できます）。」**＝#43 が勝つ | ✅ |
| **V11** | ⭐ **破壊試験**: `jpFiltered` を常に `false` に | V1 が分岐3に落ちる | **「日本語テキストには一致しましたが、カード情報を取得できませんでした（時間をおいて再度お試しください）。」**<br>⭐ **`jpFiltered` が実際に分岐を選んでいることを確認**。⚠️ **作業ツリーは書き換えていない**（Playwright の `route()` でレスポンスだけ差し替えたので戻し忘れが起きない） | ✅ |
| **V12** | 分岐3の到達性 | 絞り込みなしのJP検索では0件にならない | `/?q=アリス` 8件 / `/?q=の` 1,599件 / `/?q=ドラゴン` 1件<br>⭐ **通常操作で分岐3は一度も出なかった**（＝設計どおり「出たら異常」の位置にある） | ✅ |
| **V13** | `npm run validate` | exit 0 | **exit 0 / `all checks passed.`**（`no line-number refs in code comments — 0件（45ファイル走査）` も緑） | ✅ |
| ⭐ **V14** | ⚠️ **自動追従が入っていないこと** | 開いた直後 **40回**・**3回**クリック | `/?q=の&set=RDOPD&rarity=6` を開いた直後の api.gatcg.com へのリクエスト **40回**（累計。141回ではない）。<br>⭐ 以降は**クリックのたびに +40 / +40 / +21**（累計 80 → 120 → **141**）で、**終端まで3回のクリックが要る**。⭐ **勝手に進まない** | ✅ |

### 補足検証（V番号外・自主的に追加）

⚠️ **デッキ構築ツールにはレアリティ絞り込みが無い**ため、**通常操作では「0件×`hasMore`」（分岐A）を作れない**
（エキスパンション等は索引で候補を絞るので、候補は必ず条件を満たす）。
⭐ **入れた分岐Aが死にコードでないことを確かめるため**、カード取得を失敗させて同じ状態を作った:

- `q=の` ＋ エキスパンション RDOPD で `api.gatcg.com/cards/**` を全部 abort
- → **「候補 141 件のうち 24 件を確認しましたが、絞り込み条件に合うカードはまだありません。「もっと見る」で続きを確認できます。」**
- ⭐ **24 はデッキ構築ツールの `jpPageSize`**（トップは40）。**`jpChecked` がページサイズを正しく反映している**

---

## 3. 実行できなかった検証・残っている確認

| 項目 | 状況 |
|---|---|
| **デッキ構築ツールの実ログイン状態での確認** | ⚠️ **未実施**。V7・V8 は CLAUDE.md / #51 の標準手順（`#view-editor` の `hidden` を外し、**空のデッキ状態を用意してから**ページ本来の `searchCtl` を動かす）で検証した。⭐ **0件文言はデッキの中身を一切読まない**（`searchStatusText()` は `info` と結果グリッドの子要素数だけを見る）ので、この手順で担保できる範囲に収まっている。⚠️ ただし**実ログインでの目視は行っていない** |
| **本番での確認** | ⚠️ **未実施**（未push）。`_headers` や `404.html` に関わる変更ではないので `npm run dev` で再現する範囲 |
| ⭐ **ユーザーによる実物確認（3000番）** | ⚠️ **これから**。CLAUDE.md の「レビュー合格後・承認の前にユーザーがローカルサーバーで操作する」工程が残る（設計 §5・D7）。⚠️ **検証で使ったサーバーは締めの規約どおり停止済み**なので、その工程では**改めて 3000番で上げ直す**こと |

---

## 4. 設計書の想定との食い違い

⚠️ **数値は設計書 §7 の注記どおり実装時に測り直した。結果、期待値はすべて 2026-09-04 の値と一致した**
（候補 **141** 件 / **40・80・120** 件・`?q=アリス` **8** 件）。⭐ **差異なし。**

食い違いと言える差は**1点だけ**で、いずれも**設計書の書き方の粗さ**であり実装の逸脱ではない:

| 箇所 | 設計書の書き方 | 実際の画面 | 判断 |
|---|---|---|---|
| **V6** | 「**141 件を表示**（…）」 | `40 件を表示 / 全 141 件（日本語テキスト一致・翻訳済みのみ）` | ⭐ **実際の画面が正**。1ページ40件なので初回に141件は並ばない。**この表示は変更前から同じで、本タスクでは1行も触っていない**（差分で確認済み）＝ V6 の意図（「0件でないときの表示に一切手が入っていないこと」）は満たしている |

---

## 5. 設計書からの逸脱と根拠

⭐ **逸脱なし。** 確認した点:

- ⭐ **R1**（絞り込みの結果を1件も変えない）: `matchesActiveFilters()` / `metaMatches()` に触っていない。
  足したのは `info` の**読み取り専用の2値だけ**（`hasJpFilters()` の呼び出しは副作用なし）
- ⭐ **R2**（`jpDropped` の注記は触らない）: 触っていない。`jpBlindDropped` も**使っていない**（設計 D2）
- ⭐ **R3**（#43 を先に評価）: 分岐の順序を保った。**V10 で実測確認**
- ⭐ **R4**（`jpMatched === 0` の文言は現行のまま）: **V5・V8 で実測確認**
- ⭐ **R5**（`total` を「結果の件数」として言い切らない）: 文言は**「候補 141 件のうち」**とし、候補の件数としてだけ使った
- ⭐ **R6**（デッキ構築ツールを片方だけ直さない）: **同じ3分岐を同じ順序で**入れた。**V7・V8＋補足検証で実測確認**
- ⭐ **R7**（CSP）: `textContent` / 戻り値の文字列だけの変更。style属性は増やしていない
- ⭐ **D6**（自動追従は入れない）: 入れていない。**V14 で実測確認**。
  ⚠️ 両ファイルの新分岐に「**自動で追い掛けない**」というコメントを残した（次に触る人が良かれと思って足さないように）
- ⚠️ **接尾の注記の語**（デッキ構築側の「日本語一致」）は**変えていない**（設計 §6・範囲外）
- ⚠️ **`<file>.js:<行番号>` 形式の参照**は新しいコメントに1つも書いていない
  （関数名で参照した）。`npm run validate` の `no line-number refs in code comments` が緑

### git の担当パス

`git status --short` で確認し、**ステージしたのは `app.js` / `shared/js/card-search.js` /
`tools/deck-builder/app.js` の3件だけ**。⚠️ `git add -A` は使っていない。
`docs/` 配下・`CLAUDE.md` に他セッションの変更は無かった。

---

## 6. 設計担当への確認事項

⭐ **仕様の判断が必要になった場面は無かった**（未決事項ゼロの設計書どおりに実装できた）。
以下は**判断を仰ぐものではなく、承認時の材料としての報告**:

1. ⭐ **分岐A（途中の文言）は、デッキ構築ツールでは通常操作では到達しない。**
   レアリティ絞り込みがトップにしか無いため（設計 §6 の「`jpBlindDropped` は使わないので影響しない」と同根）。
   ⚠️ **死にコードではない**（取得失敗時に到達する。補足検証で実測）が、
   **設計 §6 の「同じ3分岐を同じ順序で入れる」という決定に従って入れてある**ことを明記しておく
2. ⭐ **派生の候補（設計 §10・文言の二重定義）は本タスクでは起票していない。**
   ⚠️ 今回**同じ文言を2ファイルに書き写した**ため、二重定義の量は増えた（3分岐 × 2ファイル）。
   ⭐ **承認時に派生issueの要否を判断してほしい**
3. ⚠️ **未pushのまま。** 本番公開の可否はユーザーの判断（自律運用の規則どおり止まっている）
