**対象: 「日本語モードの0件表示」** — この宣言は 2026-09-11 に遡って追記した（索引の転記ドリフト防止）

# 設計完了コメント — 日本語モードの0件表示（2026-09-04）

⚠️ **GitHubアカウント停止中のため、issueの「設計完了コメント」の代わりにこのファイルを置く。**
復旧後に `gh issue comment <n> --body-file` でそのまま投函する
（索引 [`待ち行列と復旧手順.md`](../待ち行列/待ち行列と復旧手順.md) §2-④）。

⭐ **状態: `実装待ち`**（ラベルの代わり。**未決事項ゼロ・着手してよい**）

---

## 1. 設計書のパス

⭐ **[`日本語モードの0件表示_設計.md`](日本語モードの0件表示_設計.md)（v1.1）が正。**

⚠️ **冒頭の「実装担当へ — 先に読む3点」を必ず読むこと**（複製ファイル・自動追従の不採用・表示だけの変更）。

⚠️ **起票案は「下書き」であって正ではない。** とくに起票案 §4 の
**「案2はAPI負荷が増える」は実測して誤りと判明している**（設計書 §3-3 が正）。

## 2. 対象範囲

| 対象 | 内容 |
|---|---|
| ⭐ **`app.js`**（トップ） | `updateSearchStatus()` の0件分岐（終端3分岐＋途中1分岐） |
| ⚠️ ⭐ **`tools/deck-builder/app.js`** | **同じ3分岐が複製されている。同じ順序で同じ変更を入れる** |
| `shared/js/card-search.js` | ⭐ **読み取り専用の2値を `info` に足すだけ**（`jpFiltered` / `jpChecked`）。**絞り込みの判定には触らない** |

⚠️ **範囲外**: 絞り込みロジック（`matchesActiveFilters` / `metaMatches`）・`jpDropped` の注記・
0件でないときの件数表示・接尾の注記の語（デッキ構築側の「日本語一致」）・**自動追従**。

## 3. 検証項目の要約（**V1〜V14**・設計書 §7 が正）

| 区分 | # | 要点 |
|---|---|---|
| 直す症状 | **V1〜V4** | 絞り込みで0件になったとき、**終端は「日本語テキストには一致しましたが、絞り込み条件に合うカードはありませんでした（絞り込みを外すと表示できます）。」**／**途中は「候補 141 件のうち 40／80／120 件を確認しましたが、…」** |
| 壊さない | **V5・V6・V9** | 日本語一致0（`?q=zzzzあい`）と**英語モード**の文言が**変わらない**・0件でないときの件数表示が変わらない |
| ⭐ **要** | **V10** | **#43（数値並び替え）の分岐が先に評価される**——新分岐に横取りされていないこと |
| ⭐ **要** | **V11** | ⚠️ **破壊試験**: `jpFiltered` を常に `false` にすると V1 が分岐3に落ちる＝**実際に効いていることの確認** |
| ⭐ **要** | **V14** | ⚠️ **自動追従が入っていないこと**（開いた直後のAPIが**40回**で止まり、終端まで**3回**クリックが要る） |
| 複製側 | **V7・V8** | ⚠️ **デッキ構築ツール**でも同じ3分岐が効く（**#51 の手順**。⚠️ ログイン必須） |
| その他 | **V12・V13** | 分岐3の到達性／`npm run validate` **exit 0** |

⚠️ **期待値の数値（141／40／80／120）は 2026-09-04 の実測値。**
**禁止改定・新セット・訳の追加で動く**ので、実装時に設計書 §2 の手順で測り直し、
**差異があれば報告すること**（＝勝手に合わせない）。

## 4. 判断済み事項（⚠️ **蒸し返さない**）

| # | 決定 |
|---|---|
| **D1** | ⭐ **判定は `jpMatched > 0`**。索引で絞る前の生の日本語一致数なので、**索引段階で全滅（API 0回）のケースでも効く** |
| **D2** | ⚠️ **`jpBlindDropped` は使わない**（レアリティ設計 §5-P4「表示に使わない」を踏襲）。**索引段階で落ちたか取得後かはユーザーには同じこと** |
| **D3** | ⭐ **`info` に足すのは2値だけ**（`jpFiltered` / `jpChecked`）。⚠️ **`app.js` 側で数え直さない**——絞り込みが増えた日に**無言で嘘の文言に戻る**ため |
| **D4** | ⭐ **文言は #43 の型（「日本語テキストには一致しましたが、〜（〜すると表示できます）」）に揃える**。新しい書式を発明しない |
| **D5** | ⭐ **分岐3（取得できなかった場合）を分ける**——嘘を別の嘘に置き換えないため |
| **D6** | ✅ ⭐ **案X で確定**（2026-09-04 ユーザー決定）。**自動追従は入れない**。検討記録は設計書 §8-1・§8-2 |
| **D7** | ⭐ **HTMLモックは作らない**（文字列だけの変更でレイアウトが動かないため）。⚠️ **代わりに設計書 §5 の「変更後の文字列そのもの」が合意の対象**。⚠️ **ユーザーのローカルサーバー実物確認の工程は従来どおり行う** |

## 5. 未決事項

⭐ **無し。** §8 の唯一の未決だった「自動追従を入れるか」は 2026-09-04 に**案X**で確定した。

⚠️ **実装中に仕様の判断が要る事態になったら、推測で決めずにこのファイルの隣に確認事項を書いて止まること。**

## 6. 派生の候補（⚠️ **本タスクでは起票しない**）

⭐ **0件文言が `app.js` と `tools/deck-builder/app.js` に二重定義されている**（設計書 §10）。
⚠️ **接尾の注記の語が違う**ため共有モジュールへ寄せるのは本タスクの範囲を超える。
⭐ **承認時に派生issueの要否を判断する。**
