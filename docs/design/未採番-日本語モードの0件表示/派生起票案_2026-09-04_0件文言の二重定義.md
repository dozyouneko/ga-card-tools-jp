**状態: 生きている**（最終確認 2026-09-04）— 未起票。⭐ **設計書はまだ無い**＝ハンドオフラベル（`実装待ち`）は付けない。GitHub復旧後に `gh issue create`（`enhancement` / `着手可` / `小タスク`）／ 索引: [`待ち行列と復旧手順.md`](../待ち行列/待ち行列と復旧手順.md)

# 派生issueの起票案 — 検索結果0件の文言が `app.js` と `tools/deck-builder/app.js` に二重定義されている

⭐ **由来**: [`日本語モードの0件表示`](日本語モードの0件表示_設計.md) §10（設計時に予告した派生候補）／
[レビュー §6-S1](レビュー判定_2026-09-04.md)／[実装報告 §6-2](実装報告_2026-09-04.md)。
⭐ **承認時に設計担当が起票を決めた**（[`承認_2026-09-04.md`](承認_2026-09-04.md) §4-2）。

---

## 1. 症状（⚠️ **いま壊れている経路は無い**＝将来の穴を塞ぐ側）

検索結果が0件のときの文言と分岐が、**2ファイルに同じ内容で複製されている**:

| ファイル | 関数 | 形 |
|---|---|---|
| `app.js`（トップ） | `updateSearchStatus()` | `el.status.textContent` に代入し `el.loadMore.hidden` を設定して `return` |
| `tools/deck-builder/app.js` | `searchStatusText()` | ⭐ **文字列を返す**（`hidden` は呼び出し側） |

⚠️ **片方だけ直しても何も止まらない。** `npm run validate` にもレビューの必須基準にも引っかからず、
⭐ **「画面によって0件の説明が違う」という、ユーザーからは原因がまったく見えない壊れ方**をする。

## 2. ⚠️ ⭐ このリポジトリは同じ型で **4回** 失敗している

| 回 | 場所 | 記録 |
|---|---|---|
| 1〜3 | カード画像のバッジ位置（#25 / #26 / #35） | CLAUDE.md「カード画像に重ねるバッジ」 |
| 4 | 用語ハイライト規則（`shared/js/card-detail.js` と `scripts/build-card-pages.mjs` に約125行ずつ） | [`起票案_2026-08-30_用語ハイライト規則の二重定義.md`](../未採番-用語ハイライトの語形ずれ/起票案_2026-08-30_用語ハイライト規則の二重定義.md)（**生きている**） |

⚠️ ⭐ **2026-09-04 の「日本語モードの0件表示」で、この箇所の二重定義の量が
1分岐 → 3分岐 × 2ファイル に増えた**（`5ebf817c`）。

- ⭐ **これは設計 §6 の明示的な決定に従った結果であり、実装の落ち度ではない**
  （片方だけ直すほうが有害だった）。⭐ **増やした側として受け皿を残す**のが本起票案

## 3. 難しいところ（⚠️ **単純な切り出しでは済まない**）

| # | 差異 | 
|---|---|
| **1** | ⚠️ **戻り値の形が違う**（片方は DOM を直接触り、片方は文字列を返す）。→ ⭐ **`{ text, showLoadMore }` を返す純関数に寄せる**のが素直（要検証） |
| **2** | ⚠️ **接尾の注記の語が違う**（トップ「日本語テキスト一致」／デッキ構築「日本語一致」）。⭐ **どちらに寄せるか＝仕様の判断**なので、**設計担当が決める** |
| **3** | ⚠️ **デッキ構築ツールにはレアリティ絞り込みが無い**（`jpPageSize` も 24 で、トップの 40 と違う）。⭐ **文言そのものは共通化できるが、数字はページサイズに追従する必要がある** |
| **4** | ⚠️ **#43 の数値並び替えの分岐は `GA_CARD_SEARCH.numericSortLabel?.()` を呼ぶ**。共有側へ移すときに**呼び出し順（#43 を先に評価する）を絶対に崩さない** |

## 4. ⭐ 着手順の注意

⚠️ ⭐ **待ち行列の「デッキ構築の左ペイン化と検索条件の統一」に着手するなら、その前か同時にやる。**
同じ2ファイルの検索まわりを触るので、**逆順だと同じ整理が2回入る**。

- ⚠️ ただし**左ペイン化の起票案は `searchStatusText()` に触れていない**（本文を読んで確認した）ので、
  ⭐ **放っておくと吸収されずに残る**。だから独立した起票案として残す

## 5. 検証の目安（⭐ **設計書で具体化する**）

- ⭐ **トップとデッキ構築ツールで同じ条件を作り、0件の全分岐で一字一句同じ文言が出ること**
  （分岐は5つ＋途中2つ。[設計書 §4-3](日本語モードの0件表示_設計.md) の表がそのまま検証表になる）
- ⚠️ **`jpChecked` の数字がページサイズに追従すること**（トップ 40 / デッキ構築 24）
- ⚠️ **#43 の分岐が先に評価されること**（[設計書 V10](日本語モードの0件表示_設計.md) と同じ条件を再実行）
- ⭐ **破壊試験**: 共有側の文言を1つ書き換えると**両画面が同時に変わること**
  （＝二重定義が本当に解消したことの確認。**片方だけ変わったら未達**）
- `npm run validate` が **exit 0**

## 6. ラベル案

`enhancement` `着手可` `小タスク`

⚠️ **優先度は低い**（いま食い違っている文言は無い）。⭐ **価値は「次に文言を足す人が片方を忘れられなくなること」**。
