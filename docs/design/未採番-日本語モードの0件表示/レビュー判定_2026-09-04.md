# レビュー判定 — 日本語モードの0件表示（2026-09-04）

⚠️ **GitHubアカウント停止中のため、issueの「判定コメント」の代わりにこのファイルを置く。**
復旧後に `gh issue comment <n> --body-file` でそのまま投函する
（索引 [`待ち行列と復旧手順.md`](../待ち行列/待ち行列と復旧手順.md) §2-④）。

---

## 判定: ✅ **合格**

⭐ **次工程: `設計確認待ち`**（GitHub復旧後は
`gh issue edit <n> --remove-label レビュー待ち --add-label 設計確認待ち`）。
**最終承認とクローズは設計担当が行う。**

- レビュー対象コミット: **`5ebf817c`**（実装報告 `facef576` に記載のハッシュ）
- 設計書: [`日本語モードの0件表示_設計.md`](日本語モードの0件表示_設計.md) v1.1（検証項目 **V1〜V14**）
- 実装報告: [`実装報告_2026-09-04.md`](実装報告_2026-09-04.md)
- ⚠️ **実装報告の検証結果は申告として扱い、V1〜V14 を全部レビュー担当が自分で再実施した**
  （報告と同じ結果になったが、**報告を根拠にした項目はひとつもない**）
- 証跡: `tmp/review/未採番-日本語モードの0件表示/`（⚠️ gitignore対象）

---

## 1. 必須基準

| 基準 | 結果 | 証跡 |
|---|---|---|
| **M1** 設計適合 | ✅ | **V1〜V14 の全番号**を自分で再実施し、**期待値（画面に出る最終形の文言・数字・「もっと見る」の有無）と全一致**（→ §2）。⭐ **M1-b（既存条件との対称性）に当たる欠落なし**（→ §3） |
| **M2** 実駆動 | ✅ | `PORT=3100 npm run dev` ＋ Playwright で**トップとデッキ構築ツールの両方**を操作。**PC幅(1280) と 375px の両方**。スクリーンショット12枚（→ §5） |
| **M3** 回帰 | ✅ | `npm run validate` **exit 0**（`all checks passed.`）。主要5画面が無事（トップ／カード詳細モーダル／カード個別ページ／デッキ構築／大会一覧）。**JSエラー 0件**（→ §4） |
| **M4** 生成物の同期 | ✅ | 生成ロジックを**変えていない**。`scripts/` のどのビルドも `app.js` / `shared/js/card-search.js` / `tools/deck-builder/app.js` を**入力に取らない**（参照はコメントのみ）。0件文言は生成物（`cards/` `sets/` `index.html`）に**埋め込まれていない**ので再生成対象なし |
| **M5** 静かな劣化がない | ✅ | 破壊試験を**4通り**独立に実施し、**分岐が `jpFiltered` の値で実際に選ばれている**ことを両方向で確認（→ §2 V11）。⭐ **本タスクは検査（validate等）を足していない**ので「両辺が同源」型は該当しない |
| **M6** 公開安全性 | ✅ | 本番配信物の**増減なし**（既存3ファイルの改変のみ）。`_headers` / `404.html` / キャッシュ設定に変更なし。秘匿情報なし。未push |
| **M7** スコープ | ✅ | `5ebf817c` の変更は **`app.js` / `shared/js/card-search.js` / `tools/deck-builder/app.js` の3件だけ**で、設計完了 §2 の対象範囲と完全一致。⭐ **自動追従（案Y）のコードは1行も入っていない**（→ §2 V14） |

---

## 2. 設計書の検証項目との対応（**全番号・自分で実測**）

⚠️ 実測はすべて **2026-09-04 / `PORT=3100 npm run dev` / Playwright
（`--ignore-certificate-errors` ＋ `ignoreHTTPSErrors: true`）**。
`info` は `window.GA_CARD_SEARCH` の setter を乗っ取って `onResults(cards, info)` を記録した。

| # | 期待値 | ⭐ レビュー担当の実測 | 合否 |
|---|---|---|---|
| **V1** | `/?q=アリス&set=PRD` が1画面で終端・絞り込み文言 | tiles=**0** / もっと見る**非表示** / **api.gatcg.com へのリクエスト 0回**（索引段階で全滅）<br>文言＝**「日本語テキストには一致しましたが、絞り込み条件に合うカードはありませんでした（絞り込みを外すと表示できます）。」**<br>`info` = `{total:0, hasMore:false, jpMatched:8, jpFiltered:true, jpChecked:0}` | ✅ |
| **V2** | 1画面目が **候補141／40件** | tiles=0 / もっと見る**表示**<br>**「候補 141 件のうち 40 件を確認しましたが、絞り込み条件に合うカードはまだありません。「もっと見る」で続きを確認できます。」**<br>`info` = `{total:141, hasMore:true, jpMatched:1608, jpFiltered:true, jpChecked:40, approxTotal:true}` | ✅ |
| **V3** | 2／3画面目が **80／120** | **80 件** / **120 件**（他は一字一句同文）。`jpChecked` = 80 / 120 | ✅ |
| **V4** | 4画面目（終端）が V1 と同じ文言・もっと見る非表示 | もっと見る**非表示**、文言は **V1 と完全一致**。`jpChecked:141` | ✅ |
| **V5** | `/?q=zzzzあい` の文言が**変わらない** | **「日本語テキストに一致する翻訳済みカードが見つかりませんでした（未翻訳のカードは日本語検索できません。英語での検索もお試しください）。」**（従来どおり）。`jpMatched:0 / jpFiltered:false` | ✅ |
| **V6** | 0件でないときの件数表示が従来どおり | `/?q=アリス` → **`8 件を表示（日本語テキスト一致・翻訳済みのみ）`**（tiles=8）<br>`/?q=の&set=RDOPD` → **`40 件を表示 / 全 141 件（日本語テキスト一致・翻訳済みのみ）`**（tiles=40）<br>⭐ **差分でも 0件でない経路には1行も触れていない**ことを確認 | ✅ |
| **V7** | ⚠️ **デッキ構築ツール**で日本語検索＋エキスパンションで0件 → トップと同じ3分岐 | #51 の手順（`#view-editor` の `hidden` を外し、**空のデッキ状態を用意してから**ページ本来の `runSearch()` を動かす）で実施。`q=アリス` ＋ エキスパンション **PRD**<br>→ tiles=0 / もっと見る非表示 / **トップと一字一句同じ絞り込み文言**。`jpFiltered:true` | ✅ |
| **V8** | デッキ構築で日本語一致0 → 従来の文言 | `q=zzzzあい` → **従来の文言**（変化なし）。`jpMatched:0` | ✅ |
| **V9** | 英語モードで0件 → 従来のまま | `/?q=zzzzzz` → **「該当するカードがありません。条件を変えてお試しください。」**（`jpMode:false`）。デッキ構築側でも同文 | ✅ |
| **V10** | ⭐ **#43 の分岐が先に評価される** | `/?q=アリス&set=PRD&sort=level`（**絞り込みと数値並び替えを同時に掛け、`jpFiltered:true` かつ `numericSort:"level"` かつ `jpMatched:8` ＝分岐2に横取りされうる条件**を作った）<br>→ **「日本語テキストには一致しましたが、レベルを持つカードはありませんでした（並び替えを「名前順」に戻すと表示できます）。」**＝**#43 が勝つ**<br>⭐ **デッキ構築側でも同じ条件を作り、同じく #43 が勝つ**ことを確認 | ✅ |
| **V11** | ⭐ **破壊試験**: `jpFiltered` が実際に効いていること | ⚠️ **開発担当とは別の方法で実施した**（`route()` でスクリプトを差し替えるのではなく、`onResults` に渡る `info` を横取りして書き換える＝**画面側の分岐選択そのもの**を試験）。**4通り**:<br>(a) V1 で `jpFiltered=false` を強制 → **分岐3「…カード情報を取得できませんでした（時間をおいて再度お試しください）。」に落ちた**<br>(b) V5（一致0）で `jpFiltered=true` を強制 → **文言は変わらない**（`jpMatched>0` のガードが効いている＝**逆向きの確認**）<br>(c) 途中ページ（V2）で `jpFiltered=false` を強制 → **従来の「このページには該当がありませんでした。…」に戻る**<br>(d) デッキ構築側で `jpFiltered=false` を強制 → **分岐3に落ちる**<br>⭐ さらに **`jpFiltered` が固定値でないこと**は自然な条件でも裏取り済み（V1=`true` / V6a=`false` / V9=`false`）<br>⚠️ **作業ツリーは一切変更していない** | ✅ |
| **V12** | 分岐3の到達性（通常操作では出ない） | 絞り込みなしのJP検索 `/?q=アリス`(8件) `/?q=ドラゴン`(1件) `/?q=の`(1,599件) では**一度も出なかった**<br>⭐ **さらに「実際の機構」で分岐3に到達させた**（申告の再現ではなく独立確認）: `**/api.gatcg.com/cards/**` を全部 abort ＋ 絞り込みなし → `info={total:8, jpMatched:8, jpFiltered:false, jpDropped:8}` で**分岐3の文言**。**デッキ構築側でも同じ**<br>＝設計 §4-4 の「取得できなかったときだけ」という位置づけが**実機構で成立**している | ✅ |
| **V13** | `npm run validate` exit 0 | **exit 0** / `all checks passed.`（`no line-number refs in code comments — 0件（45ファイル走査）` も緑） | ✅ |
| **V14** | ⚠️ **自動追従が入っていないこと** | `/?q=の&set=RDOPD&rarity=6` を開いた直後の api.gatcg.com へのリクエストは**累計 40回**（**141回ではない**）。以降は**クリックのたびに +40 / +40 / +21**（80 → 120 → 141）で、**終端まで 3 回のクリックが要る**＝**勝手に進まない**<br>⭐ **静的にも確認**: `5ebf817c` の追加行に `loadMore.click` / `setTimeout` / `requestAnimationFrame` / ループの類は**1つも無い** | ✅ |

### 補足（V番号外・レビュー担当が独自に追加）

| 確認 | 実測 |
|---|---|
| **デッキ構築側の分岐A（途中）が死にコードでないか** | エキスパンション RDOPD ＋ `/cards/**` を全部 abort → **「候補 141 件のうち 24 件を確認しましたが、…」**。⭐ **24 はデッキ構築ツールの `jpPageSize`（トップは40）** ＝ `jpChecked` がページサイズを正しく反映している |
| **375px 幅で文言が破綻しないか** | トップ V1／V5／V2、デッキ構築 V7 とも折り返して収まる。**`documentElement.scrollWidth === clientWidth === 375`**（横はみ出しなし） |
| **`jpFiltered` の出所が絞り込みの実体と対称か** | `hasJpFilters()` は **MULTI（cls/element/type/subtype/**rarity**）＋ format ＋ set**、`matchesActiveFilters()` も**同じ集合**。⭐ **絞り込みを1つ増やしても両方が同時に効く**（設計 D3 の狙いが構造として成立している） |
| **押し出し（伝播ラグ）耐性** | 新 `app.js` × 旧 `card-search.js` の組み合わせでは `info.jpFiltered` が `undefined`＝falsy → **従来の文言に落ちるだけで壊れない**。逆順も同様。⭐ `jpChecked` は `jpFiltered` と同じオブジェクトから来るので `undefined 件` は原理的に出ない |

---

## 3. ⭐ M1-b（既存では効いている副作用が新しい条件では効かない型）の確認

本タスクは**絞り込み・並び替えを1つも足していない**ため、M1-b の4項目は原理的に該当しないが、
⚠️ **「新しく画面に出す値」があるので、既存の同種の値と対称かどうかは見た**:

| 副作用 | 確認結果 |
|---|---|
| 絵柄の追従 | ⭐ **該当なし**（結果0件のときの文字列だけを変える。タイル・`dataset.artUrl`・🎨バッジ・詳細モーダルに触れていない） |
| 共有URL（#20） | ⭐ **該当なし**（新しい条件を足していない。`setKeyOf` の prefix 方式は変更なし。V1・V2 とも**URL直打ちで同じ画面が復元される**ことを実測） |
| バッジ集計 | ⭐ **該当なし**（`選択中の条件` チップ・レアリティ群のバッジは従来どおり出ている＝`pc-page1.png`） |
| 件数表示 | ✅ **正しい**。`approxTotal:true`（レアリティ絞り込みで概算）でも文言は **「候補 141 件」** と書き、**「全141件」とは言い切っていない**（設計 R5・§4-2 のとおり。`total` は `jpCand.length` ＝候補数として正確） |

⭐ **M1 の欠落として扱うべきものは見つからなかった。**

---

## 4. 回帰（M3）の証跡

```text
$ npm run validate
… loaded OK — total cards: 2516
generated JSON up to date — data/tl-names.json / data/tl-effects.json
top set links up to date — index.html に /sets/ リンク 63本
meta.sets covers all sets — 63件
meta.subtypes covers all subtypes — 156種
meta.rarities covers all rarities — 9種（スナップショット 2495枚から観測）
card-meta-index sort keys OK — 2516slug / level 133 / power 1006 / life 858 / cost_memory 515 / flip 22
cron git add paths in sync — 8パス（README.md / CLAUDE.md）
issue drafts in sync — 起票案22件（生きている12 / 完了9 / 取り下げ1）
no line-number refs in code comments — 0件（45ファイル走査）
card term highlights up to date — 2495 ページ / 2517 節
all checks passed.
EXIT=0
```

| 画面 | 実測 |
|---|---|
| トップ（通常検索） | `/?q=アリス` → `8 件を表示（日本語テキスト一致・翻訳済みのみ）` / tiles=8 |
| **カード詳細モーダル** | タイルをクリック → `#gacd-modal` が表示され `#d-name` = **「アリス、歪みの女王」** |
| **カード個別ページ** | `/cards/abnegation/` → **200** / `<title>` = `自己放棄 | Abnegation - Grand Archive 日本語カードDB` |
| **デッキ構築** | `/tools/deck-builder/` がログイン画面まで正常に描画 |
| **大会一覧** | `/tournaments/` → **200** / 行 454 |
| JSエラー | ⭐ **`pageerror` 0件**（Web Analytics ビーコンのCORSはローカル固有で無関係） |

---

## 5. 証跡の置き場所

`tmp/review/未採番-日本語モードの0件表示/`（⚠️ **gitignore対象**）

| 種別 | ファイル |
|---|---|
| 検証スクリプト | `lib.mjs`（`info` 横取りフック）/ `probe.mjs`(V1) / `v2_v4_v14.mjs`(V2〜V4・V14) / `rest.mjs`(V5・V6・V9・V10・V12) / `v11.mjs`(破壊試験4通り) / `deck2.mjs`(V7・V8・デッキ構築の破壊試験と分岐A) / `mobile.mjs`(375px) / `regression.mjs`・`regression2.mjs`(回帰) / `edge.mjs`(§6-参考1) |
| スクリーンショット | `pc-page1.png` `pc-page4.png` `375-v1.png` `375-v2.png` `375-v5.png` `deck-pc-v7.png` `deck-375-v7.png` `reg-top.png` `reg-detail.png` `reg-cardpage.png` `reg-deck.png` `reg-tournaments.png` |

⚠️ **起動したサーバー（3100）は停止済み**（`ss -ltn` で 3100 / 8888 とも空きを確認）。
⚠️ **作業ツリーは変更していない**（`git status --short` が空）。

---

## 6. 推奨基準の指摘・参考（**合格を妨げない**）

### S1 コード品質

- ✅ 周囲の慣習に合っている（日本語コメント・`⚠` マーカー・理由を書くスタイル）。
  **style属性は増えていない**（CSP `style-src 'self'` に抵触しない）。バッジは触っていない
- ⚠️ **同じ3分岐・同じ文言が2ファイルに増えた**（`app.js` と `tools/deck-builder/app.js`）。
  ⭐ **これは設計 §6 の明示的な決定に従った結果であり、実装の落ち度ではない**が、
  **二重定義の量は 1分岐 → 3分岐 × 2ファイルに増えた**。
  ⭐ **設計 §10 の派生候補（共有モジュールへ寄せる）の要否判断は、承認時に設計担当へ**

### S3 fail-open

- ✅ **新旧の組み合わせで壊れない**（§2 の「押し出し耐性」）。
  `info.jpFiltered` が無い＝従来の文言に落ちるだけ

### 参考1（⚠️ **基準に紐づかないので合否に含めない**・**設計担当への確認事項に回す**）

⚠️ **「絞り込みあり × 候補を1件も取得できなかった」ときは、分岐2（絞り込み文言）が出る。**
実測（`/?q=の&set=RDOPD` ＋ `/cards/**` を全部 abort、終端まで4画面）:

```text
画面4: 日本語テキストには一致しましたが、絞り込み条件に合うカードはありませんでした（絞り込みを外すと表示できます）。
        （info: total=141, hasMore=false, jpFiltered=true, jpDropped=141）
```

- ⭐ **実装は設計 §4-3-1 の分岐表どおり**（分岐2の条件は `jpMode && jpMatched>0 && jpFiltered` だけ）
  なので **M1 の欠落ではない**。**設計の書き方が「絞り込みなし × 取得失敗」しか想定していない**という話
- ⚠️ ただし設計 §4-4 が分岐3を分けた理由は **「嘘を別の嘘に置き換えないため」**であり、
  この経路では **「絞り込みを外すと表示できます」が（外しても取得は失敗するので）その趣旨に外れる**
- ⭐ **退化ではない**——変更前も「日本語テキストに一致する翻訳済みカードが見つかりませんでした（…英語での検索も…）」
  という**より的外れな文言**だった。**改善したうえで残った端**である
- ⚠️ **極めて稀**（候補を1件も取得できないのは通信断・API障害のとき）。→ **§7 の確認事項1**

### 参考2（⚠️ **本タスクの範囲外・既存の挙動**）

⚠️ **`jpDropped`（#45「出たら異常の信号」）の注記は、結果が0件のときには画面に出ない。**
`jpDropNote()` は **0件分岐を抜けた後の `suffix` 側**にしか付いていないため
（`app.js` / `tools/deck-builder/app.js` とも）、上の参考1の状況では **`jpDropped=141` が画面に一切出ない**。

- ⭐ **変更前からそうであり、本コミットは1行も触っていない**（＝本タスクの不合格理由にはならない）
- ⭐ **参考1と同じ根に見える**ので、派生を検討するならまとめて扱うのが自然。→ **§7 の確認事項1**

---

## 7. 設計担当への確認事項

1. ⚠️ **「絞り込みあり × 取得失敗」のときの文言をどう扱うか**（→ §6 参考1・参考2）。
   ⭐ **合否には含めていない**（実装は設計どおり）。派生issueにするか、
   「稀なので現状のままでよい」と設計書に明記して閉じるかの**判断だけ**を求めたい
2. ⭐ **設計 §10 の派生候補（0件文言の二重定義を共有モジュールへ寄せる）の要否**。
   ⚠️ 本タスクで**二重定義の量が増えた**（1分岐 → 3分岐 × 2ファイル）ため、
   開発担当の報告 §6-2 と合わせて**承認時に判断してほしい**。
   ⚠️ **レビュー担当は自分で起票しない**
3. ⚠️ **設計書 §7 V6 の期待値の書き方**（「141 件を表示」）は実画面（`40 件を表示 / 全 141 件`）と食い違う。
   ⭐ **変更前から同じ表示で本タスクは1行も触っていない**ので合否に影響しないが、
   **次に同じ検証項目を書くときのために設計書側を直すかどうか**は設計担当の判断に委ねる

---

## 8. 残っている確認

| 項目 | 状況 |
|---|---|
| **デッキ構築ツールの実ログイン状態での目視** | ⚠️ **未実施**（Discord OAuth が要るためヘッドレスでは到達できない）。**#51 の標準手順**で検証した。⭐ **`searchStatusText()` は `info` と `#result-grid` の子要素数しか読まない**ことをソースで確認済みなので、**この文言に関してはデッキの中身に依存しない＝手順の担保範囲に収まる**（CLAUDE.md の「デッキ内容を読む表示は未検証として報告する」に該当する表示は本タスクに無い） |
| **本番での確認** | ⚠️ **未実施・未push**。⭐ `_headers` / `404.html` / Pages Functions に関わらない変更なので `npm run dev` で再現する範囲。**pushは要求しない**（本番公開の可否はユーザーの判断） |
| ⭐ **ユーザーによる実物確認（3000番）** | ⚠️ **これから**。CLAUDE.md の「レビュー合格後・承認の前にユーザーがローカルサーバーで操作する」工程が残る（設計 §5・D7）。⚠️ ⭐ **渡すときは 3000番で上げ直すこと**（`forwardPorts` が `[3000]` だけなので、レビュー用の 3100 ではユーザーのブラウザに届かない） |

---

## 9. まとめ

⭐ **必須基準 M1〜M7 をすべて満たしており、合格。**

⭐ **本タスクの要（設計書 §7 の但し書き）である V10・V11・V14 は、
「動いたか」ではなく「分岐が正しい順序で・正しい理由で選ばれているか」を見る形で独立に再実施した**:

- **V10** — 絞り込みと数値並び替えを**同時に掛けて**分岐2に横取りされうる状態を作り、**#43 が勝つ**ことを両画面で確認
- **V11** — `jpFiltered` を**両方向に**壊して分岐が入れ替わることを確認（さらに `jpMatched>0` ガードの逆向きも）
- **V14** — 初回40リクエスト・クリック3回を**実測**し、追加行に自動追従のコードが**1行も無い**ことを静的にも確認

⚠️ ⭐ **「直すファイルが2つある」型の失敗は起きていない。**
トップとデッキ構築ツールで**同じ条件を自分で作り、一字一句同じ文言が出る**ことを確認した
（`deck-pc-v7.png` / `deck-375-v7.png`）。
