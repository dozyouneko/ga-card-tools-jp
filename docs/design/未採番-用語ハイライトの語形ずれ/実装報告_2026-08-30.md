**対象: 「用語ハイライトの語形ずれ」** — この宣言は 2026-09-11 に遡って追記した（索引の転記ドリフト防止）

# 実装報告 — 訳文に書かれている用語がハイライト・用語解説に出ない（155件 / 140枚）

⚠️ **これは issueコメントの代替**（GitHubアカウント停止中で `gh issue comment` が打てないため、
ファイルに残す）。復旧後、起票したissueにこの内容をコメントとして転記する。

- 日付: **2026-08-30**
- 担当: **開発（実装）担当**
- 着手時の HEAD: **`93c2348c`**（作業ツリーはクリーン）
- 設計書: `docs/design/未採番-用語ハイライトの語形ずれ/用語ハイライトの語形ずれ_設計.md`（設計版）
- 申し送り: 同フォルダ `設計完了_2026-08-30.md`（H1〜H9 を読んだうえで着手）
- 次の担当: **レビュー担当**（ラベル相当: `レビュー待ち`）

---

## 1. 実施内容

### コミット

| ハッシュ | 内容 | ファイル数 |
|---|---|---|
| **`44980ecb`** | 実装4箇所 + 再生成した生成物 | **160**（`cards/<slug>/index.html` **155** + ソース5） |
| `（本報告のコミット）` | 本報告書（`docs/` のみ） | 1 |

⚠️ **pushはしていない**（本番公開の可否はユーザーの判断。`gh` も停止中で未使用）。

### 変更したファイル（ソース5件）

| # | ファイル | 変更 |
|---|---|---|
| 変更1 | `shared/js/card-detail.js` / `scripts/build-card-pages.mjs` | `matchedTerms()` を **案C′（英語ゲート ∪ 訳文駆動）** の規則にした（設計書 §3-1 の手順1〜3をそのまま実装）。`termCore()` を切り出し、`highlightTerms()` の中核語抽出も同関数に寄せた（規則の二重定義を作らないため） |
| 変更2 | 同上（2箇所） | `applyOutsideSpans()` が**複数クラスを保護**できるようにし（文字列 or 配列）、**2回目**の呼び出しの保護対象を `["card-name-hl", "term-hl"]` にした。**1回目は変更していない**（その時点で `term-hl` は存在しない） |
| 変更3 | `scripts/validate.mjs` | 生成済みカードページを走査する**片方向**の検査を追加（exit 1）。`node:fs/promises` の `readFile` を**並列8**（H7）。⚠️ `n += await …` の形は使っていない（H6） |
| 変更4 | `data/translations.js` / `scripts/check-terminology.mjs` | **コメントのみ**。コード・出力・exit code は不変 |
| 生成物 | `cards/<slug>/index.html` | `GA_SNAPSHOT_MAX_AGE_MIN=0 npm run build:cards` で再生成（**155ファイル**） |

### 手順

1. 変更前の実測（設計書 §7 末尾のコマンド15行）を取得 → **設計書の「変更前」列と全行一致**
2. 変更1・2 を **2ファイルに同型**で適用
3. 変更3（`validate` の検査）を先に入れ、**再生成前に** `npm run validate` を実行 →
   **139ページ**で exit 1（＝検査が現に赤いことを確認・V11 の前半）
4. 変更4（コメント）
5. `GA_SNAPSHOT_MAX_AGE_MIN=0 npm run build:cards`（スナップショットは
   `fetched_at 2026-08-27T12:32:12.556Z` / **2,495枚**を再取得せず使用）
6. V1〜V15 を実施（下表）→ コミット

---

## 2. 検証結果（設計書 §7 の V1〜V15・全22行）

⚠️ 数え方は `grep -o '<パターン>' <file> | wc -l`（`grep -c` は使っていない）。

| # | 対象 | 期待値 | 実測 | 判定 |
|---|---|---|---|---|
| **V1** | `crossroads-specter` / `class="term-hl">ヒット時` | 1（前 0） | **1**（前 0） | ✅ |
| **V1b** | 同上 / `cp-term-jp">ヒット時（On Hit）` | 1（前 0） | **1**（前 0） | ✅ |
| **V2** | `cometfall` / `class="term-hl">グリンプス` | 1（前 0） | **1**（前 0） | ✅ |
| **V2b** | 同上 / `cp-term-jp">グリンプス（Glimpse）` | 1（前 0） | **1**（前 0） | ✅ |
| **V3** | `astra-sight` / `class="term-hl">グリンプス` | 2（増えないこと） | **2**（前 2） | ✅ |
| **V3b** | 同上 / `cp-term-jp">グリンプス（Glimpse）` | 1 | **1**（前 1） | ✅ |
| **V4** | `aquamirage-whisper` / `class="term-hl">ヒット時` | 1 | **1**（前 1） | ✅ |
| **V5** | `elysian-aspirant` / `class="term-hl">エリュシオンオーラ` | 1（前 0） | **1**（前 0） | ✅ |
| **V5b** | 同上 / `class="subtype-hl"` | 0（前 1） | **0**（前 1） | ✅ |
| **V6** | `shadowstrike` / `cp-term-jp">プレパレーションカウンター（preparation counter）` | 1（前 0） | **1**（前 0） | ✅ |
| **V6b** | 同上 / `class="term-hl">プレパレーションカウンター` | 1（前 0） | **1**（前 0） | ✅ |
| **V6c** | 同上 / `cp-term-jp">プレパレーション（Prepare N）`（消えないこと） | 1 | **1**（前 1） | ✅ |
| **V7** | `fractal-of-refreshment` / `cp-term-jp">リザーブ<` | 1（前 0） | **1**（前 0） | ✅ |
| **V7b** | 同上 / `cp-term-jp">リザーブ可能（Reservable）`（消えないこと） | 1 | **1**（前 1） | ✅ |
| **V8** | `lu-bu-indomitable-titan`（**裏面**）/ `class="term-hl">キル時` | 1（前 0） | **1**（前 0） | ✅ |
| **V9** | 変更される生成物 | `cards/<slug>/index.html` **155** / `sets/` **0** / `cards/index.html` **0** / `sitemap.xml` **0** | **155 / 0 / 0 / 0** | ✅ |
| **V10** | 用語解説の増分（全数） | 140枚（1件増 125 / 2件増 15）・追加総数 155件・§4のキー別内訳と一致 | **140枚（125 / 15 / 3件以上 0）・155件**。18キーの**件数もslug一覧も §4 と完全一致**。⭐ **減った用語は0件**。型⑤ **15枚**（§2-5 の一覧と一致）・**140枚との重複 0** | ✅ |
| **V11** | `npm run validate` | 変更前 **139ページ**で exit 1 → 変更後 `card term highlights up to date — 2495 ページ` で exit 0 | **変更前 139ページ / exit 1**、**変更後 2495 ページ / exit 0**（実行時間 0.9秒 → **1.8秒**） | ✅ |
| **V12** | トップの詳細モーダル（`crossroads-specter`） | 「ヒット時」がハイライトされ、用語一覧に「ヒット時（On Hit）」が1行 | `term-hl` = `["ヒット時","追放","死亡時","追放領域","レガリア","バフカウンター","エフェメラル"]`、用語一覧 **7行**でうち「ヒット時（On Hit）」**1行**、`term-hl` 内の `subtype-hl` **0** | ✅ |
| **V13** | デッキ構築ツールのモーダル（同上） | V12と同じ結果 | **V12と完全一致**（`term-hl` の配列・用語一覧の並び・効果HTMLまで同一） | ✅ |
| **V14** | `data/tl-effects.json` の取得を失敗させる（fail-open） | 現行と同じ（英語ゲートのみの）用語解説が出てエラーにならない | 両画面とも用語 **6行**（＝変更前の `crossroads-specter` と同じ集合）・「ヒット時（On Hit）」**0**・**このコード由来のJSエラー 0** | ✅ |
| **V15** | `node scripts/check-terminology.mjs` | 変更前と同じ件数・exit code も不変 | **11件 / exit 0**（設計時点の実測 11件と一致。`starcall` 10件 + `charge` 1件）。「terms 辞書に無いカウンター語: 0種」 | ✅ |

### 追加で確認したこと（設計書の項目外・自主検証）

| 確認 | 結果 |
|---|---|
| **型⑤をブラウザ2面でも確認**（`elysian-aspirant`） | トップ・デッキ構築とも `term-hl` = `["エリュシオンオーラ","死亡時","軽減不能"]` / `subtype-hl` **0個** / **入れ子 0**（＝1語が二色に割れない） |
| ⭐ **ドリフトが「対象ファイルの内側」に同居していないか**（CLAUDE.md の教訓） | 155ファイルそれぞれについて、**本タスクが変える2つの節**（`効果（日本語）` / `このカードの用語`）を除去してなお差分が残るファイル = **0**。⭐ ファイル名の集合だけでなく**差分の中身**で確認した |
| コミット内容の担当パス | `git -c core.quotepath=false show --name-only --format= 44980ecb` = 160件（カードページ155 + ソース5）。`docs/` `CLAUDE.md` など担当外は**含まれない** |

---

## 3. 実行できなかった検証・残っている確認

**設計書の V1〜V15 は全項目実施した（未検証・未実行はゼロ）。** そのうえで残るもの:

| # | 残件 | 補足 |
|---|---|---|
| R1 | **本番（Cloudflare Pages）での確認は未実施** | **未pushのため**（push はユーザー判断）。公開後は素のURLとキャッシュ回避クエリを並べて全数一致するまで反復すること（CLAUDE.md の手順） |
| R2 | デッキ構築ツールの **Discord ログイン後**の画面は未検証 | V12/V13 で確認したのは**カード詳細モーダル**で、これはデッキの中身を読まない（`GA_CARD_DETAIL.init()` に `action` を渡していない）。**モーダルの検証にデッキ状態は不要**なので、標準手順の「空のデッキ状態を用意」は今回は不要だった |
| R3 | V13 のページで観測された 404 は**本件と無関係** | 静的サーバー（`npm run dev`）に Pages Functions が無いため `/api/me` が 404 になるだけ（変更前後で同じ）。実測して URL を特定済み |

---

## 4. 差分が設計書の想定と食い違った点

**食い違いなし。** 期待値と実測は全項目で一致した。

- 生成物: **155ファイル**（`sets/` `cards/index.html` `sitemap.xml` は 0）＝設計書 §6 のとおり
- 用語の追加: **155件 / 140枚**、キー別18種の内訳・slug一覧・型⑤の15枚・2件増の15枚まで**設計書 §4 / §2-5 と完全一致**
- `validate` の変更前の引っかかり: **139ページ**＝設計書 §5-3 のとおり（`shadowstrike` は原理的に不可視）
- ⭐ ドリフト混入も**内側・外側とも0**（`GA_SNAPSHOT_MAX_AGE_MIN=0` でスナップショットを固定した）

---

## 5. 設計書からの逸脱と根拠

**規則そのものからの逸脱は無い**（§3-1 の手順1〜3をそのまま実装した）。実装上の小さな判断が3点:

| # | 判断 | 根拠 |
|---|---|---|
| L1 | 中核語の抽出を **`termCore(entry)` に切り出し**、`highlightTerms()` の中核語抽出もこれを呼ぶようにした（従来はインラインで同じ式を書いていた） | ⭐ C′ では中核語が「**用語を採るかどうかのゲート**」と「**ハイライトする文字列**」の**両方**になる。式が2箇所に分かれていると片方だけ直したときに**採るのに光らない/光るのに採らない**という食い違いが起きる。振る舞いは変わらない（同じ式） |
| L2 | `applyOutsideSpans()` の第2引数を **文字列 or 配列**の両対応にした（設計書 §3-3 が「配列にするか、クラス名の交替を受け取れるようにする」と選択を許している） | 1回目の呼び出し（`highlightTerms` 側）を**書き換えずに残せる**ため。交替は `(?:…)` の**非キャプチャ**にした（`split` のキャプチャ数が変わると分割の偶奇がずれる） |
| L3 | 手順3で採った候補の**並び順**は「中核語の長い順（＝手順3の処理順）」にした | 設計書は「手順1の採用分（現行の並び）→ **手順3で採った候補**」とだけ定めており、候補どうしの順序は指定していない。⭐ **既存ページの並びは変わらない**（追加分は必ず末尾に付く）ことを V1b/V3b/V6c/V7b と全数照合（減った用語 0件）で確認済み |

**既存表記・`TRANSLATION.md` との照合**: 今回**訳文・用語辞書の中身は1文字も変えていない**（`data/translations.js` の変更はコメント3行の追加のみ、`data/tl/*.js` は無変更）。
したがって表記の整合は既存のまま。⚠️ `prepare`「プレパレーション（Prepare N）」と `preparation`「プレパレーションカウンター」を**別の用語として扱う**（H8）点は V6/V6c で担保している。

---

## 6. 設計担当への確認事項

**判断を要する事項はありません**（設計書の未決事項が「なし」で、実装中にも新たな判断は発生しなかった）。

情報共有として1点だけ:

- ⭐ **`validate` の新検査は「用語を足したのに `build:cards` を回していない」も検出する**（設計書 §5-1 の2番目）。
  今回の実測で、この経路が**現に赤くなること**（変更前 139ページ / exit 1）を確認できた。
  ⚠️ ただし **cron は `validate` を実行しない**ので、`data/translations.js` に用語を足した日の
  自動publishは止められない（#40 と同型。CLAUDE.md の記述どおり）
- 範囲外として切り出された派生issue候補2件（`starcall` の `jp` を訳文の字面に寄せるか /
  `terms` に登録が無い新キーワードの検出経路）には**着手していない**。⚠️ V15 で報告される11件は
  そのうち1件目の出どころ（`starcall` 10件 + `charge` 1件）で、**現状維持（D1）のまま**
