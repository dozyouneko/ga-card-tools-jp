# 設計完了コメント(issueコメントの代替) — 未採番: 版順序の非決定性

⚠️ **GitHubアカウント停止中で `gh issue comment` が打てないため、issueコメントの代わりにこのファイルに書く。**
起票できたら本文をそのままコメントとして投稿し、ラベルを **`実装待ち`** にする。

- 日付: **2026-08-30**
- 状態: **設計完了 → `実装待ち`**
- 担当の次工程: **開発担当**

---

## 1. 設計書のパス

- **`docs/design/未採番-版順序の非決定性/版順序の非決定性_設計.md`**(本体)
- 要件の出所: `docs/design/未採番-版順序の非決定性/起票案_2026-08-26.md`

## 2. 対象範囲

**触るのは `scripts/build-card-pages.mjs` の1ファイルだけ**(変更は3箇所)。

| # | 箇所 | 内容 |
|---|---|---|
| §4-1 | `day()` と `newestFirst()` の間 | **`editionOrder(a, b)` を新設**(5段の比較器) |
| §4-2 | `newestFirst()`(150-157行) | `.sort(editionOrder)` に差し替え |
| §4-3 | `main()` のセット別グルーピング | 生の配列順ではなく `newestFirst(card).editions` で回す |
| §4-4 | 生成物 | `GA_SNAPSHOT_MAX_AGE_MIN=0 node scripts/build-card-pages.mjs` で再生成し、**コミットに含める** |

⚠️ **`shared/js/card-i18n.js` は変更しない**(ブラウザ側の代表画像は派生・§8-1)。

## 3. 判断済み事項(実装側で決め直さないこと)

1. ⭐ **第2キー以降は「`meta.sets` の並び順 → レアリティ → カード番号(数値考慮) → 版slug」で確定**(§3-3)。
   **候補3案を実測比較して決めた**:

   | 案 | 第2キー以降 | `og:image` が HEAD から変わる |
   |---|---|---:|
   | C1 | レアリティ → 番号 → slug | **209** ← ⚠️ **却下**(本編セットよりスターターデッキの絵柄を選んでしまう) |
   | C2 | セットprefixの辞書順 → … | 16 |
   | **C3(採用)** | **`meta.sets` の並び順 → …** | **16** |

   - C2とC3が違うのは2枚だけ(`gentle-respite` / `spurn-to-ash`。同日の RDOPD vs RDOEVP)。**どちらもC3が妥当**
   - キー2に `meta.sets` を使うのは、**`setOrder()` が既にこのファイルの「どのセットが先か」の唯一の宣言**だから
2. **キー4(カード番号)とキー5(slug)の順序**: 逆にしても代表版は0枚しか変わらないので、
   **収録セット表が番号の自然順に読める**ほうを採る
3. ⭐ **`sets/**` にも同じ問題が効いている**(§1-4)。原因は `newestFirst()` ではなく
   **`main()` が生の配列順で「セットごとに最初の版」を採っている**第2の箇所。**2箇所直さないと片手落ち**
4. **生成物は本タスクのコミットに含める**(#30。コードだけ入れると翌朝のcronが同じ差分を無関係な自動コミットにする)
5. **モックは作らない**(レイアウトは1pxも動かない。代わりに §5-3 に実物の版番号での新旧対照表を置いた)

## 4. 実測値(設計時に測り済み・そのまま期待値に使える)

⚠️ **前提: スナップショット `fetched_at` = `2026-08-27T12:32:12.556Z`(2,495枚) / HEAD `03ff53a5`。**
⭐ **V1 がこの前提を検査する**(差分が出たら前提が崩れているので、そこで止めて報告)。

| 指標 | 値 |
|---|---:|
| 複数版を持つカード | 1,248 |
| うち `release_date` が同着(行順が変わりうる) | **549** |
| うち先頭が同着(`og:image` まで変わりうる) | **371** |
| **現行コードで `editions` を逆順にしただけで動くファイル** | **581**(カード549 + セット32) |
| ↑のうち `og:image` の変化 | **371** |
| **本修正で HEAD から動くファイル** | **84**(カード**76** + セット**8**) |
| ↑のうち `og:image` の変化 | **16**(slug一覧は §5-2) |
| `cards/index.html` / `sitemap.xml` / `index.html` / `cards.css` / `cards.js` | **すべて不変** |
| 全63セットページの行数(収録枚数) | **不変** |

## 5. 検証項目の要約(V1〜V10・全10項目)

⚠️ **すべて `GA_SNAPSHOT_MAX_AGE_MIN=0` を付けて実行する。**
実施順: **V1(実装前) → V3(実装前) → 作業ツリーを戻す → 実装 → 再生成 → V4〜V9 → コミット → V2 → V10**

| # | 内容 | 期待値 |
|---|---|---|
| V1 | **対照**: 実装前に固定スナップショットで再生成 | `git status` が **0件**。完走サマリが `カード2495ページ(和訳あり2494) / セット63ページ / … / sitemap.xml(3018URL)` |
| V2 | ⭐ **本丸**: `editions` を**逆順**にしたスナップショットで生成 | **バイト一致(差分0件)** |
| V3 | 実装**前**に V2 と同じ測定 | **581ファイル**(549+32)・`og:image` **371件** |
| V4 | 影響範囲 | **84ファイル**(cards 76 / sets 8)。**他のパスに1件でも出たら止める** |
| V5 | `og:image` が変わる16枚 | §5-2 の16 slug と**集合一致** |
| V6 | 代表版と表の行順(9枚) | §5-3 の「修正後」列と一致 |
| V7 | セットページ8件 | §5-4 のとおり・**全63ページで行数不変** |
| V8 | 不変であるべき5ファイル | 差分なし |
| V9 | 比較器が必ず決着する | `editionOrder === 0` の組が**0** |
| V10 | スコープ外に触っていない | 変更が `cards/` `sets/` `scripts/build-card-pages.mjs` のみ + `npm run validate` が **exit 0** |

⚠️ **V2 は「同じ入力で2回回してバイト一致」では代用できない**(入力が同じなら一致するのは当然)。
必ず**逆順スナップショット**で測ること。

## 6. 未決事項

**実装を止めるものは無い。** ただし次の2つは**このタスクの外**として保留:

1. ⚠️ **push(本番公開)はユーザー判断**。本番配信物 **84ファイル**が動き、**SNSに貼られた
   カードページの絵柄が16枚で変わる**。⚠️ GitHubアカウント停止中なので、出すなら
   経路② `CONFIRM=publish npm run deploy:prod`
   - ⭐ 急がなくてよいが、**cron復旧後の初回cronより前に出さないと `cards/**` で rebase 衝突する**
2. **派生2件(§8)。起票は設計担当の判断待ちなので、開発担当は着手しない**:
   - §8-1 **ブラウザ側の代表画像がカードページと食い違う(現行481枚 / 本修正後487枚)**。
     ⭐ **本修正が原因ではない(+6枚)**。直すなら `editionOrder()` を `card-i18n.js` へ移して共用する
   - §8-2 他の生成スクリプトに `editions` 配列順への依存が無いかの棚卸し(未調査)

## 7. ⚠️ この修正で直らないもの(申し送り)

**「これでAPI由来の差分が全部無くなる」とは書かないこと**(レビュー担当の明示的な申し送り)。

- **公式APIのデータ訂正**は消えない。起票案の `prismspire-scepter`(P25 **#165 → #164**)は
  ⭐ **画像URLも行順も変わらない**＝並び順に一切依存しない差分
  (この訂正は既に `03ff53a5` に取り込み済み。現在のスナップショットは `#164`)
- カード内容の更新(`rule` 追加・`legality` の発売日切り替わり等)も対象外
- ⭐ **本修正が保証するのは「同じスナップショットからは `editions` の配列順に関係なく同じ生成物が出る」ことだけ**
