**対象: 「辞書の抜けと検出経路」** — この宣言は 2026-09-11 に遡って追記した（索引の転記ドリフト防止）

# 未採番（辞書の抜けと検出経路）レビュー判定

⚠️ **本来は該当issueのコメント。GitHubアカウント停止中で `gh issue comment` が打てないため、
代替としてこのファイルに記録する**（#73 の `実装報告_ユニット2.md` と同じ扱い）。
復旧後、本文をそのまま `gh issue comment <n> --body-file docs/design/…/レビュー判定_2026-08-21.md` で投稿する。
⚠️ ラベル操作は未実施（issueが存在しない）。本来は合格なので `レビュー待ち` → `設計確認待ち`。

- 判定日: **2026-08-21**
- 対象コミット: **`1665e1ea`**（130ファイル / +303 −193）
- 参照設計書: `docs/design/未採番-辞書の抜けと検出経路/辞書の抜けと検出経路_設計.md` **第2版**
- 実装報告: `07a302f2` → `実装報告_2026-08-21.md`
- 証跡の置き場: `tmp/review/未採番-辞書の抜け/`（gitignore）

---

## 判定: **合格**

⚠️ **開発担当の報告は検証の出発点としてのみ使い、V0〜V9 は全項目をレビュー担当が独立に再実施した。**
とくに負テスト V2 / V3 / V8 と、M7 の「123件の内訳」は、報告の数字を見る前に
**自分でスナップショットから数え直した結果**を先に出して突き合わせている。

### 必須基準

| 基準 | 結果 | 証跡 |
|---|:--:|---|
| **M1** 設計適合 | ✅ | §4 の **V0〜V9 を全番号レビュー担当が再実施**し、全て期待値と一致（下表） |
| **M2** 実駆動 | ✅ | Playwright で **PC(1280px) と 375px の両方**。カード個別ページ5枚・詳細モーダル5枚・デッキ構築の詳細モーダル・回帰5画面。スクリーンショット計 **32枚**（`tmp/review/未採番-辞書の抜け/`） |
| **M3** 回帰 | ✅ | `npm run validate` **exit 0**（`all checks passed.`）。トップ/カード詳細モーダル/カード個別ページ/デッキ構築/大会一覧/セットページ/エキスパンション一覧すべて **200・JSエラー0・横スクロールなし** |
| **M4** 生成物の同期 | ✅ | `gen-tl-json.mjs` 済（validate の陳腐化検査が通過）。カードページの再生成漏れを**日英の両方向から独立に走査して0件**を確認（下記 M4 詳細） |
| **M5** 静かな劣化がない | ✅ | 負テスト V2 / V8 に加え、**索引破損・索引欠損の2ケースも自分で作って exit 1 を確認**。検査の両辺が同源でないことも実証（下記 M5 詳細） |
| **M6** 公開安全性 | ✅ | 130ファイル**すべて `M`（変更）**——新規追加・削除ゼロ＝**本番配信物の増減なし**。秘匿情報なし。`_headers`・`404.html`・キャッシュ前提に変更なし |
| **M7** スコープ | ✅ | **123件のカードページが、レビュー担当が独立に導出した期待集合と1件の差もなく完全一致**（下記 M7 詳細） |

### 推奨基準

| 基準 | 結果 | 所見 |
|---|:--:|---|
| **S1** コード品質 | ✅ | `validate.mjs` の新ブロックは #40 の既存ブロックと同型・同じ体裁。style属性の追加なし・バッジの追加なし（CSPと `.badges-bl` の論点に該当しない）。スタブ説明12件の**カード名を実データと機械照合して全件一致**（下記 S1） |
| **S2** 応答性 | ✅ | ブラウザ側の追加リクエストはゼロ（`data/translations.js` の辞書追記のみ）。`check-terminology.mjs` は**既存の取得結果を再利用**しており追加のAPI往復なし |
| **S3** fail-open | ✅ | 表示側は従来どおり fail-open（辞書に無ければ素の英字が出るだけ）。新 validate 検査だけは**意図的に fail-closed**で、これは「人を止める門」として正しい（#70 の教訓に沿う。実装報告 §5 に逸脱として明記済み） |

---

## 設計書 §4 の検証項目との対応（レビュー担当による再実施）

| # | 期待値 | **レビュー担当の実測** | 合否 |
|---|---|---|:--:|
| **V0** | subtypes 10種/94枚・terms 15種 | ⭐ **報告を見る前に自分で数え直した。** スナップショット（2026-08-21T14:04:47.728Z・2,495枚）と `1665e1ea^` の辞書を突き合わせ → 実データ **156種** / 辞書(前) **147種** → **未登録10種**（`AENEAN DISCORP ELYSIAN KITCHEN LABORATORY PHONE RESONATOR SETTLEMENT VEHICLE VELTECH`）・**影響94枚**（内訳 21/20/9/6/1/2/9/1/1/31）。逆方向は **`SHENJU` の1件のみ**。カウンター語 **15種**（training 9 / blood 4 / lash 3 / page 2 / 他11種が各1）。**§2 の数字と完全一致・増えていない** | ✅ |
| **V1** | exit 0・成功行 | `npm run validate` → **exit 0**、`meta.subtypes covers all subtypes — 156種` | ✅ |
| **V2** | exit 1・`VELTECH` を名指し | ⭐ **作業ツリーを一切触らずに再現**（スクラッチパッドに検査対象を組み替えたサンドボックスを作り、`data/translations.js` だけ実体コピーして `VELTECH` を削除）→ **exit 1** / `実データにあるが data/translations.js の meta.subtypes に無い: VELTECH` | ✅ |
| **V3** | exit 0（片方向） | 同サンドボックスに**辞書にだけ存在するコード `ZZREVIEWONLY` を追加**（既存の `SHENJU` と合わせ2件）→ **exit 0**・`covers all subtypes — 156種`。片方向であることを確認 | ✅ |
| **V4** | 日本語（素の英字が消える） | **サンプルではなく `cards/` 全2,494ページを走査**。10種の訳あり出現 **101回**（distinct **94枚**・内訳がV0と一致）／**素の英字のまま残っているもの 0件** | ✅ |
| **V5** | モーダルでサブタイプ日本語・カウンター語ハイライト＋用語解説 | Playwright実駆動（PC/375px 両方・下表）。**JSエラー 0**（Cloudflare RUM のCORS警告のみ＝localhost固有） | ✅ |
| **V6** | 0件 | `data/tl/*.js` の `トレーニングカウンター` **0件**。**`data/` 配下・生成物・`cards/` 全体でも0件**（残存は設計書と報告書の本文のみ＝正）。`修練カウンター` は `data/tl/*.js` に **18箇所**・カードページ **9枚** | ✅ |
| **V7** | 15種が0件 | `node scripts/check-terminology.mjs`（全件・英語原文 **2,481件**取得）→ **`terms 辞書に無いカウンター語: 0種`**。既存の表記ゆれ報告 **11件**（`starcall`×10・`charge`×1）は残存＝**本タスク以前からあるもので設計書の対象外**（触っていないことを差分で確認済み） | ✅ |
| **V8** | `blood counter` が候補に現れる | サンドボックスの `translations.js` から `"blood counter"` の1行を削除して `check-terminology.mjs PRD` → **`"blood counter" — 2枚: perfusive-envelopment, varicose-amplification`** が出現。⚠️ **exit code は 0 のまま**＝設計どおり変えていない | ✅ |
| **V9** | exit 0 | `npm run validate` → **exit 0** / `all checks passed.`（`generated JSON up to date` も通過＝`gen-tl-json.mjs` の再生成漏れなし） | ✅ |

### V5 の実測（レビュー担当・PC 1280px / 375px の両方で同一結果）

| slug | サブタイプ行（詳細モーダル） | `.term-hl` | 用語解説（`#d-terms`） |
|---|---|---|---|
| `aenean-cryosalvo` | MAGE・**AENEAN（アエネアン）**・SPELL | クラスボーナス/レベル/フラクタル | 既存3件 |
| `nightframe-hounds-bike` | ASSASSIN・WARRIOR・**VELTECH（ヴェルテック）**・**VEHICLE（車両）** | バフカウンター 他9件 | 既存6件 |
| `leran-pastoral-hymns` | WARRIOR・SIEGEABLE・**SETTLEMENT（集落）** | インフルエンス×3 | 既存1件 |
| `crimson-vein` | CLERIC・MAGE・ARTIFACT | **ブラッドカウンター×3** | **ブラッドカウンター（blood counter）** |
| `where-futures-stir` | MAGE・TRIAL | **修練カウンター×2** | **修練カウンター（training counter）** |

⭐ **`.subtype-hl` が実際に効いていることも確認した。** 実装報告は「5枚とも0件（正常）」としているが、
`golden-measure-patisserie` の差分で **`キッチン` が `<span class="subtype-hl">` で包まれる**ようになっており、
サブタイプ訳の追加が**ハイライト経路にも効いている**ことが差分で裏づけられた（報告の記述は
サンプル5枚に限れば正しく、機構自体は動いている）。

### デッキ構築ツール（CLAUDE.md #51 の手順）

- `#view-editor` の `hidden` を外して **ページ本来の `GA_CARD_DETAIL` をそのまま起動**し、
  デッキ構築の文脈で詳細モーダルを開いた → **PC/375px とも
  `ASSASSIN（アサシン）・WARRIOR（ウォリアー）・VELTECH（ヴェルテック）・VEHICLE（車両）`**、
  用語ハイライト6件・JSエラー0・横スクロールなし
- ⚠️ **未検証として明記**: 検索結果タイル（`.result`）の描画は、空のデッキ状態の合成まで行わなかったため
  **到達していない**。ただし本タスクが変える描画面は**サブタイプ行と用語解説＝詳細モーダル側**であり、
  デッキ構築は `../../data/translations.js` と `../../shared/js/card-detail.js` を
  **トップと同一のファイルで読み込んでいる**ことをHTMLで確認済み（`tools/deck-builder/index.html:348,352`）。
  ⭐ **合否はこの未検証部分を除いた範囲で判定している**（検索タイルにサブタイプ行・用語解説は出ない）

---

## M7 の重点確認（スコープ外の差分が混ざっていないか）

⭐ **開発担当の切り分けを「再現」ではなく「独立に導出」して突き合わせた。**

1. スナップショットと `1665e1ea^` の辞書から、**本タスクで描画が変わるはずのカード集合**を自分で計算した
   （新サブタイプ10種を持つカード **94枚** ∪ 未登録カウンター語15種を英語効果文に含むカード **29枚**）
2. 両者に**重複は0**、合併 = **123枚**
3. コミットが触れたカードページの一覧（`git show --name-only`）と **`diff` で突き合わせ → 完全一致（差分0行）**

| 区分 | 期待 | 実測 | 判定 |
|---|---:|---:|:--:|
| サブタイプ影響 | 94 | 94 | ✅ |
| カウンター語影響 | 29 | 29 | ✅ |
| 重複 | 0 | 0 | ✅ |
| **合計＝コミットされたカードページ数** | **123** | **123（完全一致）** | ✅ |

- ⭐ **報告の「123 = 94 + 29」は正しい。** 数字が合っているだけでなく、**slug の集合そのものが一致**している
- **APIドリフトの混入も差分の中身で確認した。** 123ファイルの変更行 **358行**を
  `裁定|rule|edition|image|jpg|png|webp` で走査 → 該当は **4行のみ**で、すべて
  **`cards/radiant-origin-of-warrior/index.html`**（公式裁定1件 `2026-08-16 attacks using -> wields` と、
  それに伴う `description` メタの「収録セット」→「収録セット・裁定」）。
  ⭐ **申告どおり混在は1ファイルだけで、他の122ファイルにドリフトは1行も無い**
- **除外の妥当性も確認した**: `sets/*/index.html` と `cards/index.html` は
  **効果文もサブタイプ行も埋め込んでいない**（`grep -c "term-hl\|cp-effect\|サブタイプ" → 0`）。
  ⭐ よってセットページ8枚・`cards/index.html`・`sitemap.xml` の除外は正しく、
  **本タスク起因の差分をドリフトとして誤って捨てた形跡はない**
- 新規カード `jinzhuo-bands-of-virtue` の残骸なし（ディレクトリ不在・`sitemap.xml` に参照0件・未追跡ファイル0）
- **130ファイルすべて `M`（変更）**。新規追加も削除も無い

⭐ **`cards/radiant-origin-of-warrior/index.html` の混在は合格を妨げないと判断した。**
理由: ①混ざった内容が**公式裁定の追加1件だけ**で、②cronが復旧すれば**同じ内容を自動で入れる差分**であり、
③**表示の不整合を生まない**（裁定が1日早く出るだけ）。
④ 何より**開発担当がコミットメッセージと報告の両方に明記**しており、M7 が禁じる
「設計書に無い変更が、根拠の記載なく入っている」状態ではない。

---

## M4 の重点確認（再生成漏れ）

⚠️ **`npm run build:cards` の再実行は作業ツリーを書き換えるため実施していない**（レビュー担当の禁止事項）。
代わりに、**コミット済みの生成物そのものを走査**して漏れが無いことを確認した:

| 走査 | 結果 |
|---|---|
| 全2,494カードページのサブタイプ行に**素の英字10種**が残っていないか | **0件** |
| 新規 `terms` 15種の見出し語を**日本語訳文**に含むカード（英語側ではなく日本語側から逆引き） | **29枚 → 全て123件に含まれる（漏れ0）** |
| 新サブタイプ10種を持つカード（スナップショットから逆引き） | **94枚 → 全て123件に含まれる（漏れ0）** |
| 新カウンター語を含むページで**ハイライト or 用語解説が欠けている**もの | **0件**（29枚すべてで `term-hl` と `cp-term-jp` の両方を確認） |

⭐ **英語側・日本語側・生成物側の3方向から独立に数えて全て一致**したので、
「ビルドを回していないカードが残っている」経路は無いと判断した。

---

## M5 の重点確認（静かな劣化）

設計書が要求する負テスト（V2/V3/V8）に加え、**レビュー担当が独自に2ケース追加**した:

| ケース | 期待 | 実測 | 判定 |
|---|---|---|:--:|
| `data/card-meta-index.json` の `m` を空にする | 黙って通ってはいけない | **exit 1** / `索引からサブタイプを1件も取り出せません（索引の破損か形式変更の疑い）` | ✅ |
| `data/card-meta-index.json` を消す | 同上 | **exit 1** / `読み込みに失敗: ENOENT…` | ✅ |

⭐ **「検査の両辺が同源だと常に通る」という M5 の懸念に該当しないことも実証した。**
検査の左辺は `data/card-meta-index.json`（APIから生成されコミットされた生成物）、
右辺は `data/translations.js`（人が手で書く辞書）で**出所が違う**。
さらに**索引から取り出したサブタイプ156種と、APIスナップショットから取り出した156種が
集合として完全一致**することを確認したので、`entry[3]` が本当にサブタイプ列であり、
検査が**空振りしていない**ことが裏づけられている。

⚠️ `check-terminology.mjs` 側は設計どおり **exit code を変えていない**（V8 で実測）。
これは「赤くすると使われなくなる」という設計判断であり、**cronログにも `validate` にも痕跡が残らない**
＝ **人が手で打たないと気づけない**経路である点は、設計書 §5 が「守れないこと」として
既に明示しているとおり。**基準違反ではない**。

---

## S1 の所見（合格を妨げないもの）

1. ✅ **スタブ説明12件のカード名を実データと機械照合し、全件一致**を確認した
   （`music counter`→「ディスコルディア、悪意の竪琴」、`root counter`→「ユディ、繊細な翡翠」等）。
   **事実と食い違う説明文は1件も無い**
2. ✅ **複数枚で使う3種の `desc` も原文と突き合わせた**。`training`／`blood`／`lash` とも
   効果文から読み取れる範囲に収まっており、**上限・範囲・タイミングの断定は無い**
   （`lash counter` の「ニコ系のカードがチャンピオンに置く」は、3枚とも `[Nico Bonus]` を持ち
   かつ `put ... lash counter on your champion / on Nico` で**正しい**ことを確認）
3. ⭐ **`page counter` の文面変更（実装報告 §4-2）は正しい判断**。設計書 §3-2 の
   「1枚だけの12種」は**設計書自身の §2-2 の表（`page counter` は2枚）と食い違っており**、
   実際の内訳は「3種(複数枚) + `page`(2枚) + 11種(1枚)」＝**設計書側の数え間違い**。
   実装が「でのみ」を避けたのは事実に合わせた是正であり、逸脱として報告済み

---

## 設計担当への確認事項

⚠️ いずれも**合否を左右しない**。合格判定はこれらと独立に確定している。

1. ⭐ **`CLAUDE.md` の記述が今回の実装で陳腐化した**（レビュー中に発見）。
   - `CLAUDE.md:390-391` — 「現状の抜け」表が **`meta.subtypes` 10種/94枚・`terms` 15種**を
     **未解消として列挙したまま**
   - `CLAUDE.md:397` — 「検出経路の設計は …（subtypes は `validate`・カウンター語は
     `check-terminology.mjs` に置き分ける。**未実装**）」——**本コミットで実装済み**
   - ⚠️ **これはまさに本タスクが潰そうとしている「原本が実データと食い違う」型の落とし穴**
     （§2-4 の `TRANSLATION.md` と同じ構造）。放置すると次に読んだ人が「まだ抜けている」と誤認する
   - ⚠️ **レビュー担当は直さない**（越権）。また `CLAUDE.md` は**ルート直下＝本番配信物**なので、
     更新するなら**同じ `push待ち` の束に入る**点に注意
2. **APIドリフト80ファイル＋新カード1枚の扱い**（実装報告 §6-2）。
   ⭐ **レビューとしては「除外して正しい」と判定した**（#71 の教訓どおり）。
   ただし**cronが停止中で自動取り込みの当てが無い**ため、復旧までの間に手で取り込むかは設計判断。
   放置しても表示は壊れない（裁定が出ないだけ＝fail-open）ことは確認済み
3. **`check-terminology.mjs` の既存の表記ゆれ報告11件**（`starcall`×10・`charge`×1）。
   本タスクの対象外であることを差分で確認した。**派生issueにするかは設計担当の判断**
   （⚠️ レビュー担当は起票しない）

---

## 残っている確認（pushしないと確認できない項目）

⚠️ **これらは合否に含めていない**（要求もしない）。

- 本番での表示確認（`/cards/aenean-cryosalvo/` のサブタイプ行が日本語であること）。
  ⚠️ 本コミットに**新規ファイルが1つも無い**ため、CLAUDE.md の「新規追加を到達マーカーにする」手は使えない。
  **20秒間隔の反復測定で全数一致を待つ**必要がある（`_headers` 変更なしなので `curl -sI` は不要）
- ⚠️ **push そのものが不可**（GitHubアカウント停止中）。かつ本コミットは
  **本番配信物**（`data/**`・`cards/**`・`scripts/**`・`TRANSLATION.md`）を含むため、
  **公開の可否は常にユーザー判断**。設計書 §7 のとおり **`push待ち` 相当**

---

## レビュー担当の作業ログ（作業ツリーを変更していないことの担保）

- 負テストは**リポジトリ外**（スクラッチパッド）に検査対象を組み替えたサンドボックスを作って実施した。
  `data/translations.js` と `scripts/validate.mjs` / `scripts/check-terminology.mjs` だけ実体コピーし、
  残りはリポジトリへの**シンボリックリンク**。**リンク越しの書き込みは一度も行っていない**
- `git checkout` / `stash` / `restore` / ブランチ切り替えは**未使用**。読んだのは `git show` / `git log` / `git status` のみ
- 判定後の `git status --short` は**空**（HEAD = `07a302f2`）
- 起動したローカルサーバー（`PORT=3100 npm run dev`）は**停止済み**（`curl` で応答なしを確認）
- 証跡（スクリーンショット32枚）は `tmp/review/未採番-辞書の抜け/` に配置。**ほかのセッションの `tmp/` 配下は削除していない**
