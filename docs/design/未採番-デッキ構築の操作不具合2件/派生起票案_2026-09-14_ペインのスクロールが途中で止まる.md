**状態: 生きている**（最終確認 2026-09-14）— 呼び名「ペインのスクロールが途中で止まる」。⚠️ **保留**（2026-09-14 ユーザー「一旦この件は保留で作業を進めてください」）。**ユーザーが1回だけ観測（キャプチャあり）→ 同日に本人が確認したら再現せず／設計担当もヘッドレス・ヘッド付き＋GPU描画で再現せず**。⚠️ **再発の報告が来るまで設計しない**。⚠️ **投函前に必ず再現させる（しなければ取り下げ）** ／ 親タスク「デッキ構築の操作不具合2件」設計書 [`操作不具合2件_設計.md`](操作不具合2件_設計.md) §9 ／ 索引: [`待ち行列と復旧手順.md`](../待ち行列/待ち行列と復旧手順.md)

# issueの起票案（派生）— デッキ構築：サブタイプを「すべて表示」したあと、左ペインが途中までしかスクロールできない（保留）

⚠️ **これは設計書ではなく「issueの下書き」。** GitHubアカウント停止中で `gh issue create` が打てないため、本文をここに用意する。

- ⭐ **起票時**: タイトル「デッキ構築：サブタイプを『すべて表示』したあと左ペインが途中までしかスクロールできない（再現待ち）」／ラベル `bug` `保留`（理由: 再現しない）
- 要件の出所: 親タスク「デッキ構築の操作不具合2件」の**2回目の実物確認**（[`実物確認結果_2回目_2026-09-14.md`](実物確認結果_2回目_2026-09-14.md)）で報告された **2d**

---

## 1. なぜ親タスクから切り出したか

⭐ **親タスクは実物確認 6〜9 の結果しだいで承認・完了に進む。** 2d を親タスクの設計書 §9 の中にだけ置くと、
**完了した瞬間に索引の「✅ 完了済み」側へ移り、保留中の症状があることが待ち行列から見えなくなる**（＝記録ごと埋もれる）。
⭐ **この起票案が `生きている` で残る限り、索引 §1 の生成部に1行出続ける。**

⚠️ 親タスクの直し方（案1・F1・scroll-padding・F2）の**どれが原因かは切り分けられていない**（§3）。**親タスクの承認はこの件を待たない**（ユーザー判断: 保留で作業を進める）。

## 2. 症状と経緯

| 日付 | 出来事 |
|---|---|
| 2026-09-14 | ユーザーが実物（3000番の wrangler・HEAD `7a394ded`・実ログイン・Chrome）で報告: 「サブタイプのすべてを表示をしたあと左ペインのスクロールバーが途中までしかスクロールできず、サブタイプを少ない表示に戻したりフォーマットなどの検索条件までスクロールできません。」 |
| 同日 | 設計担当が5ツリー（本番相当 `15598b3f`〜HEAD）× ヘッドレス／ヘッド付き＋GPU描画 × DSF 1〜1.5 × 4サイズ × 小さいデッキで試行 → **ホイールもつまみのドラッグも最大まで届き、再現せず**（設計書 §9-2） |
| 同日 | **ユーザー原文**: 「今確認したら再現できませんでした。正しく表示されています。一旦この件は保留で作業を進めてください。」 |

**キャプチャの要点**（リードの読み取り。画像はリポジトリに無い）: ページは最下部・ペインの上端が編集バーより上に押し上げられている・**サブタイプのチップが B の途中でペインの下端に切られている**・**つまみは最下部付近**・カードの少ないデッキ・倍率 約1.47。

## 3. 未確認の候補（設計書 §9-3）

| # | 候補 | 直すなら |
|---|---|---|
| (a) | ⭐ スクロール位置は最下端なのに**描画が古いまま残る**（親タスクの 2c と同じ系統） | 合成スクロール（F2）まわり |
| (b) | 「すべて表示」で中身が伸びたのに**スクロール範囲の更新が遅れる** | F1 以降のどれか |
| (c) | ページ最下部で**ペインが押し上げられ、つまみと上端が編集バーの裏に隠れる**（本番から同じ幾何） | 既存挙動 |

⭐ **「つまみが最下部なのに中身は途中」は (a) を示唆する**が、読み取りが画像由来なので断定しない。
⭐ **一度だけ起きて、あとで再現しない**という経過は (a)（描画のタイミング依存）と整合する。

## 4. ⚠️ 再発したときに聞くこと（設計書 §9-4 と同じ。**その場で**聞く）

1. **止まった状態でしばらく待つ／ペインの中を一度クリックすると、続きが表示されるか**（(a) と (b) の切り分け）
2. **ペインの上でホイールを回すと先へ進めるか、つまみを引いたときだけ止まるか**
3. **本番サイトでも起きるか**（起きれば (c) か既存）
4. **ページの一番上でも起きるか**
5. **Windows の表示スケールと Chrome の拡大率**
6. （任意）止まった状態で DevTools のコンソールに貼った結果:
   `(() => { const e = document.querySelector("#search-top > .pane-scroll"); return [e.scrollTop, e.scrollHeight - e.clientHeight]; })()`
   ⭐ **2つの数が同じなら (a)**、1つ目が小さければ (b)/(c)
7. ⭐ **できれば画面録画**（一瞬の状態なのでキャプチャ1枚では (a)〜(c) を区別できなかった）

## 5. 投函・取り下げの条件

- ⚠️ ⭐ **`gh issue create` の直前に再現させる**（CLAUDE.md「タスク管理」）。**再現しなければ投函せず取り下げる**
- ⭐ **取り下げの目安**: 親タスクの公開後、**ユーザーから再発の報告が無いまま次の大きな左ペイン関連の変更（検索結果のタブ化など）に着手する時点**。そのとき `取り下げ_<日付>.md`（1行目 `（→ 取り下げ）`）を書く
- ⭐ **再発したら**: §4 の回答を持って設計書（親タスクの §9 を引き継ぐ新しい設計書）を起こす
