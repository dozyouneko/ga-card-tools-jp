**対象: 「デッキ構築の操作不具合2件」**

# 実装報告（単位3・F2 合成スクロール）— デッキ構築の操作不具合2件（2026-09-14）

⚠️ **これは issueコメントの代替**（GitHubアカウント停止中で `gh issue comment` が打てないため、ファイルに残す）。
復旧して採番されたら、本文をそのまま `gh issue comment <n> --body-file docs/design/未採番-デッキ構築の操作不具合2件/実装報告_合成スクロール_2026-09-14.md` で投函する。

- 実施日: **2026-09-14**
- 参照: [`追加指示_合成スクロール_2026-09-14.md`](追加指示_合成スクロール_2026-09-14.md)・[`操作不具合2件_設計.md`](操作不具合2件_設計.md) **0.4 §8**（§8-4・§8-5 V25〜V29）・[`実物確認結果_2026-09-14.md`](実物確認結果_2026-09-14.md)（いずれも `e422a1a0`）
- 既存の報告 [`実装報告_2026-09-14.md`](実装報告_2026-09-14.md)・[`実装報告_追加_2026-09-14.md`](実装報告_追加_2026-09-14.md) は**書き換えていない**
- 次の担当: ⭐ **レビュー担当**（ラベル相当: **`レビュー待ち`**）。⭐ **レビュー対象は単位3 のコミット `994de8dc` だけ**（単位1・単位2・scroll-padding は `29ee6a06` で合格済み）
- ⚠️ ラベル操作は未実施（issueが存在しないため）
- ⚠️ 前回の呼び出しはセッション上限で中断し、コミットも変更も残っていなかった（リードの実測）。**本報告の作業はすべて今回の呼び出しで最初からやり直したもの**

---

## 1. 実施内容

| # | 手順 | 対象 |
|---|---|---|
| 1 | `@media (min-width: 1200px)` 内の `#search-top > .pane-scroll { … }` の**末尾**（`scroll-padding-bottom: 2rem;` の後）に **`background: var(--bg);`** と、設計書 §8-4 のコメント（同文）を足した | `tools/deck-builder/style.css` |
| 2 | 中断に備え、**実装のコミットを検証の前に済ませた**。検証結果は `tmp/dev/操作不具合2件/f2_results.jsonl`・`v29_results.jsonl` に逐次追記した | — |

- ⭐ **コミット: `994de8dc`**（`tools/deck-builder/style.css` のみ・+9 −0）
- **push: していない**
- `#search-top`（`.search-top`）の背景は触っていない・`will-change` は使っていない・`@media` の新設なし・行番号参照なし・F1 と案1 のコードは不変
- `var(--bg)` は `shared/css/tokens.css` の `:root` にある（`--bg: #0f1115`）。`:root` は増やしていない

## 2. 検証の条件

- 差し替え方式は既存の報告と同じ（`/api/*` を `page.route` で差し替え。`tmp/dev/操作不具合2件/harness.mjs`。今回 `deviceScaleFactor` と `PW_CHANNEL` を渡せるようにした）
- ブラウザ: **V25〜V28 は新ヘッドレス（`channel: "chromium"`）**（設計書 §8-5 の指定）。V29 は既存の報告と同じ `chromium_headless_shell`（前回値との比較のため）
- サーバー: **変更後 3010**（作業ツリー `994de8dc`）／**変更前 3011**（`0d3da4e9` を `git archive`）／**参考 3012**（本番相当 `3f317196` を `git archive`）。
  ⭐ 検算: `style.css` の `background: var(--bg);` の件数が 3010 で **2**・3011 で **1**（他の規則ぶん）、`scroll-padding-bottom: 2rem` が 3010/3011 に **1**・3012 に **0**、`index.html` の `class="pane-scroll"` が 3012 だけ **0** であることを確認してから測った。
  ⚠️ **3000番（リードの wrangler）には触れていない**
- スクリプト: `tmp/dev/操作不具合2件/f2.mjs`（V25〜V28）・`u2.mjs`（V10・V12）・`u3.mjs`（V39）。画像 `v27_*.png`・矩形 `v28.json`
- LayerTree の測り方は設計書 §8-5 の冒頭どおり（`DOM.enable`・`LayerTree.enable` → スクロール領域の `scrollTop` を 5→0→5→0 と変えて `layerTreeDidChange` を起こす → 各レイヤーに `LayerTree.compositingReasons` と `DOM.describeNode({ backendNodeId })`）

## 3. 検証結果（設計書 §8-5）

| # | 何を | 変更前（`0d3da4e9`・3011） | 変更後（`994de8dc`・3010） | 判定 |
|---|---|---|---|---|
| **V25** | 1440×700・ページ 400px・エレメントを開く・DSF **1** | `.pane-scroll` の背景 `rgba(0, 0, 0, 0)`・スクロール領域のレイヤーは `div#search-top`（`Overlap`）の**1枚だけ**＝**`OverflowScrolling` なし** | 背景 `rgb(15, 17, 21)`・⭐ **`div.pane-scroll` 319×576 に `OverflowScrolling` が1つ**（ほかに中身の層 `div.pane-scroll` 319×847 `Overlap`） | ✅ |
| V25 | 同・DSF **1.25** | **なし**（同上） | ⭐ **`div.pane-scroll` 319×576 に `OverflowScrolling` が1つ**（同上） | ✅ |
| V25（参考） | 本番相当 `3f317196`（3012）・DSF 1 / 1.25 | — | `div#search-top` 319×643 に `OverflowScrolling`（中身 319×914）＝**設計書 §8-3 の表と一致** | 参考 |
| **V26** | 破壊試験・DSF 1: `@media (min-width: 1200px) { #search-top > .pane-scroll { background: transparent !important } }` | — | 足す前 **1** → 足すと背景 `rgba(0, 0, 0, 0)`・**`OverflowScrolling` 0**（`div#search-top` の `Overlap` だけ＝変更前と同じ形）→ `deleteRule` で背景 `rgb(15, 17, 21)`・**1** に戻る | ✅ |
| **V27** | 画素差（x 0〜330・全高）・1440×700・ページ 400px・エレメントを開く | `scrollY` 400・`--pane-top` 57px | 同じ状態で撮影。⭐ **チャネルあたり最大差 1**（差があるチャネル 41,030・差 3 以上 **0**） | ✅ |
| V27 | 同・1440×900・ページ最上部 | `scrollY` 0・`--pane-top` 179px | ⭐ **最大差 1**（差があるチャネル 46,264・差 3 以上 **0**） | ✅ |
| **V28** | 1199×800 / 375×812・V14 の9要素の矩形（トグル閉・開） | — | **両幅とも 18組すべて一致（差 0）**。`.pane-scroll` は `display: contents`・`backgroundColor` **`rgba(0, 0, 0, 0)`**（変更前も同じ） | ✅ |
| **V29** | V10（1652×934・18点） | — | **18点すべて ✓**（0〜121: 866〜933・400: 867〜934・top は3点で一致）。⭐ **前回 `0d3da4e9` のツリーで採った出力と `rows` が JSON で完全一致** | ✅ |
| V29 | V12（F1 の構造） | — | `.row-actions` `static`・`#search-top` `overflowY hidden`・sticky 子孫 **0件**・`.pane-scroll` `overflowY auto`・`scrollTop` 0→500 で中身 **500px**・ボタン行 **0px** | ✅ |
| V29 | V39（157個） | — | `scroll-padding-bottom` **32px**・**157個すべて `.pane-scroll` の矩形の中（切れ 0）**・続けて `#s-format → #s-set → #s-sort → #s-order → #s-reset → #s-search` に到達 | ✅ |
| V29 | V20 `npm run validate` | — | **exit 0**（実装コミット前と報告を足した後の2回）。`shared css tokens in sync — 68 トークン参照 / 未定義 0`・`:root owners in sync — 4ファイル`・`onRemoveOne wiring in sync — 渡しているのは 1ファイル（tools/deck-builder/app.js）／25ファイル走査`・`pane threshold in sync — 2ページ（トップ 1200px / デッキ構築 1200px）`・`mobile band marker in sync — 1組（style.css 1199px ↔ app.js MOBILE_MQ 1199px）`・`no line-number refs in code comments — 0件（47ファイル走査）` | ✅ |

## 4. 実行できなかった検証・残っている確認

- ⚠️ ⭐ **2c（スクロールバーを速く動かすと群カードの背景だけが古い位置に残る）が直ったかは、ユーザーの実物確認 6 でしか判定できない。**
  ヘッドレスではドラッグ中の描画崩れが変更前も起きない（設計書 §8-2）ので、**指示どおりドラッグ中の描画は検証していない**。
  ここで示せたのは**原因の見立て（合成スクロールでなくなった）の構造が元に戻ったこと**（V25・V26）と、**見た目を変えていないこと**（V27・V28）まで
- ⚠️ 見立ては **`devicePixelRatio` 1.5 未満**が前提（設計書 §8-3）。DSF 1.5 / 2 は測っていない（指示の範囲外。設計書では変更前から合成済み）
- 実ログイン（Discord）での確認は未実施（既存の報告と同じ理由）

## 5. 差分が設計書の想定と食い違った点

- **V25 の変更前のレイヤー**: 設計書 §8-3 は修正後の `.pane-scroll` について「**スクロールバーの層だけ**」と書いているが、新ヘッドレスの今回の測定では `.pane-scroll` に紐づくレイヤーは**1枚も無かった**（`div#search-top` の `Overlap` 1枚のみ）。
  ヘッドレスはスクロールバーの幅が 0（設計書 §8-2）なのでスクロールバーの層が作られないためと判断した。⭐ **判定（`OverflowScrolling` の有無）には影響しない**
- **V29 の V12 の測定前の `.pane-scroll` の位置が 324**（`initialScrollTop`）: 前回報告（`実装報告_追加_2026-09-14.md` §4）で切り分けたとおり、**Playwright の `page.click()` がクリック前に行う画面内への送り**によるもので、判定は 0 に戻してから測っている
- それ以外は設計書 §8-5 の期待値どおり（V27 の最大差 1 は設計担当の試作値と一致）

## 6. 設計書からの逸脱

- なし。宣言・値・コメント・置き場所は設計書 §8-4／追加指示 §2-1 のとおり

## 7. 設計担当への確認事項

- なし
