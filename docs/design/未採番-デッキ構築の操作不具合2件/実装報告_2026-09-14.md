**対象: 「デッキ構築の操作不具合2件」**

# 実装報告 — デッキ構築の操作不具合2件（2026-09-14）

⚠️ **これは issueコメントの代替**（GitHubアカウント停止中で `gh issue comment` が打てないため、ファイルに残す）。
復旧して採番されたら、本文をそのまま `gh issue comment <n> --body-file docs/design/未採番-デッキ構築の操作不具合2件/実装報告_2026-09-14.md` で投函する。

- 実施日: **2026-09-14**
- 参照した設計書: [`操作不具合2件_設計.md`](操作不具合2件_設計.md) **0.2**（§3-3・§3-4 V1〜V8／§4-7・§4-8 V9〜V19／§5 V20〜V23）＋ [`設計完了_2026-09-14.md`](設計完了_2026-09-14.md)
- 次の担当: ⭐ **レビュー担当**（ラベル相当: **`レビュー待ち`**）
  - ⚠️ ⭐ **ただし単位2 の V18（左ペイン化 V39）が期待値を満たしていない**（§B-2・§B-6 Q1）。
    原因は設計の判断済み事項 **D5（`scroll-padding-bottom` は F1 で不要）の前提の見落とし**で、直すと D5 を覆すため**実装側で決めていない**。
    **レビューの前に設計担当へ Q1 を回すかどうかはリードの判断に委ねる**
- ⚠️ ラベル操作は未実施（issueが存在しないため）
- 設計書 §6 の指示どおり、**単位ごとに報告を分けた**（§A＝単位1・§B＝単位2。「変更前」が違うため）

---

## 共通：検証の条件

- ⭐ **`/api/*` だけを Playwright の `page.route("**/api/**")` で差し替え**、ログイン済み・デッキを開いた状態を作った（設計書 §2 と同じ方式）。
  カード・絵柄・チップ・`renderZones()` は本物。デッキの中身は `モック_左ペイン化_2026-09-05.stub.js` の `STD_ROWS`（マテリアル12／メイン60／サイド9）＋検討中1枚、
  パンテオンは同 `PAN_ROWS`。⚠️ **保存系（POST/PATCH/DELETE）は差し替え側で状態を持ち、`{ card: { card_slug, board, qty } }` を150ms遅延で返す**（設計書 §2 の注意どおり）
- ブラウザ: Playwright 1.61.1 の Chromium（`chromium_headless_shell`）・`--ignore-certificate-errors`＋`ignoreHTTPSErrors`
- サーバー: **変更後 3010**（作業ツリー）／**変更前 3011**（`git archive` した別ツリーの `scripts/serve.mjs`）。⚠️ **3000番（リードがユーザー確認用に起動中）には触れていない**
  - ⭐ 検算: 3011 の `app.js` に目印（単位1: `shown.join`／単位2: `updatePaneTop`）が**無い**こと、3010 には**ある**ことを `curl | grep -c` で確認してから測った
- 検証スクリプトと画像は **`tmp/dev/操作不具合2件/`**（gitignore）に置いた: `harness.mjs`（差し替え）・`u1.mjs`（V1〜V7）・`u2.mjs`（V9〜V19・V23）・`top.mjs`（V21・V22）・`probe39.mjs`・`probe16.mjs`・`probe16b.mjs`・`v13_*.png`・`v16_*.png`・`v14_*.json`
  - 使い方: `node u1.mjs <port> v1|v2|v3|v5|v7|v23`／`node u2.mjs <port> v9,v10,…`（ポート 3010 を「after」、それ以外を「before」と表示する）

---

# §A. 単位1 — デッキ側の＋/−/枚数入力でページが上へ飛ぶ

## A-1. 実施内容

| # | 手順 | 対象 |
|---|---|---|
| 1 | `renderZones()` の描画ループ内の `pane.insertBefore(zoneEl, memoBlock)` を削除し、**描画ループの直前**（`ZONES.forEach(... zoneEl.hidden = ...)` の直後）に「表示するゾーンの並びが `order` と違うときだけ並べ直す」ブロックを置いた（設計書 §3-3 のコードどおり） | `tools/deck-builder/app.js` |
| 2 | 変更前ツリー（`15598b3f`）で V1 の Δ−474・V4 の8件を**先に再現**してから本測定 | — |

- ⭐ **コミット: `3f317196`**（`tools/deck-builder/app.js` のみ・+10 −1）
- **push: していない**
- `overflow-anchor` は**入れていない**（`git show 3f317196 | grep -c overflow-anchor` → 0・`grep -rn overflow-anchor tools/deck-builder/` → 0件）

## A-2. 検証結果（設計書 §3-4）

| # | 何を | 変更前（`15598b3f`・3011） | 変更後（`3f317196`・3010） | 判定 |
|---|---|---|---|---|
| **V1** | 1440×900 の＋ | メイン **669→195（Δ−474）**／サイド **1143→0（−1143）**／検討中 **1419→0（−1419）** | メイン **669→669（Δ0）**・`×3→×4`／サイド **Δ0**・`×3→×4`／検討中 **Δ0**・`×1→×2` | ✅ |
| **V2** | −／枚数入力（Enter）／キーボード（Tab で＋ → Enter） | いずれもメイン **Δ−474** | **3経路とも Δ0**（−: `×3→×2`／入力: `×3→×4`／キーボード: Tab 後の `activeElement` は `aria-label="増やす"`・`×3→×4`） | ✅ |
| V2（0枚） | −で最後の1枚を消す（`beseech-the-winds`） | 880→406（**Δ−474**） | **Δ0**。マテリアル・メインの `getBoundingClientRect().top`（−635／−161）も**不変**。タイルは消えた | ✅ |
| **V3** | 幅と高さ | 1200×900 **−435**／1440×700 **−474**／1920×1080 サイド **−862**／1199×900 **−448**／375×812 **−1440** | **5条件すべて Δ0**・枚数+1 | ✅ |
| **V4** | `#ed-pane-deck` 直下の `childList`（subtree なし）・＋1回 | **8件**（`-material,+material,-main,+main,-side,+side,-maybe,+maybe`） | **0件**（V1〜V3 の全11回で0件） | ✅ |
| **V5** | ①パンテオン用デッキを開く | — | 表示順 **`material,main,pantheon,maybe,side`**・メモが最後・サイド見出し「サイドボード（このフォーマットでは使いません）」 | ✅ |
| V5 | ②スタンダード → パンテオン → スタンダード → パンテオン → スタンダード（`#ed-format` を押す・confirm は承諾） | — | 標準 `material,main,side,maybe` ↔ パンテオン `material,main,pantheon,maybe,side` と**毎回切り替わる**・**メモは常に最後** | ✅ |
| **V6** | 破壊試験 | 上の「変更前」列＝**V1 Δ−474・V4 8件に戻る** | 本体に `overflow-anchor` 無し（A-1） | ✅ |
| **V7** | 結果ダイアログの「＋マテリアル」「＋メイン」（scrollY=600） | Δ0・`12 / 12 → 13 / 12`／`60 / 60以上 → 61 / 60以上` | **Δ0**・マテリアル `12 / 12 → 13 / 12`（`aithne-spirit-of-fire` `(none)→×1`）・メイン `60 / 60以上 → 61 / 60以上`（`aenean-flurry-of-fire` `(none)→×1`） | ✅ |
| **V8** | 実ログインでの確認 | — | ⚠️ **上の V1〜V7 はすべて差し替えた `/api/*` での確認**。実ログイン＝本物の保存経路は**ユーザーの実物確認（設計書 §5 の1）**で行う | 未実施（設計どおり） |

## A-3. 実行できなかった検証・残っている確認

- **V8（実ログイン）**: ヘッドレスでは Discord ログインできないため未実施。設計書 §5 の実物確認 1 に委ねる
- ⚠️ **フォーカスが `BODY` に落ちる挙動は変わっていない**（設計書 §3-3・§7 の1 のスコープ外）。V2 のキーボード経路でも Enter 後は `BODY`

## A-4. 差分が設計書の想定と食い違った点

- なし。変更前の値（Δ−474・−1143・−1419・−435・−862・−448・−1440・8件）は**設計書 §3-1・§3-2 の表と完全に一致**した

## A-5. 設計書からの逸脱

- コメントを1行足した: 「⚠️ 描画ループの中へ戻さないこと（戻すと空振りの抜き差しが毎回走り、不具合が再発する）」。設計書 §7 の2（将来の差分更新でガードを落とさない）の趣旨を、ガードの隣に残すため
- それ以外はコードも位置も設計書 §3-3 のとおり

## A-6. 設計担当への確認事項

- なし

---

# §B. 単位2 — 左ペインの🔍検索が押せない（案1＋F1）

## B-1. 実施内容

| # | 手順 | 対象 |
|---|---|---|
| 1 | **F1**: `#search-top` の子のうち `.row-actions` 以外の5つ（`.row`／`#s-toggle`／`#s-selected-filters`／`#s-filter-row`／`.result-sort`）を `<div class="pane-scroll">` で包み、設計書 §4-7 (1) のコメントを付けた。`.row-actions` は直下・最後の子のまま、既存コメントも残した。`id` は付けていない | `tools/deck-builder/index.html`（⭐ **`git diff -w` では +8行のみ**。残りは包んだぶんの字下げ） |
| 2 | **F1**: 基本に `.search-top > .pane-scroll { display: contents; }`（`.search-top .row` の直後）。`@media (min-width: 1200px)` の `#view-editor #search-top` を §4-7 (2-b) に書き換え、`#search-top > .pane-scroll` の規則を追加、`#search-top > .row-actions` から `position: sticky; bottom: 0; z-index: 3; margin: 0 -16px` を外して `flex: none; margin: 0` に。**旧 `overflow-y: auto`・`padding: 14px 16px 0; gap: 12px`・`scroll-padding-bottom: 5rem` を削除**し、sticky の仕組みを説明していた段落を F1 の説明に書き直した | `tools/deck-builder/style.css` |
| 3 | **案1**: `updatePaneTop()` を `updateEditorBarH()` の近くに追加。呼び出しは ①`window` の `scroll`（`passive: true`・rAF で間引き）②既存 `resize` の rAF 内（`updateEditorBarH()` の直後）③`renderEditorBar()` の末尾（`updateEditorBarH()` の直後） | `tools/deck-builder/app.js` |

- ⭐ **コミット: `8e5f413a`**（3ファイル・+108 −61）
- **push: していない**
- 変更前ツリーは**単位1 の実装コミット `3f317196`** を `git archive` したもの（3011）

## B-2. 検証結果（設計書 §4-8・§5）

| # | 何を | 変更前（`3f317196`・3011） | 変更後（`8e5f413a`・3010） | 判定 |
|---|---|---|---|---|
| **V9** | 1440×900・scrollY=0・fire で検索 → エレメントを開く | ダイアログ開・閉とも 🔍検索 **967〜1009**・`elementFromPoint` **`null`** | ダイアログ**開**: **845〜887**・`#s-search`・実クリックで `cards/search` **1本**／ダイアログ**閉**: **845〜887**・`#s-search`・**1本**。`--pane-top: 179px` | ✅ |
| **V10** | 1652×934・サブタイプ157種すべて表示・scrollY 0/30/60/90/121/400 × `.pane-scroll` 0/中間/最下端 | — | scrollY 0〜121: **866〜933 ✓×3**（top 一致）／400: **867〜934 ✓×3**（top 一致）。**18点すべて下端 ∈ [933, 934]・押せる**（設計書 §4-4 の表の値と一致） | ✅ |
| **V11** | 1200×800／1280×720／1536×730／1920×1080／1200×800 最長デッキ名（`.editor-bar` **102px**） | — | 各6×3＝18点すべて ✓・top 一致。下端: 1200×800 **799〜800**／1280×720 **719〜720**／1536×730 **729〜730**／1920×1080 **1079〜1080**／最長名 **800** | ✅ |
| **V12** | F1 の構造（1440×900） | `.row-actions` **sticky**・`#search-top` **overflowY auto**・sticky 子孫 **1件**（`row row-actions`） | ①`.row-actions` **`static`** ②`#search-top` **`hidden`** ③`#search-top` 配下 `querySelectorAll("*")` で sticky **0件** ④`.pane-scroll` `overflowY auto`・`scrollTop` 0→500 で中身（`#s-name`）が **500px** 動き、ボタン行は **0px**、`#search-top.scrollTop` は 0 のまま | ✅ |
| **V13** | 1652×934・ページ300px・選択中の条件3つ（WIND/REGALIA/TAMER）・レアリティ群を開く・`.pane-scroll` 最下端 | 並び替え **778〜847** < ボタン行 **867〜934**・画素: 緑 **778〜846** → マゼンタ **868〜933** | **変更前と完全に同じ値**（並び替え 778〜847 < ボタン行 867〜934・下端 934・画素の順序も同じ） | ✅（⚠️ 下記の注） |
| **V14** | 1199×800／900×800／375×812 の9要素の矩形（トグル閉・開） | — | **54組すべて変更前と一致（差 0）**。`--pane-top` は3幅とも **`""`**（scrollY=300 に送った後も） | ✅ |
| **V15** | 破壊試験（旧 `max-height` を `insertRule`） | — | 🔍検索 **967〜1009**・`elementFromPoint` **`null`**（ダイアログ開・閉とも）＝**V9 の変更前の値に戻る** → `deleteRule` で **845〜887・`#s-search`** に戻る | ✅ |
| **V16-a** | 破壊試験（`.pane-scroll { display: block !important; overflow: visible !important }`・1440×900・157種・scrollY=400） | — | ⚠️ **赤くはなったが、設計書の予想した壊れ方ではない**（§B-4）。ボタン行は **833〜900 のまま**・🔍検索は押せる。代わりに **チップがボタン行の上にはみ出し、リセットの中心で `elementFromPoint` が「アクセサリ ACCESSORY」のチップを返す**・**ペインがスクロールできず、ホイールはページへ抜ける**（scrollY 400→1419）。画像 `v16_broken.png` | ⚠️ 期待値と不一致（効き目は示せた） |
| V16-b | 1199×800 で `display: block` を足す | — | 矩形が **6要素で変わる**（`#search-top` 高さ 249→229・`#s-toggle` top 244→234・`#s-sort`/`#s-order`/`#s-reset`/`#s-search` が 20px 上へ）＝`.search-top` の gap が効かなくなる | ✅ |
| **V17** | 1440 → 1199 → 1440 | — | 1199 の間 `--pane-top` は `179px` のまま（1199px以下では書かない＝設計どおり）。1440 に戻すと scrollY=0 で **832〜899 ✓**・scrollY=400 で **833〜900 ✓** | ✅ |
| **V18** | V33（ダイアログを開いたまま押せる・scrollY=400） | — | 🔍検索・リセットとも `elementFromPoint` が本体・実クリックで検索 **1本** | ✅ |
| V18 | V34（背景アルファ） | — | `rgb(27, 31, 40)`（アルファ 1） | ✅ |
| V18 | ⚠️ **V39**（Tab でサブタイプ157種を末尾まで） | ⭐ **隠れるチップ 0件**（`probe39.mjs`） | ⚠️ **157個中1個が一部隠れる**: 13個目の `ANIMAL`（動物）でチップ **808〜842** に対し `.pane-scroll` の下端（＝ボタン行の上端）**833** → **下 9px が切れる**。ボタン行へは Tab で到達（`s-format → s-set → s-sort → s-order → s-reset → s-search`） | ⚠️ **不一致**（§B-6 Q1） |
| V18 | V40（`.editor-bar` の sticky） | — | `position: sticky`・上端 **0**・下端 57・ペイン上端 57（重なり無し） | ✅ |
| **V19** | カード0枚のデッキ・エレメント／サブタイプを開いてページ上でホイール上下 | — | `scrollHeight` は **1258 の1種類だけ**・止めてから1.5秒間 `--pane-top` の書き込み **8 → 8（増えない）** | ✅ |
| **V20** | `npm run validate` | — | **exit 0**。`onRemoveOne wiring in sync — 渡しているのは 1ファイル（tools/deck-builder/app.js）／25ファイル走査`・`pane threshold in sync — 2ページ（トップ 1200px / デッキ構築 1200px）`・`mobile band marker in sync — 1組（style.css 1199px ↔ app.js MOBILE_MQ 1199px）`・`no line-number refs in code comments — 0件（47ファイル走査）` | ✅ |
| **V21** | 変更範囲＋トップの V32 | — | `git -c core.quotepath=false diff --name-only d3210669..HEAD -- . ':(exclude)docs/*'` → **`tools/deck-builder/app.js`・`index.html`・`style.css` の3つだけ**。トップ 1440×900・サブタイプすべて表示で `#reset-pane` が `.controls` の scrollTop 0／1879／3757 で **846〜888 一致・3点とも押せる** | ✅ |
| **V22** | トップ 375×812 | — | 最上部で FAB は `is-hidden`・ホイールで送ると（scrollY 2400）`is-hidden` が外れ `elementFromPoint` が FAB・押すとシート **195〜812**（下端＝812） | ✅ |
| **V23** | デッキ構築のスマホ表示 | — | 1199×800・375×812 とも絞り込みトグル 閉 `none` → 開 `flex` → 閉 `none`。**単位2 のツリーで不具合1 を再測定**: 1199×800 / 375×812 / 1440×900 のメインの＋が **Δ0** | ✅ |

⚠️ **V13 の注（設計書の指示どおり明記する）**: **ヘッドレスでは変更前も同じ結果になる**（2b はヘッドレスで再現しない＝設計書 §4-3-1）。
**これは「F1 で壊していない」確認であって、「2b が直った」証明ではない。**

## B-3. 実行できなかった検証・残っている確認（⭐ 2b の証明の範囲）

| 区分 | 内容 |
|---|---|
| ✅ **証明できた** | **2a**（ページ上端付近でボタン行が画面外）は直った: V9・V10・V11 の実測と、V15 の破壊試験（旧式に戻すと画面外に戻る） |
| ✅ **証明できた** | **F1 の構造が設計書 §4-3-2 の見立ての前提を満たす**: `#search-top` 配下に sticky が**0件**・ボタン行は `static`・スクロールするのは `.pane-scroll` だけ（V12）。変更前は sticky が1件あった |
| ✅ **証明できた** | F1 で見た目・配置を壊していない（V10・V11・V13・V14・V18 の V33/V34/V40） |
| ⚠️ **証明できない** | **2b（実機でボタン行の描画だけが約120px ずれ、当たり判定は正しい位置に残る）が直ったこと**。ヘッドレスでは変更前も再現しない（V13 が変更前・変更後で同値）ので、**このツリーでは差を観測する手段が無い**。合否は**ユーザーの実物確認 2（設計書 §5）だけ**で決まる |
| ⚠️ **未実施** | 実ログインでの確認（V8 と同じ理由）・ちらつき（実物確認 4）・スクロールバーの見た目（実物確認 5） |

## B-4. 差分が設計書の想定と食い違った点

1. **V16-a の壊れ方**: 設計書は「ボタン行がペインの外へ押し出され、`overflow: hidden` で見えなくなる」と予想したが、**`.pane-scroll` に残る `flex: 1 1 auto; min-height: 0` で縮むため、ボタン行は押し出されなかった**。
   代わりに**中身がはみ出してボタン行に重なり、ペインがスクロールできなくなる**（リセットがチップに覆われる）。⭐ **「壊したら赤くなる」こと自体は示せた**ので、F1 の外枠が効いている証拠にはなる。
   補足の実測（`probe16.mjs`・scrollY=400）:

   | 足したルール | ボタン行 | 🔍検索の `elementFromPoint` |
   |---|---|---|
   | （なし） | 833〜900 | ✓ |
   | 設計書どおり（`display: block; overflow: visible`） | 833〜900 | ✓（リセットはチップに覆われる） |
   | ＋`flex: none !important` | **4559〜4626** | **`null`**（＝設計書の予想した壊れ方） |
   | `min-height: auto !important` だけ | 833〜900 | ✓（スクロールコンテナの自動最小高さは0なので効かない） |

2. **V13 の手順**: 設計書 §4-3-1 の再現手順どおり「ペインをホイールで最下端まで」回すと、**ホイールがページへ連鎖してページ最下端（scrollY 1279）まで行き**、sticky のペインが `#view-editor` の終端で押し上げられて**ボタン行の下端が 805 になる**（変更前も同値）。
   V13 の条件「ページを 300px 送る」「ボタン行の下端 ＝ 934」を満たすため、**ホイールは3回だけにして、`.pane-scroll` は `scrollTop` で最下端へ、ページは `scrollTo(0, 300)` に戻してから**測った（表の値はこの手順）
3. V9 の「エレメントを開く前」の🔍検索の位置が設計書 §4-2 の表（838〜880）と違い **828〜870** だった（変更前・変更後とも）。開いた後の変更前の値（967〜1009）は一致しているので、**測った時点のペインの中身の差（結果の件数表示等）と判断**し、判定には使っていない
4. **V12 の初回の値**: 最初の測定で「中身が 488px 動く」と出たが、**「すべて表示」を押した直後に `.pane-scroll` が 12px スクロールしていた**ためだった（`initialScrollTop: 12`）。0 に戻してから測り直して **500px**

## B-5. 設計書からの逸脱

- **コメントの文言**: `#search-top > .row-actions` の直前の段落は、設計書 §4-7 の指示（「古い仕組みの説明を残すと、次の人が sticky に戻す」）に従って**全面的に書き直した**。旧段落のうち **「背景は不透明」「1199px以下はこのブロックの外に書かない」の2点は今も有効なので残した**。
  旧段落の「z-index は 3（チップが position:relative なので後ろに回るとボタンが押せない）」「margin の -16px」は、F1 でボタン行がスクロール領域の外に出て**前提が無くなった**ので消した
- `#view-editor #search-top` のコメントに「⚠️ ここを overflow-y: auto に戻して .row-actions を sticky にしないこと」を1行足した（HTML 側の同じ警告と対にするため）
- `.pane-scroll` の規則のコメントに「scroll-padding-bottom は要らない」理由を書いた（設計書 §4-7 の2つ目の箇条・D5 をそのまま写した）。⚠️ **ただし Q1 の結果次第で書き直しが要る**
- それ以外のコード・値・置き場所は設計書 §4-7 のとおり。`@media` は新設していない（V20 の検査②が緑）

## B-6. 設計担当への確認事項

### ⚠️ Q1（判断が要る）: V39 — Tab で辿ったチップの下 9px が切れる（D5 の前提の見落とし）

- **症状**: 1440×900・scrollY=400・サブタイプ157種を「すべて表示」→ 先頭のチップから Tab で辿ると、**13個目の `ANIMAL` だけ**、チップ 808〜842 のうち **833 から下の 9px が `.pane-scroll` の下端で切れる**（変更前は0件）
- **原因（実測）**: チップの `<input type="checkbox">` は **`position: absolute; opacity: 0; width/height: 0; top: 12.875px`**。フォーカス時にブラウザが画面内へ送る対象は**この 0×0 の点（825）**で、スクロールポートの下端 833 より上にあるので**スクロールしない**。
  変更前は `#search-top` の **`scroll-padding-bottom: 5rem`** が「下端から 80px 以内は見えていない扱い」にしていたので、**ボタン行の重なりだけでなくこのケースも救っていた**。⭐ **設計書 §4-7 と D5 の「ボタン行はスクロール領域に重ならないので不要」は、この2つ目の役割を見落としている**
- **候補（CSSOM で試しただけ。本体には入れていない）**: `#search-top > .pane-scroll { scroll-padding-bottom: 5rem }` → **隠れるチップ 0件**／`2rem` → **0件**
- ⚠️ **D5 は「判断済み・蒸し返さない」事項で、`.pane-scroll` に移すと D5 を覆すため、実装側では入れていない**。どちらにするか（入れる／値／V39 の期待値を「一部なら可」に変える）の判断をお願いします

### Q2（確認のみ）: V16-a の期待値

- §B-4 の1のとおり、設計書の CSSOM では**ボタン行は押し出されない**（壊れ方が違う）。
  ⭐ **効き目は示せている**ので実装は変えていないが、**レビューは設計書の期待値の文言で判定する**ため、期待値を「中身がボタン行に重なる／ペインがスクロールできない」に直すか、ルールに `flex: none !important` を足すかを決めてください

### 申し送り（設計書 §7 の3 の再掲）

- 承認時に `左ペイン化_設計.md`（§12-3 F-a・§12-7 の4 の `scroll-padding-bottom`・§12-9 の 1-2）へ「操作不具合2件で F1 に置き換えた」注記を足すこと。⚠️ **Q1 の結果により §12-7 の4 の注記の中身が変わる**
