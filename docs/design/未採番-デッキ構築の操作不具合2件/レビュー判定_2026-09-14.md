**対象: 「デッキ構築の操作不具合2件」**

# レビュー判定 — デッキ構築の操作不具合2件（2026-09-14）

⚠️ **これは issueコメントの代替**（GitHubアカウント停止中で `gh issue comment` が打てないため、ファイルに残す）。
復旧して採番されたら、本文をそのまま `gh issue comment <n> --body-file docs/design/未採番-デッキ構築の操作不具合2件/レビュー判定_2026-09-14.md` で投函する。

- レビュー対象: **`3f317196`（単位1）・`8e5f413a`（単位2）・`0d3da4e9`（追加指示）**。変更前の基準は `d3210669`（設計完了時点。`tools/` は `15598b3f` と同一であることを `git diff --stat` で確認）
- 参照: [`操作不具合2件_設計.md`](操作不具合2件_設計.md) **0.3**・[`設計完了_2026-09-14.md`](設計完了_2026-09-14.md)・[`追加指示_scroll-padding_2026-09-14.md`](追加指示_scroll-padding_2026-09-14.md)・[`実装報告_2026-09-14.md`](実装報告_2026-09-14.md)・[`実装報告_追加_2026-09-14.md`](実装報告_追加_2026-09-14.md)
- 次の担当: ⭐ **設計担当**（ラベル相当: **`設計確認待ち`**）。⚠️ **承認の前にユーザーの実物確認（設計書 §5・3000番・実ログイン）が要る**。とくに **2b は下記のとおりレビューでは証明できていない**
- ⚠️ 開発担当の数値は写していない。**検証スクリプトはレビュー担当が独自に書いた**（`tmp/review/操作不具合2件/harness.mjs`・`u1.mjs`・`u2.mjs`・`top.mjs`・`smoke.mjs`）

---

## 判定: 合格

| 基準 | 結果 | 証跡 |
|---|---|---|
| M1 設計適合 | ✅ | 検証項目 V1〜V24（V8 は設計どおりユーザーの実物確認へ）すべて期待値と一致（下表）。M1-b は下記 |
| M2 実駆動 | ✅ | レビュー担当が Playwright で操作。PC幅（1200〜1920）と **375×812** の両方。`tmp/review/操作不具合2件/m2_deck_1440.png`・`m2_deck_375.png`・`v9_after.png`・`v13_after.png`・`v23_deck_375_open.png` ほか |
| M3 回帰 | ✅ | `npm run validate` **exit 0**（`validate.log`）。主要画面: トップ（50件表示）・カード詳細モーダル（`m3_top_modal_1440.png`）・カード個別ページ（`/cards/spirit-of-fire/` の h1「火の精霊Spirit of Fire」）・デッキ構築（本判定全体）・大会一覧（454行）を 1440 と 375 で開き、`pageerror` 0件 |
| M4 生成物の同期 | ✅（該当なし） | 変更は `tools/deck-builder/` の手書き3ファイルだけ。生成ロジック・訳・生成物に変更なし |
| M5 静かな劣化がない | ✅ | 検査の追加は無し。代わりに**対策ごとの破壊試験**をレビュー側で再実施: 単位1（変更前ツリーで Δ−474・8件に戻る）／案1（V15）／F1 の外枠（V16 の 1200px以上・1199px 両方）／`scroll-padding-bottom`（V24）。いずれも**壊すと赤くなり、戻すと緑に戻る**。`--pane-top` 未設定時は旧式にフォールバック（fail-open） |
| M6 公開安全性 | ✅ | 本番配信物の増減なし（既存3ファイルの変更のみ）。`_headers`・`404.html`・`functions/`・`shared/`・ルートの `index.html/style.css/app.js` は差分0。秘匿情報なし。CSP: style 属性の追加なし（`--pane-top` は既存の `--editor-bar-h` と同じ CSSOM の `setProperty`） |
| M7 スコープ | ✅ | `git -c core.quotepath=false diff --name-only d3210669..HEAD -- . ':(exclude)docs/*'` → `tools/deck-builder/app.js`・`index.html`・`style.css` の3つだけ。実装3コミットは `tools/` のみ、報告コミット（`f7739972`・`083338a1`）と設計の回答（`f7196ac2`）は `docs/` のみ。設計書にないコメントの追記2件は実装報告 §A-5・§B-5 に根拠あり。`overflow-anchor` の併用なし（`grep -rn overflow-anchor tools/deck-builder/` → 0件） |

### M1-b（既存条件で効いている副作用が新しい条件で効かない型）

- ⭐ **本タスクは絞り込み・並び替えの追加ではない**ので、4つの副作用（絵柄の追従・共有URL・バッジ集計・件数表示）は対象外
- 代わりに同じ考え方で**「`#search-top` がスクロールコンテナである／子が直下にある」ことに依存していた既存の経路**を探した:
  - JS: `el.searchTop` の参照は `classList`（`filters-open`）だけで、`scrollTop`・`scrollIntoView` を使う箇所は `tools/deck-builder/app.js`・`shared/js/` に無い
  - CSS: `#search-top > …` の子結合子は `.row-actions` と新設の `.pane-scroll` だけ（変更前も `.row-actions` だけ）。`.search-top.filters-open .filter-row` 等は子孫結合子なので外枠を足しても当たる
  - Tab のスクロール（旧 `scroll-padding-bottom: 5rem` の副作用）は 0.3 で `.pane-scroll` に移り、V39 で確認済み
  - → **欠落なし**

## 検証の条件

- `/api/*` だけを Playwright の `page.route("**/api/**")` で差し替え（デッキの中身は `モック_左ペイン化_2026-09-05.stub.js` の `STD_ROWS`/`PAN_ROWS` を読み込んで使用・検討中2枚・保存系は状態を持ち150ms遅延で `{card}` を返す）。カード・絵柄・チップ・`renderZones()` は本物
- Chromium（Playwright 1.61.1・`chromium_headless_shell`）・`--ignore-certificate-errors`＋`ignoreHTTPSErrors`
- サーバー（すべてレビュー担当が起動し、終了後にポートから PID を引いて停止。**3000番には触れていない**）:

  | ポート | ツリー | 用途 |
  |---|---|---|
  | 3100 | 作業ツリー（HEAD `083338a1`・未コミット変更なし） | 変更後 |
  | 3101 | `git archive 15598b3f` | 単位1の変更前 |
  | 3102 | `git archive 3f317196` | 単位2の変更前 |
  | 3103 | `git archive 8e5f413a` | 追加指示の変更前 |

- ⭐ **検算（既知の差）**: `app.js` の `shown.join`／`function updatePaneTop`、`style.css` の `scroll-padding-bottom: 2rem`／`5rem` の件数が 3100=1/1/1/0・3101=0/0/0/1・3102=1/0/0/1・3103=1/1/0/0 と**ツリーごとに期待どおり**。さらに**本測定の前に 3101 で V1 の Δ−474・8件が出ることを確認**した

## 設計書の検証項目との対応

### 単位1（§3-4）

| # | 期待値 | 実測（変更前 3101 → 変更後 3100） | 合否 |
|---|---|---|---|
| V1 | 1440×900 の＋でメイン・サイド・検討中とも Δ0・枚数+1 | メイン 669→195（**−474**）→ **669→669（Δ0）** `×3→×4`／サイド 1143→0（−1143）→ **Δ0** `×3→×4`／検討中 1419→0（−1419）→ **Δ0** `×1→×2` | ✅ |
| V2 | −／枚数入力（Enter）／キーボード（Tab で＋ → Enter）で Δ0 | 3経路とも **−474 → Δ0**（−: `×3→×2`／入力: `×3→×4`／キーボード: Tab 後の `aria-label` は「増やす」・`×3→×4`） | ✅ |
| V2（0枚） | タイルが消える以外に飛ばない・上のゾーンの top 不変 | `beseech-the-winds` を −: 880→406（−474）→ **880→880**。消去・トースト「…削除しました 元に戻す」。マテリアル −635／メイン −161 の top **不変** | ✅ |
| V3 | 1200×900／1440×700／1920×1080（サイド）／1199×900／375×812 で Δ0 | 変更前 −435／−474／−862／−448／−1440 → **すべて Δ0・枚数+1**（追加で 1199×800 も Δ0） | ✅ |
| V4 | `#ed-pane-deck` 直下 `childList` が＋1回で 0件 | **8件**（`-material,+material,-main,+main,-side,+side,-maybe,+maybe`）→ **0件**（V1〜V3 の全12回で0件） | ✅ |
| V5 | パンテオン `material,main,pantheon,maybe,side`／切替のたびに並べ直し・メモ最後 | パンテオン用デッキ: `material,main,pantheon,maybe,side`・サイド見出し「サイドボード（このフォーマットでは使いません）」・メモ最後。スタンダードから4回切替: `material,main,side,maybe` ↔ `material,main,pantheon,maybe,side` と毎回切り替わり、**メモは常に最後** | ✅ |
| V6 | 変更前ツリーで V1・V4 が変更前の値／`overflow-anchor` 併用なし | 上の「変更前」列（3101）＝Δ−474・8件。`overflow-anchor` 0件 | ✅ |
| V7 | 結果ダイアログの「＋マテリアル」「＋メイン」で Δ0・ゾーンに反映 | scrollY=600 のまま（600→600 ×2）。`aithne-spirit-of-fire` マテリアル `×1`・`aenean-flurry-of-fire` メイン `×1`。見出し `12 / 12 → 13 / 12`／`60 / 60以上 → 61 / 60以上` | ✅ |
| V8 | 差し替えた `/api/*` での確認である旨を明記・実ログインはユーザーの実物確認 | 本判定の V1〜V7 はすべて差し替えた `/api/*`。**実ログインは未確認**（設計どおり §5 の1へ） | ✅（設計どおりの委任） |

### 単位2（§4-8）＋ 追加指示（0.3）

| # | 期待値 | 実測 | 合否 |
|---|---|---|---|
| V9 | 1440×900・scrollY=0・fire で検索 → エレメントを開く: 🔍検索の下端 ≤ 900・`elementFromPoint` が本体・実クリックで検索1本（ダイアログ開・閉） | 変更前（3102）: 開・閉とも **967〜1009・`null`**。変更後: 開・閉とも **845〜887・`#s-search`・座標クリックで `api.gatcg.com/cards/search` 1本**・`--pane-top: 179px`（`v9_before.png`／`v9_after.png`） | ✅ |
| V10 | 1652×934・157種・scrollY 0/30/60/90/121/400 × `.pane-scroll` 0/中間/最下端: 下端 ∈ [933,934]・top 3点一致・押せる | 変更前: scrollY=0 で 988〜1055（押せない）…（設計書の表と一致）。変更後: 0〜121 は **866〜933**、400 は **867〜934**。**18点すべて合格**（「すべて表示」後のチップ数 157 を確認） | ✅ |
| V11 | 1200×800／1280×720／1536×730／1920×1080／1200×800 最長名で V10 と同じ | 下端 **799〜800／719〜720／729〜730／1079〜1080／800**（最長名は `.editor-bar` **102px**）。5条件×18点すべて top 一致・押せる | ✅ |
| V12 | ①`.row-actions` static ②`#search-top` overflowY hidden ③sticky 子孫0件 ④`.pane-scroll` だけがスクロール | 変更前: sticky・auto・**1件**（`row row-actions`）。変更後: **static・hidden・0件**・`#search-top` の子は `pane-scroll`, `row row-actions` の2つ・`.pane-scroll` の `scrollTop` 0→500 で `#s-name` が **500px** 動きボタン行 **0px**・`#search-top.scrollTop` 0 | ✅ |
| V13 | ユーザーの状態（1652×934・ページ300・条件3つ・レアリティ群・`.pane-scroll` 最下端）で並び替え下端 < ボタン行上端・ボタン行下端 934・画素でも同順 | 選択中「WIND／TAMER／REGALIA」・レアリティ開。**並び替え 778〜847 < ボタン行 867〜934**。画素（x=20 と x=200 の列）: 緑 **778〜846** → マゼンタ **867〜933**。⚠️ **変更前（3102）も完全に同値**（`v13_before.png`／`v13_after.png`） | ✅（⚠️ 下記） |
| V14 | 1199×800／900×800／375×812 の9要素矩形がトグル閉・開で変更前と一致・`--pane-top` 未設定 | **54組すべて差 0**（`v14_before.json`／`v14_after.json`。開閉で `#s-search` が 620→1065 に動くのでトグルは効いている）。`--pane-top` は3幅とも `""`（scrollY=300 後も） | ✅ |
| V15 | 旧 `max-height` を insertRule → 画面外に戻る／消すと戻る | 基準 845〜887 ✓ → **967〜1009・`null`** → deleteRule で **845〜887・`#s-search`** | ✅ |
| V16 | (0.3) 1440×900・157種・scrollY=400 で `display:block; overflow:visible` → ①リセット中心が `#s-reset` 以外 ②`scrollTop=500` でも 0。1199px で `display: block` → V14 の矩形が変わる | ①**「アクセサリ ACCESSORY」のチップ**（`v16a_broken_after.png`）②**0**（ボタン行は 833〜900 のまま＝0.3 の是正どおり）。ルール削除で `scrollTop=500` が効き、リセットは本体に戻る。1199×800: **6要素が変化**（`#search-top` 高さ 249→229、`#s-toggle`・`#s-sort`・`#s-order`・`#s-reset`・`#s-search` が 10〜20px 上へ）→ 削除で差 0 | ✅ |
| V17 | 1440 → 1199 → 1440 で V10 相当の値 | 1199 の間 `--pane-top` は書かれない（179px のまま）。1440 に戻して scrollY=0 **832〜899**・400 **833〜900**・3点とも押せる | ✅ |
| V18 | V33／V34／V39（0.3 の数値判定）／V40 | V33: scrollY=400・ダイアログ開のまま🔍検索・リセットとも `elementFromPoint` 本体、座標クリックで検索 **1本**。V34: `rgb(27, 31, 40)`（アルファ1）。V39: 下記。V40: `.editor-bar` sticky・上端 **0**・下端 57・ペイン上端 57 | ✅ |
| V39 | 157個すべてチップが `.pane-scroll` の矩形内・続けて `#s-format → … → #s-search` | 変更前（3103＝`8e5f413a`）: **1個切れ**（`ANIMAL` 808.3〜842.1／下端 833）。変更後: `scroll-padding-bottom` **32px**・**157個訪問・切れ 0個**。続く Tab は `morebtn → SUMMARY（レアリティ） → AND → OR → s-format → s-set → s-sort → s-order → s-reset → s-search` | ✅ |
| V24 | `scroll-padding-bottom: 0 !important` で切れが1個以上／消すと0個 | **0px・切れ1個（`ANIMAL` 808.3〜842.1／833）** → deleteRule で **32px・0個** | ✅ |
| V19 | カード0枚のデッキでホイール上下 → 文書の高さ不変・止めたら `--pane-top` の書き込みが止まる | `scrollHeight` の観測値 **1258 の1種類のみ**。`<html>` の style 属性の変化回数は停止後 0.5秒時点 **11** → さらに1.5秒後 **11**（増えない） | ✅ |

⚠️ **V13 について（設計書の指示どおり明記）**: ヘッドレスでは**変更前も同じ結果**になる（2b はヘッドレスで再現しない＝設計書 §4-3-1）。**これは「F1 で壊していない」確認であって、「2b が直った」証明ではない。**

### 共通（§5）

| # | 期待値 | 実測 | 合否 |
|---|---|---|---|
| V20 | `npm run validate` exit 0・UI規約3本と行番号参照が緑 | **exit 0**。`onRemoveOne wiring in sync — 渡しているのは 1ファイル（tools/deck-builder/app.js）／25ファイル走査`・`pane threshold in sync — 2ページ（トップ 1200px / デッキ構築 1200px）`・`mobile band marker in sync — 1組（style.css 1199px ↔ app.js MOBILE_MQ 1199px）`・`no line-number refs in code comments — 0件（47ファイル走査）`・`all checks passed.` | ✅ |
| V21 | 変更は `tools/deck-builder/` の3ファイルだけ＋トップの V32 | 3ファイルだけ（M7 の欄）。トップ 1440×900・サブタイプ157種: `#reset-pane` が `.controls` の scrollTop **0／1879／3757** で **846〜888 一致・3点とも押せる** | ✅ |
| V22 | トップ 375×812: 送ると FAB が出る・シート下端 812 | 最上部で `is-hidden`。ホイールで scrollY 3750 → `is-hidden` 無し・`elementFromPoint` が FAB。押すとシート **195〜812**（`v22_top375_sheet.png`）。ルートの `style.css`/`app.js` は差分0なので3対策は無変更 | ✅ |
| V23 | デッキ構築 1199×800／375×812 でトグル開閉と不具合1の Δ0 が両立 | 両幅とも `#s-filter-row` の display `none → flex → none`（`v23_deck_1199_open.png`／`v23_deck_375_open.png`）。不具合1は V3 を**単位2・追加指示を含む HEAD**で測っており 1199・375 とも Δ0 | ✅ |

## ⚠️ 証明できた範囲と、ユーザーの実物確認に委ねる範囲

| 区分 | 内容 |
|---|---|
| ✅ レビューで証明 | **不具合1**は直った（変更前ツリーとの並走で Δ−474→0・原因の抜き差し 8件→0件） |
| ✅ レビューで証明 | **2a**（ページ上端付近でボタン行が画面外）は直った（V9〜V11・破壊試験 V15） |
| ✅ レビューで証明 | **F1 の構造が設計書 §4-3-2 の見立ての前提を満たす**（sticky 子孫 1件→0件・ボタン行 static・スクロールするのは `.pane-scroll` だけ）。**見た目・配置・1199px以下・トップを壊していない**（V10〜V14・V18・V21〜V23） |
| ⚠️ **レビューでは証明できない** | **2b（実機でボタン行の描画だけが約120px ずれ、当たり判定は正しい位置に残る）が直ったこと**。ヘッドレスでは変更前も再現しない（V13 が変更前・変更後で同値）。⭐ **合否はユーザーの実物確認 2 だけで決まる**（直っていなければ承認せず設計へ戻す＝設計書 §6） |
| ⚠️ 未確認（設計どおり委任） | 実ログイン＝本物の保存経路での不具合1（実物確認 1）・スクロール時のちらつき（実物確認 4）・スクロールバーがボタン行の上で終わる見た目（実物確認 5） |

⭐ **2b を未証明のまま合格にした理由**: 設計書・設計完了 R1 が「2b はヘッドレスで再現しない／合否はユーザーの実物確認で判定する」と工程を明示しており、**レビューの対象は「その前提で求められた検証項目」**だから。「未検証」を合格にしているのではなく、**レビューで検証できる項目はすべて実施して一致し、残りは設計が定めた別工程へ明示的に回している**。

## ユーザーの実物確認で見てほしい点（設計書 §5 の表に加えて）

1. ⭐ **実物確認 2（本命）**: スクリーンショットのときと**同じブラウザ・同じ拡大率**で行う。🔍検索が「並び替え」の下・ペインの最下端に描かれ、**描かれた位置を押して検索が走る**こと。直っていなければ**ブラウザの種類・バージョン・拡大率**を控える（設計書 §7 の4）
2. **実物確認 4**: 案1 は `scroll` のたびに rAF で `--pane-top` を書くので、**ページ最上部付近（ヘッダが見え隠れする 0〜121px の範囲）を速く上下したとき**に、ボタン行が1フレーム遅れて上下にぶれないか
3. **キーボード**: サブタイプを「すべて表示」にして Tab で辿ったとき、フォーカス中のチップが下端で欠けないこと（V39 の実機版）。⚠️ **Shift+Tab で上へ戻るときの上端の切れは設計書 §7 の5 で対象外**（気になるかどうかだけ聞く）

## 推奨基準の指摘（合格を妨げないもの）

- **S1（コメント）**: `tools/deck-builder/style.css` の `#search-top > .pane-scroll` のコメントが「⚠️ **0.3 で追加（D5 を覆した）**」と**設計書の版番号と判断ID**を本文に書いている。設計書は今後も改版されるので、コードだけを読む人には「0.3」「D5」が何を指すか分からず、将来は腐る（既存のコメントは「左ペイン化_設計 §12-7」のように**文書名＋節**で参照する慣習）。追加指示 §3-1 の文面どおりに写したものなので**開発担当の逸脱ではない**。直すなら「操作不具合2件_設計 §4-7 (2-b)」への参照に置き換える程度（対応要否は設計担当の判断待ち）
- S2（応答性）: 指摘なし（公式APIへの逐次リクエストは増えていない。`scroll` は passive＋rAF で間引き、停止後に書き込みが止まることを V19 で確認）
- S3（fail-open）: 指摘なし（`--pane-top` 未設定時は `var(--editor-bar-h, 57px)` へフォールバック。V15 で旧式を当てると 2a の症状に戻るだけで落ちない）

## 参考（合否に含めない）

- 実装報告_追加 §4 の「`page.click()` で『すべて表示』を押すと `.pane-scroll` が 324px 送られる」は、レビューでは踏んでいない（「すべて表示」は最初から**座標クリック（`page.mouse.click`）**で押し、各測定の前に `.pane-scroll` の位置を明示的に設定した）。Playwright がクリック前に要素を画面内へ送る際に `scroll-padding-bottom` が効くというテストハーネス側の現象という開発担当の判断に異論はない。**実機では要素がスクロールポート下端 32px 以内にある状態でキーボードからフォーカスすると少し余分に送られる**が、これは V39 のために意図した挙動
- 1199px 以下では `--pane-top` が最後に 1200px 以上で書いた値のまま残る（V17）。CSS は `min-width: 1200px` の中でしか読まず、1200px 以上へ戻った瞬間に `resize` の rAF で書き直されるので実害はない（設計どおり）

## 設計担当への確認事項

- なし（合否を左右する仕様の曖昧さは見つからなかった）
- 申し送りの再掲: 承認時に `左ペイン化_設計.md`（§12-3 F-a・§12-7 の4・§12-9 の 1-2）へ「操作不具合2件で F1 に置き換えた」注記を足す（設計書 §7 の3）

## 証跡の置き場

`tmp/review/操作不具合2件/`（gitignore）: `harness.mjs`・`u1.mjs`・`u2.mjs`・`top.mjs`・`smoke.mjs`／`u1_*.json`・`u2_*.json`・`u1_before.log`・`u2_before_*.log`・`v14_before.json`・`v14_after.json`・`validate.log`／画像 `v9_before.png`・`v9_after.png`・`v13_before.png`・`v13_after.png`・`v16a_broken_after.png`・`v22_top375_sheet.png`・`v23_deck_1199_open.png`・`v23_deck_375_open.png`・`m2_deck_1440.png`・`m2_deck_375.png`・`m3_*.png`
