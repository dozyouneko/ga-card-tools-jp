**対象: 「デッキ構築の操作不具合2件」**（→ 継続）

# ⭐ ユーザーの実物確認の結果（2026-09-14）— 設計書 0.3 §5

⚠️ **これは issueコメントの代替**（GitHubアカウント停止中で `gh issue comment` が打てないため、ファイルに残す）。
復旧して採番されたら、本文をそのまま `gh issue comment <n> --body-file docs/design/未採番-デッキ構築の操作不具合2件/実物確認結果_2026-09-14.md` で投函する。

- 記録者: 設計担当（ユーザーの回答はリード経由で受け取った）
- ⚠️ **承認はしない**——**新しい描画崩れ（2c）が見つかったため、設計担当に戻して調査した**（→ 設計書 **0.4 §8**・[`追加指示_合成スクロール_2026-09-14.md`](追加指示_合成スクロール_2026-09-14.md)）

---

## 1. 環境

| 項目 | 内容 |
|---|---|
| サーバー | ⭐ **3000番で `npx wrangler pages dev . --local --port 3000`**（リードが起動・ローカルD1） |
| 配信内容 | **HEAD `29ee6a06`**（実装は単位1 `3f317196`・単位2 `8e5f413a`・追加指示 `0d3da4e9`） |
| ログイン | ⭐ **実ログイン（Discord）**。ユーザーが **Discord Developer Portal に `http://localhost:3000/api/auth/discord/callback` を登録した**（2026-09-14・ユーザー作業）。登録前は「OAuth2 redirect_uriが無効です」でログインできなかった |
| ブラウザ | **Chrome**（⚠️ バージョン・拡大率・Windows の表示スケールは**不明**） |

⭐ **ローカルの wrangler で実ログインできる経路が今回はじめてできた**（それまでデッキの中身が要る確認はヘッドレスの差し替えだけだった）。
⚠️ **登録した callback URL はユーザーの Discord アプリの設定に残っている**（本番の URL とは別に追加した形）。

## 2. §5 の5項目

| # | 見るところ | 結果 |
|---|---|---|
| 1 | ＋／−／枚数入力で画面が上へ飛ばない（不具合1） | ✅ 飛ばない |
| 2 | スクリーンショットと同じ操作で、描かれた🔍検索の位置を押すと検索される（2b） | ✅ ⭐ **2b は直っていた**（設計書 §4-3-2 の見立て＝F1 が当たった） |
| 3 | ページ最上部でも🔍検索が見えていて押せる（2a・案1） | ✅ |
| 4 | ページを上下に送ってもボタン行がちらつかない（案1） | ✅ |
| 5 | ペインの縦スクロールバーがボタン行の上で終わる（F1 の見た目の差） | ✅ 気にならない |

## 3. ⚠️ 新しい症状（2c）

**ユーザーの原文**:

> 左ペインのスクロールバーを検索条件を開いた状態などで動かすとキャプチャのように崩れて見えることがあります。少し時間を置くと戻りますが、おかしい挙動です。調査をお願いします。

**追加の回答**: 「**本番では起きない**」（＝修正前の本番サイトでは起きない）

**キャプチャ（約 323×818・左ペイン付近の切り出し。画像はリポジトリに無く、リードが読み取った値）**:

| y（画像のピクセル） | 描かれているもの |
|---|---|
| 0〜50 | 編集バー（← 一覧／#4検証 S1 スタンダード(回帰) ✎） |
| ⚠️ **55〜170** | ⚠️ **中身の無い枠だけ**（群カードと同じ暗い背景・角丸の枠線） |
| ⚠️ **177〜232** | ⚠️ **「エレメント」の見出し行（AND/OR 付き）が水色の矩形（フォーカスリング状）で囲まれている** |
| ⚠️ **232〜430** | ⚠️ **エレメントのチップ一覧が、群カードの背景・枠線なしで描かれている**（右端に内側スクロールバー・下端で切れる） |
| 440〜 | クラス・タイプ・サブタイプ・レアリティの各群、フォーマット等が**通常どおり** |
| 右端 x≈296〜310 | ペイン全体の縦スクロールバー（つまみ y≈75〜555） |
| 770〜815 | ⭐ **ボタン行（リセット／🔍検索）はペインの最下端に正しく描かれている** |

- **時間を置くと元に戻る**
- ⭐ **設計担当の読み（§8-1）**: クラス群（440〜）との間隔から、**見出しとチップはレイアウトどおりの位置**にあり、**エレメント群カードの「背景と枠」だけが約260px 上の古い位置に残っている**（カード 177〜430 の高さ 253px を 260px 上へずらすと −83〜170 で、ペイン上端 55 で切られて **55〜170 の空の枠**になる）。**スクロールした直後の古い描画が一部だけ残っている**形

## 4. 次にやること

- 設計担当: 調査 → 設計書 **0.4 §8** と **追加指示**（→ 開発担当 `実装待ち`）
- ⚠️ **ユーザーに聞きたいこと（ブロックはしない）**: **Windows の表示スケール（100%／125%／150%…）と Chrome の拡大率**。⭐ 見立て（§8-3）は **`devicePixelRatio` が 1.5 未満のときだけ当てはまる**ので、**150% 以上だった場合は見立てが外れている可能性が高い**
- 修正後にもう一度、**このキャプチャと同じ操作**（群を開いた状態でペインのスクロールバーをつまんで動かす）を実物で確認してもらう（設計書 §5 の実物確認 6）
