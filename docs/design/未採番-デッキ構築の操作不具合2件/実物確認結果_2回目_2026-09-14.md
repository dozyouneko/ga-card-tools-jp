**対象: 「デッキ構築の操作不具合2件」**（→ 継続）

# ⭐ ユーザーの実物確認の結果（2回目・2026-09-14）

⚠️ **これは issueコメントの代替**（GitHubアカウント停止中で `gh issue comment` が打てないため、ファイルに残す）。
復旧して採番されたら、本文をそのまま `gh issue comment <n> --body-file docs/design/未採番-デッキ構築の操作不具合2件/実物確認結果_2回目_2026-09-14.md` で投函する。

- 記録者: 設計担当（ユーザーの回答はリード経由で受け取った）
- ⚠️ **承認はしない**——**新しい不具合（2d）が報告されたため、設計担当に戻して調査した**（→ 設計書 **0.5 §9**）
- 1回目の記録: [`実物確認結果_2026-09-14.md`](実物確認結果_2026-09-14.md)（書き換えていない）

---

## 1. 環境

| 項目 | 内容 |
|---|---|
| サーバー | 3000番の `npx wrangler pages dev . --local --port 3000`（リードが起動・ローカルD1） |
| 配信内容 | **HEAD `7a394ded`**（F2 `994de8dc` 入り。`style.css` に `background: var(--bg)` が配信されていることをリードが確認済み） |
| ログイン | 実ログイン（Discord。1回目で登録した callback を使用） |
| ブラウザ | Chrome（⚠️ バージョン・Windows の表示スケール・Chrome の拡大率は**未回答**） |

## 2. 実物確認 6〜9 の結果

⚠️ **未回答**（2c の崩れ・ホイール／Tab・地色と文字・1〜5 の再確認。リードがユーザーに聞いている）。

## 3. ⚠️ 新しい不具合（2d）

**ユーザーの原文**:

> サブタイプのすべてを表示をしたあと左ペインのスクロールバーが途中までしかスクロールできず、サブタイプを少ない表示に戻したりフォーマットなどの検索条件までスクロールできません。
> 修正をお願いします。

- ⚠️ **本番（修正前）でも起きるかは聞いていない**

**キャプチャ（約 697×1923px。画像はリポジトリに無く、リードが読み取った値）**:

| y（画像のピクセル） | 描かれているもの |
|---|---|
| 0〜70 | 編集バー（← 一覧／#4検証 S1 スタンダード(回帰) ✎／公開／スタンダード）。⚠️ **その裏に、ペインの「クラス」群の見出しと AND/OR が上端で切れて薄く見える** |
| 70〜145 | 「タイプ」群 |
| 160〜 | 「サブタイプ」群: 見出し → 絞り込み入力（ドラゴン / dragon）→「157種」→ チップ（クレリック CLERIC、呪文 SPELL、…、アバター AVATAR、斧 AXE、バリア BARRIER、装身具 BAUBLE、熊 BEAR、鳥 BIRD、ビショップ BISHOP） |
| ⚠️ **1580〜1630** | ⚠️ **「鳥 BIRD／ビショップ BISHOP」の行がペインの下端で切れている**（アルファベット順で B の途中まで） |
| x≈452〜468・y≈70〜1625 | ペインの縦スクロールバー（下端に下矢印）。**つまみは最下部付近に見える**（リードの読み取り） |
| 1645〜1710 | リセット／🔍検索のボタン行（正しく表示）。ペイン下端 y≈1725 |
| 1790〜1900 | ページのフッター＝**ページは最下部までスクロールされている** |
| 右側 | デッキのゾーン（Aqua Vitae・Advent of the Stormcaller・サイドボード Argus・検討中「カードがありません。」・メモ）＝⚠️ **カードの少ないデッキ** |

⭐ **設計担当の読み（§9-1）**:

- ペイン幅 320px が画像で約 470px ＝ **倍率 約1.47**（Windows の表示スケール 150% に近い）。画像の高さ 1923px は CSS で約 1308px
- **ペインの上端が編集バーより上に押し上げられている**（`#view-editor` の終端に当たって sticky のペインが押し上げられた状態。**本番でも同じ幾何になる**＝§9-2）
- ⚠️ ⭐ **「つまみは最下部付近」と「中身はサブタイプの B までしか見えていない」は両立しない**（ペインを最下端まで送れば、下端には並び替え行が来る）。
  ⭐ **どちらかが成り立っていない**——**(a) スクロール位置は最下端なのに描画が古いまま**（2c と同じ系統）か、**(b) 本当にスクロール範囲が短い**か、**(c) つまみの読み取りが違う**

## 4. 次にやること

- 設計担当: 再現を試みた → **再現せず**（設計書 0.5 §9-2）。**原因を切り分ける質問をユーザーに出す**（§9-4）。**直し方の設計は回答を待つ**
- 起票案の状態は `実装中` のまま
