# デッキ構築の操作不具合2件 — 設計書（2026-09-14）

## 改版履歴

| 版 | 日付 | 内容 |
|---|---|---|
| **0.2** | **2026-09-14** | ⭐ **設計完了**。**①Q2＝案1で決定**（ユーザー「推奨案でおねがいします」。⚠️ **2026-09-10 の実物確認 1-2「(A) このまま」を上書き**）**②Q1 の回答とスクリーンショットで、不具合2が「2a 画面外」と「2b 描画だけのずれ」の2症状だと判明**（§4-3。⚠️ **ダイアログの開閉は再現条件ではない**）**③2b はヘッドレスで再現せず**（DSF 0.8〜1.5・新旧ヘッドレス・GPU系フラグで試行。§4-3-1）。⭐ **見立て「sticky の中のスクロール領域の中の sticky」を §4-3-2 に書き、構造を無くす F1 を単位2 に追加**（見た目は変えない。**合否はユーザーの実機でしか判定できない**）**④検証項目を V9〜V23 に振り直し** |
| **0.1** | **2026-09-14** | 初版。⭐ **2件とも実測で再現した**（§2・§4-2）。**不具合1は設計確定（単位1）**。⚠️ **不具合2は直し方に見た目の違う3案があるため、ユーザー判断待ち（単位2）**（§4-5）。⚠️ **不具合2の「何も表示されていないところを押すと検索される」だけはヘッドレスでは再現していない**（§4-3） |

### 実装済み / 実装待ち

| 単位 | 内容 | 状態 |
|---|---|---|
| **単位1** | 不具合1：デッキ側の＋/−/枚数入力でページが上へ飛ぶ（§3） | ⭐ **実装待ち** |
| **単位2** | 不具合2：左ペインの🔍検索が押せない（§4）＝**案1**（ペインの高さを実際の上端から計算）＋ **F1**（ボタン行を sticky にしない構造へ） | ⭐ **実装待ち**（⚠️ **2b の合否はユーザーの実物確認**。§5） |

---

## 1. 要件（ユーザーの報告・2026-09-14）

> デッキ構築ツールにて修正要望とバグを発見しました。まずは以下の修正を優先してください。
> - カードを追加した時、画面が上にスクロールするのを修正してほしい
> - カードを検索してからエレメントなどを開いた状態でにして左ペインのスクロールを出すと検索ボタンが押せない。下の方の何も表示されていないところをクリックすると検索されるのでおそらく描画がおかしい。

リードがユーザーに確認した補足:

- 画面幅は **PC（左ペイン表示・1200px以上）**
- 1件目は **デッキ側（ゾーン内のカード）の＋で追加**したとき
- **2件を1タスクにまとめる**

---

## 2. 測定の方法（再現し直すための手順）

- 対象コードは **HEAD `15598b3f`**（`tools/deck-builder/` と `shared/js/card-search.js` は本番 `ecb2b51d` と同一＝**ユーザーが触った本番と同じもの**）
- ログイン前提の画面なので、**`/api/*` だけを差し替えて「ログイン済み・デッキを開いた状態」**を作った。
  設計担当は **Playwright の `page.route("**/api/**")`** で差し替えた（⭐ **CLAUDE.md の srcdoc 方式と差し替える範囲は同じ**——
  カード・絵柄・チップ・`renderZones()` は本物。srcdoc 方式との違いは**トップレベルのページで動く**ことだけで、
  ⭐ **ページのスクロールが本物の window になる**ので不具合1の測定にはむしろ忠実）
  - デッキの中身は **`モック_左ペイン化_2026-09-05.stub.js` の `STD_ROWS`**（実データ: マテリアル12／メイン60／サイド9）＋検討中1枚
  - ⚠️ **スタブの `POST`/`PATCH` は `card: null` を返すので＋が例外で落ちる。** 差し替えでは
    **`{ card: { card_slug, board, qty } }` を返す**こと（さもないと `renderZones()` まで到達せず再現しない）。応答は150ms遅延させた
- ブラウザは Playwright の Chromium 149（`chromium_headless_shell` と新ヘッドレス `channel: "chromium"` の両方で同じ結果）
- ⚠️ **ここに書く `scrollY` の絶対値は上の条件での値。** 期待値は**差（Δ）**と**画面内に入っているか**で判定する

---

## 3. 不具合1：デッキ側の＋でページが上へ飛ぶ（単位1）

### 3-1. 再現（実測）

**手順**: デッキを開く → ページを下へ送り、メインデッキの11枚目のタイルを画面の上から約300pxの位置に置く →
タイルにホバーして **＋** を押す → 1.2秒待って `scrollY` を読む。

| 画面 | 押したゾーン | scrollY（前 → 後） | Δ |
|---|---|---|---|
| 1440×900 | メイン | 669 → 195 | ⚠️ **−474** |
| 1440×900 | サイド | 1143 → 0 | ⚠️ **−1143**（0で頭打ち） |
| 1440×900 | 検討中 | 1419 → 0 | ⚠️ **−1419**（0で頭打ち） |
| 1200×900 | メイン | 609 → 174 | ⚠️ **−435** |
| 1920×1080 | サイド | 862 → 0 | ⚠️ **−862**（0で頭打ち） |
| 1199×900（スマホ表示） | メイン | 878 → 430 | ⚠️ **−448** |
| 375×812（スマホ表示） | メイン | 3078 → 1638 | ⚠️ **−1440** |

- ⭐ **Δ は「押したゾーンより上にあるゾーンの高さの合計」に一致する**。1440×900 ではマテリアルゾーンの上端 245・メインゾーンの上端 719 で、**719 − 245 = 474**
- ⭐ **同じ症状が出る操作**: ＋（上表）／**−**（メイン Δ−474）／**枚数の直接入力**（メイン Δ−474）／**キーボード（Tabで＋にフォーカスして Enter）**（メイン Δ−474）
- ⭐ **出ない操作**: ⋯メニューの「サイドへ移動」（Δ0）／**検索結果ダイアログの「＋マテリアル」**（Δ0）
- ⚠️ **1199px以下（スマホ表示）でも起きる**。ユーザーの報告は PC だが、**直せば全幅で直る**（§3-3 の変更は幅に依存しない）

### 3-2. 原因（実測で特定）

**`renderZones()`（`tools/deck-builder/app.js`）が、枚数が1枚変わるだけでも全ゾーンを DOM から抜いて差し直している**ことと、
**Chrome のスクロールアンカリングが「フォーカス中の要素」を優先的な基準にする**ことの組み合わせ。

```js
order.forEach((zone) => {
  const zoneEl = pane.querySelector(`.zone[data-zone="${zone}"]`);
  pane.insertBefore(zoneEl, memoBlock); // 上から order の順に並べ直す(メモは常に最後)
  ...
  grid.innerHTML = "";   // ← 押した＋ボタンはここで捨てられる
```

⚠️ **下の1〜4は Chrome 内部の順序についての推定**（内部の実装は読んでいない）。**確かなのは次の表の実験結果**で、
**「並べ直しを止めれば直る」「フォーカスが無ければ起きない」「Δ が上のゾーンの高さの合計に一致する」の3点**。

1. ＋を押すと、そのボタンにフォーカスがある
2. `renderZones()` の最初の周で **マテリアルゾーンが `memoBlock` の直前（＝一番下）へ移される**。
   この瞬間、**メインゾーン以下が一時的にマテリアルの高さぶん上へ詰まる**
3. メインゾーンの `grid.innerHTML = ""` で**フォーカス中のボタンが捨てられる**。ブラウザはフォーカスを外すためにレイアウトを確定させ、
   **フォーカス中だったボタンを基準にスクロールを補正する**（＝上へ詰まったぶんだけページを上へ送る）
4. その後マテリアルは元の位置（最上段）に戻るが、**補正されたスクロールは戻らない**

**裏付け（どれも 1440×900・メイン・実測）**:

| 実験 | Δ | 読み取れること |
|---|---|---|
| そのまま | **−474** | 再現 |
| ⭐ **`.zone` の `insertBefore` だけを握りつぶす**（並べ直しをしない） | **0** | ⭐ **原因は並べ直し**（サイド・検討中・1920×1080・1199・375 でも **0**） |
| `#ed-pane-deck { overflow-anchor: none }` | 0 | スクロールアンカリングが補正の主体 |
| クリックの捕獲フェーズで `activeElement.blur()` | 0 | **フォーカス中の要素**が基準になっている |
| クリックせず `dispatchEvent("click")`（フォーカスが移らない） | 0 | 同上 |
| `#ed-pane-deck` 直下の `childList` を MutationObserver で数える | ＋1回で **8件**（4ゾーン × 抜く/差す） | ⭐ **標準デッキでは並び順は最初から正しい**のに、毎回8件動いている |

⭐ **標準デッキの DOM は、1回目の描画のあと `pantheon(hidden), material, main, side, maybe, memo-block` で安定している**（実測）。
つまり**2回目以降の並べ直しは常に空振り**で、**不具合を起こすためだけに走っている**。

### 3-3. 変更（単位1）

**`tools/deck-builder/app.js` の `renderZones()` だけ。⭐ 並び順が既に正しいときは、ゾーンを動かさない。**

```js
// ⚠️ ゾーンの並べ直しは「表示するゾーンの並びが order と違うとき」だけ行う（操作不具合2件_設計 §3）。
//    ＋/−で毎回全ゾーンを抜き差しすると、Chrome のスクロールアンカリングが
//    フォーカス中の＋ボタンを基準にページを上へ送ってしまう（メインで −474px を実測）。
//    ⚠️ hidden のゾーン（標準デッキの pantheon 等）は比較から外す。混ぜると毎回「違う」と判定して元に戻る。
const shown = [...pane.querySelectorAll(":scope > .zone")].map((z) => z.dataset.zone).filter((z) => order.includes(z));
if (shown.join() !== order.join()) {
  order.forEach((zone) => pane.insertBefore(pane.querySelector(`.zone[data-zone="${zone}"]`), memoBlock));
}
```

- ⚠️ **並べ直しのループを、描画のループ（`order.forEach((zone) => { … grid.innerHTML = ""; … })`）から分けること。**
  既存のループ内の `pane.insertBefore(zoneEl, memoBlock)` の1行は**削除**し、上のブロックを**描画ループの直前**に置く
  （`ZONES.forEach(... zoneEl.hidden = ...)` の直後）
- ⚠️ **比較は「表示するゾーンだけ」で行う**（`filter((z) => order.includes(z))`）。
  標準デッキでは hidden の `pantheon` が DOM 上に残っているので、**全 `.zone` で比べると毎回不一致になり、不具合がそのまま残る**
- ⭐ **フォーマット切替（スタンダード ↔ パンテオン）では並びが実際に変わるので、従来どおり並べ直す**。
  そのときフォーカスは editor-bar のフォーマット切替にあり、ゾーンの中には無い
- ⚠️ **`#ed-pane-deck { overflow-anchor: none }` は採らない**（上表で効くことは確認済み）。
  **原因（空振りの抜き差し）を残したまま症状だけ消す**ことになり、ブラウザ既定の補正も一律に失うため。
  ⚠️ **両方入れることもしない**——片方を外しても症状が戻らず、**破壊試験で効き目を示せなくなる**
- ⚠️ **スコープ外（手を付けない）**: ＋を押したあと**フォーカスが `BODY` に落ちる**のは変更前からの挙動（ボタンが作り直されるため）。
  キーボードで続けて押せない点は**別の課題**（§7 の申し送り）。⚠️ **もし将来フォーカスを戻すなら `focus({ preventScroll: true })`**
  （既定の `focus()` は要素を画面内へスクロールさせる）

### 3-4. 検証項目（単位1）

⚠️ **「変更前」は HEAD `15598b3f` のコード。** レビューは作業ツリーを変えられないので、
**`git archive 15598b3f` を別ディレクトリへ展開し、そこの `scripts/serve.mjs` を別ポートで起動する**
（⚠️ CLAUDE.md「環境の注意」：`serve.mjs` の配信ルートは `import.meta.url` で決まるので **`cd` では切り替わらない**。
⭐ **本測定の前に、変更前で V1 の Δ−474 が出ることを先に確認して検算する**）。

| # | 何を | 期待値 |
|---|---|---|
| **V1** | ⭐ **症状が直ったこと**（1440×900・§3-1 の手順） | **メインの＋で Δ0**（変更前は **−474**）。**サイド・検討中の＋も Δ0**（変更前は 0 まで飛ぶ）。＋を押したタイルの枚数表示が **1増える**（例 `×3` → `×4`） |
| **V2** | 他の操作経路 | **−／枚数の直接入力（Enter）／キーボード（Tab で＋にフォーカス → Enter）** がいずれも **Δ0**（変更前はメインで **−474**）。⚠️ **−で枚数が0になる場合**（タイルが消えて「元に戻す」トーストが出る）は、**消えた行のぶんだけ下のゾーンが上がるのは正**——**それ以外に飛ばないこと**（押したゾーンより上のゾーンの `getBoundingClientRect().top` が変わらない） |
| **V3** | 幅と高さ | **1200×900 / 1440×700 / 1920×1080 / 1199×900 / 375×812** のメイン（1920×1080 はサイド）で **Δ0** |
| **V4** | ⭐ **原因に効いていること（直接の証拠）** | `#ed-pane-deck` に `MutationObserver({ childList: true })`（subtree なし）を付けて＋を1回押す → **記録 0件**（変更前は **8件**＝`material/main/side/maybe` をそれぞれ抜いて差す） |
| **V5** | ⭐ **並べ直しが必要な場面で並べ直すこと**（退行防止） | ①パンテオン用デッキ（スタブの `data-deck="pan"` 相当＝メイン60種・Boon 2・サイド2）を開いたとき、**表示されるゾーンの DOM 順が `material, main, pantheon, maybe, side`**（サイドは「このフォーマットでは使いません」の見出し）②フォーマットを**スタンダード → パンテオン → スタンダード**と切り替えるたびに、表示順が **`material, main, side, maybe`** ↔ **①の順** に切り替わり、**メモ（`.memo-block`）が常に最後**。⚠️ 比較は `#ed-pane-deck > .zone:not([hidden])` の `data-zone` の並び |
| **V6** | ⭐ **破壊試験** | §3 冒頭の「変更前」サーバーで **V1 と V4 が変更前の値（Δ−474・8件）に戻る**こと。⚠️ **本体に対策を2つ入れていないこと**（`overflow-anchor` を足していない）を `git diff` で確認——**入っていると変更前サーバーとの差が「ガードの効果」だと言えなくなる** |
| **V7** | 検索結果ダイアログからの追加 | ダイアログの「＋マテリアル」「＋メイン」で **Δ0** のまま（変更前も 0）・**ゾーンに反映**（枚数表示とゾーン見出しの `N / 12` 等が増える） |
| **V8** | ⚠️ **デッキの中身に依存するもの（R5）** | 上の V1〜V7 は**差し替えた `/api/*`**での確認であることを報告に明記する。⭐ **実ログインでの確認はユーザーの実物確認（§5）で行う** |

---

## 4. 不具合2：左ペインの🔍検索が押せない（単位2）

### 4-1. 何が起きているか（仕組み）

1200px以上の左ペイン（`#search-top`）は **`position: sticky; top: var(--editor-bar-h)`** で、高さの上限が

```css
max-height: calc(100vh - var(--editor-bar-h, 57px));
```

になっている（`tools/deck-builder/style.css` の `@media (min-width: 1200px)`）。
⚠️ **この式は「ペインが上端に貼り付いている（＝上端が `--editor-bar-h`）」ことを前提にしている。**
**ページ最上部ではペインの上にサイトヘッダ（約121px）がある**ので、**ペインの下端がそのぶん画面の下へはみ出し**、
**下端に貼ってある リセット／🔍検索 の行（F-a）も一緒に画面外に出る**。

- ⭐ **ここまでは既知**（`左ペイン化_設計.md` §12-9 の **1-2**。2026-09-10 の実物確認でユーザーが **(A) このまま**を選んだ）。
  ⚠️ **ただしその判断の前提は「少しでも下へスクロールすれば以後は常に見えます」だった。**
- ⚠️ ⭐ **新しく分かったこと：検索結果ダイアログが開いていると、ページを下へ送れない。**
  `openModal()` が **`document.body.style.overflow = "hidden"`** にするため、**ホイールがページに伝わらない**
  （ペイン上でホイール → ペインだけが最下端まで動いて止まる。結果ダイアログ上でホイール → ダイアログの中だけが動く）。
  ⭐ **ユーザーの「カードを検索してから」がこの状態**で、**1-2 の前提が崩れて、ボタンに届く手段が無くなる**

### 4-2. 再現（実測・1440×900）

**手順**: デッキを開いた直後（ページ最上部・scrollY=0）→ カード名に `fire` → **🔍検索**（結果ダイアログが開く）→ 左ペインの **エレメント** を開く

| 時点 | ペイン（表示／中身） | 🔍検索の位置（上〜下） | 画面の高さ | `elementFromPoint(ボタン中心)` |
|---|---|---|---|---|
| 検索の直前 | 714 / 714（スクロール無し） | 838〜880 | 900 | ⭐ `#s-search`（押せる） |
| 検索後（ダイアログ開） | 714 / 714 | 838〜880 | 900 | `#s-search`（押せる） |
| ⚠️ **エレメントを開いた後** | **843 / 914（ペインにスクロールが出る）** | ⚠️ **967〜1009** | 900 | ⚠️ **`null`（画面外）** |
| そのままペイン上・ダイアログ上でホイール | 843 / 914（ペインは 71px 動く） | ⚠️ **967〜1009（scrollY は 0 のまま）** | 900 | ⚠️ `null` |

⚠️ **0.1 ではこれをユーザーの報告と同じ現象だと考えたが、0.2 で「ユーザーは最上部にいなかった」と分かった**（§4-3）。
⭐ **この現象（2a）は実在し、案1で直す**。**ユーザーが実際に踏んだのは別の症状（2b）**。

**画面サイズ・ページ位置ごと（エレメントを開いた状態・結果ダイアログ開）**:

| 画面 | scrollY=0 | 60 | 121 | 400 |
|---|---|---|---|---|
| 1200×800 | ⚠️ 画面外（867〜909） | ⚠️ 画面外（807〜849） | 押せる（746〜788） | 押せる |
| 1280×720 | ⚠️ 画面外（787〜829） | ⚠️ 画面外 | 押せる（666〜708） | 押せる |
| 1536×730（Windows 125% 相当） | ⚠️ 画面外（797〜839） | ⚠️ 画面外 | 押せる（676〜718） | 押せる |
| 1920×950 | ⚠️ 画面外（1017〜1059） | ⚠️ 画面外 | 押せる（896〜938） | 押せる |

- ⭐ **はみ出す量は常に「121 − scrollY」px**（サイトヘッダの高さ。デッキ名が長く `.editor-bar` が2行でも同じ）
- ⭐ **ダイアログが閉じていても同じ位置に出る**（違いは「ページを送れるかどうか」だけ）
- ⭐ **トップページ（`/`）には起きない**——トップの 1200px以上は `body.page-top { height: 100vh; overflow: hidden }` のアプリシェルで、
  **ペインが常に画面の高さちょうど**に収まる（構造が違う）。⭐ **不具合2はデッキ構築だけの問題**

### 4-3. ⭐ ユーザーの回答（2026-09-14）とスクリーンショット — **症状は2つに分かれる**

**0.1 の Q1 への回答**（リード経由・原文）:

- 検索結果ダイアログは → 「開いていなかった。」／追って「**ダイアログは閉じていても開いていても再現されるようです**」＝⭐ **ダイアログの開閉は再現条件ではない**
- ページは一番上だったか → 「**ではなかった**。キャプチャが参考になるとおもいます。」
- 何も無いところは → 「**添付の左下の赤枠あたりをクリックしたら検索ができた**」

**スクリーンショット（約 1652×934・リポジトリには無い。リードが読み取った値。端末の拡大率は不明）**:

| 位置（y） | 描画されているもの |
|---|---|
| 0〜 | **編集バー（← 一覧／デッキ名…）が上端**＝⭐ **ページは最上部ではなく、ペインは上端に貼り付いた状態** |
| 左ペイン上部〜 | 選択中の条件（風・レガリア・テイマー）→ 各群 → **レアリティ群が開いて内側スクロールあり** → フォーマット → エキスパンション（**途中で切れている**） |
| **≈755〜795** | ⚠️ **リセット／🔍検索 の行** |
| **≈812〜850** | ⚠️ **名前順／▲昇順 の行**（＝**ボタン行の「下」に並び替えが描かれている**） |
| ≈855〜934 | ⚠️ **何も描かれていない**。ペインの縦スクロールバーは上端〜下端（934）まで |
| **赤枠 ≈862〜925** | ⭐ **ここを押すと検索が走った**（描かれた🔍検索の **約110〜130px 下**） |

⭐ **この配置は、レイアウト上の正しい位置と照合すると説明がつく**:

- DOM の並びは **…エキスパンション → 並び替え → リセット／🔍検索（最後の子）**。ペインを**最下端まで**送ると、ボタン行は**自然位置＝ペインの下端**（934 − 67 ≈ **867〜934**、🔍検索は **≈880〜922**）に来て、**並び替えはその直上**（≈812〜850）に来る
- ⭐ **赤枠 862〜925 は、このレイアウト上のボタン行の位置とほぼ一致する**（＝**当たり判定は正しい位置にある**）
- ⚠️ **ボタン行の「描画」だけが約120px 上（≈743〜807）にずれている**。そのためエキスパンションの下半分を覆い、**本来の位置（855〜934）は背景だけが見える**
- ⭐ **ペインの他の中身（並び替え・エキスパンション等）は正しい位置に描かれている**＝**ずれているのは sticky なボタン行だけ**

⭐ **結論: 不具合2は2つの症状の重なり**

| | 症状 | 条件 | 再現 |
|---|---|---|---|
| **2a** | ボタン行が**画面の外**に出る | ページが上端から 0〜120px の位置（ヘッダが見えている） | ⭐ **ヘッドレスで再現**（§4-2） |
| **2b** | ボタン行の**描画だけが約120px 上にずれ**、**当たり判定は正しい位置**に残る | ⭐ **ユーザーの実機**（ページは貼り付いた位置・ペインにスクロール・ダイアログの開閉は無関係） | ⚠️ **ヘッドレスでは再現せず**（下記） |

### 4-3-1. ⚠️ 2b をヘッドレスで再現しようとした記録（再現せず）

**条件はユーザーに合わせた**: 1652×934・ダイアログ閉・**ホイールでページを 300〜700px 送って編集バーを貼り付かせる**・レアリティ群を開く（内側スクロールあり）・チップを2つ選んで「選択中の条件」を出す・**ペインをホイールで最下端まで**（そこからページへ連鎖）・ページを戻す・ペインを戻す。

**測り方**: ボタン行・並び替え行・編集バーに CSSOM で目印の色を付け、**1.5秒待ってから**スクリーンショットの画素で描画位置を取り、`getBoundingClientRect()` と比べた（＝**要素ごとに「描画 − レイアウト」を出す**）。あわせて `elementFromPoint(ボタン中心)` と実クリックで検索リクエストが出るかを見た。

| ブラウザ | `deviceScaleFactor` | 追加の起動フラグ | ボタン行の描画 − レイアウト |
|---|---|---|---|
| `chromium_headless_shell` | 1 / 1.25 / 1.5 | — | **0**（全手順） |
| 新ヘッドレス（`channel: "chromium"`） | 1 / 1.25 / 1.5 / **0.9 / 0.8 / 1.1**（ブラウザ拡大率の代用） | — | **0〜1**（全手順） |
| 新ヘッドレス | 1.25 | `--enable-gpu-rasterization --ignore-gpu-blocklist --use-angle=swiftshader --enable-unsafe-swiftshader --enable-prefer-compositing-to-lcd-text` | **0**（全手順） |

- ⚠️ **待たずに撮ると、ホイールの慣性スクロール中の1枚でページ全体が一様に 20〜30px ずれて写る**ことがあった（編集バーも同じだけずれる）。**ボタン行だけがずれる 2b とは別物**（1.5秒待つと 0 になる）
- ⚠️ **ヘッドレスで見られないもの**: **実機の GPU 合成**・**ヘッド付きの描画**・**Windows の表示スケール／ブラウザ拡大率そのもの**（DSF で代用しただけ）・**ユーザーのブラウザの種類とバージョン**（Chrome と仮定。未確認）

### 4-3-2. 2b の原因の見立て（⚠️ **推定**・実機で未確認）

⭐ **デッキ構築の左ペインにだけある構造**が1つある:

```text
ページ（ルートのスクロール）
 └ #search-top        … position: sticky（ページに対して上端に貼り付く）＋ overflow-y: auto（自分もスクロールする）
    └ .row-actions    … position: sticky; bottom: 0（ペインのスクロールに対して下端に貼り付く）  ← F-a
```

**「sticky の中のスクロール領域の中の sticky」**。ブラウザは sticky をスクロールと同じ合成スレッドで動かすので、
**2段の sticky が別々のスクローラに対して貼り付く**この形は、**描画側の位置計算だけが食い違う**余地がある。

**見立ての根拠（どれも状況証拠）**:

1. ⭐ **ずれているのは sticky なボタン行だけ**で、同じペインの非 sticky な中身は正しく描かれている
2. ⭐ **ずれの量（約110〜130px）が、ペイン自身の sticky のずれ幅（ヘッダの高さ 121px）と同じ桁**
3. ⭐ **トップページのリセットのバー（F-b）は同じ `sticky; bottom: 0` なのに報告が無い**——トップの `.controls` は **`position: static`**（高さ固定のグリッドの中でスクロールするだけ）で、**2段目の sticky が無い**
4. ⭐ **当たり判定（メインスレッドのレイアウト）は正しい**＝レイアウトの誤りではなく**描画側だけ**の問題

⚠️ **ブラウザ内部の不具合を断定はしない。** ⭐ **ただし、この構造を無くせば「2段の sticky」という前提そのものが消える**ので、原因がこの見立てで合っていれば直る。
**見立てが外れていたら直らない**——⚠️ **確かめる手段はユーザーの実機しかない**（§5 の実物確認 2）。

### 4-4. 直し方（⭐ **案1で決定**＋ **2b のための構造変更 F1**）

**Q2 の回答: 案1（推奨）**（2026-09-14 ユーザー「推奨案でおねがいします」）。
⚠️ ⭐ **これは 2026-09-10 の実物確認 1-2「(A) このまま」を上書きする判断**
（1-2 の前提「少しでも下へスクロールすれば以後は常に見えます」は、**結果ダイアログがページのスクロールを止める間は成り立たなかった**＝§4-1）。

| 変更 | 直す症状 | 見た目 |
|---|---|---|
| **案1** — ペインの `max-height` を**実際の上端**から計算する | **2a** | ⭐ ページ最上部でもボタン行が画面の下端に見える。下へ送るとヘッダが消えたぶんペインが伸びる |
| ⭐ **F1** — **ボタン行を sticky にするのをやめ、ペインの中身だけを内側の箱でスクロールさせる** | **2b**（見立てが正しければ） | ⭐ **変わらない**（ボタン行は常にペインの下端）。⚠️ **唯一の差: ペインの縦スクロールバーがボタン行の上で終わる**（現状はボタン行の裏まで伸びている） |

⭐ **F1 はユーザー判断が要らない**——**見た目を変えずに、構造だけを「sticky の中の sticky」でなくする**変更で、選択肢の比較ではない。
⚠️ **ただし「2b が直る」ことはヘッドレスで示せない**ので、**§5 の実物確認 2 が F1 の唯一の合否判定**になる。

⭐ **2案とも同じ `@media (min-width: 1200px)` の同じ規則を触るので、1つの実装単位（単位2）にまとめる。**

**案1 と F1 を仮に入れた実測（CSSOM と DOM 操作で試作・1652×934・サブタイプ157種をすべて表示・ボタン行の上〜下、`✓`＝`elementFromPoint` が `#s-search`）**:

| ページ位置 | 現状（ペインのスクロール 0／中間／最下端） | 案1＋F1（同） | 実クリックで検索 |
|---|---|---|---|
| scrollY=0 | ⚠️ 988〜1055✗ / 988〜1055✗ / 988〜1055✗ | ⭐ **866〜933✓ / 866〜933✓ / 866〜933✓** | 現状 0本 → **1本** |
| scrollY=60 | ⚠️ 928〜995✗ ×3 | ⭐ **866〜933✓ ×3** | 0本 → **1本** |
| scrollY=400 | 867〜934✓ ×3 | 867〜934✓ ×3（**変わらない**） | 1本 → 1本 |

- ⭐ **F1 でボタン行の `position` は `static`**。3点のスクロール位置で `top` が一致する（＝**F-a の V32 の性質は F1 でも成り立つ**。仕組みが sticky から「スクロールしない位置に置く」へ変わるだけ）
- ⭐ **1199px以下では F1 の外枠が `display: contents` になり、変更前と矩形が1pxも変わらない**（1199×800・900×800・375×812 で `#search-top`・`#s-name`・`#s-text`・`#s-toggle`・`#s-filter-row`・`#s-sort`・`#s-order`・`#s-reset`・`#s-search` の矩形を、**絞り込みトグルの閉・開の両方**で比較して一致）
- 案1 単独の実測（0.1 の表）は §4-4-1 に残す

#### 4-4-1. 案1 単独の実測（0.1・サブタイプ157種をすべて表示）

| 画面 | scrollY=0 | 60 | 400 |
|---|---|---|---|
| 1440×900 | 現状 ⚠️ 954〜1021（画面外）→ **案1 832〜899**（押せる） | 現状 ⚠️ 894〜961 → **案1 832〜899** | 現状 833〜900 → **案1 833〜900**（変わらない） |
| 1536×730 | 現状 ⚠️ 784〜851 → **案1 662〜729** | 現状 ⚠️ 724〜791 → **案1 662〜729** | 663〜730 → 663〜730 |
| 1200×800 | 現状 ⚠️ 854〜921 → **案1 732〜799** | 現状 ⚠️ 794〜861 → **案1 732〜799** | 733〜800 → 733〜800 |
| 1920×1080 | 現状 ⚠️ 1134〜1201 → **案1 1012〜1079** | 現状 ⚠️ 1074〜1141 → **案1 1012〜1079** | 1013〜1080 → 1013〜1080 |
| 1200×800・**最長デッキ名**（`.editor-bar` 102px） | 現状 ⚠️ 854〜921 → **案1 733〜800** | → **案1 733〜800** | 733〜800 → 733〜800 |

- ⭐ **ページが短いデッキ（カード0枚）でも、スクロールで高さが揺れ続けることは無かった**（ドキュメントの高さ 1258 で不変）
- 採らなかった案（0.1 で提示）: **案2**（ダイアログ中もページのスクロールを止めない＝⚠️ ダイアログ閉でも再現するので**そもそも直らない**）／**案3**（ペインを常にヘッダぶん短くする＝ページを送っても伸びない）

### 4-5. ✅ ユーザーに聞いたこと（回答済み）

- **Q1（症状の確認）** → §4-3 のとおり回答済み（ダイアログは無関係・ページは最上部ではない・赤枠の位置）
- **Q2（直し方）** → **案1**

### 4-6. モックの開き方（0.1 の案比較用・⭐ 残す）

```bash
PORT=3000 npm run dev    # ⚠️ 3000 以外はコンテナ外のブラウザに届かない
```

→ <http://localhost:3000/docs/design/未採番-デッキ構築の操作不具合2件/モック_ペインの高さ_2026-09-14.html>

- **左＝現状／右＝選んだ案**。どちらも**実物**（`/tools/deck-builder/` を srcdoc＋スタブで開き、案は CSSOM と iframe 内のリスナーで足している）
- ⚠️ **このモックは案1 だけを試せる（F1 は入っていない）**。F1 は見た目が変わらないのでモックを作らない
- ⚠️ **スタブの保存系は握りつぶすので、モックで＋を押してもデッキは増えない**
- ⭐ スタブは `../未採番-デッキ構築ツールUI刷新/モック_左ペイン化_2026-09-05.stub.js` を共用（⚠️ **消さないこと**）

### 4-7. 変更（単位2 = 案1 ＋ F1）

#### (1) `tools/deck-builder/index.html` — ⭐ **外枠を1つ足す（F1）**

`#search-top` の子のうち、**`.row-actions` 以外の5つ**（`.row`（カード名・効果テキスト）／`#s-toggle`／`#s-selected-filters`／`#s-filter-row`／`.result-sort`）を
**`<div class="pane-scroll">` で包む**。**`.row-actions` は `#search-top` の直下・最後の子のまま**:

```html
<div class="search-top" id="search-top">
  <!-- ⭐ 1200px以上ではこの箱だけがスクロールし、下の .row-actions はスクロールしない位置に置く
       （操作不具合2件_設計 §4-7）。1199px以下は display: contents で「無いのと同じ」。
       ⚠️ 絞り込みの要素を足すときはこの箱の中に足すこと。外（.row-actions の前後）に置くと、
          1200px以上でスクロールしない帯に入ってペインの高さを食う。
       ⚠️ .row-actions を position: sticky に戻さないこと。「sticky なペインの中のスクロール領域の中の sticky」になり、
          実機でボタン行の描画だけがずれて、見えている🔍検索が押せなくなった（同 §4-3）。 -->
  <div class="pane-scroll">
    <div class="row"> …カード名・効果テキスト… </div>
    <button id="s-toggle" …>…</button>
    <div id="s-selected-filters"> … </div>
    <div class="row filter-row" id="s-filter-row"> … </div>
    <div class="result-sort"> … </div>
  </div>
  <div class="row row-actions"> …リセット／🔍検索… </div>
</div>
```

- ⚠️ **既存のコメント（`.row-actions` の直前の「row-actions クラスは必須…」）は残す**。
- ⚠️ **`id` は付けない**（JS から参照しない。付けると `el` に足したくなる）

#### (2) `tools/deck-builder/style.css`

**(2-a) 基本（`@media` の外）** — `.search-top .row { … }` の直後に1行:

```css
/* 1199px以下では外枠が無いのと同じにする（操作不具合2件_設計 §4-7）。
   ⚠️ 消すと .search-top の gap が子の間に効かなくなり、スマホ表示の並びが詰まる。 */
.search-top > .pane-scroll { display: contents; }
```

⚠️ **置き場所は `MOBILE-BAND:START` マーカーより前**（`validate` 検査③: マーカーの `*/` と `@media (max-width: 1199px) {` の間には何も置けない）。

**(2-b) `@media (min-width: 1200px)` の中** — 既存の2つの規則を書き換え、1つ足す:

```css
#view-editor #search-top {
  grid-column: 1; grid-row: 2 / span 50; align-self: start;
  position: sticky; top: var(--editor-bar-h, 57px);
  /* ⭐ 案1: 上端はペインの「実際の」上端（--pane-top）。ページ最上部ではサイトヘッダの下にあるので、
     --editor-bar-h で引くとヘッダぶん（約121px）下へはみ出し、下端のボタン行が画面外に出る
     （操作不具合2件_設計 §4）。--pane-top は app.js の updatePaneTop() が scroll/resize で更新する。
     未設定のときは従来の式に戻る（fail-open）。 */
  max-height: calc(100vh - var(--pane-top, var(--editor-bar-h, 57px)));
  /* ⭐ F1: ペイン自体はスクロールしない縦並びの箱。スクロールするのは中の .pane-scroll だけ */
  display: flex; flex-direction: column;
  overflow: hidden;
  padding: 0; gap: 0;
  border-bottom: none; border-right: 1px solid var(--border);
}
/* ⭐ F1: 絞り込み一式だけがスクロールする。padding/gap は旧 #search-top の値をここへ移した
   （下 12px は旧 .row-actions の上の gap のぶん） */
#search-top > .pane-scroll {
  display: flex; flex-direction: column; gap: 12px;
  flex: 1 1 auto; min-height: 0;
  overflow-y: auto;
  padding: 14px 16px 12px;
}
#search-top > .row-actions {
  flex-direction: row;
  flex: none;
  /* ⚠️ sticky にしない（F1・§4-3）。スクロールしない箱の最後に置くだけで常にペインの下端に来る */
  margin: 0;
  padding: 12px 16px;
  background: var(--panel);   /* ⚠️ 不透明のまま（§12-7 の1。ペインの地色と揃える） */
  border-top: 1px solid var(--border);
}
```

- ⚠️ **消すもの**: 旧 `#view-editor #search-top` の **`overflow-y: auto`**・**`padding: 14px 16px 0; gap: 12px`**・**`scroll-padding-bottom: 5rem`** と、旧 `.row-actions` の **`position: sticky; bottom: 0; z-index: 3; margin: 0 -16px`**。**関連するコメントも F1 に合わせて書き直す**（「最後の子に bottom:0 の sticky を置くと…」の段落は**消して**上のコメントに置き換える。⚠️ **古い仕組みの説明を残すと、次の人が sticky に戻す**）
- ⭐ **`scroll-padding-bottom` を `.pane-scroll` に移さない**——ボタン行はもうスクロール領域に**重ならない**ので、Tab で辿ったチップが隠れる余地が無い（左ペイン化_設計 §12-7 の4 の「保険」は F1 で不要になる）
- ⚠️ **`@media` を新しく足さない**（検査②: `min-width` の集合 ＝ `PANE_MIN_WIDTH`）
- ⚠️ **`#search-top .fgroup .chips { max-height: 200px }` 等のペイン内の規則はそのまま**（子孫セレクタなので外枠を足しても当たる）

#### (3) `tools/deck-builder/app.js` — **案1 の JS**（`updateEditorBarH()` の近くに関数を1つ）

```js
// 左ペインの実際の上端を --pane-top に入れる（操作不具合2件_設計 §4-7 案1）。
// ⚠️ 値は「sticky の top（編集バーの高さ）」と「実際の上端」の大きい方。ページ最上部ではヘッダの下にある。
// ⚠️ 1200px未満（スマホ表示）とエディタ非表示のときは何もしない（CSS 側も min-width の中でしか読まない）。
// ⚠️ Math.ceil で切り上げる。四捨五入だとペインの下端が画面から 1px 未満はみ出すことがある。
function updatePaneTop() {
  const pane = el.searchTop;
  if (!pane || !pane.getClientRects().length) return;
  if (!matchMedia(`(min-width: ${PANE_MIN_WIDTH}px)`).matches) return;
  const bar = document.querySelector("#view-editor .editor-bar");
  const barH = bar ? Math.round(bar.getBoundingClientRect().height) : 0;
  const top = Math.max(barH, Math.ceil(pane.getBoundingClientRect().top));
  document.documentElement.style.setProperty("--pane-top", `${top}px`);
}
```

- **呼ぶ場所**: ①`window` の **`scroll`**（`passive: true`・**rAF で1フレーム1回に間引く**。既存の `resize` の rAF と同じ書き方）②既存の **`resize` の rAF の中**（`updateEditorBarH()` の直後）③**`renderEditorBar()` の末尾**（`updateEditorBarH()` の直後。バーの高さが変わると sticky の上端が変わるため）
- ⚠️ **`PANE_MIN_WIDTH` を使う**（1200 を直書きしない）
- ⚠️ **コメントに `<file>.js:<行番号>` を書かない**（`validate` が exit 1。関数名で参照する）
- ⚠️ **`openModal()` の `body.style.overflow` のロックは変えない**（案2 は採らない）

### 4-8. 検証項目（単位2）

⚠️ **「変更前」は単位1 の実装コミット**（単位2 の直前）。§3-4 と同じく `git archive` した別ツリーを別ポートで起動する。

| # | 何を | 期待値 |
|---|---|---|
| **V9** | ⭐ **2a が直ったこと**（1440×900・ページ最上部） | 🔍検索（fire）→ エレメントを開いた後、**🔍検索の下端 ≤ 900**（変更前は **967〜1009**）・**`elementFromPoint(中心)` が `#s-search`**・**実クリックで `api.gatcg.com/cards/search` のリクエストが1本出る**。⭐ **結果ダイアログを開いた状態・閉じた状態の両方**で |
| **V10** | ⭐ **ページ位置によらず下端にあること** | サブタイプ157種を「すべて表示」で、**scrollY = 0 / 30 / 60 / 90 / 121 / 400** の6点 × **`.pane-scroll` のスクロール 0／中間／最下端**の3点で、**ボタン行の下端が `innerHeight − 1` 〜 `innerHeight`**・**`top` が3点で一致**・**全点で押せる**（1652×934 なら §4-4 の表の「案1＋F1」の値） |
| **V11** | 画面サイズ | **1200×800 / 1280×720 / 1536×730 / 1920×1080** と **1200×800・最長デッキ名**（`.editor-bar` 102px）で V10 と同じ（ボタン行の下端 ＝ 画面の下端） |
| **V12** | ⭐ **F1 の構造**（2b の見立てを満たしていること） | 1200px以上で、**①`getComputedStyle(.row-actions).position === "static"` ②`getComputedStyle(#search-top).overflowY === "hidden"` ③`#search-top` の子孫で `position: sticky` の要素が0件**（⭐ `#search-top` の中を `querySelectorAll("*")` で全走査）**④スクロールするのは `.pane-scroll`（`scrollHeight > clientHeight` のとき `scrollTop` を動かすと中身が動き、ボタン行は動かない）** |
| **V13** | ⭐ **スクリーンショットの配置が起きないこと** | ユーザーの状態を再現する——**1652×934・ページを 300px 送る・選択中の条件を3つ・レアリティ群を開く・`.pane-scroll` を最下端へ**。**並び替え行の下端 < ボタン行の上端**（＝**ボタン行が並び替えの下にある**）・**ボタン行の下端 ＝ 934**。⭐ CSSOM で**ボタン行と並び替え行に目印の色**を付けて、**スクリーンショットの画素でも同じ順序**であること（1.5秒待ってから撮る）。⚠️ **ヘッドレスでは変更前も同じ結果になる**（2b はヘッドレスで再現しない＝§4-3-1）ので、**これは「F1 で壊していない」確認であって「2b が直った」証明ではない**と報告に書くこと |
| **V14** | ⚠️ ⭐ **1199px以下で何も変わらないこと** | **1199×800 / 900×800 / 375×812** で、`#search-top`・`#s-name`・`#s-text`・`#s-toggle`・`#s-filter-row`・`#s-sort`・`#s-order`・`#s-reset`・`#s-search` の `getBoundingClientRect()`（left/top/width/height を整数に丸めたもの）が、**絞り込みトグルの閉・開の両方で変更前と一致**。⭐ **`--pane-top` が設定されない**（1199px以下で読み込んだ場合 `document.documentElement.style.getPropertyValue("--pane-top") === ""`） |
| **V15** | ⭐ **破壊試験（案1）** | 変更後のページで、CSSOM で `@media (min-width: 1200px) { #view-editor #search-top { max-height: calc(100vh - var(--editor-bar-h, 57px)) } }` をシート末尾に `insertRule` → V9 の手順で **🔍検索が画面外（1440×900 で 967〜1009 付近）に戻り押せなくなる**。ルールを消すと V9 の値に戻る |
| **V16** | ⭐ **破壊試験（F1 の外枠）** | CSSOM で `.search-top > .pane-scroll { display: block !important; overflow: visible !important }` を足す → **サブタイプ157種を「すべて表示」の状態で、1200px以上で V10 が崩れる**（ボタン行がペインの外へ押し出され、ペインの `overflow: hidden` で見えなくなる＝**`elementFromPoint` が `#s-search` を返さない**）。⭐ **1199px以下で `display: contents` の行だけを消す**（`display: block` を `insertRule`）→ **V14 の矩形が変わる**（`.search-top` の gap が子の間に効かなくなる）。⚠️ **2つとも「壊したら赤くなる」ことで、その行が効いていることを示す** |
| **V17** | 幅の往復 | **1440 → 1199 → 1440** と `setViewportSize` で往復して、1440 に戻ったとき **V10 の値**（`--pane-top` が古い値のまま残って高さがずれていない） |
| **V18** | ⭐ **左ペイン化の既存項目の再実施** | **V33**（結果ダイアログを開いたままボタン行が押せる）・**V34**（ボタン行の背景アルファ 1）・**V39**（Tab でサブタイプのチップを末尾まで辿っても**フォーカスされたチップがボタン行に隠れない**・ボタン行のボタンに Tab で到達できる）・**V40**（`.editor-bar` の sticky＝上端 0 が変わらず、上下のバーが重ならない） |
| **V19** | ページが短いデッキ | カード0枚のデッキでエレメント／サブタイプを開き、ページ上でホイールを上下 → **ドキュメントの高さが変わらない**・**スクロールを止めたあと `--pane-top` の書き込みが止まる**（rAF が回り続けない） |

---

## 5. 共通の検証・段取り

| # | 何を | 期待値 |
|---|---|---|
| **V20** | `npm run validate` | **exit 0**。⭐ UI規約の3本が緑のまま——`onRemoveOne wiring in sync — 渡しているのは 1ファイル（tools/deck-builder/app.js）…`／`pane threshold in sync — 2ページ（トップ 1200px / デッキ構築 1200px）`／`mobile band marker in sync — 1組（style.css 1199px ↔ app.js MOBILE_MQ 1199px）`。`no line-number refs in code comments — 0件` |
| **V21** | ⭐ **トップページで退行しないこと** | `git -c core.quotepath=false diff --name-only <設計完了のコミット>..HEAD -- . ':(exclude)docs/*'` が **`tools/deck-builder/app.js`・`tools/deck-builder/style.css`・`tools/deck-builder/index.html` の3つだけ**（⚠️ **ルートの `index.html` / `style.css` / `app.js` と `shared/` を触っていない**）。念のため **トップ 1440×900 でサブタイプ157種を「すべて表示」→ リセットのバー（`#reset-pane`）が `.controls` の3点のスクロール位置で同じ `top`・押せる**（左ペイン化 V32 の再実施） |
| **V22** | ⚠️ **トップのスマホ帯の3対策**（CLAUDE.md「環境の注意」） | 触っていないことを V21 の差分で確認したうえで、**トップ 375×812 でページを下へ送ると絞り込みFABが出る**（`is-hidden` が外れ `elementFromPoint` が FAB を返す）・**FAB を押すとシートの下端が画面の下端（812）** |
| **V23** | デッキ構築のスマホ表示 | **1199×800 と 375×812** で、絞り込みトグルの開閉・**不具合1の Δ0**（V3）が両立する |

### ⭐ ユーザーの実物確認（レビュー合格後・承認の前）

⚠️ **UIの変更なので、承認の前にユーザーが3000番で実物を操作する工程が入る**（2026-09-02 のユーザー指示）。
⚠️ **3000番で上げる**（3100/3300 はコンテナ外のブラウザに届かない）。**実ログインでデッキを開いて**見る:

| # | 何をする | 見るところ |
|---|---|---|
| 1 | デッキを下へ送り、メインデッキ・サイドボードのカードで **＋／−／枚数入力** | ⭐ **画面が上へ飛ばない**（不具合1）。⭐ **実ログイン＝本物の保存経路**での確認はここだけ（V8） |
| 2 | ⭐ **スクリーンショットと同じ操作**: ページを少し下へ送る → 条件をいくつか選ぶ → レアリティ等の群を開く → ペインをいちばん下まで送る → 🔍検索を押す | ⭐ **🔍検索が「並び替え」の下・ペインの最下端に描かれ、描かれた位置を押すと検索される**。⚠️ ⭐ **2b（描画だけがずれる）が直ったかは、ここでしか確かめられない**（§4-3-1）。⚠️ **直っていなければ承認せず設計に戻す**（見立て §4-3-2 が外れていたことになる） |
| 3 | ページ最上部で 🔍検索 → エレメントを開く | ⭐ **🔍検索が画面の下端に見えていて押せる**（2a・案1） |
| 4 | ページを上下にゆっくり・速く送る | ⚠️ **ペインの下端のボタン行がちらつかない／遅れて付いてこない**（案1 はスクロールのたびにペインの高さを変える） |
| 5 | ペインの縦スクロールバーを見る | ⭐ **ボタン行の上で終わる**（現状はボタン行の裏まで伸びている＝F1 の唯一の見た目の差）。気になるかどうか |

---

## 6. 段取りと状態

- ⭐ **設計完了（0.2・2026-09-14）**。起票案の状態は **`実装中`**
- 実装の順序: **単位1 → 単位2**（**別コミット**。単位1は `app.js` だけ、単位2は `index.html`＋`style.css`＋`app.js`）。
  ⭐ **単位1 のレビューを待たずに単位2 へ進んでよい**が、**報告は単位ごとに分ける**（V1〜V8 と V9〜V19 が別の「変更前」を使うため）
- ⚠️ **ユーザーの実物確認（§5）で 2b が直っていなければ、承認せずに設計担当へ戻す**（F1 の見立てが外れていたことになる）

---

## 7. 申し送り（このタスクでは直さない）

| # | 内容 | 扱い |
|---|---|---|
| 1 | ＋/−を押すとフォーカスが `BODY` に落ちる（ボタンが作り直されるため）。**キーボードで続けて押せない** | 変更前からの挙動。⚠️ **起票はユーザーが困っていると言ったとき**（本人が症状を確認していない起票案を増やさない）。直すなら `focus({ preventScroll: true })`（§3-3） |
| 2 | `renderZones()` は枚数1つの変更でも**全ゾーンのタイルを作り直す** | 性能上の問題は観測していない。⚠️ **将来ここを差分更新にするときも、§3-3 のガード（空振りの並べ直しをしない）を落とさないこと** |
| 3 | 左ペイン化_設計 §12-9 の **1-2（初期表示でボタン行が隠れる）** | ⭐ **案1 の決定で「(A) このまま」は上書きされた**。⚠️ **承認時に `左ペイン化_設計.md` 側（§12-3 の F-a・§12-7 の4 の `scroll-padding-bottom`・§12-9 の 1-2）へ「操作不具合2件で F1 に置き換えた」注記を足す**（⚠️ **足さないと、次に読む人が F-a の sticky を正と読んで戻す**） |
| 4 | **2b の見立て（§4-3-2）が外れていた場合** | ⚠️ ユーザーの実物確認 2 で直っていなければ、**ユーザーのブラウザの種類・バージョン・拡大率**を聞き、画面録画をもらう |
