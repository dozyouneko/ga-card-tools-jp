**対象: 「デッキ構築の操作不具合2件」**（→ 完了）

> ⚠️ **これは issueコメントの代替**（GitHubアカウント停止中で `gh` が403）。GitHub復旧後に
> `gh issue comment <n> --body-file docs/design/未採番-デッキ構築の操作不具合2件/承認_2026-09-14.md` で投函する。
> ⚠️ **このタスクはまだ起票されていない**ので、投函の前に起票案から `gh issue create` する。

# 最終承認 — 「デッキ構築の操作不具合2件」（2026-09-14）

- ⭐ **承認したのはユーザー**（2026-09-14・リードセッション経由）。リードの問い
  「承認して完了にしますか？（対象: ＋／−で画面が飛ぶ修正・🔍検索が押せない修正・Tabでチップが切れる修正・スクロール時の描画崩れの修正。2dは別の起票案で保留中。公開は別途判断）」に **「承認する」**。
  本記録はその判断を、設計担当が根拠つきで書き留めたもの
- 設計書: [`操作不具合2件_設計.md`](操作不具合2件_設計.md)（本コミットで **0.7**）
- 起票案: [`起票案_2026-09-14_デッキ構築の操作不具合2件.md`](起票案_2026-09-14_デッキ構築の操作不具合2件.md)（本コミットで状態を **`完了`** に）
- 実装: **`3f317196`**（単位1・不具合1）／ **`8e5f413a`**（単位2・案1＋F1）／ **`0d3da4e9`**（単位2 追加・scroll-padding）／ **`994de8dc`**（単位3・F2）
- 直前のレビュー判定: [`再判定_2026-09-14.md`](再判定_2026-09-14.md)（**合格**・`7a394ded`）
- ⚠️ **公開はしていない**（別途ユーザー判断。2026-09-17 の大会データ週1更新とあわせる想定）

---

## 1. ユーザーの実物確認（3回分のまとめ）

| 回 | 記録 | 結果 |
|---|---|---|
| 1回目 | [`実物確認結果_2026-09-14.md`](実物確認結果_2026-09-14.md) | 1〜5 合格（⭐ **2b は F1 で直った**）。新症状 **2c**（描画崩れ）→ F2 |
| 2回目 | [`実物確認結果_2回目_2026-09-14.md`](実物確認結果_2回目_2026-09-14.md) | 新症状 **2d**（すべて表示後にスクロールが途中で止まる）→ 本人の再確認で**再現せず・保留** |
| ⭐ **3回目（本記録）** | — | ⭐ **6〜9 すべて問題なし** |

**3回目の回答（原文）**:

- 実物確認: 「**確認６～９は問題なし**」
  （6＝群を開いてスクロールバーを速く動かしても崩れない＝**2c**／7＝ホイール・Tab でのスクロール／8＝左ペインの地色と文字の見え方／9＝1〜5 の軽い再確認。設計書 §5・再判定の「見てほしい点」）
- 表示環境: 「**Windows の表示スケールは1920×1080と3840×2160で確認**」「**Chromeの拡大率は100％**」

⚠️ ⭐ **表示スケールは％ではなく解像度で回答された（スケール％は未確認）。**
**3840×2160 の Windows の既定スケールは 150% のことが多く**、その場合 `devicePixelRatio` は 1.5 で、
**2c の見立て（設計書 §8-3「合成されないのは DPR 1.5 未満のとき」）の前提から外れる**。
⭐ **ただし 1920×1080 の既定は 100%（DPR 1）で、見立ての範囲に入る。**
**どちらの画面で 2c が出ていたかも、それぞれのスケールも分からない**ので、⚠️ **「F2 が 2c を直した」は見立てと矛盾しないが、証明はされていない**と記録する（確認6 が問題なしだったことは事実）。

## 2. 承認の理由

1. ⭐ **ユーザーが報告した2件は、どちらも実物で直ったことを本人が確認した**
   - 不具合1（＋／−で画面が上へ飛ぶ）: 実物確認 1 合格。原因は `renderZones()` の空振りの並べ直し＋フォーカス中要素を基準にしたスクロール補正で、**原因に直接効いていることを V4（`childList` 8件→0件）と V6（破壊試験）で示した**
   - 不具合2（🔍検索が押せない）: **2a**（上端付近で画面外）は案1、**2b**（描画だけのずれ）は F1。実物確認 2〜4 合格
2. ⭐ **途中で見つかった 2c（描画崩れ）も F2 で直り、実物確認 6 合格**。**2c・2b ともヘッドレスでは再現しない**性質だったので、**原因の構造（sticky の子孫・合成スクロールの層）を検証項目にし、合否は実物に委ねる**段取りを最後まで守った
3. ⭐ **1199px以下とトップページで退行していない**（V14・V21・V22・V28。変更は `tools/deck-builder/` の3ファイルだけ）
4. レビューは単位ごとに合格（差し戻し 0往復）。**設計の誤り（D5）は開発担当の報告で見つかり、設計担当が撤回して追加指示で直した**（0.3）

## 3. レビューの推奨指摘（S）と参考の扱い

| 出所 | 内容 | 扱い |
|---|---|---|
| [`レビュー判定_2026-09-14.md`](レビュー判定_2026-09-14.md) **S1** | `style.css` の `#search-top > .pane-scroll` のコメントに「**0.3 で追加（D5 を覆した）**」と**設計書の版番号と判断ID**が入っている（コードだけ読む人には意味が分からず腐る。慣習は「文書名＋節」） | ⭐ **このタスクでは直さない（完了にする）**。**理由**: ①**コメントの残りの本文が理由を自己完結で説明している**（0×0 の `<input>`・切れる量・2rem の根拠）ので、「0.3」「D5」が読めなくても**誤った編集を誘わない** ②直すには実装→レビューの1周が要り、**ユーザーの承認後に動作に関係しない1行のためだけに周を立てる価値が無い**（F-a の S-2 を次の単位に含めた前例と同じ基準だが、今回は次の単位が無い）。⚠️ ⭐ **次にこのブロック（`.pane-scroll` の規則）を触る実装単位で、「操作不具合2件_設計 §4-7 (2-b)」への参照に置き換える**——**その設計書に1行の指示として書くこと**（本記録と設計書 §7 の6 に残した） |
| [`再判定_2026-09-14.md`](再判定_2026-09-14.md) **参考** | ⚠️ **F2 は見た目の変わらない静かな対策で、防いでいるのはコメントだけ**（ブラウザ内部の判断に依存・消しても半透明にしても画面は変わらない）。CLAUDE.md の「スマホ帯の3対策」と同型 | ⭐ **採る**: **`CLAUDE.md`「環境の注意」に、デッキ構築の左ペインの「外すと静かに壊れる対策3つ」（F1・F2・案1）を表で追加した**（スマホ帯の3対策の直後）。⭐ **判定を構造で行う方法**（F1＝`#search-top` の子孫に sticky 0件／F2＝`LayerTree` の `OverflowScrolling`）と、**F2 は将来の Chrome で黙って効かなくなりうる**ことも書いた。⚠️ ⭐ **`CLAUDE.md` は配信物**（`/CLAUDE.md` が 200）なので、**この変更の公開は本タスクの公開と一緒にユーザー判断**。⚠️ **自動検査は入れない**——**CSS の字面の検査では F2 の「効いているか」は分からず**（層の有無はブラウザで測るしかない）、**空回りする検査を増やさない** |
| 同 **申し送り** | 承認時に `左ペイン化_設計.md`（§12-3 F-a・§12-7 の4・§12-9 の 1-2）へ「操作不具合2件で F1 に置き換えた」注記。⭐ F2 も | ⭐ **本コミットで実施**（同書 **1.15**。改版履歴＋§12-3・§12-7 の冒頭に注記、1-2 の行に「上書き済み」）。⚠️ **§12-3 のコードを正として sticky に戻さない・背景を外さない**と明記 |
| レビュー判定 **参考** | テストハーネスで `page.click()` が `scroll-padding-bottom` のぶん余分に送る／1199px以下で `--pane-top` が古い値のまま残る（実害なし） | **対応不要**（どちらも設計どおり） |

## 4. 派生の要否

| # | 内容 | 扱い |
|---|---|---|
| 1 | ⭐ **2d**（サブタイプ「すべて表示」後にペインが途中までしかスクロールできない） | ⭐ **派生起票案 [`派生起票案_2026-09-14_ペインのスクロールが途中で止まる.md`](派生起票案_2026-09-14_ペインのスクロールが途中で止まる.md)（`生きている`）で保留中**。**本タスクの完了で埋もれないよう既に切り出してある**。⚠️ **投函前に必ず再現させる（しなければ取り下げ）** |
| 2 | ＋／−のあとフォーカスが `BODY` に落ちる（キーボードで続けて押せない） | **起票しない**（変更前からの挙動・**本人が困っていると言っていない**）。設計書 §7 の1 |
| 3 | Shift+Tab で上へ辿るとチップの上端が切れうる（未実測） | **起票しない**（変更前から `scroll-padding-top` は無い＝退行ではない）。設計書 §7 の5 |
| 4 | S1 のコメント | **起票しない**（§3。次にそのブロックを触る単位で直す） |
| 5 | ⭐ ローカルの wrangler（3000番）で**実ログインできる経路**ができた（ユーザーが Discord に `http://localhost:3000/api/auth/discord/callback` を登録） | **起票しない**。⚠️ ⭐ **ただし `CLAUDE.md` の「デッキ構築ツールはDiscord OAuthログインが前提のため、ヘッドレスでは検索パネルまで到達できない」は、ユーザーの実物確認については事実でなくなった**。⚠️ **ローカル D1 はプレビューと違い本番 DB ではない**ので、次の UI タスクの実物確認でもこの経路を使える。**`CLAUDE.md` への反映は締め作業で扱う**（本コミットでは書かない＝実ログインの経路の安全性をまだ整理していないため） |

## 5. 公開について

⚠️ **本タスクの成果（`tools/deck-builder/` の3ファイル）と `CLAUDE.md` の追記は未公開**。公開はユーザー判断（経路②の `deploy:prod` は `git archive HEAD` から作るので、**他の未公開タスクと一緒に出る**＝CLAUDE.md「大会データの週1更新」の注意）。
