**対象: 「デッキ構築の操作不具合2件」**

# 設計完了 — デッキ構築の操作不具合2件（2026-09-14）

⚠️ **これは issueコメントの代替**（GitHubアカウント停止中で `gh issue comment` が打てないため、ファイルに残す）。
復旧して採番されたら、本文をそのまま `gh issue comment <n> --body-file docs/design/未採番-デッキ構築の操作不具合2件/設計完了_2026-09-14.md` で投函する。

- 日付: **2026-09-14**
- 設計の起点 HEAD: **`15598b3f`**（`tools/deck-builder/` は本番 `ecb2b51d` と同一）
- 次の担当: ⭐ **開発担当**（ラベル相当: **`実装待ち`**）——**単位1から**

---

## 1. 設計書のパス

- ⭐ **正**: [`操作不具合2件_設計.md`](操作不具合2件_設計.md)（**0.2**）
- 起票案: [`起票案_2026-09-14_デッキ構築の操作不具合2件.md`](起票案_2026-09-14_デッキ構築の操作不具合2件.md)
- モック（案1 の比較用・0.1）: [`モック_ペインの高さ_2026-09-14.html`](モック_ペインの高さ_2026-09-14.html)（⚠️ **F1 は入っていない**）

## 2. 対象範囲

| 単位 | 症状 | 変えるファイル | 設計書 |
|---|---|---|---|
| **単位1** | **デッキ側の＋/−/枚数入力でページが上へ飛ぶ**（1440×900 メインで −474px） | `tools/deck-builder/app.js`（`renderZones()` のみ） | §3 |
| **単位2** | **左ペインの🔍検索が押せない**＝ **2a**（ページ上端付近でボタン行が画面外）＋ **2b**（実機でボタン行の描画だけが約120px 上にずれる） | `tools/deck-builder/index.html`・`style.css`・`app.js` | §4 |

⚠️ **範囲外**: トップページ（`/`・ルートの `index.html` / `style.css` / `app.js`）と `shared/`。**触ったら V21 で不合格**。

## 3. 検証項目の要約（設計書 §3-4・§4-8・§5 が正）

| 区分 | # | 要点 |
|---|---|---|
| 単位1 | **V1〜V3** | ＋／−／枚数入力／キーボードで **Δ0**（変更前はメイン −474・サイド／検討中は 0 まで飛ぶ）。1200/1440×700/1920/1199/375 でも |
| 単位1 | ⭐ **V4** | `#ed-pane-deck` 直下の `childList` の変化が＋1回で **0件**（変更前 **8件**）＝**原因に効いている証拠** |
| 単位1 | ⭐ **V5** | **フォーマット切替では並べ直す**（パンテオン `material, main, pantheon, maybe, side` ↔ スタンダード `material, main, side, maybe`・メモは常に最後） |
| 単位1 | ⭐ **V6** | 破壊試験＝**変更前ツリー（`git archive`）で Δ−474・8件に戻る**。`overflow-anchor` を併用していないこと |
| 単位2 | **V9〜V11** | 2a: ページ最上部でも🔍検索が画面内・押せる（**ダイアログ開・閉の両方**）。scrollY 0〜400 × ペインのスクロール3点で **ボタン行の下端 ＝ 画面の下端** |
| 単位2 | ⭐ **V12** | F1 の構造: ボタン行 `position: static`・`#search-top` は `overflow-y: hidden`・**`#search-top` の子孫に sticky が0件**・スクロールするのは `.pane-scroll` |
| 単位2 | **V13** | ユーザーのスクリーンショットの状態で**並び替え → ボタン行の順**に描かれる。⚠️ **ヘッドレスでは変更前も同じ**なので「壊していない」確認であって「2b が直った」証明ではない、と報告に書く |
| 単位2 | ⭐ **V14** | **1199/900/375px で主要9要素の矩形が変更前と一致**（トグル閉・開）・`--pane-top` が設定されない |
| 単位2 | ⭐ **V15・V16** | 破壊試験: 案1 の `max-height` を旧式に戻すと画面外に戻る／`.pane-scroll` の `display` を壊すと 1200px以上・1199px以下それぞれで崩れる |
| 単位2 | **V17〜V19** | 幅の往復・左ペイン化 V33/V34/V39/V40 の再実施・カード0枚で rAF が止まる |
| 共通 | **V20〜V23** | `npm run validate` exit 0（UI規約3本が緑）／**変更は `tools/deck-builder/` の3ファイルだけ**＋トップの V32 再実施／トップのスマホ帯の3対策／デッキ構築のスマホ表示 |

⭐ **「変更前」は単位ごとに違う**: 単位1 は `15598b3f`、単位2 は**単位1 の実装コミット**。どちらも `git archive` した別ツリーを別ポートで起動する（`serve.mjs` は `cd` では切り替わらない）。

## 4. 判断済み事項（⚠️ 蒸し返さない）

| # | 決定 |
|---|---|
| **D1** | ⭐ **不具合1は「並び順が既に正しいときはゾーンを動かさない」で直す**。`overflow-anchor: none` は**採らない**（原因を残して症状だけ消すため）・**併用もしない**（破壊試験で効き目を示せなくなる） |
| **D2** | ⭐ **不具合2の 2a は案1**（ペインの `max-height` を実際の上端から計算）。**2026-09-14 ユーザー決定**。⚠️ **2026-09-10 の実物確認 1-2「(A) このまま」を上書きする** |
| **D3** | ⭐ **2b には F1**（ボタン行を sticky にせず、中身だけを内側の `.pane-scroll` でスクロールさせる）。**見た目は変えない**ので**ユーザー判断は取っていない**。⚠️ **唯一の見た目の差はペインのスクロールバーがボタン行の上で終わること**（実物確認 5 で見てもらう） |
| **D4** | ⚠️ **案2（ダイアログ中もページのスクロールを止めない）は採らない**——ユーザーによれば**ダイアログ閉でも再現する**ので、そもそも直らない |
| **D5** | ⭐ **`scroll-padding-bottom` は F1 で不要**（ボタン行がスクロール領域に重ならない）。`.pane-scroll` に移さない |
| **D6** | ⭐ **フォーカスを `BODY` に落とす既存挙動（＋のあと）は直さない**（設計書 §7 の1。起票もしない） |

## 5. 未決事項・リスク

| # | 内容 |
|---|---|
| ⚠️ **R1** | ⭐ **2b はヘッドレスで再現していない**（`deviceScaleFactor` 0.8〜1.5・新旧ヘッドレス・GPU系フラグ。設計書 §4-3-1）。原因は**推定**（§4-3-2「sticky の中のスクロール領域の中の sticky」）。⚠️ **F1 で直るかは、レビュー合格後のユーザーの実物確認 2 でしか判定できない**。**直っていなければ承認せず設計担当へ戻す**（そのときはブラウザの種類・バージョン・拡大率を聞く） |
| R2 | 案1 はスクロールのたびにペインの高さを変える。**実機でちらつかないか**は実物確認 4 で見る |

## 6. 段取り

1. 開発担当: **単位1 → 単位2**（別コミット・報告は単位ごと）
2. レビュー担当: 単位ごとに判定
3. ⭐ **ユーザーの実物確認（3000番・実ログイン）**: 設計書 §5 の表の 1〜5。⚠️ **2 が本命**
4. 設計担当: 承認。⚠️ **承認時に `左ペイン化_設計.md`（§12-3 F-a・§12-7 の4・§12-9 の 1-2）へ「操作不具合2件で F1 に置き換えた」注記を足す**（設計書 §7 の3）
