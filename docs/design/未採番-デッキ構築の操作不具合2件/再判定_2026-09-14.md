**対象: 「デッキ構築の操作不具合2件」**

# 再判定（単位3・F2 合成スクロール）— デッキ構築の操作不具合2件（2026-09-14）

⚠️ **これは issueコメントの代替**（GitHubアカウント停止中で `gh issue comment` が打てないため、ファイルに残す）。
復旧して採番されたら、本文をそのまま `gh issue comment <n> --body-file docs/design/未採番-デッキ構築の操作不具合2件/再判定_2026-09-14.md` で投函する。

- レビュー対象: **`994de8dc`**（単位3・F2。`tools/deck-builder/style.css` +9）。変更前の基準は **`0d3da4e9`**（前回の合格 [`レビュー判定_2026-09-14.md`](レビュー判定_2026-09-14.md)＝`29ee6a06` 時点の実装）
- 参照: [`実物確認結果_2026-09-14.md`](実物確認結果_2026-09-14.md)・[`追加指示_合成スクロール_2026-09-14.md`](追加指示_合成スクロール_2026-09-14.md)・[`操作不具合2件_設計.md`](操作不具合2件_設計.md) **0.4 §8**（V25〜V29）・[`実装報告_合成スクロール_2026-09-14.md`](実装報告_合成スクロール_2026-09-14.md)（`2b98f32f`）
- ⚠️ 前回の判定ファイルは書き換えていない。単位1・単位2・scroll-padding の判定は前回のまま
- 次の担当: ⭐ **設計担当**（ラベル相当: **`設計確認待ち`**）。⚠️ **承認の前にユーザーの実物確認 6（設計書 §5）が要る**——**2c が直ったかはレビューでは証明できていない**（下記）
- ⚠️ 開発担当の数値は写していない。検証スクリプトはレビュー担当が書いた（`tmp/review/操作不具合2件/f2.mjs`・既存の `u2.mjs`・`m2f2.mjs`）

---

## 判定: 合格

| 基準 | 結果 | 証跡 |
|---|---|---|
| M1 設計適合 | ✅ | §8-5 の V25〜V29 すべて期待値と一致（下表）。M1-b は下記 |
| M2 実駆動 | ✅ | レビュー担当が Playwright で操作。PC幅（1440×700／1440×900／1652×934）と **375×812**（V28）。`tmp/review/操作不具合2件/m2_f2_deck_1440.png`（エレメントを開いた左ペイン）・`m2_f2_deck_375.png`（絞り込みトグルを開いた状態）・`v27_*.png` |
| M3 回帰 | ✅ | `npm run validate` **exit 0**（`validate_f2.log`）。変更は `tools/deck-builder/style.css` の `@media (min-width: 1200px)` 内1宣言だけで、トップ・カード詳細モーダル・カード個別ページ・大会一覧が読むファイルは差分0（`git diff 0d3da4e9..HEAD -- . ':(exclude)docs/*'` → `tools/deck-builder/style.css` のみ）。デッキ構築は 1440 と 375 で開き `pageerror` 0件。既存項目は V29 で再実施 |
| M4 生成物の同期 | ✅（該当なし） | 手書き CSS の1宣言。生成物・訳に変更なし |
| M5 静かな劣化がない | ✅ | 破壊試験 V26 を再実施（`transparent` で合成レイヤーが消え、消すと戻る）。⭐ 追加でコメントの「半透明にすると外れる」も実測（`rgba(15,17,21,.92)` → レイヤー 0）。`var(--bg)` は `shared/css/tokens.css` にあり検査(i) 緑・`:root` 増減なし |
| M6 公開安全性 | ✅ | 配信ファイルの増減なし。`_headers`・`404.html`・`functions/` 差分0。秘匿情報なし。`will-change` 不使用（0件） |
| M7 スコープ | ✅ | `994de8dc` は `tools/deck-builder/style.css` のみ。設計の記録（`e422a1a0`）と実装報告（`2b98f32f`）は `docs/` のみ。宣言・値・コメント・置き場所は §8-4 と同文。F1・案1 のコードは不変。行番号参照 0件 |

### M1-b

- 本単位は絞り込み・並び替えの追加ではないので4つの副作用は対象外
- 同じ考え方で「**変更前（本番相当 `3f317196`）で効いていた性質が F1 で失われていないか**」を見るのがこの単位そのもの。本番相当の `#search-top` が持っていた**合成スクロール**が、F2 で `.pane-scroll` に戻ったことを V25 で確認した（⭐ 本番相当・変更前・変更後の3ツリーを並べて測った）
- 1199px以下では `.pane-scroll` が `display: contents` のまま背景を持たない（V28）→ 欠落なし

## 検証の条件

- `/api/*` だけを Playwright の `page.route("**/api/**")` で差し替え（前回の判定と同じハーネス `harness.mjs`）。カード・絵柄・チップは本物
- ブラウザ: **V25〜V28 は新ヘッドレス（`channel: "chromium"`）**（§8-5 の指定）、V29 は前回と同じ `chromium_headless_shell`。いずれも `--ignore-certificate-errors`＋`ignoreHTTPSErrors`
- サーバー（レビュー担当が起動し、終了後にポートから PID を引いて停止。⚠️ **3000番（リードの wrangler）には触れていない**）:

  | ポート | ツリー | 用途 |
  |---|---|---|
  | 3100 | 作業ツリー（HEAD `2b98f32f`・未コミット変更なし。実装は `994de8dc`） | 変更後 |
  | 3104 | `git archive 0d3da4e9` | 変更前 |
  | 3102 | `git archive 3f317196`（前回の判定で展開済み） | 参考（本番相当＝F1 前） |

- ⭐ **検算（既知の差）**: `style.css` の `background: var(--bg);` の件数 3100=**2**／3104=**1**／3102=**1**、F2 のコメント見出し 3100=**1**／他=**0**、`scroll-padding-bottom: 2rem` 3100/3104=**1**／3102=**0**、`index.html` の `class="pane-scroll"` 3100/3104=**1**／3102=**0**。さらに**本測定の前に 3104 で V25 の「変更前はレイヤーなし」を確認**した
- LayerTree の測り方: CDP で `DOM.enable`・`DOM.getDocument`・`LayerTree.enable` → スクロール領域の `scrollTop` を 5→0→5→0 と変えて `layerTreeDidChange` を起こす → 各レイヤーに `LayerTree.compositingReasons` と `DOM.describeNode({ backendNodeId })`。`#search-top`／`.pane-scroll` に紐づくレイヤーを抜き出した

## 設計書の検証項目との対応（§8-5）

| # | 期待値 | 実測 | 合否 |
|---|---|---|---|
| **V25** | 1440×700・ページ 400px・エレメントを開く。DSF 1 と 1.25 で `div.pane-scroll` に `OverflowScrolling` が1つ（変更前は無い） | **変更前 3104**: 背景 `rgba(0, 0, 0, 0)`・DSF 1／1.25 とも**レイヤーは `div#search-top`（`Overlap`）の1枚だけ＝`OverflowScrolling` 0**。参考 DSF 1.5 では `div.pane-scroll` 319×576 に `OverflowScrolling` あり（＝§8-3「DPR 1.5 未満だけ合成されない」と一致）。**変更後 3100**: 背景 `rgb(15, 17, 21)`・**DSF 1／1.25／1.5 とも `div.pane-scroll` 319×576 に `OverflowScrolling` が1つ**（ほかに中身の層 319×847 `Overlap`）。**参考 3102（本番相当）**: DSF 1／1.25／1.5 とも `div#search-top` 319×643 に `OverflowScrolling`（中身 319×914）＝§8-3 の表と一致（`f2_before_v25.json`・`f2_after_v25-v26-v27-v28.json`・`f2_ref3f3_v25.json`） | ✅ |
| **V26** | CSSOM `@media (min-width: 1200px) { #search-top > .pane-scroll { background: transparent !important } }` で DSF 1 のレイヤーが消える・消すと戻る | 足す前 **1** → 足すと背景 `rgba(0, 0, 0, 0)`・**0**（`div#search-top` の `Overlap` だけ＝変更前と同じ形）→ `deleteRule` で `rgb(15, 17, 21)`・**1**。⭐ 追加: `rgba(15,17,21,.92) !important`（半透明）でも **0**＝コメントの主張どおり | ✅ |
| **V27** | x 0〜330 のスクリーンショットの画素差がチャネルあたり最大 2 以下（1440×700・ページ 400px／1440×900・最上部。いずれもエレメントを開く） | 両ツリーで同じ状態（`scrollY` 400・`--pane-top` 57px・`.pane-scroll` 0／`scrollY` 0・`--pane-top` 179px・0）を撮影し PIL で比較。**1440×700: 最大差 1**（差のあるチャネル 41,030・差 3 以上 **0**）／**1440×900: 最大差 1**（46,264・差 3 以上 **0**）（`v27_1440x700_y400_{before,after}.png`・`v27_1440x900_y0_{before,after}.png`） | ✅ |
| **V28** | 1199×800 / 375×812 で V14 の9要素の矩形が変更前と一致・`.pane-scroll` の `backgroundColor` が `rgba(0, 0, 0, 0)` | **36組（2幅×閉・開×9要素）すべて差 0**（`v28_before.json`／`v28_after.json`）。`.pane-scroll` は両幅とも `display: contents`・**`rgba(0, 0, 0, 0)`** | ✅ |
| **V29** | V10・V12・V39・V20 の再実施 | **V10**（1652×934・157種）: scrollY 0/30/60/90/121 は **866〜933**、400 は **867〜934**、各3点で top 一致・押せる＝18点すべて合格（前回の判定と同値）。**V12**: 子は `pane-scroll`・`row row-actions`、`.row-actions` `static`・`#search-top` `hidden`・sticky 子孫 **0件**・`scrollTop` 0→500 で中身 **500px**／ボタン行 **0px**・`scroll-padding-bottom` 32px。**V39**: 157個訪問・**切れ 0**・続けて `s-format → s-set → s-sort → s-order → s-reset → s-search`。**V20**: `npm run validate` **exit 0**（`shared css tokens in sync — 68 トークン参照 / 未定義 0`・`:root owners in sync — 4ファイル`・`onRemoveOne wiring in sync — 渡しているのは 1ファイル（tools/deck-builder/app.js）／25ファイル走査`・`pane threshold in sync — 2ページ（トップ 1200px / デッキ構築 1200px）`・`mobile band marker in sync — 1組（style.css 1199px ↔ app.js MOBILE_MQ 1199px）`・`no line-number refs in code comments — 0件（47ファイル走査）`） | ✅ |

## ⚠️ 証明できた範囲と、ユーザーの実物確認に委ねる範囲

| 区分 | 内容 |
|---|---|
| ✅ レビューで証明 | **原因の見立ての構造が元に戻った**: F1 後に失われていた `OverflowScrolling`（合成スクロール）が、DSF 1／1.25 で `.pane-scroll` に戻った（V25）。背景を外すと失われる（V26） |
| ✅ レビューで証明 | **見た目を変えていない**（V27 最大差 1）・**1199px以下を変えていない**（V28）・**2a／F1／Tab の性質を壊していない**（V29） |
| ⚠️ **レビューでは証明できない** | **2c（群を開いた状態でスクロールバーを速く動かすと、群カードの背景・枠だけが古い位置に残る）が直ったこと**。§8-2 のとおりヘッド付き＋GPU描画でも変更前から再現しないため、**ドラッグ中の描画はヘッドレスで検証していない**（指示どおり）。⭐ **合否はユーザーの実物確認 6 だけで決まる** |
| ⚠️ 見立ての前提 | 見立ては **`devicePixelRatio` 1.5 未満**が前提。V25 の変更前 DSF 1.5 で既に合成されていたことを確認したので、**ユーザーの環境が DPR 1.5 以上なら、2c は別の原因で、F2 では直らない可能性が高い** |

⭐ **2c を未証明のまま合格にした理由**: 前回と同じ。設計書 0.4 §8-5 が「描画崩れはヘッドレスで再現しない／合成スクロールの有無で判定し、2c の合否は実物確認 6」と工程を明示しており、**レビューで検証できる項目はすべて実施して一致した**。

## ユーザーの実物確認で見てほしい点

1. ⭐ **実物確認 6（本命）**: 前回のキャプチャと**同じ操作**——エレメント等の群を開いた状態で、**左ペインのスクロールバーのつまみをつまんで上下に速く動かす**。群カードの背景・枠が中身とずれて残らないか。⚠️ **直っていなければ承認せず設計に戻す**
2. ⭐ **Windows の表示スケール（100%／125%／150%…）と Chrome の拡大率**を聞く。**150% 以上**なら見立ての前提外（上表）。**直っていてもこの値は記録に残す**（直った理由の裏付けになる）
3. **ホイールでのペインのスクロール**と**キーボード（Tab）でのスクロール**でも崩れないか（合成スクロールに変わったのはこの箱のスクロール全般なので、つまみ以外の経路も軽く）
4. **1〜5 の軽い再確認**: とくに**左ペインの地色・群カードの見え方が前回と変わっていないこと**（V27 は画素差 最大 1 だが、実機の文字描画＝ClearType はヘッドレスでは見えない。不透明な背景は文字のサブピクセル描画を保つ設計なので、**文字がにじんで／細くなって見えないか**を一応見る）

## 推奨基準の指摘（合格を妨げないもの）

- S1（コード品質）: 指摘なし。コメントは §8-4 と同文で、節参照（「操作不具合2件_設計 §8」）の慣習に沿う。CSP 上の問題なし
- S2（応答性）: 指摘なし
- S3（fail-open）: 指摘なし（`var(--bg)` が未定義でも背景が付かないだけで、合成されない＝変更前の状態に戻るだけ）

## 参考（合否に含めない）

- ⚠️ **F2 はブラウザ内部の判断（「不透明な背景のスクロール領域は低 DPR でも合成する」）に依存している**。仕様で保証された挙動ではないので、**将来の Chrome の変更や、この宣言を消す／半透明に変える編集で、画面は何も変わらないまま 2c だけが実機で戻りうる**（V26 の半透明の実測どおり、見た目では区別が付かない）。防いでいるのはコメントだけで自動検査は無い。CLAUDE.md の「スマホ帯の3対策」と同じ種類の「外すと静かに壊れる対策」なので、**承認時に CLAUDE.md かいずれかの恒久的な場所へ記録するかは設計担当の判断**（レビュー側では起票しない）
- `.fgroup .chips`（`max-height: 200px` の内側スクロール）の合成レイヤーは、本番相当・変更前・変更後のどれにも見当たらなかった（同じ測り方でノード名を広げて確認）。今回の変更で変わった点ではない

## 設計担当への確認事項

- なし（合否を左右する仕様の曖昧さは無い）
- 申し送り（前回の再掲）: 承認時に `左ペイン化_設計.md`（§12-3 F-a・§12-7 の4・§12-9 の 1-2）へ「操作不具合2件で F1 に置き換えた」注記を足す（設計書 §7 の3）。⭐ F2 もあわせて書くと、F-a の sticky に戻す・背景を外す、の両方を防げる

## 証跡の置き場

`tmp/review/操作不具合2件/`（gitignore）: `f2.mjs`・`u2.mjs`・`m2f2.mjs`・`harness.mjs`／`f2_before_v25.json`・`f2_after_v25-v26-v27-v28.json`・`f2_ref3f3_v25.json`・`f2_before_v27-v28.json`・`v28_before.json`・`v28_after.json`・`u2_after_f2_v10-v12-v39.json`・`validate_f2.log`／画像 `v27_1440x700_y400_before.png`・`v27_1440x700_y400_after.png`・`v27_1440x900_y0_before.png`・`v27_1440x900_y0_after.png`・`m2_f2_deck_1440.png`・`m2_f2_deck_375.png`
