# #53・#54 `tmp/api-cache/` の堅牢化 — 設計

⚠️ **本設計書は #53 と #54 の両方を対象とする。** 直すのは同じ2ファイルで、
片方だけ直すと同じ場所をもう一度触ることになるため、**1回の実装でまとめて閉じる**。

## 改版履歴

| 版 | 日付 | 内容 | 状態 |
| --- | --- | --- | --- |
| 1.0 | 2026-08-08 | 初版。共通化の範囲を「原子性と安全な読み込み」に限定する方針を確定 | 実装済み（`e764a2b6`） |
| 1.1 | 2026-08-08 | 実装時の確認事項への回答を反映（追加ガード2件を仕様化・Q2は見送りとして記録）。**設計変更なし** | 実装済み |

## 1. 背景

`tmp/api-cache/` には**キャッシュが2つ**あり、#52 で直したのは片方だけだった。

| ファイル | 大きさ | 管理場所 | 有効期限 | 原子的書き込み | 壊れたときの読み込み |
| --- | --- | --- | --- | --- | --- |
| `cards-snapshot.json` | 約17MB | `scripts/lib/cards-snapshot.mjs` | ✅ #52で追加 | ❌ 無し | ⚠️ `fetched_at` のみfail-safe。**JSON全体が壊れると例外** |
| `featured-sets.json` | 約8KB | `scripts/build-card-pages.mjs`（インライン） | ❌ **無し**（#53） | ❌ 無し | ❌ 例外 |

- **#53**: `build-card-pages.mjs:59` の `if (!REFRESH && existsSync(FEATURED))` は
  **存在するかどうかしか見ない**。#52 で直したのと**まったく同じ形**が同じディレクトリに残っている
- **#54**: 書き込みが `writeFileSync` 1発で**原子的でない**。中断すると壊れたJSONが残り、
  `loadCards()` を使う**4スクリプトすべてが `SyntaxError` で起動不能**になる
  （#52 の検証中に開発担当・レビュー担当の**両方が実際に踏んだ**）

## 2. 2つを一緒に直す理由

- 直す場所が同じ（`cards-snapshot.mjs` と `build-card-pages.mjs` の2ファイル）
- **#54 の対処は #53 の対処の前提になる**。featured-sets に有効期限を持たせるには
  ファイルに取得時刻を持たせる必要があり、**形を変える＝旧形式を安全に読み飛ばす仕組み（#54側）が要る**
- 片方ずつ直すと、同じ関数を2回書き換えて2回レビューすることになる

## 3. #53 が実害になる場面（優先度の根拠）

`featured-sets` は**エキスパンションのグループ分けと公式ロゴ**の出所。腐ると:

- 新しい製品ラインがグループに入らず `cards/index.html` で**「その他」に落ちる**
- セットページの**ロゴと `og:image` が付かない**

⭐ **実例が控えている**: #51 の PRDEVP は現在どのグループにも属さず「その他」にいる。
親セット `.asphodel/paradise`（PRD）が公式に featured-sets へ入った日、
**手元のキャッシュが古いままだとローカルのビルドではグループ化されない**。
その生成物をコミットすると、cronが作った正しいグループ分けを巻き戻す。

⚠️ ただし #52 より**気づきにくい**。製品ラインの追加は数か月に1度なので、
腐っても差分に現れる機会がほとんど無い。

## 4. 設計方針 — 共通化の範囲を絞る

### 4-1. 採用する形

**共通モジュール `scripts/lib/api-cache.mjs` を新設**し、**「原子的な書き込み」と「安全な読み込み」だけ**を担わせる。
⚠️ **キャッシュの中身の形（スキーマ）と有効期限の判断は、各呼び出し元に残す。**

```text
scripts/lib/api-cache.mjs   ← 新設
  writeJsonAtomic(file, obj)  … 同じディレクトリに一時ファイルを書いて rename
  readJsonSafe(file)          … 読めない/壊れている/無い → null を返す。★例外を投げない
```

### 4-2. なぜ「全部を共通化」しないのか

| 案 | 却下/採用 | 理由 |
| --- | --- | --- |
| **A. 原子性と安全な読み込みだけ共通化（採用）** | ⭐ 採用 | **`cards-snapshot.json` の中身の形を変えずに済む**。#52 で通したV1〜V13がそのまま有効で、17MBの再取得も発生しない |
| B. キャッシュ層を完全に共通化（形も統一） | 却下 | `cards-snapshot.json` の形が変わり、**既存の全スナップショットが無効化**して再取得（75リクエスト）が走る。#52 の検証項目も作り直しになる。**得るものに対して壊す範囲が大きい** |
| C. #53 と #54 を別々に直す | 却下 | §2のとおり同じ関数を2回書き換えることになる |

⭐ **判断の軸**: #52 は**通ったばかりの変更**。その土台（オンディスクの形）を揺らさずに、
**足りない性質だけを足す**のが最も安全。

### 4-3. 有効期限は既存の環境変数を流用する

`GA_SNAPSHOT_MAX_AGE_MIN`（#52で新設・既定60分／`0`=無期限）を
**`featured-sets` にもそのまま効かせる**。

- ⚠️ 名前が `SNAPSHOT` なのに2つのキャッシュを支配するのは**名前のズレ**だが、
  **改名はしない**（#52で文書化・周知したつまみを増やす/変えるほうが混乱する）
- CLAUDE.md に「`tmp/api-cache/` 全体の有効期限」と説明を書き直す（設計担当が実施）

## 5. 変更内容

### 5-1. `scripts/lib/api-cache.mjs`（新設）

```text
writeJsonAtomic(file, obj)
  1. mkdir -p (dirname)
  2. 同じディレクトリに一時ファイル(例: <name>.tmp-<pid>-<乱数>)を書く
  3. renameSync(一時ファイル, file)          ← 同一FS内の rename は原子的
  ⚠ 失敗したら一時ファイルを消してから例外を投げる(ゴミを残さない)

readJsonSafe(file)
  ファイルが無い / 読めない / JSON.parse に失敗 → null を返す
  ⚠ 例外を投げない。呼び出し元が「取り直す」判断をする
```

### 5-2. `scripts/lib/cards-snapshot.mjs`

- 書き込みを `writeJsonAtomic()` に置き換え
- 読み込みを `readJsonSafe()` に置き換え、**`null` なら取り直す**（＝#54の本丸）
- ⚠️ **オンディスクの形は変更しない**（`{ fetched_at, total, cards }` のまま）

追加する stderr（1行）:

| 場面 | 出力 |
| --- | --- |
| JSONが壊れている・読めない | `スナップショットを読めないため取り直します` |

### 5-3. `scripts/build-card-pages.mjs` の `loadFeaturedSets()`

**tmpキャッシュに取得時刻を持たせ、有効期限を効かせる。**

```text
形: { "fetched_at": "<ISO>", "data": [ …groups… ]  }     ← tmpキャッシュのみ
```

判定の順序:

```text
REFRESH                                   → 取得
readJsonSafe(FEATURED) が null            → 取得   （無い/壊れている・#54）
fetched_at が無い or 配列そのもの(旧形式)   → 取得   （自動で新形式に移行する）
fetched_at が壊れている                    → 取得
有効期限 0（無期限）                        → 再利用
経過 > 有効期限                            → 取得   （★#53の本丸）
それ以外                                   → 再利用
```

stderr（1行）:

| 場面 | 出力 |
| --- | --- |
| 期限内で再利用 | `featured-sets キャッシュ使用: {N}件 (取得: {ISO} / {相対})` |
| 無期限指定で再利用 | `featured-sets キャッシュ使用: {N}件 (取得: {ISO} / {相対} / 有効期限なし)` |
| 期限切れ | `featured-sets キャッシュが古いため取り直します ({相対} / 有効期限{M}分)` |
| 旧形式・取得時刻が壊れている | `featured-sets キャッシュが旧形式のため取り直します` |
| 無い・JSONが壊れている | *（何も出さない。従来どおり取得に進む）* |

`{相対}` は `relativeAge()` を流用（`{n}分前` / `{n}時間前` / `{n}日前`）。

- 取得成功時の `エキスパンショングループ取得: {N}件(公式ロゴ付き)` は**現状のまま**
- 取得失敗時のフォールバック（`data/featured-sets.json`）も**現状のまま**

⚠️ **取得に失敗したとき、期限切れのtmpキャッシュへ戻さないこと。**
コミット済みの `data/featured-sets.json` は**cronが毎日更新している**ため、
**期限切れのローカルキャッシュより新しい可能性が高い**。現行のフォールバック順序を変えない。

### 5-4. ⚠️ 変えてはいけないもの

| 対象 | 理由 |
| --- | --- |
| **`data/featured-sets.json` の形（インデント付きの素の配列＋末尾改行）** | ⚠️ **コミットされるファイル**。形が変わると cron が全量差分の自動コミットを作る。**素の配列のまま**にすること（`{fetched_at, data}` で包まない） |
| `cards-snapshot.json` の形 | §4-2。#52 の検証項目を無効化しないため |
| `.github/workflows/build-tournaments.yml` | 変更不要 |
| 環境変数名 `GA_SNAPSHOT_MAX_AGE_MIN` | §4-3 |
| 生成物（`cards/**` `sets/**` `index.html` `sitemap.xml` `data/**`） | 本修正で出力は変わらない |
| `CLAUDE.md` | **設計担当が行う** |

## 6. 期待される差分

| ファイル | 変化 |
| --- | --- |
| `scripts/lib/api-cache.mjs` | **新規** |
| `scripts/lib/cards-snapshot.mjs` | 読み書きを共通関数へ・壊れたJSONのfail-safe |
| `scripts/build-card-pages.mjs` | `loadFeaturedSets()` に有効期限と新形式 |

**これ以外に差分が出たら異常。** とくに `data/featured-sets.json` に差分が出たら
**コミットせずに報告すること**（§5-4の最重要項目）。

## 7. 検証項目

⚠️ **`tmp/` 配下の他セッションの作業物は消さないこと。** キャッシュは直接書き換えて検証してよいが、
**現物を退避してから行い、終わったら戻すこと。**

### #54（原子性・安全な読み込み）

| # | 手順 | 期待値（最終形） |
| --- | --- | --- |
| V1 ⭐ | `cards-snapshot.json` を**壊す**（先頭1KBだけ残して切り詰める＝書き込み中断の再現）→ `build-card-pages.mjs` を実行 | **例外を投げず**に `スナップショットを読めないため取り直します` → `公式APIから全カードを取得中...` → 正常終了（exit 0） |
| V2 ⭐ | 同じ壊し方で **4スクリプトすべて**を実行<br>（`build-card-pages` / `gen-card-meta-index` / `build-tournament-pages --no-scan` / ⚠️`gen-element-orbs` は sharp 必須のため実行不可なら経路同一で代替可） | **すべて起動でき、`SyntaxError` が出ない**。⭐ **これが #54 の解決そのもの** |
| V3 | `featured-sets.json` を壊して `build-card-pages.mjs` | 例外を投げず取得に進み、正常終了 |
| V4 | 正常実行を1回行ったあと `ls tmp/api-cache/` | **`.tmp-*` の残骸が無い**（一時ファイルが片付いている） |
| V5 | 書き込み中の中断を模す（`writeJsonAtomic` の rename 前に例外を投げるよう一時改変して実行） | ⚠️ **既存の正しいキャッシュが壊れていない**（rename前なので本体は無傷）。一時ファイルも残らない |

### #53（featured-sets の有効期限）

| # | 手順 | 期待値（最終形） |
| --- | --- | --- |
| V6 | 取得直後（60分未満）のキャッシュで `build-card-pages.mjs` | `featured-sets キャッシュ使用: 9件 (取得: … / {n}分前)`。⭐ **`/featured-sets` へのAPIリクエストが0件** |
| V7 | `fetched_at` を **25時間前**に書き換えて実行 | `featured-sets キャッシュが古いため取り直します (1日前 / 有効期限60分)` → `エキスパンショングループ取得: 9件(公式ロゴ付き)` |
| V8 | `fetched_at` を **90分前**に書き換えて実行 | `featured-sets キャッシュが古いため取り直します (1時間前 / 有効期限60分)` |
| V9 | V8の状態で `GA_SNAPSHOT_MAX_AGE_MIN=0` | 取り直さず `featured-sets キャッシュ使用: 9件 (取得: … / 1時間前 / 有効期限なし)` |
| V10 ⭐ | **旧形式**（素の配列）のキャッシュを置いて実行 | `featured-sets キャッシュが旧形式のため取り直します` → 取得 → **新形式で保存される**。⭐ **もう一度実行すると V6 の再利用になる**（自動移行が完了している） |
| V11 | `--refresh` を付けて実行（期限内） | 期限内でも取り直す（既存互換） |

### 回帰（⚠️ 落とすと本番に影響する）

| # | 対象 | 期待値 |
| --- | --- | --- |
| V12 ⭐ | **`data/featured-sets.json` の形** | `git status --short` に**出ない**。⚠️ 素の配列・インデント付き・末尾改行のまま。**ここが変わると cron が全量差分の自動コミットを作る** |
| V13 ⭐ | **生成物が変わらない** | 取得直後のキャッシュでフル生成 → `git status --short` は**変更した3スクリプトのみ**。`cards/**` `sets/**` `index.html` `sitemap.xml` `data/**` に差分ゼロ |
| V14 | **#52 の検証項目が生きていること** | #52 設計書 §7 の **V1〜V6・V9** を再実行して全部合格（スナップショットの形を変えていないことの確認） |
| V15 ⭐ | **CIの相乗り**（#52 V9と同じ手順） | `tmp/api-cache/` を空にしてcron順に3本 → **1本目だけがカード一覧を取得**。⚠️ **`featured-sets` も1回だけ**取得されること（2本目以降で再取得しない） |
| V16 | 取得失敗時のフォールバック | `/featured-sets` を失敗させ、**期限切れの**tmpキャッシュがある状態で実行 → ⚠️ **期限切れキャッシュではなく `data/featured-sets.json` を使う**（`featured-sets 取得失敗 → data/featured-sets.json(コミット済み9件)で続行`） |
| V17 | `npm run validate` | **exit 0** |

### 負テスト（⚠️ 必須）

| # | 壊し方 | 期待 |
| --- | --- | --- |
| V18 | featured-sets の期限判定を一時的に無効化（常に期限内とみなす）して **V7** を再実行 | **V7が失敗する**（取り直さない）。⭐ 失敗しないなら V7 は期限判定以外の理由で通っていたことになるので検証をやり直す |
| V19 | `readJsonSafe()` を「例外をそのまま投げる」実装に一時的に戻して **V1** を再実行 | **V1が失敗する**（`SyntaxError` で落ちる） |

⚠️ V18・V19 とも**リポジトリのコードを書き換えず、コピーにパッチを当てて**行うこと（#52 のレビュー担当の方式）。

## 8. 未決事項（実装側で決めない）

| # | 論点 | 扱い |
| --- | --- | --- |
| U1 | 環境変数を `GA_API_CACHE_MAX_AGE_MIN` に改名するか | **改名しない**（§4-3）。実装側で変えない |
| U2 | `readJsonSafe()` が握りつぶした理由（ENOENT なのか壊れているのか）をログに出すか | **出さなくてよい。** 呼び出し元が「取り直します」を出すので二重になる。⚠️ ただし**取り直しに失敗したときは例外を投げる**こと（#52 D6） |
| U3 | 他の一時キャッシュ（将来増えるもの）への横展開 | 本issueでは扱わない。**現存するのは2つだけ**であることを確認済み |

## 10. 1.1 で仕様化した追加ガード（実装が発見した設計の穴）

実装が設計書に無いガードを2件足し、レビューが**負テストで効いていることを実証**した（G1〜G3）。
どちらも**設計書 §5 の指示どおりに作ると #54 が別の形で残る**穴であり、1.1 で正式仕様とする。

| # | ガード | 塞いだ穴 |
| --- | --- | --- |
| G-1 | `loadCards()`: `readJsonSafe()` が `null` のときに加え **`snap.cards` が配列でないとき**も取り直す（文言は `スナップショットを読めないため取り直します` を共用） | **parseは通るが中身が違うJSON**（`{}`・`{"fetched_at":"…"}` など）。⚠️ 設計書どおり `null` だけを見ると `snap.cards.length` が `TypeError` になり「4スクリプト起動不能」が残る。レビューの **G3**（ガードを外すと `TypeError` で落ちる）で実証済み |
| G-2 | `cachedFeaturedSets()`: `data` が**空配列**のときも「取り直す」側に倒す | 空配列を再利用すると、#30 の「HTTP 200 の空応答は失敗扱い」ガードを**キャッシュ経由ですり抜け**、ロゴなし・「その他」1個に潰れたページが生成される |

⚠️ **G-2 の文言は `featured-sets キャッシュが旧形式のため取り直します` のままとする（不正確を許容）。**
空配列は「旧形式」ではないが、新実装は空配列を**書き込まない**（#30ガードが取得時点で弾く）ため、
この経路に到達するのは手編集か旧バージョンの書き込みに限られる。
到達実質ゼロのログ文言のために 実装→レビュー→承認 の1周を回すコストが釣り合わない。

⭐ 教訓（#52 の「同じ形を同じ場所から探す」に続くもの）:
**「壊れた入力」を仕様化するときは、「parseが失敗する入力」だけでなく
「parseは通るが形が違う入力」まで列挙する。** 切り詰め（V1/V2）だけでは後者を検出できなかった。

## 11. 見送った指摘（1.1）

| 指摘 | 判断 |
| --- | --- |
| `data/featured-sets.json` への書き込みが非原子的なまま（実装Q2・レビュー参考） | **見送り（起票もしない）。** 理由: ①cron経路では書き込みクラッシュ＝job失敗となり**コミット前に止まる**ため、壊れたファイルが本番・リポジトリに残らない ②ローカルで壊れても**gitに直前のコミットがあり `git checkout --` で即復旧できる**（`tmp/` と決定的に違う点） ③壊れたまま気づかない経路は fetch失敗時の `JSON.parse` 例外＝**fail-loud** で、静かな劣化ではない。tmp側と危険度が桁違いに低い |
| `Date.parse(cached?.fetched_at)` の `?.` が冗長（レビューS1） | 対応不要。動作に影響なし |
| env不正値の警告が1実行で2回出る（レビューS1・`loadCards` と `cachedFeaturedSets` が各1回） | 対応不要。不正値はそもそも利用者の誤設定で、既定60分への退避は正しく働いている。重複排除の状態管理を足すほうが複雑化する |
| `try` 内の書き込み順（tmp書き込み失敗時にフォールバック未更新のままcatchへ落ちる） | **変更前と同じ順序**であり本コミットの退行ではない（レビューも同見解）。実害はfetch成功が1日分無駄になるだけで、翌日のcronが回復する |

## 9. 参考

- #52 設計書: `docs/design/52-スナップショット鮮度/スナップショット鮮度_設計.md`（1.1）
- #52 で確立した原則: **取り直しに失敗したときに古いキャッシュへ黙って落ちない**（D6）
- CLAUDE.md: `data/featured-sets.json` は**手で編集しない**（取得できた日だけcronが更新する）
- ⭐ #52 の教訓: **不具合の「形」を特定したら、同じ形が他にないかを同じ場所から探す**
  — 本設計は #52 でそれをやらなかった結果の後始末にあたる
