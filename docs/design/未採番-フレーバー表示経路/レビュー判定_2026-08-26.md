**対象: 「フレーバー表示経路」** — この宣言は 2026-09-11 に遡って追記した（索引の転記ドリフト防止）

# 未採番（フレーバー表示経路）レビュー判定

⚠️ **本来は該当issueのコメント。GitHubアカウント停止中で `gh issue comment` が打てないため、
代替としてこのファイルに記録する**（先行事例: `docs/design/未採番-辞書の抜けと検出経路/レビュー判定_2026-08-21.md`）。
復旧後、本文をそのまま
`gh issue comment <n> --body-file docs/design/未採番-フレーバー表示経路/レビュー判定_2026-08-26.md` で投稿する。
⚠️ ラベル操作は未実施（issueが存在しない）。**本判定は不合格なので、本来の付け替え先は
`レビュー待ち` → `実装待ち`**（`gh issue edit <n> --remove-label レビュー待ち --add-label 実装待ち`）。

- 判定日: **2026-08-26**
- 対象コミット: **`a7125896`**（498ファイル / +547 −541）
- 参照設計書: `docs/design/未採番-フレーバー表示経路/フレーバー表示経路_設計.md` **第3版**
- 実装報告（⚠️ 実装者本人ではなく別セッションによる**追認検証**）: `f65cd7bb` → `実装報告_2026-08-26.md`
- 証跡の置き場: `tmp/review/未採番-フレーバー/`（gitignore・スクリーンショット36枚＋計測スクリプト）

---

## 判定: **不合格（M7）**

⚠️ **報告の数字は一切出発点にせず、V1〜V9 は全項目をレビュー担当が独立に再実施した。**
とくに V1（件数）・V2（495の内訳）・V5/V6（決定性）は、**報告を読む前に自分で測った値**を先に出して
突き合わせている。⭐ **`flavorOf()` の実装そのものは設計書 §4-1 に忠実で、欠陥は見つからなかった**
（M1〜M6 はすべて合格）。**不合格の理由は1点だけ** — **タスクと無関係なAPIドリフトが
コミット済みカードページ14枚に混入しており、その事実が記録されていない**（むしろ
実装報告 §4-3 は「一切混入していない」と明記している）。

### 必須基準

| 基準 | 結果 | 証跡 |
|---|:--:|---|
| **M1** 設計適合 | ✅ | V1〜V9 を全番号レビュー担当が再実施し全て一致（下表）。⭐ さらに **設計書 §4-1 の仕様からレビュー担当が独自に書き起こした `flavorOf` と、コミット済み全2,494ページのフレーバー文面を機械照合 → 食い違い0件** |
| **M2** 実駆動 | ✅ | Playwright で **PC(1280px) と 375px の両方**。詳細モーダル7枚×2幅・静的ページ・回帰6画面×2幅。スクリーンショット **36枚**（`tmp/review/未採番-フレーバー/`）。⭐ デッキ構築ツールの詳細モーダルでも同一文面を確認 |
| **M3** 回帰 | ✅ | `npm run validate` **exit 0**（`all checks passed.` / `total cards: 2516`）。トップ・カード詳細モーダル・カード個別ページ・エキスパンション一覧・デッキ構築・大会一覧・セットページとも **200 / pageerror 0 / 横スクロールなし**（両幅） |
| **M4** 生成物の同期 | ✅ | 同一スナップショットで再生成 → **495ページすべてバイト一致**。訳データは未変更のため `gen-tl-json.mjs` 不要（validate の陳腐化検査も通過）。セットページ・`sitemap.xml` に差分が出ないのは**フレーバーを埋め込んでいないため**で正しい（`grep` で0件を確認） |
| **M5** 静かな劣化がない | ✅ | 本タスクは検査を追加していないので「両辺同源」には該当しない。代わりに ⭐ **わざと壊す試験**を実施 — サンドボックスの `flavorOf()` を旧実装に差し戻して再生成 → **変化するのはちょうど495ページで、コミット済みの495枚と集合が完全一致**。＝この495枚は確かに新ロジックの産物であり、検査が空振りしていない |
| **M6** 公開安全性 | ✅ | 498ファイル**すべて `M`（変更）** ＝ 新規追加・削除ゼロ＝**本番配信物の増減なし**。秘匿情報の混入なし（コード差分を `token/secret/key/password` で走査 → 0件）。`_headers`・`404.html`・`functions/` に変更なし。§7 に**新規ファイルが無いこと**と反映確認手順（素のURLと `?cb=` を並べて反復）が明記されている |
| **M7** スコープ | ❌ | ⚠️ **495ページのうち14ページに、フレーバーと無関係な差分が混入している**（公式裁定セクション6件／収録セット表の行順・代表画像 8件。うち**5件は `og:image`・`twitter:image`・本文の代表画像が別の版に入れ替わっている**）。**コミットメッセージにも報告にも記載が無く**、報告 §4-3 は逆に「一切混入していない」と述べている（→ **❌ M7-1**） |

### 推奨基準

| 基準 | 結果 | 所見 |
|---|:--:|---|
| **S1** コード品質 | ✅ | `flavorOf()` は既存の `seasonalBanState()` と同型（共有モジュール1箇所に集約）。コメントの密度・⚠️記法とも周囲に揃っている。style属性の追加なし・バッジの追加なし（CSP／`.badges-bl` の論点に該当しない）。消費側3箇所すべてが `flavorOf()` を通ることを `grep` で確認（`.flavor` の残存参照は `gen-tl-json.mjs` と訳エントリ構築のみ＝表示経路ではない） |
| **S2** 応答性 | ✅ | 追加のAPI往復ゼロ。`flavorOf()` はカードあたり最大でも版数のソート1回 |
| **S3** fail-open | ✅ | ⭐ **異常入力11パターンを注入して実測**（下記 S3）。`null`／`editions` 欠落／要素が `null`／`set` 欠落／`flavor` が数値／空白1文字 — **例外は1件も出ず、常に文字列を返す** |

---

## 設計書 §5 の検証項目との対応（レビュー担当による再実施）

| # | 期待値 | **レビュー担当の実測** | 合否 |
|---|---|---|:--:|
| **V1** | 726 / 473 / 2 | ⭐ **報告を見る前に自分で数え直した。** スナップショット（2026-08-21T14:04:47.728Z・**2,495枚**）で、置き場の内訳は **`card.flavor` 非空 614 / 版側のみ 586**（§1-1 と一致）。表示状態は **表示されている 726（原文609＋和訳117）/ 版側にしか無く和訳も無い 474 / 空白1文字 2（`virgil-altered-future`・`zinn-volnia-abbess`）**。⭐ **「473 + 空枠2」と「474 + 空枠1」は `zinn` をどちらに数えるかの違いだけで同じ実測**（§1-2 の説明どおり）。設計時（8/20・2,494枚）との差は**フレーバーを持たない新カード1枚**のみ | ✅ |
| **V2** | **495** | ⭐ **数を確かめる前に、設計書 §4-1 の仕様から自分で `flavorOf` を書き起こして期待集合を導出した** → **新たに原文が出る474 / 空枠が消える1 / trimだけ20 = 495**、文面が別物に変わるカードは**0件**。この495 slug と `git show --name-only a7125896 -- cards` の495 slug を `diff` → **差分0行（集合が完全一致）** | ✅ |
| **V3** | `zinn-volnia-abbess` に原文 | `<p class="cp-flavor">“If the price of brilliance is to burn, then so be it.”</p>` | ✅ |
| **V4** | `virgil-altered-future` は `cp-flavor` ごと消える | `cp-flavor` **0回** / 見出し「フレーバーテキスト」**0回** / 空の `<p>` **0件**。モーダルでも `#d-flavor-wrap` が `hidden=true`（両幅で確認） | ✅ |
| **V5** | 2回目の差分0 | ⭐ **作業ツリーを触らずに検証**（後述のサンドボックス）。同じキャッシュで2回実行 → `cards/**` + `sets/**` + `sitemap.xml` + `index.html` の **2,563ファイルが md5 完全一致** | ✅ |
| **V6** | フレーバー文面が全枚数で一致 | ⭐ **報告より強い攪乱を掛けた** — 逆順ではなく **slugハッシュによる決定的シャッフル**で1,248枚の `editions` を並べ替えて再生成 → **フレーバーを持つ1,200ページの文面が1件も変わらない**。一方ページ全体では**338ファイルが不一致**で、原因は `newestFirst()`（下記） | ✅ |
| **V7** | モーダルと静的ページが同じ文面 | ⭐ **報告とは別のカードを自分で選んだ。** 版で文面が違う110枚（レビュー担当が独自に数えて **110**・同一文面の重複96・同着13 と設計書 §2-3/§4-1 に一致）から **`annul-spell`** を選定 — **配列先頭は PRD版（2026-08-21）だが初出は P25版（2025-02-06）** という、配列順ルールなら必ず外す条件。静的ページ・詳細モーダル（**公式APIから実取得する経路**）とも **P25版の "A good alchemist always has a back up plan!…"**。`another-beginning`（初出=P25）・`acolyte-of-cultivation`・`zinn-volnia-abbess` も一致。**PC/375px の両幅**、デッキ構築ツール上のモーダルでも同一 | ✅ |
| **V8** | 日本語フレーバーが出る（優先順が不変） | ⭐ **設計書の例 `crimson-vein` が使えないことを自分で確認した**（スナップショットで `flavor: null`・`editions[].flavor` 非空 **0件**・和訳も無い＝`flavorOf` は `""` を返す）。報告の差し替え先で実駆動 — **`cato-meadows-channeler`**（和訳 > `card.flavor`）「今や、私は自分自身の主人だ。」/ **`aenean-cyclone`**（和訳 > `editions[].flavor`）「子どもたちが目を覚ます前に、片づけてしまおう。」。**両幅・静的/モーダルとも一致** ＝ 優先順は変わっていない | ✅ |
| **V9** | exit 0 | `npm run validate` → **exit 0** / `all checks passed.` | ✅ |

### 進行役から名指しされた論点1〜5への回答

| # | 論点 | レビュー担当の判断 |
|---|---|:--|
| **1** | 「475ではなく495。差の20枚は trim だけ」 | ⭕ **妥当**。20枚を自分で特定し（`reduce-to-ash` 等）、**20枚すべてで「新 = 旧.trim()」が成立し、かつ旧≠新**であることを機械照合した（20/20）。文面が別物に変わったカードは**全2,495枚で0件**。§4-1 の `pick` が `v.trim()` を返す仕様どおりで**実装の逸脱ではない** |
| **2** | 「V6のバイト一致は成立しない。原因は `newestFirst()`」 | ⭕ **妥当**。⭐ **核心の「`flavorOf` は順序非依存」は自分の攪乱試験で確認**（1,248枚をシャッフルしてもフレーバー文面は1,200ページ全て不変）。`newestFirst()`（`build-card-pages.mjs:153`）が `release_date` だけで比較し**同着の第2キーを持たない**ことをコードで確認し、実データでも裏づけた（`disenchant` は `rdopd-a/b/c` が**同一発売日 2026-04-03**）。**`a7125896` はこの関数を触っていないので本タスクのスコープ外**という判断に同意する → **別issueの候補として設計担当へ**（⚠️ レビュー担当は起票しない）。⚠️ ただし**この非決定性が M7-1 の8ファイルとして実際にコミットへ入り込んでいる**（下記） |
| **3** | 「V8の例 `crimson-vein` は使えないので差し替えた」 | ⭕ **妥当**。`crimson-vein` はどこにもフレーバーを持たないことを自分で確認済み。差し替え後の2枚で**優先順（和訳 > `card.flavor` > `editions[].flavor`）が変わっていない**ことを実駆動で確認した |
| **4** | V7の要（版で文面が違う1枚で静的＝モーダル） | ⭕ **満たされている**。⭐ 報告と別のカード（`annul-spell`）で、**配列順ルールなら外す条件**を選んで確認した |
| **5** | M6（新規ファイルが無く到達マーカーが使えない） | ⭕ **§7 に手順が書かれている**（素のURLと `?cb=` を並べて20秒間隔で反復・`/cards/zinn-volnia-abbess/` で新旧判別）。⚠️ ただし **§7 の変更規模だけが「475ファイル」のまま**（第3版で §1-2・§4-3・V2 は 495 に直したが §7 と §8-3 が取り残されている）＝下記「参考」 |

---

## ❌ M7-1 タスクと無関係なAPIドリフトが14ファイルに混入している（記録は「混入なし」）

- **該当**（コミット `a7125896` に含まれる `cards/<slug>/index.html`）:
  - **公式裁定セクションの増加（6件）**: `hoarfrost-spine` / `pawn-piece` / `siphoning-stab`（いずれも
    **`<meta name="description">` の「収録セット」→「収録セット・裁定」も併発**） /
    `incendiary-shot`（2件→3件） / `rowland-schwartz-knight`（1件→2件） / `galahad-court-knight`
  - **`editions` の並び順に由来する差分（8件）**: `break-apart` / `gentle-respite` /
    `overlapping-visages` / `spirit-of-fortuitous-wind` / `wandering-glaivier`
    （⚠️ **`og:image`・`twitter:image`・本文の代表画像が別の版の絵柄に入れ替わっている**） /
    `disenchant` / `pantheon-barrier` / `prismspire-scepter`（収録セット表の行順のみ）
- **再現**（作業ツリーを変更しない手順。実際にこれで検出した）:

  ```bash
  # 495ファイルの差分から「フレーバーセクション」を取り除いても残る差分を数える
  git show --name-only --format= a7125896 -- cards | while read f; do
    diff <(git show "a7125896^:$f" | perl -0pe 's|<section class="cp-block"><h2>フレーバーテキスト</h2>.*?</section>||s') \
         <(git show "a7125896:$f"  | perl -0pe 's|<section class="cp-block"><h2>フレーバーテキスト</h2>.*?</section>||s') \
      > /dev/null || echo "$f"
  done
  ```

- **期待**: 設計書 §4-3 —「**差分が495ファイル以外に出たら、それはドリフト。混ぜずに戻す**」。
  ＝ **495ファイルの中身もフレーバーの差分だけであること**。
  併せて CLAUDE.md「無関係な差分はタスクのコミットに混ぜず、翌朝のcronに任せる」
- **実測**: **14ファイル**にフレーバー以外の差分。うち**5ファイルは公開面（`og:image`）が変わる**。
  ⚠️ **コミットメッセージは「cards/** 495ページを再生成(新たに原文が出る474 + 空枠が消える1 + 末尾空白のtrim 20)」**
  としか書いておらず、**実装報告 §4-3 は「本タスクのコミット `a7125896` には一切混入していないことを確認した」と明記**している。
  ⭐ 報告の「積集合0件」は**今日再生成して出る67件との積集合**を測ったもので、その計算自体は正しい
  （レビュー担当も再現した: 今日のドリフトは **カード58 + セット8 + `sitemap.xml` + 新規カード1** で、
  495ページとの積集合は**0件**）。⚠️ **しかし「コミットの中にドリフトが無い」ことの証明にはなっていない** —
  ドリフトは**495ファイルの内側**に入っており、**ファイル数の突き合わせでは原理的に検出できない**
- **重大度**: **軽微**（表示は壊れず、裁定はcron復旧後に同じ内容が入る）。
  ⚠️ ただし**記録が事実と食い違っている点**は看過できない。この差分は**pushで本番公開される**もので、
  設計担当は承認時、ユーザーはpush判断時に、**issueに書かれたことしか見ない**
- **根拠**: 評価基準 **M7**「設計書に無い変更が入っていない。**入っているなら根拠がissueに書かれている**」/
  設計書 §4-3 / CLAUDE.md「無関係な差分はタスクのコミットに混ぜない」/
  ⭐ **本リポジトリの前例 `e6023abc`「#71: レビュー指摘(M7)対応。無関係な31件の生成物差分を `bb2e28f7^` の内容に復元」**

### 差し戻しで求めること（⚠️ 修正内容を決めるのはレビュー担当ではない）

1. **事実を報告に書く**（これは開発担当の作業）。14ファイルの一覧・種別・
   「なぜ495ファイルの内側に入ったか」（＝コミット時点のスナップショットが、
   cron最終稼働 2026-08-18 より新しい 2026-08-21 のものだったため）を記載し、
   §4-3 の「一切混入していない」の記述を訂正する
2. **戻すか残すかは決めない**。⭐ 前例（#71）は**復元**したが、今回は**フレーバーの差分と同じファイルに同居している**ため
   「そのファイルだけ親の内容に戻す」と**フレーバー修正ごと消える**。機械的に剥がすには生成物の手編集が要り、
   **cron復旧後に同じ差分が戻ってくる**性質のものでもある。**どちらを採るかは設計担当の判断**なので、
   1 を書いたうえで「設計担当への確認事項」に上げること（越権の禁止・CLAUDE.md）
3. ⭐ **`newestFirst()` の件（下記）と一体で判断すると早い**。5ファイルの代表画像の入れ替わりは
   **`newestFirst()` の同着非決定性そのもの**で、放置すると**APIが `editions` を並べ替えるたびに再発する**

---

## M5 の重点確認（わざと壊して、検査が空振りしていないことを実証）

⚠️ **本タスクは新しい検査（validate等）を追加していない**ので「検査の両辺が同源」には該当しない。
代わりに、**生成物が本当に新ロジックの産物か**を負テストで確かめた:

| ケース | 期待 | 実測 | 判定 |
|---|---|---|:--:|
| サンドボックスの `flavorOf()` を**旧実装**（`(t&&t.flavor) \|\| card.flavor`）に差し戻して再生成 | 495ページだけが元に戻る | **495ページが変化し、コミット済み495枚と slug 集合が完全一致** | ✅ |
| 同じキャッシュで2回生成（V5） | 差分0 | **2,563ファイル md5一致** | ✅ |
| `editions` を決定的シャッフルして生成（V6） | フレーバー文面は不変 | **1,200ページ全て不変**（ページ全体では338ファイルが不一致＝`newestFirst()`） | ✅ |

⚠️ **残る「静かな劣化」の経路**（基準違反ではないので合否に含めない・設計担当へ）:
**フレーバーの表示経路には検出経路が無い**。`flavorOf()` を誰かが `card.flavor` だけに戻しても
**`npm run validate` は緑のまま**で、症状は「586枚のフレーバーが黙って消える」＝#40 と同じ型の無言の劣化になる。
（今回の設計書は検出経路を要求していないため**不合格の理由にはしていない**）

## S3 の重点確認（fail-open）

サンドボックスの `card-i18n.js` を直接叩いて異常入力を注入した（**リポジトリ本体は未変更**）:

| 入力 | 戻り値 |
|---|---|
| `flavorOf(null, null)` / `(undefined, undefined)` | `""` |
| `editions` が欠落／`null` | `""` |
| `editions` の要素が `null`・`undefined` を含む | `"ok"`（生きている要素を拾う） |
| `set` 欠落（発売日不明）2版 | `"A"`（**slug昇順で決着**） |
| `1970-01-01` と `2020-01-01` | `"NEW"`（**1970は末尾へ**） |
| `card.flavor` が空白1文字 | `""` |
| 和訳が空白1文字 | `"EN"`（**空白の和訳で原文を潰さない**） |
| `card.flavor` が数値 `123` | `"E"`（版側にフォールバック） |

⭐ **例外は1件も出ず、戻り値は常に文字列**。設計書 §4-1 の「戻り値を `""` に統一」を満たしている。

---

## 設計担当への確認事項

⚠️ **1 は合否に直結する**（M7-1 の処置方針）。2〜4 は合否と独立。

1. ⭐ **M7-1 の14ファイルを「残す」か「剥がす」か**（上記「差し戻しで求めること」2）。
   レビュー担当は**判断しない**。⚠️ **push すると `og:image` が変わる5枚がそのまま本番に出る**
2. ⭐ **`newestFirst()`（`build-card-pages.mjs:153`）の順序非依存化を別issueに切り出すか**。
   実装報告 §7-1 と同じ提起だが、**レビュー担当としても妥当と判断した**うえで
   **M7-1 の8ファイルがその実害である**ことを追加報告する。同着の版（例: `disenchant` の
   `rdopd-a/b/c` は全て 2026-04-03）で**入力配列の順序がそのまま代表画像・`og:image`・収録セット表に出る**。
   `edition.slug` を第2キーに足せば `flavorOf()` と同じ手で解ける見込み（⚠️ レビュー担当は起票しない）
3. **cron停止中に溜まった陳腐化（今日時点で カード58 + セット8 + `sitemap.xml` + 新規カード
   `jinzhuo-bands-of-virtue` 1件）をどう扱うか**。⭐ **本タスクに混ぜないという判断は正しい**
   （レビュー担当も積集合0件を再現した）。放置しても表示は壊れない（裁定が出ないだけ＝fail-open）
4. **§8-2（裏面フレーバー）**: `backFace()` は `flavorOf()` に寄ったが**表示側が無い**ことを
   `grep` で再確認した（`shared`・`tools`・`scripts` に `.flavor` の表示経路は3箇所のみ）。
   死んだフィールドのままで**現状維持が正しい**。起票の要否は設計担当の判断

## 参考（基準に紐づかないので合否に含めない）

- 設計書 **§7 の「`cards/**` 475ファイル」と §8-3 の「475ファイルを再生成する」が第3版の訂正から漏れている**
  （§1-2・§4-3・V2 は 495 に直っている）。⚠️ **§7 は push 判断のときにユーザーが読む節**なので、
  数字が古いままだと「498ファイルのはずが475?」と読み手が止まる
- V1 の付随実測: 設計書 §4-1 の裏づけ表のうち **「配列順ルールと初出順ルールで結果が変わる」は
  本日時点で 57枚**（設計時は65枚）。⭐ **これは公式API側の配列順が変わったためで、実装の問題ではない**
  （むしろ「配列順は当てにならない」という §2-3 の前提を追認する数字）。他の3項目
  （110枚 / 13枚 / 同一文面96枚）は設計書と一致

---

## レビュー担当の作業ログ（作業ツリーを変更していないことの担保）

- ⭐ **`build:cards` の再実行は、リポジトリ外のサンドボックスで行った**
  （`scripts/` `shared/` `data/` `index.html` とスナップショット2つを**実体コピー**した独立ツリーを3つ作り、
  そこで生成・比較した）。**リポジトリの `cards/` `sets/` `sitemap.xml` `index.html` は一度も書いていない**
- **全4回の生成とも `GA_SNAPSHOT_MAX_AGE_MIN=0` を付けた**。stderr は毎回
  `スナップショット使用: 2495枚 (取得: 2026-08-21T14:04:47.728Z / 4日前 / 有効期限なし)` で、
  **公式APIへのアクセスは発生していない**
- ✅ **スナップショットの md5 は判定後も `297ab372fc9b2cd4a82485cdbde1e3f5`**（退避 `tmp/dev/snapshot-backup/` と一致）
- `git checkout` / `stash` / `restore` / ブランチ切り替えは**未使用**。読んだのは `git show` / `git log` / `git status` のみ。
  判定時の `git status --short` は**空**
- 起動したローカルサーバー（`PORT=3100 npm run dev`）は**停止済み**（`curl` が接続拒否＝exit 7）
- 証跡は `tmp/review/未採番-フレーバー/`（スクリーンショット36枚＋実行スクリプト）。
  **ほかのセッションの `tmp/` 配下（`tmp/dev/` 等）は削除していない**

## 残っている確認（pushしないと確認できない項目・合否に含めない）

- 本番での表示確認（`/cards/zinn-volnia-abbess/` に原文が出ること）。⚠️ **新規ファイルが1つも無い**ため
  到達マーカーが使えず、**素のURLと `?cb=` を並べて20秒間隔で反復**する必要がある（設計書 §7）
- ⚠️ **push そのものが不可**（GitHubアカウント停止中）。本コミットは**本番配信物498ファイル**を含むため、
  公開の可否は常にユーザー判断。**M7-1 の処置が決まるまでは push しないこと**
