# issue #未採番: `editions[].flavor` のフレーバーが表示されない(473枚) 設計書

⚠️ **フォルダ名が `未採番-` なのは、GitHubアカウント停止中で `gh issue create` が打てないため。**
起票できたら `docs/design/<issue番号>-フレーバー表示経路/` にリネームする
(手順は `docs/design/73-PRD新カード和訳/issue反映待ち_2026-08-19.md`)。

## 改版履歴

| 日付 | 版 | 内容 |
|---|---|---|
| 2026-08-20 | 初版 | #73 の派生issue③を設計。骨子の実測値(726 / 473 / 2)を**完全に再現**。⚠️ ただし骨子の修正案は**そのままでは採れない**ことが判明 — **110枚は版ごとにフレーバーが違い**、配列順で拾うと**出力が非決定的**になる(§2-3)。⭐ 選択規則を「**初出の版**」に定めた。**全項目が実装待ち** |
| 2026-08-21 | 第2版 | ⭐ **§3 の方針(英語原文を出す)をユーザーが承認**(推奨どおり)。§8-3(出さない場合の代替案)は**不採用として確定**。⚠️ §8-1(版切り替えへの追従)・§8-2(裏面フレーバー)は**今回やらない=別issue**のまま変更なし。**未決事項は無くなった＝実装に着手できる** |
| 2026-08-26 | **第5版** | ✅ **レビュー再判定 `d412b2b3` で合格 → 設計担当が承認**（`承認_2026-08-26.md`）。⭐ **状態は `push待ち`（ユーザー判断）でクローズしない**。レビューからの確認事項4点に回答: ①`newestFirst()` の順序非決定性は**派生issueとして起票する**（`docs/design/未採番-版順序の非決定性/起票案_2026-08-26.md`。⚠️ `prismspire-scepter` は直らない旨を明記） ②cron停止中の陳腐化67件は**復旧後の初回cronに任せる**（⚠️ ただし **pushを初回cronより前に**行う。後回しにすると `cards/**` で rebase 衝突する） ③**§8-1・§8-2 は1件にまとめて起票**（`派生起票案_2026-08-26.md`・`enhancement`/`検討中`） ④**ドリフト検査の教訓を `CLAUDE.md` に昇格**（実施済み） |
| 2026-08-26 | **第4版** | ⚠️ **レビュー判定(`343c3c59`)で M7 不合格→差し戻し(1往復目)の対応。M1〜M6は合格・`flavorOf()` の実装自体に欠陥なし。** ①⚠️ **`a7125896` の495ページのうち14ページに、本タスクと無関係なAPIドリフトが混入している**ことが判明(裁定6 / `editions` 順序由来7 / API側のデータ訂正1。⭐ **うち5件は `og:image` が別の版に変わる**)。**§7 に明記**した(→ push判断の材料) ②⭐ **処置はユーザー判断で「剥がさず残す」に決定**(2026-08-26。理由: **手で剥がすとどのビルドでも再現しない生成物になる** / **cron復旧後の初回cronが同じ差分を作るので保てない**。⚠️ CLAUDE.md #71 の「翌朝のcronに任せる」は**cron停止中(2026-08-18〜)で使えない**) ③⚠️ **§7・§8-3 に残っていた「475ファイル」を495に訂正**(第3版の訂正漏れ・レビューの参考指摘) ④§4-3 のドリフト検査を「**495ファイルの内側も見る**」手順に訂正・§6 に決定性の限定を追記 |
| 2026-08-26 | 第3版 | ✅ **実装済み(`a7125896`・2026-08-21)を別セッションが追認検証**し、V1〜V9を全件やり直した(報告: `実装報告_2026-08-26.md`)。⚠️ **2点を訂正** — ①**変更ファイル数は475ではなく495**(末尾空白の `trim` だけが変わるカード**20枚**を§1-2が数え漏らしていた。§4-1の `pick` 仕様どおりの挙動で**実装の逸脱ではない**) ②**V6の期待値「バイト一致」は成立しない**(本タスク外の `newestFirst()` が配列順に依存するため。⭐ **フレーバー文面は2,495枚すべて一致**＝設計意図は達成)。V8の例 `crimson-vein` は**和訳フレーバーを持たない**ため差し替え |

## 実施状況

| 項目 | 状態 |
|---|---|
| §1 症状と実測 | ✅ 設計時に完了 |
| §2 原因(消費側3箇所の食い違い・決定性の問題) | ✅ 設計時に完了 |
| §3 方針判断(英語原文を出すか) | ✅ **出す**で確定(✅ **2026-08-21 ユーザー承認済み**) |
| §4-1 `card-i18n.js` に `flavorOf()` を新設 | ✅ **実装済み**(`a7125896`) |
| §4-2 消費側3箇所を `flavorOf()` に寄せる | ✅ **実装済み**(`a7125896`)。⭐ 表示経路が3箇所だけであることも追認済み |
| §4-3 生成物の再生成(**495**ファイル) | ✅ **実装済み**(`a7125896`)。⚠️ **475ではなく495**(→ §4-3の注) |
| §5 検証項目 V1〜V9 | ✅ **全件合格**(2026-08-26・別セッションによる追認検証)。⚠️ V2は期待値を読み替え・V6は判定方法を訂正 |
| **§設計確認（承認）** | ✅ **2026-08-26 承認**（`承認_2026-08-26.md`）。⭐ **`push待ち`** |
| §7 push | ⬜ **ユーザー判断待ち**(⚠️ **本番配信物・計498ファイル**)。⚠️ GitHubアカウント停止中でpush不可。⚠️ ⭐ **本タスクと無関係な差分が14ファイル同梱される**(→ §7。**うち5件は `og:image` が変わる**。ユーザー判断で残すことが決定済み) |

**モックなし**(既存の「フレーバーテキスト」枠に文字が入るだけで、レイアウトの変更を含まないため)。

---

## 1. 症状と実測

### 1-1. 何が起きているか

公式APIのフレーバーテキストは **2箇所**に入りうる:

| 置き場 | 枚数(2,494枚中) |
|---|---:|
| トップレベル `card.flavor` が非空 | 614 |
| `card.flavor` は無い/空白だが **`editions[].flavor` が非空** | **586** |
| どこにも非空が無い | 1,294(うち**2枚は空白1文字**を持つ) |

サイト側は **`card.flavor` しか見ていない**ため、586枚は**画面のどこにも出ない**
(うち112枚は和訳が入ったので日本語だけが出ている＝§1-4)。

### 1-2. 実測(2026-08-20・スナップショット2,494枚)

⭐ 骨子(`issue反映待ち_2026-08-19.md`)の数字を**完全に再現した**。

| 状態 | 枚数 |
|---|---:|
| 表示されている(原文 614 + 和訳 112) | **726** |
| ⚠️ **原文が版側にしか無く、和訳も無いため何も出ない** | **474** |
| ⚠️ **中身が無いのに空白1文字のせいで空枠が出る** | **1**(`virgil-altered-future`) |
| フレーバー自体が無い | 1,293 |

⭐ 骨子の「726 / **473** / **2**」との差は **`zinn-volnia-abbess` をどちらに数えるか**だけで、
**同じ実測**である(このカードは `card.flavor` が空白1文字・`editions[].flavor` に原文があるため、
**空枠が出ていると同時に原文が出ていない**)。本設計書は**原文の有無**を主として上表の数え方を採る。

修正後に出力が変わるカードは ~~**475枚**~~ → **495枚**である。
⚠️ **初版の「475」は数え漏らしだった**(2026-08-26の追認検証で判明):

| 変化の種類 | 枚数 | 初版 |
|---|---:|---|
| 新たに原文が出る(版側にしか無かった) | **474** | ⭕ 数えていた |
| 空枠が消える(`virgil-altered-future`) | **1** | ⭕ 数えていた |
| ⚠️ **前後の空白が `trim` されるだけ**(文面は変わらない) | **20** | ❌ **数え漏らし** |
| **合計** | **495** | 475 |

⭐ trim の20枚は**すべて `card.flavor` 側**(版側ではない)で、末尾に空白・改行を持つカード
(例: `reduce-to-ash` = `"Ashes to ashes. Dust to dust. "`)。差分は
`<p class="cp-flavor">…dust. </p>` → `…dust.</p>` の**末尾空白の除去のみ**。
**§4-1 の `pick` が `v.trim()` を返す仕様どおりの挙動**であり、
上表が「版側にしか無い474枚」の外側にあるため見落とした。**実装の逸脱ではない**。
**495枚すべてに `cards/<slug>/index.html` が実在する**ことを確認済み(＝差分は495ファイル)。

### 1-3. 空枠2枚の中身

| slug | `card.flavor` | `editions[].flavor` | 修正後 |
|---|---|---|---|
| `zinn-volnia-abbess` | `" "`(空白) | `“If the price of brilliance is to burn, then so be it.”` | **原文が出る** |
| `virgil-altered-future` | `" "`(空白) | `" "`(空白) | **枠ごと消える** |

⚠️ **`||` だけでは直らない。** `" "` は truthy なので、空白を falsy として扱う判定が要る。

### 1-4. なぜ今まで気づかなかったか

⚠️ **和訳を入れると出るようになるため、翻訳作業では露見しない。**
#73 バッチ2で96枚のフレーバーが表示されるようになったのは**訳を入れたからで、経路が直ったわけではない**。

⭐ **同じ「未訳」でも表示が割れる**のがこの不具合の本質: フレーバーがトップレベルにあれば
英語原文が出るのに、`editions[]` 側にしか無いと**何も出ない**。

---

## 2. 原因

### 2-1. フレーバーの取り出し方が消費側3箇所で**バラバラ**

| # | 場所 | 現在の式 | `editions` を見るか |
|---|---|---|---|
| 1 | `scripts/build-card-pages.mjs:368`(静的カードページ) | `(t && t.flavor) \|\| card.flavor` | ❌ |
| 2 | `shared/js/card-detail.js:237`(詳細モーダル) | `(t && t.flavor) \|\| card.flavor` | ❌ |
| 3 | `shared/js/card-i18n.js:335`(`backFace()`・裏面) | `b.flavor \|\| bed.flavor \|\| null` | ⭕ **見ている** |

⭐ **3 だけが正しい形を知っていた**。つまり「版側にもフレーバーがある」ことは実装済みの箇所があり、
**1・2 に伝わらなかった**のが取りこぼしの実態。

⚠️ **ただし 3 は描画されていない。** `backFace()` は `flavor` を組み立てているが、
詳細モーダルの裏面ブロック(`card-detail.js:325〜`)も静的ページの裏面ブロック
(`build-card-pages.mjs:389〜`)も**名前・画像・効果しか出さない**。**死んだフィールド**である
(→ §8-2)。

### 2-2. 骨子の修正案では **1箇所に寄せられていない**

骨子は「両方の消費側を同じ形に」としていたが、**同じ式を2箇所にコピーする**形だった。
⚠️ それでは**3箇所目(`backFace`)との食い違いが残り**、次に増えたときまた割れる。

⭐ このリポジトリには**先例がある**: シーズン禁止の判定は
**`seasonalBanState()` 1箇所**に集約され、ブラウザ5面とビルドが共用している(CLAUDE.md)。
`shared/js/card-i18n.js` は **ブラウザとビルドの両方が評価する共有モジュール**
(ビルドは `scripts/lib/page-i18n.mjs` 経由)なので、**ここに関数を1本置けば3箇所とも同じ規則で動く**。

### 2-3. ⚠️ **「最初の非空を採る」は決定的ではない**(骨子に無い論点)

実測: **110枚は版によってフレーバーの文面が違う**(同一文面の重複は別に96枚)。

```text
acolyte-of-cultivation
  AMB(2024-10-11) → "The pursuit of transcendence by any means…"
  P25(2025-02-06) → "Heed the call of the immortal."
```

⚠️ **`editions` の配列順は公式API側で入れ替わる**(CLAUDE.md・#71 実測:
「既存カードの `editions` 配列の並び替え・画像URLの入れ替えが無関係な差分として現れる」)。
配列順で拾うと、**中身が変わっていない日に生成物の差分が出て、cronが毎日ノイズコミットを作る**
(「出力は決定的でなければならない」に反する)。

実測: **配列順ルールと初出順ルールで結果が変わるのは65枚**。⭐ **無視できない規模**。

---

## 3. 方針判断: 日本語サイトに英語原文を出すか

⭐ **出す**(＝473枚に英語フレーバーを表示する)。✅ **2026-08-21 ユーザー承認済み**。根拠:

1. **既存の挙動がすでにそうなっている。** 現在表示されている726枚のうち、**日本語訳があるのは117枚だけ**で、
   **残り609枚は英語原文がそのまま出ている**。出さない選択は**現状の726枚から英語を消す**ことになり、
   影響が逆に大きい
2. `data/translations.js` 冒頭に明記された設計思想 —
   **「訳が無いカードは英語原文のまま『翻訳募集中』バッジで表示されます」**
3. **表示の割れを直す**のがこのissueの目的で、「原文があるのに版の置き場所によって出たり出なかったりする」
   という**一貫性の欠如**こそが不具合

~~⚠️ **ユーザーが承認時に「出さない」と決めた場合**でも、**空枠2枚(§1-3)は直す**。
その場合の変更は `virgil-altered-future` / `zinn-volnia-abbess` の2ファイルのみになる。~~
→ ✅ **2026-08-21 に「出す」で承認されたため、この代替案は不採用**(§8-3)。

---

## 4. 変更内容

### 4-1. `shared/js/card-i18n.js` に `flavorOf(card, t)` を新設し、公開する

```js
// フレーバーテキストの取り出し(#未採番)。
// ⚠ 置き場が2つある: トップレベル card.flavor と版ごとの editions[].flavor。
//    後者しか持たないカードが473枚あるため、両方を見ないと「原文があるのに出ない」状態になる。
// ⚠ 空白1文字(" ")を持つカードが実在する。truthy なので || では弾けず、空の枠が描画される。
// ⚠ 版によって文面が違うカードが110枚ある。editions の配列順は公式API側で入れ替わるため、
//    配列順で拾うと出力が非決定的になり、cronが毎日ノイズコミットを作る。→ 初出の版に固定する。
function flavorOf(card, t) {
  const pick = (v) => (typeof v === "string" && v.trim()) ? v.trim() : "";
  const jp = pick(t && t.flavor);
  if (jp) return jp;
  const own = pick(card && card.flavor);
  if (own) return own;
  const eds = ((card && card.editions) || []).filter((e) => pick(e.flavor));
  if (!eds.length) return "";
  // 初出(release_date の昇順)。⚠ 1970-01-01 は API 未設定を意味するので「不明」として末尾へ。
  // 同着は edition.slug の昇順で決める(slug は版ごとに一意なので必ず決着する)。
  const key = (e) => {
    const d = (e.set && e.set.release_date) || "";
    return (!d || d.startsWith("1970")) ? "9999" : d;
  };
  eds.sort((a, b) => (key(a) < key(b) ? -1 : key(a) > key(b) ? 1
    : String(a.slug) < String(b.slug) ? -1 : 1));
  return pick(eds[0].flavor);
}
```

- **戻り値の型を `""` に統一する**(現状は `null` / `undefined` / `" "` が混ざる)
- ⚠️ **`return` 文の並びに `t` を先に置く**(日本語訳が最優先。現状の優先順を変えない)
- 公開: モジュール末尾の `return { … }` に `flavorOf` を追加する

**実測で決めた規則の裏付け**:

| 論点 | 実測 | 決めたこと |
|---|---:|---|
| 版ごとに文面が違うカード | **110枚** | 配列順は使わない |
| 配列順ルールと初出順ルールで結果が違う | **65枚** | 初出順に固定 |
| `release_date` が `1970-01-01`(API未設定)の版を含む | **41枚** | 1970は「不明」として**末尾**へ |
| 最古が同着で文面が違う | **13枚** | `edition.slug` の昇順で決着 |

### 4-2. 消費側3箇所を `flavorOf()` に寄せる

| # | 場所 | 変更 |
|---|---|---|
| 1 | `scripts/build-card-pages.mjs:368` | `const flavor = CI.flavorOf(card, t);` |
| 2 | `shared/js/card-detail.js:237` | `const flavor = flavorOf(card, t);` |
| 3 | `shared/js/card-i18n.js:335`(`backFace`) | `flavor: flavorOf(b, null) \|\| pick(bed.flavor) \|\| null` ⚠️ 裏面は `b.editions` を持たないため、**版側は `bed`(その版)から拾う**形を維持する |

⚠️ **`build-card-pages.mjs` の `flavorBlock` は条件式を変えない**
(`flavor ? … : ""` のまま。`flavorOf` が `""` を返すので空枠は自動的に消える)。

### 4-3. 生成物の再生成

```bash
node scripts/gen-tl-json.mjs   # ⚠ 不要(翻訳データは変えないため)。実行しても差分は出ない
npm run build:cards            # ⭐ 差分は cards/**/index.html が495ファイル(⚠ 初版の475は誤り=§1-2)
```

⚠️ **`build:cards` の前にスナップショットキャッシュを温める**(CLAUDE.md・#73の教訓)。
期限(既定60分)が切れていると**APIを取り直し、無関係なカードの `editions` 並び替えが差分に混ざる**
(#71 では31ファイルが混入し、レビューM7で差し戻しになった)。

⚠️ ⭐ **ドリフト検査は「ファイル数」だけでは不十分**(第4版で訂正。**この指定の穴が実際に M7 不合格を招いた**)。

| 見るもの | 検査 | 見落とすもの |
|---|---|---|
| ❌ 旧: 差分が**495ファイル以外**に出たか | ファイル名の集合 | ⚠️ **495ファイルの内側に同居するドリフト** |
| ✅ 新: 495ファイル**それぞれの差分の中身** | 下記コマンド(フレーバー節を除去してなお残る差分) | — |

⚠️ **1つのファイルの中でフレーバーの差分とドリフトが同居しうる**。
親コミットの生成物は**前回cron時点のAPI応答**で作られているので、
**前回cron〜今回のスナップショット取得の間にAPI側が動いたページ**が再生成対象と重なると、内側に入り込む。

```bash
# 対象コミットの cards/** から、フレーバー以外の差分が残るファイルを数える(作業ツリーを変更しない)
git show --name-only --format= <commit> -- cards | while read f; do
  diff -q <(git show "<commit>^:$f" | perl -0pe 's|<section class="cp-block"><h2>フレーバーテキスト</h2>.*?</section>||s') \
          <(git show "<commit>:$f"  | perl -0pe 's|<section class="cp-block"><h2>フレーバーテキスト</h2>.*?</section>||s') \
    >/dev/null || echo "$f"
done
```

⚠️ **2026-08-26 の追認検証時点では、cron停止(2026-08-18〜)による陳腐化で
無関係な差分が67件出る**(公式裁定セクションが30ページ分未反映・新規カード `jinzhuo-bands-of-virtue` が未生成)。
⭐ **フレーバーの495ページとは積集合0件**なので**外側には**混ざらないが、**再生成したら差し戻すこと**。
⚠️ **内側には14ファイル混ざっている**(→ §7。処置はユーザー判断で「残す」に決定済み)。

---

## 5. 検証項目

| # | 内容 | 期待値 |
|---|---|---|
| **V1** | 実装前に §1-2 の数字を追認(スナップショットを取り直して再測定) | 726 / 473 / 2。⚠️ ずれていたら**改版してから**実装する ✅ **2026-08-26 再現**(726=原文609+和訳117 / 473 / 2) |
| **V2** | `npm run build:cards` 後の `git status --short cards/ \| wc -l` | **495**(⚠️ 初版の475は誤り＝§1-2。それ以外はドリフト＝§4-3)。⚠️ **コミット後は差分0になるため再現不可** — その場合は「**同じスナップショットで再生成してコミット済み495ページとバイト一致**」に読み替える ✅ **2026-08-26 合格** |
| **V3** | `cards/zinn-volnia-abbess/index.html` | 空枠が消え、`“If the price of brilliance is to burn…”` が入る ✅ **2026-08-26 合格** |
| **V4** | `cards/virgil-altered-future/index.html` | ⭐ **`cp-flavor` の要素ごと消えている**(空の `<p>` が残っていない) ✅ **2026-08-26 合格**(`cp-flavor` 0回・見出しも0回) |
| **V5** | ⭐ **決定性**: `build:cards` を**同じキャッシュで2回**実行 | 2回目の差分が**0ファイル**(バイト一致) ✅ **2026-08-26 合格**(2,560ファイルがmd5一致) |
| **V6** | ⭐ **決定性(順序)**: `editions` を逆順にしたスナップショットで生成 | ~~**出力がバイト一致**~~ → ⚠️ **2026-08-26 訂正: ページ全体のバイト一致では判定できない**。逆順にすると**581ファイルが変化する**が、原因は**本タスク外の `newestFirst()`**(下の注)。⭐ 正しい期待値は「**`<p class="cp-flavor">` の文面が全2,495枚で一致**」。⚠️ **この項目が §2-3 の要** ✅ **2026-08-26 合格**(文面は完全一致) |
| **V7** | Playwright実駆動・詳細モーダル(トップから `zinn-volnia-abbess` を開く) | フレーバー欄に原文が出る。⚠️ **静的ページと同じ文面**であること(消費側の一致確認) ✅ **2026-08-26 合格** — `acolyte-of-cultivation`(版で文面が違う110枚の1枚)が静的・モーダルとも**初出のAMB版** `The pursuit of transcendence by any means.` |
| **V8** | Playwright実駆動・**日本語訳があるカード**(~~例: `crimson-vein`~~) | **日本語フレーバーが出る**(優先順が変わっていない)。⚠️ **2026-08-26 訂正: `crimson-vein` は和訳フレーバーを持たない**(`flavorOf` が `""` を返す)ので検証に使えない。⭐ 優先順の2経路を**両方**見る: **`cato-meadows-channeler`**(和訳 > `card.flavor`・該当5枚)と **`aenean-cyclone`**(和訳 > `editions[].flavor`・該当112枚) ✅ **2026-08-26 合格** |
| **V9** | `npm run validate` | **exit 0** ✅ **2026-08-26 合格** |

⚠️ **V6 が「バイト一致」にならない理由**(2026-08-26判明・**本タスクの対象外**):
`scripts/build-card-pages.mjs:153` の `newestFirst()` が **`release_date` だけで比較し、同着時の
第2キーを持たない**ため、**同着の版は入力配列の順序をそのまま保つ**。その結果
**収録セット表の行順・代表画像・`og:image`** が `editions` の配列順に依存する。
⭐ これは §2-3 が `flavorOf()` について解いた問題と**同じ型が隣の経路に残っている**もので、
**#71 で観測された「無関係なカードページ・セットページの差分」の発生源**である可能性が高い。
→ **別issueの候補**(`edition.slug` を第2キーに足せば解ける見込み)。
⚠️ `flavorOf()` 自体は**自前で昇順ソートし直し同着を `slug` で決着させる**ため影響を受けない。

⚠️ **V7 の「静的ページと同じ文面」は、110枚(版で文面が違う)のうち1枚を選んで確認する**。
どちらか片方だけ直すと**画面によって別のフレーバーが出る**という、より分かりにくい不具合になる。

---

## 6. 保証の範囲と、残るリスク

| 守れること | 守れないこと |
|---|---|
| 原文フレーバーが**置き場所によらず**出る | ⚠️ **どの版のフレーバーを出すかは「初出」に固定**。ページで版を切り替えても**フレーバーは変わらない**(→ §8-1) |
| **フレーバーの選択**が決定的(`flavorOf()` は `editions` の配列順に依存しない。実測: 1,248枚をシャッフルしても文面は1,200ページ全て不変) | ⚠️ 公式APIが `release_date` を後から埋めると、**その版が初出に繰り上がって文面が変わる**日がありうる(41枚が該当。⭐ 変化は1回きりで、cronが翌朝に自動追従する) |
| — | ⚠️ ⭐ **決定的なのは `flavorOf()` の部分だけ。ページ全体は依然として非決定的**(第4版で追記) — `newestFirst()`(`build-card-pages.mjs:153`)が `release_date` **だけ**で比較し**同着の第2キーを持たない**ため、公式APIが `editions` を並べ替えた日に**代表画像・`og:image`・収録セット表の行順**が無関係に変わる。**本タスクのスコープ外**(→ 別issueの候補。§7の14ファイルのうち7件がこの実害) |
| 静的ページとモーダルが**同じ文面**になる | ⚠️ **裏面のフレーバーは引き続き表示されない**(現状維持。→ §8-2) |

---

## 7. push の扱い

⚠️ **本番配信物。`docs/` 例外は使えない。**

- 変更: `shared/js/card-i18n.js` / `shared/js/card-detail.js` / `scripts/build-card-pages.mjs` /
  `cards/**` **495ファイル**(合計 **498ファイル / +547 −541**。⚠️ **第3版までの「475」は誤り**=§1-2)
- ⭐ **新規ファイルが1つも無い**ので、**デプロイ到達マーカーが使えない**(CLAUDE.md)。
  反映確認は**素のURLと `?cb=` を並べて20秒間隔で反復**し、全数一致するまで見る
  (実績: 6分・18回で確定。ドキュメントのみなら2分)
- ⭐ 確認に使うURLの候補: `/cards/zinn-volnia-abbess/`(**空枠→原文**なので新旧の判別が明確)

### ⚠️ このコミットには本タスクと無関係な差分が **14ファイル** 含まれる(第4版で追記)

⭐ **push の可否を判断するときに、必ずこの節を読むこと。** `a7125896` の `cards/**` 495ファイルのうち
**14ファイルは、フレーバー以外の差分も同時に含んでいる**(レビュー判定 `343c3c59` の M7-1 で検出)。
⚠️ **原因は、親コミットの生成物が cron最終稼働 2026-08-18 時点のAPI応答で作られているのに対し、
本コミットが 2026-08-21 取得のスナップショットで再生成されたこと**。フレーバーの修正対象と、
その3日間にAPI側が動いたページが**同じファイルの中で重なった**。

| 種別 | 件数 | 内容 | 公開面への影響 |
|---|:--:|---|---|
| **A. 公式裁定(`rule`)の増加** | **6** | `hoarfrost-spine` / `pawn-piece` / `siphoning-stab`(いずれも0→1件・`<meta name="description">` も「収録セット・**裁定**」に変化) / `incendiary-shot`(2→3) / `rowland-schwartz-knight`(1→2) / `galahad-court-knight`(2→3)。⭐ **6件とも追加された裁定の `date_added` は 2026-08-16**(公式の同一改定バッチ) | 裁定が**早く出るだけ**。cron復旧後に同じ内容が入る |
| **B. `editions` の配列順に由来** | **7** | ⚠️ **`og:image`・`twitter:image`・本文の代表画像が別の版に入れ替わる 5件**: `break-apart`(RDOPD #048C→**#048A**) / `gentle-respite`(→RDOPD #066A) / `overlapping-visages`(→ReC-BRV #025A) / `spirit-of-fortuitous-wind`(→RDO 1st #009B) / `wandering-glaivier`(→RDOPD #151B)。**収録セット表の行順のみ 2件**: `disenchant` / `pantheon-barrier` | ⚠️ ⭐ **5件はSNSカードの絵柄が変わる**。原因は `newestFirst()` の同着非決定性(→ §6)で、**放置するとAPIが並べ替えるたびに再発する** |
| **C. 公式API側のデータ訂正** | **1** | `prismspire-scepter` の収録セット表 **P25 #165 → #164**(`collector_number` の訂正。⭐ **画像URLは同一・行順も不変**＝並び順の問題ではない) | 表示上の採番が正しくなる |

⭐ **処置: 剥がさず残す**(**2026-08-26・ユーザー判断**)。理由は2点:

1. ⭐ **手で剥がすと「どのビルドでも再現しない生成物」になる。** 同一スナップショット
   (md5 `297ab372fc9b2cd4a82485cdbde1e3f5`)から再生成すれば**必ずこの内容になる**ことはレビューのM4で実証済み。
   ⚠️ **フレーバーの差分と同居しているため、ファイル単位で親に戻すと修正ごと消える**
2. ⭐ **GitHub復旧後の初回cronが結局同じ差分を作るため、剥がしても保てない。**
   ⚠️ 通常なら CLAUDE.md の「無関係な差分はタスクのコミットに混ぜず、翌朝のcronに任せる」(#71)が使えるが、
   **cronが 2026-08-18 から止まっている今はその逃げ道が無い**(「翌朝」が来ない)。
   よって**「混ぜない」ではなく「混ざっている事実を正確に記録する」で処理する**

⚠️ **前例 `e6023abc`(#71)は復元を選んだが、今回は上の1の理由で同じ手が使えない**。

→ **`push待ち` ラベルを付けてユーザーの判断を待つ**。

⚠️ **#73 の派生issueがもう1件ある**(`docs/design/未採番-辞書の抜けと検出経路/`)。
どちらも `scripts/` と `shared/` に触るため、⭐ **同時に走らせない**(WIP=1)。

---

## 8. 未決事項

### 8-1. ページの版切り替えにフレーバーを追従させるか(**今回はやらない**)

カードページと詳細モーダルには**版(イラスト)の切り替え**がある(`cp-arts` / `#art=<set-slug>`)。
**110枚は版ごとに文面が違う**ので、本来は選択中の版のフレーバーを出すのが正しい。

⚠️ ただし**静的ページでは版ごとの文面をすべて埋め込む必要があり**、
**この不具合(何も出ない)の修正とは別の機能追加**になる。→ **別issueに切り出す**。

### 8-2. 裏面のフレーバーを表示するか(**今回はやらない**)

`backFace()` は `flavor` を組み立てているのに、**表示する側が無い**(§2-1)。
死んだフィールドを消すか、裏面ブロックに表示を足すかの判断が要る。→ **別issueに切り出す**。

### 8-3. ~~英語原文を出さない選択をする場合~~ — ✅ **不採用が確定**(2026-08-21)

⚠️ **以下は採らない案の記録**。ユーザーが §3 の推奨(出す)を承認したため、**495ファイルを再生成する本来の方針で実装する**。

§3 のとおり**出す**方針だが、ユーザーが覆した場合の変更は**空枠2枚のみ**になる
(`flavorOf()` の `editions` 探索を落とし、空白トリムだけを入れる)。
⭐ その場合でも **`flavorOf()` を1箇所に置く**設計は維持する(§2-2 の理由は変わらない)。
