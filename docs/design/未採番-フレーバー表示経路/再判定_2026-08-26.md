# 未採番（フレーバー表示経路）レビュー**再判定**（差し戻し1往復目の対応に対して）

⚠️ **本来は該当issueのコメント。GitHubアカウント停止中で `gh issue comment` が打てないため、
代替としてこのファイルに記録する**（先行事例: `docs/design/未採番-辞書の抜けと検出経路/レビュー判定_2026-08-21.md`）。
復旧後、本文をそのまま
`gh issue comment <n> --body-file docs/design/未採番-フレーバー表示経路/再判定_2026-08-26.md` で投稿する。
⚠️ ラベル操作は未実施（issueが存在しない）。**本判定は合格なので、本来の付け替え先は
`レビュー待ち` → `設計確認待ち`**（`gh issue edit <n> --remove-label レビュー待ち --add-label 設計確認待ち`）。

- 判定日: **2026-08-26**（前回判定: `343c3c59`・**不合格 M7**）
- 今回の対象コミット: **`91d5cc19`**（`docs/design/未採番-フレーバー表示経路/` の2ファイルのみ / +208 −12）
- 実装コミット: **`a7125896`**（⚠️ **1バイトも変わっていない**。下記で機械的に確認）
- 往復回数: **1往復目の対応**（2往復の上限内）
- 証跡: `tmp/review/未採番-フレーバー/`（前回の36枚に加え、今回の検算スクリプト）

---

## 判定: **合格**

**M7-1（唯一の不合格理由）は解消した。** 対応は**記録の訂正のみ**で、⭐ **レビュー担当が求めた
「事実を書く」「処置は自分で決めずに設計担当／ユーザーへ上げる」の両方が守られている**。
さらに ⭐ **開発担当が指摘を鵜呑みにせず自分で抽出し直し、レビュー担当の分類の誤りを1件見つけて
訂正している**（`prismspire-scepter`。下記のとおり**開発担当が正しい**）。

### 必須基準

| 基準 | 結果 | 証跡 |
|---|:--:|---|
| **M1** 設計適合 | ✅ | 前回合格を維持。⭐ **コードと生成物が `a7125896` から1バイトも変わっていないこと**を確認: `git diff --stat a7125896 HEAD -- cards shared scripts sets sitemap.xml index.html data` → **出力0行** |
| **M2** 実駆動 | ✅ | 同上（描画に影響する変更が無いため再実施不要と判断。前回スクショ36枚が有効） |
| **M3** 回帰 | ✅ | ⭐ **`npm run validate` を再実行 → exit 0**（`all checks passed.`） |
| **M4** 生成物の同期 | ✅ | 前回合格を維持（生成物は不変。スナップショット md5 も不変） |
| **M5** 静かな劣化がない | ✅ | 前回合格を維持。⭐ **むしろ検出経路が1つ増えた** — 設計書 §4-3 のドリフト検査が「ファイル数を見る」から「**495ファイルそれぞれの中身から、フレーバーセクションを除去してなお残る差分を見る**」に差し替わった。⚠️ **同じ穴（外側しか見ない）に次の人が落ちない形になっている** |
| **M6** 公開安全性 | ✅ | `91d5cc19` は **`docs/` 配下2ファイルのみ**（`git show --name-only \| grep -v '^docs/'` → 0件）＝**本番配信物の増減なし**。⭐ **さらに改善** — 設計書 §7 に「このコミットには無関係な差分が14ファイル含まれる／うち5件は `og:image` が変わる」が明記され、**push可否を判断するユーザーが設計書だけで気づける** |
| **M7** スコープ | ✅ | ⭐ **解消。** M7 が求めるのは「設計書に無い変更が入っていない、**入っているなら根拠がissueに書かれている**」。14ファイルの混入は**実装報告 §4-4（内訳）・§4-5（処置とユーザー判断）・設計書 §7（push判断者向け）** に記録された。**内容はレビュー担当が全件検算して事実と一致**（下表） |

### 推奨基準

前回と同じ（S1 ✅ / S2 ✅ / S3 ✅）。コードが不変のため再評価しない。

---

## ⭐ `prismspire-scepter` の分類 — **開発担当が正しい。前回判定の内訳を訂正する**

進行役から名指しされた論点。**報告を読む前に自分で測り直した**結果:

| 測ったこと | 実測 |
|---|---|
| 収録セット表の**行順** | **不変**（3行とも同じ順序: RDOA #015 → ReC-BRV #008 → P25） |
| 行の**中身** | ⚠️ **P25 の番号だけが `#165` → `#164`** に変化 |
| **代表画像**（`cp-main-img`）・`og:image` | **完全に同一**（`w7pJvr9qtg.jpg`） |
| スナップショット側の実データ | `prismspire-scepter-p25` の `collector_number` が **`164`** |

⭐ **＝ `editions` の並び順ではなく、公式APIが `collector_number` を訂正したもの。**
**`newestFirst()` に第2キーを足してもこの差分は消えない**（並び順に一切依存していないため）。

⚠️ **前回判定（`343c3c59`）の「`editions` 順序由来 8件」は誤りで、正しくは
「順序由来 **7件** / API側のデータ訂正 **1件**」である。ここに訂正する。**
（件数の合計14と、M7-1 の指摘そのものは変わらない）

⭐ **レビュー担当としての所見**: 分類の粒度がこの精度で問われるのは、**「`newestFirst()` を直せば
将来この種の差分は消えるのか」という判断に直結する**ため。**1件は消えない**ので、
`newestFirst()` の別issueで「これで全部直る」と書かないこと。

⚠️ **なお、報告 §4-4 分類C が根拠に挙げた画像URL `ywaIvocRGG.jpg` は、
代表画像ではなくP25版の画像**（ファイル内に新旧とも1回ずつ出現・不変）。
**主張（画像は同一・採番だけ変化）は正しい**が、`w7pJvr9qtg.jpg`（代表画像）と併記しておくと誤読が減る。
⭐ **合否には影響しない**ので指摘のみ。

---

## 訂正された記録の検算（レビュー担当が全件、自分で測り直した）

⚠️ **開発担当の表を読み合わせたのではなく、スナップショットと `git show` から独立に導出して突き合わせた。**

### 分類A: 公式裁定の増加 6件 — ✅ 記載どおり

| slug | 件数の変化 | **追加された裁定の日付（実測）** |
|---|:--:|---|
| `hoarfrost-spine` / `pawn-piece` / `siphoning-stab` | 0 → **1** | **2026-08-16** |
| `incendiary-shot` | 2 → **3** | **2026-08-16** |
| `rowland-schwartz-knight` | 1 → **2** | **2026-08-16** |
| `galahad-court-knight` | 2 → **3** | **2026-08-16** |

⭐ **「6件とも `date_added` は 2026-08-16（公式の同一改定バッチ）」という記載は正しい**（6/6 一致）。
`<meta name="description">` が「収録セット・**裁定**」に変わるのが前3件だけ、という記載も一致
（残り3件は元から裁定があったため）。

### 分類B: `editions` の配列順に由来 7件 — ✅ 記載どおり

代表画像が変わる5件について、**旧・新それぞれの代表画像がどの版のものか**を実データから逆引きした:

| slug | 旧代表 → 新代表（実測） | 記載 | 最新発売日が**同着**か |
|---|---|:--:|---|
| `break-apart` | RDOPD **#048C → #048A** | ⭕ 一致 | ⭕ #048A/B/C = 2026-04-03 |
| `gentle-respite` | RDOPD **#066B → #066A** | ⭕ 一致 | ⭕ #066A/B・RDOEVP #011 = 2026-04-03 |
| `overlapping-visages` | ReC-BRV **#025B → #025A** | ⭕ 一致 | ⭕ #025A/B = 2025-12-05 |
| `spirit-of-fortuitous-wind` | RDO 1st **#009A → #009B** | ⭕ 一致 | ⭕ #009A/B/C = 2026-04-03 |
| `wandering-glaivier` | RDOPD **#151A → #151B** | ⭕ 一致 | ⭕ #151A/B = 2026-04-03 |

⭐ **5件とも「最新発売日が同着の版どうしの入れ替わり」**＝ `newestFirst()` に第2キーが無いことの
実害であることを、レビュー担当も独立に確認した。行順のみの2件（`disenchant` #055A⇄#055B /
`pantheon-barrier` PP1 #059系）も同着（それぞれ 2026-04-03 / 2026-03-28）で一致。

- ⚠️ 些細な点（**合否に影響しない**）: `pantheon-barrier` の PP1 は **`#059`・`#059A`〜`#059D` の5版**が
  同一発売日で、記載の「#059A〜D」は素の `#059` を落としている。**同着であるという結論は変わらない**

### 分類C: 公式API側のデータ訂正 1件 — ✅ 記載どおり（上記の詳細）

### 記録全体の一致

| 記録 | レビュー担当の検算 |
|---|:--:|
| 実装報告 §4-3 の訂正（「積集合0件」は計算は正しいが**外側しか見ていない**） | ⭕ **同意**。今日のドリフト67件と495ページの積集合が0件であることはレビュー担当も再現済み。⭐ 「何を測ったか／何を見落とすか」の表は**この失敗の構造を正しく一般化している** |
| 実装報告 §4-3 の「正しい検出手順」のコマンド | ⭕ **そのまま実行して14件を再現**（レビュー担当の環境で `wc -l` → **14**） |
| 実装報告 §4-4 の内訳（6 / 7 / 1） | ⭕ **全件一致**（上表） |
| 実装報告 §4-5（ユーザー判断「残す」と理由2点） | ⭕ 記載が判断内容と整合。⭐ **開発担当が自分で処置を決めていない**ことが明記されている（越権なし） |
| 設計書 §7 の新設節 | ⭕ 内容一致。⭐ **push判断者が読む場所に置かれている**のが要点 |
| 設計書 §7・§8-3 の 475→**495** | ⭕ 訂正済み。残る「475」は**すべて「誤りだった」と説明する文脈**で、生きた数字としての475は無い |
| 設計書 §6 の決定性の限定行 | ⭕ **重要な追記**。「出力が決定的」と言い切っていた行が「**決定的なのは `flavorOf()` の部分だけ。ページ全体は `newestFirst()` の同着で非決定的**」に是正された |
| 設計書 §4-3 のドリフト検査手順 | ⭕ 「内側も見る」形に差し替え済み（M5 の所見のとおり、**検出経路の改善**） |

---

## 設計担当への確認事項

⚠️ **いずれも合否を左右しない。** 合格判定はこれらと独立に確定している。

1. ⭐ **`newestFirst()`（`build-card-pages.mjs:153`）の順序非依存化を別issueに切り出すか**。
   ⚠️ **今回の14ファイルのうち7件がその実害**であることが確定した（レビュー担当も検算済み）。
   放置すると**公式APIが `editions` を並べ替えるたびに、無関係なカードの `og:image` が変わり続ける**。
   ⚠️ **ただし1件（`prismspire-scepter`）はこれでは直らない**ので、
   起票時に「これで全部直る」と書かないこと（⚠️ レビュー担当は起票しない）
2. **cron停止中に溜まった陳腐化（今日時点で カード58 + セット8 + `sitemap.xml` + 新規カード1件）**の扱い。
   ⭐ **本タスクに混ぜない判断は正しい**（積集合0件をレビュー担当も再現）。放置しても表示は壊れない
3. **§8-1（版切り替えへの追従）・§8-2（裏面フレーバー）**の起票要否は従来どおり設計担当の判断
4. ⭐ **CLAUDE.md への昇格候補**（レビュー中に気づいた点。⚠️ **レビュー担当は `CLAUDE.md` を書き換えない**）:
   **「生成物のドリフト検査は、変更ファイル数だけでは不十分。1ファイルの中でタスク由来の差分と
   ドリフトが同居しうる」**——⚠️ **cronが止まっている間は必ずこれが起きる**（親の生成物と
   スナップショットの時点がずれるため）。今は設計書 §4-3 にしか無い教訓で、
   **次のタスクの設計書には引き継がれない**

## 残っている確認（pushしないと確認できない項目・合否に含めない）

- 本番での表示確認。⚠️ **新規ファイルが1つも無い**ため到達マーカーが使えず、
  **素のURLと `?cb=` を並べて20秒間隔で反復**する必要がある（設計書 §7）
- ⚠️ **push そのものが不可**（GitHubアカウント停止中）。本コミット群は**本番配信物498ファイル**を含み、
  ⭐ **うち14ファイルには本タスクと無関係な差分（5件は `og:image` の変化）が含まれることを
  ユーザーが承知したうえでの push** になる（設計書 §7 に明記済み）

---

## レビュー担当の作業ログ

- 今回は**生成を1回も走らせていない**（コードと生成物が不変であることを `git diff` で確認したため）。
  ⭐ **スナップショットの md5 は `297ab372fc9b2cd4a82485cdbde1e3f5`**（退避 `tmp/dev/snapshot-backup/` と一致）
- `git checkout` / `stash` / `restore` / ブランチ切り替えは**未使用**。読んだのは `git show` / `git log` / `git diff` / `git status`
- ローカルサーバーは**起動していない**（前回のポート3100は停止済みのまま）
- 判定時の `git status --short` は**空**。⚠️ **ほかのセッションの `tmp/` 配下は削除していない**
