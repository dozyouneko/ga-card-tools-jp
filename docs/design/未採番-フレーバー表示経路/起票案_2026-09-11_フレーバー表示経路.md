**状態: 完了**（最終確認 2026-09-11）— 呼び名「フレーバー表示経路」。⭐ **起票案を持たずに設計書から始まった完了タスク**に、2026-09-11 に後から付けた起票案（「1タスク＝起票案1枚」の規約。→ [`索引の転記ドリフト防止_設計.md`](../未採番-索引の転記ドリフト防止/索引の転記ドリフト防止_設計.md) の D1）。完了の根拠は [`承認_2026-08-26.md`](承認_2026-08-26.md)。GitHub復旧後は「完了済みタスク」として起票する（⚠️ **`実装待ち` を付けない**）— ✅ **#86 として起票・クローズ済み**（2026-09-20） ／ 索引: [`待ち行列と復旧手順.md`](../待ち行列/待ち行列と復旧手順.md)

# issueの起票案（完了済みタスク）— `editions[].flavor` のフレーバーが表示されない（474枚）

⚠️ **これは設計書ではなく「issueの下書き」。** 実装・レビュー・承認はすべて済んでいる。
GitHub復旧後、**完了済みタスクの記録として**起票する。

- ⭐ **起票時**: タイトル「`editions[].flavor` のフレーバーが表示されない（474枚）」／
  ラベル `bug` `着手可` → 起票後は `push待ち`（索引 §0 の push が済んでいればクローズ）
- 設計書（正）: [`フレーバー表示経路_設計.md`](フレーバー表示経路_設計.md)
- 工程記録: 投稿順は `node scripts/gen-queue-index.mjs --posting-plan` が出す（索引 §2 ③）。
  ⚠️ **設計完了のコメントは存在しない**（設計書が起票より先に出来ていたため）＝本文に設計書のパスを書けば足りる
- ⚠️ **同じフォルダの [`派生起票案_2026-08-26.md`](派生起票案_2026-08-26.md)（呼び名「フレーバー表示の追補」）は別のタスク**（生きている）

## 要旨

フレーバーテキストの置き場はトップレベル `card.flavor` と版ごとの `editions[].flavor` の2箇所あり、
**版側にしか持たないカードで何も表示されていなかった**。取り出しを `shared/js/card-i18n.js` の
`flavorOf(card, t)` 1箇所に集約した（詳細は設計書・CLAUDE.md「フレーバーテキストの置き場は2箇所ある」）。
