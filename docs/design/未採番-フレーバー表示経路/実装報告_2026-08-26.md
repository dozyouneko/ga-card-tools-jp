# 未採番（フレーバー表示経路）実装報告 — **追認検証**

⚠️ **これは実装者本人による報告ではない。** 実装は **2026-08-21 のコミット `a7125896`** で既に
完了しているが、**当時の実施記録（issueコメント相当）が一切残っていない**。実装したセッションは
同日中に終了しており、**コンテキストは失われている**。本書は**別セッション（開発担当）が
2026-08-26 に検証項目 V1〜V9 を全件やり直して追認した記録**である。
前任の数値は**先に見ずに自分で測ってから突き合わせた**（§2 の「実測」欄はすべて本セッションの測定値）。

⚠️ **本来は該当issueのコメント。GitHubアカウント停止中（`gh auth status` がトークン無効）で
`gh issue comment` が打てないため、代替としてこのファイルに記録する**（先行事例:
`docs/design/未採番-辞書の抜けと検出経路/実装報告_2026-08-21.md` /
`docs/design/73-PRD新カード和訳/実装報告_ユニット2.md` と同じ扱い）。
復旧後、本文をそのまま `gh issue comment <n> --body-file docs/design/未採番-フレーバー表示経路/実装報告_2026-08-26.md` で投稿する。

- 検証実施日: **2026-08-26**（実装日: 2026-08-21）
- 参照した設計書: `docs/design/未採番-フレーバー表示経路/フレーバー表示経路_設計.md` **第2版**（§4-1〜§4-3 / §5 V1〜V9）
- ⚠️ ラベル操作は未実施（issueが存在しないため）。本来は `実装待ち` → **`レビュー待ち`**

---

## 1. 実施内容

### 1-1. 追認対象のコミット（本セッションでは変更していない）

**コミット `a7125896`**「未採番(フレーバー表示経路): editions[].flavor を拾う flavorOf() を1箇所に新設し、消費側3箇所を寄せる」

| 区分 | ファイル | 内容 |
|---|---|---:|
| コード | `shared/js/card-i18n.js` | `flavorPick()` / `flavorOf()` を新設・`flavorOf` を公開（+36 −2） |
| コード | `shared/js/card-detail.js` | 詳細モーダルを `flavorOf()` に寄せる（+2 −2） |
| コード | `scripts/build-card-pages.mjs` | 静的カードページを `CI.flavorOf()` に寄せる（+1 −1） |
| 生成物 | `cards/**/index.html` | **495ファイル**（⚠️ 設計書の想定は475。§4 で内訳を説明） |
| **合計** | | **498ファイル / +547 −541** |

- **push: していない**（GitHubアカウント停止中。かつ本番公開の可否は常にユーザー判断）
- ⚠️ **`push待ち` 相当**。本コミットは**本番配信物**（`shared/**`・`scripts/**`・`cards/**`）を含む（設計書 §7）

### 1-2. 本セッションが行った作業

| # | 手順 | 結果 |
|---|---|---|
| 1 | `a7125896` の差分を読み、設計書 §4-1〜§4-2 との一致を確認 | 一致（§5 に詳細） |
| 2 | **V1** をスナップショットから独自に再測定 | §2 のとおり再現 |
| 3 | **V2/V5**: `GA_SNAPSHOT_MAX_AGE_MIN=0 npm run build:cards` を2回実行 | §2 のとおり |
| 4 | **V6**: `editions` を逆順にしたスナップショットで生成し比較 | §2・§4-2 のとおり |
| 5 | **V3/V4/V9**: 生成物の検分と `npm run validate` | 合格 |
| 6 | **V7/V8**: Playwright実駆動（`http://localhost:3000`・開発担当の既定ポート） | 合格。⚠️ **サーバーは停止済み** |
| 7 | 検証で生じた**タスク無関係の差分67件を差し戻し**、作業ツリーを clean に戻した | §4-3 |

- ⭐ **コード修正は一切行っていない**（不具合が見つからなかったため）。本セッションのコミットは**本書と設計書の更新のみ**
- ⭐ **スナップショットは元のまま**: `tmp/api-cache/cards-snapshot.json` の md5 は
  検証の前後とも **`297ab372fc9b2cd4a82485cdbde1e3f5`**（退避 `tmp/dev/snapshot-backup/cards-snapshot-20260821.json` と一致）
- ⚠️ **`build:cards` は毎回 `GA_SNAPSHOT_MAX_AGE_MIN=0` を付けて実行**した。付けないと
  8/21取得のスナップショット（115時間前＝期限切れ）を**取り直しに行き**、
  **8/21発売のPRD新カードが大量に混入する**（#71 の再来）。全4回の実行とも
  stderr は `有効期限なし` を表示し、APIアクセスは発生していない

---

## 2. 検証結果（設計書 §5 の V1〜V9 に対応）

| # | 内容 | 期待値 | **実測（本セッション）** | 判定 |
|---|---|---|---|:--:|
| **V1** | §1-2 の数字を追認 | 726 / 473 / 2 | **726（原文609 + 和訳117）/ 473 / 2**（空枠= `virgil-altered-future`, `zinn-volnia-abbess`）。置き場の内訳も **`card.flavor` 非空 614 / 版側のみ 586** と§1-1に完全一致 | ✅ |
| **V2** | `build:cards` 後の `git status --short cards/` | 475 | ⚠️ **読み替えて実施**（§3-1）。**コミット済み495ページとの積集合 0件**＝**495ページすべてがバイト一致で再現**。コミット同梱数は `git show --stat a7125896 -- cards/` で **495**（⚠️ 設計書の475と食い違う→§4-1） | ✅ |
| **V3** | `cards/zinn-volnia-abbess/index.html` | 空枠が消え原文が入る | `<p class="cp-flavor">“If the price of brilliance is to burn, then so be it.”</p>` | ✅ |
| **V4** | `cards/virgil-altered-future/index.html` | `cp-flavor` の**要素ごと**消える | `cp-flavor` **0回** / 見出し「フレーバーテキスト」**0回** / 空の `<p>` **0件**。コミット差分は `-<section …><p class="cp-flavor"> </p></section>` の**行ごと削除** | ✅ |
| **V5** | 同じキャッシュで2回実行 | 2回目の差分0 | `cards/**` + `sets/**` + `sitemap.xml` の **2,560ファイルが md5 完全一致** | ✅ |
| **V6** | `editions` 逆順のスナップショットで生成 | 出力がバイト一致 | ⚠️ **期待値そのものは成立しない**（581ファイルが変化。原因は本タスク外の `newestFirst()`＝§4-2）。⭐ **本質である「フレーバー文面」は2,495枚すべてで完全一致**＝`flavorOf()` は配列順に依存しない | ✅※ |
| **V7** | Playwright・詳細モーダル | 原文が出る・**静的ページと同じ文面** | **`acolyte-of-cultivation`**（版で文面が違う110枚の1枚）が静的・モーダルとも **`The pursuit of transcendence by any means.`**＝**初出のAMB版**（P25版の `"Heed the call of the immortal."` ではない）。`zinn-volnia-abbess` も両者一致 | ✅ |
| **V8** | Playwright・和訳があるカード | 日本語フレーバーが出る | ⚠️ 設計書の例 `crimson-vein` は**和訳フレーバーを持たない**ため差し替え（§5-2）。**`cato-meadows-channeler`**（和訳 > `card.flavor`）・**`aenean-cyclone`**（和訳 > `editions[].flavor`）とも**日本語が出て静的・モーダル一致**＝**優先順は変わっていない** | ✅ |
| **V9** | `npm run validate` | exit 0 | **exit 0** / `all checks passed.`（`total cards: 2516` / 生成JSON・メタ索引・cron列挙の各検査を通過） | ✅ |

※ V6 は**期待値の書き方が過大**だったため、判定は「設計意図（§2-3 の決定性）を満たすか」で行った。詳細は §4-2。

### V7 / V8 の内訳（Playwright・`http://localhost:3000`）

| slug | 位置づけ | 静的ページ | 詳細モーダル | 一致 |
|---|---|---|---|:--:|
| `acolyte-of-cultivation` | ⭐ **版で文面が違う110枚の1枚** | `The pursuit of transcendence by any means.` | 同左 | ✅ |
| `zinn-volnia-abbess` | 空枠 → 原文（V3の画面側） | `“If the price of brilliance is to burn, then so be it.”` | 同左 | ✅ |
| `cato-meadows-channeler` | 和訳 > `card.flavor`（該当5枚） | `「今や、私は自分自身の主人だ。」` | 同左 | ✅ |
| `aenean-cyclone` | 和訳 > `editions[].flavor`（該当112枚） | `「子どもたちが目を覚ます前に、片づけてしまおう。」` | 同左 | ✅ |
| `virgil-altered-future` | 枠ごと消える（V4の画面側） | フレーバー枠なし | `#d-flavor-wrap` **非表示** | ✅ |

- **JSエラーなし**（Cloudflare RUM の CORS 警告のみ＝localhost固有・本タスクと無関係。先行事例と同じ）

---

## 3. 実行できなかった検証と代替手段・残っている確認

### 3-1. ⚠️ V2 は**期待値を読み替えて**実施した

設計書の V2「`git status --short cards/ | wc -l` が **475**」は、**実装が既にコミット済みである今日は
原理的に再現できない**（再生成しても差分は0になる）。そこで次の2つに読み替えた:

1. **同じスナップショットで再生成し、コミット済み495ページと突き合わせて差分0件**（＝バイト一致）
2. **コミットに同梱されたカードページ数を `git show --stat a7125896 -- cards/` で数える** → **495**

⭐ 1 は「生成物がコミットと一致している」ことを、2 は「変更範囲が想定どおりか」を見るもので、
**合わせれば元の V2 と同等以上の保証になる**（元の V2 は 2 しか見ていない）。

### 3-2. 本セッションで確認できていないこと

| 項目 | 理由 | 代替 |
|---|---|---|
| **本番反映の確認**（設計書 §7） | **pushしていない**（ユーザー判断待ち＋GitHub停止中） | 未実施。push時に §7 の手順（素のURLと `?cb=` を20秒間隔で反復・`/cards/zinn-volnia-abbess/` で新旧判別）が必要 |
| **`?cb=` を使った削除確認** | 削除したファイルが無い | 不要 |
| **裏面（`backFace`）のフレーバー表示** | ⚠️ **表示する側が存在しない**（設計書 §2-1・§8-2）。`flavorOf(b, null) \|\| flavorPick(bed.flavor)` に寄せた**変更自体は入っているが、画面に出る経路が無いため実駆動で確認できない** | コードの一致確認のみ。**§8-2 の別issue待ち**（現状維持で正しい） |
| **版切り替えへの追従** | 設計書 §8-1 で**今回やらない**と決定済み | 対象外（初出固定のまま） |

---

## 4. 差分が設計書の想定と食い違った点

### 4-1. ⚠️ カードページが **475 → 実際は 495**（＋20）

**設計書 §1-2 / §4-3 / V2 は「475ファイル」と書いているが、コミットは 495 ファイルである。**
本セッションが独自に数え直した内訳:

| 変化の種類 | 枚数 | 設計書の想定 |
|---|---:|---|
| 新たに原文が出る（版側にしか無かった） | **474** | ⭕ 想定どおり |
| 空枠が消える（`virgil-altered-future`） | **1** | ⭕ 想定どおり |
| ⚠️ **前後の空白が `trim` されるだけ** | **20** | ❌ **数えられていない** |
| **合計** | **495** | 475 |

⭐ **これはドリフトではなく、設計書自身の算術漏れである。** 根拠:

1. 20枚はすべて **`card.flavor` の末尾に空白・改行がある**カード（例: `reduce-to-ash` =
   `"Ashes to ashes. Dust to dust. "`）。差分は `<p class="cp-flavor">…dust. </p>` → `…dust.</p>` の
   **末尾空白1文字の除去のみ**で、文面は変わらない
2. 設計書 §4-1 の `flavorOf()` 仕様は **`pick = (v) => (typeof v === "string" && v.trim()) ? v.trim() : ""`**
   と**明示的に `v.trim()` を返す**と定めている。つまり **trim は設計どおりの挙動**であり、
   §1-2 が「出力が変わるカード」を数えるときに **trim だけが変わるケースを勘定に入れ忘れた**だけ
3. 20枚は**すべて `card.flavor` 側**（版側ではない）＝ §1-2 が対象にしていた「版側にしか無い474枚」の
   外側にあり、見落としやすい位置にある

→ **実装は設計に忠実で、逸脱ではない**と判断した。**設計書 §1-2・§4-3・V2 の「475」を「495」に改訂**した（§6 参照）。

### 4-2. ⚠️ V6 の期待値「バイト一致」は**成立しない**（実装の問題ではない）

`editions` を逆順にしたスナップショット（1,248枚が該当）で生成すると、**581ファイルが変化する**。
原因を追ったところ、**本タスクが触っていない `scripts/build-card-pages.mjs:153` の `newestFirst()`** だった:

```js
const eds = [...(card.editions || card.result_editions || [])].sort((a, b) =>
  day(b.set && b.set.release_date).localeCompare(day(a.set && a.set.release_date))
);
```

⚠️ **発売日だけで比較しており、同着時の第2キーが無い**ため、**同着の版は入力配列の順序をそのまま保つ**。
その結果、**収録セット表の行順・代表画像・`og:image`** が `editions` の配列順に依存する。
これは設計書 §2-3 が問題視した「配列順で拾うと非決定的になる」のと**同じ型の問題が隣の経路に残っている**もので、
**`a7125896` 以前から存在する**（本タスクの対象外）。

⭐ **`flavorOf()` 自体は無関係**である。`flavorOf()` は渡された配列を**自前で昇順ソートし直し、
同着は `edition.slug` で決着させる**ため、入力順に依存しない。実測で裏づけた:

| 測り方 | 正順 vs 逆順 |
|---|---|
| ページ全体のバイト比較（設計書の V6 の字面） | ❌ 581ファイルが不一致（原因は `newestFirst()`） |
| ⭐ **`<p class="cp-flavor">` の文面を2,495枚ぶん比較** | ✅ **完全一致** |

→ **V6 の設計意図（§2-3 の決定性＝cronがノイズコミットを作らないこと）は満たされている**と判定した。
なお **V5（同じキャッシュで2回）は2,560ファイルすべてバイト一致**しており、
**実運用（cronは配列順を勝手に逆順にはしない）でのノイズコミットは起きない**。
`newestFirst()` の順序非依存化は**別issueの候補**として §7 に挙げる。

### 4-3. ⚠️ 再生成で**タスクと無関係な差分が67件**出た（混ぜていない）

`build:cards` を回すと、フレーバーとは無関係に **67件**（カードページ57 + `cards/index.html` +
セットページ8 + `sitemap.xml`、加えて未コミットの新規カードディレクトリ1件）の差分が出た。内訳:

| 内容 | 規模 |
|---|---|
| **公式裁定（`rule`）セクションの増加** | コミット済み **448ページ → 再生成後 478ページ**（**+30**） |
| `<meta name="description">` の「…収録セット」→「…収録セット・**裁定**」 | 上記に付随 |
| **未コミットの新規カード** `cards/jinzhuo-bands-of-virtue/`（PRXY・`created_at` 2026-08-20） | 1ディレクトリ |

⭐ **原因は日次cronが 2026-08-18 から停止していること**（GitHubアカウント停止）。
通常なら翌朝のcronが自動で追従する範囲の陳腐化であり、**フレーバーとは無関係**。

- ✅ **本タスクのコミット `a7125896` には一切混入していない**ことを確認した
  （フレーバー495ページと、この67件の**積集合は0件**）
- ✅ 検証後、**`git checkout -- cards/ sets/ sitemap.xml` と新規ディレクトリの削除で差し戻し**、
  作業ツリーを clean に戻した（`git status --short` が0行）
- ⚠️ **本セッションでは直していない**。67件はレビュー対象外の本番配信物であり、
  **スコープ外の変更**にあたるため（→ §7 で設計担当の判断を仰ぐ）

---

## 5. 設計書からの逸脱と根拠

### 5-1. コードは設計書 §4-1 / §4-2 に**忠実**（逸脱なし）

| 設計書の指定 | 実装 | 一致 |
|---|---|:--:|
| §4-1 `flavorOf(card, t)` を `card-i18n.js` に新設 | 同名で新設。空判定は `flavorPick()` に切り出し（設計書の `pick` に相当） | ⭕ |
| §4-1 優先順は **和訳 > `card.flavor` > `editions[].flavor`** | `jp` → `own` → `eds` の順に `return`。V8で実駆動確認 | ⭕ |
| §4-1 戻り値を `""` に統一 | `flavorPick()` が常に文字列を返す | ⭕ |
| §4-1 初出固定（`release_date` 昇順・`1970` は末尾・同着は `slug` 昇順） | 同一ロジック。V6・V7で確認 | ⭕ |
| §4-1 `return { … }` に `flavorOf` を追加して公開 | 追加済み（`flipEdition, backFace, flavorOf`） | ⭕ |
| §4-2 消費側1 `build-card-pages.mjs:368` | `const flavor = CI.flavorOf(card, t);` | ⭕ |
| §4-2 消費側2 `card-detail.js:237` | `const flavor = flavorOf(card, t);`（分割代入にも `flavorOf` を追加） | ⭕ |
| §4-2 消費側3 `backFace()` は `flavorOf(b, null) \|\| pick(bed.flavor) \|\| null` | `flavorOf(b, null) \|\| flavorPick(bed.flavor) \|\| null` | ⭕ |
| §4-2 `flavorBlock` の条件式は変えない | `flavor ? … : ""` のまま | ⭕ |
| §4-3 `gen-tl-json.mjs` は不要 | 実行していない（翻訳データ未変更） | ⭕ |

⭐ **消費側の網羅性も確認した**: リポジトリ全体で `.flavor` を参照する箇所を洗い出したところ、
**表示に関わる経路は上記3箇所のみ**で、すべて `flavorOf()` に寄っている。
残る参照は `gen-tl-json.mjs`（訳データ→JSONの変換）と `card-i18n.js:115`（訳エントリ構築）で、
**どちらも表示経路ではない**ため対象外で正しい。

### 5-2. ⚠️ V8 の検証カードを設計書の例から差し替えた

設計書 V8 は例として **`crimson-vein`** を挙げているが、**このカードは和訳フレーバーを持たない**
（`t.flavor` が `undefined`・`card.flavor` も `editions[].flavor` も無く、`flavorOf()` は `""` を返す）。
そのまま使うと**「日本語フレーバーが出る」ことを確認できない**。

→ 和訳フレーバーを持つ117枚から、**優先順の2経路を両方カバーする2枚**に差し替えた:

- `cato-meadows-channeler` — **和訳 > `card.flavor`**（この関係にあるのは5枚のみ）
- `aenean-cyclone` — **和訳 > `editions[].flavor`**（112枚。⭐ 設計書 §1-1 の「うち112枚は和訳が入ったので日本語だけが出ている」に対応）

⭐ 差し替えにより、**元の V8 より強い確認**になっている（元は1経路しか見ていない）。

### 5-3. 検証手順の逸脱

- **V2**: 期待値を読み替えた（§3-1・進行役の指示による）
- **V6**: 期待値「バイト一致」では判定できないため、**フレーバー文面の一致**で判定した（§4-2）

---

## 6. 設計書の更新

本報告に合わせて `フレーバー表示経路_設計.md` を更新した:

- **改版履歴に第3版を追加**（追認検証の結果・475→495の訂正・V6の期待値の訂正）
- **「実施状況」表**: §4-1／§4-2／§4-3 を **✅ 実装済み（`a7125896`）**、§5 を **✅ V1〜V9 追認済み** に更新
- **§1-2・§4-3・V2 の「475」を「495」に訂正**し、trim20枚の内訳を追記
- **V6 の期待値**を「フレーバー文面が一致すること」に訂正し、`newestFirst()` の件を注記
- **V8 の例**を `crimson-vein` から `cato-meadows-channeler` / `aenean-cyclone` に訂正
- §7 は **`push待ち`（ユーザー判断）のまま**変更なし

---

## 7. 設計担当への確認事項

⚠️ **いずれも本セッションでは判断せず、そのままにしてある**（開発担当は仕様を決めない）。

1. ⭐ **`newestFirst()`（`build-card-pages.mjs:153`）の順序非依存化を別issueに切り出すか**
   （§4-2）。発売日が同着の版で**入力配列の順序がそのまま出力に出る**ため、
   **公式APIが `editions` を並べ替えた日に、収録セット表・代表画像・`og:image` が
   無関係に変わる**（実測: 逆順で581ファイル）。⚠️ **これは #71 で観測された
   「無関係なカードページ・セットページの差分」の発生源そのものである可能性が高い**。
   設計書 §2-3 が `flavorOf()` について解いた問題と**同じ型**なので、
   同じ手（`edition.slug` の第2キー）で解ける見込み。**本タスクの範囲外**として手を付けていない。

2. **cron停止中に溜まった生成物の陳腐化（67件）をどう扱うか**（§4-3）。
   **公式裁定セクションが30ページ分・新規カード1枚が未反映**。
   cronが復旧すれば自動で解消するが、**アカウント停止が長引く場合は
   手動で再生成してコミットする判断**が要る。⚠️ **本タスクに混ぜるのは不適切**と考え差し戻した。

3. **§8-2（裏面フレーバー）の扱い**。`backFace()` は `flavorOf()` に寄せられたが、
   **表示する側が無いままなので画面での確認ができない**（§3-2）。
   設計書は「別issueに切り出す」としているが、**起票は設計担当の判断待ち**。

4. **本コミットは `push待ち` 相当**（本番配信物498ファイル）。
   ⚠️ ただし **GitHubアカウント停止中でpushできない**ため、
   **復旧後にユーザーの判断を仰ぐ**必要がある。反映確認の手順は設計書 §7 のとおり
   （新規ファイルが無いので**到達マーカーが使えず**、素のURLと `?cb=` の反復測定が要る）。

---

## 8. 状態

- ✅ **不具合は見つからなかった**（コード修正なし）
- ✅ 作業ツリーは **clean**、スナップショットの md5 は **`297ab372fc9b2cd4a82485cdbde1e3f5`**（変更なし）
- ✅ ローカルサーバー（ポート3000）は**停止済み**
- ⚠️ **本来はここで `実装待ち` → `レビュー待ち` へ付け替える状態**
  （`gh issue edit <n> --remove-label 実装待ち --add-label レビュー待ち`）。
  **issueが存在しないためラベル操作は未実施**。レビュー担当は**本書と `a7125896` を出発点**にしてよい
