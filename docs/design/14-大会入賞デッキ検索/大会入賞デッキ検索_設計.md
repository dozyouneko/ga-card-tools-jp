# タスク#14: 大会入賞デッキの検索機能 設計書

GitHub issue: [#14](https://github.com/dozyouneko/ga-card-tools-jp/issues/14)(優先度高・ピン留め)

## 改版履歴

| 日付 | 版 | 内容 |
|---|---|---|
| 2026-07-19 | 初版 | データ取得方式・対象大会・実装方針を確定(実装待ち)。ユーザー決定: 対象=主要大会のみ(regular除外)・全世界／方式=静的生成(build-card-pages.mjsと同方式) |
| 2026-07-19 | 画面モック追加 | 3画面(大会検索→大会詳細→デッキ詳細)をリンクで実際に遷移できる形で作成。データはすべてOmnidex API実測の実データ(イベント#57690 CANTRIP RDOストアチャンピオンシップ・1位yokomi選手の実提出デッキ)を使用。Playwrightで導線の実クリック確認済み |
| 2026-07-19 | デッキ詳細モックをv2に更新 | ユーザー指摘反映: デッキ詳細画面を**既存の共有デッキ画面(`renderDeckView()`)ベースのカードタイル表示**に変更。イラストは各カードの先頭の版(既存`imageUrl()`と同じデフォルト仕様。複数版がある場合の「最新版採用」等は実装時に`cardImages()`で対応可・追加検討)。画像45枚すべてapi.gatcg.comの実カード画像で読み込み確認済み(0件失敗) |
| 2026-07-19 | デッキ詳細モックをv3に更新(カード詳細ダイアログ) | ユーザー要望「クリックでカード詳細・スマホでもダイアログ表示」に対応。**モックではなく実物の`shared/js/card-detail.js`+`card-i18n.js`をそのまま読み込んで動かす**方式を採用(`fetchCard`省略時は公式APIを直接fetchする仕様を利用)。PC=中央モーダル/スマホ幅(≤640px)=既存CSSの仕様どおり自動で全画面シートになることをPlaywrightで実クリック確認済み(追加実装なしで動作)。実装時もこの組み込み方をそのまま踏襲できる |
| 2026-07-19 | 対象大会の再検討(結論: regular除外を維持) | 「regularも含めたらどの程度影響があるか」を追加実測(500件)。regular平均5.2人・デッキ提出可はわずか2%(主要大会は100%)。件数は40〜50倍に増えるが「デッキが見られる大会」の増分は主要大会と同程度(倍増止まり)。一覧画面の「件数が少ないためクライアント側フィルタのみ」という設計前提が崩れる点と、差別化ポイント(主要大会の日本語訳)が薄まる点を踏まえ、**現行の「主要大会のみ・regular除外」を維持**と結論(ユーザー決定) |
| 2026-07-19 | 新規大会の反映方式を確定 | ユーザー決定: **最初からGitHub Actionsで自動定期実行**(手動実行のMVPは経ずに着手)。日次cronで差分スキャン→変更あればmainに直接push→Cloudflare Pages自動デプロイ。開催前に先行作成されるイベント(未来日程)への対応として「pending」の概念を追加(下記「自動反映」節参照) |
| 2026-07-20 | 実装完了・容量方針を確定(実装待ちへ差し戻し) | 開発担当が実装([`955e741`](https://github.com/dozyouneko/ga-card-tools-jp/commit/955e7417336afa20894d9d73674f7bf8fcf5c7b3)、未push)・検証8項目のうち7項目✅を報告。初回バックフィル実測で415大会/9,307デッキ=221MBとなり想定(regular除外で約1/50)を超過。原因は**「順位表(モックではTop8想定)」の記述が設計書の静的ページ節では「全提出デッキ」のままだった矛盾**(モックの挙動メモと本文の齟齬)。これを機に**順位表・保存デッキとも全大会Top8までに統一**(下記「決定事項」節参照)。ヘッダナビ導線・カテゴリ許可リストも合わせて確定 |
| 2026-07-20 | ユーザー再検討によりTop8限定を撤回・カテゴリ許可リストを拡大 | ユーザーがローカルで実機確認のうえ再指示: **Top8限定を撤回し全順位・全デッキを表示**する方針に変更(上記版のTop8決定を上書き)。あわせて「nationals(全世界大会)は他サイトで確認できているはず」との指摘を受けて実地再調査した結果、**`nationals`は実在すると確認**(前版の「未検出」は誤り。id 46,500〜58,650=12,151件の再スキャンで発見。詳細は「カテゴリ分布の再実測」節)。未知のカテゴリ`ascent`(公式主催のプレミア大会)も新たに発見し許可リストに追加。カテゴリ許可リストを`store-championships`/`regionals`/`nationals`/`ascent`の4種に拡大。あわせてユーザーがローカルで実機確認した3件のUI改善指摘(シーズン絞り込み・国名表示バグ・開催国カラム)にも対応 |
| 2026-07-20 | ページ構成を3階層→2階層に戻す(ファイル数上限リスク対応) | 全デッキ表示+カテゴリ拡大の結果、生成ファイル数が導入前2,418件→約13,800件(Cloudflare Pages Freeプランの上限20,000件の約69%)まで跳ね上がる見積りが判明(詳細は「運用リスク」節)。シーズンを重ねるたびに同規模の増分が続くため、次シーズンでFreeプラン上限を超えデプロイ失敗に至るリスクが高いと判断。**ユーザー決定: デッキページを大会詳細ページに内包する2階層構成に戻す**(初版設計の想定に復帰。「1デッキ=1ファイル」の3階層をやめ、1大会=1ファイルの中に全デッキをアコーディオン/タブで内包する形にすることでファイル数を大会数(約420)基準に削減=約96%減)。モック3/3の「デッキ詳細は別画面」というページ遷移の導線は、同一ページ内スクロール/アコーディオン展開に置き換える(カードクリックでの実物カード詳細ダイアログ表示は変更なし) |

## 目的・差別化ポイント

大会(Regionals/Store Championship等)の入賞デッキを日本語訳付きで検索・閲覧できるようにする。**大会入賞結果を日本語訳付きで見られるサイトは現時点で他に存在しない**(2026-07-19時点調査)。カードDBの既存翻訳データをそのまま適用できるのは本サイトならではの強み。

参考事例: カードゲームショップCANTRIPのデッキ検索 <https://cantripmtg.com/pages/decks-grand-archive>(手動キュレーションによる自社ページ。出典表記なし)

## 調査結果(2026-07-19)

### 公式Omnidex APIは正式ドキュメント化されている(訂正)

issue #14登録時点では「未ドキュメントAPI」としていたが、**api-docs.gatcg.com に `/endpoints/omnidex` として正式収録されている**ことを確認(most updated 5ヶ月前)。以下がすべて公式ドキュメント記載のエンドポイント:

| エンドポイント | 内容 |
|---|---|
| `GET /omnidex/events/{event_id}` | 大会情報(名称/カテゴリ/フォーマット/日程/主催/形式) |
| `GET /omnidex/events/{event_id}/players` | 参加者(username/country/CP/世界rank/emblem) |
| `GET /omnidex/events/{event_id}/teams` | チーム戦の場合のチーム構成 |
| `GET /omnidex/events/{event_id}/judges` | ジャッジ一覧 |
| `GET /omnidex/events/{event_id}/standings` | 順位・勝敗・ゲーム勝率・タイブレーカー |
| `GET /omnidex/events/{event_id}/pairings` | ラウンドごとの対戦組み合わせ |
| `GET /omnidex/events/{event_id}/decklists` | **入賞者ではなく提出者全員のフルデッキ**(material/main/sideboard、英語カード名+枚数) |
| `GET /omnidex/events/{event_id}/statistics` | 大会の統計データ |

- **一覧・検索エンドポイントは存在しない**(idによる個別取得のみ)。イベントIDは連番で作成順に増加し、`api.gatcg.com/omnidex/events/{id}` を辿ってスキャンする以外に発見手段がない
- レート制限は公式ドキュメント上に明記なし。CORSは `*`(ブラウザから直接fetch可能)だが、無闇な高頻度アクセスは避け、キャッシュ・低並列を徹底する(既存 `tmp/ga-omnidex-mock/build.mjs` の並列数8を踏襲)
- 個人をIDで直接引くAPI(`/omnidex/players/{id}` 等)は存在しない。個人の戦績はイベント経由(`standings`)でのみ取得可能

### カテゴリ分布の実測(2026-07-19、1,200件スキャン)

| カテゴリ | 件数 | 割合 |
|---|---|---|
| regular(週次リーグ等) | 1,173 | 97.8% |
| store-championships | 24 | 2.0% |
| regionals | 3 | 0.25% |

- 欠番0件(スキャン範囲内でIDは概ね密に埋まっている)
- startAt範囲は2026-06-20〜2026-09-05(未来日程で先行登録される大会が一定数ある)
- 国別上位(1,200件中): US 371 / TW 158 / MY 106 / SG 77 / NZ 66 / **JP 65** / AU 62 / PH 54
- → **regular除外だけでデータ量が約1/50に減り、運用可能な規模になる**。nationals/invitational/worlds等の他カテゴリは今回のサンプル範囲では未検出だが、カテゴリはAPI側で自由文字列のため許可リスト方式(下記)で今後追加が容易

### カテゴリ分布の再実測(2026-07-20、id 46,500〜58,650=12,151件走査・訂正版)

前回(2026-07-19)の「nationals/invitational/worlds等は未検出」は誤りだったため、ユーザー指摘([fractalofin.site](https://fractalofin.site/RDO/56265.html)が台湾のNational結果を掲載している事実)を受けて再調査した。

| カテゴリ | 件数 | 備考 |
|---|---|---|
| regular | 11,575 | 除外(既定どおり) |
| store-championships | 463 | |
| regionals | 64 | |
| **nationals** | 5 | **前回誤って「未検出」としていたが実在**。うち1件(id 50271「Test」)は`status:"canceled"`のテストデータで実大会ではない。実大会4件: 北米(52777)・欧州(54238)・オセアニア(55400)・東南アジア(56265)の各Nationals 2026 |
| **ascent**(新発見) | 2 | 前回調査で未把握だった新カテゴリ。`host.name`が**公式(`Weebs of the Shore Events`)**。「Ascent Los Angeles」「Ascent Las Vegas」の2件で、いずれも`ranked:true`・`singleEliminationCutSize:8`の公式プレミア大会 |

主要大会1件あたりのデッキ提出数(実測・decklistsエンドポイントで直接カウント):

| イベント | カテゴリ | デッキ数 |
|---|---|---|
| SEA National 2026(56265) | nationals | 417 |
| North American National Championship(52777) | nationals | 306 |
| GA EU Nationals(54238) | nationals | 85 |
| OCE Nationals 2026(55400) | nationals | 129 |
| Ascent Los Angeles(48556) | ascent | 391 |
| Ascent Las Vegas(52587) | ascent | 325 |

nationals/ascentは店舗規模のstore-championships(平均20〜25人)と比べて**1大会あたりのデッキ数が桁違いに多い**(300〜400件規模)。これらを許可リストに追加すると6大会だけで+1,653デッキ(既存9,307デッキの+18%程度)になる一方、件数自体は少数(合計6件)なので大会一覧・国別集計への影響は軽微。

#### シーズン期間(`omni.gatcg.com/api/season?id=N`で取得・実測)

| season id | 名称 | 開始 | 終了 |
|---|---|---|---|
| 7(現行) | Radiant Origins(RDO) | 2026-04-10 | 2026-08-21 |
| 6 | Phantom Monarchs(PTM) | 2025-12-12 | 2026-04-03 |
| 5 | Distorted Reflections | 2025-08-01 | 2025-12-05 |
| 4 | Abyssal Heaven | 2025-03-14 | 2025-07-26 |

**訂正: PTMシーズンはRDOシーズンより前(2025-12-12〜2026-04-03)。** 「PTMシーズン以降を対象にすれば容量が減るのでは」という想定は逆で、**PTM以降(=PTM+RDOの2シーズン分)にすると対象期間が広がり容量は増える方向**(店舗規模の大会がRDOシーズン単独で440件→PTM+RDOで概算527件に増加。上記nationals/ascentの6件中、PTM期間のものはAscent Los Angeles(48556)の1件のみで残り5件はRDO期間)。容量を減らしたい場合は逆に**期間を絞る**必要がある(例: 直近シーズンのみ→現行のRDO単独のまま)。今回は「Top8限定」を撤回し全デッキ表示にする分、**期間は現行どおりRDOシーズン単独を維持**し、PTMシーズンへの拡張は任意の将来検討とする(下記「未決定・将来検討」参照)。

### 決定事項(2026-07-19ユーザー決定)

1. **対象大会**: **主要大会のみ・全世界**。カテゴリ許可リスト = **`store-championships` / `regionals` / `nationals` / `ascent`**(2026-07-20拡大。実データで存在確認済み。`invitational`/`worlds`は今回のスキャン範囲では未検出のため引き続き未追加・判明次第追加)。`regular` は除外。また`status`が`canceled`のイベントは許可リストに一致してもその場で除外する(pendingにも入れない。テストイベント等が永久にpendingへ残留するのを防ぐ)
2. **実装方針**: **静的生成**(`scripts/build-card-pages.mjs` と同方式)。ビルド時にAPIをスキャンし、対象大会データをgit管理のJSONに保存→静的HTMLを事前生成。Cloudflare側の追加費用・サーバー処理なし。既存の「翻訳データはgit管理→pushで自動デプロイ」の運用にそのまま乗る

### 既存資産の再利用(カード名→slug解決)

デッキ構築ツールの「Omnidexインポート」機能([tools/deck-builder/app.js:1671-1714](../../../tools/deck-builder/app.js#L1671-L1714))が、まさに同じ課題(Omnidex提出フォーマットの英語カード名→slug解決)を解決済み:

- `normCardName()`: 記号・大小文字ゆれを吸収する正規化(`toLowerCase().replace(/[^a-z0-9]+/g,"")`)
- 完全一致のみ採用(曖昧一致は誤登録防止のため不採用)という方針も踏襲する

本機能はビルド時のバッチ処理のため、ライブAPI呼び出し(`resolveCardSlug`)ではなく**ローカルのカードスナップショット(`tmp/api-cache/cards-snapshot.json`)を使った同ロジックの一括解決**に置き換える(`scripts/build-card-pages.mjs` が同スナップショットを既に利用しているのと同じ考え方)。

### 読み取り専用デッキ表示との構造共通性

大会デッキは「編集不可」という点でデッキ構築ツールの共有画面(`#deck/<id>`)の表示ロジックに近い。データを `{card_slug, board(material/main/side), qty}` の形に正規化しておけば、統計タブの `computeDeckStats()` 等の集計ロジックを**そのまま入賞デッキにも適用できる可能性がある**(例: 「この大会の使用エレメント分布」)。ただし今回のMVPスコープでは表示のみとし、統計流用は将来検討(#3のコストカーブ検討と合わせて考える余地あり)。

## 運用リスク: ファイル数上限・リポジトリ容量(2026-07-20調査)

「全デッキ表示+カテゴリ4種」を反映した場合の生成ファイル数・容量を実測ベースで見積もった。

### Cloudflare Pagesのファイル数上限(近い将来に深刻化しうる・最優先で対処)

Cloudflare Pagesは1デプロイあたりのファイル数上限が **Freeプラン=20,000件・有料プラン=100,000件**(2026-01の変更で有料プランは20,000→100,000に緩和。[Cloudflare changelog](https://developers.cloudflare.com/changelog/post/2026-01-23-pages-file-limit-increase/))。

| | ファイル数 |
|---|---|
| 導入前(既存トラッキング済みファイル) | 2,418(カード2,241件+その他) |
| 今回の追加分(大会421+デッキ10,960+一覧1。**1デッキ=1ファイル**の3階層構成の場合) | 約11,382 |
| **導入後の合計** | **約13,800** |

Freeプランなら1シーズン分のバックフィルだけで上限20,000の約69%を消費する計算になる。RDOシーズン(約4ヶ月)ごとに同規模の増分が続くため、**次シーズンが終わる頃にはFreeプランの上限を超え、サイト全体のデプロイが失敗し始める**リスクが高い(Cloudflare Pagesはファイル数超過時にビルド自体を失敗させるため、カード・機能の更新も含めて全て止まる)。有料プランなら100,000件まで余裕があるが、本プロジェクトのプラン種別は`wrangler.toml`等のリポジトリ内設定からは判別できず未確認。

**ユーザー決定(2026-07-20): ページ構成を2階層(デッキを大会詳細ページに内包)に戻す。** 1デッキ=1ファイルをやめることで、ファイル数の基準が「デッキ数」(約1万・増え続ける)から「大会数」(約420・同ペースでも遥かに緩やか)に変わり、約96%削減できる。詳細は「静的ページ」節を参照。

### GitHubリポジトリの容量(こちらは長期的な懸念)

現在の`.git`は6.9MB、`cards/`ディレクトリ(2,240件)は23MBという非常に軽量な状態。今回のバックフィルで`tournaments/`配下のHTMLが221〜300MB規模になる見込みのため、一気に10〜40倍に跳ね上がる。GitHubは1GB未満を推奨し、5GB超で警告メールが来るソフトキャップがある([GitHub Docs](https://docs.github.com/en/repositories/creating-and-managing-repositories/repository-limits))。ページ構成を2階層化してもJSONデータ量(`data/tournaments/`)自体は変わらないため、こちらは引き続き「未決定・将来検討」節の「データ増加の長期的な運用」で監視する。

## 画面モック

3画面をリンクで実際に遷移できる形で用意した(いずれも実データ使用・ブラウザで直接開ける自己完結HTML):

- [モック_大会検索.html](モック_大会検索.html) — 大会一覧・検索画面(1/3)
- [モック_大会詳細.html](モック_大会詳細.html) — 大会詳細・順位表画面(2/3)
- [モック_デッキ詳細.html](モック_デッキ詳細.html) — デッキ内容表示画面(3/3)

1/3のCANTRIP行 → 2/3のrank1「yokomi」行「デッキを見る」→ 3/3、の順にクリックで導線を確認できる。各画面下部に「挙動メモ」として実装時の判断材料(絞り込み方式・デッキ未提出時の扱い・表記規則等)を記載。

## データモデル

### 保存先(git管理・data/tournaments/)

```text
data/tournaments/
├── events/
│   └── <event-id>.json      … 1大会分。取得済みの生データを正規化して保存
└── index.json                … 大会一覧(id/name/date/category/country/format/player数)。一覧ページ・スキャン再開の基準に使う
```

`events/<id>.json` の構造(案):

```json
{
  "id": 57690,
  "name": "CANTRIP RDOストアチャンピオンシップ",
  "category": "store-championships",
  "format": "standard",
  "startAt": "2026-07-12T03:30:00.000Z",
  "host": { "name": "CANTRIP", "country": "JP" },
  "playerCount": 22,
  "standings": [
    { "rank": 1, "player": 4571, "wins": 4, "losses": 0, "ties": 1, "gwPercent": 88.889, "mwPercent": 80 }
  ],
  "players": { "4571": "PlayerName" },
  "decklists": [
    {
      "player": 4571,
      "material": [{ "slug": "spirit-of-wind", "name": "Spirit of Wind", "qty": 1 }],
      "main": [{ "slug": "distilled-water", "name": "Distilled Water", "qty": 3 }],
      "sideboard": [{ "slug": null, "name": "未解決カード名", "qty": 1 }]
    }
  ]
}
```

- `slug: null` は名前解決に失敗したカード(表示は英語名のみ・リンクなし。ビルドログに警告出力)
- `index.json` は一覧ページと「次回スキャンの開始ID」判定の両方に使う(git管理されたファイル自体がチェックポイントを兼ねる。隠しキャッシュファイルを別途持たない)
- **(2026-07-20撤回)** 一時「rank 1〜8のみ保存」としていたが、ユーザー指示により撤回。**`standings`・`decklists`は全順位・全提出デッキを保存**する(nationals/ascent級の大規模大会で300〜400デッキになるケースを容認)
- **(2026-07-20確定・継続)** `events/<id>.json`はJSONを**非整形(改行・インデントなし)で保存**(容量削減。Top8限定の撤回後も独立して有効な最適化のため継続。差分を人間が読みたい場合は`jq .`で整形して確認する運用とする)

### 保留イベントの扱い(`data/tournaments/pending.json`)

イベントIDは**大会開催前に先行作成される**(実測: startAtが未来の日程で登録済みのIDが多数存在。例: 現在の最大IDより前に2026-07-25/07-28開催のイベントが既に存在)。そのため「idを見つけた時点でカテゴリ対象=即確定」とはできず、以下の状態管理が必要:

```json
// data/tournaments/pending.json
[
  { "id": 58520, "firstSeenAt": "2026-07-19", "category": "store-championships", "startAt": "2026-07-25T..." }
]
```

- 新規スキャンでカテゴリ対象と分かったが `status !== "complete"` または `decklists` が空のイベントは、`events/<id>.json` を確定させず `pending.json` に記録
- 毎回のスキャンで **pending内の全idを再チェック**(件数は少数に留まる想定)。`status === "complete"` かつ decklists が揃っていれば `events/<id>.json` を確定生成し `pending.json` から除去
- `firstSeenAt` から一定期間(例: 60日)経っても確定しない場合は「開催中止・記録なし」とみなし `pending.json` から削除(無限に溜まるのを防ぐ)
- **(2026-07-20追加)** `status === "canceled"` が判明した時点で60日待たずに即座に`pending.json`から削除する(実測でid 50271「Test」のような`canceled`のテストイベントを確認済み。永久にpendingへ残留させない)

### 自動反映(GitHub Actions・2026-07-19決定)

リポジトリはpublicのため、GitHub Actionsの実行時間は無料枠に実質制限がない。以下の運用で新規大会を自動反映する:

```yaml
# .github/workflows/build-tournaments.yml (イメージ)
on:
  schedule:
    - cron: "0 18 * * *"   # 毎日 03:00 JST(イベント作成頻度は約115件/日・世界全体なので日次で十分)
  workflow_dispatch: {}     # 手動実行(動作確認・不具合対応用)
permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20 }
      - run: node scripts/build-tournament-pages.mjs
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/tournaments tournaments sitemap.xml
          git diff --cached --quiet || git commit -m "大会データ自動更新 $(date -u +%Y-%m-%d)"
          git push
```

- **mainへ直接push**(PRを経由しない)。理由: 追加専用(既存イベントは確定後書き換えない)の公開データであり、秘密情報・設定変更を含まないため、レビューを挟まなくてもリスクは低いと判断(ユーザー決定)
- pushをトリガーに既存のCloudflare Pages自動デプロイがそのまま動く(新しいデプロイ経路は不要)
- 実行ログはGitHub Actionsの実行履歴で確認できる(異常があれば`workflow_dispatch`で手動再実行して調査)
- スキーマ変更等でスクリプトが失敗した場合はワークフローが失敗するのみ(pushされない)。次回改修まで大会データは古いまま=安全側に倒れる

### 静的ページ

```text
tournaments/
├── index.html            … 大会一覧(日付降順。カテゴリ/国/フォーマット/シーズンで絞り込み)
└── <event-id>/
    └── index.html         … 大会詳細: 概要+順位表(全順位)+全提出デッキ(デッキごとにmaterial/main/sideboard)
```

- **(2026-07-20再確定)** 大会詳細とデッキ詳細を1ページに戻す(2階層)。**経緯**: 一時的に3階層(デッキ1件=1ファイル)で実装されたが、全デッキ表示+カテゴリ拡大の結果ファイル数がCloudflare Pages Freeプランの上限(20,000件)の約69%に達する見積りとなったため撤回(詳細は「運用リスク」節)。初版設計の2階層構成に復帰する
- 順位表への「デッキを見る」導線は、別ページへの遷移ではなく**同一ページ内のアコーディオン展開(または該当デッキへのスクロール)**にする。モック3/3で確認済みの「クリックでカード詳細ダイアログを開く」導線・アコーディオン内のカードタイル表示自体は変更なし(1ページに複数デッキが同居する形に読み替える)
- **(2026-07-20撤回)** 順位表・デッキ詳細とも「Top8のみ」としていた制限は撤回。**全順位・全提出デッキ分を表示する**(1ページに内包するため、ファイル数への影響なくデッキ数を増やせる)
- 個別カードは `cards/<slug>/` への通常リンク。可能ならカードDBと同じ詳細モーダル(`shared/js/card-detail.js` + `card-i18n.js`)を読み込み、その場でポップアップ表示できるようにする(既存ページと同じUX)
- `sitemap.xml` への合流: `scripts/build-card-pages.mjs` のsitemap生成ステップが `tournaments/*/index.html` も走査して追加できるよう改修する(実行順序: 大会スキャン→カードページ生成、の順に実行する運用とする)
- **(2026-07-20確定)** サイト全体のヘッダnav(`index.html`・`cards/<slug>/`等の共通ヘッダ)に「大会入賞デッキ」リンクを追加する。既存カードページ2,240件の再生成を伴うが、`npm run build:cards` の通常再生成に含めれば追加コストなし。具体的な追加箇所は2つ:
  - [index.html:41-46](../../../index.html#L41-L46) の `#header-actions`(`ツール ▾`トグル配下)に他の`tool-link`と同じ形式で追加。モバイルは既存のJSトグル([style.css:300](../../../style.css#L300)の`.tools-open .tool-link`)にそのまま乗るため追加対応不要
  - `scripts/build-card-pages.mjs:191`の`siteHeader()`関数(`cards/<slug>/`等の全生成ページ共通ヘッダ)。`scripts/build-tournament-pages.mjs:372`の同名関数には**追加済み**なので、そちらの文言と揃える。`.cp-site`は`flex-wrap`済みでモバイルでも折り返し表示になる([build-card-pages.mjs:458](../../../scripts/build-card-pages.mjs#L458))

## ローカル動作確認で判明した改善点(2026-07-20)

ユーザーがローカルサーバーで生成済みページを実際に操作して発見した3点。

### 1. 国名表示バグ(「jpJP」のように見える)

`scripts/build-tournament-pages.mjs`の`flag(cc)`(325-328行目)は国コードをUnicode地域指示記号2文字に変換してflag絵文字にする実装だが、**絵文字非対応の環境ではただの文字っぽいグリフにフォールバックする**。さらに一覧ページ(400行目)・大会詳細ページ(497行目)で`flag(cc) + cc`を**セパレータなしで連結**しているため、絵文字が描画されない環境では国コードが二重に見えてしまう(例: 🇯🇵JP → 環境によっては「JPJP」寄りの表示)。`<select id="f-country">`(425行目)はスペース区切りのため症状は軽微。

**対応**: 400行目・497行目の連結箇所に区切り(スペースや中点)を入れる。絵文字が描画されない環境でも国コードが重複して見えないようにする。

### 2. 一覧テーブルに「開催国」カラムを追加

現状、国データは各行に`data-country`属性(396行目)として保持されているが、表示は`.ev-host`内にホスト名と一緒に埋め込まれるのみ(400行目)で独立した列がない。ヘッダ(434行目)に`<th>開催国</th>`、行テンプレート(400行目付近)に対応する`<td>`を追加する(バグ1の表記修正と合わせて実装)。

### 3. シーズンでの絞り込みを追加

現状のデータモデルには`season`フィールドが存在せず、`startAt`の日時のみ保持している。シーズン名(RDO/PTM等)は大会名の自由文字列に紛れ込んでいるだけで構造化されていない。

**対応方針**: スキャン時(`collectEvent`)またはインデックス生成時に、`startAt`と「シーズン期間(上記「シーズン期間」表を参照。`omni.gatcg.com/api/season?id=N`で正確な開始・終了日時を取得可能)」を突き合わせて`season`(例: `"RDO"`)を導出し`data/tournaments/index.json`・`events/<id>.json`に保持する。一覧ページの絞り込み用に`data-season`属性を各行に追加し、既存のクライアント側フィルタ(`LIST_JS`、689-734行目の`apply()`)に他の`<select>`と同じパターンで`#f-season`を追加する。シーズン境界はハードコードの定数テーブルとして管理し、新シーズン開始時に手動更新する運用とする(API側にシーズン一覧の取得エンドポイントが見当たらないため)

## 実装ステップ(実装担当セッション向け)

1. **スキャンスクリプト作成**: `scripts/build-tournament-pages.mjs`
   - `data/tournaments/index.json` から既知の最大event idを読み、そこから昇順(新規発見)+ 初回は指定の遡り基準(例: RDOシーズン開始 2026-04-10、`omni.gatcg.com/api/season` で取得可能)まで降順スキャン
   - **加えて `data/tournaments/pending.json` の全idを再チェック**(未完了だった大会が確定したか確認。詳細は「保留イベントの扱い」節)。**(2026-07-20追加)** `status === "canceled"` はpendingに入れず即除外
   - 各idで `/omnidex/events/{id}` を取得。`category` が許可リスト(**2026-07-20拡大**: `store-championships`/`regionals`/`nationals`/`ascent`。定数で管理し追加しやすく)に一致する場合のみ `/players`・`/standings`・`/decklists` も取得。`status !== "complete"` ならpendingに記録して次回再チェック
   - **(2026-07-20撤回)** rank 1〜8への絞り込みは行わない。**取得した`standings`・`decklists`は全件そのまま保存**する
   - カード名→slug解決は `tmp/api-cache/cards-snapshot.json` を読み込み、`normCardName()` 相当のロジックで突き合わせ(deck-builder/app.jsの実装を移植・共通化を検討)
   - **(2026-07-20追加)** `startAt`とシーズン期間の定数テーブル(「シーズン期間」節参照)を突き合わせて`season`フィールドを導出し保存する
   - 取得結果を `data/tournaments/events/<id>.json` に**非整形JSONで**保存、`index.json`・`pending.json` を更新
   - 並列数は控えめ(6〜8程度)。進捗ログを出す(既存 `tmp/ga-omnidex-mock/build.mjs` を土台にする)
2. **静的ページ生成**: 同スクリプト内、または `build-card-pages.mjs` を拡張し、`data/tournaments/` から `tournaments/index.html`・`tournaments/<id>/index.html`(全提出デッキ分をアコーディオンで内包。**(2026-07-20再確定)** 1デッキ=1ファイルの構成は撤回。ファイル数上限対策のため大会詳細1ページに全デッキを内包する)を生成
   - **(2026-07-20追加)** 一覧テーブルに「開催国」列を追加。国コード表示は`flag(cc)`と国コード文字列の間に区切りを入れて重複表示バグを解消(「ローカル動作確認で判明した改善点」節参照)
   - **(2026-07-20追加)** 一覧ページのクライアント側フィルタに「シーズン」の`<select>`を追加(既存の`LIST_JS`の`apply()`に他フィルタと同じパターンで組み込み)
3. **sitemap連携**: `build-card-pages.mjs` のsitemap生成ステップに `tournaments/` 配下のURLを追加
4. **GitHub Actionsワークフロー作成**: `.github/workflows/build-tournaments.yml`(日次cron+`workflow_dispatch`。詳細は「自動反映」節のYAMLイメージ参照)。**(2026-07-20確定)** 容量方針確定・初回反映完了までは `cron` を無効化し `workflow_dispatch` のみで運用してよい。本番反映後にcronを有効化する
5. **CLAUDE.md/README更新**: 新しいnpmスクリプト(`npm run build:tournaments` 等)・自動反映の仕組みを追記
6. **サイト共通ヘッダnav追加**: `index.html`の`#header-actions`(41-46行目)と`scripts/build-card-pages.mjs`の`siteHeader()`(191行目)の両方に「大会入賞デッキ」リンクを追加し、既存カードページを再生成する(差分ゼロ確認済みの再生成フローにヘッダ変更を反映するだけなので追加リスクなし。`build-tournament-pages.mjs`側の`siteHeader()`には追加済みなので文言を揃える)

## 検証項目(実装時)

1. カテゴリフィルタが機能し、`regular` の大会が一切生成されないこと。`nationals`/`ascent`の大会が新たに生成されること(実例: id 56265 SEA National 2026・417デッキ)
2. 既知の実例(#57690 CANTRIP RDOストアチャンピオンシップ)で、22名**全員分**のデッキ・順位が正しく表示される
3. カード名→slug解決: 既存の全カード名で解決成功率を確認(未解決があればログで一覧化・原因確認)
4. 2回目以降の実行が「新規大会のみ」を追記し、既存の `data/tournaments/events/*.json` を書き換えないこと(冪等性)
5. `sitemap.xml` に `tournaments/` のURLが追加されていること
6. CSP・生成ページのアクセシビリティが既存の `cards/` ページと同水準であること
7. pending扱いの動作: 未来日程の大会(`status !== "complete"`)が`events/`に確定生成されず`pending.json`に記録されること。完了後の再実行で`events/`に移動し`pending.json`から消えること。**(2026-07-20追加)** `status === "canceled"`のイベント(実例: id 50271)がpendingに残留せず即除外されること
8. GitHub Actionsワークフローが`workflow_dispatch`で手動実行でき、変更があった場合のみcommit・pushされること(差分なしなら何もしない)
9. **(2026-07-20更新)** バックフィル後にファイル数が大会数基準(2階層構成)になっていること。**生成ファイル数が既存2,418件+大会数分(目安421件前後)に収まり、デッキ数(約1万)に比例して増えていないこと**を確認する(1デッキ=1ファイルの3階層に戻っていないことのチェック)。容量は`data/tournaments/`数十MB・`tournaments/`生成HTML(2階層化により大幅に圧縮される見込み。想定を大きく超える場合はissueで報告し再検討する)
10. サイト共通ヘッダに「大会入賞デッキ」が表示され、`index.html`・`cards/<slug>/`から遷移できること(モバイル幅でも既存の折り返し/トグルで問題なく表示されること)
11. **(2026-07-20追加)** 一覧ページで国コードが重複表示(「jpJP」のような見え方)にならないこと。「開催国」列が表示されること
12. **(2026-07-20追加)** 一覧ページの「シーズン」フィルタで絞り込みができ、他のフィルタ(カテゴリ/国/フォーマット/検索)と併用できること

## 未決定・将来検討(実装時に判断 or 別issue)

- 初回スキャンの遡り基準日(本設計書は「RDOシーズン開始」を仮案としているが、実装時に運用上妥当な範囲を再検討してよい)
- **(2026-07-20更新)** カテゴリ許可リストへの`invitational`/`worlds`の追加(今回のスキャン範囲では未検出。実データで確認でき次第、個別issue化して追加する)
- 入賞デッキの統計タブ流用(#3のコストカーブ検討と合わせて再検討)
- チーム戦(`teams`エンドポイント)への対応(現状は個人戦を前提)
- **(2026-07-20更新)** バックフィル対象期間をPTMシーズン(2025-12-12〜)まで拡張する案: 「カテゴリ分布の再実測」節のとおり**拡張は容量を減らすのではなく増やす**(store-championships/regionalsが概算+87件、ascentが+1件、容量にして数十MB規模の増加見込み)。2シーズン分のアーカイブとしての価値はあるため、今回のバックフィルをRDOシーズン単独でリリースしたあと、実際の容量実績を見てから改めて検討する
- データ増加の長期的な運用(GitHubリポジトリの容量が将来問題になる場合は「直近N年のみ保持」等のローテーションを別issueで検討。今回「Top8限定」は撤回したため、容量増加ペースは実装後の実測値で監視する)

## 参考

- issue: <https://github.com/dozyouneko/ga-card-tools-jp/issues/14>(調査コメント含む)
- 公式ドキュメント: <https://api-docs.gatcg.com/endpoints/omnidex>
- 既存資産: `tmp/ga-omnidex-mock/build.mjs`(IDスキャン+キャッシュのモック実装)
- 既存資産: `tools/deck-builder/app.js` の `parseOmnidexText`/`normCardName`/`resolveCardSlug`(Omnidexインポート機能)
- カードスナップショット: `tmp/api-cache/cards-snapshot.json`(`scripts/build-card-pages.mjs` と共用)
- シーズン期間: `https://omni.gatcg.com/api/season`(現行シーズン)・`?id=N`(過去シーズン、id降順で遡れる。2026-07-20確認)
