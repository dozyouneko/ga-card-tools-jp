# 撤去済みの「結果ダイアログ」を指す残存コメントの是正 — 設計書（2026-09-23）

## 改版履歴

| 版 | 日付 | 内容 |
|---|---|---|
| 1.1 | **2026-09-23** | ⭐ **承認時の改版**（レビュー S1 への回答）。**C1〜C3 は全件実装済み・V1〜V9 は dev とレビューの双方が実測して一致**したので、実施状況を実装済みに更新。⚠️ ⭐ **レビューが `ダイアログ\|モーダル\|modal` で全数走査し、同型の腐りを `app.js` にあと2件見つけた**（`el.resultGrid` の `click` ハンドラ直前／`totalQtyInDeck()` 直前）。**語が「ダイアログ」単独で、§2-2 が確定した `結果ダイアログ` の grep に構造上かからない**——⭐ **本書の範囲は変えず、派生issue #112 に出した**（判断は承認コメント）。⭐ **次に同型を洗うときの検索キーは `結果ダイアログ` ではなく `ダイアログ\|モーダル\|modal`** |
| 1.0 | **2026-09-23** | 初版。⭐ **起票案（2026-09-17）の主張を設計担当が再測定してから起こした**。**核の症状は再現した**が、起票案には**過少（対象が3件ではなく1件しか挙がっていない）**と**誤り（`V1` の期待値「0件」は達成してはいけない）**が1件ずつあったので、**両方を是正して仕様化した**（→ §2）。✅ **実装済み**（`73289eec`） |

- 起票: **#106**（`enhancement` / `着手可` / `小タスク`）
- 起票案: [`../未採番-検索結果のタブ化/派生起票案_2026-09-17_結果ダイアログ前提の残存コメント.md`](../未採番-検索結果のタブ化/派生起票案_2026-09-17_結果ダイアログ前提の残存コメント.md)
  （⚠️ **親タスクのフォルダなのでリネーム・移動しない**）
- 親タスク: [「検索結果のタブ化」設計書](../未採番-検索結果のタブ化/検索結果のタブ化_設計.md)（1.5 §11-10 で本件を派生に送った）と
  [`承認_2026-09-17.md`](../未採番-検索結果のタブ化/承認_2026-09-17.md) §4-1
- 設計の起点 HEAD: **`e26778f5`**（`tools/deck-builder/` は 2026-09-20 公開ぶんを含む）

---

## 1. このタスクで何をするか

**「結果ダイアログ」という、T4（`f3c4e8a5`・`533ff220`）で撤去済みの画面部品を、まだ存在するかのように指しているコメントを、いまの実態に合わせて書き換える。**

- ⭐ **コメントだけを変える。コードは1行も変えない**（→ §5 の V4・V5 が機械的に担保する）
- ⭐ **画面の見た目・挙動は変わらないので、ユーザーの実物確認工程は挟まない**（進行役経由のユーザー決定・2026-09-23）。
  ⚠️ 設計担当の再測定でも**挙動が変わる要素は見つからなかった**（§2-3）

---

## 2. ⚠️ 起票案の再測定（2026-09-23・設計担当が実施）

⭐ **起票案は「確定した事実」ではなく投函前の下書き**なので、仕様化の前に主張を全部測り直した。

### 2-1. 測定結果

| # | 起票案の主張 | 実測 | 判定 |
|---|---|---|---|
| A | `searchStatusText()` の直前のコメントが「結果ダイアログの件数表示。」と書いている | **実在**（2行コメントの1行目）。`git blame` は **`51b1df48`（2026-07-26）**＝**T4 より前**に書かれたまま取り残されている | ✅ **再現** |
| B | 結果ダイアログは T4 で撤去済みで、件数はタブの結果パネルに出る | **正しい**。`#result-modal` の実体はコード上に無く（残る言及はすべて「旧 #result-modal」と断った経緯の記録）、`rerunIfResultOpen` は**リポジトリ全体で0件**。`searchStatusText()` の戻り値は `tab.statusText` を経由して **`#result-count`**（`#ed-pane-search` の直下）に入る | ✅ **再現** |
| C | 直し方は「コメント1行の文言を書き換えるだけ」 | ⚠️ **過少**。**同型の腐りがほかに2件ある**（§2-2 の C2・C3）。C2 は**同じ `app.js` の中**にあり、起票案は**見落としている** | ⚠️ **是正** |
| D | 検証項目 `V1`: `grep -n '結果ダイアログ' tools/deck-builder/app.js` が **0件** | ❌ **達成してはいけない**。`updateElementWarn()` の直前には、**T5（`395d61e6`）が意図して置いた**「⚠️ ⭐ 旧コメントの前提『結果ダイアログが検索フォームを覆う』は T4 で消えた」がある。**0件にするとこの経緯の記録を壊す** | ❌ **誤り → §5 で是正** |
| E | 「同型の取り残しは `updateElementWarn()` 側だけ T5 で消えている」 | **正しい**。`395d61e6` が同関数のコメントだけを直し、`searchStatusText()` 側は残った（親設計書 1.3 の ④ が「コメント2行を消す」と指示していたが、**片方しか消えなかった**） | ✅ **再現** |

### 2-2. ⭐ 実測で確定した「腐っているコメント」の全数（＝本タスクの対象）

**`tools/deck-builder/` の非 `docs` ファイルにある `結果ダイアログ` の言及は 8件**（`app.js` 3・`index.html` 2・`style.css` 3）。
そのうち **腐っている＝存在しない部品を現存するかのように指しているのは次の3件だけ**:

| # | 場所 | いまの文面（要点） | なぜ腐っているか |
|---|---|---|---|
| **C1** | `app.js`・**`searchStatusText()` の直前**（2行コメントの1行目） | 「**結果ダイアログの件数表示。**AND指定・日本語モードでは…」 | **描画先の名前が誤り**。実際は `#result-count`（結果パネル `#ed-pane-search` の直下）。⭐ **起票案の核** |
| **C2** | `app.js`・**`el.resultGrid` の `change` ハンドラの直前**（1行） | 「**枚数の直接入力(検索結果ダイアログ)**」 | **同型**。`#result-grid` は `#ed-pane-search` の直下で、ダイアログではない。⚠️ **起票案の見落とし**（`blame` は **`f76cdd78`（2026-07-08）**＝C1 より更に古い） |
| **C3** | `index.html`・**`.result-sort` の直前**（HTMLコメント3行） | 「⚠️ **結果ダイアログの中ではなくここに置く**」「変更したら**結果ダイアログが閉じていても再検索する**」 | **部品名に加えて挙動の説明も腐っている**。いまの挙動は `updateActiveSearchTab()` による「**表示中の検索タブをその場で更新する（タブは増やさない）**」で、`app.js` 側の同じ箇所のコメントには**既に正しく書かれている**＝**2つの記述が食い違っている** |

### 2-3. ⭐ 触ってはいけない5件（**意図して残っている経緯の記録**）

⚠️ **次の5件は「旧」「〜だった頃の名残」「器ごと不要になった」と明示しており、`結果ダイアログ` / `#result-modal` の語を
わざと含んでいる。消すと T4・T5 が残した判断の根拠が失われる**:

| # | 場所 | 残す理由 |
|---|---|---|
| K1 | `app.js`・**`updateElementWarn()` の直前** | ⭐ **T5（`395d61e6`）が本件と同型の是正として書いたもの**。旧前提を引用して「T4 で消えた」と打ち消している |
| K2 | `app.js`・**`el.resultPane` の定義の直前** | 「旧 `#result-modal` を撤去し…タブ面にした」＝経緯の記録 |
| K3 | `index.html`・**`#ed-pane-search` の直前** | 「旧 `#result-modal` の中身をそのまま右列へ移した」＝経緯の記録 |
| K4 | `style.css`・**`.result-sort` 系の2箇所** | 「置き場は結果ダイアログではなく検索パネルの中」「**結果ダイアログの中にあった頃の名残で**、旧値へ戻すとまた後勝ちで小さくなる」＝⚠️ **将来の編集者への警告そのもの** |
| K5 | `style.css`・**スマホ帯の末尾** | 「旧 `#result-modal { left: 320px }` は**器ごと不要になった**」＝経緯の記録 |

⭐ **K1・K4・K5 があるため、「`結果ダイアログ` を全部消す」は誤った目標**になる（§2-1 の D）。

---

## 3. 変更内容（⭐ **文面は本書で確定する。実装側で言い換えない**）

⚠️ **下の「変更後」をそのまま入れる**。⭐ **コメントの文面が本タスクの成果物そのもの**なので、
レビューが照合できるように一字一句を設計書で決める。

### 3-1. C1 — `app.js`・`searchStatusText()` の直前

**変更前（この2行を置き換える）**

```js
// 結果ダイアログの件数表示。AND指定・日本語モードでは客側で後段フィルタが入るため、
// 総件数を正確に出せないことがある(#31 変更6)。
```

**変更後（3行）**

```js
// 検索結果パネル（#ed-pane-search）の件数欄 #result-count に出す文言。
// ⚠️ ⭐ 描画先は T4 でモーダルから右列のタブ面に変わった（戻り値は tab.statusText 経由で入る）。
//    AND指定・日本語モードでは客側で後段フィルタが入るため、総件数を正確に出せないことがある(#31 変更6)。
```

### 3-2. C2 — `app.js`・`el.resultGrid` の `change` ハンドラの直前

**変更前（1行）**

```js
// 枚数の直接入力(検索結果ダイアログ)
```

**変更後（1行）**

```js
// 枚数の直接入力（検索結果パネル #ed-pane-search の結果カード内）
```

### 3-3. C3 — `index.html`・`.result-sort` の直前

**変更前（3行）**

```html
      <!-- 並び替え・昇降順（左ペイン化_設計 §5）。⚠️ 結果ダイアログの中ではなくここに置く。
           トップページの .filter-rest（#f-sort / #order）と同じ位置づけで、
           変更したら結果ダイアログが閉じていても再検索する（app.js のハンドラ・§5-2）。 -->
```

**変更後（4行）**

```html
      <!-- 並び替え・昇降順（左ペイン化_設計 §5）。⚠️ 結果パネル側ではなくこの検索パネルに置く。
           トップページの .filter-rest（#f-sort / #order）と同じ位置づけで、
           変更したら表示中の検索タブをその場で更新する
           （app.js の updateActiveSearchTab()・検索結果のタブ化_設計 §2-3。タブは増やさない）。 -->
```

---

## 4. 判断済み事項（⚠️ **実装側で蒸し返さない**）

| # | 判断 | 理由 |
|---|---|---|
| **J1** | ⭐ **C1・C2 の置き換え文には「結果ダイアログ」の語を残さない** | **T5（K1）は旧前提を引用したが、あれは「前提が変わったこと自体」が情報だったから**。C1・C2 は**単に描画先の名前が誤っている**だけで、旧名を引用しても情報が増えず、**`grep` のノイズだけが永久に残る** |
| **J2** | ⭐ **対象を `app.js` の外（C3・`index.html`）にも広げる** | ⚠️ **issue のタイトルは `app.js` と書いているが、C3 は同じ型で、しかも「挙動の説明が `app.js` 側と食い違っている」ぶん C1 より有害**。⭐ **1行の差し替えで、型そのものを消し切れる**（起票案の「同じ型が2件あって片方だけ直した状態が解消する」の趣旨）。**issue のタイトルは変えなくてよい**（本書が範囲の正） |
| **J3** | ⭐ **`style.css` の3件（K4・K5）は触らない** | §2-3 のとおり**意図して残っている警告・経緯**。⚠️ **本タスクで「ついでに」整形しない** |
| **J4** | ⭐ **実物確認工程は挟まない** | **コメントのみの変更で、`node --check` と差分検査（V4・V5）で挙動不変を機械的に示せる**。⚠️ **もし実装中に「コードも変えないと直らない」と判明したら、その時点で止めて `設計確認待ち` に戻すこと**（推測で挙動を変えない） |
| **J5** | **行番号で参照しない** | `npm run validate` の `no line-number refs in code comments` 検査に当たる（`app.js` は走査対象）。⭐ **関数名・id で書く**（§3 の文面は既にその形） |

---

## 5. 検証項目

⚠️ ⭐ **V1〜V3 の期待値は、§3 で確定した文面を設計担当が実際に適用して数えた**（2026-09-23 実測）。
**測り方**: 2ファイルを作業ツリー外へ複製し、§3 の「変更前」を**一致1回**であることを確かめてから「変更後」へ置換し、
`grep -c` / `node --check` / `diff -U0` / `REF_RE` を回した。⭐ **アンカーは3件とも「ちょうど1回」一致した**
（＝実装側でも一意に置換できる）。

| # | 見るもの | 期待値 |
|---|---|---|
| **V1** | `grep -c '結果ダイアログ' tools/deck-builder/app.js` | ⭐ **`1`**（⚠️ **`0` ではない**。残るのは **K1＝`updateElementWarn()` の直前の T5 のコメント1行だけ**。変更前は `3`） |
| **V1-b** | `grep -n '結果ダイアログ' tools/deck-builder/app.js` の**唯一のヒット行** | **「⚠️ ⭐ 旧コメントの前提「結果ダイアログが検索フォームを覆う」は T4 で消えた（結果はモーダルではなく」** で始まる行であること（＝**K1 が消されていない**こと） |
| **V2** | `grep -c '結果ダイアログ' tools/deck-builder/index.html` | **`0`**（変更前は `2`） |
| **V3** | `grep -c '結果ダイアログ' tools/deck-builder/style.css` | ⭐ **`3`（変更前と同じ＝手を触れていない）** |
| **V4** | `git diff -U0 -- tools/deck-builder/app.js` の変更行（先頭 `+`/`-`、`+++`/`---` を除く）| ⭐ **全行が空白を除いて `//` で始まる**（＝**コメント行しか動いていない**） |
| **V5** | `git diff -U0 -- tools/deck-builder/index.html` の変更行 | ⭐ **全行が §3-3 の HTML コメントブロックの内側**（`<!--` 〜 `-->`）であること |
| **V6** | `node --check tools/deck-builder/app.js` | **exit 0**（構文が壊れていない） |
| **V7** | `npm run validate` | **exit 0**。⚠️ とくに成功行 **`no line-number refs in code comments — 0件（46ファイル走査）`** が出ること（J5） |
| **V8** | 変更したファイルの集合（`git show --name-only`） | ⭐ **`tools/deck-builder/app.js` と `tools/deck-builder/index.html` の2つだけ**（⚠️ `style.css`・生成物・`docs/` を含めない） |
| **V9** | ⭐ **挙動が変わらないこと**（デッキ構築の検索→件数表示・枚数の直接入力・並び替えの変更） | **変更前と同じ**。⚠️ **コメントのみの変更なので V4〜V6 が通れば足りる**——**ヘッドレスでの実駆動までは求めない**（→ §6） |

### 5-1. ⚠️ V1 の読み違いを防ぐための注意（レビュー向け）

- ⭐ **`0件` を良い結果として報告してはいけない。** `0件` は **K1 を消してしまった**ことを意味し、**不合格**とする
- ⭐ **`3件` のまま**なら C1・C2 が手つかず（不合格）

---

## 6. 実物確認（ユーザー操作）について

⭐ **本タスクでは挟まない**（§4 の J4・進行役経由のユーザー決定）。

- 理由: **画面に出る文字列・DOM・CSS のいずれも変わらない**。変わるのは**ソースコード中のコメントだけ**で、
  V4・V5 がそれを機械的に示す
- ⚠️ **例外**: 実装中に「コメントを直すにはコードの変更が要る」と判明した場合は、**その時点で作業を止めて
  `設計確認待ち` に戻す**。⭐ **本書の前提（挙動不変）が崩れたら、実物確認の要否から設計し直す**

---

## 7. 実装単位

⭐ **1単位**（C1・C2・C3 をまとめて1コミット）。⚠️ **分割しない**——3件とも同じ型の是正で、
分けると V1 の期待値（`1`）が途中のコミットで成立しない。

- コミットメッセージの例: `106(結果ダイアログ前提の残存コメント): 撤去済みの部品を指すコメント3件を実態に合わせる`
- ⚠️ **担当パスは `tools/deck-builder/` のみ**。`git add -A` は使わず、**2ファイルを明示してステージする**

---

## 8. 公開について

- ⚠️ **`tools/` は本番配信対象**なので、**push は本番公開**になる（コメントだけでも配信物は変わる）
- ⭐ **承認後は `push待ち`**。⚠️ **単独で push する必要はない**——**次の「大会データの週1更新」や他タスクの公開に同乗させてよい**
  （⭐ ただし**同乗するものを提示してから**ユーザーの可否を仰ぐこと）
