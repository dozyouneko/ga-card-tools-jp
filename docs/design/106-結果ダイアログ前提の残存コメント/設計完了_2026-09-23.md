**対象: 「結果ダイアログ前提の残存コメント」**

## 設計完了（#106）— 撤去済みの「結果ダイアログ」を指す残存コメントの是正

- **設計書**: `docs/design/106-結果ダイアログ前提の残存コメント/残存コメント_設計.md`（1.0）
- **設計の起点 HEAD**: `e26778f5`

### 1. ⚠️ まず、起票案を再測定した（結論: **核は再現。ただし起票案に過少1件・誤り1件**）

| # | 起票案の主張 | 実測 |
|---|---|---|
| A | `searchStatusText()` の直前コメントが「結果ダイアログの件数表示。」 | ✅ **再現**（`blame` は `51b1df48`・2026-07-26＝T4 より前の取り残し） |
| B | 結果ダイアログは T4 で撤去済み、件数は結果パネルに出る | ✅ **再現**（`#result-modal` の実体は無し・`rerunIfResultOpen` は全体で0件。戻り値は `tab.statusText` 経由で `#result-count` に入る） |
| C | 「コメント1行を書き換えるだけ」 | ⚠️ **過少。同型がもう2件ある** — 同じ `app.js` の `el.resultGrid` の `change` ハンドラ直前「枚数の直接入力(検索結果ダイアログ)」（起票案の**見落とし**）と、`index.html` の `.result-sort` 直前 |
| D | 検証 `V1`: `grep 結果ダイアログ` が **0件** | ❌ **誤り。0件にしてはいけない** — `updateElementWarn()` の直前には **T5（`395d61e6`）が意図して置いた**「旧コメントの前提…は T4 で消えた」がある。0件にすると**経緯の記録を壊す** |

⭐ **`index.html` のものは C1 より有害**: 部品名だけでなく**挙動の説明（「結果ダイアログが閉じていても再検索する」）も腐っており、
`app.js` 側の同じ箇所の正しい記述と食い違っている**（いまは `updateActiveSearchTab()` による「表示中のタブをその場で更新・タブは増やさない」）。

### 2. 対象範囲

- **直す3件（C1〜C3）**: `tools/deck-builder/app.js` の **`searchStatusText()` 直前**と
  **`el.resultGrid` の `change` ハンドラ直前**、`tools/deck-builder/index.html` の **`.result-sort` 直前**
- ⚠️ **触らない5件（K1〜K5）**: `app.js` の `updateElementWarn()` 直前（T5 が意図して残した）・`el.resultPane` 定義の直前・
  `index.html` の `#ed-pane-search` 直前・`style.css` の3箇所（「〜だった頃の名残」「器ごと不要になった」＝**将来の編集者への警告と経緯**）
- ⭐ **置き換える文面は設計書 §3 で一字一句を確定してある**（コメントの文面が成果物そのものなので、実装側で言い換えない）

### 3. 検証項目の要約（詳細は設計書 §5）

| # | 期待値 |
|---|---|
| **V1** | `grep -c '結果ダイアログ' tools/deck-builder/app.js` = ⭐ **`1`**（⚠️ **`0` は不合格**＝K1 を消した証拠。変更前は `3`） |
| **V1-b** | その唯一のヒットが **`updateElementWarn()` 直前の T5 のコメント**であること |
| **V2** | 同 `index.html` = **`0`**（変更前 `2`） |
| **V3** | 同 `style.css` = **`3`**（変更前と同じ＝未変更） |
| **V4/V5** | `git diff -U0` の変更行が **すべてコメント行**（`app.js` は `//`・`index.html` は当該 HTML コメントの内側） |
| **V6** | `node --check tools/deck-builder/app.js` が exit 0 |
| **V7** | `npm run validate` が exit 0（成功行 `no line-number refs in code comments` を含む） |
| **V8** | 変更ファイルは **2つだけ**（`app.js` / `index.html`） |
| **V9** | 挙動が変わらないこと（⭐ **V4〜V6 で足りる。実駆動までは求めない**） |

⭐ **V1〜V3 の数値は、設計担当が作業ツリー外の複製に §3 の文面を実際に適用して数えた**
（アンカーは3件とも**ちょうど1回**一致＝一意に置換できる）。

### 4. 判断済み事項（⚠️ 蒸し返さない）

- **J1**: C1・C2 の置き換え文に **「結果ダイアログ」の語を残さない**（T5 は旧前提の引用に意味があったが、こちらは**描画先の名前が誤っているだけ**で、残すと `grep` のノイズが永久化する）
- **J2**: ⭐ **`index.html` にも範囲を広げる**（issue のタイトルは `app.js` だが、**同型で、より有害**。**タイトルは変えなくてよい＝範囲の正は設計書**）
- **J3**: `style.css` の3件は**触らない**（意図して残っている警告・経緯）
- **J4**: ⭐ **ユーザーの実物確認工程は挟まない**（**コメントのみの変更で挙動不変**。進行役経由のユーザー決定）
- **J5**: **行番号では参照しない**（関数名・id で書く。`validate` の検査対象）

### 5. 未決事項

- **なし**（実装に必要な判断はすべて設計書で確定した）
- ⚠️ **ただし1つだけ条件付きの止め方を指定してある**: 実装中に**「コメントを直すにはコードの変更も要る」と判明した場合は、
  その場で作業を止めて `設計確認待ち` に戻すこと**（⭐ **挙動不変という本書の前提が崩れたら、実物確認の要否から設計し直す**。推測で挙動を変えない）

### 6. 公開

- ⚠️ `tools/` は**本番配信対象**なので push は本番公開。⭐ **承認後は `push待ち`** とし、
  **単独 push は不要**——次の週1更新や他タスクの公開に**同乗させてよい**（同乗するものを提示してから可否を仰ぐ）

### 7. 実装単位

⭐ **1単位・1コミット**（C1〜C3 をまとめる。⚠️ 分割すると V1 の期待値 `1` が途中で成立しない）。
担当パスは `tools/deck-builder/` の**2ファイルのみ**を明示してステージ（`git add -A` は使わない）。
