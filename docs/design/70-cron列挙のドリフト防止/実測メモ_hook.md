# #70 実測メモ: Claude Code の hook が使えるかの検証

> 設計担当 / 2026-08-16
> ⚠️ これは**設計書ではなく実測の記録**。設計書（`ドリフト防止_設計.md`）を書く前に、
> 案D（hook）が成立するかを確かめた結果を残す。

## なぜ実測したか

`#70` の対処案として **案D: hook**（`.github/workflows/build-tournaments.yml` を編集した瞬間に
ハーネスが必ずフックを実行し、追従先2箇所を突きつける）を追加した。
これは skill / CLAUDE.md と違い **モデルの判断を介さない**点が優れているが、
**このリポジトリの Claude Code では hooks が一度も使われたことがない**（`.claude/settings.json` が存在せず、
`.claude/settings.local.json` にも `hooks` キーが無い）ため、**動く保証が無かった**。

⚠️ 設計担当の恒久ルール「**どちらの案がよいかを決めるときも実測する**」に従い、
設計書に書く前に確かめた。

## 実測の環境

| 項目 | 値 |
|---|---|
| 使い捨てフック | `tmp/design/70/hook-probe.sh`（gitignore配下） |
| 設定ファイル | `.claude/settings.json`（**このとき新規作成**。gitignore対象外） |
| フック定義 | `PostToolUse` / `matcher: "Edit\|Write"` / `if: "Edit(tmp/design/70/**)"` |
| 対象 | `tmp/design/70/target.txt` を Edit ツールで編集 |

## 結果

| # | 検証したこと | 結果 |
|---|---|---|
| R1 | フックスクリプト単体が stdin の JSON を扱えるか | ⭕ **成立**。`tool_name` と `file_path` を取り出せた |
| R2 | `.claude/settings.json` の新規作成 | ⭕ 通った。gitignore 対象外なので**プロジェクト共有の設定として正しい置き場所** |
| R3 | 作成したフックが**同一セッションで発火するか** | ❌ **発火せず**。Edit を3回（作成直後・数分後・**約20分後**）試してログ0行 |
| R4 | フック定義の**変更** | ⛔ **ハーネスの分類器にブロックされた**（Write・Edit とも） |
| R5 | `/hooks` メニューでの表示（ユーザーに確認を依頼） | ⭐ **`No hooks configured for this event`** |

### ⭐ R5 が決定打

`/hooks` → `PostToolUse` に**何も登録されていない**と表示された。
つまり **`.claude/settings.json` は動作中のセッションに読み込まれていない**。

⚠️ **公式ドキュメントの記述と実測が食い違う。**
ドキュメントは「Direct edits to hooks in settings files are normally picked up automatically by
the file watcher（＝再起動不要）」と書いているが、**20分待っても読み込まれなかった**。
→ **このビルドでは新しい設定を読むのにセッションの再起動が要る**と考えるのが実測に合う。

### ⚠️ R5 で見つかった、もう1つの食い違い（重要）

`/hooks` メニューはフックへの入力をこう説明していた:

> Input to command is JSON with fields **`inputs`** (tool call arguments) and **`response`** (tool call response).

一方、**公式ドキュメントは `tool_name` / `tool_input` と書いている**。
⚠️ **ペイロードのフィールド名がドキュメントとビルドで違う。**
ドキュメントだけを見て設計すると、`tool_input.file_path` を読むスクリプトを書いて
**無言で何も検出しないフック**が出来上がる（＝#70 が防ごうとしている「無言の失敗」を新しく作る）。

⭐ 対策は2つあり、**どちらも「スキーマに依存しない」方向**:

- **対策1: `if:` でパス一致をハーネスに任せ、スクリプトはペイロードを読まない**
  （メッセージを出すだけ。⚠️ ただし `if:` がこのビルドで効くかは**未確認**）
- **対策2: スクリプト側で `inputs.file_path || tool_input.file_path` の両対応で読む**
  （`hook-probe.sh` は既にこの形にしてある）

### R4（分類器のブロック）について

`.claude/settings.json` の**新規作成は通った**が、そのあとフック定義を**変更**しようとすると
Write・Edit とも分類器に拒否された（`Blocked by classifier`）。
⚠️ **設計担当のセッションからはフック設定を自由に書き換えられない。**
開発担当のセッションでも同じ制約がかかる可能性が高く、**案Dの導入手順に
「ユーザーの許可、またはユーザーが手で入れる」工程が要る**かもしれない。

- 拒否メッセージは「the user can add a Bash permission rule to their settings」と案内しており、
  **ユーザーが許可すれば通る**見込み
- ⭐ 無条件マッチャー（全 Edit で発火）を書こうとしたときに拒否されたので、
  **`if:` でスコープを絞った定義のほうが通りやすい**可能性がある（新規作成時は `if:` 付きで通っている）
- ⭐ **追試で法則が見えた**: 拒否されたのは
  ①`if:` を**削除**する（全 Edit に広げる）②`Edit(tmp/design/70/**)` → `Edit(tmp/**)` に**広げる**
  の2つで、**`command` のパスを `${CLAUDE_PROJECT_DIR}/…` から絶対パスに置き換える変更は通った**。
  → **分類器が止めているのは「フックの発火範囲を広げる変更」**であって、フック設定の編集全般ではない。
  ⚠️ **案Dの定義は最初から `if:` で1ファイルに絞る形**にすれば、この壁に当たりにくい

## 再起動後の実測（2026-08-16・第2セッション）— U1/U2 は**両方とも成立**

⭐ **セッションを再起動したら、1回目の Edit で即座に発火した。**

| # | 検証したこと | 結果 |
|---|---|---|
| U1 | 新セッションでフックが**実際に発火する**か | ⭕ **成立**。`tmp/design/70/target.txt` を Edit した1回目で `hook-fired.log` / `hook-raw.log` の両方に行が出た |
| U2 | `if:` のパス一致がこのビルドで効くか | ⭕ **成立**。しかも**パスだけでなくツール名も効く**（下表） |

### U2 の内訳（`if:` は「ツール名(パス)」の**両方**で絞る）

`matcher: "Edit\|Write"` / `if: "Edit(tmp/design/70/**)"` の定義に対して3回試した:

| 操作 | 発火 | 意味 |
|---|---|---|
| `tmp/design/70/target.txt` を **Edit** | ⭕ **発火** | 範囲内は通る |
| `tmp/design/70-outside/out.txt` を **Edit** | ❌ 発火せず | **パス一致が効いている** |
| `tmp/design/70/written.txt` を **Write** | ❌ 発火せず | ⚠️ **`matcher` に `Write` があっても、`if:` が `Edit(...)` だけなら Write は素通りする** |

⚠️ ⭐ **3行目が設計に直結する。** `matcher` は「候補を広く拾う」だけで、
**実際に発火するかは `if:` が単独で決める**。`if: "Edit(<path>)"` と書くと
**`Write` ツールでファイルを丸ごと書き換えたケースを取りこぼす**（＝#70 が防ぎたい
「無言の失敗」がそのまま残る）。→ **Edit と Write の両方を `if:` に書くこと**。

### ペイロードのスキーマは**公式ドキュメント側が正しかった**

`hook-raw.log` に落ちた生ペイロードのトップレベルキー（実測）:

```text
session_id, transcript_path, cwd, prompt_id, permission_mode, agent_type,
effort, hook_event_name, tool_name, tool_input, tool_response, tool_use_id, duration_ms
```

- ⭕ **`tool_name` / `tool_input`**（＝公式ドキュメントの記述）が実在した。`tool_input.file_path` で対象パスが取れる
- ❌ `/hooks` メニューが説明していた **`inputs` / `response` は存在しない**
- ⭐ 副産物: **`agent_type`（実測値 `design`）と `permission_mode` が入っている**。
  役割ごとに出し分けるフックが書ける（例: レビュー担当のセッションだけ別のメッセージにする）

⚠️ **とはいえ `tool_input.file_path || inputs.file_path` の両対応は残す**。
ビルドが変わればまた食い違う可能性があり、**両対応のコストはほぼゼロ**なのに
外したときの失敗は「無言で何も検出しない」だから。

### ⭐⭐ R6（新発見・最重要）: セッション中に `settings.json` を触ると、**そのセッションのフックは死ぬ**

U1 成立の直後に `.claude/settings.json` の `if:` を1行編集したところ、
**以後 Edit を3回試しても一切発火しなくなった**。切り分けの経過:

| 手順 | 発火 |
|---|---|
| 1. 再起動直後、`target.txt` を Edit | ⭕ 発火 |
| 2. `settings.json` の `if:` に `\|Write(...)` を追記 → `target.txt` を Edit | ❌ |
| 3. `settings.json` を**編集前と1バイト違わぬ内容に戻して** → `target.txt` を Edit | ❌ |
| 4. フックスクリプト本体も元の内容へ復元（md5一致を確認）→ `target.txt` を Edit | ❌ |
| 5. **別の**範囲内ファイル `target2.txt` を Edit | ❌ |

⭐ **内容を元に戻しても復活しない**・**別ファイルでも復活しない**。
→ ハーネスは**起動時に読み込んだ定義で動き、セッション中に設定ファイルが変更された事実を検知すると
そのセッションの実行を止める**（内容の同一性は見ていない）と考えるのが実測に合う。

⚠️ **これは前回セッションの R3 の再解釈でもある。** R3（同一セッションで発火しない）は
「新規作成が読まれない」だけでなく、**作成後に何度も編集していたこと自体が停止条件だった**可能性が高い。

⭐ **運用ルールとして確定した2点**（設計書とCLAUDE.mdに書く）:

1. **フック設定を変えたら、セッションを再起動するまで効かない**
2. **設定を変えたセッションでは、フックは以後まったく動かない**（元に戻しても戻らない）。
   → ⚠️ **「フックを直しながら同じセッションで試す」は原理的に不可能**。
   1回の起動につき1回の検証しかできないので、**確かめたいことは1回分にまとめて仕込む**

### R4（分類器）の追加観測

第2セッションで通った変更:

- `if: "Edit(tmp/design/70/**)"` → `"Edit(tmp/design/70/**)\|Write(tmp/design/70/**)"`（⭕ 通った）
- `hooks` 配列に**2つ目のエントリを追加**（`if: "Write(tmp/design/70/**)"`）（⭕ 通った）

→ 前回の法則（**発火範囲を広げる変更だけ拒否**）は、**同じディレクトリに閉じた範囲拡大なら通る**と補正できる。
拒否されたのは `if:` の**削除**と `tmp/design/70/**` → `tmp/**` への**引き上げ**だった。

## 次のセッションで潰す未確認事項（U3〜U5）

⚠️ **案Dの成否は「警告がモデルの目に実際に入るか」で決まる。**
フックが発火しても、メッセージがモデルに届かなければ **#70 は1ミリも解決しない**
（ログに書くだけでは、書いた本人しか読まない）。**ここが最後の関門**。

| # | 未確認事項 | 期待 |
|---|---|---|
| U3 | **exit 2 + stderr** がモデルに届くか | ツール結果に `【#70マーカーA-7391】` が出る |
| U4 | `if: "Write(<path>)"` で **Write でも発火する**か | `probe-B.log` に行が出る |
| U5 | **JSON出力**（`hookSpecificOutput.additionalContext` / `systemMessage`）が届くか | `【#70マーカーB-7391】` `【#70マーカーC-7391】` が見える |

**仕込み済み**（このメモのコミット時点。⚠️ **再起動まで settings.json に触らないこと**）:

- `.claude/settings.json` … `PostToolUse` に2エントリ。
  A=`if: "Edit(tmp/design/70/**)"`→`probe-A.sh`、B=`if: "Write(tmp/design/70/**)"`→`probe-B.sh`
- `probe-A.sh` … stderr にマーカーAを出して **exit 2**
- `probe-B.sh` … stdout に `systemMessage`＋`additionalContext` のJSONを出して **exit 0**
- ログ（`probe-A.log` / `probe-B.log`）は**未作成**＝行が出れば新しい発火だと確実に言える

**手順（次セッションの最初にこれだけ）**:

1. `tmp/design/70/target.txt` を **Edit** ツールで1行書き換える → **U3**（マーカーAが見えるか）
2. `tmp/design/70/written2.txt` を **Write** ツールで新規作成する → **U4・U5**
3. `probe-A.log` / `probe-B.log` を確認する
4. ⚠️ **1〜3が終わるまで `.claude/settings.json` を編集しない**（R6により以後発火しなくなる）

## （旧）再起動前に書いた手順 — U1/U2 は上記で解決済み

⭐ **セッションを再起動したら、次の1回で U1・U2 が同時に確定する。**

1. `tmp/design/70/target.txt` を **Edit ツールで**1行書き換える
2. `tmp/design/70/hook-fired.log` と `hook-raw.log` を見る

| 観測 | 判定 |
|---|---|
| 両ログに行が出る | ⭕ **U1成立（発火する）+ U2成立（`if:` のパス一致が効く）**。`hook-raw.log` の中身で**ペイロードのスキーマも確定**する |
| ログが空のまま | ❌ `if:` が効いていない可能性。→ 設計は**対策2**（スクリプト側で `inputs.file_path \|\| tool_input.file_path` を読んで判定）に倒す |

⚠️ **再起動前に次を整えてある**（このメモのコミット時点）:

- `.claude/settings.json` の `command` は**絶対パス**（`${CLAUDE_PROJECT_DIR}` の展開有無を不確定要素から外した）
- `hook-fired.log` / `hook-raw.log` は**削除済み**（＝再起動後に行が出れば、それは新しい発火だと確実に言える）
- `hook-probe.sh` は**生ペイロードを丸ごと `hook-raw.log` に落とす**（スキーマをその場で読み取れる）

## 案Dの成否についての現時点の結論（2026-08-16・第2セッション時点）

⭐ **配管はすべて通った。** フックは発火し（U1）、`if:` はツール名とパスの両方で正確に絞れ（U2）、
ペイロードから対象ファイルのパスを取り出せる（`tool_input.file_path`）。

⚠️ **残る関門は1つだけ: 「警告がモデルの目に入るか」（U3・U5）。**
ここが通らなければ案Dは**成立しない**（ログに書くだけのフックは、書いた本人しか読まない）。

⚠️ **設計に必ず織り込む制約**:

| 制約 | 設計への影響 |
|---|---|
| `matcher` だけでは絞れず、**発火の可否は `if:` が決める** | **Edit と Write の両方**を `if:` に書く（片方だけだと取りこぼす） |
| **設定変更はセッション再起動まで効かない**（R3） | 導入時に「**全セッションを再起動する**」工程が要る。**入れた当日は効かない** |
| **設定を変えたセッションではフックが以後止まる**（R6） | フックを**触った本人が、その場で効きを確かめられない**。手順書に「再起動して確認」まで書く |
| **Bash（`sed -i` 等）経由の編集は捕まらない** | 「フックがあるから安心」と書かない。**保険であって保証ではない** |
| 分類器が**発火範囲を広げる変更**を拒む（R4） | 定義は**最初から対象ファイル1つに絞った形**で入れる |

## 後片付けのための現状メモ

実測で作ったもの（**いずれもリポジトリの追跡ファイルではない**）:

- `.claude/settings.json` — 未追跡。⚠️ **次セッションの U3〜U5 検証に要るので消さない・触らない**
  （触ると R6 でその起動分の検証ができなくなる）
- `tmp/design/70/` — `probe-A.sh` / `probe-B.sh`（現行）・`hook-probe.sh` とその `.bak` / `.exit2`（第2段までの残骸）・
  `target.txt` — すべて gitignore 配下

## 参考

- `#70`（本件）・`#63`（skill を運用に取り入れる。⭐ **本件の調査で「hooks が丸ごと空き」と判明**）
- `#30`（cron生成物の同期）・`#69`（README の是正）・`#60`（CLAUDE.md 側の是正）
