**対象: 「起票案の整合検査」** — この宣言は 2026-09-11 に遡って追記した（索引の転記ドリフト防止）

# 未採番（起票案の整合検査）実装報告 — **承認後の残作業 S1-1 / S1-2**（✅ 完了 → `push待ち` 相当）

⚠️ **本来は該当issueのコメント。GitHubアカウント停止中で `gh issue comment` が打てないため、
代替としてこのファイルに記録する**（#73・未採番5件で確立済みの運用）。
復旧して採番されたら、本文をそのまま次のコマンドで投函する:

```bash
gh issue comment <n> --body-file docs/design/未採番-起票案の整合検査/実装報告_2026-09-02_残作業.md
```

⚠️ 投函順は承認記録 §8-4 のとおり **`実装報告_2026-09-02.md` の直後・`レビュー判定_2026-09-02.md` の前**。

- 日付: **2026-09-02**
- 対象: レビュー判定 `c747bb79` の**推奨指摘 S1-1 / S1-2**（ユーザーが**2件とも「採る」**と判断＝承認記録 §1）
- 前提: 実装本体 `04044d84` は**レビュー合格・ユーザー承認済み**。本報告は**その残作業だけ**を扱う
- ⚠️ **再レビューは不要**とユーザーが判断済み（承認記録 §4-4）。フルの V1〜V13 は再実行していない

---

## 1. 実施内容

| # | 指摘 | 変更したファイル | 内容 | 行数 |
|---|---|---|---|---|
| **S1-1** | `validate.mjs` の使い方コメントに本検査が載っていない | `scripts/validate.mjs` | 冒頭コメントに**1行追加**（`// 続いて docs/design/** の起票案の状態行と、索引（待ち行列と復旧手順.md）の整合を検査する（収録漏れと件数一致）。`） | **+1** |
| **S1-2** | `CLAUDE.md` に「命名の制約」が書かれていない | `CLAUDE.md` | 「タスク管理」節・`04044d84` の追記ブロック末尾に**3行追加** | **+3** |

- **コミット**: **`d5839e59`**（`未採番(起票案の整合検査): 承認後の残作業 S1-1/S1-2(コメント1行・文書3行)`）
- **変更ファイル**: ⚠️ **`CLAUDE.md` と `scripts/validate.mjs` の2つだけ**（`git show --name-only` で確認）
- **差分**: **4 insertions(+) / 0 deletions**＝⭐ **追加のみ。既存行は1行も変更していない**（設計書 §6）
- **push**: ❌ **していない**（本番公開の可否は常にユーザー判断）
- **生成物**: **0ファイル**（`git diff --stat d5839e59^ d5839e59 -- cards sets sitemap.xml data index.html tournaments` が空）

### 1-1. 承認記録 §4 の指示との対応

| 指示 | 遵守 | 実際 |
|---|:--:|---|
| §4-1 入れる位置 = #70 の行の直後・`// 最後に、` の行の直前 | ✅ | `validate.mjs` の**17行目**に挿入（16行目=#70 / 18行目=`// 最後に、`） |
| §4-1 文面（`続いて` で始める・issue番号を書かない） | ✅ | 承認記録の文面を**そのまま**使用。`加えて` の重複なし |
| §4-1 `// 最後に、` の行はそのまま | ✅ | 未変更 |
| §4-2 入れる位置 = `- ⚠️ **中身の「腐り」は防げない**…` の直後 / `- ⚠️ ⭐ **\`gh issue create\` する直前に…**` の前 | ✅ | `CLAUDE.md` **78〜80行目** |
| §4-2 インデントは2スペース・丸括弧は半角 `()` | ✅ | 既存の子項目と同一（`  - ⚠️ **…**(前方一致)。`） |
| §4-2 `<!-- … -->` マーカーは足さない | ✅ | 足していない（#70 の検査に触れない） |
| §4-5 ステージするのは2ファイルだけ・`git add -A` を使わない | ✅ | `git add scripts/validate.mjs CLAUDE.md` |
| §4-5 1コミットにまとめる | ✅ | `d5839e59` の1本 |
| §4-5 push しない | ✅ | していない |
| **ロジックに触れない** | ✅ | S1-1 は `//` で始まる行の追加のみ。S1-2 は文書のみ |

---

## 2. 検証結果（承認記録 §4-3 の R1 / R2）

⚠️ **フルの V1〜V13 ではなく、承認記録が指定した2点のみ**（再レビュー不要のため）。

| # | 手順 | 期待 | 結果 |
|---|---|---|:--:|
| **R1** | `npm run validate` | **exit 0** かつ末尾が所定の順・件数 | ✅ **合格** |
| **R2** | `git status --short` | `scripts/validate.mjs` と `CLAUDE.md` の2件だけ | ✅ **合格** |

### 2-1. R1 の実出力（末尾）

```text
cron git add paths in sync — 8パス（README.md / CLAUDE.md）
issue drafts in sync — 起票案11件（生きている6 / 完了4 / 取り下げ1）
card term highlights up to date — 2495 ページ

all checks passed.
```

- **exit code = 0**（`EXIT=0` を実測）
- ⭐ **順序・件数とも承認記録 §4-3 R1 の期待値と完全一致**
- ⚠️ ⭐ **`CLAUDE.md` を触ったので #70 の検査（`cron git add paths in sync`）を必ず通す**という
  §4-4 の要求は満たしている（**8パス一致で通過**）
- ⚠️ ⭐ **設計書 §5 の V1 が書く `7 / 3 / 1` との差は正常**——承認時に本タスク自身の起票案を
  `生きている` → `完了` に変えたため（承認記録 §6-1・§4-3 R1 の注記）。**食い違いとして報告しない**

### 2-2. R2 の実出力

- コミット**前**: `M CLAUDE.md` / `M scripts/validate.mjs` の**2件のみ**（`docs/` に差分なし）
- コミット**後**: **空**（クリーン）
- ⭐ 追加確認: `git -c core.quotepath=false show --name-only --format= d5839e59` →
  `CLAUDE.md` / `scripts/validate.mjs` の2行のみ（⚠️ 日本語パスのクォート誤判定を避けるため
  `core.quotepath=false` を付けた）

### 2-3. 足した行が意図どおり入ったことの確認

- `scripts/validate.mjs`: 冒頭コメントが **#70 → 本検査 → 用語ハイライト** の順に並び、
  ⭐ **実際の検査の実行順と一致**している（`git diff` で目視確認）
- `CLAUDE.md`: 追記ブロックの子リスト**3つ目**として入り、
  次の項目（`gh issue create` の直前に実測）の**前**にある

---

## 3. 実行できなかった検証と、残っている確認

| 項目 | 扱い |
|---|---|
| V1〜V13 のフル再実行 | ⭐ **実施していない**。⚠️ **ユーザーが再レビュー不要と判断**（承認記録 §4-4）。根拠は「S1-1 はコメント1行・S1-2 は文書のみでロジックに触れない」こと。実装本体は `04044d84` から**1バイトも変わっていない** |
| 破壊試験（V2〜V12） | 同上。⭐ **検査ロジックが未変更なので、結果は `c6b2d932`（実装報告）と `c747bb79`（レビュー判定）の実測がそのまま有効** |
| 本番での反映確認 | ❌ **未実施**。⚠️ **push / `deploy:prod` はユーザー判断待ち**（承認記録 §7）。手順と見どころは承認記録 §7 の表を参照 |

---

## 4. 差分が想定と食い違った点

**なし。** 追加4行がすべて承認記録 §4-1 / §4-2 の指定どおりで、副次的な差分は出ていない。

---

## 5. 設計書からの逸脱と根拠

**逸脱なし。**

- 設計書 §6「**既存行は変更しない**」— ✅ **守っている**。追加は**新規4行のみ**で、既存行の書き換えは0
- ⭐ S1-1 が §6 に抵触しないことは**承認記録 §3 が明示**している
  （§6 の趣旨は「既存の**検査ロジック**を触らせない」であり、冒頭の目次コメントへの追記は対象外。
  ⚠️ **責は曖昧に書いた設計側にある**と設計担当が記録済み）
- `CLAUDE.md` の表記は**入れる先の流儀**に合わせた（丸括弧は半角 `()`・インデント2スペース）＝承認記録 §4-2 の指示どおり

---

## 6. 設計担当への確認事項

**なし。** 承認記録 §4 が「これだけ読めば作業できる形」で書かれており、判断が要る箇所は残っていない。

---

## 7. ⏭️ 次の担当

⭐ **次の担当: ユーザー（ラベル相当: `push待ち`）**

- 根拠: `CLAUDE.md`「#64 の例外」——⭐ **承認後に軽微な残作業で `実装待ち` へ戻したときは、
  その作業を終えた開発担当が `push待ち` を付けてよい**（承認記録 §1-1 が本タスクをこの経路と明示）
- ⚠️ **GitHub停止中のためラベルの実体は無い。** 本ファイルの本節が `push待ち` の記録である
  （復旧後の起票時は承認記録 §8-2 のとおり **`enhancement` `小タスク` ＋ `push待ち`**）
- ⚠️ **クローズしない**（本番公開の判断が残っている）

### 7-1. ⚠️ 未公開のコミット（本タスク分）

| コミット | 内容 |
|---|---|
| **`04044d84`** | 実装本体（`scripts/validate.mjs` +138行 / `CLAUDE.md` +8行） |
| **`d5839e59`** | 本残作業（`scripts/validate.mjs` +1行 / `CLAUDE.md` +3行） |

- ⚠️ ⭐ **GitHubアカウント停止中なので経路①（`git push origin main`）は使えない。**
  出すなら**経路②** `CONFIRM=publish npm run deploy:prod`。⚠️ **経路②も本番公開である**
- ⭐ **本番の見た目は変わらない**（`npm run validate` はビルドにも配信にも関与しない）が、
  ⚠️ **`scripts/` も `CLAUDE.md` も本番配信される**ので「見た目が変わらないから安全」とは判断しない（#24）
- ⚠️ **新規ファイルが1つも無い**ため 404→200 の到達マーカーが使えない。
  **素のURLと `?cb=` を並べ、20秒間隔で反復して全数一致を見る**
- ⭐ 反映確認の見どころ: `/scripts/validate.mjs` に **`issue drafts in sync`** が含まれること
  （⭐ 本残作業まで届いたかは **`// 続いて docs/design/**`** の1行、`/CLAUDE.md` なら
  **`起票案 で始まる名前の .md`** の行で見分けられる）
