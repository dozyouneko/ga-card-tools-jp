# 設計完了 — 起票案の「状態行」と索引の整合検査を `npm run validate` に足す

⚠️ **これは issueコメントの代替**（GitHubアカウント停止中で `gh issue comment` が打てないため、
ファイルに残す）。復旧して採番されたら、次のコマンドで**そのまま投函する**:

```bash
gh issue comment <n> --body-file docs/design/未採番-起票案の整合検査/設計完了_2026-09-02.md
```

- 日付: **2026-09-02**
- 設計時の HEAD: **`cfd743f8`**（作業ツリーはクリーン）
- 次の担当: **開発担当**（ラベル相当: **`実装待ち`**）

---

## 1. 設計書のパス

- ⭐ **正**: `docs/design/未採番-起票案の整合検査/状態行と索引の整合検査_設計.md`
- 起票用の下書き（復旧後に `gh issue create` する本文）:
  `docs/design/待ち行列/起票案_起票案の状態行と索引の整合検査.md`
  - ⚠️ ⭐ **実装は設計書だけを読むこと。起票案を読んではいけない。**
    起票案 §2-2 の照合方式（基名の部分一致）を**そのまま実装すると、正しい状態のリポジトリで
    exit 1 になる**（設計書 §2-2 で実測）。仕様は設計書 §3 が正
  - ⚠️ **起票案ファイルはまだ移動しない**（採番後に本フォルダへ移すのは
    `docs/design/待ち行列/待ち行列と復旧手順.md` §2 ③ の手順）

## 2. 対象範囲

`docs/design/**` の起票案（`起票案*.md` / `派生起票案*.md`・実測 **11件**）の**1行目の状態行**と、
索引 `docs/design/待ち行列/待ち行列と復旧手順.md` が**ドリフトしたら `npm run validate` を exit 1 にする**。

| 変更 | ファイル | 内容 |
|---|---|---|
| 変更1 | **`scripts/validate.mjs`** | 検査ブロックを1つ**追加**（既存行は変更しない）。設計書 §3 |
| 変更2 | **`CLAUDE.md`** | 「タスク管理」節に1項目を**追記**（文面は設計書 §4-3 に確定済み） |
| 生成物 | — | ⭐ **0ファイル** |

- 置き場所: **#70（cron列挙）ブロックの直後・用語ハイライト検査（唯一の `await` ブロック）の直前**。
  ⭐ **本検査は同期処理のままでよい**
- 見るのは **2点だけ**: ⑴ **収録漏れ**（起票案が索引にリンクされているか）
  ⑵ **件数一致**（索引の見出し `（**N件**）` と状態行から数えた実数）
- ⚠️ **索引の表構造・列は見ない**（索引を整形しただけで落ちる検査にしないため）

## 3. 検証項目の要約（**V1〜V13**・設計書 §5 が正）

⚠️ ⭐ **期待値は「実行して画面に出る最終形」で書いてあり、`- ` のエラー行数まで指定してある。**
**設計時にプロトタイプを書いて11シナリオを実行し、実データで数えた**（推測ではない）。

| # | 何を見るか | 期待 |
|---|---|---|
| **V1** | 正常時 | **exit 0**・`issue drafts in sync — 起票案11件（生きている7 / 完了3 / 取り下げ1）` が既存2行の間に出る |
| V2 | 起票案の1行目を削除 | exit 1・**2行**（状態行なし ＋ **件数比較を省略**した旨） |
| V3 | 状態を片方だけ変更 | exit 1・**2行**（`生きている 索引7 / 実数6` と `完了 索引3 / 実数4`） |
| V4 | 索引に載せずに起票案を追加 | exit 1・**3行**（収録漏れ ＋ 全件11/12 ＋ 生きている7/8） |
| **V5** | ⭐ **基名が重複する起票案のリンクを索引から削除** | exit 1・**1行**（**パスで**出ること）。⚠️ **起票案の照合方式では検出0件**＝この項目が方式の妥当性そのものを検証する |
| V6 | 索引の件数だけ変更 | exit 1・**1行** |
| **V7** | ⭐ 見出しの**強調を外す**（抽出失敗） | exit 1・**1行**（`見出しが見つかりません`）。⚠️ **「一致」に倒して緑にならないこと** |
| **V8** | ⭐ 見出しを**複製** | exit 1・**1行**（`2個 あります`）。#70 のレビュー指摘 S1 と同型 |
| V9 | 索引をリネーム | exit 1・**1行**のみ（⚠️ 収録漏れ11件が併発しないこと） |
| V10 | 索引のリンクに `./` を付ける | **exit 0**（正規化が効いていること） |
| **V11** | ⭐ 走査先を起票案0件のフォルダに変える | exit 1・`起票案が1件も見つかりません`。⚠️ **「0件だから全部一致」で緑にならないこと** |
| **V12** | ⚠️ **全部戻す** | `git status --short` が **`scripts/validate.mjs` と `CLAUDE.md` の2件だけ**・exit 0・**V1と同一出力** |
| V13 | 生成物0の確認 | `cards/` `sets/` `sitemap.xml` `data/` `index.html` に差分**0件** |

- ⭐ **ネットワーク不要・オフラインで全項目実施できる**（索引と `docs/design/**` を読むだけ）
- ⭐ `npm run validate` は**約2秒**（実測 `real 0m2.0s`）。破壊試験を13回回しても負担にならない
- ⚠️ **V2〜V11 の一時変更は1つずつ行い、次に進む前に必ず戻す**（併発すると期待するエラー行数が変わる）

## 4. 判断済み事項（**開発担当が悩まなくてよい点**）

設計書 §1-1 の D1〜D11 が全文。要点だけ:

| # | 決定 |
|---|---|
| **D1** | ⭐ **照合はパスの完全一致**（索引のリンクを索引の位置から解決して正規化する）。**基名の部分一致は採らない** |
| **D2** | 対象は `docs/design/**` の `.md` で、ファイル名が **`起票案` / `派生起票案` で始まる**もの（前方一致） |
| **D6** | 状態行を読めない起票案が1件でもあれば、**件数比較は行わず**「省略した」旨を1行出す |
| **D7** | 索引が読めない・見出しが取れない → **exit 1**（A・B とも実行しない。**「一致」に倒さない**） |
| **D8** | 起票案が **0件**なら **exit 1**（走査の壊れが黙って緑になるのを防ぐ） |
| **D9** | 見出しが **2個以上**なら **exit 1** |
| **D10** | ⭐ **`CLAUDE.md` の追記は同じコミットに含める**（文面は設計書 §4-3 に確定済み） |

## 5. ⚠️ 実装時に必ず読むこと（設計時に実測で潰した落とし穴）

| # | 落とし穴 | 設計書 |
|---|---|---|
| ① | ⚠️ **索引と同じフォルダの起票案は、リンクが `](起票案_….md)` と**スラッシュ無し**。`/` を要求すると**正しい状態で exit 1** になる（実測: 誤検知1件） | §2-2 ① |
| ② | ⚠️ **基名 `起票案_2026-08-26.md` が2ファイルある**（スターコーリング／版順序）。基名照合では**片方が他方を満たす** | §2-2 ② |
| ③ | ⚠️ **`起票案_2026-08-26.md` は `派生起票案_2026-08-26.md` の部分文字列**。部分一致では**派生側のリンクが本体側を満たす** | §2-2 ③ |
| ④ | ⚠️ **全件見出しは `**` の直後が `）` ではなく `・`**。閉じ括弧まで要求する正規表現は **0回一致** | §2-3 |
| ⑤ | ⚠️ **近接する誤爆候補が2本ある**（`#### ②-A 完了済みタスク **5件**` と `### ② issueを起票する（**完了済み5件 …**）`）。**正規表現を緩めると拾って壊れる** | §2-4 |
| ⑥ | ⚠️ ⭐ **`起票案` で始まる名前の `.md` は、起票案以外の目的で置けない。** 設計中に**本設計書自身が収集対象に入って exit 1 になり**、`状態行と索引の整合検査_設計.md` に改名して解決した。⚠️ **除外リストで逃げないこと**（本物を取りこぼす穴が増える） | §2-5 / §3-1 |
| ⑦ | ⚠️ **エラーには必ずリポジトリ相対パスを出す**（基名だけだと②の重複でどちらの話か分からない） | §3-4 |
| ⑧ | ⚠️ BOM 付きで保存された起票案が「1行目が無い」に見える。**先頭の BOM を除去してから照合する** | §3-2 |

## 6. ⚠️ この変更が持ち込む依存関係（**消費側が1つ増える**）

⭐ **`docs/design/待ち行列/待ち行列と復旧手順.md` が `npm run validate` の依存先になる。**
これまで人だけが読むファイルで、壊しても何も起きなかった。

| 索引への操作 | 何が起きるか |
|---|---|
| 削除・リネーム・移動 | ⚠️ **exit 1**（`索引が見つかりません`） |
| 見出しの語・階層を変える／件数の強調を外す | ⚠️ **exit 1**（`見出しが見つかりません`） |
| 見出しを複製する | ⚠️ **exit 1**（`2個 あります`） |
| 一覧を素のパス表記に変える | ⚠️ **exit 1**（収録漏れ11件） |
| 件数の直し忘れ・載せ忘れ | ⚠️ **exit 1**（＝**狙いどおりの動作**） |

- ⭐ **黙って通る形は無い**（fail-open が1つも無いことは設計書 §6 に明記）
- ⚠️ **索引を編集したら `npm run validate` を通してからコミットする**
- ⭐ 復旧後の手順 §2 ③（フォルダのリネーム）で**索引のリンク直し忘れが機械的に検出される**
  ——索引 §2 ③ の「→ §4-3 の検査があれば機械的に検出される」が実現する
- ⚠️ **これが D10（CLAUDE.md への追記を同じコミットに入れる）の理由。**
  片方だけ先に入ると、「存在しない検査を説明した記述」か「説明の無い検査」のどちらかが必ず生まれる

## 7. ⚠️ 未決事項

**なし。** 設計書 §1-2 のとおり、判断が要る点は D1〜D11 ですべて決めてある。
⭐ **実装中に仕様の判断が要ると感じたら、決めずに設計担当へ戻すこと**（CLAUDE.md「越権の禁止」）。

## 8. ⚠️ push について

- 変更は **`scripts/validate.mjs`** と **`CLAUDE.md`**。⚠️ **どちらもルート配下＝本番配信物**なので、
  **`docs/` だけの push 例外には乗らない**
- ⭐ **コミットまでで止め、push はユーザー判断を待つ**（本番の見た目は変わらないが、規則は変わらない）
- 参考: 索引 §0 のとおり **GitHub は 67コミット遅れ**で、復旧後の初回cronより前の push が最優先事項。
  本タスクの2ファイルもその push に相乗りする

## 9. 設計時の実測サマリ（**投函前の再測定**・索引 §4-2）

⭐ **起票案の主張は全数を測り直し、数値はすべて再現した**（腐っていなかった）。

| 項目 | 実測（2026-09-02・`cfd743f8`） |
|---|---|
| 起票案の件数 | **11件** |
| 状態の内訳 | **生きている 7 / 完了 3 / 取り下げ 1** |
| 状態行の正規表現に一致 | **11 / 11**（未一致 0） |
| 索引への収録漏れ（パス完全一致） | **0件** |
| 索引の見出しの数 | **全11 / 🟢7 / ✅3 / 🚫1**（4種とも**ちょうど1回**一致） |
| 索引の `.md` リンク | **15本**（重複除去）・**すべて実在** |
| `npm run validate` の現状 | **exit 0**・`all checks passed.`・**約2.0秒** |

⚠️ **ただし照合方式は起票案どおりではない**（§5 の①②③）。**そこだけが設計で変わった点**。
