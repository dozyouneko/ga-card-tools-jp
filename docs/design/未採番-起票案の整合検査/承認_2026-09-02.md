**対象: 「起票案の整合検査」**（→ 完了） — この宣言は 2026-09-11 に遡って追記した（索引の転記ドリフト防止）

# 未採番（起票案の整合検査）設計確認 — **✅ 承認（ユーザー承認済み）／⚠️ 残作業あり → `実装待ち` 相当**

⚠️ **本来は該当issueのコメント。GitHubアカウント停止中で `gh issue comment` / `gh issue edit` が
打てないため、代替としてこのファイルに記録する**（#73・未採番5件で確立済みの運用）。
復旧して採番されたら、本文をそのまま次のコマンドで投函する:

```bash
gh issue comment <n> --body-file docs/design/未採番-起票案の整合検査/承認_2026-09-02.md
```

⚠️ ⭐ **承認したのはユーザーである。** 設計担当はその判断を記録し、確認事項に設計判断で答え、
残務を整理する役。**本ファイルは設計担当が承認を下した記録ではない。**

- 承認日: **2026-09-02**（設計・実装・レビュー・承認がすべて同日。⭐ **滞留ゼロ**）
- 差し戻し: **0回**（1周で合格）
- 工程: **リード主導スプリント**（リードが design → dev → review を直接呼ぶ体制）

| 工程 | コミット | 結果 |
|---|---|---|
| 設計 | **`4e0d51f7`** | 設計書＋設計完了記録。検証項目 **V1〜V13**（うち破壊試験10件） |
| 実装 | **`04044d84`** | `scripts/validate.mjs`（+138行）＋ `CLAUDE.md`（+8行）＝ **2ファイル・追加のみ・生成物0** |
| 実装報告 | **`c6b2d932`** | V1〜V13 **全項目合格**。仕様上の逸脱**0**・確認事項1件（S1-1 の元） |
| レビュー判定 | **`c747bb79`** | **✅ 合格**（M1〜M7 充足・不合格の指摘0件・推奨指摘 **S1-1 / S1-2**） |
| **ユーザー最終承認** | — | **2026-09-02・✅ 承認／⚠️ 推奨指摘を2件とも採用（残作業）／⏸️ 本番公開は保留** |

---

## 1. ユーザーの判断（＝本ファイルの主題）

| 項目 | 判断 |
|---|---|
| **変更内容の承認** | ✅ **承認** |
| **推奨指摘 S1-1（`validate.mjs` の使い方コメント）** | ⭕ **採る**（→ §4-1） |
| **推奨指摘 S1-2（`CLAUDE.md` に命名の制約を書く）** | ⭕ **採る**（→ §4-2） |
| **再レビュー** | ❌ **不要**（→ §4-4） |
| **派生issue** | ❌ **起こさない**（→ §5） |
| **本番公開（push / `deploy:prod`）** | ⏸️ **保留**（ユーザー判断待ち。→ §7） |

### 1-1. ⚠️ いまの状態 — **承認済みだが、まだ `push待ち` ではない**

⭐ **状態は `設計確認待ち` → `実装待ち`（残作業）→（開発担当が）`push待ち`。**

- 承認は下りているが、**S1-1 / S1-2 の2行の追記が残っている**。
  これは `CLAUDE.md` が **#64 の実例として明記している経路**そのものである:

  > ⭐ **例外: 承認後に軽微な残作業で `実装待ち` へ戻したときは、その作業を終えた開発担当が
  > `push待ち` を付けてよい**

- ⚠️ **`実装待ち` に据え置いたまま放置しない。** 残作業を終えたら**開発担当が `push待ち` 相当に上げる**
  （GitHub停止中なので、実体はラベルではなく §6 の記録）
- ⚠️ **クローズもしない**（本番公開の判断が残っているため）

---

## 2. 承認の理由（設計意図との適合確認）

⚠️ **実装の動作確認はレビュー担当の担当**（証跡は `c747bb79` と `tmp/review/未採番-起票案の整合検査/`）。
ここで確認したのは「**設計の意図を満たしているか**」だけである。

### 2-1. 設計意図（D1〜D11）と実装の対応

| # | 設計意図 | 満たされたか | 根拠 |
|---|---|:--:|---|
| **D1** | 照合は**パスの完全一致**（基名の部分一致を採らない） | ✅ | ⭐ **V5 がこの設計判断そのものを検証している。** 基名 `起票案_2026-08-26.md` は2ファイルあり、さらに `派生起票案_2026-08-26.md` の部分文字列でもあるが、**削除を1件だけ・フルパスで**検出した（起票案の字義どおりなら**検出0件**） |
| **D2** | 対象は `起票案` / `派生起票案` で**始まる** `.md`（前方一致） | ✅ | 収集 **11件**。除外リストは1つも入っていない（実装報告 §5） |
| **D6** | 状態行を読めない起票案があるときは**件数比較を省略**する | ✅ | V2 が**ちょうど2行**（状態行なし＋省略の告知）。⭐ **1つの原因で2種類のエラーを出していない** |
| **D7** | 索引が読めない／見出しが取れないとき **exit 1**（「一致」に倒さない） | ✅ | V7（強調外し）・V9（索引リネーム）とも exit 1 で、**V9 では収録漏れ11件が併発していない** |
| **D8** | 起票案が0件なら **exit 1** | ✅ | V11。⭐ **「0件だから全部一致」で緑にならない** |
| **D9** | 見出しが2個以上なら **exit 1** | ✅ | V8（#70 のレビュー指摘 S1 と同型の穴を、先に塞いである） |
| **D10** | `CLAUDE.md` の追記を**同じコミットに含める** | ✅ | `04044d84` に同梱。⭐ **文面は設計書 §4-3 と `diff` で IDENTICAL**（レビューが確認） |
| **D11** | exit code は `problems++` で既存と揃える | ✅ | V1 の出力が**既存2行に挟まれた位置**に入り、`all checks passed.` の体裁が崩れていない |
| §3-1 | **命名の制約**を除外リストで逃げない | ✅ | 実装コメントに明記・除外0件（実装報告 §5） |
| §6 | 生成物 **0ファイル** | ✅ | `git diff --stat 04044d84^ 04044d84 -- cards sets sitemap.xml data index.html tournaments` が**空**（M4・V13） |

### 2-2. ⭐ 設計担当として特に評価した点

| # | 内容 |
|---|---|
| ① | ⭐ **fail-open を1つも作らなかった。** 「検査が壊れたまま黙って通る」のが最悪の失敗（#40 の教訓）で、本タスクは**索引が読めない・見出しが取れない・見出しが複数・収集0件**の4つすべてを exit 1 にしている。⚠️ **V7 と V11 は、実装がここで手を抜いていれば緑になる項目**であり、どちらも赤くなることを実測で確認済み |
| ② | ⭐ **レビューが「報告の再現」で止まらなかった。** 独自シナリオ **E1〜E4** を足し、とくに **E2（復旧後のフォルダリネームの実地再現）** は、索引 §2 ③ が期待していた「直し忘れが機械的に検出される」を**実際に起こして確認**している。⚠️ **設計書が「そうなるはず」と書いた効能を、レビューが実地で裏取りした** |
| ③ | ⭐ **E3（BOM）・E4（日付の形式崩れ）は報告にも設計書の検証項目にも無い経路**で、どちらも設計どおりに振る舞った。⭐ **設計書 §3-2 に「BOM に注意」と書いた1行が、実装され、かつ検証された** |
| ④ | **実装が「決めずに戻した」判断（S1-1）は正しい。** ⚠️ 設計書 §6 は「既存行は変更しない」と書いており、実装がその字義に従ったうえで**確認事項として残した**のは、`CLAUDE.md`「開発担当は仕様を決めない」のとおり。⭐ **勝手に足していたら、それはそれで是としたが、戻したほうが手続きとして正しい** |
| ⑤ | ⚠️ ⭐ **設計時の事故（設計書自身が収集対象に入った）を「検査を緩める」で解決しなかったこと**が、実装まで一貫している。除外リストを足すたびに**本物の起票案を取りこぼす穴**が増えるので、**名前のほうを規約に合わせる**のが正しい |

---

## 3. ⚠️ 設計書 §6「既存行は変更しない」と S1-1 の関係（**抵触しない**）

⭐ **S1-1 を採っても設計書に反しない。** 足すのは**新規の1行**で、**既存行は1行も書き換えない**。

- 設計書 §6 の「既存行は変更しない」は、⚠️ **既存の検査ロジックを触らせないための制約**であって、
  **ファイル冒頭の目次コメントを更新するなという意味ではない**
- ⚠️ ⭐ **むしろ書かないほうが規約違反に近い。** 冒頭コメントは #22 / #37 / #40 / #70 /
  用語ハイライトと、**検査が増えるたびに1行ずつ追記されてきた**。
  ここで止めると、**「手で維持している一覧が実態とずれる」**——
  ⭐ **本タスクが機械化しようとしている型そのもの**をこのコミット自身が1つ増やすことになる
- ⚠️ **設計書の書き方が不十分だった。** 「既存行は変更しない」ではなく
  「**既存の検査ロジックは変更しない（冒頭の目次コメントには本検査の行を足す）**」と書くべきだった。
  ⭐ **責は設計側にある**ので、実装の逸脱としては扱わない

---

## 4. レビューの推奨指摘（S）の扱い — **⚠️ 開発担当への作業指示**

⚠️ ⭐ **開発担当は設計担当のコンテキストを持っていない。以下は、これだけ読めば作業できる形で書いてある。**

⭐ **両件に共通する採用理由**: どちらも「**手で維持している一覧・ルールが実態とずれる**」型であり、
**本タスクが機械化しようとしている型そのもの**。ここで放置すると、
**ドリフト検査を入れたコミット自身がドリフトを1つ増やす**。
しかも**両方とも、この実装コミットが既に触っているファイルの中**にある。

### 4-1. ⭕ S1-1 — `scripts/validate.mjs` の冒頭コメントに1行足す

**変更するファイル**: `scripts/validate.mjs`（**冒頭のコメント1行を追加するだけ**）

- **入れる位置**: ⚠️ **#70 の行の直後・`// 最後に、…` の行の直前**（＝**検査の実行順と一致させる**。
  本検査は #70 ブロックの直後・用語ハイライト検査の直前に置かれているため）
- **現状**（2026-09-02 時点の 16〜18行目）:

  ```js
  // 加えて cronワークフローの git add 対象と README.md / CLAUDE.md の列挙が一致するかを検査する（#70）。
  // 最後に、生成済みカードページの日本語効果文に「ハイライトされていない用語」が残っていないかを
  // 検査する（用語ハイライトの語形ずれ・片方向）。⚠ この検査だけ非同期（並列読み込み）。
  ```

- **追記後**（⭐ **足すのは真ん中の1行だけ**）:

  ```js
  // 加えて cronワークフローの git add 対象と README.md / CLAUDE.md の列挙が一致するかを検査する（#70）。
  // 続いて docs/design/** の起票案の状態行と、索引（待ち行列と復旧手順.md）の整合を検査する（収録漏れと件数一致）。
  // 最後に、生成済みカードページの日本語効果文に「ハイライトされていない用語」が残っていないかを
  // 検査する（用語ハイライトの語形ずれ・片方向）。⚠ この検査だけ非同期（並列読み込み）。
  ```

- ⚠️ **文頭を `加えて` にしなかったのは意図的**（直前の #70 の行が `加えて` で始まるため。
  既存は `さらに` / `加えて` / `同型で` / `最後に` を重複させずに使い分けている）
- ⚠️ **issue番号は書かない**（本タスクは未採番）。⭐ 用語ハイライトの行が
  `（用語ハイライトの語形ずれ・片方向）` と**説明で代用している**のと同じ流儀に合わせた
- ⭐ **`// 最後に、` の行はそのまま**でよい（本検査を前に入れても、用語ハイライト検査が最後であることは変わらない）

### 4-2. ⭕ S1-2 — `CLAUDE.md` に「命名の制約」を1項目足す

**変更するファイル**: `CLAUDE.md`（**「タスク管理(2026-07-18〜)」節**）

- **入れる位置**: ⚠️ **`04044d84` で追記したブロックの末尾**（`- ✅ **索引と状態行のドリフトは…**` の
  子リストの**3つ目**として。＝`- ⚠️ **中身の「腐り」は防げない**…` の直後、
  `- ⚠️ ⭐ **\`gh issue create\` する直前に、…**` の**前**）
- **足す内容**（⭐ **インデントは2スペース。既存の子項目に合わせる**）:

  ```markdown
  - ⚠️ **`docs/design/**` に `起票案` で始まる名前の `.md` を、起票案以外の目的で置けない**(前方一致)。
    設計書・解説文書には別の名前を付ける(⭐ **本タスクの設計書自身がこれを踏んで改名した**)。
    ⭐ **黙って通ることはない**——`起票案の1行目に状態行がありません` で **exit 1** になる
  ```

- ⚠️ **丸括弧は半角 `()`**（`CLAUDE.md` 本文の流儀。設計書側は全角だが、**入れる先の流儀に合わせる**）
- ⚠️ **`<!-- … -->` のマーカーは足さない**（#70 の検査に触れる）
- ⭐ **なぜ「踏んでも exit 1 になるから不要」ではないのか**: `CLAUDE.md` は**全セッションが読む運用ルールの正**で、
  ⚠️ **設計担当が `docs/design/**` に新しい文書を置くのは日常動作**。
  **踏んでから知るより、先に知るほうが安い**（レビュー S1-2 の指摘に同意する）

### 4-3. ⚠️ 残作業の検証（**この2点だけ**）

| # | 手順 | 期待 |
|---|---|---|
| **R1** | `npm run validate` | **exit 0**。末尾は次の順（⭐ **`04044d84` の時点から件数が動いている**。理由は §6-1）:<br>`cron git add paths in sync — 8パス（README.md / CLAUDE.md）`<br>**`issue drafts in sync — 起票案11件（生きている6 / 完了4 / 取り下げ1）`**<br>`card term highlights up to date — 2495 ページ`<br>（空行）`all checks passed.` |
| **R2** | `git status --short` | ⚠️ **`scripts/validate.mjs` と `CLAUDE.md` の2件だけ**。`docs/` に差分が出ていたら**それは設計担当のコミット漏れ**なので、混ぜずに報告する |

⚠️ ⭐ **R1 の `生きている6 / 完了4 / 取り下げ1` は、設計書 §5 の V1（`7 / 3 / 1`）と違う。**
**これは正常**——承認時に本タスク自身の起票案を `完了` に変えたため（→ §6-1）。
**2026-09-02 に設計担当が実測して exit 0 を確認済み**。⚠️ **食い違いとして報告しなくてよい。**

### 4-4. ❌ 再レビューは不要（**理由**）

- **S1-1**: ⭐ **コメント1行のみ。ロジックには一切触れない**（`//` で始まる行の追加）
- **S1-2**: ⭐ **文書のみ**。`CLAUDE.md` の本文で、マーカーにも `git add` 列挙にも触れない
- ⚠️ **ただし `npm run validate` の exit 0 再確認（R1）は必ず行う**——
  `CLAUDE.md` は **#70 の検査の入力**でもあるため、**触ったら必ず通す**

### 4-5. ⚠️ 残作業の進め方（**git の約束**）

- ⚠️ **`git add -A` / `git commit -a` を使わない。** ステージするのは
  **`scripts/validate.mjs` と `CLAUDE.md` の2つだけ**（担当パスを明示する）
- ⭐ **1コミットにまとめる**（S1-1 と S1-2 は同じ指摘への対応で、分ける理由がない）
- ⚠️ **push はしない**（→ §7）。⭐ **コミットまでで止める**
- ⭐ **残作業の報告**は `docs/design/未採番-起票案の整合検査/実装報告_2026-09-02_残作業.md` に新規で書く
  （GitHub停止中のissueコメント代替。コミットハッシュ・R1/R2 の結果・状態が **`push待ち` 相当**である旨を明記）

---

## 5. 派生issueの要否（レビュー §5 確認事項1・2への回答）

| 起票するか | 内容 | 判断 |
|:--:|---|---|
| ❌ **しない** | S1-1（`validate.mjs` の冒頭コメント） | ⭐ **1行の追記**。§4-1 の指示で完結する |
| ❌ **しない** | S1-2（`CLAUDE.md` の命名の制約） | ⭐ **3行の文書修正**。§4-2 の指示で完結する |

⚠️ ⭐ **起票しない理由（労力が釣り合わない）**: いま起票案を1件足すと、
**12件目の起票案**が待ち行列に積まれ、索引 §4-2 により **`gh issue create` の直前に
「主張する症状の実測」が必要**になる。⚠️ **2行の文書修正のためにその手続きを回すのは明らかに過剰**で、
しかも**起票案が増えるほど腐る面が増える**（本タスクが減らそうとしているもの）。

⭐ **レビュー担当の「派生issueを起こすほどのものではない」という見立てに同意する。**

### 5-1. ⭐ 参考事項（レビュー §4 末尾）の扱い

| 内容 | 扱い |
|---|---|
| **見出しの抽出に失敗すると検査A（収録漏れ）も止まる**（V7・V8 の状態）。「見出しの強調を外した」うえに「起票案を1件載せ忘れた」場合、見出しを直すまで収録漏れは見えない | ⭐ **D7 の意図どおり。追加対応なし。** ⚠️ **exit 1 のままなので、見落としは発生しない**（人は必ず止まる）。⭐ 「抽出失敗を一致に倒さない」ほうが優先で、**2段階で赤くなること自体は害ではない** |
| `fail-open` が1つも無いことを E1〜E4 でも崩せなかった | ⭐ **良い点として記録。追加対応なし** |

---

## 6. 承認にあわせて設計担当が行った記録の更新

⚠️ ⭐ **これは開発担当の作業ではない。すでに設計担当がコミット済みである**（→ §8）。

### 6-1. ⭐ 本タスク自身の起票案を `完了` にした（**件数が動いた理由**）

`docs/design/待ち行列/起票案_起票案の状態行と索引の整合検査.md` の**1行目の状態行**を
**`生きている` → `完了`** に変えた。

- ⚠️ ⭐ **変えないと、復旧後の起票が間違う。** 索引 §1 の「🟢 生きている」は
  **「復旧後に `gh issue create` する」＝これから着手するタスク**の表で、
  **`実装待ち` を付けて起票することになる**。⚠️ **もう実装もレビューも承認も終わっているのに、である**
- ⭐ 正しい行き先は **§2 ②-A「完了済みタスク」**（＝工程記録を投稿して**いきなり `push待ち`**）
- **同時に直した索引の箇所**（⚠️ **本検査の対象**）:

  | 箇所 | 変更 |
  |---|---|
  | §1 見出し `🟢 生きている` | **7件 → 6件**（表から本タスクの行を削除） |
  | §1 見出し `✅ 完了済み` | **3件 → 4件**（顛末つきで行を追加） |
  | §1 見出し `全11件` | ⭐ **不変**（内訳が移っただけ） |
  | §2 ② / ②-A / ②-B | **完了済み5→6件・派生7→6件**（合計12件は不変）。②-A に⑥の行を追加 |
  | §2 ③ | `git mv` に⑥を追加。⚠️ **起票案ファイルを設計フォルダへ移すのも同じコミットで**、と明記 |
  | §2 ④ | ⑥の工程記録（設計完了→実装報告→レビュー判定→承認）の投稿順を追加 |
  | §3 | 本番公開を「未公開ゼロ」→**未公開1件（`04044d84`）**、GitHub遅れを **67→72コミット**に更新 |
  | §4-3 | 「実装は復旧後に回す」を削除し、**実装・承認済み**と**索引が `validate` の依存先である**旨に差し替え |

- ⭐ **この作業そのものが本検査の実地試験になった**——状態行だけ直して索引を直さなければ exit 1。
  **両方直して `npm run validate` の exit 0 を確認してからコミットした**

### 6-2. 設計書の改版（**承認版**）

`状態行と索引の整合検査_設計.md` に**承認版**の行を足し、設計版の状態を **実装済み（`04044d84`）** にした。
⚠️ **§5 の検証項目の数値（`7 / 3 / 1`）は書き換えていない**——
⭐ **レビュー判定がその値で全項目を実測しているため、書き換えると判定記録と食い違う**。
代わりに **§5 の冒頭に「承認後の実数は `6 / 4 / 1`」の注記**を足した（R1 の期待値の出どころ）。

---

## 7. ⏸️ 本番公開について（**ユーザー判断待ち**）

⚠️ **承認は「変更内容の承認」であって、公開の承認ではない。**

- ⚠️ ⭐ **GitHubアカウント停止中なので経路①（`git push origin main`）は使えない。**
  出すなら**経路②**: `CONFIRM=publish npm run deploy:prod`
  - ⚠️ ⭐ **経路②も本番公開である。**「pushしていないから安全」ではない。
    **公開の可否は常にユーザーが判断する**
- **公開時にユーザーへ伝えること**:

  | 内容 |
  |---|
  | ⭐ **本番の見た目は変わらない**。`npm run validate` は**ビルドにも配信にも関与しない** |
  | ⚠️ ただし **`scripts/` も `CLAUDE.md` も本番配信される**ので、「見た目が変わらないから安全」とは判断しない（#24 の対象） |
  | ⚠️ **新規ファイルが1つも無い**ため「404→200 の到達マーカー」が使えない。**素のURLと `?cb=` を並べ、20秒間隔で反復して全数一致を見る**（ドキュメント/設定だけの小さなコミットの実績: **約2分・計8回**） |
  | ⭐ 反映確認の見どころ: **`/scripts/validate.mjs` に `issue drafts in sync` が含まれること**（旧 0件）。⚠️ **`/CLAUDE.md` は 200 で配信される**ので、そちらでも見られる |
  | ⭐ 経路②は `git archive HEAD` から作るので、**HEAD の内容がまるごと公開される**（本タスク以外の未公開分も同時に出る＝現状は本タスクの2ファイルのみ） |

---

## 8. GitHub 復旧後の残務（⚠️ **この順で打つ**）

⚠️ **本タスクはissue未採番。** 手順の全体は
[`../待ち行列/待ち行列と復旧手順.md`](../待ち行列/待ち行列と復旧手順.md) §2（⑥として追記済み）。

1. **push の期限**（索引 §0）— ⚠️ ⭐ **復旧後の初回cron（03:00 JST）より前に push する**
2. **起票**（`gh issue create`）— **「完了済みタスク」として**起票する。本文は
   `../待ち行列/起票案_起票案の状態行と索引の整合検査.md`。
   ⚠️ **ラベルは `enhancement` `小タスク` ＋ `push待ち`**（未pushなら）。
   ⚠️ **`実装待ち` を付けない**（実装済み）。⚠️ **本文 §2-2 の照合方式は誤り**なので、
   起票時に**設計書が正である旨を1行添える**
3. **フォルダのリネーム**:
   `git mv "docs/design/未採番-起票案の整合検査" "docs/design/<番号>-起票案の整合検査"`
   - ⚠️ ⭐ **起票案ファイルを `待ち行列/` から設計フォルダへ移すのも同じコミットで行う**。
     **分けると、その間 `npm run validate` が `索引に載っていない起票案: …` で exit 1 になる**
   - ⚠️ **`grep -rn "未採番-起票案の整合検査" docs/ CLAUDE.md` で参照元を全部直す**
   - ⚠️ **索引 §1 のリンクも直す**（⭐ **直し忘れは本検査が止める**——本タスクが作った効能そのもの）
4. **コメントを工程順に投稿**（⚠️ **投稿し終えてからラベルを付ける**）:
   `設計完了_2026-09-02.md` → `実装報告_2026-09-02.md` → `実装報告_2026-09-02_残作業.md`
   → `レビュー判定_2026-09-02.md` → **本ファイル**
5. **派生issueの起票**: ⭐ **なし**（→ §5）

---

## 9. ⭐ 今回の運用から得た知見（次に活かす）

| 知見 |
|---|
| ⭐ **「手で維持している一覧」を機械化するタスクでは、その一覧の**仲間**が同じコミットの中に無いかを見る。** S1-1（冒頭コメントの列挙）は、まさに本検査が守ろうとしている型の**手書き一覧**だった。⚠️ **ドリフト検査を入れるコミット自身がドリフトを増やす**のは、指摘されるまで設計担当も気づかなかった |
| ⚠️ ⭐ **設計書に「既存行は変更しない」と書くと、実装は目次コメントの更新まで止める。** 制約は**目的語を狭く書く**（「既存の**検査ロジック**は変更しない」）。⭐ **実装が迷って戻してきたのは正しい振る舞いで、責は曖昧に書いた設計側にある** |
| ⭐ **検査を入れたら、その検査が縛る対象を `CLAUDE.md` に書く。** 本検査は「索引を壊すと落ちる」だけでなく **「`docs/design/**` に置けるファイル名」まで縛る**。⚠️ **後者は設計書にしか書いていなかった**（S1-2）——⭐ **新しい制約は、それを踏む人が読む場所に書く** |
| ⭐ **承認のついでに「自分自身の起票案」の状態を直すと、その場で新しい検査の実地試験になる**（§6-1）。⚠️ **状態行と索引の2箇所を直す必要があり、片方だけだと exit 1 で止まる**——**入れた検査に最初に止められるのは自分**、という体験が1周目で得られた |
| ⚠️ ⭐ **検証項目の数値は、記録を書き換えるより「注記で上書き」するほうが安全な場合がある。** §5 の `7 / 3 / 1` はレビュー判定が実測した値なので、**表を書き換えると判定記録と食い違う**。⭐ **「いつ時点の値か」を明示して、新しい値を注記で足した** |
