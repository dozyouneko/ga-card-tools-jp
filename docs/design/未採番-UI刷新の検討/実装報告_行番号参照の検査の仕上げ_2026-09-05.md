# 実装報告 — 行番号参照の検査の**仕上げ3点**（2026-09-05）

⚠️ **これは issue コメント（実装報告）の代替。** GitHubアカウント停止中のためファイルで受け渡す。
復旧後は `gh issue comment <n> --body-file` でそのまま投函する
（索引 [`待ち行列と復旧手順.md`](../待ち行列/待ち行列と復旧手順.md) §2-④）。

- 呼び名: **「行番号参照の検査の仕上げ」**（記号で呼ばない → 索引 §4-4）
- 指示（正）: [`追加指示_行番号参照の検査の仕上げ_2026-09-04.md`](追加指示_行番号参照の検査の仕上げ_2026-09-04.md)
- 前提（承認済み・本体タスク）: [`承認_行番号参照の検査_2026-09-04.md`](承認_行番号参照の検査_2026-09-04.md)（実装 `8e2c8782`）
- **コミット: `f44f6cce`**（`scripts/validate.mjs` の1ファイルのみ・+9 / -6）／本報告（`docs/` のみ）は `6eecd734`
- ⚠️ **push・`wrangler pages deploy` は実行していない**（本番公開の可否は常にユーザーが判断する）

---

## 1. 実施内容

親コミットは `3071c113`。⭐ **触ったのは `scripts/validate.mjs` の1ファイルだけ**（指示 §0 のとおり）。

| # | 変更 | 実際に書いた内容 |
|---|---|---|
| **#1** | 走査ルートを広げる | `const LINEREF_ROOTS = ["scripts", "shared", "functions", "app.js", "tools"];`（`"shared/js"` → `"shared"`）。⭐ **`LINEREF_EXCLUDE` は `["shared/vendor"]` のまま増やしていない**（指示 §1「やらないこと」） |
| **#1付随** | `shared/vendor` 除外コメントの書き換え | 「今のルート（shared/js）からは届かないが、ルートを shared/ へ広げても穴が開かないよう除外を明示しておく。」→ ⭐ **「ルートが shared/ なのでこの除外は実際に効いている（外すと shared/vendor/ の配布物を拾って赤くなる）。整理目的で消さないこと。」** |
| **#2** | 冒頭コメントの検査一覧に3件追記 | `meta.subtypes` の次に **`meta.rarities`** と **`card-meta-index` の並び替えキー**、`issue drafts` の次（＝「最後に、…用語ハイライト…」の**前**）に **行番号参照** を1行ずつ。⚠️ **既存の記述は書き換えていない**（足しただけ） |
| **#3** | 腐る実測値の撤去 | 「（2026-09-04 実測で278件ある）」→ ⭐ **「（2026-09-04 時点で約300件あり、記録を書くたびに増える＝固定値として書く意味が無い）」**。⚠️ **正確な数への更新では済ませていない**（指示 §3） |

### 追記した3行（実行順どおり）

```
// 同型で meta.rarities（レアリティ絞り込みの選択肢）が実データの全レアリティを覆っているかも検査する。
// さらに data/card-meta-index.json の並び替えキー（8要素・数値4項目・フリップ面）の内部整合を検査する。
// 続いてコード内に「<file>.js:<行番号>」の形の参照が残っていないかを検査する（行番号は腐るため）。
```

⚠️ **「最後に、…」で始まる文は用語ハイライトのまま1つだけ**（`grep -c '^// 最後に、'` ＝ **1**）。

---

## 2. 検証結果（指示 §4 の V1〜V7）

| # | 内容 | 期待値 | 実測 | 判定 |
|---|---|---|---|---|
| **V1** | `npm run validate` | exit 0・`no line-number refs in code comments — 0件（46ファイル走査）` | **exit 0**・⭐ **`no line-number refs in code comments — 0件（46ファイル走査）`**・`all checks passed.`（成功行は**10本**すべて出た） | ✅ |
| **V2** | `shared/pdf/card-sheet.js` の先頭コメントに `zzprobe.js:123` を1行足す | exit 1・`shared/pdf/card-sheet.js:<行番号> — zzprobe.js:123` と名指し | **exit 1**・⭐ **`shared/pdf/card-sheet.js:1 — zzprobe.js:123`**（`STALE LINE REFS (scripts / shared / functions / app.js / tools):` / `行番号参照: 1件`） | ✅ |
| **V3** | `shared/vendor/jspdf.umd.min.js` に `zzprobe.js:123` を足す | exit 0 のまま・`0件（46ファイル走査）` | **exit 0**・⭐ **`0件（46ファイル走査）`**（＝ルートが `shared` になっても除外が守っている） | ✅ |
| **V4** | 冒頭コメントに3件が載ったか | 3件の記述があり、「最後に」で始まる文は用語ハイライトのまま1つ | ⭐ **3件とも記載**（実行順どおり）・`^// 最後に、` は **1件** | ✅ |
| **V5** | `grep -c '278件' scripts/validate.mjs` | **0**（実施前は 1） | **実施前 1 → 実施後 0** | ✅ |
| **V6** | 担当パス | `scripts/validate.mjs` の1行だけ | `git -c core.quotepath=false show --name-only --format= f44f6cce` ＝ ⭐ **`scripts/validate.mjs` の1行のみ**（+9 / -6） | ✅ |
| **V7** | 生成物に差分が出ないこと | `git status --short` に `scripts/validate.mjs` 以外が出ない | コミット後 **出力なし**（＝作業ツリーはクリーン。⚠️ 本報告 `docs/` は本コミットの後に追加） | ✅ |

### V2・V3 の復元について

⚠️ **指示 §4 のとおり、破壊試験の2ファイルは確認後に復元した**（一時退避のコピーから戻した）。
⭐ **各試験の直後に `git status --short` を確認し、`M scripts/validate.mjs` の1行だけに戻ったことを実測している**
（V3 の実行中は `M shared/vendor/jspdf.umd.min.js` が並んでいたが、復元後に消えた）。
**コミット時点の `git status --short` は `M scripts/validate.mjs` の1行だけだった。**

---

## 3. 実行できなかった検証・残っている確認

- ⭐ **無し。V1〜V7 をすべて実施した**（Playwright は不要——本件は `npm run validate` の挙動のみで、
  サイトの表示・ブラウザ挙動を一切変えない）
- ⚠️ **本番での確認は原理的に不要**（`scripts/` は配信されるが、`validate` は**サイトの表示を変えない**。
  承認記録 §7 の「本番公開の前提条件ではない」と同じ）
- ⚠️ **未push**（本件のコミットに加え、リポジトリ全体で未pushが積み上がっている状態は継続）

---

## 4. 差分が設計書の想定と食い違ったか

⭐ **食い違いは無い。**

- **V1 の「46」は指示どおり再現した。** 設計担当が示した式
  `find scripts shared functions tools app.js \( -name '*.js' -o -name '*.mjs' \) -not -path 'shared/vendor/*' | wc -l`
  を実装前に独立に実行して **46** を確認し、実装後の成功行も **46ファイル走査** で一致した
  （⭐ **実装前の実測は 45ファイル走査**＝増えた1本は指示どおり `shared/pdf/card-sheet.js`）
- ⚠️ **`shared/` 配下の `.js`/`.mjs` は6本しか無い**（`shared/js/` 4本 ＋ `shared/pdf/card-sheet.js` ＋
  `shared/vendor/jspdf.umd.min.js`）。⭐ **除外1本を引いて +1** という承認記録 §4-1 の見立てと一致する
- **差分は 9行追加 / 6行削除**。想定（ルート1語＋コメント3行＋コメント書き換え）の範囲内

---

## 5. 設計書からの逸脱と根拠

⭐ **仕様上の逸脱は無し。** ただし**指示に明記されていない書式上の判断を1つ**したので記録する:

| # | やったこと | 根拠 |
|---|---|---|
| **N1** | #3 の書き換えで、⭐ **その段落の折り返し（3行）を組み直した** | 指定の文言をそのまま1行に入れると**その行だけ他のコメント行より大幅に長くなる**ため。⚠️ **趣旨（`docs/` を対象に含めるな）と文の内容は変えていない**（指示 §3「コメントの趣旨は変えない」）。差分上は3行の置換に見えるが、**削除された情報は無い** |

### スコープ外（指示 §5）の遵守

⚠️ **4つとも一切入れていない**（先回りしない）:

| やらないこと | 実装での扱い |
|---|---|
| `tools/` の `.html` / `.css` を対象に足す | ⭐ **走査は `.js` / `.mjs` のみのまま**（拡張子の判定は無変更） |
| 「コメント内」限定の判定を入れる | ⭐ **行単位の素の文字列一致のまま**（生成元テンプレートリテラル内を被覆する唯一の経路を保持） |
| `file.js#L231` / 日本語表記を拾う | ⭐ **`REF_RE` は無変更** |
| 除外の設定ファイル・`disable` 機構 | ⭐ **作っていない。`LINEREF_EXCLUDE` も `["shared/vendor"]` のまま** |

---

## 6. 設計担当への確認事項

⭐ **判断を要する事項は無い**（3点とも指示に判断が書かれており、そのとおり実装した）。
参考として1点だけ挙げる:

- **P1（参考・対応不要）**: ⭐ **`shared/` 配下は今後も増えうる**（現状 `js/` `pdf/` `vendor/` の3ディレクトリ）。
  ⚠️ **他人の配布物が増えたら赤くなるが、それは承認記録 §4-1-4 のとおり fail-closed で意図どおり**——
  **人が気づいて `LINEREF_EXCLUDE` に足す**運用で問題ない、と理解している。
  ⭐ **この理解が設計意図と違う場合のみ指摘してほしい**

---

## ハンドオフ

- ⭐ **`レビュー待ち`**（GitHub復旧後は `gh issue edit <n> --remove-label 実装待ち --add-label レビュー待ち`）
- 対象コミット: **`f44f6cce`**（レビューはこのハッシュで差分を特定できる）
- ⚠️ **push・`wrangler pages deploy` は実行していない**
