**対象: 「行番号参照の是正と検査」**（→ 継続） — この宣言は 2026-09-11 に遡って追記した（索引の転記ドリフト防止）

# 承認記録 — コメント内の腐った行番号参照を関数名参照に直す（2026-09-04）

⚠️ **GitHubアカウント停止中のため、issueの承認コメントの代わりにこのファイルを置く。**
復旧後に `gh issue comment <n> --body-file` でそのまま投函する
（索引 [`待ち行列と復旧手順.md`](../待ち行列/待ち行列と復旧手順.md) §2-④）。

- 指示書: [`追加指示_腐った行番号参照_2026-09-04.md`](追加指示_腐った行番号参照_2026-09-04.md)
- 実装報告: [`実装報告_腐った行番号参照_2026-09-04.md`](実装報告_腐った行番号参照_2026-09-04.md)
- 実装: `895bade2` / 報告: `df82dd8c`
- 呼び名: **「腐った行番号参照の是正」**（記号で呼ばない → 索引 §4-4）

---

## 1. 判定 — ⭐ **承認する**

T1〜T5 全合格・`npm run validate` exit 0・対象4件すべてが関数名参照になっている。
⭐ **設計担当が独立に再測定して確認した**（下記）。

| 確認 | 結果 |
|---|---|
| `<file>.js:<行番号>` 形式の残存（`scripts/` `shared/js/` `functions/` `app.js` `tools/`） | ⭐ **0件**（設計担当が再実行） |
| 参照先の関数の実在 | ⭐ **確認**（`card-detail.js` の `render()` は `:231`、`flavorOf()` は `card-i18n.js:323`） |

---

## 2. ⭐ 逸脱の追認（報告 §6）— **追認する**

指示書 #3 は直す先を **`card-detail.js` のフレーバー描画（`flavorOf(card, t)` 周辺）**と書いたが、
開発担当は **`render()`** を採用した。⭐ **これを正式に追認する。**

### ⚠️ 指示書のほうが誤っていた

**設計担当が実測して確認した**:

| 事実 | 実測 |
|---|---|
| `flavorOf()` の定義位置 | ⭐ **`shared/js/card-i18n.js:323`** |
| `card-detail.js` に `flavorOf()` の**定義**があるか | ❌ **無い**（**呼び出しが2箇所あるだけ**） |
| `card-detail.js` の `render()` | `:231` から93行＝ **`:231`〜`:323`** |
| フレーバー描画（`const flavor = flavorOf(card, t)`）の位置 | `:305` ＝ ⭐ **`render()` の内側** |

⭐ **指示書のとおりに書いていたら、T3（参照先の関数が同ファイルに実在すること）を満たせなかった。**
⚠️ **開発担当の判断が正しく、指示書の記述が誤っていた。** 指し示す場所は同一（`:305` を含む関数）で、
**意味は変わっていない**。

⭐ **評価すべき点**: 指示書どおりに書けば形式上は「指示に従った」ことになるのに、
**自分の検証項目（T3）に照らして矛盾を見つけ、自己申告した**。
⚠️ **黙って指示どおりに書かれていたら、新しい腐った参照が1件増えていた**（＝本タスクの目的の逆）。

---

## 3. ⭐ 指示書 §1 の根拠の訂正（報告 §7-1）— **訂正する**

指示書 §1 は「**4件中4件が腐っている**＝この書き方は必ず腐ると実証された」と書いたが、
**#3 は「腐った」のではなく、導入時点で既に誤っていた**。

### ⚠️ 設計担当が実測して裏を取った

```
導入コミット: 9e835e56（2026-07-26・「#22 フェーズ2: 翻訳データをJSONに分割して遅延読み込みする」）
  その時点の card-detail.js:200      → banned バナーの className 代入（無関係）
  その時点のフレーバー描画の実際の行 → :219
  ⇒ 導入した瞬間に 19行ずれていた
```

⭐ **開発担当の申告どおり**（「19行ずれていた」も一致）。

### ⭐ 結論は変わらない。むしろ強まる

| | 指示書 §1 の書き方 | ⭐ 実際 |
|---|---|---|
| 4件の内訳 | 4件とも「腐った」 | **3件が腐った ＋ 1件は最初から誤り** |
| 導かれること | **時間が経つとずれる** | ⭐ **時間が経つとずれる。しかも書いた本人がその場で合わせることすらできない** |

⚠️ ⭐ **「最初から合っていない」ほうが重い。** 腐りは時間の関数なので「新しいうちは正しい」と言えるが、
**導入時点で誤っているなら、この書き方には正しい瞬間が一度も無い**。
→ **§4 の検査を入れる判断の根拠が強くなる。**

⭐ **指示書の本文は書き換えない**（当時の判断の記録として残す）。
**本記録が訂正の正である**ことを、指示書の冒頭から参照する。

---

## 4. ⭐ 追加作業 — **`validate` に検査を1本足す**（報告 §7-2 の3択 → **(a)**）

⚠️ **開発担当の3択（(a) 検査を足す / (b) 起票案にする / (c) 放置）に対し (a)。**

### 根拠

| 選択肢 | 判断 |
|---|---|
| (c) 放置 | ❌ **だめ。** 4件のうち**3件が腐り・1件は最初から誤り**＝**この書き方が正しく保たれた例が1つも無い**（§3） |
| (b) 起票案 | ❌ **割に合わない。** 起票案を書いて索引に載せて投函前に再測定するほうが、**検査1本より高くつく**（指示書 §1 と同じ理由） |
| **(a) 検査** | ⭐ **採用。** **現在0件なので足せば即座に緑**。⭐ **「覚えておく」のではなく構造で防ぐ**（CLAUDE.md の一貫した方針） |

### ⚠️ ⭐ 仕様（**スコープを間違えると大量ヒットする**）

⚠️ **設計担当が実測した。スコープがこの検査の成否を決める**:

| 対象 | ヒット数（2026-09-04 実測） |
|---|---|
| ⭐ **コード**（`scripts/` `shared/js/` `functions/` `app.js` `tools/`・`shared/vendor/` を除く） | ⭐ **0件** |
| ⚠️ **`docs/`** | ⚠️ ⭐ **321件** |

⚠️ ⭐ **`docs/` を対象に含めてはいけない。** 設計書・実測メモは
**「その時点の実測値」を記録する文書**なので、行番号が残るのは正常である
（⭐ そもそも本タスクの指示書自身が `card-i18n.js:110` 等を実測値として書いている）。

**検査の要件**:

1. **対象**: `scripts/` `shared/js/` `functions/` `app.js` `tools/` の `.js` / `.mjs`。
   ⚠️ **`shared/vendor/` を除外する**（ライブラリの配布物にソースマップ由来の記述が入りうる）
2. **検出する形**: `<なにか>.js:<数字>` / `<なにか>.mjs:<数字>`（⭐ 実装報告 T2 の grep と同じもの）
3. **失敗時**: **exit 1**。⚠️ ⭐ **メッセージに「なぜだめか」と「どう書くか」を必ず出す**——
   `行番号は腐ります。関数名で参照してください（例: card-i18n.js の bannedFormats()）`
4. **成功時**: ⭐ **1行出す**（例: `no line-number refs in code comments — 0件`）。
   ⚠️ **無言で通さない**（「検査が動いていないこと」と「0件であること」が区別できなくなる）
5. ⚠️ **`docs/` は対象外であることをコードのコメントに理由つきで書く**
   （⚠️ **後から「docsも見たほうがよいのでは」と広げられると321件で即赤になる**）

### ⚠️ 検証（**破壊試験を必ず行う**）

| # | 内容 | 期待 |
|---|---|---|
| **W1** | そのまま `npm run validate` | **exit 0** ＋ 成功行が出る |
| **W2** | ⭐ **どれか1ファイルのコメントに `foo.js:123` を1行足す** | ⭐ **exit 1**・そのファイルと行が名指しされる。⚠️ **確認後に必ず戻す** |
| **W3** | ⚠️ ⭐ **`docs/` 配下に `foo.js:123` があっても緑のまま** | **exit 0**（＝スコープが効いている。⭐ **実際に321件あるので、何もしなくても確認できる**） |
| **W4** | `shared/vendor/` の除外 | ⭐ **除外されている**ことをコメントか実測で示す |

⚠️ ⭐ **これは `scripts/validate.mjs` の変更＝開発担当の担当パス。**
**設計担当は書かない**（越権になる）。⭐ **本記録を指示として `実装待ち` で渡す。**

---

## 5. 派生issueの要否 — ⭐ **不要**

- §4 は本記録の中の追加作業として処理する（起票案にしない＝ §4 の根拠のとおり）
- §2・§3 は記録の訂正で完結しており、コードの追加作業を生まない

---

## 6. ハンドオフ

⭐ **`実装待ち`**（§4 の検査1本）。⚠️ **§4 以外に残作業は無い。**
⚠️ **本番公開の前提条件ではない**（`validate` は本番配信物ではあるが、サイトの表示を変えない）。
