**対象: 「行番号参照の是正と検査」** — この宣言は 2026-09-11 に遡って追記した（索引の転記ドリフト防止）

# レビュー判定 — `shared/vendor` 除外コメントの**断定を外す**（2026-09-05）

⚠️ **GitHubアカウント停止中のため、issueの判定コメントの代わりにこのファイルを置く。**
復旧後に `gh issue comment <n> --body-file` でそのまま投函する
（索引 [`待ち行列と復旧手順.md`](../待ち行列/待ち行列と復旧手順.md) §2-④）。

- 呼び名: **「vendor除外コメントの断定」**（記号で呼ばない → 索引 §4-4）
- 指示（正）: [`追加指示_vendor除外コメントの断定_2026-09-05.md`](追加指示_vendor除外コメントの断定_2026-09-05.md)
- 由来（判断の根拠の正）: [`承認_行番号参照の検査の仕上げ_2026-09-05.md`](承認_行番号参照の検査の仕上げ_2026-09-05.md) **§4**
- 実装報告: [`実装報告_vendor除外コメントの断定_2026-09-05.md`](実装報告_vendor除外コメントの断定_2026-09-05.md)
- **レビュー対象コミット**: ⭐ **`ee97c6c7`**（`scripts/validate.mjs` の +3 / -3）
- 証跡: `tmp/review/未採番-vendor除外コメントの断定/`（gitignore対象）
- ⚠️ **V1〜V8 は報告を読むだけで済ませず、レビュー担当が独立に全項目を再実施した**

---

## 判定: ⭐ **合格**（差し戻し0往復）

| 基準 | 結果 | 証跡 |
|---|---|---|
| **M1 設計適合** | ✅ | 指示 §4 の **V1〜V8 全番号**に合否を付け、**全て期待値と一致**（下の対応表）。⭐ 新しい3行は指示 §2 の「変更後」ブロックと**バイト完全一致**（`byte-match.txt`・Python で `in src` → `True`、旧ブロックは `not in src` → `True`） |
| **M2 実駆動** | ✅ | ⭐ **検査自身を壊して実行**（V5・`LINEREF_EXCLUDE = []`）。サイト側も **PC 1280px / 375px の両幅**で5画面を操作、スクリーンショット10枚（`pc_1_top.png` 〜 `375_5_tournaments.png`）、**pageerror 0件** |
| **M3 回帰** | ✅ | `npm run validate` **exit 0**・成功行**10本すべて**（`v1-validate.txt`）。トップ検索7件 / 詳細モーダル（`#d-name`＝「ロレイン、閃光の剣士」）/ カード個別ページ / デッキ構築 / 大会一覧454行を**両幅で確認** |
| **M4 生成物の同期** | ✅ | ⭐ **生成ロジックを一切触っていない**（差分はコメント3行のみ）。`validate` の陳腐化検査（`generated JSON up to date` / `card term highlights up to date` / `meta.*`）も全て緑。`git status --short` **空** |
| **M5 静かな劣化がない** | ✅ | ⭐ **本タスクの要点。新文言の主張を破壊試験で直接裏取りした**——除外を `[]` にすると走査が **46→47** に動き（`v5-validate-broken.txt`）、⚠️ **exit 0 のまま＝「赤くなる」は今日成立しない**ことを独立に再現。⭐ 新文言の3主張は**いずれも1コマンドで反証できない**（下の §反証の試み） |
| **M6 公開安全性** | ✅ | 本番配信物の増減なし（`scripts/` は配信されるが `validate` は**どのページも読み込まない**＝表示不変。5画面の実駆動でも変化なし）。差分は**日本語コメント3行のみ**で秘匿情報なし。`_headers`・`404.html`・キャッシュに変更なし |
| **M7 スコープ** | ✅ | 変更ファイルは `scripts/validate.mjs` **1つだけ**（`^scripts/` 以外 **0行**）。⭐ **コメント行を除去して親コミットと `diff` した結果が空**＝ロジックに1文字も差分なし。指示 §3 のスコープ外**5点とも先回りなし**（下の §スコープ外の確認） |

---

## 1. 指示 §4 の検証項目との対応（⭐ **レビュー担当が独立に再実施**）

| # | 期待値 | ⭐ レビュー担当の実測 | 合否 |
|---|---|---|---|
| **V1** | exit 0・`no line-number refs in code comments — 0件（46ファイル走査）`・`all checks passed.`・成功行10本 | ⭐ **exit 0**・`no line-number refs in code comments — 0件（46ファイル走査）`・`all checks passed.`・**成功行10本**（`loaded OK` は読み込みの報告なので数えない） | ✅ |
| **V2** | `grep -c '配布物を拾って'` ＝ **0**（実施前 1） | ⭐ **0**（親コミット `14734d8d` の同ファイルで **1** を実測） | ✅ |
| **V3** | `grep -c '赤くなる'` ＝ ⚠️ **4**（0ではない。実施前 5） | ⭐ **4**（親コミットで **5**）。⚠️ **中身を開いて確認**（下の §V3 の内訳） | ✅ |
| **V4** | `grep -c '走査対象に入る'` ＝ **1**（実施前 0） | ⭐ **1**（親コミットで **0**） | ✅ |
| **V5** | `LINEREF_EXCLUDE` を `[]` にして **exit 0** のまま・`0件（47ファイル走査）` | ⭐ **exit 0**・`no line-number refs in code comments — 0件（47ファイル走査）`・`all checks passed.`。⚠️ **`赤くなる`（exit 1）にはならない**ことを自分の手で再現 | ✅ |
| **V6** | `scripts/validate.mjs` の **+3 / -3**・コメントのみ | ⭐ `1 file changed, 3 insertions(+), 3 deletions(-)`。⭐ **コメント行を落とした差分が空**＝ロジック不変 | ✅ |
| **V7** | `git -c core.quotepath=false show --name-only --format= ee97c6c7` ＝ 1行だけ | ⭐ **`scripts/validate.mjs` の1行のみ**（`grep -vc '^scripts/'` ＝ **0**） | ✅ |
| **V8** | `git status --short` に `scripts/validate.mjs` 以外が出ない | ⭐ **空**（レビュー開始時・破壊試験の復元後・作業完了時の3回とも） | ✅ |

### ⭐ V1 の「46」の裏取り（指示 §4 の脚注）

```
find scripts shared functions tools app.js \( -name '*.js' -o -name '*.mjs' \) -not -path 'shared/vendor/*' | wc -l
→ 46
```

⭐ **設計担当・開発担当の実測と一致。前提は変わっていない。**
⭐ **`shared/vendor` 配下の走査対象は `jspdf.umd.min.js` の1ファイル**（＝V5 の 46→47 と整合）。

---

## 2. ⚠️ ⭐ V3 の内訳（**取り違えていないことを中身を開いて確認**）

`grep -n '赤くなる' scripts/validate.mjs`（実施後・4件）と、親コミット `14734d8d` の同 grep（5件）を**並べて比較**した:

| 行 | 何の説明か | 実施前後 |
|---|---|---|
| **467** | 起票案の状態行の検査で、**日付の鮮度は見ない**理由 | ⭐ **行番号も中身も不変** |
| **528** | 起票案の索引見出しの正規表現を**緩めると壊れる**警告 | ⭐ **行番号も中身も不変** |
| **592** | ⚠️ ⭐ **`docs/` を走査対象に含めない理由**（「広げると即座に赤くなるだけ」） | ⭐ **行番号も中身も不変**。⚠️ **今回の置換対象と同じコメント塊の数行上にあるが、取り違えられていない**（`v3-context.txt` に前後4行つきで採取） |
| **668** | 用語ハイライト検査が赤くなる主な3原因の前置き | ⭐ **行番号も中身も不変** |
| ~~596~~ | ⭐ **`LINEREF_EXCLUDE` 直前の1件だけが消えた**（＝置換対象） | **消えた（正しい）** |

⭐ **残る4件は行番号すら動いていない**（置換が3行→3行で行数を変えていないため）。
⚠️ **`grep -c` の期待値を 0 と書く型の誤りは発生していない。**

---

## 3. ⚠️ ⭐ 反証の試み（**新文言の3主張を、レビュー担当が自分で崩しにいった**）

⭐ **本タスクの本質はコメントの文言**なので、「反証できる主張が1つも残っていないか」を**能動的に確かめた**。

| 新しい主張 | 反証を試みた方法 | ⭐ 結果 |
|---|---|---|
| ① **「この除外を外すと配布物が走査対象に入る」** | 除外を `[]` にして実行（V5） | ⚠️ **反証できない**。走査が **46→47** に増え、増分は `shared/vendor/jspdf.umd.min.js`。⭐ `LINEREF_ROOTS` に `shared` がある限り**構造的に成立**（`walk()` は `excluded()` で刈るだけなので、除外を外せば必ず入る） |
| ② **「（成功行のファイル数が動く）」** | 数字が書かれていないか・実際に動くか | ⚠️ **反証できない**。⭐ **数値を1つも書いていない**ため腐りようがなく、実測でも `（46ファイル走査）`→`（47ファイル走査）`と**実際に動いた** |
| ③ **「配布物にソースマップ由来の記述が入りうるが、自分では直せない」** | 配布物の中身を実測 | ⚠️ **反証できない**。`sourceMappingURL` は **1件実在**（「入りうる」は妥当）。⭐ **「入りうる」は可能性の主張なので、現在ヒット0件（実測）でも嘘にならない**。「自分では直せない」は他人の配布物という**構造的事実** |

⭐ **旧文言との違いが決定的**: 旧「**赤くなる**」は `LINEREF_EXCLUDE` を `[]` にする**1コマンドで exit 0 が返って反証できた**
（V5 で再現）。⚠️ ⭐ **警告の唯一の読者＝消そうか迷っている人が、試した瞬間に「嘘だから消してよい」と
誤った確信を得る経路**は、この置換で**消えている**（＝指示 §1 が狙ったとおり）。

⚠️ **設計担当が退けたレビュー案（「現在の配布物にヒットは無いが」）を採らなかったのも確認した**——
その文言は**測定値**なので `jspdf` を更新した日に腐る。⭐ **採用文言には測定値が1つも入っていない。**

---

## 4. スコープ外（指示 §3）の確認 — ⭐ **5点とも先回りなし**

| やらないこと | 実測 |
|---|---|
| `LINEREF_EXCLUDE` を増やす／減らす | ⭐ `["shared/vendor"]` のまま（**親コミットと同じ600行目・同じ中身**）。V5 の破壊は**レビュー担当の作業ツリーでの一時的なもの**で、コミットには含まれない |
| `LINEREF_ROOTS` を触る | ⭐ `["scripts", "shared", "functions", "app.js", "tools"]` のまま |
| 他の「赤くなる」4箇所を直す | ⭐ **1件も触っていない**（§2 の表・行番号も中身も不変） |
| 冒頭コメントの検査一覧を触る | ⭐ **触っていない**（差分は 591〜599 行の範囲のみ）。成功行10本と順序が一致したままであることも V1 で確認 |
| 検査ロジック（`REF_RE`・走査・fail-closed）を触る | ⭐ **触っていない**。⚠️ **コメント行を除去して親コミットと `diff` した結果が空**＝コード側は1文字も動いていない |

### 既存表記との整合

⭐ **コメントの体裁が周囲と揃っている**ことを確認した——`// ⚠ ` で始まり続き行は `//   `（スペース3つ）、
⭐ **`⚠` は VS16 なし**（`repr()` で前後2件（`docs/` 除外・fail-closed）と**同一の字形**であることを実測）。

---

## 5. ⚠️ 破壊試験の後始末（レビュー担当の規約）

- ⚠️ **`git checkout` / `stash` / ブランチ切り替えは使っていない**（作業ツリーを変更しない規約）。
  ⭐ **仕込みは自分でバックアップを取り、自分で書き戻す形**にした
- 手順: `cp` でバックアップ → Python の**一時ファイル + `os.replace()`** で書き換え（VS Code 対策）→ 実行 →
  同じ方法で書き戻し
- 復元の確認: ⭐ **`md5sum` が破壊前と一致**（`00f9dc50193f34014b5d3bb084c5a89f`）・`diff -q` で **IDENTICAL**・
  `git status --short` が ⭐ **空**（コミット済みなので `M` すら出ないのが正）・
  復元後の `npm run validate` が ⭐ **exit 0・`0件（46ファイル走査）`** に戻ることまで確認
- ⚠️ **`tmp/` 配下の他セッションの作業物には触れていない**

---

## 6. 実駆動（M2・M3）の証跡

| 画面 | PC 1280×900 | 375×812 |
|---|---|---|
| トップ（「ロレイン」で検索） | `pc_1_top.png`・**7件を表示** | `375_1_top.png`・**7件を表示** |
| カード詳細モーダル | `pc_2_modal.png`・`#d-name`＝**ロレイン、閃光の剣士** / `#d-name-en`＝Lorraine, Arclight Saber / 効果文あり | `375_2_modal.png`・**同上** |
| カード個別ページ | `pc_3_cardpage.png`・`h1`＝**ロレイン、放浪の戦士** | `375_3_cardpage.png`・**同上** |
| デッキ構築 | `pc_4_deckbuilder.png`（⚠️ 未ログインのため `#boot-status` は「読み込み中…」＝**既知の正常**） | `375_4_deckbuilder.png`・同上 |
| 大会一覧 | `pc_5_tournaments.png`・**454行** | `375_5_tournaments.png`・**454行** |

⭐ **両幅とも `pageerror` 0件**（`smoke.txt` / `smoke2.txt` / `smoke3.txt`）。
サーバーは ⚠️ **3100番**（レビュー担当のポート）で起動し、**待受ポートから PID を引いて個別に停止済み**。

---

## 7. 推奨基準（S）の指摘 — ⭐ **無し**

⭐ **今回は S の指摘が1件も無い。** コメント3行の置換が指示のとおり実施され、体裁・スコープ・
主張の反証耐性のいずれにも不足が見当たらなかった。

---

## 8. 設計担当への確認事項

⭐ **合否を左右する確認事項は無い**（本判定は確定）。以下は**参考**（回答不要）:

### 参考1 — ⭐ 主張②が成立する条件（**現時点では成立。指摘ではない**）

「成功行のファイル数が動く」は、⭐ **`shared/vendor/` に走査対象（`.js`/`.mjs`）が1つ以上あること**に依存する。
現在は `jspdf.umd.min.js` の**1ファイル**があるので成立する（V5 で実測）。
⚠️ **仮に将来 `shared/vendor/` が空になれば「動かない」が、⭐ そのときはこの除外自体が無意味になる**ので、
**文言としての問題ではない**と判断した（＝**不合格の理由にしない**）。

### 参考2 — ⚠️ 開発担当の P2（自動検査が無い点）はレビューでも同じ観測

⭐ **`LINEREF_EXCLUDE` から `shared/vendor` が消えたことを検出する自動検査は依然として無い**——
守っているのは**コメントだけ**である点は本タスクでも変わらない。
⚠️ **ただし本タスクの目的は「反証できる断定を消すこと」であって検査を足すことではない**ので、
⭐ **これを不合格の理由にしない**（承認記録 §4 の趣旨どおり）。検査の要否は**設計担当の領分**。

---

## 9. ハンドオフ

- ⭐ **`設計確認待ち`**（GitHub復旧後は `gh issue edit <n> --remove-label レビュー待ち --add-label 設計確認待ち`）
- ⚠️ **最終承認とクローズは設計担当が行う**
- ⚠️ **push・`wrangler pages deploy` は実行していない**（本コミットは未pushのまま）
