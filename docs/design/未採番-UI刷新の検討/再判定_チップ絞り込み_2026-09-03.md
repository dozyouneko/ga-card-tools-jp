**対象: 「チップ絞り込み欄」** — この宣言は 2026-09-11 に遡って追記した（索引の転記ドリフト防止）

# 再判定（差し戻し1往復目）— チップ絞り込み欄の M1-1 修正（2026-09-03）

⚠️ **GitHubアカウント停止中で `gh` が使えないため、issueコメント／ラベル付け替えの代わりにこのファイルに書く**
（索引 [`待ち行列と復旧手順.md`](../待ち行列/待ち行列と復旧手順.md) §2-③）。
⚠️ **前回の判定 [`レビュー判定_チップ絞り込み_2026-09-03.md`](レビュー判定_チップ絞り込み_2026-09-03.md) は上書きせずに残してある。**

- 対象: **`105357c2`**（`shared/js/card-search.js` の Escape ハンドラのみ・2行）＋ 報告追記 **`1bf06874`**
- 差分: `git diff 613420f6..HEAD`（変更は `shared/js/card-search.js` と実装報告の2ファイルのみ）
- 設計書: [`チップ絞り込み欄_設計.md`](チップ絞り込み欄_設計.md) **v1.1**（＝契約）
- 実装報告: [`実装報告_チップ絞り込み_2026-09-03.md`](実装報告_チップ絞り込み_2026-09-03.md) §11〜§14（第3周）
- 検証環境: レビュー担当の **3100番**（⚠️ 3000番は設計担当のユーザー確認用。**触っていない**）/
  Playwright 1.61.1 + Chromium（`--ignore-certificate-errors` + `ignoreHTTPSErrors`）
- 証跡: `tmp/review/未採番-チップ絞り込み/`（gitignore対象。本周の分は `log-r3-*.txt` / `r3-*.png`）

---

## 判定: 合格

⭐ **前回の不合格理由 M1-1（W6 / S-1 の `Esc` 経路）は解消**。
⭐ **修正で壊れうる箇所（C4 / `reset()` / `setValues()`）を「逆方向の壊れ方」として測り直し、
いずれも回帰なし**。W1〜W15 全数と M1〜M7 を満たす。

| 基準 | 結果 | 証跡 |
|---|---|---|
| **M1** 設計適合 | ✅ | W1〜W15 が全番号一致（下表）。⭐ **W6 は 3経路 × 2状態 = 6通りを全数実測**（`log-r3-matrix.txt`） |
| **M2** 実駆動 | ✅ | 自分で操作。**PC 1400px（左ペイン320px）** と **375px（ボトムシート）** の両方。`r3-matrix-end.png` / `r3-375-searching.png` / `r3-375-after-esc1.png` / `r3-hintpos-*.png` ほか |
| **M3** 回帰 | ✅ | `npm run validate` **exit 0**（`log-r3-validate.txt`）。トップ（全2495件）・**カード詳細モーダル**（`gacd-modal`「自己放棄」）・**カード個別ページ**（`/cards/abnegation/`）・**デッキ構築**・**大会一覧**（454行）。⭐ **レアリティ絞り込み（直前タスク）も実駆動**（`?rarity=3` → 全645件） |
| **M4** 生成物の同期 | ✅ | 生成ロジック（`scripts/**`）に差分なし・訳の追加なし。`git status --short` は**空** |
| **M5** 静かな劣化がない | ✅ | ⭐ **`:has()` 相互作用の破壊試験を再実施**：ルールを CSSOM で `max-height:200px` に戻すと入れ子スクロールが **0個 → 2個**（`log-r3-t4-W12destructive.txt`）。⭐ **今回の修正が「経路によって挙動が違う」型だったので、W6 は経路網羅で塞いだ**（下表） |
| **M6** 公開安全性 | ✅ | 新規の配信物ゼロ（既存1ファイルの2行変更）。`_headers` / `404.html` / キャッシュの前提に変更なし。秘匿情報なし |
| **M7** スコープ | ✅ | `git -c core.quotepath=false diff --name-only 613420f6..HEAD` は `shared/js/card-search.js` と実装報告の**2本のみ**。⚠️ **`reset()` / `setValues()` / C4 の判定順序には手が入っていない**（差分で確認） |

### 推奨基準

| 基準 | 結果 | 内容 |
|---|---|---|
| **S1** コード品質 | ✅ | ⭐ **「`clearChipFilter()` は `reset()`/`setValues()` 専用」とコメントで明記**され、同じ踏み違いの再発を止める形になっている（消さないこと） |
| **S2** 応答性 | ✅ | api.gatcg.com へのリクエストは **46 → 46**（打鍵・Enter とも増えない） |
| **S3** fail-open | ✅ | 前周から変化なし（`:has()` 非対応でも「200pxの枠が2つ並ぶ」だけ） |

---

## ⭐ M1-1 の検証 — W6 を「3経路 × 2状態 = 6通り」で全数実測

⚠️ **前回の見落としは症状ではなく「検証設計の穴」**（＝入力を空にする経路が3つあることを観点にしていなかった）。
⭐ **経路を網羅したことが分かる形で残す。**すべて**実操作**（`page.fill` での一括代入ではなく、
打鍵・実マウスクリック・キー入力）で測った（`tmp/review/未採番-チップ絞り込み/r3-matrix.mjs`）。

| 検索前の状態 | 空にする経路 | `restHidden` 前→中→後 | 後の入力 | 後の可視チップ | 後の `.morebtn` | 後の `.cfhint` | 群の丈 前→中→後 | 判定 |
|---|---|---|---|---|---|---|---|---|
| **畳** | 打鍵削除（Backspace） | true → false → **true** | `""` | 157 | ＋ すべて表示（157種） | 157種 | 394 → 180 → **394px** | ✅ |
| **開** | 打鍵削除（Backspace） | false → false → **false** | `""` | 157 | − よく使う分だけ表示 | 157種 | 3942 → 180 → **3942px** | ✅ |
| **畳** | ⭐ **ネイティブ ✕（実マウスクリック）** | true → false → **true** | `""` | 157 | ＋ すべて表示（157種） | 157種 | 394 → 180 → **394px** | ✅ |
| **開** | ⭐ **ネイティブ ✕（実マウスクリック）** | false → false → **false** | `""` | 157 | − よく使う分だけ表示 | 157種 | 3942 → 180 → **3942px** | ✅ |
| **畳** | ⭐ **`Esc`**（前回 ❌ の経路） | true → false → **true** | `""` | 157 | ＋ すべて表示（157種） | 157種 | 394 → 180 → **394px** | ✅ **解消** |
| **開** | ⭐ **`Esc`** | false → false → **false** | `""` | 157 | − よく使う分だけ表示 | 157種 | 3942 → 180 → **3942px** | ✅ |

- ⭐ **6通りとも「後」が「前」と完全一致**（`restHidden` / 可視157 / ボタン文言 / `.cfhint` / 群の丈 / `.more` の可視）。
  ⚠️ **前回 Esc・畳で 394px → 3942px（約10倍）になっていた症状は再現しない**
- ⚠️ ⭐ **ネイティブ ✕ は「クリックが当たったこと」を確かめてから測っている。**
  入力欄の右端から 4〜30px を 2px 刻みで総当たりし、**右端 −16px で初めて `value === ""` になる**ことを確認した
  （`log-r3-probe-clear.txt`）。⚠️ **当たらない座標で測ると「何も起きなかった」を「復元しない」と誤読する**
  （実際、前回の測定で右端 −9px を使い ❌NG と出たが、それは**入力が `"dragon"` のまま**＝クリックが外れていた）
- 修正の中身も差分で確認: `clearChipFilter()` の呼び出しをやめ **`cfInput.value = ""` → `applyChipFilter()`**。
  ⭐ **Backspace / ✕ と同じ経路に合流**しており、経路ごとの分岐が減っている

---

## 設計書の検証項目との対応（W1〜W15・全数を再測）

| # | 期待値 | 実測 | 合否 |
|---|---|---|---|
| **W1** | 30種以下の群に欄が出ない | エレメント13 / クラス9 / タイプ13 / レアリティ9 → 4群とも `.cfilter` **0個**（4群にも `search: true` は渡っている＝閾値が効いている） | ✅ |
| **W2** | サブタイプ（157種）に出る | チップ157個・`.cfilter` 1個・`.cfhint="157種"`・プレースホルダ `絞り込む… ドラゴン / dragon`・`aria-label` あり・**`aria-live` なし**・`title` なし | ✅ |
| **W3** | `ドラゴン` / `dragon` どちらでも一致 | `ドラゴン`→1件 / `dragon`→1件 / `DRAGON`→1件 / `zzz`→0件（`一致なし`） | ✅ |
| **W4** | 「よく使う20種」の外を取りこぼさない（C-A） | 上位20種に WOLF は無い → `WOLF` で `.chips-rest` が自動展開されチップ可視（74.5×33.75px・「狼WOLF」） | ✅ |
| **W5** | 選択済みは一致しなくても消えない（C-B） | WOLF 選択中に `zzz` → 可視は WOLF の1件・`一致なし（選択中のみ表示）` | ✅ |
| **W6** | 入力を空に戻すと検索前の展開状態に戻る（S-1） | ⭐ **3経路 × 2状態 = 6通りすべて一致**（上表） | ✅ |
| **W7** | APIが増えない・結果件数が変わらない | api.gatcg.com **46 → 46**（打鍵＋Enter後も不変）・`#status` は「50 件を表示 / 全 2495 件」のまま・Enterで遷移しない | ✅ |
| **W8** | `#reset` で検索欄も空（S-3） | ⚠️ **逆方向の壊れ方を狙って「開いた状態で検索中 → reset」**で実測 → 入力=""・`.chips-rest` **畳み**・`.cfhint="157種"`・`hidden` 残り **0件**・文言も畳んだ側 | ✅ |
| **W9** | `?subtype=WOLF` で検索欄が空・隠れ側が復元（S-4） | 入力=""・`.chips-rest` 展開・選択 `["WOLF"]`・WOLF 可視・`hidden` 残り0件・「7 件を表示」。⭐ **絞り込み中（畳・`zzz`）に `setValues(["HUMAN"])`** でも 入力=""・可視157・選択 `["HUMAN"]` | ✅ |
| **W10** | 375px で `font-size:16px`・Esc の2挙動（C4） | `.cfinput` **16px**（`#q` と同値）・破線・横スクロール0。⭐ **入力あり Esc → 入力だけ消え、シートは開いたまま（`aria-expanded=true`）、しかも `.chips-rest` は畳みに復元** / **空で Esc → シートが閉じ `no-scroll` 解除** | ✅ |
| **W11** | デッキ構築に出ない・見た目が変わらない（S1） | 4群とも `.cfilter` **0個**。⭐ 旧CSS（`05ef5267` 版）を `page.route` で差し替えて撮った `#s-g-subtype` が新CSSと **md5 一致**（`c42a8eb3…`・寸法 1352×224.22 も一致） | ✅ |
| **W12** | 検索中に二重スクロールが出ない（`:has()`） | 検索中の `.chips` / `.chips-rest` は `max-height: none` / `overflow-y: visible`＝入れ子スクロール0（ペインの1本だけ）。⭐ **破壊試験でルールを戻すと2個に増える** | ✅ |
| **W13** | `npm run validate` exit 0 | **exit 0**（`all checks passed.`） | ✅ |
| **W14** | 320pxのペインでプレースホルダが見切れない | `value` に入れて実測: `scrollWidth 259px` vs `clientWidth 259px` ＝ **はみ出し 0px**。横スクロールは 文書0 / ペイン0 / `.cfilter` 0。スクリーンショットでも全文が読める | ✅ |
| **W15** | 一致なしで選択を外してもチップは残る（C7） | 外した直後: 可視 `["WOLF"]`・`一致なし（選択中のみ表示）` → 次の打鍵で 可視0・`一致なし` | ✅ |

### 追加で見た回帰

| 確認 | 結果 |
|---|---|
| **レアリティ絞り込み（直前タスク）** | ✅ `MULTI` / `isIndexBlind()` / `haveOf()` / `metaMatches()` に差分なし。実駆動で レアリティ「3」→ `?rarity=3`・「50 件を表示 / **全 645 件**」 |
| 絞り込み欄から選んだ条件で実検索が走る | ✅ `wolf` で絞る → WOLF 選択 → `?subtype=WOLF`・「7 件を表示」・タイル7枚 |
| 主要画面 | ✅ トップ / カード詳細モーダル / カード個別ページ / デッキ構築 / 大会一覧 |
| JSエラー | ✅ `pageerror` **0件**（全シナリオ） |

---

## ⚠️ 実装報告の記述の確認（合否には無関係）

⭐ **第3周の §13（375px の `.cfhint` の訂正）は実態と一致している。**独立に測り直した結果:

| 幅 | `157種` | `2件が一致` | `一致なし` | `一致なし（選択中のみ表示）` |
|---|---|---|---|---|
| **320px（PC左ペイン）** | 下の行 | 下の行 | 下の行 | 下の行 |
| **375px（ボトムシート）** | **横並び** | **横並び** | **横並び** | ⭐ **下の行** |

（`log-r3-hintpos.txt`。同じ行かどうかは **`.cfhint` の `left` が `.cfinput` の `right` より右か**で判定）

- ⚠️ ⭐ **前回のレビューが「375px でも下の行に回る」と書いたのは、こちらの測り方の誤りだった。**
  `top` の一致で同じ行かを判定していたが、`.cfilter` は `align-items: center` なので
  **高さの違う要素は同じ行でも `top` が 30px ずれる**。⭐ **本項をもって前回の参考指摘 S1-1 は取り下げる**
  （＝**開発担当の第2周の記述「375px は横並び」は、短い文言については正しかった**）
- ⭐ **教訓は第2周の `canvas.measureText()` と同型**：**数値だけで見た目を判定せず、スクリーンショットで裏を取る**
  （今回もスクリーンショットで気づいた）

---

## 推奨基準の指摘（合格を妨げない・参考）

- **参考1**: `.fgroup .chip[hidden] { display: none }`（C6）が**この機能の生命線**で、
  外れると**絞り込みが何も隠さないのに検査は緑のまま**になる。⚠️ 自動検査は無い（気づけるのは目視だけ）。
  ⭐ CSSに理由コメントがあるので現状のままで可。恒久化の要否は設計担当の判断で
- **参考2**: `setValues()` の後は「検索前の展開状態」ではなく**既存の展開処理の結果**が残る
  （畳んだ状態で検索中に復元が走ると、`.chips-rest` は開いたまま）。⭐ **設計書 S-4 が
  「既存の展開処理に任せる」と決めている**とおりなので**仕様どおり**。URL復元は画面の作り直しに近く、
  実害は無いと判断した
- **参考3**: W15 の「外した直後」は選択0件でも `.cfhint` が `一致なし（選択中のみ表示）` のまま
  （次の打鍵で戻る）。⭐ **C7 で追認済みの挙動の帰結**

---

## 設計担当への確認事項

1. **Q3（`.cfhint` の行送り）**: ⭐ **合否には含めていない**。実測でも
   **320px でプレースホルダ全文が見え・横スクロールは増えていない**ので、レビューとしては妥当な実装判断と考える。
   ⚠️ **位置を設計側で決めたい場合のみ**差し戻してください
2. **差し戻しの記録**: 往復は **1回**で解消（M1-1 のみ）。⭐ **基準の解釈違いも仕様の曖昧さも無かった**
   （設計書 S-1 の文言どおりの取りこぼしで、開発担当も反論なく受け入れている）

---

## 次の工程

- ⚠️ **`gh` が使えないため、ラベルの付け替えはできません。** 本判定は **`設計確認待ち` 相当**。
  最終承認とクローズは設計担当が行う
- ⚠️ ⭐ **UI の変更なので、承認の前に「ユーザーがローカルサーバーで実物を触る工程」が入る**
  （CLAUDE.md 締め作業 項目5の例外・2026-09-02 にユーザーが明示要求）。
  ⭐ **設計担当が上げている 3000番**がその用途で、レビュー中も稼働継続を確認している
- ⚠️ **レビュー担当はコードを直していません**（作業ツリー無変更・`git status --short` は空）
- ⚠️ **push はしていません**（本番公開の可否はユーザー判断）。
  レビュー用に起動した **3100番は停止済み**（⚠️ **3000番は触っていません**）
