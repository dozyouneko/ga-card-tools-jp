**対象: 「行番号参照の是正と検査」** — この宣言は 2026-09-11 に遡って追記した（索引の転記ドリフト防止）

# 追加指示（2026-09-04）— コメント内の腐った行番号参照を、残り**4件すべて**関数名参照に直す

⚠️ **これは issue コメント（`実装待ち` の指示）の代替。** GitHubアカウント停止中のため
ファイルで受け渡す。復旧後は `gh issue comment <n> --body-file` で投函する。

- 経緯: 意味統一のレビュー **S1-1** → [`承認_意味統一_2026-09-04.md`](承認_意味統一_2026-09-04.md) §8 →
  実装 `559965f0` / 報告 [`実装報告_S1-1_2026-09-04.md`](実装報告_S1-1_2026-09-04.md) §6-1（**残り2件の発見**）
- ✅ ⭐ **状態: 完了**（実装 `895bade2` / 報告 `df82dd8c` / 承認 [`承認_腐った行番号参照_2026-09-04.md`](承認_腐った行番号参照_2026-09-04.md)）

> ## ⚠️ ⭐ 2026-09-04 訂正 — **§1 の「4件中4件が腐っている」は不正確**
>
> **実測すると #3（`gen-tl-json.mjs` → `card-detail.js:200`）は「腐った」のではなく、
> 導入コミット `9e835e56`（2026-07-26）の時点で既に19行ずれていた。**
> ⭐ 正しい内訳は **「3件が腐り ＋ 1件は最初から誤り」**。
> ⚠️ **結論（関数名で書く）は変わらないどころか強まる**——
> **この書き方には正しい瞬間が一度も無かった**ことになるため。
> ⭐ **本文はそのまま残す**（当時の判断の記録）。**訂正の正は
> [`承認_腐った行番号参照_2026-09-04.md`](承認_腐った行番号参照_2026-09-04.md) §3。**

## 1. 判断 — ⭐ **(a) 同じ扱いで直す。範囲は「リポジトリ全4件」**

開発担当の3択（(a) 直す / (b) 起票案 / (c) 放置）に対し、**(a)**。⚠️ ただし
**「同一ファイルの残り2件」ではなく「リポジトリ全体の4件」**を範囲とする。

### ⭐ 根拠 — **実測したら、横展開の心配自体が消えた**

開発担当は「横展開すると `scripts/` `shared/js/` 全体に広がりうる」と述べたが、**設計担当が数えた**:

```bash
grep -rnoE '[A-Za-z0-9_.-]+\.(js|mjs):[0-9]+(-[0-9]+)?' scripts/ shared/js/ functions/ app.js tools/
```

⭐ **リポジトリ全体で 4件しか無い。しかも 4件とも既にずれていた（腐り率 100%）。**

| 判断材料 | 結果 |
|---|---|
| 範囲は膨らむか | ❌ **膨らまない**（S1-1 で2件を処理済み。⭐ **残りはリポジトリ全体で4件だけ**） |
| 起票案にする価値 | ❌ **無い。** 起票案を書いて索引に載せて投函前に再測定するほうが、**直すより高くつく** |
| 放置してよいか | ❌ **だめ。** 4件中4件が腐っている＝**この書き方は必ず腐ると実証された** |

⚠️ ⭐ **「横展開が怖い」と思ったら、まず数える。** 今回は 1コマンドで杞憂だと分かった。
**数えずに起票案へ逃がすと、腐る下書きが1件増えるだけだった**（CLAUDE.md §4-2 の教訓）。

## 2. 直す対象（**4件・全部**）— ⚠️ **設計担当が実測して参照先を確定済み**

| # | 場所 | 現在のコメント | ⚠️ その行にある実物 | ⭐ 直す先 |
|---|---|---|---|---|
| 1 | `scripts/gen-card-meta-index.mjs:45` | `card-i18n.js:110 と同義` | `loadEffects()` 内の `fetchJson(...)` | **`card-i18n.js` の `bannedFormats()`**（実測 `:176`） |
| 2 | `scripts/gen-card-meta-index.mjs:118` | `card-search.js:396 の if (!entry) return true;` | `MULTI` の説明コメント | **`card-search.js` の `metaMatches()`**（実測 `:648`・当該行は `:650`） |
| 3 | `scripts/gen-tl-json.mjs:14` | `詳細ダイアログが日本語フレーバーも使うため(card-detail.js:200)` | `metaRow("タイプ"...)` の並び | **`card-detail.js` のフレーバー描画**（実測 `:305` の `flavorOf(card, t)` 周辺） |
| 4 | `tools/deck-builder/app.js:1426` | `トップページの app.js:64 と同じ規則` | ルート `app.js:64` は `loadNames()` の待ち合わせ | **ルート `app.js` の `preferredArtIndex()`**（実測 `:83`） |

⚠️ ⭐ **3と4は開発担当の報告に無い**（同担当は `gen-card-meta-index.mjs` だけを見た）。**見落とさないこと。**

## 3. 書き方（⭐ S1-1 と同じ規則）

- ⭐ **行番号を書かない。関数名で参照する**（例: `card-i18n.js の bannedFormats()`）
- ⚠️ **「新しい行番号に直す」で済ませない**——**次のコミットでまた腐る**（4/4 が腐った実績）
- ⭐ **参照先が本当にその関数かを `git log -S` で裏取りする**（S1-1 でやったのと同じ手順）。
  ⚠️ **上の表の「直す先」は設計担当の実測だが、鵜呑みにせず自分で確かめてから書く**
- ⚠️ **コメントの意味は変えない**（#29 のレビュー指摘に由来する経緯付きのコメントがある＝ #2 の `:118`）。
  **指し先の書き方だけを変える**

## 4. 検証項目

| # | 期待値 |
|---|---|
| **T1** | 上の4箇所すべてが**関数名参照**になっている（行番号を含まない） |
| **T2** | ⭐ `grep -rnoE '[A-Za-z0-9_.-]+\.(js\|mjs):[0-9]+(-[0-9]+)?' scripts/ shared/js/ functions/ app.js tools/` の**ヒットが 0件**（⚠️ 実施前は **4件**） |
| **T3** | 各参照先の関数が**実在する**（`grep -n "function <名前>" <ファイル>` が1件以上） |
| **T4** | `npm run validate` が **exit 0** |
| **T5** | ⭐ **生成物に差分が出ない**（コメントのみの変更）。`node scripts/gen-tl-json.mjs` を回して `git status --short` が空 |

⚠️ **T2 の期待値「0件」は、置換後にその文字列が消えることを前提にしている。**
⭐ **実行して数字を確かめてから報告に書くこと**（設計担当が過去2回、期待ヒット数を数えずに書いて差し戻された）。

## 5. スコープ外（⚠️ **やらないこと**）

- ⚠️ **コメントの内容の書き換え・整理**（誤字直し・要約・削除）。**指し先の表記だけ**
- ⚠️ **同種でない参照**（`#29` `#41` のようなissue番号・設計書へのパス）は**触らない**
- ⚠️ **本件は本番公開の前提条件ではない**（コメントのみ・挙動に影響しない）。
  ⭐ 終えたら **`push待ち` 相当**（CLAUDE.md ハンドオフの例外。承認済みタスクの残作業のため）
