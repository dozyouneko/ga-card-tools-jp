# 追加指示（2026-09-04）— 行番号参照の検査の**仕上げ3点**

⚠️ **これは issue コメント（`実装待ち` の指示）の代替。** GitHubアカウント停止中のため
ファイルで受け渡す。復旧後は `gh issue comment <n> --body-file` で投函する
（索引 [`待ち行列と復旧手順.md`](../待ち行列/待ち行列と復旧手順.md) §2-④）。

- 呼び名: **「行番号参照の検査の仕上げ」**（記号で呼ばない → 索引 §4-4）
- 前提（完了済み）: [`承認_行番号参照の検査_2026-09-04.md`](承認_行番号参照の検査_2026-09-04.md)
  ＝ **本体タスクは承認済み**（実装 `8e2c8782`）。⚠️ **本件はその上に積む小タスクで、承認を覆すものではない**
- 由来: 承認記録 **§4-1（Q1）/ §5（S1-1）/ §6**
- 触るファイル: ⭐ **`scripts/validate.mjs` の1ファイルだけ**（⚠️ 生成物・データ・HTML/CSSは触らない）

---

## 0. なぜやるのか（1行ずつ）

| # | 直すこと | ⚠️ 直さないと |
|---|---|---|
| **#1** | 走査ルートを `"shared/js"` → **`"shared"`** に広げる | ⚠️ **手書きの `shared/pdf/card-sheet.js` が検査の外に残る**（⭐ **実質の穴はこの1本だけ**と実測済み） |
| **#2** | 冒頭コメントの検査一覧に**3件**を追記する | ⚠️ **一覧が当てにならない状態が続き、「一覧は当てにならない」と学習される** |
| **#3** | コメント中の `278件` を腐らない書き方に直す | ⚠️ ⭐ **「コードに書いた数字は腐る」と実証したタスクの検査自身が、腐った数字を抱えている**（現在は **300件**） |

---

## 1. #1 — 走査ルートを `shared` に広げる

### 変更する行

```js
const LINEREF_ROOTS = ["scripts", "shared/js", "functions", "app.js", "tools"];
//                                 ^^^^^^^^^^ → "shared"
```

### ⚠️ 同時に直すコメント（**忘れると、直った直後に嘘になる**）

現在の `shared/vendor` 除外の説明はこう書いてある:

```
//   今のルート（shared/js）からは届かないが、ルートを shared/ へ広げても穴が開かないよう
//   除外を明示しておく。
```

⚠️ ⭐ **#1 を入れた瞬間、この「今のルートからは届かない」は事実でなくなる。**
⭐ **「ルートが `shared` なので除外が実際に効いている（外すと `shared/vendor/` を拾う）」という趣旨に書き換える。**

### ⚠️ やらないこと

- ⚠️ **`LINEREF_EXCLUDE` を増やさない**（`shared/vendor` のままでよい。**実測でヒット0件**）
- ⚠️ **`shared/css` を拾う心配は不要**（走査は `.js` / `.mjs` だけ）

---

## 2. #2 — 冒頭コメントの検査一覧に3件を追記する

⚠️ **新検査だけでなく、⭐ 未記載の既存2件も同時に足す**（承認記録 §5）。

### 現状と、あるべき姿

`node scripts/validate.mjs` が出す**成功行は10本**で、実行順は次のとおり:

| 実行順 | 成功行の先頭 | 冒頭コメントに載っているか |
|---|---|---|
| 1 | `generated JSON up to date` | ✅（#22 フェーズ2） |
| 2 | `top set links up to date` | ✅（#37） |
| 3 | `meta.sets covers all sets` | ✅（#40） |
| 4 | `meta.subtypes covers all subtypes` | ✅ |
| 5 | `meta.rarities covers all rarities` | ❌ **未記載** |
| 6 | `card-meta-index sort keys OK` | ❌ **未記載** |
| 7 | `cron git add paths in sync` | ✅（#70） |
| 8 | `issue drafts in sync` | ✅ |
| 9 | `no line-number refs in code comments` | ❌ **未記載**（今回追加した検査） |
| 10 | `card term highlights up to date` | ✅（「最後に、…」） |

### 書き方

- ⭐ **既存の文体に合わせる**（「さらに…」「加えて…」「同型で…」「続いて…」）
- ⚠️ ⭐ **順序を実行順に合わせる**——**新検査は「最後に、…用語ハイライト…」の前**に入る。
  ⚠️ **「最後に」が指すのは用語ハイライトのままにする**（そこが本当に最後）
- ⭐ **1行ずつ・各1文で足りる**（例: 「同型で `meta.rarities`（レアリティ絞り込みの選択肢）が
  実データの全レアリティを覆っているかも検査する。」）
- ⚠️ **既存の記述を書き換えない**（足すだけ。整理・要約はスコープ外）

---

## 3. #3 — コメント中の `278件` を腐らない書き方に直す

### 変更する行

```
//   文書なので、行番号が残るのはむしろ正常（2026-09-04 実測で278件ある）。ここへ広げると
```

⚠️ **`278` は実装した時点の実測値で、⭐ 同じ日のうちに 300 になった**
（設計担当が時点ごとに測り直した内訳は承認記録 §6）。**この文書群自身が行番号参照を書くので、
記録が増えるたびに増える**＝**固定値として書く意味がない**。

### ⭐ 直す方向

- ⭐ **桁と「増えること」だけを書く**（例: `2026-09-04 時点で約300件あり、記録が増えるたびに増える`）
- ⚠️ **正確な数に更新して済ませない**（⭐ **それは「新しい行番号に直す」と同じ失敗**）
- ⚠️ **コメントの趣旨は変えない**（`docs/` を対象に含めるな、という警告が本体）

---

## 4. 検証項目

⚠️ **W2・W3 は実際にファイルを壊して確認し、確認後に必ず復元する**
（`git status --short` が `M scripts/validate.mjs` の1行だけに戻ることまで確認する）。

| # | 内容 | ⭐ 期待値（**設計担当が実測して書いた**） |
|---|---|---|
| **V1** | `npm run validate` | **exit 0**。成功行が ⭐ **`no line-number refs in code comments — 0件（46ファイル走査）`**（⚠️ **45ではなく46**。増える1本は `shared/pdf/card-sheet.js`） |
| **V2** | ⭐ **`shared/pdf/card-sheet.js` のコメントに `zzprobe.js:123` を1行足す** | **exit 1**。⭐ **`shared/pdf/card-sheet.js:<行番号> — zzprobe.js:123` と名指しされる**（＝ #1 が効いている直接の証拠） |
| **V3** | ⚠️ ⭐ **`shared/vendor/jspdf.umd.min.js` に `zzprobe.js:123` を足す** | **exit 0** のまま・**`0件（46ファイル走査）`**（＝ルートが `shared` になっても除外が守っている。⭐ **これは対照実験ではなく本番の条件になった**） |
| **V4** | 冒頭コメントに3件が載ったか | ⭐ **`meta.rarities` / `card-meta-index`（並び替えキー）/ 行番号参照 の3つの記述がある**。⚠️ **「最後に」で始まる文は用語ハイライトのまま1つだけ** |
| **V5** | `grep -c '278件' scripts/validate.mjs` | ⭐ **0**（⚠️ **実施前は 1**） |
| **V6** | ⚠️ **担当パス** | `git -c core.quotepath=false show --name-only --format= <commit>` ＝ ⭐ **`scripts/validate.mjs` の1行だけ** |
| **V7** | 生成物に差分が出ないこと | `git status --short` に `scripts/validate.mjs` 以外が出ない（⚠️ 報告ファイルの `docs/` を除く） |

⚠️ ⭐ **V1 の「46」は設計担当が実際に数えて書いた**
（`find scripts shared functions tools app.js \( -name '*.js' -o -name '*.mjs' \) -not -path 'shared/vendor/*' | wc -l` ＝ **46**）。
**合わなければ、実装ではなく前提が変わっている**ので報告に書くこと。

---

## 5. スコープ外（⚠️ **やらないこと**）

承認記録 §4・§5 で**設計担当が判断済み**。⚠️ **先回りして入れないこと**:

| やらないこと | 判断の理由（正は承認記録） |
|---|---|
| ⚠️ **`tools/` の `.html` / `.css` を対象に足す** | §4-2。⭐ 足しても拾えるのは `tools/` の3ファイルだけで、**ルート直下の `index.html` と生成HTMLは対象外のまま**＝**見かけだけ増えて穴は残る** |
| ⚠️ **「コメント内」に限定する判定を入れる** | §4-3。⭐ **限定すると、生成物を被覆している唯一の経路（生成元のテンプレートリテラル内）を取り逃がす** |
| ⚠️ **`file.js#L231` / `file.js の 110行目` の形を拾うよう正規表現を広げる** | §5（S1-2）。⭐ `#L` 付きパーマリンクは**腐らない場合がある**・日本語表記は**誤検知が出やすい**・**現存0件** |
| ⚠️ **除外の設定ファイル・`disable` コメント機構を作る** | §4-3。**使われないまま腐る**（必要になったら判断を仰ぐ） |

---

## 6. ハンドオフ

- ⭐ **`実装待ち`**（GitHub復旧後は `gh issue edit <n> --add-label 実装待ち`）
- 終えたら**実装報告**を書いて **`レビュー待ち`**（CLAUDE.md の6項目。⚠️ **コミットハッシュ必須**）
- ⚠️ **本番公開の前提条件ではない**（`validate` はサイトの表示を変えない）。⭐ 合格・承認後は **`push待ち` 相当**
- ⚠️ **push・`wrangler pages deploy` は実行しない**
