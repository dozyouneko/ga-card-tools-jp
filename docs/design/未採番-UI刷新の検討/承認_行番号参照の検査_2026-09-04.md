**対象: 「行番号参照の是正と検査」**（→ 継続） — この宣言は 2026-09-11 に遡って追記した（索引の転記ドリフト防止）

# 承認記録 — コード内の行番号参照を止める検査を `validate` に足す（2026-09-04）

⚠️ **GitHubアカウント停止中のため、issueの承認コメントの代わりにこのファイルを置く。**
復旧後に `gh issue comment <n> --body-file` でそのまま投函する
（索引 [`待ち行列と復旧手順.md`](../待ち行列/待ち行列と復旧手順.md) §2-④）。

- 仕様（正）: [`承認_腐った行番号参照_2026-09-04.md`](承認_腐った行番号参照_2026-09-04.md) **§4**（要件1〜5・破壊試験 W1〜W4）
- 実装報告: [`実装報告_行番号参照の検査_2026-09-04.md`](実装報告_行番号参照の検査_2026-09-04.md)
- レビュー判定: [`レビュー判定_行番号参照の検査_2026-09-04.md`](レビュー判定_行番号参照の検査_2026-09-04.md)（**合格・差し戻し0往復・M1〜M7 すべて ✅**）
- コミット: 実装 **`8e2c8782`** / 報告 `70f8546f`・`e407919d` / レビュー `43fc9bbc`
- 呼び名: **「行番号参照の検査」**（記号で呼ばない → 索引 §4-4）

---

## 1. 判定 — ⭐ **承認する（＝このタスクは完了）**

⚠️ ⭐ **承認の可否そのものは、設計担当ではなくユーザーが直接判断した。**

> **ユーザー（2026-09-04）**: 「『行番号参照の検査を足す』のタスクについては完了していますね。
> それであればそのタスクは承認します」

⭐ **経緯**: 本来この工程を担う設計担当セッションが、**資料の読み込み段階でセッション上限に達して中断した**
（書き込みは1文字も行われていない）。⚠️ **承認を保留したままにすると、レビュー合格済みの成果が
記録の無いまま宙に浮く**（CLAUDE.md「記録が無いまま実装だけがコミットされている状態」と同型）。
そこで**ユーザーが直接承認を出し**、⭐ **技術的な判断（§3〜§6）だけを後任の設計担当が引き受けた。**

⚠️ **後任の設計担当は「動いたかどうか」を再確認していない**（それはレビュー担当の担当であり、
判定コメントに証跡がある）。**見たのは「設計の意図を満たしているか」と「判断が要る問い」だけである。**

### 設計担当が独立に測り直したこと（＝設計意図との適合）

| 確認 | 実測（2026-09-04・作業ツリー `43fc9bbc`） |
|---|---|
| 実装の担当パス | ✅ `git -c core.quotepath=false show --name-only --format= 8e2c8782` ＝ **`scripts/validate.mjs` の1ファイルのみ**（+77 / -1） |
| 走査対象での実ヒット数 | ✅ **0件**（承認 §4 と同じ式を設計担当が独立に実行） |
| 検出する形（要件2） | ✅ `card-i18n.js:110` / `gen-tl-json.mjs:72` / `card-detail.js:231-323` は **HIT**、推奨する書き方 `card-i18n.js の bannedFormats()` は **miss** |
| ⚠️ 走査対象の外に残る**手書き**の `.js`/`.mjs` | ⭐ **`shared/pdf/card-sheet.js` の1本だけ**（リポジトリ全体を数え直して確認。§4-1） |

⭐ **仕様（承認 §4）の意図は「覚えておくのではなく構造で防ぐ」ことで、実装はそれを満たしている。**
とくに ⭐ **fail-open を1つも残していない**点（§3-D2）は、**この検査そのものが無言で緑になる型**
（CLAUDE.md「ハイライト検査の無言緑」）を先回りで塞いでおり、**仕様の字面より意図に忠実である。**

---

## 2. レビュー担当の仕事について

⭐ **記録として明記する**: レビュー担当は**実装報告の「✅」を根拠にせず**、
**W1〜W4 を自分で仕込み直して再実行**したうえ、**報告に無い破壊試験を4つ追加した**（R1〜R4）。

- ⭐ **R1（ルート改名）・R2（0ファイル）** は、**逸脱 D2 が無ければ黙って空回りしていた**ことの直接の証拠
- ⭐ **R3（生成物の生成元テンプレート内で検出できる）** は、**§4-3（Q3）の判断の根拠そのもの**になった
- ⭐ **Q1 の範囲を自分で数え直し、「実質の穴は1本だけ」と確定させた**（開発担当の申告は正しかったが、
  **それが全部なのかは書かれていなかった**）

⚠️ **この3つが無ければ、設計担当は §4 の判断を「たぶん1本だけだろう」で出すことになっていた。**

---

## 3. ⭐ 逸脱3点（報告 §5）の追認 — **3点とも追認する**

| # | 逸脱 | 判断 | 根拠 |
|---|---|---|---|
| **D1** | 成功行に**走査ファイル数**を足した（`0件（45ファイル走査）`） | ⭐ **追認** | 承認 §4 要件4の**目的**は「検査が動いていない」と「0件である」の区別。⭐ **ファイル数はその目的を強める**（R1・R2 で実際に数字と exit が動く）。例示の文字列もそのまま含んでいる＝**仕様の字面も満たしている** |
| **D2** | ルートが読めない・**0ファイル**を **exit 1**（fail-closed 化） | ⭐ **追認。むしろ本タスクの核心** | ⚠️ **これが無ければ、ルートを改名した瞬間に検査が黙って空回りする**＝レビュー担当の言うとおり **M5 不合格**だった。⭐ `validate.mjs` の既存2検査（#40・#70）と**同じ方針**でもある |
| **D3** | エラー表示を**先頭20件で打ち切り** | ⭐ **追認** | **総件数は必ず出る**ので情報は欠けない（W2′ の5件は全数出た）。⚠️ 大量ヒット時に**本来のエラーが流れる**ほうが害が大きい |

⚠️ ⭐ **D2 は「仕様に無いことをやった」逸脱だが、仕様に書かなかった設計担当のほうが不足していた。**
承認 §4 は要件3で「失敗時は exit 1」と書いただけで、**検査自身が失敗したときの倒し方を指定していなかった**。
⭐ **今後、検査を1本足す仕様を書くときは「抽出に失敗したときにどちらへ倒すか」を要件に含める**
（CLAUDE.md の fail-open 事例が繰り返し示しているとおり、**指定しないと fail-open に倒れるのが既定**）。

---

## 4. ⭐ 走査範囲の仕様判断（報告 §6 / レビュー §5 の Q1〜Q3）

### 4-1. Q1（`shared/pdf/card-sheet.js`）— ⭐ **広げる**（`"shared/js"` → `"shared"`）

⚠️ **設計担当がリポジトリ全体を数え直して確認した**（レビュー担当の結論と一致）:

| 走査対象外の `.js`/`.mjs`（`node_modules` `tmp` `.git` `.wrangler` `docs/` `data/` `shared/vendor/` を除く） | 素性 | 実質の被覆 |
|---|---|---|
| `cards/cards.js` | **生成物**（`build-card-pages.mjs` の `CARDS_JS`） | ⭐ **被覆されている**（レビュー R3 が実証） |
| `tournaments/tournaments.js` / `tournaments/deck.js` | **生成物**（`build-tournament-pages.mjs` の `LIST_JS` / `DECK_JS`） | ⭐ **被覆されている**（同上） |
| **`shared/pdf/card-sheet.js`** | ⭐ **手書きの自製コード**（9.2KB） | ⚠️ **被覆されていない** |

⭐ **穴は1本だけ。しかも塞ぐのは1語。** 広げる根拠:

1. ⚠️ ⭐ **`card-sheet.js` は「行番号参照が生えやすい側」のファイルである。**
   冒頭に長い説明コメントがあり、**他ファイルへの言及を実際に含んでいる**
   （「印刷PDFツール（`tools/print/`）と、将来のカードDB（ルート）から…共通利用できるようにするための共有モジュール」）。
   ⭐ **「参照を書く習慣のあるファイル」が検査の外にある**のは、本タスクの目的からして放置できない
2. ⭐ **費用が1語**（`LINEREF_ROOTS` の `"shared/js"` → `"shared"`）。
   **`shared/vendor/` を除外リストが守ることは、開発担当（W4-b1/b2）とレビュー担当が
   それぞれ独立に対照実験で実証済み**＝「今は届かないから通っているだけ」ではない
3. ⭐ **現在のヒットは0件**なので、広げても**即座に緑**（実測: `shared/pdf/` のヒット **0件**）
4. ⚠️ **将来 `shared/` の下に他人の配布物が増えたら赤くなる**が、⭐ **それは fail-closed** ＝
   **人が気づいて `LINEREF_EXCLUDE` に足す**。**黙って穴が開く方向ではない**

⚠️ ⭐ **「先回りしない」を理由に据え置かない。** それが妥当なのは**実態を測っていないとき**であって、
本件は **①穴が1本と確定 ②費用が1語 ③緑になることまで実測済み** の3つが揃っている。
**ここで据え置くと「覚えておく」に戻る**（CLAUDE.md が一貫して避けている形）。

### 4-2. Q2（`tools/` の `.html` / `.css`）— ⚠️ **広げない**（現状維持）

| 判断材料 | 実測 |
|---|---|
| `tools/*/index.html` の**インライン `<script>`** | ⭐ **3ファイルとも 0個**（レビュー §5-Q2） |
| 非 `.js`/`.mjs` ファイルの現在のヒット数 | ⭐ **0件**（報告 §6-Q2） |

⭐ **据え置く理由は「今0件だから」ではなく、広げても被覆が中途半端にしかならないから**:

- ⚠️ **ルートは `scripts` `shared` `functions` `app.js` `tools` である。**
  ここに `.html` を足しても、拾えるのは **`tools/` の3ファイルだけ**で、
  ⚠️ ⭐ **ルート直下の `index.html`・生成物の `cards/**/*.html` は依然として対象外のまま**
- ⚠️ ⭐ **「HTMLも見ている」という見かけだけが増えて、実際の穴は残る**＝
  **検査に対する誤った安心を生む**（これは無言の緑と同じくらい悪い）
- ⭐ **本当に必要になる条件ははっきりしている**——**`tools/` のHTMLにインライン `<script>` を書いたとき**。
  ⚠️ **そのときは `.html` を足すのではなく、そのスクリプトを `.js` に切り出すのが先**
  （既存3ツールはすべて外部 `.js` を読む形で統一されている＝**規約はもう存在する**）

### 4-3. Q3（「コメント内」に限定していない）— ⭐ **限定しない（現状維持）。将来も入れない**

⚠️ ⭐ **これは「今は先回りしない」ではなく、**⭐ **入れないほうが正しいと確定した。**

- ⭐ **レビュー R3 が決め手**: `build-card-pages.mjs` の **`CARDS_JS` テンプレートリテラル内**に仕込んだ参照を
  **検査は exit 1 で捕まえた**。⚠️ **コメント判定を入れると、これを取り逃がす**——
  そして ⭐ **生成物（`cards/cards.js` 等）を被覆している唯一の経路がここである**（§4-1 の表）
- ⚠️ コメント判定にはブロックコメント・テンプレートリテラル・正規表現リテラルの解析が要り、
  **実装が重くなるうえ取りこぼしが増える**
- ⭐ **誤検知のリスクは受け入れる**: 現在0件で、⚠️ **将来コード中の文字列に `foo.js:12` の形が
  本当に必要になったら、そのときに判断を仰ぐ**（`// eslint-disable` 相当の除外機構を
  **先回りで作らない**——使われないまま腐る）

⭐ **開発担当・レビュー担当がともに「限定しない」を支持しており、設計担当もこれを正式な仕様とする。**
**承認 §4 要件2は今後も「T2 の grep と同じ形」のままとする。**

---

## 5. レビューの推奨指摘（S）2件の扱い

### S1-1（`validate.mjs` 冒頭の検査一覧に新検査が未記載）— ⭐ **対応する**

⚠️ **ただし新検査だけでなく、⭐ 未記載の既存2検査（`meta.rarities` / `card-meta-index` の並び替えキー）も同時に足す。**

- ⚠️ ⭐ **レビュー担当が「既存も未記載だから S 止まり」と判断したのは正しい**（M1-b の但し書きどおり）。
  ⭐ **だが設計担当としては「既存も効いていない」を放置の理由にしない**——
  **冒頭コメントが検査の一覧として当てにならなくなっている**のが本体の問題で、
  **1件足して3件漏れたままにすると、次に見た人が「一覧は当てにならない」と学習する**
- ⭐ **費用は3行**。⚠️ **順序も直す**（新検査は「最後に…用語ハイライト…」の**前**に挿入されている）

### S1-2（`file.js#L231` / `file.js の 110行目` は拾わない）— ⚠️ **現状維持（広げない）**

| 判断材料 | 実測（設計担当が独立に確認） |
|---|---|
| `card-detail.js#L231` | **miss**（拾わない） |
| `card-i18n.js の 110行目` | **miss**（拾わない） |
| 走査対象での実ヒット | ⭐ **0件**（どちらの形も現存しない） |

- ⚠️ ⭐ **`#L231` の形は「必ず腐る」とは限らない**——GitHubのパーマリンク（コミットSHA付き）は**腐らない**。
  ⭐ **一律に禁じると、腐らない書き方まで書けなくなる**
- ⚠️ **`の 110行目` の形は日本語の自由記述で、正規表現で狙うと誤検知が出やすい**
  （「110行目」は行番号以外の文脈でも書く）
- ⭐ **本タスクが実証したのは `<file>.js:<行番号>` という1つの形が4/4で腐ったこと**であって、
  **腐りうる書き方すべてを禁じたわけではない**。⚠️ **実測された害の範囲を超えて検査を広げない**
- ⭐ **実物が出てきたときに判断する**（そのときは「腐った実例」という根拠が手元にある）

---

## 6. ⚠️ ⭐ `docs/` の件数の食い違いの確定（321 / 278 / 290）

⚠️ **3つの資料が別々の数を書いていた。** 検査は `docs/` を見ないので**結論には影響しない**が、
⭐ **「数え方の違いなのか実数が動いたのか」を確定させておく**（放置すると、次に測った人が4つ目の数を書く）。

### 設計担当が git 上で時点ごとに測り直した

同一のコマンド（`git grep -hoE '[A-Za-z0-9_.-]+\.(js|mjs):[0-9]+(-[0-9]+)?' <commit> -- docs/ | wc -l`）:

| 時点（コミット） | マッチ数（`-o`） | 行数 | その時点の記録 |
|---|---|---|---|
| `855b0e14`（承認記録を書いた時点） | **277** | 245 | ⚠️ 承認記録は **321件**と書いた → ❌ **再現しない** |
| `8e2c8782`（実装した時点） | **278** | 246 | ✅ 実装報告の **278件**と**完全一致** |
| `70f8546f`（レビューが測った時点） | **290** | 254 | ✅ レビュー判定の **290件**と**完全一致** |
| `43fc9bbc`（現在） | **300** | 259 | ⭐ **いまの正しい数** |

### ⭐ 結論 — **「両方」だが、内訳ははっきりしている**

1. ⭐ **実数は動いている。** 278 → 290 → 300 は**同じ数え方で時点が違うだけ**＝
   ⚠️ **この文書群自身が行番号参照を書くので、記録を書くたびに増える**（1日で277→300）
2. ⚠️ ⭐ **承認記録の「321件」だけは、どの数え方でも再現できなかった。**
   拡張子を広げる（338）・任意拡張子（344）・パス区切り込み（277）・行数（245）・
   `js|mjs|ts|jsx|cjs`（277）など、**設計担当が10通りの式を試したが 321 にならなかった**
   （⚠️ **当時の未コミットの作業ツリーは復元できないので、そこに差があった可能性だけは残る**）。
   ⭐ **測定の誤り（別のコマンド・別のスコープ）と判断し、記録として訂正する**
3. ⭐ **正は「2026-09-04 現在 300件（`grep -o` のマッチ数）／行数では259」**。
   ⚠️ **ただしこの数はもう固定値として書く意味が無い**（毎日増える）

### ⚠️ ⭐ 波及: `validate.mjs` のコメントに書いた「278件」も、同じ理由で腐る

⚠️ ⭐ **本タスクは「コードに書いた数字は腐る」と実証したタスクである。**
その検査自身のコメントに **`（2026-09-04 実測で278件ある）`** という**日々ずれる実測値**が書いてある。

- ⭐ **行番号参照ほど有害ではない**（指し先を誤らせるわけではなく、桁を伝える役割）が、
  ⚠️ **「300件になっているのに278件と書いてある」状態は、この検査の説得力そのものを削る**
- ⭐ **`約300件（日々増える）` のように「桁」と「増えること」を書く形へ直す**（→ §7 の追加指示 #3）

---

## 7. 派生issueの要否・残作業

⭐ **派生issue（起票案）は不要。** ⚠️ **代わりに、小さな追加指示を1件立てる**
（承認 §4 と同じ理由——**起票案を書いて索引に載せて投函前に再測定するほうが、直すより高くつく**）。

| 追加指示の内容 | 由来 |
|---|---|
| **#1** `LINEREF_ROOTS` の `"shared/js"` を `"shared"` に広げる | §4-1（Q1） |
| **#2** 冒頭コメントの検査一覧に**3件**（新検査＋既存2件）を追記する | §5（S1-1） |
| **#3** コメント中の「278件」を、腐らない書き方（`約300件・日々増える`）に直す | §6 |

→ [`追加指示_行番号参照の検査の仕上げ_2026-09-04.md`](追加指示_行番号参照の検査の仕上げ_2026-09-04.md)（**`実装待ち`**）

⚠️ ⭐ **本タスク（行番号参照の検査）そのものは、この承認をもって完了とする。**
**追加指示は別タスク**であって、承認を条件付きに戻すものではない
（⭐ **ユーザーの承認は「いまの実装で完了」と読む**）。

### ハンドオフ

- **本タスク**: ⭐ **完了**。⚠️ **未pushのまま**＝ **`push待ち` 相当**
  （⭐ **本番公開の前提条件ではない**——`scripts/` は配信されるが**サイトの表示を変えない**）
- **追加指示**: ⭐ **`実装待ち`**（→ 索引 §5）
- ⚠️ **push・`wrangler pages deploy` は実行していない**（公開の可否は常にユーザーが判断する）
