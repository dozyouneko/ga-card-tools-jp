**対象: 「行番号参照の是正と検査」**（→ 完了） — この宣言は 2026-09-11 に遡って追記した（索引の転記ドリフト防止）

# 承認記録 — `shared/vendor` 除外コメントの**断定を外す**（2026-09-05）

⚠️ **GitHubアカウント停止中のため、issueの承認コメントの代わりにこのファイルを置く。**
復旧後に `gh issue comment <n> --body-file` でそのまま投函する
（索引 [`待ち行列と復旧手順.md`](../待ち行列/待ち行列と復旧手順.md) §2-④）。

- 呼び名: **「vendor除外コメントの断定」**（記号で呼ばない → 索引 §4-4）
- 指示（正）: [`追加指示_vendor除外コメントの断定_2026-09-05.md`](追加指示_vendor除外コメントの断定_2026-09-05.md)
- 由来: [`承認_行番号参照の検査の仕上げ_2026-09-05.md`](承認_行番号参照の検査の仕上げ_2026-09-05.md) **§4**（レビューの S1-A）
- 実装報告: [`実装報告_vendor除外コメントの断定_2026-09-05.md`](実装報告_vendor除外コメントの断定_2026-09-05.md)
- レビュー判定: [`レビュー判定_vendor除外コメントの断定_2026-09-05.md`](レビュー判定_vendor除外コメントの断定_2026-09-05.md)（**合格・差し戻し0往復**・M1〜M7 全✅・**S の指摘なし**）
- コミット: 実装 **`ee97c6c7`** / 報告 `d144de6d` / レビュー `4ebfd437`

---

## 1. 判定 — ⭐ **承認する（＝このタスクは完了）**

⚠️ **設計担当は「動いたかどうか」を再確認していない**（それはレビュー担当の担当で、判定コメントに証跡がある）。
**見たのは「設計の意図を満たしているか」と「判断が要る問い」だけである。**

### 設計担当が独立に測り直したこと

| 確認 | 実測（2026-09-05・作業ツリー `4ebfd437`） |
|---|---|
| 担当パス | ✅ `git -c core.quotepath=false show --name-only --format= ee97c6c7` ＝ **`scripts/validate.mjs` の1ファイルのみ**（+3 / -3） |
| ⭐ **文言が指示 §2 と一致するか** | ✅ 指示書のコードブロックを**機械的に切り出して照合**——**変更後ブロックは `in src` ＝ True / 変更前ブロックは False**（＝**書き写しのゆらぎが1文字も無い**） |
| ロジック不変 | ✅ 差分のハンクは **`@@ -591,9 +591,9 @@` の1つだけ**。`LINEREF_ROOTS` / `LINEREF_EXCLUDE` / `REF_RE` / `walk()` / fail-closed のいずれも差分に出ていない |
| 現在の状態 | ✅ `npm run validate` **exit 0**・`no line-number refs in code comments — 0件（46ファイル走査）`・`all checks passed.`・**成功行10本**・`git status --short` **空** |

### ⭐ 意図は満たされている

指示 §1 が狙ったのは「**この警告の唯一の読者＝消そうか迷っている人が、試した瞬間に
『嘘だから消してよい』と誤った確信を得る経路を断つ**」ことだった。
⭐ **新しい3行には、その場で反証できる主張が1つも無い**——レビュー担当が**能動的に3主張を崩しに行き**、
いずれも1コマンドでは崩せなかった（判定 §3）。⭐ **設計の意図はそのまま実現している。**

---

## 2. 逸脱 — ⭐ **無し**

報告 §5 の「逸脱なし」を追認する。指示 §3 のスコープ外**5点**とも先回りが無いことは、
レビュー担当が**コメント行を除去して親コミットと `diff` し、結果が空である**ことまで確かめている（判定 §4）。

⭐ **前タスクの N1（折り返しの組み直し）と同型の判断が今回は発生しなかったのは正しい**——
指示 §2 が**3行のままの完成形**を与えており、折り返しを実装側が決める余地が無いように書いてあるため。

---

## 3. ⭐ 参考1（主張②の成立条件）— **据え置く。派生タスクにしない**

レビュー担当の観測: 「成功行のファイル数が動く」は **`shared/vendor/` に走査対象（`.js`/`.mjs`）が
1つ以上あること**に依存する（現在 `jspdf.umd.min.js` の**1ファイル**）。

### ⭐ 設計担当の判断 — **レビュー担当と同じ結論。ただし根拠をもう一段強くできる**

⚠️ **これは「条件付きで成立する主張」ではなく、⭐ 同値である。**
`excluded()` は `walk()` の入口で枝を刈るだけなので、次が厳密に成り立つ:

> ⭐ **主張②が偽になる（除外を外してもファイル数が動かない）** ⟺
> **`shared/vendor/` 配下に `.js`/`.mjs` が1つも無い** ⟺ ⭐ **この除外は何も刈っていない＝存在意義が無い**

⭐ **つまり、この文言は「効いている限り必ず真」であり、偽になるのは*除外そのものが不要になった時*だけである。**
⚠️ **腐る余地が無い**ので、⭐ **文言としての問題ではない**（レビュー担当の判断は正しい）。

- ⭐ **`shared/vendor/` が消えても壊れない**ことも確認した——`walk()` は `excluded()` で
  **`statSync` の前に return する**ため、**除外先が存在しなくてもエラーにならない**
- ⚠️ 補足: `shared/vendor/` に `.css` や `.wasm` しか無い状態でも同じ（**除外は no-op**）

**→ 派生タスクにしない。追記も不要。** ⭐ **記録として本節を残すに留める。**

---

## 4. ⚠️ ⭐ 参考2（`LINEREF_EXCLUDE` の削除を検出する自動検査が無い）— **入れない。ただし引き金に載せる**

開発担当 P2・レビュー担当 参考2 の観測は**正しい**。
⭐ **`LINEREF_EXCLUDE` から `shared/vendor` が消えたことを検出する自動検査は、いまも無い。**

### 4-1. ⭐ まず、消えたときに何が起きるのかを確定させた（**推測で書かない**）

⚠️ **「無言で通る」という報告の書き方は、正確には**次の2通りに分かれる:

| 消したあと | 何が起きるか | ⭐ 性質 |
|---|---|---|
| **配布物に `<file>.js:<行番号>` が無い**（＝今日） | 走査が **46→47** に増えるだけ。**exit 0**。検査の結論は**1件も変わらない** | ⭐ **無害**（成功行の数字が動くので**痕跡は残る**） |
| **配布物に `<file>.js:<行番号>` がある**（jspdf を更新した日に起こりうる） | **exit 1**。⭐ **エラー本文が `shared/vendor/jspdf.umd.min.js:NNN — …` と場所を名指しする**うえ、末尾の案内行が ⭐ **「`docs/` と `shared/vendor/` は対象外です（…配布物は直せない）」**と出る | ⭐ **fail-closed かつ自己診断的** |

⚠️ ⭐ **どちらも fail-open ではない。** 後者は**赤くなった人の画面に、直し方（除外に戻す）がそのまま印字される**。
⭐ **これが「守っているのはコメントだけ」という表現との違い**である——
**コメント以外に、失敗時の案内行そのものが2つ目の道しるべになっている**（`scripts/validate.mjs` の `console.error` の最終行）。

### 4-2. ⚠️ ⭐ そもそも、この検査は**書けない**（書いても常に通る）

⭐ **`LINEREF_EXCLUDE` が `shared/vendor` を含むことを `validate` 自身に検査させると、
両辺が同じファイルの中に並ぶ**——**削除する人はその2箇所を1回の編集で消す**。

⚠️ **これは CLAUDE.md が用語ハイライト検査について明記している
「両辺が同源になり常に通る」型と完全に同じ**である。⭐ **足しても守れない検査を足さない。**

### 4-3. ⭐ 前タスクの判断との整合 — **同じ判断・同じ引き金に載せる**

⚠️ **前タスク（[`承認_行番号参照の検査の仕上げ_2026-09-05.md`](承認_行番号参照の検査の仕上げ_2026-09-05.md) §3）で
「#70 同型の集合一致検査は入れない」と決めたばかりである。** 本件も**同じ判断にそろえる**:

| 前タスク §3 の理由 | 本件に当てはまるか |
|---|---|
| ① 守る対象の変更頻度が桁違いに低い | ⭕ **当てはまる**。`LINEREF_EXCLUDE` は**導入以来1度も動いていない**（ずれ **0回**。`LINEREF_ROOTS` ですら1回） |
| ② 機構自身に既知の footgun がある（#70 のマーカー） | ⭕ **当てはまる**。⚠️ **加えて本件は 4-2 のとおり「書いても常に通る」** |
| ③ 「正の所在」の明記で実害が消える | ⭕ **当てはまる**。`CLAUDE.md` に **正は `LINEREF_ROOTS` / `LINEREF_EXCLUDE`**（＝**コード側が勝つ**）と**既に明記済み**——⭐ **`LINEREF_EXCLUDE` も名指しで入っている** |

⚠️ ⭐ **ただし据え置きの理由は「まだ壊れていないから」ではない。引き金を先に決めておく**
（前タスク §3 で決めた引き金を、⭐ **`LINEREF_EXCLUDE` にも及ぶよう明示的に広げる**）:

> ⭐ **`LINEREF_ROOTS` *または* `LINEREF_EXCLUDE` と、それを書き写した文書（`CLAUDE.md`・
> `validate` の失敗時の案内行）が **2回目** のずれを起こしたら、そのときは検査を入れる。**
> ⚠️ **「同じ失敗を2回繰り返したら止まる」（CLAUDE.md の自律運用のストップ条件）と同じ基準にそろえる。**
> ⭐ **現在の回数: `LINEREF_ROOTS` = 1回（2026-09-05 に是正済み）/ `LINEREF_EXCLUDE` = 0回。**

⭐ **前タスクの引き金が `LINEREF_ROOTS` だけを名指ししていたのは、本件の観測より前だったからである。**
⚠️ **広げないと、引き金が「気にしている対象」より狭いまま据え置かれる**（＝引き金を決めた意味が薄れる）。

**→ 派生タスクにしない。追加指示も出さない。** ⭐ **本節が引き金の記録である。**

---

## 5. ⚠️ ⭐ 索引の是正 — **今度は §2 が腐っていた（同じ型の3件目）**

⚠️ **前タスクの承認で「§5 には自動検査が無いので黙って腐る」と記録したばかりだったので、
今回は §5 を更新する前に §1・§2・§4・§5 を突き合わせた。** ⭐ **§5 は無事だったが、§2 に3件の腐りが見つかった。**

| 箇所 | 索引の記載 | 実際 | 影響 |
|---|---|---|---|
| §2-② の見出し「**派生14件**」 | 14件 | ⭐ **13件**（§1 の「🟢 生きている」＝ `validate` が数えている実数） | ⚠️ **復旧後に起票する件数が1件多い** |
| §2-② の見出し「**完了済み11件**」 | 11件 | ⭐ **13件**（⚠️ **2026-09-04〜09-05 に完了した5タスクが1つも載っていない**） | ⚠️ ⭐ **復旧後に「起票されないまま消える完了タスク」が出る** |
| §2-④ の投稿テーブル | ⑪ まで | ⭐ **⑫⑬ の記録が抜けている**（日本語モードの0件表示・行番号参照の一連） | ⚠️ **工程記録が投函されない** |

### ⭐ 完了済みの数え直し（11 → 13）

⭐ **2026-09-04 以降に承認まで終わった5タスクを、issue単位に畳んで数えた**:

| 追加する行 | 中身 | ⭐ 畳み方 |
|---|---|---|
| **⑫ 日本語モードの0件表示** | 承認 2026-09-04（`5ebf817c`） | ⭐ **単独で1件**（症状も設計書も独立している） |
| **⑬ 腐った行番号参照の是正と、再発を止める検査** | 腐った行番号参照の是正 → 行番号参照の検査 → 仕上げ → **vendor除外コメントの断定**（本件） | ⭐ **4タスクを1 issue に畳む**。⚠️ **後ろの3つはすべて前の承認から派生した「追加指示」**で、**単独では文脈が読めない**——⭐ **1本のissueに工程順のコメントとして並べるのが読み手に親切** |

⚠️ ⭐ **畳んだ結果、§2-④ の投稿順が本件の要である**（4タスク分・11通の記録を時系列に並べる）。

### ⭐ なぜ §2 が腐ったのか（＝**§5 と同じ構造**）

> ✅ **`npm run validate` の起票案検査が見ているのは §1 だけ。**
> ⚠️ ⭐ **§2 にも §5 にも自動検査は無い。**
> ⭐ **だから §1（＝`validate` が見る側）だけが揃い、§2・§5 は黙って腐る。**

⚠️ ⭐ **同じ型の観測は、これで **4件目** である**（①索引 §5・② `CLAUDE.md` の走査ルート＝いずれも 2026-09-05・
③ 本件の §2-②の件数・④ 本件の §2-④の抜け）。⭐ **「検査のある側だけが揃う」は偶然ではなく法則として扱う。**

### ⚠️ §2 への自動検査も、いまは入れない（**判断は前タスク §6 のまま**）

⭐ **理由は前タスク §6 と同一**——**§2・§5 の完了根拠は承認記録の有無**であり、
⚠️ ⭐ **承認記録のファイル名がまだ規約化されていない**（`承認_2026-09-04.md` /
`承認_行番号参照の検査_2026-09-04.md` と揺れている）。**先に規約、あとで検査**。

⭐ **それまでの手当ては運用を1段強くする**（前タスクの「§5 を見る」を **§2 と §5** に広げる）:

> ⚠️ ⭐ **承認記録を書いたら、同じコミットで §5 の行を完了へ移し、あわせて §2-A（起票リスト）と
> §2-④（投稿順）にも行を足す。** ⭐ **「§1 を直したついでに」では、検査の無い側から順に漏れる。**

→ 索引 §4-3-1 に **4-3-1-a** として追記した。

### ⭐ あわせて §5 の「前を向いたままの注記」も2件直した

⚠️ **完了行の中に残った、もう解決済みの前方参照**（＝**行そのものは完了なので目を素通りする**）:

| 行 | 旧 | 新 |
|---|---|---|
| 版レベルの意味統一 | 「軽微な残作業1件（S1-1）＝ `実装待ち`」 | ⭐ **完了済み**（`559965f0`・第12版で既に完了していた） |
| 行番号参照の検査の仕上げ | 「残りが1件 `実装待ち`（次行）」 | ⭐ **残り1件（本件）も完了**（次行） |

⚠️ ⭐ **これも同じ型である**——**完了マークが付いた行の中身は、誰も読み返さない。**

---

## 6. 派生タスクの要否 — ⭐ **不要（起票案も追加指示も出さない）**

| 検討したもの | 判断 |
|---|---|
| **参考1**（主張②の成立条件） | ⭐ **据え置き**。⚠️ **同値なので腐る余地が無い**（§3） |
| **参考2**（`LINEREF_EXCLUDE` 削除の検出） | ⭐ **据え置き**。⚠️ **書いても常に通る検査**であり、失敗時の案内行が自己診断的（§4）。⭐ **引き金は「2回目のずれ」に載せた** |
| 索引 §2・§5 の自動検査 | ⚠️ **入れない。先に承認記録のファイル名規約を固める**（§5・前タスク §6 のまま） |
| 指示 §3 のスコープ外5点 | ⚠️ **すべて前タスクの判断のまま。実装にも入っていないことをレビューが実測済み** |

⭐ **この一連（腐った行番号参照 → 検査 → 仕上げ → 本件）はここで完了する。**

---

## 7. ハンドオフ

- **本タスク（vendor除外コメントの断定）**: ⭐ **完了**。⚠️ **未pushのまま**＝ **`push待ち` 相当**
  （⭐ **本番公開の前提条件ではない**——`scripts/` は配信されるが **`validate` はサイトの表示を変えない**）
- ⭐ **`実装待ち` は 0件になった**（→ 索引 §5「次にやること」）
- ⚠️ **push・`wrangler pages deploy` は実行していない**（公開の可否は常にユーザーが判断する）
