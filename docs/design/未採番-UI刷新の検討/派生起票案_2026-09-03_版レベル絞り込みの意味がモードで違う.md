**状態: 生きている**（最終確認 2026-09-03）— 未起票。⭐ **実測ずみ・着手できる**。GitHub復旧後に `gh issue create`（`bug` / `着手可`。⚠️ `小タスク` を付けない） ／ 索引: [`待ち行列と復旧手順.md`](../待ち行列/待ち行列と復旧手順.md)

# 派生issueの起票案 — エキスパンション×レアリティの意味が、英語モードと日本語モードで違う

⚠️ **これは設計書ではなく「issueの下書き」。** GitHubアカウント停止中で `gh issue create` が打てないため、本文をここに用意する。

- 出所: [`レアリティ絞り込み_設計.md`](レアリティ絞り込み_設計.md) **§8**（v1.3 で訂正した内容）
- ⭐ **実装担当が発見し、設計担当が実測で確認した**（2026-09-03）

> ## ⚠️ 置き場の注意（起票時にやること）
>
> ⭐ **起票したら本ファイルを該当issueの設計フォルダへ移し、索引 §1 のリンク修正を同じコミットで行う**
> （分けると `npm run validate` が `索引に載っていない起票案` で exit 1 になる）。

---

## 1. 症状

**エキスパンションとレアリティを同時に絞ると、英語モードと日本語モードで結果が違う。**

| モード | 判定するのは | 「PTM ∧ CSR」の意味 |
|---|---|---|
| **英語モード** | 公式API | ⭐ **同じ版に両方**＝「**PTM版であって、その版がCSR**」 |
| **日本語モード** | 客側の `matchesActiveFilters()` | ⚠️ **項目ごとに独立**＝「PTM版を持ち、**かつ（別の版で）CSRでもある**」 |

⚠️ **同じ条件を入れても、日本語で検索したか英語で検索したかで件数が変わる。**

## 2. 実測（2026-09-03・公式API）

| クエリ | 件数 |
|---|---|
| `name=Alice` | 8 |
| `name=Alice&prefix=PTM` | 3 |
| `name=Alice&rarity=7` | 5 |
| **`name=Alice&prefix=PTM&rarity=7`** | ⭐ **0** |
| `name=Alice&prefix=PTM 1st&rarity=7` | 3 |

⭐ `アリス、歪みの女王` は **PTM/r4** と **PTM 1st/r7** を持つ。
**`PTM` かつ `CSR` の版は存在しない**ので、APIは 0 件を返す。
⚠️ 一方、日本語モードの客側フィルタは「PTM版がある」と「CSRの版がある」を**別々に**見るため、**1件返す**。

## 3. ⚠️ これは「レアリティ絞り込み」が作った不整合

⭐ **レアリティ以前は、版レベルの絞り込みがエキスパンション1つだけだったので衝突しようがなかった。**
2つ目（レアリティ）が入って初めて「同じ版で束ねるのか、独立に見るのか」が問題になった。

## 4. ⚠️ 直すときの難しさ（＝`小タスク` にしない理由）

「同じ版がすべての版レベル条件を満たすこと」に揃えるのが素直に見えるが、**そう単純ではない**:

- ⚠️ ⭐ **AND指定のレアリティ**（「**Cでも SRでも刷られている**」）は、**同じ版では原理的に成立しない**。
  ⭐ **AND は「複数の版にまたがる」ことを意味する**ので、**同じ版で束ねると必ず0件**になる
- したがって **「どの条件を同じ版で束ね、どの条件を独立に見るか」の設計から必要**になる
- ⚠️ 変更するのは `shared/js/card-search.js` の `matchesActiveFilters()`＝**デッキ構築ツールも通る経路**

## 5. どちらが「正しい」か

⭐ **英語モード（同じ版）のほうが直感に近い**。「PTMのCSR」と言われて、
「PTM版を持ち、かつ別の版がCSR」を期待する人は少ない。
⚠️ **ただし AND指定のレアリティは独立でなければ意味を成さない**ので、**単純な統一はできない**。

## 6. ⚠️ 実害の大きさ（＝公開を止めなかった理由）

- ⭐ **どちらのモードでも結果は妥当な集合**で、**取りこぼしでも誤表示でもない**
- ⭐ 発生するのは **エキスパンションとレアリティを同時に指定したとき**だけ
- → **2026-09-03 の公開は、この件を残したまま行う判断をした**（設計書 §8）

## 7. 検証項目（素案）

⚠️ **正式な検証項目は設計書で起こす。**

| # | 検証内容 | 期待値 |
|---|---|---|
| **M-V1** | 英語 `Alice` × PTM × CSR | ⭐ **英語モードと日本語モードで同じ集合**になる |
| **M-V2** | レアリティ **AND** 指定（C ∧ SR） | ⚠️ **0件にならない**（「複数の版にまたがる」意味が保たれる） |
| **M-V3** | エキスパンション単独・レアリティ単独 | **現状と変わらない** |
| **M-V4** | `tools/deck-builder/` | ⚠️ **絞り込みの結果が変わらない**（または変更が意図どおり） |
| **M-V5** | `npm run validate` | exit 0 |

## 8. ⚠️ ほかのissueとの関係

- [`起票案_2026-09-03_レアリティ絞り込み.md`](起票案_2026-09-03_レアリティ絞り込み.md) の**派生**（＝あちらが原因を作った）
- ⚠️ [`起票案_2026-08-30_ブラウザ側の代表画像.md`](../未採番-版順序の非決定性/起票案_2026-08-30_ブラウザ側の代表画像.md) とは**別物**
  （あちらは絞り込みが無いときの既定の絵柄の話）
