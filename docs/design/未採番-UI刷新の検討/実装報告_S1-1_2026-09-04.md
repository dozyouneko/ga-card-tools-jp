**対象: 「版レベルの意味統一」** — この宣言は 2026-09-11 に遡って追記した（索引の転記ドリフト防止）

# 実装報告 — S1-1 コメント参照を行番号から関数名へ（2026-09-04）

- 対象: レビュー推奨指摘 **S1-1**（[`レビュー判定_意味統一_2026-09-03.md`](レビュー判定_意味統一_2026-09-03.md) §5）
- 設計担当の指示: [`承認_意味統一_2026-09-04.md`](承認_意味統一_2026-09-04.md) **§8**（承認済みの軽微な残作業）
- 担当: 開発（実装）担当セッション
- ⚠️ **GitHubアカウント停止中で `gh` が403のため、issueコメントの代わりにこのファイルが報告書**

---

## 0. 結論

⭐ **完了。コメントのみの変更で、実行行は0行。生成物への影響も無い。**

⚠️ **push・`wrangler pages deploy` はしていない**（本番公開はユーザーの判断）。
作業ツリーはコミット後にクリーン。

---

## 1. 実施内容

### コミット

| コミット | 内容 |
|---|---|
| ⭐ **`559965f0`** | 未採番(意味統一) S1-1: 索引生成スクリプトのコメント参照を行番号から関数名へ |

- **変更ファイルは1つだけ**（`git -c core.quotepath=false show --name-only --format= 559965f0`）:

  ```text
  scripts/gen-card-meta-index.mjs
  ```

- **push: なし**

### 変更の中身

⭐ 設計担当の指示どおり **行番号を新しい値に直すのではなく、関数名参照に書き換えた**。

| 行 | 変更前 | 変更後 |
|---|---|---|
| `:51` | `matchesActiveFilters（card-search.js:432）が参照する…` | `matchesActiveFilters()（shared/js/card-search.js）が参照する…` |
| `:55` | `読み手（card-search.js:403-415）は includes() の…` | `読み手（shared/js/card-search.js の metaMatches() → matchesMulti()）は includes() の…` |

- 併せて **再発防止の規約を1行**残した:
  `⚠ 参照先は行番号ではなく関数名で書く（行番号は無関係なコミットで黙って腐る）。`
- ⭐ **`:55` の書き換え先は、当時その行番号が実際に指していた関数を git で特定して決めた**
  （推測で埋めていない）。`git log -S "card-search.js:403-415"` → `497c92dc` の時点の
  `card-search.js:403-415` は **`metaMatches()` の本体**（MULTI ループ＋format＋prefix 判定）だった。
  実際に `includes()` の集合判定をしているのは `metaMatches()` と、そこから呼ばれる `matchesMulti()` なので
  **2つを矢印でつないだ**

---

## 2. 検証結果

⚠️ **本タスクは設計書の「検証項目」を持たない**（設計書 `版レベル絞り込みの意味統一_設計.md` §7 の
M-V1〜M-V10 は承認済みで、本コミットはその対象コードを1行も触っていない）。
したがって**指摘 S1-1 に対する検証**として次を実施した。

| # | 検証項目 | 方法 | 結果 |
|---|---|---|---|
| 1 | 書き換え後の**参照先が実在する** | `grep -n "function matchesActiveFilters\|function metaMatches\|function matchesMulti" shared/js/card-search.js` | ✅ 3つとも実在（`566` / `648` / `849`）。⭐ **新しい腐った参照を作っていない** |
| 2 | ⭐ **実行行を変えていない** | `git diff -U0` の変更行から `//` 行を除いた件数 | ✅ **0件** |
| 3 | 構文が壊れていない | `node --check scripts/gen-card-meta-index.mjs` | ✅ exit 0 |
| 4 | `npm run validate` が exit 0 | 実行 | ✅ **exit 0**（`all checks passed.`） |
| 5 | 担当パス外を触っていない | `git status --short` / `git -c core.quotepath=false show --name-only` | ✅ `scripts/` の1ファイルのみ |

`npm run validate` の完走サマリ（抜粋・すべて既存値のまま）:

```text
loaded OK — total cards: 2516
generated JSON up to date — data/tl-names.json / data/tl-effects.json
meta.sets covers all sets — 63件
meta.subtypes covers all subtypes — 156種
meta.rarities covers all rarities — 9種（スナップショット 2495枚から観測）
card-meta-index sort keys OK — 2516slug / level 133 / power 1006 / life 858 / cost_memory 515 / flip 22
cron git add paths in sync — 8パス（README.md / CLAUDE.md）
issue drafts in sync — 起票案17件（生きている9 / 完了7 / 取り下げ1）
card term highlights up to date — 2495 ページ / 2517 節

all checks passed.
```

---

## 3. 実行できなかった検証と代替手段

### ⚠️ `node scripts/gen-card-meta-index.mjs` は**意図的に実行していない**

⭐ **「回さなかった」のは手抜きではなく、回すほうが有害だと判断したため**:

1. 同スクリプトは**ネットワーク必須**で、スナップショット未収録のフリップ面22件を
   `/cards/:slug` で個別取得する。取得に失敗したslugは**索引から除外して続行する**（exit 0）ので、
   ⚠️ **失敗すると「静かに欠けた索引」で `data/card-meta-index.json` を上書きしてしまう**
2. 手元のスナップショットは **2026-08-31 取得**で、`GA_SNAPSHOT_MAX_AGE_MIN=0` で固定して回しても
   ⚠️ **APIドリフト由来の差分が生成物に混ざる**（CLAUDE.md「手動実行すると、タスクと無関係な
   差分が混ざり得る」）。**コメント1行の修正のコミットに混ぜてよい差分ではない**

**代替**: 下の §4 のとおり、**出力に影響し得ないことを構造で示した**（実行より強い根拠になる）。

### 残っている確認

- ⭐ **無し。** 本コミットは実行行を持たないため、Playwright実駆動・本番curlのいずれも対象が無い

---

## 4. ⭐ 生成物への影響（自分で判断した結果）

**結論: 影響は無い。** 根拠は次の3点で、いずれも実測:

| # | 根拠 | 確認方法 |
|---|---|---|
| 1 | 変更行が**すべてコメント**（`//` 行）で、**実行行の差分は0件** | `git diff -U0 … \| grep -vE '^\s*//' \| wc -l` → **0** |
| 2 | このスクリプトは**自分自身のソースを読まない**。`import.meta.url` の用途は `ROOT` のパス解決1箇所だけで、`readFileSync` は存在しない | `grep -n "readFileSync\|import.meta.url"` → 41行目の1件のみ |
| 3 | 出力 `data/card-meta-index.json` は**公式APIの応答から組み立てるJSON**で、ソースのコメント文字列を一切埋め込まない（`writeFileSync` に渡すのは `metaOf()` の戻り値から作った構造体のみ） | ソース確認 |

⭐ **①だけでも十分だが、②③を併せると「コメントが出力に回り込む経路が無い」ことまで言える。**
（生成物を再生成せずに「影響なし」と書くときは、この形の根拠が要ると判断した）

---

## 5. 設計書からの逸脱と根拠

**逸脱なし。** 指示（承認 §8）は「`:51` `:55` の参照を行番号ではなく関数名に書き換える」で、
そのとおり実施した。⭐ **指示の中の「⚠️ 行番号を新しい値に直すだけにしない」も守っている**。

### ⚠️ 指示に無い1行を足した点（自己申告）

再発防止のコメント `⚠ 参照先は行番号ではなく関数名で書く（…）` を1行足した。
S1-1 の指摘文が **「⭐ 行番号ではなく関数名で参照すると再発しない」** と再発防止を明記しているため
その趣旨の範囲内と判断したが、**厳密には指示に無い追加**なので記録に残す。
⚠️ **不要ならこの1行だけ落とせる**（他の変更から独立している）。

---

## 6. 設計担当への確認事項

### 6-1 ⚠️ **同じ型の腐った参照が、同じファイルにあと2件ある**（スコープ外につき触っていない）

S1-1 が名指ししたのは `:51` `:55` の2件だが、**同一ファイルを `grep -nE "\.(js\|mjs):[0-9]+"` すると
4件**あり、⚠️ **残る2件も実測で既にずれている**:

| 行 | 現在のコメント | ⚠️ 実際にその行にあるもの | 本来指したいはず |
|---|---|---|---|
| `:45` | `bannedFormats() = limit0判定（card-i18n.js:110 と同義）` | `shared/js/card-i18n.js:110` は **`loadEffects()` 内の `fetchJson(url).then(...)`** | `card-i18n.js` の `bannedFormats()` |
| `:116` | `消費側が fail-open するので安全（card-search.js:396 の `if (!entry) return true;`）` | `shared/js/card-search.js:396` は **`MULTI` の説明コメント**（`if (!entry) return true;` は **`:650`**） | `card-search.js` の `metaMatches()` |

- ⚠️ **今回は直していない。** ユーザーからの指示が「**スコープはこの1件だけ・ついでの整理をしない**」で、
  S1-1 の「該当」も `:51` `:55` に限定されているため
- ⭐ **判断を仰ぎたいこと**: この2件を **(a) 同じ扱いで直す**か、**(b) 起票案にして待ち行列へ積む**か、
  **(c) 放置する**か。⚠️ **開発担当では決めない**（`:116` は #29 のレビュー指摘に由来する
  経緯付きのコメントなので、書き換えの是非は設計判断だと考えた）
- ⚠️ **横展開すると `scripts/` 全体・`shared/js/` 全体に広がる可能性がある**（本ファイル以外は未調査）。
  範囲を決めるのも設計担当の判断

---

## 7. ハンドオフ

- ⭐ **`push待ち` 相当**。承認 §8 が **「終えた開発担当が `push待ち` を付けてよい」**（CLAUDE.md の
  ハンドオフ例外）としているため、本件は**レビューに戻さず公開待ちに置く**のが指示どおりの扱い
  （⚠️ GitHub停止中でラベル操作そのものはできないので、**この記述が代替**）
- ⚠️ **本コミットは公開の前提条件ではない**（承認 §8 に明記）。ただし
  **未pushのまま溜まっている列に1件加わった**ので、次の公開（経路②）の対象には入る
