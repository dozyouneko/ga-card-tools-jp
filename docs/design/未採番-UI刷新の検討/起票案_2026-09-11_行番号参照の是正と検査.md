**状態: 完了**（最終確認 2026-09-11）— 呼び名「行番号参照の是正と検査」。⭐ **起票案を持たずに追加指示から始まった完了タスク**（4タスクを1 issue に畳む）に、2026-09-11 に後から付けた起票案（「1タスク＝起票案1枚」の規約。→ [`索引の転記ドリフト防止_設計.md`](../未採番-索引の転記ドリフト防止/索引の転記ドリフト防止_設計.md) の D1）。完了の根拠は [`承認_vendor除外コメントの断定_2026-09-05.md`](承認_vendor除外コメントの断定_2026-09-05.md)。GitHub復旧後は「完了済みタスク」として起票する（⚠️ **`実装待ち` を付けない**）／ 索引: [`待ち行列と復旧手順.md`](../待ち行列/待ち行列と復旧手順.md)

# issueの起票案（完了済みタスク）— コメント内の腐った行番号参照を是正し、再発を `validate` で止める

⚠️ **これは設計書ではなく「issueの下書き」。** 実装・レビュー・承認はすべて済んでいる。
GitHub復旧後、**完了済みタスクの記録として**起票する。

- ⭐ **起票時**: タイトル「コメント内の腐った行番号参照を是正し、再発を `validate` で止める」／
  ラベル `bug` `小タスク` `着手可` → 起票後は `push待ち`（索引 §0 の push が済んでいればクローズ）
- ⚠️ ⭐ **4タスクを1 issue に畳む**: ①是正（腐った行番号参照）→ ②検査（行番号参照の検査）→
  ③仕上げ（行番号参照の検査の仕上げ）→ ④vendor除外コメントの断定。
  **後ろの3つは前の承認から派生した追加指示**で、単独では文脈が読めないため
- 仕様の正は各追加指示と承認記録（設計書は無い）:
  [`追加指示_腐った行番号参照_2026-09-04.md`](追加指示_腐った行番号参照_2026-09-04.md) /
  [`承認_腐った行番号参照_2026-09-04.md`](承認_腐った行番号参照_2026-09-04.md) §4 /
  [`追加指示_行番号参照の検査の仕上げ_2026-09-04.md`](追加指示_行番号参照の検査の仕上げ_2026-09-04.md) /
  [`追加指示_vendor除外コメントの断定_2026-09-05.md`](追加指示_vendor除外コメントの断定_2026-09-05.md)
- 工程記録: 投稿順は `node scripts/gen-queue-index.mjs --posting-plan` が出す（索引 §2 ③）。
  ⚠️ **①にはレビュー判定のファイルが無い**（承認記録 §1 に判定の要旨がある）

## 要旨

コメントに書いた `<file>.js:<行番号>` 形式の参照は、実測した4件が「3件が腐り＋1件は導入時点で既に19行ずれ」で、
**正しく保たれた例が1つも無かった**。4件を関数名参照に直し、**`npm run validate` がこの形を exit 1 で止める**検査を足した
（詳細は CLAUDE.md「コードのコメントに `<file>.js:<行番号>` の形を書くと exit 1 になる」）。
