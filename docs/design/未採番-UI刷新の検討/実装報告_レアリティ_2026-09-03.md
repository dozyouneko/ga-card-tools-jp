# 実装報告：カード検索に「レアリティ」の絞り込みを足す（2026-09-03）

- 設計書: [`レアリティ絞り込み_設計.md`](レアリティ絞り込み_設計.md)（v1.0）
- 起票案: [`起票案_2026-09-03_レアリティ絞り込み.md`](起票案_2026-09-03_レアリティ絞り込み.md)
- ⚠️ **GitHubアカウント停止中で `gh issue comment` が使えないため、issueコメントの代わりにこのファイルを置く。**
  復旧後に起票したら、この内容をそのままissueコメントへ転記する。
- ⚠️ **未pushである。** 本番公開の可否はユーザーが判断する。**デプロイもしていない。**

⚠️ ⭐ **結論を先に書く：実装は完了し `npm run validate` は exit 0、実駆動 23項目すべて合格。**

- **第1周**（`533baf36`）で実装し、**R-V3 の副次要件（件数表示）だけが不合格**になった。
  ⭐ これは実装の不備ではなく**設計書 1.0 の未決事項**だったため、**判断を実装側で決めずに §6 で止めた**
- **設計担当が設計書を v1.1 に更新（§5 に P4 を新設）し、案2（D-1＋D-2）を決定**
- **第2周**（`65b77632`）で **D-1 / D-2 を実装し、R-V12・R-V13 を追加検証**（§9）
- ⚠️ **第3周＝差し戻し1往復目**（`571b7059`）：**ユーザーが実物を触って P5 を発見**
  （CSRで絞ってもタイルの絵柄がCSRにならない）。設計書 v1.2 の **D-3 / D-4** を実装（§10）

---

## 1. 実施内容

| 項目 | 値 |
|---|---|
| **コミット** | **`533baf36`**（実装本体）＋ **`65b77632`**（設計判断 案2 ＝ D-1／D-2＝§9）＋ ⭐ **`571b7059`**（**差し戻し1往復目 ＝ D-3／D-4＝§10**） |
| 変更ファイル | `index.html` / `app.js` / `data/translations.js` / ⚠️ **`shared/js/card-search.js`**（共有）/ `scripts/validate.mjs` |
| push | ⚠️ **していない**（`wrangler pages deploy` もしていない） |
| `npm run validate` | ✅ **exit 0**（`all checks passed.` / 新しい成功行 `meta.rarities covers all rarities — 9種（スナップショット 2495枚から観測）`） |
| ブラウザ実駆動 | Playwright 1.61.1 / Chromium（`--ignore-certificate-errors` ＋ `ignoreHTTPSErrors: true`）／⭐ **23項目すべて合格**（`verify.mjs` 17 ＋ `verify-deck.mjs` 3 ＋ `verify-blind.mjs` 3） |
| ローカルサーバー | ⭐ **`PORT=3010 npm run dev`**（⚠️ **3000番は設計担当がユーザー確認用に起動中のため触っていない**） |
| JSエラー | ⭐ **`pageerror` 0件**（トップ・デッキ構築ツールとも） |
| 証跡 | `tmp/dev/rarity/`（`verify.mjs` / `verify-deck.mjs` / `verify-jpcount.mjs` と `results*.json`） |

### 1-1. 何を入れたか（設計書 §4-1 の表に対応）

| ファイル | 入れたもの |
|---|---|
| `index.html` | `<details class="fgroup" id="g-rarity"></details>` を **`#g-subtype` の直後**に1行 |
| `app.js` | `el.gRarity` / `create({els})` の `rarity` / ⭐ **`urlGroups()` への追加** / `fillChips(el.gRarity, "rarities", { label: "レアリティ" })` |
| `data/translations.js` | `meta.rarities`（設計書 §3 の9件をそのまま） |
| **`shared/js/card-search.js`** | `MULTI` に `["rarity", "rarity", null]` ／ **`haveOf()` を新設**（P1）／ `metaMatches()` の **fail-open**（P2） |
| `scripts/validate.mjs` | §6 の検出経路 ＋ **P3（10種目）のガード** |

⭐ **`fillChips()` / `shared/css/filter-chips.css` は無改造**（設計書の想定どおり）。
⚠️ **チップ絞り込み欄（次タスク）には一切触れていない。**

### 1-2. ⭐ `urlGroups()` に足したことの意味（設計書 §4-1 の「足し忘れ」対策）

設計書は「`filterGroups()` 相当（バッジ集計・リセット）にも足す」と書いているが、
実装を読むと **`filterGroups()` は `urlGroups()` から作られている**（`app.js`）ので、
**`urlGroups()` に1行足すと次の5経路が同時に対応する**：

1. スマホのバッジ集計（`updateFilterBadge()` — インラインのトグルと FAB の両方）
2. `#reset` によるリセット（`resetControls()`）
3. PC左ペインの「選択中の条件」（`renderSelectedFilters()` / `✕` / 「すべて解除」）
4. PC左ペインのアコーディオン（`initAccordion()`）
5. 共有URL（`queryString()` / `applyUrlQuery()`）

⚠️ **逆に言えば、ここを足し忘れると5つ同時に壊れる。** R-V6・R-V7・R-V11d でそれぞれ実測した。

### 1-3. 落とし穴 P1〜P3 への対応

| # | 対応 | 実測での裏取り |
|---|---|---|
| **P1** | `MULTI` の第3要素を `null` にし、`haveOf(card, field, key)` が `editions[].rarity` から重複を潰して組み立てる。`matchesMulti()` の `card[field]` を `haveOf(...)` に差し替え | ⭐ **スナップショット2,495枚での「いずれかの版がそのレアリティ」の集計が、公式APIの `total_cards` と9値すべて一致**（749/678/645/493/123/476/227/19/80） |
| **P2** | `metaMatches()` に `if (field === null) continue;` を追加（索引に列挙値が無い項目では候補を落とさない） | ⭐ **R-V3d の破壊試験**：この1行を route で抜いて配信すると、正解5件の検索が **0件**になる |
| **P3** | ⭐ **`fillChips()` の `.sort()` は触っていない**（設計書の指示どおり）。代わりに `validate` に「`meta.rarities` が10種以上なら exit 1」のガードを置いた | ⭐ 破壊試験で確認（§3 の R-V9） |

⚠️ **P2 の危険は机上のものではなかった。** 索引の `entry[7]`（prefix別の**最小**レアリティ）で
候補を絞ると、**実データで 119枚（レアリティ7が59枚 / 8が17枚 / 9が43枚）が静かに落ちる**
（コレクター系レアリティは、そのprefixの最小になることがまず無いため）。
⭐ **1〜6 では1枚も落ちない**ので、**レアリティ7/8/9で検証しないと素通りする**型の不具合だった。

---

## 2. 検証結果（設計書 §7 の R-V1〜R-V11）

⭐ **実駆動18項目のうち 17 PASS / 1 FAIL。** FAIL は R-V3 の副次要件のみ（§6 で判断を仰ぐ）。

| # | 結果 | 実測値（`tmp/dev/rarity/results*.json` に全文） |
|---|---|---|
| **R-V1** | ✅ | 英語モード `rarity=4` → `#status = "50 件を表示 / 全 493 件"`（**API実測 493 と一致**） |
| **R-V2** | ✅ | 英語モード `rarity=8`+`rarity=9`（OR）→ `"50 件を表示 / 全 99 件"`（**API実測 99 と一致**） |
| **R-V3a** | ✅ | 日本語モード「イン」単独 → **22件**（訳データからの期待22件と一致） |
| **R-V3b** | ✅ ⭐ | 日本語モード「イン」× CUR/CPR → **UI 5件＝公式APIから算出した真値5件と完全一致**（ロレイン、剣の達人／放浪の戦士／昇華せし翼／精霊の支配者／閃光の剣士）＝ **P2 の取りこぼしが無い** |
| **R-V12** | ✅ | ⚠️ ⭐ **第1周では ❌ だった項目**（§6 → 設計書 v1.1 の P4）。**D-1 実装後**：`#status = "5 件を表示（日本語テキスト一致・翻訳済みのみ）（一部のカードは取得後に判定するため総件数は概算です）"` ＝ **誤った「全22件」が消えた**（詳細は §9） |
| **R-V3d** | ✅ ⭐ | **破壊試験**：`metaMatches()` の `if (field === null) continue;` を抜くと同じ検索が **0件**（＝P2 の取りこぼしを実際に再現） |
| **R-V4a** | ✅ | 英語モード PTM × rarity 1/4：OR 50件 → **AND 1件「黄金の火葬台」**＝APIの1ページ目から算出した真値と一致 |
| **R-V4b** | ✅ | 日本語モード「イン」：**OR(1,4)=15件（期待15）/ AND(1,4)=1件「アーケインの爆発」（期待1）**。⭐ AND < OR |
| **R-V4c** | ✅ | 英語モード AND(8,9) = **0件**（⭐ スナップショット全数で「8と9の両方で刷られたカード」は実在しない＝真値0） |
| **R-V5** | ✅ | エキスパンションPTM × SR：**UI 28件 = API `prefix=PTM&rarity=4` の 28件**（併用前は「全 183 件」）＝**独立に効き、APIの挙動と一致**（設計書 §8 の仕様どおり） |
| **R-V6** | ✅ | URL = `?rarity=8&rarity=9&rarity_op=AND` → 復元で `values=["8","9"] / mode=AND / 群が開く`。⭐ `setValues` の `toUpperCase()` を通っても数字は不変 |
| **R-V7a** | ✅ | スマホ幅390px：初期=バッジ無し → レアリティ2件で **2**（FABも2）→ +エレメント1件で **3**（FABも3）／`aria-label="絞り込み・並び替え（3件選択中）"` |
| **R-V7b** | ✅ | `#reset`（⚠️ ボトムシートを開いてから押す本来の経路）後：バッジ消滅・`getValues()=[]`・`open=false`・`mode=OR` |
| **R-V8a** | ✅ | デッキ構築ツールの絞り込み群は **`s-g-element/s-g-class/s-g-type/s-g-subtype` の4つのまま**（レアリティ群なし）。⭐ 並び替えの「レアリティ順」は従来どおり存在 |
| **R-V8b** | ✅ | **click / setValues / reset の3経路**：`click→["ALLY"]`（バッジ1）／`setValues→["ACTION","ITEM"]`／`reset→[]`（バッジ非表示・mode=OR） |
| **R-V8c** | ✅ | 実検索も従来どおり：タイプ=ALLY で **24件描画・「24 件を表示 / 全 744 件」＝API実測 744 と一致**・`pageerror` 0件 |
| **R-V9** | ✅ ⭐ | **破壊試験2種**：①`meta.rarities` から `"7"` を消す → **exit 1**（`実データにあるが … meta.rarities に無い: 7`）／戻すと **exit 0**。②10種目 `"10"` を足す → **exit 1**（`meta.rarities が10種あります。… 1,10,2,… と並びます`）／戻すと **exit 0** |
| **R-V10** | ✅ | `npm run validate` **exit 0**（`all checks passed.`） |
| **R-V13a-c** | ✅ ⭐ | **D-2 の破壊試験3種**（§9-3）。正常時は `jpDropped` の注記が**点灯しない**（想定内の除外17件は `jpBlindDropped` へ）／**取得を失敗させると点灯**／**索引を腐らせると点灯** |
| **R-V11a-d** | ✅ | 1440px幅：群の並びは `g-element,g-class,g-type,g-subtype,g-rarity` ＝ **5番目**／見出し「レアリティ」／チップ9個／**左ペイン（幅287px）に収まる**／⭐ **レアリティを開くとサブタイプが閉じる**（アコーディオン）／⭐ 「選択中の条件」に `レアリティ: スーパーレア（SR）（4）` |

### 2-1. チップの表記（R-V11b の実測）

```
コモン（C）/1  アンコモン（U）/2  レア（R）/3  スーパーレア（SR）/4  ウルトラレア（UR）/5
プロモ（PR）/6  コレクターSR（CSR）/7  コレクターUR（CUR）/8  コレクターPR（CPR）/9
```

⭐ **9種すべてが数値順に並ぶ**（1桁なので `.sort()` の辞書順＝数値順。P3 のとおり今は無害）。

---

## 3. 実行できなかった検証と代替手段・残っている確認

| 項目 | 状況 |
|---|---|
| **デッキ構築ツールの「ログイン後の実操作」** | ⚠️ **ヘッドレスではDiscordログインできない**（CLAUDE.md 既知）。CLAUDE.md #51 の手順を使い、**`app.js` の末尾にテスト用の窓口だけを route で足して**（`deckData`・`searchCtl` がモジュールスコープのため）空のデッキ状態を用意し、**ページ本来の `searchCtl` を動かした**。⭐ 検索経路そのものは無改造で通っている（R-V8c）。⚠️ **所持枚数バッジ・適合判定などデッキの中身を読む表示は未検証**（今回の変更と無関係だが、原則どおり明記する） |
| **本番での確認** | ⚠️ **未pushのため未実施**。`_headers` / `404.html` に触っていないので `npm run dev` との差は生じない |
| **10種目のレアリティが実際に来たときの並び順** | ⚠️ **実データが無いので再現できない**。代替として **`meta.rarities` に `"10"` を足す破壊試験**で `validate` が exit 1 になることを確認した（R-V9②） |
| **スナップショットが無い環境での `validate`** | ⭐ **コードは `existsSync` で分岐し `meta.rarities check skipped — スナップショットがありません（tmp/api-cache/cards-snapshot.json）` を出す**。⚠️ **スキップ経路そのものは未実行**（既存のスナップショットを消すと他セッションの作業に影響するため実行しなかった） |

---

## 4. 差分が設計書の想定と食い違った点

**無し。** 変更は5ファイル・111行追加/6行削除で、すべて設計書 §4-1 の表の範囲。
⭐ **生成物（`cards/**` `sets/**` `data/tl-*.json` 等）の再生成は不要**であることを確認した：

- `meta.rarities` を読む**ビルド側の消費者は存在しない**（`grep -rn 'rarities'` の結果、
  ビルドが読むのは `meta.sets` と `meta.subtypes` のみ。索引生成の `rarities` は別物＝`entry[7]`）
- `data/tl/*.js` を触っていないので `gen-tl-json.mjs` は不要（`validate` の
  `generated JSON up to date` が緑）
- ⚠️ **`npm run build:cards` は意図的に実行していない。** 実行するとスナップショットの
  有効期限（既定60分）が切れて取り直しになり、**タスクと無関係なAPIドリフトが混入する**（CLAUDE.md #71）

---

## 5. 設計書からの逸脱と根拠

| # | 逸脱 | 根拠 |
|---|---|---|
| 1 | 設計書 §6 は「`meta.rarities` の網羅性検査」だけを求めているが、**「10種以上なら exit 1」のガードも同じブロックに入れた** | 設計書 §6 自身が「⚠️ **10種目を観測したら exit 1**。そのとき P3 の並び順も同時に手当てする」と書いている。⭐ **観測側（スナップショット）だけだと、辞書に `"10"` を足して検査を黙らせた瞬間に並び順が壊れる**ため、**登録側の件数も見る**ようにした（実測: `"10"` を足すと exit 1） |
| 2 | 設計書 §4-1 は「`app.js` の `filterGroups()` 相当にも足す」と書いているが、**実際に足したのは `urlGroups()` 1箇所** | `filterGroups()` は `urlGroups()` から導出される実装のため（§1-2）。**書かれた意図（バッジ・リセットに効かせる）は満たしている**うえ、二重に列挙すると片方だけ更新される事故が起きる |
| 3 | 検証で **`shared/js/card-search.js` と `tools/deck-builder/app.js` を Playwright の `route` で書き換えた** | ⚠️ **配信物は一切変えていない**（テスト実行時のみのパッチ）。①P2 の fail-open を抜いて壊れることを見る破壊試験、②デッキ構築のモジュールスコープ変数へ到達する窓口、の2用途。⭐ **どちらも「動いたから合格」ではなく「対策を外すと壊れる」を示すため**（CLAUDE.md のスマホ絞り込み導線と同じ流儀） |

⭐ **既存表記との照合**：`meta.rarities` の略号は `shared/js/card-i18n.js` の
`RARITY_CODE = { 1:"C", 2:"U", 3:"R", 4:"SR", 5:"UR", 6:"PR", 7:"CSR", 8:"CUR", 9:"CPR" }` と一致させた。
`TRANSLATION.md` にレアリティの訳語規定は無い。

---

## 6. ⚠️ 設計担当への確認事項（第1周で止まった点。⭐ **§9 で決着済み**）

⭐ **この節は第1周（`533baf36`）時点の記録である。**
**設計担当が設計書 v1.1 §5 P4 として案2（D-1＋D-2）を決定した**ので、
**対応は §9 に書いた**。以下は経緯として残す。

### 6-1. ⭐ 【要判断 → ✅ 決着】日本語モードの「全N件」が実際の件数より多く出る（R-V3c）

**症状**（実測・`tmp/dev/rarity/jpcount.json`）：

```
日本語「イン」× レアリティ CUR/CPR
  #status = "5 件を表示 / 全 22 件（日本語テキスト一致・翻訳済みのみ）
             （うち17件は取得後の判定で除いたため表示されていません）"
  行数 = 5   ←  「全 22 件」と一致しない
```

**対照実験**（同じ日本語語句で、既存の絞り込みを使った場合）：

```
日本語「イン」× タイプ CHAMPION
  #status = "7 件を表示（日本語テキスト一致・翻訳済みのみ）"
  行数 = 7   ←  一致する（総数と表示数が同じなので「全N件」自体が出ない）
```

**原因**：JPモードの `total` は **`jpCand.length`＝索引で絞ったあとの候補数**である。
**P2 の対応でレアリティだけは索引で絞らない**ことにしたので、
**レアリティで落ちる分が `total` に残ったまま**になる。

⚠️ **副作用がもう1つある**。落ちた件数は `jpDropped` に数えられ
「うち17件は取得後の判定で除いたため表示されていません」と出るが、
**CLAUDE.md はこの注記を「出たら異常の信号」（索引の腐り・取得失敗・畳み漏れ）と位置づけている**。
⭐ **レアリティ絞り込みでは正常時に必ず点灯するので、異常信号としての価値が失われる。**

**⚠️ これは実装の不備ではなく、設計書 §5 P2 が「代償は取得件数が増えること」とだけ書いて
「件数表示をどうするか」を決めていないことによる。** 実装側では決めない。**選択肢は3つ**：

| 案 | 内容 | 影響 |
|---|---|---|
| **案1** | **`jpApprox = true` にする**（レアリティのように索引で判定できない絞り込みが有効なとき） | ⭐ **既存の仕組みをそのまま使う**（`jpApprox` は元々「索引で判定しきれない候補が混じる」ための旗）。表示は `5 件を表示（…）（一部のカードは取得後に判定するため総件数は概算です）` になり、**誤った「全22件」が消える**。⚠️ **`jpDropped` の注記は残る**（＝異常信号の価値は戻らない） |
| **案2** | **案1 ＋ 「索引で判定できない絞り込みによる除外」を `jpDropped` に数えない** | ⭐ **異常信号としての価値が戻る**。⚠️ `matchesActiveFilters()` を「索引が担当する条件」と「担当しない条件」に分ける改造が要る（`shared/` の共有ファイル） |
| **案3** | **現状のまま（何もしない）** | ⚠️ **日本語モードで件数が多めに出続ける**。⭐ ただし**表示される集合そのものは正しい**（R-V3b で実証済み）ので、実害は件数表示だけ |

⭐ **開発担当としての推奨は案1**（設計書に書かれていない意味を足さず、既存の
`jpApprox` の定義そのままで説明が付くため）。⚠️ **ただし判断は設計担当に委ねる。**
⭐ 案1なら `shared/js/card-search.js` の1〜2行で済む。

> ⭐ **【2026-09-03・設計判断】案2（D-1＋D-2）を採用。** 理由は
> **「1つ機能を足すために既存の異常検出を1本殺すのは割に合わない取引だから」**。
> ⭐ **推奨した案1では止めなかったのが正しかった**——案1だけだと `jpDropped` の注記が
> 正常時に点灯し続け、**次に見た人が無視するようになる**（このリポジトリが
> 「無言の緑」「意味を失った検査」で繰り返し痛い目に遭ってきた型）。対応は §9。

### 6-2. 【参考・判断不要】P2 の危険は実データで119枚

索引の `entry[7]`（prefix別の最小レアリティ）で絞ると、**レアリティ7で59枚・8で17枚・9で43枚**が
候補から落ちる（1〜6 では0枚）。⭐ **設計書 §5 P2 の判断は正しかった**ことを実データで確認した。

### 6-3. 【参考・判断不要】次タスクとの境界

⚠️ **チップ絞り込み欄（`チップ絞り込み欄_設計.md`）には一切触れていない**
（`shared/css/filter-chips.css` に差分ゼロ）。**レアリティ群のチップは9個**なので、
次タスクの絞り込み欄の対象になるかどうかは次の設計で決めてよい。

---

## 7. 次にやること

1. ✅ **§6-1 の判断**（設計担当）→ **案2に決定。§9 で実装済み**
2. **レビュー**（⭐ 起点はこのファイル。証跡は `tmp/dev/rarity/`）
3. ⚠️ **本番公開は保留のまま**（設計書 §15 の前提により、チップ絞り込み欄が終わるまで
   PC左ペインごと公開しない）

---

## 8. 追記（2026-09-03・第2周）：設計判断 案2 の実装

- コミット: **`65b77632`**（変更は **`shared/js/card-search.js` 1ファイルのみ**・47行追加/10行削除）
- 設計書: **v1.1 §5 P4**（`abadb16a` / `193636e7`）の **D-1 ＋ D-2**
- `npm run validate`: ✅ **exit 0**
- ⚠️ **push・デプロイともしていない**。⚠️ **`shared/css/filter-chips.css` は引き続き差分ゼロ**

---

## 9. D-1 / D-2 の実装内容

### 9-1. ⭐ 「索引には見えない条件」を1か所で定義した（設計書の要点）

⚠️ 設計書は **「`field === null` を判定に使う箇所が2つになるので、同じ定数から導くこと」**を求めている。
⭐ **実際には3箇所になった**ので、`MULTI` の直後に**唯一の定義**を置き、全部そこから導いた：

```js
// ⭐ 「メタ索引には見えない条件」の唯一の定義（設計書 §5 P4）。第3要素が null＝カード直下に
//    配列プロパティが無く、索引の列挙値にも入っていない項目のこと。
// ⚠ この判定を使う場所は3つある（metaMatches の読み飛ばし／D-1 の概算判定／D-2 の想定内除外）。
//    ⭐ 必ずこの関数から導くこと——別々にハードコードすると片方だけ直したときに静かにずれる。
const isIndexBlind = (entry) => entry[2] === null;
```

| 使う場所 | 何をするか |
|---|---|
| `metaMatches()`（P2） | `if (isIndexBlind(m)) continue;` — 候補を落とさない |
| `indexBlindActive()`（**D-1**） | `MULTI.filter(isIndexBlind).some(([key]) => vals(key).length > 0)` |
| `matchesActiveFilters(card, { skipIndexBlind: true })`（**D-2**） | その条件だけ外して判定し直す |

⭐ **`MULTI` に将来また `field: null` の項目を足しても、3箇所とも自動で追随する。**

### 9-2. D-1 — 誤った「全N件」を出さない

`jpCand` を作り終えた直後に1行：

```js
if (indexBlindActive()) jpApprox = true;
```

- ⭐ **`jpApprox` は元々「索引で判定しきれない候補が混じる」ための旗**なので、意味がそのまま合う
- ⚠️ **総数を数え直してはいない**（設計書の指示どおり）。数えるには全候補を取得するしかない
- ⭐ **新しい注記は増やしていない**。既存の「（一部のカードは取得後に判定するため総件数は概算です）」で足りる
- ⚠️ **英語モード・デッキ構築ツールへの影響はゼロ**。`approxTotal` は `jpSlugs ? jpApprox : …` で
  JPモードにしか効かず、デッキ構築は `els` に `rarity` を持たないので `indexBlindActive()` が常に false
  （**実測**: R-V1 は `全 493 件`・R-V8c は `全 744 件` と、どちらも従来どおり**正確な件数のまま**）

### 9-3. D-2 — 異常信号を守る

```js
if (!matchesActiveFilters(c)) {
  // ⭐ 索引が見られない条件（レアリティ）だけが理由なら「想定内」。
  // ⚠ jpDropped に混ぜない —— この注記は「出たら異常」の信号
  if (matchesActiveFilters(c, { skipIndexBlind: true })) jpBlindDropped += 1;
  else jpDropped += 1;
  continue;
}
```

- ⚠️ **取得失敗（`!c`）・畳み漏れ（`jpSeen`）は手を触れていない**＝従来どおり `jpDropped`
- ⭐ **`jpBlindDropped` は画面に出さない**（設計書「新しい注記は増やさない」）。
  ⚠️ **ただし `onResults` の `info` には載せた**——**D-2 が効いているかを外から観測できないと
  検証もデバッグもできない**ため。**`app.js` は読んでいないので表示は一切変わらない**

### 9-4. 検証（R-V12・R-V13）

| # | 結果 | 実測値 |
|---|---|---|
| **R-V12**（D-1） | ✅ | 日本語「イン」× CUR/CPR → `#status = "5 件を表示（日本語テキスト一致・翻訳済みのみ）（一部のカードは取得後に判定するため総件数は概算です）"`。⭐ **誤った「全 22 件」が消え、概算注記に置き換わった**。行数5 |
| **R-V13a**（D-2・正常時） | ✅ ⭐ | 同じ検索で **`jpDropped = 0`・注記は点灯しない**。想定内の除外は **`jpBlindDropped = 17`** に分かれている |
| **R-V13b**（**破壊試験A**・取得失敗） | ✅ ⭐ | 5枚のうち2枚の `/cards/<slug>` を `route.abort()` → **注記が点灯**（`（うち2件は取得後の判定で除いたため表示されていません）`）・`jpDropped = 2`・行数3 |
| **R-V13c**（**破壊試験B**・索引の腐り） | ✅ ⭐ | `data/card-meta-index.json` を route で書き換え、`arcane-blast` の types に `CHAMPION` を混ぜて**過剰包含**を作る → 日本語「イン」× タイプCHAMPION で **注記が点灯**・`jpDropped = 1`。⭐ さらに `jpBlindDropped = 0`・件数は `全 8 件`と**正確なまま**＝ **D-1 / D-2 がレアリティ以外には効いていない**ことも同時に示している |

⭐ **「動いたから合格」ではなく「壊すと点灯する」ことまで見た**ので、
**異常信号が生き残っていること**が示せている。証跡は `tmp/dev/rarity/verify-blind.mjs` と
`tmp/dev/rarity/results-blind.json`。

### 9-5. 全体の実駆動結果（第2周・23項目）

| スクリプト | 内容 | 結果 |
|---|---|---|
| `tmp/dev/rarity/verify.mjs` | R-V1〜R-V7・R-V11・R-V12 ＋ P2 の破壊試験 | ⭐ **17/17 PASS** |
| `tmp/dev/rarity/verify-deck.mjs` | R-V8（デッキ構築ツール） | ⭐ **3/3 PASS** |
| `tmp/dev/rarity/verify-blind.mjs` | R-V13（D-2 の破壊試験3種） | ⭐ **3/3 PASS** |

⚠️ **第1周の `verify.mjs` にあった R-V8 ブロックは削除した**（`verify-deck.mjs` に分離済みで、
`deckData` への窓口が無いまま二重に走っていた）。

### 9-6. ⚠️ 設計担当への確認事項（第2周）

**無し。** 設計書 v1.1 §5 P4 の D-1 / D-2 をそのまま実装し、R-V12・R-V13 も指示どおり
**破壊試験まで含めて**実施した。⭐ **レビューへ回してよい状態。**

⚠️ 引き継ぎのための注意を1つだけ：
**`MULTI` に第3要素が `null` の項目を足すと、P2 の fail-open・D-1 の概算・D-2 の除外分類の
3つが同時に有効になる**。⭐ **`isIndexBlind()` のコメントにそう書いてある**が、
**自動検査は無い**（`field` を `null` 以外にしても静かに動いてしまう＝索引で絞られて取りこぼす）。

---

## 10. 追記（2026-09-03・第3周＝差し戻し1往復目）：P5（絞り込みに絵柄が追従しない）

> CSRで絞り込みを行った際、検索結果がCSRのイラストになっていない（**ユーザーが実物を触って発見**）

- コミット: **`571b7059`**（`app.js` ＋ `shared/js/card-i18n.js` の**2ファイルのみ**）
- 設計書: **v1.2 §5 P5**（`0fa81676`）の **D-3 ＋ D-4**
- `npm run validate`: ✅ **exit 0** ／ ⚠️ **push・デプロイともしていない**

### 10-1. 直したもの

| # | ファイル | 変更 |
|---|---|---|
| **D-3** | `shared/js/card-i18n.js` | `cardImages()` の返り値に **`rarity: ed.rarity` を1項目追加**しただけ |
| **D-4** | `app.js` | `preferredArtIndex()` を **「版レベルの絞り込みすべて（エキスパンション ∧ レアリティ）に一致する版を選ぶ」**形へ拡張 |

⭐ **設計書のコード片をそのまま採った**（`el.gRarity` の参照・AND の意味・
一致が無ければ先頭へフォールバック・**署名は `(imgs)` のまま**）。

### 10-2. ⚠️ `result_editions` を使わなかった理由（設計書 §5 P5 の指示どおり）

公式APIは絞り込みに一致した版を `result_editions` で返すが、
**`shared/js/card-search.js` の全件取得経路（#44・数値ソート時）が `delete c.result_editions` する**。
⭐ **これを根拠にすると「並び替えをレベル順に変えると絵柄が戻る」という、
再現条件の分かりにくい不整合になる。** **決して削除されない `editions` だけ**を根拠にした。
⭐ **R-V16 がまさにその確認**（レベル順＝全件取得経路でも同じ絵柄になること）。

### 10-3. ⭐ 波及先（署名を変えなかったことで自動的に直った／無関係だったもの）

| 面 | 結果 |
|---|---|
| タイルの初期画像 | ✅ 追従（R-V14） |
| **🎨バッジの版表示** | ✅ 追従（`🎨 2・PTM 1st`） |
| **印刷リストに入る版**（`dataset.artUrl`） | ✅ 追従（`GG1XAzHmIN.jpg` が入る） |
| **詳細モーダル**（`shared/js/card-detail.js`） | ⭐ ✅ **無改造で追従**（同じ関数を `preferredArtIndex` として受け取っているため） |
| **デッキ構築ツール** | ⭐ **無関係**（`tools/deck-builder/app.js:1427` に**自前の `preferredArtIndex`** を持つ。S1 のとおり差分ゼロ） |
| **静的カードページ・大会ページ・属性玉生成** | ⭐ **生成物に影響なし**。`cardImages()` の消費側3スクリプトは **`url` / `prefix` / `label` しか読まない**（`imgs` をそのまま直列化する箇所は無い）ので、**`rarity` を足しても出力は1バイトも変わらない**＝**再生成は不要** |

### 10-4. 検証（R-V14〜R-V16 ＋ 回帰）

証跡: `tmp/dev/rarity/verify-art.mjs` / `results-art.json`。
対象は `アリス、歪みの女王`（`editions[0]`＝PTM/r4 `QOTMVyjCSK.jpg`、`editions[1]`＝PTM 1st/r7 `GG1XAzHmIN.jpg`）。

| # | 結果 | 実測値 |
|---|---|---|
| **R-V14** | ✅ ⭐ | `?q=Alice&rarity=7` → **タイル画像 `GG1XAzHmIN.jpg`／`dataset.artUrl` も同じ／🎨バッジ `🎨 2・PTM 1st`／印刷リストに入る版 `GG1XAzHmIN.jpg`／詳細モーダル `GG1XAzHmIN.jpg`** ＝ **4面すべて一致** |
| **R-V15a** | ✅ | `set=PTM 1st ∧ rarity=7`（両方に一致する版あり）→ `GG1XAzHmIN.jpg` |
| **R-V15b** | ✅ ⭐ | **一致する版が無いケースのフォールバック**：`set=PTM ∧ rarity=7`（PTM版はr4・CSRはPTM 1st版）→ **先頭の `QOTMVyjCSK.jpg` に倒れ、JSエラー0件**。⚠️ **このケースは英語モードでは再現できない**（APIは prefix と rarity を**同じ版**に要求し `prefix=PTM&rarity=7` は0件を返す）ので、**客側フィルタが項目ごとに独立するJPモード**（`?q=歪みの女王&set=PTM&rarity=7`）で再現した |
| **R-V16** | ✅ ⭐ | `sort=level`（**全件取得経路＝`result_editions` が delete される**）でも **`GG1XAzHmIN.jpg`** ＝ `result_editions` に依存していない |
| 回帰1 | ✅ | 絞り込み無し（`?q=Alice`）→ 従来どおり先頭 `QOTMVyjCSK.jpg` |
| 回帰2 | ✅ | エキスパンション単独（`set=PTM 1st`）→ `GG1XAzHmIN.jpg`（P5 以前から正しかった経路を壊していない） |
| JSエラー | ✅ | 0件 |

### 10-5. 回帰（R-V1〜R-V13 とチップ絞り込み欄）

| スクリプト | 内容 | 結果 |
|---|---|---|
| `verify.mjs` | R-V1〜R-V7・R-V11・R-V12 ＋ P2 の破壊試験 | ⭐ **17/17 PASS**（再実行） |
| `verify-blind.mjs` | R-V13（D-2 の破壊試験3種） | ⭐ **3/3 PASS**（再実行） |
| `verify-deck.mjs` | R-V8（デッキ構築ツール） | ⭐ **3/3 PASS**（再実行） |
| `verify-art.mjs` | R-V14〜R-V16 ＋ 回帰 | ⭐ **7/7 PASS** |
| `verify-chipfilter.mjs` | ⚠️ **直後のタスク「チップ絞り込み欄」の回帰** | ⭐ **PASS** |

⭐ **合計 33項目すべて合格。**

**チップ絞り込み欄の回帰**（⚠️ **コードには一切触っていない**）:

- サブタイプ群の絞り込み欄に「ドラゴン」と入力 → **表示チップ 20 → 1**、空に戻すと **20 に復帰**
- ⭐ **レアリティ群には絞り込み欄が出ない**（チップ9種 ≤ `SEARCH_MIN`=30）＝**仕様どおり**。
  `app.js` は `search: true` を渡しているが、`fillChips()` が種類数で出し分けるため出ない
- JSエラー0件

### 10-6. ⚠️ 設計担当への確認事項（第3周）

**判断が要る項目は無し。** ただし**気づいた点を2つ**記録しておく（**どちらも今回のスコープ外**）:

1. ⚠️ ⭐ **設計書 §8 の「エキスパンションとの併用は独立に効く」は、英語モードでは成り立っていない。**
   実測で **公式APIは `prefix` と `rarity` を同じ版に要求する**（`name=Alice&prefix=PTM&rarity=7` は
   **0件**。`prefix=PTM 1st&rarity=7` は3件）。⭐ **一方JPモードの客側フィルタは項目ごとに独立**なので、
   **同じ条件でモードによって結果が変わりうる**。⚠️ **今回の変更が作った差ではない**（併用時の
   API挙動は従来から同じ）が、**§8 の文面は英語モードの実態と食い違っている**
2. `preferredArtIndex()` が見るのは**トップページの絞り込みだけ**なので、
   **カード個別ページ（静的）とデッキ構築ツールの代表画像は従来どおり**。
   ⭐ これは既知の起票案 [`起票案_2026-08-30_ブラウザ側の代表画像.md`](../未採番-版順序の非決定性/起票案_2026-08-30_ブラウザ側の代表画像.md)
   の領分で、**設計書 §5 P5 が「本項を直しても向こうは残る」と明記しているとおり**
