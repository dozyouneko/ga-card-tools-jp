# 実装報告：PC幅の検索条件を左ペインへ ＋ HeroUI 案A-しっかり（2026-09-03）

- 設計書: [`PC左ペイン_設計.md`](PC左ペイン_設計.md)（v1.2）
- 起票案: [`起票案_2026-09-02_PC左ペインとHeroUI案Aの採否.md`](起票案_2026-09-02_PC左ペインとHeroUI案Aの採否.md)
- ⚠️ **GitHubアカウント停止中で `gh issue comment` が使えないため、issueコメントの代わりにこのファイルを置く。**
  復旧後に起票したら、この内容をそのままissueコメントへ転記する。
- ⚠️ **未pushである。** 本番公開の可否はユーザーが判断する。

---

## 1. 実施内容

| 項目 | 値 |
|---|---|
| **コミット** | **`2a057112`**（`2a057112415bd7956225287513f08b009b38cd0f`） |
| 変更ファイル | `index.html` / `style.css` / `app.js` の**3ファイルのみ**（+294 / -34行） |
| push | ⚠️ **していない** |
| `npm run validate` | ✅ **exit 0**（`all checks passed.`） |
| ブラウザ実駆動 | Playwright 1.61.1 / Chromium（`--ignore-certificate-errors` ＋ `ignoreHTTPSErrors: true`）・`PORT=3000 npm run dev` |
| JSエラー | ⭐ **`pageerror` 0件**（トップ・デッキ構築ツールとも） |

### 1-1. 何を入れたか（設計書 §4 の判断1〜7 に対応）

| 判断 | 実装したもの | 主な変更点 |
|---|---|---|
| 判断1・2（L1/L2） | **案1：検索入力ごと左ペイン**・ペイン幅 **320px** | `style.css` に `@media (min-width: 1200px)` を1本追加 |
| 判断6（§11） | **ヘッダ固定＝アプリシェル** | `index.html` に **`#results` ラッパを1件追加**（DOM変更はこれだけ）。`body` を 2行×2列の Grid に。⚠️ **1199px以下は `#results { display: contents }`** |
| 判断7（§12-1） | **絞り込みのアコーディオン** | `app.js` に `initAccordion()`。⚠️ **`shared/js/card-search.js` には書いていない**（S1） |
| 判断5（§12-2） | **案A：左ペイン最上部の「選択中の条件」のみ** | `index.html` に `#selected-filters`、`app.js` に `renderSelectedFilters()`。⚠️ **案C は実装していない ＝ 検証項目 V20 は不要** |
| 判断3・4（§5） | **HeroUI 案A-しっかり** | `:root` の `--panel` / `--panel-2` / `--border` / `--radius` と、余白・行高・文字サイズ・面の分け方 |

### 1-2. 対象外にしたもの（指示どおり手を付けていない）

`git show --stat 2a057112` が `index.html` / `style.css` / `app.js` の3ファイルしか出さないことで確認済み。

- ❌ レアリティ絞り込み（別issue） / ❌ チップ絞り込み欄（別issue）
- ❌ `tools/deck-builder/` ❌ `shared/css/filter-chips.css` ❌ `shared/js/card-search.js` — **1バイトも触っていない**

---

## 2. 検証結果（設計書 §7・§15 の V1〜V19）

⚠️ **V20 は不要**（判断5で案Cを採らなかったため。設計書 §4 の注記どおり）。
測定スクリプトは `tmp/dev/pc-pane/`（`measure.mjs` / `verify.mjs` / `verify2.mjs` / `v14.mjs` / `relcheck.mjs` / `final.mjs`）。
⚠️ `tmp/` はgitignoreなので、同じ測り方で再現できる形で残してある。

### 2-1. G1・G2（成否の基準）

| # | 期待値 | **実測** | 判定 |
|---|---|---|---|
| **V1** | 1200/1280/1440/1920/2560px で `#q` が**すべて同じ固定幅** | ⭐ **全幅で 287px**（変更前 571 / 611 / 691 / 931 / **1251** px） | ✅ **G1 達成** |
| **V2** | 1440x900・4グループ全開でカード一覧の開始 y が閉じたときと同じ | ⭐ **138px（全閉）→ 138px（4グループ全開）＝ 変化なし**（変更前は 322 → 878px） | ✅ **G2 達成** |

⚠️ V2 は**アコーディオンを実行時に無効化**して測った（`Object.defineProperty(PANE_MQ, "matches", …)`）。
アコーディオンが効いている通常操作では4グループ全開にはならないため。

### 2-2. レイアウト

| # | 期待値 | **実測** | 判定 |
|---|---|---|---|
| **V3** | 列数が §3-2 の表どおり・横スクロールが出ない | 1200:**4** / 1280:**4** / 1366:**5** / 1440:**5** / 1920:**7** / 2560:**11**。**横スクロールは全15幅で 0件**（`scrollWidth === clientWidth`） | ⚠️ **1920だけ表と違う**（表は8列）→ §4-1 |
| **V4** | 1199px は上部バー / 1200px で左ペイン | 1199: `body` **block**・`#q` **571px**・一覧 y **327px** ／ 1200: `body` **grid**・`#q` **287px**・一覧 y **138px** | ✅ |
| **V5** | ペインが動かない | ⭐ **`#results` を 1500px スクロールしても `.controls` の top は 74px のまま**・`window.scrollY` は **0** | ✅（⭐ §11-1 のとおり `sticky` は不要になった。`position: static`） |
| **V6** | 1グループだけ開くとペイン内スクロールが起きない（1440x900） | エレメント/クラス/タイプ：**826 = 826（スクロールなし）** ／ ⚠️ **サブタイプのみ 849 > 826（23px あふれる）** | ⚠️ **3/4** → §4-2 |
| **V7** | 4グループ全開でも全グループに到達できる／一覧は動かない | ペイン `scrollHeight 1649 / clientHeight 826`・`scrollTop` 最大 **823** で**最終グループ（サブタイプ）が可視**。一覧 y は **138px のまま** | ✅ |
| **V11** | 641〜1199px に新しい帯を作っていない | `#q` 幅・列数とも**変更前と完全一致**（641:292/3列・768:355/3列・900:421/4列・1024:483/5列・1100:521/5列・1199:571/5列）。`body` は **block**・`.controls` は **sticky** のまま | ✅（⚠️ 余白と文字サイズは HeroUI 案A で変わる＝判断4。§4-4） |
| **V13** | ペイン320pxでサブタイプ157チップがはみ出さない | ⭐ **157チップ中はみ出し 0件**・ペインの横方向オーバーフロー **なし**・チップ枠は `max-height 200px` / `overflow-y auto` で高さ **200px** | ✅ |
| **V15** | 結果側だけがスクロールする | `#results.scrollTop = 1500` のとき **ヘッダ top = 0**・**ペイン top = 74（不動）**・`window.scrollY = 0`・`body { overflow: hidden }` | ✅ |
| **V16** | 1199px以下で `#results` が `display: contents` | 375 / 390 / 640 / 641 / 768 / 900 / 1024 / 1100 / 1199 の**全幅で `contents`**。1200px以上は `block` | ✅ |

### 2-3. 回帰（壊してはいけないもの）

| # | 期待値 | **実測** | 判定 |
|---|---|---|---|
| **V8 / V17** | 375 / 390px でスマホ導線が現状どおり。R1〜R3 を**破壊試験**で確認 | 下の表（2-4） | ✅ |
| **V9** | `404.html` のレイアウトが破綻しない（見た目は変わる＝意図） | ⭐ **本文の矩形が変更前と完全一致**（1440x900: `x=310 / w=820 / footer y=470`）。横スクロールなし。375pxでも1列（`343px`）で変わらず。**変わったのは色と角丸だけ**（リンク角丸 12→14px・面 `#171a21`→`#1b1f28`・ヘッダ背景がグラデ→`#191d25`） | ✅ |
| **V10** | `tools/deck-builder/` の絞り込みチップの見た目が変わっていない | ⭐ **デッキ構築ツールはルートの `/style.css` を読んでいない**（読むのは `/tools/deck-builder/style.css` と `shared/css/*` の5本）。チップの計算値は `border-radius 10px` / `padding 5px 11px 5px 8px` / `font-size 13.6px` / `chips max-height **none**` / `box-shadow **none**` ＝ **共有CSSのまま**。`#selected-filters` も `#results` も**存在しない** | ✅ |
| **V12** | `npm run validate` が exit 0 | ✅ `all checks passed.`（`top set links up to date — 63本` / `issue drafts in sync` 等すべて緑） | ✅ |
| **V14** | ペイン内の Tab 順・フォーカスリング・スクロール追従 | Tab順は `#q → #qtext → summary → AND → OR → 次のsummary`。フォーカスリングは **2px solid / offset 2px**（入力）・**2px solid / offset 1px**（チップ）。⭐ **チップ領域（max-height 200px）でも focus でスクロールが追従**し、20種の最後・157種の最後とも**枠内で可視**（`chips.scrollTop` 0→156→402、`.chips-rest` は 2946） | ✅（⚠️ **1行の追加が要った** → §3-3） |
| **V18** | 2つ目を開くと1つ目が閉じる／デッキ構築は従来どおり複数開ける | トップ（1440px）: エレメント開 → `[g-element]` → クラス開 → **`[g-class]`** → タイプ開 → **`[g-type]`**（常に1つ）。デッキ構築: `s-g-element` と `s-g-class` が **同時に開いたまま** | ✅ |
| **V19** | 選択・解除が反映される／✕で再検索が走る | 3チップ選択 → 表示は **「ノーム NORM ✕ / 火 FIRE ✕ / 水 WATER ✕ / すべて解除」**、`/cards/search` は **3回**（＝チップ変更ごとの既存挙動）。✕で1件外す → チェック・バッジ・URL（`?element=FIRE`）・件数（`全 336 件`）がすべて追従。⭐ **「すべて解除」は検索 1回だけ**（`setValues([])` でまとめて外してから1回検索） | ✅ |

### 2-4. 破壊試験（⭐ 対策が「効いている」ことの証明）

⚠️ **「動いたから合格」では対策が load-bearing か分からない**ので、
**CSSOM の `insertRule` で対策を実行時に外し、壊れることを測った**（ファイルは変更していない）。
⚠️ CSP `style-src 'self'` のため `<style>` 注入は使えない。既存シートへの `insertRule` で行った。

| 対策 | 外したときに期待される破綻 | **実測（375x812）** | 判定 |
|---|---|---|---|
| **R1** `html, body { overflow-x: clip }` | `hidden` に戻すと body がスクロールコンテナ化し、**子孫の `position: sticky` が死ぬ** | プローブ（`position:sticky; top:0`）を `#status` の前に置き `scrollY=1200`：**clip では top = 0（貼り付く）／ hidden では top = -950（流れる）** | ✅ **load-bearing** |
| **R2** `.controls { position: static }` | 条件バーが画面外に出ず **FABが永久に出ない** | `sticky` に戻すと `.controls` の top = **0**・FAB は **`is-hidden` のまま**。戻すと top = **-1427**・FAB **出る** | ✅ **load-bearing** |
| **R3** `.controls { backdrop-filter: none }` | シートが**画面下端に来ない** | `blur(6px)` に戻すとシートの bottom = **249**（画面高 812）。戻すと **812** | ✅ **load-bearing** |
| **R4** `#results { display: contents }`（1199px以下） | 箱ができてブロック整形文脈が1段増える | `display: block` を強制すると `#results` が **contents → block** に変わる（`body` の子要素数も変化） | ✅ **効いている** |

**破壊試験なしの現状値（375px）**: `html/body overflow-x = clip` ／ `.controls position = static` ／
`backdrop-filter = none` ／ `#results display = contents` ／ `#selected-filters display = none` ／
`#q font-size = **16px**`（iOSズーム防止が生きている）／ 横スクロール **なし**（375 = 375）／
FAB は scrollY=1500 で出現（`bottom = 796`）・シートは `bottom = 812`（画面下端）。

**スマホでアコーディオンが効かないこと**: 375px でエレメント→クラスと開くと **両方 open のまま**（設計判断 → §4-3）。

### 2-5. その他の回帰（設計書の検証項目外・自主確認）

| 確認 | 実測 |
|---|---|
| 印刷バー（1200px以上・アプリシェル下） | カードを追加すると出る。`--printbar-h = 60px` が実測で入り、バーの bottom = 900（画面下端） |
| カード詳細モーダル（1200px以上） | `#gacd-modal` が開く（`1440 × 900`）。Esc で閉じ、`body` のクラスは `page-top` のまま復帰。`body { overflow: hidden }` も維持 |
| URL復元（`?element=FIRE&type=CHAMPION`） | PC: **`g-element` だけが開く**（→ §4-3）。「選択中の条件」に **火 FIRE / チャンピオン CHAMPION の2件**が出るので、閉じた側の内容も見える。900px: **両方 open**（従来どおり） |

---

## 3. 設計書からの逸脱と根拠

⚠️ いずれも**設計書どおりに書くと壊れる**ことを実測して確認したうえでの変更。

### 3-1. `body` の Grid 化を **`body.page-top`** に限定した（⭐ 設計書の見落とし）

設計書 §3-1・§11-1 は素の `body { display: grid }` と `body > * { grid-column: 2 }` を書いている。
⚠️ ⭐ **しかし `style.css` は `404.html` も読む**（本番で `/style.css` を読むのはこの2枚だけ）。
そのまま入れると **404ページの本文が「左320pxの空列」の右へ押し込まれた**:

| 404.html（1440x900） | 設計書どおり | **`body.page-top` で限定** |
|---|---|---|
| `body` の display | **grid** | block（変更前と同じ） |
| 本文 `.notfound` の幅 | ⚠️ **542px**（変更前 820px） | ⭐ **820px（一致）** |
| 本文の x | ⚠️ **609**（変更前 310） | ⭐ **310（一致）** |
| フッタの y | ⚠️ **799**（変更前 470） | ⭐ **470（一致）** |

⭐ **対処**: `index.html` の `<body>` に `class="page-top"` を付け、Gridのルールを
`body.page-top` / `.page-top > *` / `.page-top .site-header` / `.page-top #results` に限定した。
⭐ **R8 の「404にも案Aの見た目が出る」は意図どおり残っている**（色と角丸だけが変わる＝V9）。

### 3-2. HeroUI のサイズ指定を **スマホ用メディアクエリより前**に置いた

モックは候補CSSを `style.css` の**末尾**（＝ `@media (max-width: 640px)` より後）に注入していた。
⚠️ ⭐ **そのまま実装すると `.controls input { font-size: .875rem }`(=14px) がスマホの
`font-size: 16px` を同詳細度で上書きし、iOS でフォーカスのたびに画面が拡大する**
（CLAUDE.md「iOS のフォーカス時ズーム防止」・既存コメントで明示されている対策）。

⭐ **対処**: HeroUI の節を `@media (max-width: 640px)` の**直前**に置いた。
実測で **375px の `#q` は 16px のまま**（2-4）。

### 3-3. `.controls .fgroup .chip { position: relative }` を1行足した（V14 のため）

⚠️ ⭐ **設計書に無い1行。省くと V14 が落ちる。**

- 左ペイン化で `.controls` が `position: sticky` → **`static`** になった。
- チップのチェックボックスは `position: absolute`（`shared/css/filter-chips.css`）なので、
  **包含ブロックが `.controls` から初期包含ブロックへ移った**。
- その結果 **`.chips`（`max-height: 200px` のスクロールコンテナ）が包含ブロック連鎖の外**になり、
  ⚠️ **Tab でチップにフォーカスしてもチップ領域が自動スクロールせず、フォーカスリングが枠外のまま見えなくなった**
  （実測: `chips.scrollTop` が 0 のまま・最後のチップは枠外）。
- ⭐ `.chip` を包含ブロックにすると追従する（実測: `scrollTop` 0→156→402、`.chips-rest` は 2946）。
- ⚠️ **見た目は1pxも動かない**ことを A/B で確認した（`position: static !important` を実行時に当てて
  **157チップの矩形が完全一致・差分0**）。チェックボックスに `top`/`left` を書いていないため。

### 3-4. モックの HeroUI ルールのうち **チップ寸法系は実装していない**（⭐ 元から効いていなかった）

モックの `skinCss()` には `.fgroup{border-radius:12px}` `.fgroup summary{padding:11px 14px}`
`.flabel{font-size:.875rem}` `.fgroup .chip{…}` `.fgroup .chips{…}` が含まれる。
⚠️ ⭐ **これらは `index.html` の読み込み順（`style.css` → `shared/css/filter-chips.css`）のため、
同じ詳細度では常に `filter-chips.css` が勝つ＝モックでも効いていなかった**
（実測: 実装後も `.fgroup` の `border-radius` は **10px**、`.chip` の `font-size` は **13.6px** ＝ 共有CSSの値）。

⭐ **したがって「ユーザーが承認した見た目」＝ これらが効いていない状態**。
効かせるには `shared/css/filter-chips.css` を変えることになり、**デッキ構築ツールへ波及する（R6・S1 違反）**。
⭐ **死んだルールをリポジトリに残さない**ため、実装からは外し、`style.css` に理由をコメントで残した。

### 3-5. 「選択中の条件」チップのラベル

モックは `chip.textContent` をそのまま使っており **「ノームNORM」** と繋がって出ていた。
実装では `<span>`（日本語）と `<em>`（英字キー）を分けて **「ノーム `NORM`」**（英字は muted・小さめ）にした。
⭐ チップ本体（`shared/css/filter-chips.css` の `.chip em`）と同じ見え方に揃えたもので、**情報量は同じ**。

---

## 4. 実行できなかった検証・残っている確認・判断が要る項目

### 4-1. ⚠️ V3：**1920px だけ設計書 §3-2 の表と1列ずれる**（7列。表は8列）

⭐ **原因は算数で説明がつく**。HeroUI 案A-しっかりで `.grid { gap: 16px → 18px }`、
左ペイン時の横 padding を `24px → 20px` にしたため:

| 条件 | 右列の内寸 | 列数の計算 | 列数 |
|---|---|---|---|
| 設計書 §3-2 の測定時（gap 16 / padding 24） | 1920−320−48 = 1552 | `196n ≤ 1568` → n ≤ 8.00 | **8** |
| 今回（gap 18 / padding 20） | 1920−320−40 = 1560 | `198n ≤ 1578` → n ≤ 7.97 | **7** |

⚠️ **境界ぴったりで落ちている**（8列に必要なのは 1578 に対して 1584）。
他の幅（1200:4 / 1280:4 / 1440:5 / 2560:11）は**表と一致**している。

⭐ **判断をお願いしたい**: このままでよいか、`.grid { gap: 18px }` を 16px に戻して 1920px を8列に復帰させるか。
⚠️ **実装側では決めていない**（`gap: 18px` は判断4「余白も HeroUI のスケールに揃える」の一部で、
ユーザーがモックで見た状態でもあるため）。

### 4-2. ⚠️ V6：**サブタイプだけを開いた状態は 1440x900 でペインが 23px あふれる**

| 開いているグループ | ペインの中身 | 表示枠 | ペイン内スクロール |
|---|---|---|---|
| エレメント／クラス／タイプ | 826px | 826px | ⭐ **起きない** |
| **サブタイプ（上位20種表示）** | **849px** | 826px | ⚠️ **起きる（23px）** |

⭐ **設計書 §12-1 が「サブタイプを開いたときはなお 900px を超える」と予告していた事象**そのもの。
⚠️ **あふれ量は 23px と小さい**ので、次のどちらかで消せる（**どちらも実装側では決めていない**）:
① `.controls .fgroup .chips` の `max-height` を 200px → **180px** ／
② ペイン下部の padding を 24px → **8px**。
⚠️ **ただし画面高が 900px 未満なら結局あふれる**ため、「必ず起きない」を保証はできない。

### 4-3. ⚠️ アコーディオンの適用範囲と、URL復元との相互作用（**実装側で決めた2点。追認をお願いしたい**）

設計書 §12-1 は幅について何も書いていない。**次のように決めた**:

| 決めたこと | 根拠 |
|---|---|
| **アコーディオンは 1200px 以上でだけ効かせる**（`PANE_MQ` でガード） | ① 設計書 §3-3「**1200px 未満では現状の上部バーのままにする**」／ ② §12-2 が「アコーディオンにすると閉じたグループの中身が見えなくなる」→ その**補償である案A（選択中の条件）は左ペインにしか出ない**（モックでも `min-width:1200px` の中でのみ表示）。スマホで畳むだけ畳んで補償が無い状態になるのを避けた／ ③ 検証項目 V8・V11・V16・V17 がそろって「1199px以下は現状どおり」を要求している |
| **URL復元で複数グループに値があるときは、先頭（エレメント→クラス→タイプ→サブタイプの順で最初）の1つだけが開く** | `<details>` の `toggle` は非同期に発火するため、アコーディオンを入れた時点で「複数を開いたまま」は原理的に成立しない。⭐ **閉じた側の内容は「選択中の条件」に全部出る**ので情報は失われない（実測: `?element=FIRE&type=CHAMPION` で `g-element` のみ open・条件表示は2件とも出る） |

⚠️ **どちらも仕様の判断なので、設計担当の追認がほしい**（実装は上の方針で入っている）。

### 4-4. ⚠️ V8・V11 の「現状と完全に同じ」の読み方

⭐ **レイアウトと導線は完全に同じ**（帯の切り替わり方・`#q` 幅・列数・FAB/シートの挙動・R1〜R3）。
⚠️ **色と寸法は HeroUI 案A-しっかり（判断3・判断4）で意図的に変わる**——これはスマホ・タブレットにも及ぶ:

| 幅 | 変わったところ（意図） |
|---|---|
| 375 / 390 / 640 | カード一覧の開始 y が **314 → 312px**（入力の `line-height` 1.25rem 化）。面と区切り線の色。⭐ **`#q` は 16px のまま** |
| 641〜1199 | 入力の文字が **15.2px → 14px**、余白が `10px 12px → 10px 14px`。一覧開始 y は 641:553→559 / 768:478→486 / 900:459→467 / **1024:461→417** / 1100・1199:322→327 |

⚠️ **1024px の −44px は「絞り込みグループの折り返しが変わった」ため**（文字が小さくなり3列の高さが縮んだ）。
帯・列数（5列）・`#q` 幅（483px）は変更前と一致している。
⭐ **V9 が「404の見た目は変わる＝意図」と書いているのと同じ扱い**と判断したが、
⚠️ **「V8/V11 は色も含めて完全一致であるべき」という読み方なら差し戻してほしい**（その場合は HeroUI 案A を
`min-width: 1200px` の中だけに閉じる必要があり、`:root` を使えなくなるので設計からやり直しになる）。

### 4-5. 実行できなかった検証

| 項目 | 理由 | 代替 |
|---|---|---|
| 実ブラウザ（ユーザーの Windows / iOS 実機）での確認 | コンテナ内から実機を触れない | ⭐ **設計書 §8-4 のとおり、レビュー合格後にユーザーが 3000番のローカルサーバーで実物を操作する工程**が残っている |
| デッキ構築ツールの**ログイン後**の絞り込み | Discord OAuth が要る | チップの構築は `/api/me` より前に走るため、未ログインでも `.fgroup` は組み上がる。**その状態で計算値と同時開閉を確認**（V10・V18）。⚠️ **ログイン後の画面は未検証** |
| 本番（Cloudflare Pages）での確認 | ⚠️ **未pushのため** | `_headers` / `404.html` の挙動は `npm run dev` では再現しないが、**今回 `_headers` は変更していない**。`404.html` は**中身を変更しておらず**、変わるのは `/style.css` 経由の見た目だけ |
| スマホ実機での iOS ズーム | 実機なし | `#q` の計算 `font-size` が **375px で 16px** であることを実測（2-4） |

---

## 5. 設計担当への確認事項（⚠️ 実装側では決めていない）

1. **§4-1**: 1920px の列数が 7（設計書の表は 8）。`.grid { gap: 18px }` を 16px に戻すか、このままか。
2. **§4-2**: サブタイプを開いたときのペイン内スクロール 23px を、`max-height 180px` 等で消すか、仕様として許容するか。
3. **§4-3**: アコーディオンを **1200px 以上限定**にした判断と、**URL復元で1つだけ開く**挙動の追認。
4. **§4-4**: V8・V11 の「現状と完全に同じ」に**色・寸法も含む**か（含むなら HeroUI 案A の適用範囲から見直しが要る）。
5. **§3-4**: HeroUI のチップ寸法系ルールを**実装しなかった**判断の追認（効かせるには共有CSSに触れる＝R6・S1）。
6. ⭐ **`#results` ができたことで「スクロールする要素」が `document` から `#results` に変わった**（設計書 §11-4 H2）。
   派生起票案「絞り込み変更でスクロール位置が先頭に戻る」の直し先は、**PC幅では `#results` 側**になる。
   ⚠️ **この報告では触っていない**（別issue）。

---

## 6. 次の工程

- ⚠️ **未push**。本番公開の可否はユーザーの判断。
- ⚠️ 設計書 §8-4 のとおり、**レビュー合格後・承認の前にユーザーがローカルサーバーで実物を操作する工程**が要る。
  ⭐ **その際は `PORT=3000 npm run dev` で上げること**（`forwardPorts` が `[3000]` だけなので、
  3100 等で上げるとユーザーのブラウザから届かない＝設計書 §10）。
- スクリーンショット（レビュー・ユーザー確認用）: `tmp/dev/pc-pane/shot-*.png`
  （2560 / 1920 / 1440 / 1280 / 1200 / 1199 / 1024 / 375 ／ 選択中の条件つき ／ サブタイプ157種 ／ 404）
