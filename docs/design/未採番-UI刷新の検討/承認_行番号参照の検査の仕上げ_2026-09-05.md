**対象: 「行番号参照の是正と検査」**（→ 継続） — この宣言は 2026-09-11 に遡って追記した（索引の転記ドリフト防止）

# 承認記録 — 行番号参照の検査の**仕上げ3点**（2026-09-05）

⚠️ **GitHubアカウント停止中のため、issueの承認コメントの代わりにこのファイルを置く。**
復旧後に `gh issue comment <n> --body-file` でそのまま投函する
（索引 [`待ち行列と復旧手順.md`](../待ち行列/待ち行列と復旧手順.md) §2-④）。

- 呼び名: **「行番号参照の検査の仕上げ」**（記号で呼ばない → 索引 §4-4）
- 指示（正）: [`追加指示_行番号参照の検査の仕上げ_2026-09-04.md`](追加指示_行番号参照の検査の仕上げ_2026-09-04.md)
- 前提（承認済み・本体タスク）: [`承認_行番号参照の検査_2026-09-04.md`](承認_行番号参照の検査_2026-09-04.md)（実装 `8e2c8782`）
- 実装報告: [`実装報告_行番号参照の検査の仕上げ_2026-09-05.md`](実装報告_行番号参照の検査の仕上げ_2026-09-05.md)
- レビュー判定: [`レビュー判定_行番号参照の検査の仕上げ_2026-09-05.md`](レビュー判定_行番号参照の検査の仕上げ_2026-09-05.md)（**合格・差し戻し0往復**・M1〜M7 全✅）
- コミット: 実装 **`f44f6cce`** / 報告 `6eecd734`・`bfc924d9` / レビュー `1d844bac`

---

## 1. 判定 — ⭐ **承認する（＝このタスクは完了）**

指示 #1〜#3 の3点が、**それぞれ意図どおりに実現している**。

⚠️ **設計担当は「動いたかどうか」を再確認していない**（それはレビュー担当の担当で、判定コメントに証跡がある）。
**見たのは「設計の意図を満たしているか」と「判断が要る問い」だけである。**

### 設計担当が独立に測り直したこと

| 確認 | 実測（2026-09-05・作業ツリー `1d844bac`） |
|---|---|
| 担当パス | ✅ `git -c core.quotepath=false show --name-only --format= f44f6cce` ＝ **`scripts/validate.mjs` の1ファイルのみ**（+9 / -6） |
| 走査ファイル数（#1 が効いたか） | ✅ 指示の式で **46**。`npm run validate` の成功行も **`0件（46ファイル走査）`**・exit 0 |
| 冒頭コメントの一覧（#2） | ✅ 成功行**10本**の並びと、冒頭コメントの記載順が**完全に一致**した |
| 腐る実測値の撤去（#3） | ✅ `278件` は消え、**桁と「増えること」だけを書く形**になっている |

⭐ **#2 の効果は「3行足した」ことではなく、⭐ 冒頭コメントが再び「検査の目次」として使える状態に戻ったこと**である。
成功行の並びと一致しているので、**次に検査を足す人は挿入位置を迷わない**（＝一覧が自分で自分を保守する形になった）。

---

## 2. ⭐ 逸脱1点（報告 §5 の N1）の追認 — **追認する**

| # | 逸脱 | 判断 | 根拠 |
|---|---|---|---|
| **N1** | #3 の書き換えで、**その段落の折り返しを3行に組み直した** | ⭐ **追認** | 指示 §3 が禁じたのは**趣旨を変えること**と**正確な数への更新で済ませること**の2つで、⭐ **折り返しはどちらにも当たらない**。実際に差分を読み、**削除された情報が無いこと**を確認した（`docs/` を対象に含めるなという警告・その理由・「広げないこと」の3要素がすべて残っている） |

⚠️ **書式の判断をわざわざ報告に書いたのは正しい。** 差分だけを見ると「3行の置換」に見えるため、
**書かなければレビューが「意図しない削除では」と疑う**ところだった。

---

## 3. ⭐ Q1（`CLAUDE.md` の走査ルートが古くなった）— **是正した**

⚠️ **レビュー担当の指摘は正しく、承認の前に設計担当が直した。**

- **該当**: `CLAUDE.md` の `npm run validate` の節（走査対象の記述）
- **旧**: 対象は `scripts/` **`shared/js/`** `functions/` `app.js` `tools/` の `.js`/`.mjs`
- **新**: 対象は `scripts/` **`shared/`** `functions/` `app.js` `tools/` の `.js`/`.mjs`

### ⭐ 設計担当が範囲を数え直した（＝直すのはここ1箇所でよいか）

レビュー担当の見立て（「運用文書は `CLAUDE.md` の1箇所だけ」）を独立に確認した:

| 箇所 | 実測 | 扱い |
|---|---|---|
| `CLAUDE.md` の走査対象の記述 | ⚠️ **1件・古い** | ⭐ **直した** |
| `README.md` | ⭐ **記載なし**（0件） | — |
| `CLAUDE.md` の他の `shared/js/…` 8件 | ⭐ **すべて実在のファイルパス**（`shared/js/card-i18n.js` 等）＝**走査ルートの記述ではない** | ⚠️ **直さない**（直すと嘘になる） |
| `CLAUDE.md` の成功行の説明 | ⭐ **`0件（N ファイル走査）` と `N` で書いてある** | ⭐ **もともと腐らない書き方**＝そのまま |
| `docs/` 配下の `45ファイル走査` / `shared/js` | **その時点の実測の記録**（設計書・報告・レビュー判定） | ⚠️ **直さない**（⭐ **記録を後から書き換えるのが一番悪い**） |

⭐ **レビュー担当の見立ては正しかった。**

### ⭐ あわせて「どちらが正か」を書き足した

⚠️ **同じ食い違いは今後も起きうる**ので、`CLAUDE.md` 側に**正の所在**を明記した:

> ⚠️ ⭐ **正は `scripts/validate.mjs` の `LINEREF_ROOTS` / `LINEREF_EXCLUDE`**——
> **食い違ったらコード側が勝つ**（⚠️ **この食い違いを止める自動検査は無い**。2026-09-05 に実際にずれた）。

⭐ **これは「防止」ではなく「解決可能にする」ための追記である。**
食い違いは依然として起こりうるが、**見つけた人がどちらを直せばよいか迷わない**。

### ⚠️ 自動検査（#70 と同型）は**入れない** — ただし引き金を決めておく

⭐ **`git add` 列挙（#70）と同じ「3箇所の集合一致」検査を作ることは技術的には可能**だが、**今は入れない**:

1. ⚠️ **守る対象の変更頻度が桁違いに低い。** `LINEREF_ROOTS` が動いたのは
   **検査が新品で調整中だったから**であって、⭐ **ディレクトリの改名はこのリポジトリで年に数回も起きない**。
   **年に一度も動かない定数のために、マーカー付きの機構を1本増やす**のは割に合わない
2. ⚠️ ⭐ **その機構自身に既知の踏み方がある。** #70 のマーカーは
   **行頭に置くとその行のMarkdownが死ぬ**・**1組しか置けない**・**消すと exit 1** という3つの注意を抱えている
   （CLAUDE.md に明記済み）。⭐ **守る価値が小さい対象に、footgun を持つ機構を足すと差し引きで損をする**
3. ⭐ **今回は「正の所在」の明記で実害が消える。** #70 が検査を必要としたのは
   **3箇所が対等に見えてどれが正か分からなかった**からで、⭐ **本件はコードが正だと1行書けば決着する**

⚠️ ⭐ **ただし据え置きの理由は「まだ壊れていないから」ではない**（実際に1回ずれた）。
**引き金を先に決めておく**:

> ⭐ **`LINEREF_ROOTS` と `CLAUDE.md` の記述が 2回目 のずれを起こしたら、そのときは検査を入れる。**
> ⚠️ **「同じ失敗を2回繰り返したら止まる」（CLAUDE.md の自律運用のストップ条件）と同じ基準にそろえる。**

---

## 4. ⭐ S1-A（`赤くなる` は現時点で成立しない）— **採用する**

### 4-1. 事実確認（設計担当が独立に実測）

| 確認 | 実測 |
|---|---|
| `shared/vendor/jspdf.umd.min.js` の `<file>.js:<行番号>` 実ヒット | ⭐ **0件** |
| 同ファイルに `.js` 名が出るか | ⭐ **出る**（`pdfobject.min.js` `md5.js` `security.js` 等・**行番号が付かない**） |
| `sourceMappingURL` | ⭐ **1件ある**（＝「ソースマップ由来の記述が入りうる」は妥当） |

⭐ **レビュー担当の R1（除外を外すと 46→47 だが exit 0）は正しい。**
⚠️ **いまこの除外を外しても赤くはならない。**

### 4-2. ⭐ 採用する理由 — **レビュー担当より一段強く見ている**

⚠️ レビュー判定は害を「**警告が信用を落とすに留まる**」と評価したが、⭐ **設計担当はもう一段先まで見る**:

> ⭐ **この一文の唯一の仕事は「消すな」と言うことである。**
> ⚠️ **消そうか迷っている人＝この警告の唯一の読者**は、⭐ **たいてい試してから消す**。
> そして試すと **exit 0（緑）** が返る。⚠️ ⭐ **つまりこの警告は、狙った相手にだけ嘘だと見破られ、
> 見破った相手に「消してよい」という誤った確信を与える。**

⭐ **害の向きは「信用を落とす」ではなく、⚠️ 警告が防ごうとした削除を、警告自身が後押しする**である。
⚠️ **これは fail-open を1つも残さないという本体タスクの方針（承認記録 §3-D2）と正面から食い違う。**

さらに ⭐ **本タスクは「コードに書いた事実は腐る」を実証したタスクである**。
⚠️ **その検査自身のコメントに、1コマンドで反証できる断定が残るのは据わりが悪い**——
これはレビュー担当も同じことを書いており、⭐ **設計担当も同意する**。

### 4-3. ⚠️ ⭐ 文言はレビュー案を**そのままは採らない**

レビュー担当の修正案:

> 外すと `shared/vendor/` も走査対象に入る（**現在の配布物にヒットは無いが**、ソースマップ由来の記述が入れば赤くなる）

⚠️ ⭐ **「現在の配布物にヒットは無い」は、それ自体が測定値であり腐る**（jspdf を更新した日に嘘になる）。
⭐ **断定を1つ消して測定値を1つ増やすのでは、この一連のタスクの趣旨に反する。**

⭐ **採る方針は「反証できる主張を1つも残さない」。** 具体案は
[`追加指示_vendor除外コメントの断定_2026-09-05.md`](追加指示_vendor除外コメントの断定_2026-09-05.md) §2 に置いた。

### 4-4. ⚠️ 受け渡し — **追加指示を書いて `実装待ち` に戻す**

⚠️ ⭐ **設計担当は `scripts/validate.mjs` を書かない**（越権の禁止）。
⭐ **「次の機会に回す」は選ばない**——⚠️ **それは「覚えておく」であり、
この一連のタスクが構造で置き換えようとしているものそのもの**だから。

- → [`追加指示_vendor除外コメントの断定_2026-09-05.md`](追加指示_vendor除外コメントの断定_2026-09-05.md)（**`実装待ち`**）
- ⭐ **本タスクの承認は条件付きではない。** 追加指示は**別タスク**で、⭐ **合格判定を覆すものではない**
  （承認記録 §7 の「仕上げ3点」と同じ扱い）

---

## 5. Q3（開発担当の P1・`shared/` が今後増えること）への回答

⭐ **報告 §6 の理解は正しい。** 承認記録 §4-1-4 のとおり、
**他人の配布物が `shared/` に増えて赤くなるのは fail-closed ＝意図どおり**で、
**人が気づいて `LINEREF_EXCLUDE` に足す**運用でよい。

⚠️ **1点だけ補正する**（レビュー担当の補足と同じ）:

- ⚠️ **「赤くなる」のは、その配布物が実際に `<file>.js:<行番号>` を含むときだけ**である
- ⭐ **含まなければ、静かに走査対象が増えるだけ**——⭐ **ただし成功行のファイル数が動くので気づける**
  （⚠️ **これが D1「成功行に走査ファイル数を足した」逸脱を追認した価値**であり、
  ⭐ **本件はその価値が早くも効いた実例**になった）

---

## 6. ⚠️ ⭐ 索引の是正 — **§5 に腐った行が2つあった**

⭐ **レビュー担当の Q2（索引の該当行が `実装待ち` のまま）は正しかったが、⚠️ 腐りはそれ1行では済まなかった。**
設計担当が §5 を全部読み直したところ、**もう1行**が古かった:

| 行 | 索引の記載 | 実際 |
|---|---|---|
| **行番号参照の検査の仕上げ** | `実装待ち` | ⭐ **本承認で完了** |
| ⚠️ ⭐ **日本語モードの0件表示** | `実装待ち` | ⚠️ ⭐ **2026-09-04 に承認まで完了していた**（`a40dcdf2`・[`承認_2026-09-04.md`](../未採番-日本語モードの0件表示/承認_2026-09-04.md)） |

### ⭐ なぜ §5 だけが腐ったのか（＝構造の指摘）

⚠️ **`a40dcdf2` は索引の §1（起票案）と §4 を正しく更新しており、⭐ §5 だけを更新し忘れていた。**
⭐ **これは偶然ではない**:

> ✅ **`npm run validate` の起票案検査が見ているのは §1 だけ**（収録漏れと件数一致）。
> ⚠️ ⭐ **§5「いま着手できるタスク」には自動検査が一切無い。**
> ⭐ **だから §1 は更新され、§5 は黙って腐った。**

⚠️ ⭐ **これは §3（Q1）の CLAUDE.md と完全に同じ型である**——
**自動検査のある側は揃い、無い側だけがずれる**。⭐ **同じ日に2件観測した。**

⚠️ **§5 への自動検査は、いまは入れない**（§1 と違い、§5 の行は**外部に「状態行を持つファイル」という
突き合わせ先が無い**——完了の根拠は承認記録の有無だが、⭐ **承認記録のファイル名は規約化されていない**）。
⭐ **先に規約（承認記録は `承認_<呼び名>_<日付>.md`）を固めるのが順序で、検査はその後**。
→ ⭐ **本承認では観測の記録に留め、索引 §4 に注意書きを足す**。

---

## 7. 派生タスクの要否

⭐ **派生の起票案は不要。** ⚠️ **代わりに追加指示を1件だけ立てる**（§4-4）。

| 検討したもの | 判断 |
|---|---|
| **S1-A（`赤くなる` の断定）** | ⭐ **追加指示として `実装待ち`**（§4） |
| `LINEREF_ROOTS` と CLAUDE.md の集合一致検査 | ⚠️ **入れない。引き金は「2回目のずれ」**（§3） |
| 索引 §5 の自動検査 | ⚠️ **入れない。先に承認記録のファイル名規約を固める**（§6） |
| 指示 §5 のスコープ外4点（`.html` 拡大・コメント判定・`#L231` 形・除外設定機構） | ⚠️ **すべて承認記録 §4・§5 の判断のまま。実装にも入っていないことをレビューが実測済み** |

---

## 8. ハンドオフ

- **本タスク（行番号参照の検査の仕上げ）**: ⭐ **完了**。⚠️ **未pushのまま**＝ **`push待ち` 相当**
  （⭐ **本番公開の前提条件ではない**——`scripts/` は配信されるが **`validate` はサイトの表示を変えない**）
- **追加指示（vendor除外コメントの断定）**: ⭐ **`実装待ち`**（→ 索引 §5）
- ⚠️ **push・`wrangler pages deploy` は実行していない**（公開の可否は常にユーザーが判断する）
