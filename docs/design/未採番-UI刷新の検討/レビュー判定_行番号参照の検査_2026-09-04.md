**対象: 「行番号参照の是正と検査」** — この宣言は 2026-09-11 に遡って追記した（索引の転記ドリフト防止）

# レビュー判定（2026-09-04）— コード内の行番号参照を止める検査

⚠️ **これは issue コメントの代替。** GitHubアカウント停止中で `gh issue comment` / `gh issue edit` が
打てないため、**判定コメントとラベル付け替えをこのファイルで代行する**
（書式は同フォルダ `レビュー判定_意味統一_2026-09-03.md` に倣う。
⭐ **GitHub復旧後は本文をそのまま `gh issue comment <n> --body-file` で投函できる**）。

⭐ **状態: `設計確認待ち`（次は設計担当。最終承認とクローズは設計担当が行う）**

| 項目 | 値 |
|---|---|
| **判定** | **✅ 合格** |
| レビュー対象コミット | **`8e2c8782`**（`scripts/validate.mjs` の1ファイルのみ・+77/-1） |
| 仕様（正） | `docs/design/未採番-UI刷新の検討/承認_腐った行番号参照_2026-09-04.md` **§4**（要件1〜5・破壊試験 W1〜W4） |
| 実装報告 | 同フォルダ `実装報告_行番号参照の検査_2026-09-04.md`（報告コミット `70f8546f` / `e407919d`） |
| 検証環境 | `PORT=3100 npm run dev` ＋ Playwright 1.61.1（`--ignore-certificate-errors` / `ignoreHTTPSErrors: true`）。**終了時に停止済み**（`ss -ltnp` で 3100 の待受なしを確認）。⚠️ **3000番・3200番には触れていない** |
| 証跡 | `tmp/review/未採番-行番号参照の検査/`（スクリーンショット10枚・自作スクリプト3本。⚠️ gitignore・**他セッションの `tmp/` 配下は消していない**） |
| **不合格の指摘** | **0件** |
| 推奨基準（S）の指摘 | 2件（§4。合否には含めない） |
| 設計担当への確認事項 | 3件（§5。⭐ **うち Q1 はレビュー担当が範囲を実測し直した**） |

⚠️ ⭐ **本判定は実装報告の「✅」を根拠にしていない。**
**W1〜W4 はすべてレビュー担当が自分で仕込み直して再実行した**うえ、
⭐ **報告に無い破壊試験を4つ追加した**（全ルートの個別確認・改名時の fail-closed・
0ファイル時の fail-closed・**生成物の生成元テンプレート内**の検出）。

---

## 1. 必須基準（M1〜M7）

| 基準 | 結果 | 証跡 |
|---|---|---|
| **M1 設計適合** | ✅ | 承認 §4 の要件1〜5・破壊試験 W1〜W4 を全数再実行して一致（§2 の対応表） |
| **M2 実駆動** | ✅ | 検査自身を**自分で5回壊して**実行（§2）。サイト側も PC(1280px)・**375px** の両幅で操作し、`tmp/review/未採番-行番号参照の検査/*.png` 10枚 |
| **M3 回帰** | ✅ | `npm run validate` **exit 0**（§2 W1）。トップ / 詳細モーダル / カード個別ページ / デッキ構築 / 大会一覧を両幅で確認、**JSエラー0件**（§3） |
| **M4 生成物の同期** | ✅ | 生成ロジックを一切触っていない（差分は `scripts/validate.mjs` の1ファイル）。`git status --short` が空 |
| **M5 静かな劣化がない** | ✅ | ⭐ **本タスクの要点**。わざと壊して落ちることを**5系統**で確認し、**fail-open を1つも見つけられなかった**（§2） |
| **M6 公開安全性** | ✅ | 配信物の増減なし（新規ファイル0）。`_headers` / `404.html` / キャッシュの前提に変更なし。秘匿情報なし（§6） |
| **M7 スコープ** | ✅ | 承認 §4 が指定した `scripts/validate.mjs` のみ。逸脱3点は自己申告済みで、いずれも根拠あり（§2-3） |

⚠️ **M1-b（既存で効いている副作用が新条件で効かない）は非該当。**
本件は絞り込み・並び替えの追加ではないため、絵柄追従・共有URL・バッジ集計・件数表示のいずれも対象外。
⭐ 同型の観点として「**既存の各検査に効いている副作用が新検査に効いているか**」を見た:

| 既存検査に共通する副作用 | 新検査 | 判定 |
|---|---|---|
| 失敗時に `problems++` → **exit 1** | ✅ 効く（W2 で実測） | M充足 |
| 成功時に**1行出す** | ✅ 効く（`no line-number refs in code comments — 0件（45ファイル走査）`） | M充足 |
| ファイル冒頭のコメントに検査を列挙する | ❌ **効いていない**（新検査が未記載） | ⚠️ **S止まり** — `meta.rarities` `card-meta-index` の**既存2検査も未記載**＝この副作用は既存の同種条件でも効いていないため、M ではなく S（§4-1） |

---

## 2. 承認 §4 との対応（要件1〜5 / 破壊試験 W1〜W4）

### 2-1. 要件1〜5

| 要件 | 期待 | 実測 | 合否 |
|---|---|---|---|
| **1. 対象** | `scripts/` `shared/js/` `functions/` `app.js` `tools/` の `.js`/`.mjs`、`shared/vendor/` 除外 | ⭐ **5ルートすべてを個別に実証**（下記 W2′）。走査 **45ファイル**＝`scripts`20+`shared/js`4+`functions`17+`tools`3+`app.js`1（`find` の実測と一致） | ✅ |
| **2. 検出する形** | 実装報告 T2 の grep と同じ | 実装の正規表現を**ファイルから抜き出して**判定: `card-i18n.js:110` / `gen-tl-json.mjs:72` / `card-detail.js:231-323` は **HIT**、`card-i18n.js の bannedFormats()`（＝推奨する書き方）は **miss** | ✅ |
| **3. 失敗時** | exit 1 ＋「なぜだめか」「どう書くか」 | exit 1。`行番号は腐ります。関数名で参照してください（例: card-i18n.js の bannedFormats()）` ＋「新しい行番号に直すで済ませない」＋対象外の説明。**ファイル名と行番号を名指し**する | ✅ |
| **4. 成功時** | 1行出す（無言で通さない） | `no line-number refs in code comments — 0件（45ファイル走査）` | ✅ |
| **5. `docs/` 対象外の理由をコードのコメントに書く** | 理由つきで明記 | 「その時点の実測値を記録する文書」「実測278件」「⚠ `docs` も見たほうがよいのでは で広げないこと」を明記 | ✅ |

⭐ **要件2の裏取り**: レビュー担当が**独立の grep**（承認 §4 と同じ式）で走査対象を数え、**0件**を確認した
（＝検査の緑が「見ていないから0」ではないことを、実装と別経路で確かめた）。
`docs/` は同じ式で **290件**（承認 §4 は321件・報告は278件）。⚠️ **3者とも数が違うが、これは
`docs/` が日々増えるためで、3者いずれの数でも「`docs/` を含めたら即赤」という結論は変わらない**。

### 2-2. 破壊試験（⭐ 報告の再現ではなく、レビュー担当が仕込み直したもの）

| # | 内容 | 期待 | 実測 | 合否 |
|---|---|---|---|---|
| **W1** | そのまま `npm run validate` | exit 0 ＋ 成功行 | ✅ `EXIT=0` / `no line-number refs in code comments — 0件（45ファイル走査）` | ✅ |
| **W2′** | ⭐ **報告より強い版**: 5ルート**すべて**に同時に `zzprobe.js:123` を仕込む（`scripts/lib/` `shared/js/` `functions/api/` は**入れ子**、`tools/print/` は **`.mjs`**、`app.js` は**単一ファイルのルート**） | exit 1・**5件すべて名指し** | ✅ `EXIT=1`・`行番号参照: 5件` と `scripts/lib/…:1` `shared/js/…:1` `functions/api/…:1` `app.js:1005` `tools/print/….mjs:1` の5行 | ✅ |
| **W3** | `docs/` 配下にあっても緑 | exit 0 | ✅ 実在290件＋**自分で置いた `docs/design/待ち行列/__review_probe.md`** があっても `EXIT=0` | ✅ |
| **W4** | `shared/vendor/` の除外 | 除外されている | ✅ **対照実験を自分で再実行**（下記） | ✅ |

**W4 の対照実験**（⭐ 「今はルートが届かないから通っているだけ」ではないことの直接証拠）:

| 条件 | 結果 |
|---|---|
| `shared/vendor/__review_probe.js` を置く（ルート既定） | `EXIT=0` / `0件（45ファイル走査）` ＝ 走査対象外 |
| ルートを `shared` に広げる（**除外あり**） | `EXIT=0` / **`0件（46ファイル走査）`** ＝ 除外が効き vendor だけ飛ぶ（46 = 45 ＋ `shared/pdf/card-sheet.js`） |
| さらに `LINEREF_EXCLUDE = []`（**除外なし**） | **`EXIT=1`** / `shared/vendor/__review_probe.js:1 — zzprobe.js:123` ＝ 除外が無ければ拾う |

**⭐ レビュー担当が追加した破壊試験（報告に無いもの）**:

| # | 内容 | 期待 | 実測 | 合否 |
|---|---|---|---|---|
| **R1** | ⭐ **ルートが改名された**（`shared/js` → `shared/js-RENAMED`） | fail-open にしない | ✅ **exit 1** / `shared/js-RENAMED を読めません: ENOENT … — 走査対象の指定が陳腐化しています` | ✅ |
| **R2** | ⭐ **走査対象が0ファイル**（ルートを `migrations` に） | fail-open にしない | ✅ **exit 1** / `走査対象の .js / .mjs が1つもありません — 走査対象の指定が陳腐化しています` | ✅ |
| **R3** | ⭐ **生成物の生成元**（`build-card-pages.mjs` の `CARDS_JS` **テンプレートリテラル内**に仕込む） | 検出される | ✅ **exit 1** / `scripts/build-card-pages.mjs:766 — zzprobe.js:777`（§5-1 の根拠） | ✅ |
| **R4** | 検査自身が自分の正規表現に引っかからないか | 引っかからない | ✅ W1 が緑（`\.(?:js\|mjs):\d+` は `:` の直後が数字でないため自己一致しない） | ✅ |

⚠️ **すべて確認後に復元した。** `sha256sum -c` で `app.js` / `scripts/validate.mjs` /
`scripts/build-card-pages.mjs` が**バイト一致で戻ったこと**を確認し、`git status --short` が
**空**であることまで確認している（⚠️ `git checkout` / `restore` / `stash` は**一度も使っていない**）。

### 2-3. 逸脱3点（報告 §5）の扱い — ⭐ **3点とも妥当。合格を妨げない**

| # | 逸脱 | レビュー担当の判断 |
|---|---|---|
| **D1** | 成功行に走査ファイル数を足した | ⭐ **妥当**。承認 §4 要件4の目的は「検査が動いていない」と「0件」の区別で、**ファイル数はその目的を強める**（R1・R2 で実際に数字とexitが動くことを確認）。例示の文字列もそのまま含む |
| **D2** | ルート読めない・0ファイルを exit 1 に | ⭐ **妥当。むしろ本タスクの目的に沿う**。R1・R2 で実測。⚠️ この逸脱が無ければ**改名で黙って空回りする**＝ M5 で不合格になっていた |
| **D3** | エラー出力を先頭20件で打ち切り | ⭐ **妥当**。総件数は必ず出るので情報は欠けない（W2′ の5件は全数出た） |

---

## 3. M3 回帰の詳細（主要画面）

`PORT=3100 npm run dev` ＋ Playwright で **PC(1280×900)** と **375×800** の両方を操作した。

| 画面 | PC | 375px | 証跡 |
|---|---|---|---|
| トップ（検索・タイル） | ✅ `50 件を表示 / 全 2495 件`・タイル50 | ✅ 同じ | `top-pc.png` / `top-375.png` |
| カード詳細モーダル | ✅ `#gacd-modal` が 1280×900 で可視・`#d-name`=自己放棄 | ✅ 375×800 で可視 | `modal-pc.png` / `modal-375.png` |
| カード個別ページ | ✅ `/cards/academy-attendant/` h1=「学院の付き人 / Academy Attendant」 | ✅ 同じ | `cardpage-*.png` |
| デッキ構築 | ✅ `#boot-status`=「読み込み中…」 | ✅ 同じ | `deck-*.png` |
| 大会一覧 | ✅ 454行 | ✅ 同じ | `tournaments-*.png` |

- ⚠️ デッキ構築が「読み込み中…」で止まるのは **Discord OAuth 未ログインのため既知**（CLAUDE.md 記載）。本変更とは無関係
- **JSエラー: 0件**（`pageerror` を両幅で監視）
- ⭐ そもそも本コミットは `scripts/validate.mjs` の1ファイルで、**ブラウザが読むファイルを1バイトも変えていない**。上記は「変えていないこと」の確認として実施した

---

## 4. 推奨基準（S）の指摘 — ⭐ **合格を妨げない**

### S1-1 `validate.mjs` 冒頭の検査一覧に新検査が載っていない

- **該当**: `scripts/validate.mjs` の冒頭コメント（`import` より前のブロック）
- **内容**: 冒頭コメントは各検査を「さらに…」「加えて…」「最後に…」と列挙しているが、**新検査が載っていない**。
  現状は「最後に、…用語ハイライト…」の**前に**新検査が挿入された形で、列挙と実際の順序がずれている
- ⚠️ **M ではない理由**: 同じ副作用が **`meta.rarities`・`card-meta-index` の既存2検査でも効いていない**
  （どちらも冒頭コメントに未記載）。**既存の同種条件でも効いていない＝新規要望**なので S に置く
  （レビュー基準 M1-b の但し書きに従った）
- **提案**: 1行足すだけ。対応要否は設計担当の判断待ち

### S1-2 検出できるのは `file.js:123` の形だけ

- **内容**: `card-detail.js#L231` や `card-i18n.js の 110行目` のような**同じく腐る書き方**は検出しない
  （実装の正規表現を抜き出して確認済み）
- ⭐ **現状の実害は無い**: 走査対象での実測は **0件**
- ⚠️ **承認 §4 要件2が「T2 の grep と同じもの」と明示しているので、実装は仕様どおり**。
  広げるかは仕様の判断（§5 の Q3 と同種）。**先回りしないという実装側の判断に賛成する**

---

## 5. 設計担当への確認事項

⭐ **いずれも合否を左右しない**（実装は承認 §4 の字面に忠実で、現状のヒットはすべて0件）。
本ファイルが `設計確認待ち` へ渡るので、そこで判断を仰ぐ。

### Q1（報告 §6-Q1）走査対象外の自製コードは **`shared/pdf/card-sheet.js` の1本だけ** — ⭐ レビュー担当が範囲を実測し直した

開発担当は `shared/pdf/card-sheet.js` の1件を挙げていたが、**それが全部なのかは書かれていなかった**ので、
リポジトリ全体を数え直した。

| 走査対象外の `.js`/`.mjs`（`node_modules` `tmp` `.git` `docs/` `data/` `shared/vendor/` を除く） | 素性 | 実質の被覆 |
|---|---|---|
| `cards/cards.js` | **生成物**（`build-card-pages.mjs` の `CARDS_JS`） | ⭐ **被覆されている**（R3 で実証: 生成元テンプレート内の参照を **exit 1** で検出した） |
| `tournaments/tournaments.js` / `tournaments/deck.js` | **生成物**（`build-tournament-pages.mjs` の `LIST_JS` / `DECK_JS`） | ⭐ **被覆されている**（同上。検査は行単位の素の文字列一致なので、テンプレートリテラル内も見る） |
| **`shared/pdf/card-sheet.js`** | ⭐ **手書きの自製コード** | ⚠️ **被覆されていない**（現在のヒットは **0件**） |

⭐ **結論: 実質の穴は `shared/pdf/card-sheet.js` の1本のみ**で、開発担当の申告と一致する。
広げるなら `LINEREF_ROOTS` の `"shared/js"` を `"shared"` にするだけで済むことは、
**レビュー担当が W4 の対照実験で実測した**（46ファイル走査・緑・vendor は除外が守る）。
⚠️ **広げるかどうかは仕様の判断**なので、実装しなかった開発担当の判断は正しい。

### Q2（報告 §6-Q2）`tools/` の `.html` / `.css` が対象外

- 実測: `tools/deck-builder/index.html` `tools/glossary/index.html` `tools/print/index.html` は
  ⭐ **インライン `<script>` を1つも持たない**（`grep -c '<script>'` が3ファイルとも **0**）。
  外部 `.js` を読むだけなので、**現状の実害は無い**
- ⚠️ 承認 §4 要件1が `.js`/`.mjs` と明示しているため、**広げなかった実装は仕様どおり**

### Q3（報告 §6-Q3）「コメント内」に限定していない

- 承認 §4 要件2が「T2 の grep と同じもの」なので**仕様どおり**。現状の誤検知 **0件**
- ⭐ **むしろ限定しないほうが良い場面がある**ことを実測で確認した: R3（生成元の**テンプレートリテラル内**）は
  「コメントだけを見る」実装だと**取り逃がす**。⚠️ **コメント判定を入れるべきではない**というのがレビュー担当の見解

---

## 6. M6（公開安全性）の内訳

| 観点 | 実測 |
|---|---|
| 配信物の増減 | **増減なし**（新規ファイル0・削除0。差分は `scripts/validate.mjs` の1ファイルのみ） |
| ⚠️ `scripts/` は本番配信される（CLAUDE.md） | ✅ 追加されたのは**検査コードとコメントだけ**で、秘匿情報・トークン・内部URLを含まない |
| `_headers` / `404.html` / キャッシュ | **未変更** |
| 担当パス | `git -c core.quotepath=false show --name-only --format= 8e2c8782` ＝ `scripts/validate.mjs` のみ |

⚠️ **push・`wrangler pages deploy` は実行していない**（レビュー担当は本番公開に一切触れない）。
⭐ 承認 §6 のとおり**本番公開の前提条件ではない**（サイトの表示を変えない）。

---

## 7. ハンドオフ

⭐ **`設計確認待ち`**（GitHub復旧後は `gh issue edit <n> --remove-label レビュー待ち --add-label 設計確認待ち`）。

- **不合格の指摘は0件。** 最終承認とクローズは設計担当が行う
- 設計担当に見てほしいのは **§5 の Q1〜Q3**（とくに ⭐ **Q1: 実質の穴は `shared/pdf/card-sheet.js` 1本**）と **§4 の S 2件**
- ⚠️ **未pushのまま**。承認後は `push待ち` 相当（本番公開の可否は常にユーザーが判断する）
