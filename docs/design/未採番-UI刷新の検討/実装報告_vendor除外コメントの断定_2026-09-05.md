# 実装報告 — `shared/vendor` 除外コメントの**断定を外す**（2026-09-05）

⚠️ **GitHubアカウント停止中のため、issueコメント（実装報告）の代わりにこのファイルを置く。**
復旧後に `gh issue comment <n> --body-file` でそのまま投函する
（索引 [`待ち行列と復旧手順.md`](../待ち行列/待ち行列と復旧手順.md) §2-④）。

- 呼び名: **「vendor除外コメントの断定」**（記号で呼ばない → 索引 §4-4）
- 指示（正）: [`追加指示_vendor除外コメントの断定_2026-09-05.md`](追加指示_vendor除外コメントの断定_2026-09-05.md)
- 由来（判断の根拠の正）: [`承認_行番号参照の検査の仕上げ_2026-09-05.md`](承認_行番号参照の検査の仕上げ_2026-09-05.md) **§4**（レビューの S1-A）
- ⚠️ **本タスクは前タスクの承認を覆すものではない**（その上に積む小タスク）

---

## 1. 実施内容

| 項目 | 内容 |
|---|---|
| **実装コミット** | ⭐ **`ee97c6c776a139ae4b77d1d75975469455531d22`**（短縮 **`ee97c6c7`**）「未採番(vendor除外コメントの断定): 反証できる断定を外す」 |
| 親コミット | `14734d8d`（未採番(行番号参照の検査の仕上げ): 承認 + Q1是正 + S1-A採用で追加指示1件） |
| 触ったファイル | ⭐ **`scripts/validate.mjs` の1ファイルのみ**（+3 / -3・**コメント3行の置換だけ**） |
| **push** | ⚠️ ⭐ **していない**（`wrangler pages deploy` も**していない**）。公開の可否は常にユーザーが判断する |
| 報告コミット | 本ファイル（`docs/` のみ・別コミット） |

### 手順

1. 変更前の状態を実測（`npm run validate` exit 0・`0件（46ファイル走査）`、`grep -n '赤くなる'` **5件**でうち置換対象が
   `LINEREF_EXCLUDE` 直前の1件だけであることを**目視で確認**）
2. 指示 §2 の**変更前3行**を**変更後3行**にそのまま置換（`str.replace` で**出現数1件を assert してから**適用）
   - ⚠️ ファイルの書き込みは**一時ファイル + `os.replace()`** で行った
     （CLAUDE.md「環境の注意」の**VS Codeで開いたままのファイルは上書きに失敗する**への対処）
3. V1〜V8 を実施（V5 は**実際にファイルを壊して確認し、確認後に復元**）
4. `git add scripts/validate.mjs`（⚠️ **`git add -A` は使っていない**）→ コミット

### ⭐ 変更の中身（実際の差分）

```diff
-// ⚠ shared/vendor/ も対象外。配布物にソースマップ由来の記述が入りうるうえ、自分では直せない。
-//   ルートが shared/ なのでこの除外は実際に効いている（外すと shared/vendor/ の配布物を拾って
-//   赤くなる）。整理目的で消さないこと。
+// ⚠ shared/vendor/ も対象外。配布物にソースマップ由来の記述が入りうるが、自分では直せない。
+//   ルートが shared/ なので、この除外を外すと配布物が走査対象に入る（成功行のファイル数が動く）。
+//   整理目的で消さないこと。
```

⭐ **指示 §2 の「変更後」と1文字も違わない**（コピー元は指示書のコードブロック）。

---

## 2. 検証結果（指示 §4 の V1〜V8）

⭐ **全8項目とも期待値どおり。**

| # | 内容 | 期待値 | ⭐ 実測 | 判定 |
|---|---|---|---|---|
| **V1** | `npm run validate` | exit 0・`no line-number refs in code comments — 0件（46ファイル走査）`・`all checks passed.`・成功行10本 | ⭐ **exit 0**・`no line-number refs in code comments — 0件（**46**ファイル走査）`・`all checks passed.`・**成功行10本**（下記） | ✅ |
| **V2** | `grep -c '配布物を拾って' scripts/validate.mjs` | **0**（実施前 1） | ⭐ **0**（実施前が **1** であることも実測） | ✅ |
| **V3** | `grep -c '赤くなる' scripts/validate.mjs` | ⚠️ **4**（0ではない。実施前 5） | ⭐ **4**（実施前 **5**。残る4件は**別の検査の説明**＝下表で中身を確認） | ✅ |
| **V4** | `grep -c '走査対象に入る' scripts/validate.mjs` | **1**（実施前 0） | ⭐ **1**（実施前 **0**） | ✅ |
| **V5** | `LINEREF_EXCLUDE` を一時的に `[]` にして `npm run validate` | **exit 0** のまま・`0件（**47**ファイル走査）` | ⭐ **exit 0**・`no line-number refs in code comments — 0件（**47**ファイル走査）`・`all checks passed.` ⚠️ **`赤くなる`（exit 1）にはならなかった** | ✅ |
| **V6** | 差分の形 | `scripts/validate.mjs` の **+3 / -3**・コメントのみ | ⭐ **`1 file changed, 3 insertions(+), 3 deletions(-)`**。⭐ **差分はコメント3行だけでロジックに差分なし**（`LINEREF_ROOTS` / `LINEREF_EXCLUDE` / `REF_RE` / 走査 / fail-closed は**すべて未変更**） | ✅ |
| **V7** | 担当パス | `scripts/validate.mjs` の1行だけ | ⭐ `git -c core.quotepath=false show --name-only --format= ee97c6c7` ＝ **`scripts/validate.mjs`** の**1行のみ**（`^scripts/` 以外は **0行**） | ✅ |
| **V8** | 生成物に差分なし | `git status --short` に `scripts/validate.mjs` 以外が出ない | ⭐ コミット前 **`M scripts/validate.mjs` の1行のみ**、コミット後 **空**（報告ファイルの `docs/` は本コミットの後） | ✅ |

### V1 の成功行10本（変更前後で**同一**）

```text
generated JSON up to date — data/tl-names.json / data/tl-effects.json
top set links up to date — index.html に /sets/ リンク 63本
meta.sets covers all sets — 63件
meta.subtypes covers all subtypes — 156種
meta.rarities covers all rarities — 9種（スナップショット 2495枚から観測）
card-meta-index sort keys OK — 2516slug / level 133 / power 1006 / life 858 / cost_memory 515 / flip 22
cron git add paths in sync — 8パス（README.md / CLAUDE.md）
issue drafts in sync — 起票案24件（生きている13 / 完了10 / 取り下げ1）
no line-number refs in code comments — 0件（46ファイル走査）
card term highlights up to date — 2495 ページ / 2517 節
```

⭐ **`loaded OK — total cards: 2516` は検査の成功行ではなく読み込みの報告**なので上の10本に数えていない
（冒頭コメントの検査一覧の記載順とも一致する＝前タスク #2 の状態が保たれている）。

### ⚠️ ⭐ V1 の「46」の裏取り（指示 §4 の脚注）

```
find scripts shared functions tools app.js \( -name '*.js' -o -name '*.mjs' \) -not -path 'shared/vendor/*' | wc -l
→ 46
```

⭐ **設計担当の実測値と一致した。前提は変わっていない。**

### ⚠️ ⭐ V3 — 残した4件の中身（**取り違えていないことの証跡**）

```
467:  //   ＝その日の変更と無関係に時計だけで赤くなる検査にしない）
528:  //   「見出しが N個 あります」で赤くなる。
592://   増える＝固定値として書く意味が無い）。ここへ広げると即座に赤くなるだけで、コード側の
668://   突き合わせない（両辺が同源になり常に通る）。この検査が赤くなるのは主に次の3つ:
```

| 行 | 何の説明か | 判断 |
|---|---|---|
| 467 | **起票案の状態行**の検査で、**日付の鮮度は見ない**理由 | ⚠️ **無関係**＝残す |
| 528 | **起票案の索引見出し**の正規表現を**緩めると壊れる**警告 | ⚠️ **無関係**＝残す |
| **592** | ⚠️ ⭐ **`docs/` を走査対象に含めない理由**（「広げると即座に赤くなるだけ」） | ⚠️ ⭐ **今回の置換対象と同じコメント塊の数行上**にある。指示 §4 の注意どおり**取り違えず残した** |
| 668 | **用語ハイライト検査**が赤くなる主な3原因の前置き | ⚠️ **無関係**＝残す |

⭐ **置換前の5件目（旧 596行）だけが消え、上の4件は行の中身も位置関係も変わっていない。**

### ⚠️ V5 の復元の確認（指示 §4 の但し書き）

- `LINEREF_EXCLUDE` を `["shared/vendor"]` に戻したあと、⭐ **破壊前に取ったバックアップと `diff -q` で
  バイト一致（`IDENTICAL`）**することを確認した
- `git status --short` が ⭐ **`M scripts/validate.mjs` の1行だけ**に戻ったことを確認した
- 復元後に `npm run validate` を再実行し、⭐ **exit 0・`0件（46ファイル走査）`** に戻ることを確認した

---

## 3. 実行できなかった検証と代替手段・残っている確認

⭐ **指示 §4 の V1〜V8 はすべて実行できた。代替手段を使った項目は無い。**

| 項目 | 状況 |
|---|---|
| Playwright での実駆動 | ⭐ **不要**（本件は `scripts/validate.mjs` の**コメントだけ**の変更で、**サイトの表示・生成物・ブラウザ挙動を一切変えない**） |
| 生成物の再生成（`gen-tl-json` / `build:cards` / `gen-card-meta-index`） | ⭐ **不要**（訳データも `meta.sets` も生成ロジックも触っていない）。⚠️ **`npm run validate` が exit 0 で、陳腐化の検査もすべて通っている**ことで裏取りした |
| **本番での確認**（`curl -sI` 等） | ⚠️ ⭐ **していない・不要**。⭐ **pushしていない**うえ、`validate` は**サイトの表示を変えない**（`scripts/` は配信されるが実行はされない） |
| **残っている確認** | ⭐ **無し**。⚠️ ただし ⭐ **本コミットは未pushのまま積み上がっている**（合格・承認後は **`push待ち` 相当**） |

---

## 4. 差分が設計書の想定と食い違った点

⭐ **食い違いは無い。**

- 指示 §4 V6 の想定「**+3 / -3**」＝実測 `3 insertions(+), 3 deletions(-)`
- 指示 §4 V1 の想定「**46ファイル走査**」＝実測 46（`find` の式でも独立に 46）
- 指示 §4 V5 の想定「**47ファイル走査・exit 0**」＝実測どおり
- 指示 §4 V3 の想定「**5 → 4**」＝実測どおり（⚠️ **0 ではない**）

⚠️ ⭐ **ロジックへの差分は1文字も出ていない**（V6）。指示 §3 が「出たら逸脱として報告」と定めた事象は**発生していない**。

---

## 5. 設計書からの逸脱と根拠

⭐ **逸脱は無い。**

| 指示 §3 のスコープ外 | 実装 |
|---|---|
| `LINEREF_EXCLUDE` を増やす／減らす | ⭐ **`["shared/vendor"]` のまま**（V5 で一時的に `[]` にしたが**確認後にバイト一致で復元**し、コミットには含まれない） |
| `LINEREF_ROOTS` を触る | ⭐ **`["scripts", "shared", "functions", "app.js", "tools"]` のまま**（差分に出ていない） |
| 他の「赤くなる」4箇所を直す | ⭐ **1件も触っていない**（§2 の V3 の表で中身まで確認） |
| 冒頭コメントの検査一覧を触る | ⭐ **触っていない**（差分は 591〜599 行の範囲のみ） |
| 検査ロジック（`REF_RE`・走査・fail-closed）を触る | ⭐ **触っていない** |

### 既存表記との照合

- ⭐ **コメントの体裁は既存に合わせた**——`// ⚠ ` で始まり、続き行は `//   `（スペース3つ）で字下げする既存の形を保っている
- ⭐ **記号の使い分けも既存どおり**（コード内コメントは `⚠`（VS16なし）で、`⚠️` ではない）。⚠️ **指示 §2 の
  コードブロックがそのまま `⚠` を使っていたため、コピーで自然に揃っている**
- ⭐ **`TRANSLATION.md` は無関係**（翻訳データを触っていない）
- ⚠️ ⭐ **前タスクの N1（折り返しの組み直し）と同型の判断は今回**不要**だった**——指示 §2 が
  **3行のまま**の完成形を与えているので、折り返しを自分で決める余地が無い

---

## 6. 設計担当への確認事項

⭐ **判断を仰ぐ必要のある事項は無い。** 以下は**確認事項ではなく所見**（回答不要）:

### P1（所見）— ⭐ V5 は「文言が事実であること」の直接の証拠になっている

⭐ **新しい文言の2つの主張が、V5 で同時に裏取りできた**:

| 新しい文言 | V5 の実測 |
|---|---|
| 「この除外を外すと配布物が**走査対象に入る**」 | ⭐ 走査ファイル数が **46 → 47** に増えた（`shared/vendor/jspdf.umd.min.js` の1件） |
| 「（**成功行のファイル数が動く**）」 | ⭐ 成功行の `（46ファイル走査）` が `（47ファイル走査）` に**実際に動いた** |

⚠️ ⭐ **同時に、旧文言の「赤くなる」が今日は成立しないことも再現した**（exit 0 のまま）。
⭐ **承認記録 §4-1 の設計担当の実測・レビュー担当の R1 と、独立に同じ結果になった。**

### P2（所見）— ⚠️ 新しい文言も「守っているのはコメントだけ」である点は変わらない

⭐ **`LINEREF_EXCLUDE` から `shared/vendor` が消えたことを検出する自動検査は、依然として無い。**
⚠️ **ただし本件で害の向きは変わった**——消しても**赤くならない**のは前と同じだが、
⭐ **コメントが「赤くなる」と嘘をつかなくなったので、試した人が「嘘だから消してよい」と
誤った確信を得る経路が消えた**（＝指示 §1 が狙ったとおり）。
⚠️ **検査を足す／足さないの判断は設計担当の領分**なので、⭐ **ここでは観測の記録に留める**
（承認記録 §3 が `LINEREF_ROOTS` について「引き金を先に決めておく」形を採ったのと同じ扱いが妥当と考える）。

---

## 7. ハンドオフ

- ⭐ **`レビュー待ち`**（GitHub復旧後は `gh issue edit <n> --remove-label 実装待ち --add-label レビュー待ち`）
- ⚠️ ⭐ **レビュー対象のコミットは `ee97c6c7`**（`scripts/validate.mjs` の +3 / -3）
- ⚠️ **push・`wrangler pages deploy` は実行していない**
