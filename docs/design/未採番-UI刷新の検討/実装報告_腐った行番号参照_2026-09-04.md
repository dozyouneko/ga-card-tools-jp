**対象: 「行番号参照の是正と検査」** — この宣言は 2026-09-11 に遡って追記した（索引の転記ドリフト防止）

# 実装報告 — 腐った行番号参照を関数名参照へ（残り4件・2026-09-04）

- 対象: 設計担当の指示書 [`追加指示_腐った行番号参照_2026-09-04.md`](追加指示_腐った行番号参照_2026-09-04.md)
- 前提: [`実装報告_S1-1_2026-09-04.md`](実装報告_S1-1_2026-09-04.md) §6-1（残り2件の発見）→ 設計判断 **(a) 直す・範囲はリポジトリ全4件**
- 担当: 開発（実装）担当セッション
- ⚠️ **GitHubアカウント停止中で `gh` が403のため、issueコメントの代わりにこのファイルが報告書**

---

## 0. 結論

⭐ **完了。指示書の4箇所すべてを関数名参照に是正した。T1〜T5 は全て合格。**

- **コメントのみの変更で、実行行の差分は0件**。生成物にも差分は出ない（§4）
- ⚠️ **push・`wrangler pages deploy` はしていない**（本番公開はユーザーの判断）
- 作業ツリーはコミット後にクリーン

---

## 1. 実施内容

### コミット

| コミット | 内容 |
|---|---|
| ⭐ **`895bade2`** | 未採番(意味統一): 腐った行番号参照の残り4件を関数名参照に是正 |

**変更ファイルは3つ**（`git -c core.quotepath=false show --name-only --format= 895bade2`）:

```text
scripts/gen-card-meta-index.mjs
scripts/gen-tl-json.mjs
tools/deck-builder/app.js
```

- 差分は **3ファイル / 5挿入 5削除**（うち2行は同じコメント文の折り返し行）
- **担当パス外の混入なし**（上記を `grep -vE '^(scripts/|tools/)'` して空を確認）
- **push: なし**

### 変更の中身（4箇所）

| # | 場所 | 変更前 | ⭐ 変更後 |
|---|---|---|---|
| 1 | `scripts/gen-card-meta-index.mjs:45` | `（card-i18n.js:110 と同義）` | `（shared/js/card-i18n.js の bannedFormats() と同義）` |
| 2 | `scripts/gen-card-meta-index.mjs:118` | `（card-search.js:396 の \`if (!entry) return true;\` により…）` | `（shared/js/card-search.js の metaMatches() の \`if (!entry) return true;\` により…）` |
| 3 | `scripts/gen-tl-json.mjs:14` | `使うため(card-detail.js:200)。` | `使うため(shared/js/card-detail.js の render() のフレーバー描画)。` |
| 4 | `tools/deck-builder/app.js:1426` | `トップページの app.js:64 と同じ規則。` | `トップページの app.js の preferredArtIndex() と同じ規則。` |

- ⭐ **指示書 §3 のとおり「新しい行番号に直す」はしていない**。すべて関数名参照
- ⚠️ **コメントの意味は変えていない**（#29 由来の経緯付きコメント＝ #2 も、指し先の表記だけを差し替え）
- ⭐ **S1-1 と違い、再発防止の注意書き（`⚠ 参照先は行番号ではなく関数名で書く`）は足していない。**
  同ファイルには S1-1 で既に1行入っており、指示書 §5 が「コメントの内容の書き換え・整理」を
  スコープ外としているため（**指し先の表記だけ**に厳密に留めた）

---

## 2. ⭐ 参照先の裏取り（指示書 §3「鵜呑みにせず自分で確かめる」）

**4件すべて `git log -S` で導入コミットを特定し、`git show <commit>:<file>` で
当時その行番号にあった実物を読んでから書いた**（推測で埋めていない）。

| # | 参照文字列 | 導入コミット | ⭐ 当時その行にあった実物 | 判定 |
|---|---|---|---|---|
| 1 | `card-i18n.js:110` | `77a24a84`（#27） | `:110` = **`function bannedFormats(card) {`** | ✅ 設計担当の実測どおり |
| 2 | `card-search.js:396` | `241bb48b`（#30 #29） | `:396` = **`if (!entry) return true;`**（`metaMatches()` の本体・`:394` が関数宣言） | ✅ 設計担当の実測どおり |
| 3 | `card-detail.js:200` | `9e835e56`（#22 フェーズ2） | ⚠️ `:200` = **`bannedEl.className = …`（使用禁止バナー）**。フレーバーは **`:219` の `const flavor = (t && t.flavor) || card.flavor;`** | ⚠️ **導入時点で既にずれていた**（下記） |
| 4 | `app.js:64` | `521d7392`（#41） | `:64` = **`function preferredArtIndex(imgs) {`** | ✅ 設計担当の実測どおり |

### ⚠️ #3 だけは「腐った」のではなく **最初から19行ずれていた**

⭐ **1・2・4 は導入時点では正しく、その後のコミットで腐ったもの**（＝指示書の想定どおり）。
一方 **#3 は導入コミット `9e835e56` の時点で既に別の場所を指していた**（フレーバーは `:219`、
`:200` は使用禁止バナー）。⚠️ **行番号参照は「腐る」だけでなく「最初から合っていない」型もある**
＝指示書 §1 の「この書き方は必ず腐る」という判断をさらに補強する材料。

- ただし**コメントの意図は一意**（`詳細ダイアログが日本語フレーバー(t.flavor)も使うため`）なので、
  **「詳細ダイアログがフレーバーを読む箇所」**を指す先として確定させた
- 現在の実装ではその箇所は **`shared/js/card-detail.js` の `render()` 内 `:305` の `flavorOf(card, t)`**。
  ⚠️ ⭐ **`flavorOf()` は `card-i18n.js` に集約された関数で `card-detail.js` には無い**ため、
  **参照先は同ファイルに実在する `render()` にした**（指示書の「実測 `:305` の `flavorOf(card, t)` 周辺」と
  同じ場所を、T3 が検査できる形＝**そのファイルに実在する関数名**で書いた）

---

## 3. 検証結果（指示書 §4 の T1〜T5）

| # | 期待値 | 方法 | 結果 |
|---|---|---|---|
| **T1** | 4箇所すべてが関数名参照（行番号を含まない） | `git diff -U0` の追加行を目視 | ✅ **合格**（§1 の表の4件。追加行に `:数字` は無い） |
| **T2** | 指定 grep の**ヒットが0件** | 下記コマンド | ✅ **合格 — 実施前 `4` 件 → 実施後 `0` 件**（⭐ **実行して数えた**） |
| **T3** | 各参照先の関数が実在する | `grep -n "function <名前>" <ファイル>` | ✅ **合格**（4つとも1件以上ヒット。下表） |
| **T4** | `npm run validate` が exit 0 | 実行 | ✅ **合格**（`all checks passed.` / `EXIT=0`） |
| **T5** | 生成物に差分が出ない | `node scripts/gen-tl-json.mjs` → `git status --short` | ✅ **合格**（生成物の行が1つも出ない） |

### T2 — ⭐ 実行して数えた結果

```bash
grep -rnoE '[A-Za-z0-9_.-]+\.(js|mjs):[0-9]+(-[0-9]+)?' scripts/ shared/js/ functions/ app.js tools/
```

| 時点 | ヒット数 | 内訳 |
|---|---|---|
| **実施前** | **4件** | `gen-card-meta-index.mjs:45` / `gen-card-meta-index.mjs:118` / `gen-tl-json.mjs:14` / `deck-builder/app.js:1426` |
| ⭐ **実施後** | ⭐ **0件** | （出力なし） |

⭐ **指示書の「期待ヒット数は実行して確かめてから書くこと」に従い、実施前後の両方を実測して記載した。**
実施前の4件は指示書 §2 の表と**完全に一致**（＝直す対象を取りこぼしていない）。

### T3 — 参照先の実在確認

| 参照先 | 検査コマンド | 結果 |
|---|---|---|
| `card-i18n.js` の `bannedFormats()` | `grep -n "function bannedFormats" shared/js/card-i18n.js` | ✅ `:176` |
| `card-search.js` の `metaMatches()` | `grep -n "function metaMatches" shared/js/card-search.js` | ✅ `:648` |
| `card-detail.js` の `render()` | `grep -n "function render" shared/js/card-detail.js` | ✅ `:231`（他に `renderAction` `renderTerms` 等） |
| `app.js` の `preferredArtIndex()` | `grep -n "function preferredArtIndex" app.js` | ✅ `:83` |

⭐ **新しい腐った参照を作っていない**（＝書いた先が実在する）。

### 追加で実施した検証（指示書に無いが、S1-1 と同じ形で確認したもの）

| # | 検証項目 | 方法 | 結果 |
|---|---|---|---|
| A | **実行行を変えていない** | `git diff -U0` の変更行から `//` 行を除いた件数 | ✅ **0件** |
| B | 構文が壊れていない | `node --check` を3ファイルすべてに実行 | ✅ 3つとも exit 0 |
| C | 担当パス外を触っていない | `git -c core.quotepath=false show --name-only` を `grep -vE '^(scripts/\|tools/)'` | ✅ 出力なし |

### `npm run validate` の完走サマリ（抜粋・すべて既存値のまま）

```text
loaded OK — total cards: 2516
generated JSON up to date — data/tl-names.json / data/tl-effects.json
top set links up to date — index.html に /sets/ リンク 63本
meta.sets covers all sets — 63件
meta.subtypes covers all subtypes — 156種
meta.rarities covers all rarities — 9種（スナップショット 2495枚から観測）
card-meta-index sort keys OK — 2516slug / level 133 / power 1006 / life 858 / cost_memory 515 / flip 22
cron git add paths in sync — 8パス（README.md / CLAUDE.md）
issue drafts in sync — 起票案22件（生きている12 / 完了9 / 取り下げ1）
card term highlights up to date — 2495 ページ / 2517 節

all checks passed.
```

---

## 4. ⭐ 生成物への影響（自分で判断した結果）

**結論: 影響は無い。** 3ファイルそれぞれについて根拠を分けて示す。

| 変更ファイル | 生成物との関係 | ⭐ 影響が無い根拠 |
|---|---|---|
| `scripts/gen-tl-json.mjs` | `data/tl-names.json` / `data/tl-effects.json` を生成 | ⭐ **実際に再生成して実測**（T5）。`git status --short` に生成物が1行も出ない＝**バイト一致** |
| `scripts/gen-card-meta-index.mjs` | `data/card-meta-index.json` を生成 | **構造で判断**（下記）。⚠️ **実行していない**（§5 に理由） |
| `tools/deck-builder/app.js` | ⭐ **生成物ではなく、そのまま配信されるソース**（何も生成しない） | 変更は**コメント1行のみ**で実行行の差分0件（検証A）。ビルド経路に入らない |

### `gen-card-meta-index.mjs` を回さずに「影響なし」と言える根拠

S1-1 の報告 §4 と同じ3点で、いずれも実測:

1. 変更行が**すべてコメント**（`//` 行）で、**実行行の差分は0件**（検証A）
2. このスクリプトは**自分自身のソースを読まない**。`import.meta.url` の用途は `ROOT` のパス解決1箇所だけで、
   `readFileSync` は存在しない
3. 出力 `data/card-meta-index.json` は**公式APIの応答から組み立てるJSON**で、
   ソースのコメント文字列を一切埋め込まない

⭐ **①だけでも十分だが、②③を併せると「コメントが出力に回り込む経路が無い」ことまで言える。**

---

## 5. 実行できなかった検証と代替手段

### ⚠️ `node scripts/gen-card-meta-index.mjs` は**意図的に実行していない**（S1-1 と同じ判断）

1. 同スクリプトは**ネットワーク必須**で、スナップショット未収録のフリップ面22件を `/cards/:slug` で
   個別取得する。取得に失敗したslugは**索引から除外して続行する**（exit 0）ので、
   ⚠️ **失敗すると「静かに欠けた索引」で `data/card-meta-index.json` を上書きしてしまう**
2. `GA_SNAPSHOT_MAX_AGE_MIN=0` で固定して回しても、⚠️ **APIドリフト由来の差分が生成物に混ざり得る**
   （CLAUDE.md「手動実行すると、タスクと無関係な差分が混ざり得る」）。
   **コメントのみの修正のコミットに混ぜてよい差分ではない**

**代替**: §4 のとおり、**出力に影響し得ないことを構造で示した**（実行より強い根拠になる）。
⭐ なお指示書 T5 が名指ししている `gen-tl-json.mjs` の側は**実際に回して実測済み**（オフラインで完結するため）。

### 残っている確認

- ⭐ **無し。** 本コミットは実行行を持たないため、Playwright実駆動・本番curlのいずれも対象が無い

---

## 6. 設計書からの逸脱と根拠

**逸脱なし。** 指示書 §2 の4箇所を、§3 の書き方（関数名参照・行番号を書かない・意味を変えない）で修正した。
§5 のスコープ外（コメント内容の書き換え、issue番号・設計書パスの参照）には**一切触れていない**。

### ⚠️ 指示書の表と1点だけ書き方が違う点（自己申告）

指示書 #3 の「直す先」は **`card-detail.js` のフレーバー描画（実測 `:305` の `flavorOf(card, t)` 周辺）**だが、
⚠️ **`flavorOf()` は `card-i18n.js` に集約された関数で `card-detail.js` には存在しない**ため、
そのまま書くと **T3（参照先の関数が同ファイルに実在すること）を満たせない**。
→ **同じ場所を指す、同ファイルに実在する関数名 `render()` を採用した**（§2 参照）。
**指し示す場所は指示書と同一**（`card-detail.js:305` を含む関数）。

---

## 7. 設計担当への確認事項

### 7-1 ⭐ 行番号参照は「腐る」だけでなく「最初から合っていない」型もある（§2 の #3）

指示書 §1 は「4件中4件が腐っている＝この書き方は必ず腐ると実証された」としているが、
**実測すると #3 は導入コミット時点で既に19行ずれていた**（腐ったのではなく最初から誤り）。
⭐ **結論（関数名で書く）は変わらないどころか強まる**が、**根拠の表現が事実と少し違う**ので報告する。
⚠️ **こちらでは指示書の文面を書き換えていない**（設計書は設計担当の担当パスのため）。

### 7-2 ⚠️ この修正を継続的に守る仕組みは無い（＝また増えうる）

今回で0件になったが、⭐ **`npm run validate` はこの形式の参照を検査していない**ため、
**次に誰かが `foo.js:123` と書いても何も止まらない**。
⚠️ **開発担当では決めない**（検査を足すかどうかは設計判断）。選択肢は
**(a) `validate` に1検査を足す** / **(b) 起票案にして待ち行列へ積む** / **(c) 放置する**。
⭐ 参考: 検査自体は今回の T2 の grep 1本で書ける（**現状0件なので、足せば即座に緑**）。

---

## 8. ハンドオフ

- ⭐ **`push待ち` 相当**（指示書 §5 に明記。承認済みタスクの残作業のため、レビューに戻さず公開待ちに置く）。
  ⚠️ GitHub停止中でラベル操作そのものはできないので、**この記述が代替**
- ⚠️ **本コミットは本番公開の前提条件ではない**（コメントのみ・挙動に影響しない）。ただし
  **未pushのまま溜まっている列に1件加わった**ので、次の公開（経路②）の対象には入る
