**対象: 「復旧後の起票案と検査の扱い」**

# 設計完了 — GitHub復旧後の運用方針（2026-09-20・ユーザー決定）

⚠️ **この記録は「復旧後の起票案と検査の扱い」だけでなく、復旧作業全体の前提**になる。
起票案は [`派生起票案_2026-09-11_復旧後の起票案と検査の扱い.md`](派生起票案_2026-09-11_復旧後の起票案と検査の扱い.md)。

## 決定（ユーザー・2026-09-20）

| 問い | 決定 |
|---|---|
| **J1** 状態の正をどちらにするか | ⭐ **役割を分ける** — 「**いま誰の番か**」は**GitHubのラベル**、「**何を決めたか**」は `docs/design` |
| **J2** 起票案と検査の扱い | ⭐ **案B＋案D-2** — **検査2本と索引の生成部を畳む**／**フォルダは `未採番-` のまま**／⭐ **🟢 生きているタスクだけ、着手時に `<番号>-` へリネームする** |
| **J3** 日次cronを再開するか | ⭐ **再開しない**（当面） |

## J1 の含意

- ⭐ **モードB（ファイル運用）は役目を終える。** 状態の正は**起票案1行目の5語ではなくGitHubのラベル**に戻る
- ⚠️ **失うもの**: `task records in sync` は「`完了` にするなら根拠の記録が要る」という
  **GitHubとは独立した品質検査**でもあった。⭐ **承知のうえで畳む**
- ⚠️ **両方を正にしない。** 37件を起票して待ち行列も正のままにすると、
  **この索引の改版履歴が7回数えている「検査の無い側から黙って古くなる」型**を自分で作る

## J2 の含意

- ⭐ **`未採番-` は17件で打ち止め**（今後増えない）。GitHubが戻れば**起票→採番→フォルダ作成**の順に戻るため、
  番号なしでフォルダを作る理由そのものが消える
- ⭐ **完了済みのフォルダはリネームしない**（もう動かないので番号を付ける実用上の意味が無い）
- ⭐ **生きているタスクは、着手するときに1件ずつ `<番号>-` へ**。
  ⚠️ **16件を一度にリネームしない**——`npm run validate` が `完了の根拠がありません` で exit 1 になる問題は、
  検査を畳んでから消える。**畳む前にリネームしない**
- ⚠️ **`CLAUDE.md` に「この17件は `未採番-` のまま残す」と明記する**。
  書かないと、後から「規約違反」として直しにかかる人が出る

### ⭐ 規約にはすでに例外が3種類ある（2026-09-20 実測・45フォルダ）

「17件が初めての逸脱」ではない。**案Dは4種類目の例外を明示的に作るという位置づけ**:

| 既存の逸脱 | 実例 |
|---|---|
| 旧独自番号の系列（GitHub採番ではない） | `12-デッキ統計タブ` `16-フォーマット検索`（⭐ CLAUDE.md に明記済み） |
| **同じ番号が2つある** | `16-フォーマット検索` と **`16-大会一覧ソート`** |
| 番号が2つ連結 | `53-54-api-cacheの堅牢化` `58-59-メイン枚数とLv0チャンピオン` |

## J3 の含意

- ⭐ **大会データの週1手動更新が引き続き唯一の更新経路**（恒久の運用義務のまま）
- ⚠️ **「翌朝のcronが自動で追従する」と書いてある箇所はすべて成り立たない**
  （新カードのページ・`sitemap.xml`・メタ索引・シーズン禁止の境界日・発売日の `legality` 切り替え・`pending.json` の再チェック）
- ⭐ **再開は `gh workflow enable build-tournaments.yml` の1コマンド**。いつでも戻せる
- ⚠️ ⭐ **pushとcron再開を同時にやらない理由**: 再びフラグが立ったときに**原因が切り分けられなくなる**。
  ⚠️ **push自体が「307コミットが一度に届く」形**で、停止のもう一方の自己申告要因
  （短期間の高頻度コミット）と**無関係とは言い切れない**（⚠️ これは推測で、実際に材料になるかは不明）

## ⚠️ 畳むのは別タスク（実装を伴う）

**検査2本と索引の生成部の撤去は `scripts/validate.mjs` と `scripts/gen-queue-index.mjs` を触る**＝
⚠️ **`scripts/` は配信物**なので、**設計→実装→レビュー→公開判断**の工程が要る。
⭐ **この記録は方針の確定までで、撤去の設計は別に書く**。

⚠️ **畳むまでのあいだ、起票案の1行目を編集したら索引の再生成が要る**（検査が生きているため）。
⭐ **起票（#n の書き足し）を先にやるか、畳むのを先にやるかは着手順の判断**（下記）。

## ⭐ 残るタスク（J1〜J3 適用後）

| # | タスク | 備考 |
|---|---|---|
| ~~T1~~ | ~~**push**~~ | ✅ **2026-09-20 完了**（`c5777923..e1f5a040`・308コミット・遅れ0）。⭐ 早送りで rebase 不要。#73 は予告どおり自動クローズ・判定コメントも投稿済み |
| ~~**T2a**~~ | ✅ **完了済み18件の起票 — 2026-09-20 完了（#75〜#92・全件クローズ）** | ⭐ **実測は不要**（作業は終わっており記録もある）。本文は起票案・**コメントは1件（docsへのリンク）**・すぐクローズ |
| ~~**T2b**~~ | ✅ **2026-09-21 完了（#93〜#110・18件）** — 🟢 生きている17件＋実装中1件 | ⭐ **18件とも投函前に実測して再現を確認し、実測結果をコメントで各issueに残した**。⚠️ **「ペインのスクロールが途中で止まる」だけは投函せず取り下げた**（再現しないため・下記） |
| ~~T3~~ | ~~工程記録145件をコメント投稿~~ | ⭐ **C2 の採用で実質消えた**（下記） |
| ~~T4~~ | ✅ **2026-09-21 完了** — 既存 open issue 21件の棚卸し | ⭐ **持ち越し3件を全部片付けた**（下記「T4 の実施結果」） |
| **T5'** | ⭐ **モードBを畳む**（検査2本＋生成部の撤去） | ⚠️ **新設**。実装を伴うので設計から |
| T6 | 運用記録の更新（`CLAUDE.md`・待ち行列・メモリー） | ⚠️ `CLAUDE.md` は配信物 |
| ~~T7~~ | ~~GitLab backup を残すか~~ | ✅ **2026-09-20 決定: 常設ミラーとして残す**（下記） |

⭐ **T5（16フォルダの一括リネーム）は D-2 の採用により消えた。**

---

## ⭐ T2b の実施結果（2026-09-21）

**#93〜#110 の18件を起票した**（すべて OPEN・ハンドオフラベルは付けていない＝設計書がまだ無いため）。
⭐ **各issueに「投函前の実測」コメントを1件ずつ付けた**（CLAUDE.mdの規約: 再現しなければ投函しない）。

⚠️ ⭐ **投函しなかったものが1件ある**: 「**ペインのスクロールが途中で止まる**」——⭐ **2026-09-21 にユーザー判断で取り下げた**
（[`取り下げ_2026-09-21_…`](../未採番-デッキ構築の操作不具合2件/取り下げ_2026-09-21_ペインのスクロールが途中で止まる.md)）。
2026-09-14 に**ユーザー本人が再確認しても再現せず**、設計担当のヘッドレス／ヘッド付き＋GPU描画でも再現しなかったもので、
**今回も再現の材料が増えていない**。⭐ **起票案自身が「投函前に必ず再現させる（しなければ取り下げ）」と書いている**ため、
⭐ **ユーザーは「取り下げる」を選んだ**（再発したら新しい観測つきで起票し直す）。

⭐ **設計フォルダのリネーム（`未採番-` → `<番号>-`）は着手時に行う**（J2/D-2）。今回はリネームしていない。

---

## ⭐ T4 の実施結果（2026-09-21）

⚠️ **「T4 でやること」に積んだ持ち越し3件は、すべて片付いた。**

### 1. ハンドオフラベルの残留 — ✅ 解消

**CLOSED なのにラベルが残っていたのは3件**（#73 だけではなかった）:

| issue | 残っていたラベル |
|---|---|
| **#73** | `レビュー待ち`（2026-09-20 に判定コメントだけ投稿済み） |
| **#29** | `レビュー待ち` |
| **#30** | `レビュー待ち` |

3件とも外した。⭐ **これで「ハンドオフラベルが付いた issue」は全リポジトリで0件**（open/closed 問わず）。
⭐ **J1（手番＝GitHubのラベル）の意味では、これが正しい状態**——
**いま手番が回っている工程は1つも無い**（設計書を持つタスクが無いため）。

### 2. 状態ラベルの整合 — ✅ 確認

⭐ **open 37件すべてが「状態ラベル1つ＋種別ラベル1つ」を持つ**ことを機械的に確認した
（着手可17 / 検討中15 / 保留4 / 凍結中1 ＝ 37、enhancement32 / bug3 / documentation2 ＝ 37）。

### 3. ⭐ cron停止で前提が崩れた issue — 4件に注記、1件はクローズ

| issue | 判断 |
|---|---|
| **#66** | ⭐ **クローズした**（下記） |
| **#38**（IndexNow） | ⚠️ **通知経路が存在しない**（cronの後段ステップ＝週1手動更新は通らない）と注記。`保留` 据え置き |
| **#28**（発見経路） | **更新頻度が毎日→週1（手動）**に落ちたと注記。`着手可` 据え置き |
| **#67**（regular の3v3） | 前提は現在も成立（4大会は未収録・許可リスト4種のまま）。⚠️ **実装しても反映は週1更新のタイミング**・**その回の差分が大きくなる**と注記 |
| **#48**（日次X投稿） | ⚠️ **いま実装しても「日次」にならない**（土台が無い）と注記。`保留` 据え置き |

#### ⭐ #66 は「永久に来ない解除条件」ではなくなっていた

⚠️ **保留の解除条件は「cronが1回走ったこと」だったが、cronは止まった。**
⭐ **ところが実測したら、週1手動更新が既に取り込んでいた**（`#59730`・**645デッキ・237チーム**）。
**6項目すべてを実データで確認してクローズ**した（証跡はissueのコメント）:

- **`Player #undefined` は `tournaments/` 全ファイルで0件**
- **0名チーム21件が「メンバー情報なし」で描画**（⭐ **実データで初めて通った経路**。⚠️ 1名・2名は実データに無く未確認のまま）
- **237チームが 100/100/37 の3ブロックに折りたたまれた**
- **チーム名**（最長37字・CJK2件・曲がったアポストロフィ3件）は **PC・375px とも順位表のセル7,146件ではみ出し0**
- ⚠️ **項目5（`standings` がスイス終了時点か）は実データからは断定できない**。⭐ **注記は「含みません」と安全側なので現状維持**と判断した

### 4. ⭐ 重複の統合 — #8 を #99 へ

**#8（フォーマット検索:「専用カード」絞り込みの再検討・2026-07-18）は、
2026-09-21 に起票した #99（フォーマット絞り込みを「使用可／限定／禁止」で整理し直す）に完全に含まれる**
（「パンテオン限定」「ドラフト限定」＝「専用カード」）。⭐ **#8 をクローズし、見送り理由（件数表示が不正確になる）を #99 へ引き継いだ**。

⚠️ ⭐ **T2b で18件を起票した直後だからこそ見つかった重複**である。
**起票案は停止期間中に書かれたので、既存 issue との突き合わせを誰もしていなかった。**

### 5. 進捗の付け合わせ（範囲が狭まった issue）

**#2（カードDB絞り込み強化）**: 起票後に **#80（レアリティ）・#79（チップ絞り込み欄）・#76（意味統一）**が完了しており、
⭐ **残っているのは数値項目（コスト・レベル等）の絞り込みだけ**。メタ索引が数値4項目を既に持っていること、
⚠️ **`entry` に9要素目を足せないこと**、#93 と構造が重なることを注記した。

そのほか **#24**（ルート直下の配信・3パスとも 200 で変化なし）、**#74**（モーダルは `rule` を1回も参照していない）、
**#55**（⚠️ **手で `setup:env` を打ったあとでは検証にならない**）にも再実測の結果を残した。

---

## ✅ T4 でやること（**2026-09-21 に全件消化済み**・以下は経緯の記録）

⚠️ **ここに書いてあるものを片付けずに T4 を終えない。**

### 1. ⭐ #73 のラベルを外す（2026-09-20 に持ち越し）

⚠️ **#73 は CLOSED なのにラベルが `レビュー待ち` のまま**。
停止中に工程が進み、**ラベルだけが 2026-08-18 で止まった**ため。

```bash
gh issue edit 73 --remove-label レビュー待ち
```

⭐ **単独で直さず T4 でまとめる**とユーザーが判断した（2026-09-20）。
理由は **#73 だけ直しても他の21件と一貫性が取れない**から。

- ✅ 判定コメントは投稿済み（`issues/73#issuecomment-5750341434`・2026-09-20）
- 本来の遷移は `レビュー待ち` → `設計確認待ち` → クローズだった

### 2. ハンドオフラベル全体の整理

⚠️ **22件すべて 2026-08-18 で止まっている。** 2026-09-20 実測のハンドオフラベル:

| ラベル | 付いているissue |
|---|---|
| `実装待ち` | （なし） |
| `レビュー待ち` | **#73 のみ**（⚠️ CLOSED） |
| `設計確認待ち` | （なし） |
| `push待ち` | （なし） |

⭐ **J1（手番はGitHubのラベルが正）を採ったので、T4 で「いまの手番」に合わせ直す**のが本題。

### 3. ⚠️ cron停止で前提が変わったissue

**J3 で日次cronを再開しないと決めた**ので、「cronが動く前提」で書かれたissueは前提が崩れている。
⚠️ **少なくとも次の2件は本文の見直しが要る**（⭐ 着手時に全22件を読んで洗い出す）:

| issue | 前提 |
|---|---|
| **#66** | 「**cron取り込み後に**表示を検証する」＝ ⚠️ **cronが動かないので永久に来ない**。週1手動更新で代替するか、取り下げるか |
| **#38** | IndexNow の通知は**cronの後段**で走る。⚠️ cron停止中は**通知そのものが発火しない** |

---

## ⭐ T7 の決定（2026-09-20・ユーザー）— **GitLab backup は常設ミラーとして残す**

- remote `backup`（`https://gitlab.com/dozyouneko-dev/ga-card-tools-jp.git`・**Private**）を**消さない**
- ⭐ **GitHubが復旧しても残す理由**: 停止は「解除された」だけで**再発しない保証はない**。
  ⚠️ **一度止まると `gh` も push も同時に失う**ので、独立した退避先の価値は変わらない
- ⚠️ **PATの期限は 2027-08-30**。切れると push が突然通らなくなる（⭐ **期限切れを認証設定の問題と混同しない**）
- ⚠️ **GitLab を Cloudflare Pages に接続しない**・**日次cronを GitLab CI に移植しない**（従来どおり）
- **押し方**: まず素の `git push backup main`。落ちたときだけ `git -c credential.helper=store push backup main`
  （⭐ 2026-09-13 以降は**素で4回連続成功**）

---

## ⭐ T2/T3 の再スコープ（2026-09-20・ユーザー決定 ＝ **案C2**）

### 測った事実

⭐ **工程記録145件のうち143件（98.6%）は完了済みタスクのもの**（`--posting-plan` で実測）:

| 状態 | タスク数 | 工程記録 |
|---|---:|---:|
| ✅ 完了 | 18 | **143** |
| 🟢 生きている | 18 | **0** |
| 🔧 実装中 | 1 | 1 |

### 決定

⭐ **完了済み18件は「案C2」で起票する** — **本文は起票案・コメントは1件（`docs/design` の該当フォルダへのリンク）・すぐクローズ**。

⚠️ ⭐ **これは待ち行列 §2-② からの変更である。** §2-② は
「完了済みも起票し、工程記録を全部コメントとして投稿する」（＝案C3）を前提に書かれていた。

**変更の理由**:

1. ⭐ **J1 で「GitHubのラベル＝いま誰の番か」と決めた**ので、**完了済みタスクには手番が無い**
2. ⭐ **T1 で push 済みなので、143件の全文は既に GitHub 上で読める**（`docs/design/**`）。
   **コメントに貼り直すのは同じ内容の二重化**＝このリポジトリが繰り返し踏んできた転記の型
3. `gh` 呼び出しが **約161回 → 約36回**

⚠️ **失うもの**: 工程記録が issue のコメント欄から時系列で読めなくなる（⭐ **フォルダを開けば読める**）。

### ⚠️ 待ち行列 §2-② を直すこと

⚠️ **§2-② は C3 のままなので、T6 で直す**（直さないと、次に読んだ人が C3 を実行する）。
