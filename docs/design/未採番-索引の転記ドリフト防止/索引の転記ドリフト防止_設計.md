# 設計書 — 索引の転記ドリフトを構造で止める（状態は起票案の1行目だけ／索引は生成／完了の根拠と状態を突き合わせる）

- 対象issue: **未採番**（GitHubアカウント停止中）。起票用の下書き＝本フォルダの
  [`起票案_2026-09-11_索引の転記ドリフト防止.md`](起票案_2026-09-11_索引の転記ドリフト防止.md)（呼び名「**索引の転記ドリフト防止**」）
- 設計時の HEAD: **`10f71a45`**（作業ツリーはクリーン）／設計時の実測日: **2026-09-11**／⭐ **設計コミット: `086843a9`**（V1 の正解データ）
- 要件の出所: リードがユーザーと直接ヒアリング（方針「**転記をなくす**」・確定要件4点 → §2-0）
- ⚠️ **この設計書1本で実装できるように書いてある。** ⭐ **正解データは索引そのもの**——設計コミットの索引に入っている
  生成部（`<!-- QUEUE-INDEX:START -->` 〜 `END`）は、§5 の規則どおりに設計担当が作ったもので、**単位1の実装はこれとバイト一致しなければならない**（V1）

## 改版履歴

| 版 | 日付 | 内容 | 状態 |
|---|---|---|---|
| **1.0** | **2026-09-11** | 初版。⭐ **データ移行（起票案31件・工程記録95件の1行目）は設計担当が本コミットで済ませた**（§11）。仕様の期待値は**すべて使い捨てのプロトタイプで実行して確定**（scratchpad・リポジトリには入れていない）。⚠️ 設計中に2点を実測で変えた: ①投稿順の取得を `git log --follow` の1ファイルずつから**1回の `git log`** へ（前者は95件で**120秒を超えた**・後者は**0.2秒**）②**承認と実物確認結果に `（→ 継続）` の明示を必須にした**（付け忘れで緑になる穴を実測で確認したため → §6-4） | **単位1〜3すべて実装待ち**（§8） |

---

## 0. 読み方

| 読む人 | 読む節 |
|---|---|
| 開発担当 | **§3〜§8**（仕様と実装単位）。⚠️ **§8-2 の「単位1で本タスクの起票案を `実装中` にする」を落とさない** |
| レビュー担当 | **§9**（検証項目）と §12（検査が防げないもの＝合格にしてよい緑） |
| 設計担当（承認時） | **§10**（承認時に入れる運用規約の文面） |

---

## 1. 背景と実測

### 1-1. 何が起きていたか

索引 `docs/design/待ち行列/待ち行列と復旧手順.md` は、**タスクの状態・件数・本番の位置を §0・§1・§2-A・§2-B・§2-④・§3・§5 と
各起票案・設計書に手で書き写していた**。`npm run validate`（`issue drafts in sync`）が見るのは **§1 の件数と収録漏れの2点だけ**。

- 2026-09-11: 完了・公開済みの「デッキ構築の左ペイン化」が、**起票案の状態行ごと `生きている` のまま**残っていた。
  **索引も `生きている` で整合していたので `validate` は緑**（是正は `10f71a45`＝索引 第23版）
- ⚠️ **同型の是正は改版履歴で7回目**（第3・7・13・14・18・19・23版）。09-05 に入れた運用規約
  （承認記録を書いたら同じコミットで §2 と §5 も見る）でも止まらなかった

### 1-2. ⭐ 設計の根拠にした実測（2026-09-11・`10f71a45`）

| # | 実測 | 設計への反映 |
|---|---|---|
| M1 | ⚠️ **索引の手書き部分に、第23版の是正後もずれが3件残っていた**: ①§2-④ の⑬は「**11通**」と書いて**14ファイル**を列挙 ②§2-④ の⑪に `実装報告_S1-1_2026-09-04.md` が**無い** ③§2-④ の⑥は `実装報告_2026-09-02_残作業.md` を**レビュー判定の前**に置いていたが、git の追加順では**承認の後**（`64bebfc1`） | 投稿順は書き写さず **git から作る**（§7） |
| M2 | ⚠️ ⭐ **Cloudflare の公開記録（`wrangler pages deployment list --environment production`・API の `created_on`）を測ると、経路②の本番公開は 08-27 以降 **9回**。うち **`a40dcdf2`（2026-09-04 12:28 UTC）は本番公開としては索引のどこにも記録が無い**（ハッシュは承認のコミットとしてだけ出ていた）。`a40dcdf2` は `5ebf817c`（日本語モードの0件表示の実装）を含む＝**0件表示は 09-04 から公開済み**で、第23版までの「**09-10 公開**」は誤り | **本番の位置と「公開済みか」は書かずに測る**（§2-0 要件4・索引 §3） |
| M3 | 承認記録26件の命名は **`承認_<日付>` 9件 / `承認_<呼び名>_<日付>` 7件 / `承認_<単位>_…_<日付>` 10件**。⚠️ **単位承認（デッキ構築の A〜F-b の10件）はタスクの完了ではない**（完了は `実物確認結果_2026-09-10.md`） | **ファイル名から完了は判定できない**→ **1行目の宣言で持たせる**（§4） |
| M4 | 工程記録（`docs/design/未採番-*/` と `待ち行列/`）は **95件**。⭐ **95件すべてが `<種類>_` で始まる**（種類は9つ・§4-1）。1行目は全件 `# ` の見出し・BOM／CRLF 0件 | 種類はファイル名の接頭辞で取れる。1行目を宣言に使える |
| M5 | ⚠️ **完了済みタスク14件のうち3件（辞書・フレーバー・行番号参照）は起票案を持たない**。⚠️ **1フォルダに6タスクが同居**している（`未採番-UI刷新の検討/` の工程記録34件） | **1タスク＝起票案1枚**に揃える（D1）。**フォルダでタスクを判定しない** |
| M6 | ⚠️ **同じ日に複数の単位が交互に進んだ**（デッキ構築の 09-06 に A・BC・D）。ファイル名の日付で並べると `実装報告_A → 実装報告_BC → …` と**工程が崩れる**。git の追加時刻なら**旧 §2-④ の手書きの順と一致**（⑭32件・⑬14件・⑫・② で確認） | 投稿順は **git の追加時刻**（§7） |
| M7 | `git log --follow` を1ファイルずつ95回 → **120秒で終わらない**。`git log --diff-filter=A --name-only -- docs/design` を1回 → **0.2秒**。範囲内のリネームは **0件** | `--follow` を使わない（§7-2） |
| M8 | GitHub の Markdown API（`POST /markdown`・`gfm`・未認証で可）で、**単独の行に置いたマーカー**は描画に出ず、前後の段落・見出し・表のインラインコードも**全部生きる**（生成部で表3・`<code>` 33・生のバッククォート0）。⚠️ **マーカーの後ろに同じ行で文字を書くと、その文字の Markdown は死ぬ**（CLAUDE.md の注意と同じ現象） | **マーカーは単独の行**・検査は**行の完全一致**で数える（§5-2） |
| M9 | 同じ工程記録を追加したコミットで同時刻になる組は **2組だけ**で、どちらも「**承認 ＋ その承認が出した追加指示**」 | 同時刻は **承認を先**に並べる（§7-2） |

---

## 2. 方針と決定事項

### 2-0. 確定要件（ユーザーが選んだ方針「転記をなくす」）

1. **タスクの状態は1か所だけに書く**（起票案の1行目）
2. **索引の件数・一覧表・§2 の起票リストはスクリプトで生成し、古ければ `validate` が exit 1**
3. ⭐ **完了の根拠になる記録に対象を宣言させ、状態行と食い違えば exit 1**（最重要）
4. **測れば分かる値は索引から消し、確認コマンドに置き換える**

### 2-1. 決定事項

| # | 論点 | 決定 | 根拠 |
|---|---|---|---|
| **D1** | タスクの単位 | ⭐ **1タスク＝起票案1枚**（＝モードBでの issue）。起票案の無かった完了タスク3件には**後付けした**（§11） | M5。起票案だけを数えると漏れる問題を、**数える側でなく作る側で**解く |
| **D2** | 状態の置き場 | **起票案の1行目だけ**。索引・設計書・§5 には**写さない** | 要件1 |
| **D3** | 状態の語彙 | **`生きている` / `設計中` / `実装中` / `完了` / `取り下げ`** の5つ（§3-2）。⚠️ **`公開済み` は足さない** | M2——**公開状態こそ手書きで最も腐った値**。測れば分かる（索引 §3）。⚠️ **手番（実装待ち等）も足さない**＝往復のたびに書き換える値は必ず古くなる |
| **D4** | タスクの識別子 | ⭐ **呼び名**（1行目に `呼び名「…」`）。一意 | 索引 §4-4 で「名前で呼ぶ」と決め済み。**パスで持つと復旧時のリネームで全宣言が壊れる** |
| **D5** | 完了の根拠 | ⭐ **工程記録の1行目 `**対象: 「呼び名」**` と、`承認_`・`実物確認結果_` の `（→ 完了）`/`（→ 継続）`** | 要件3。**宣言は出来事が起きたときに書くファイル（記録）に置く**——状態を直し忘れる人は、状態の側に置いた宣言も書き忘れる |
| **D6** | 過去分の扱い | ⭐ **遡って全件に宣言を足す**（95件・日付で線を引かない）。**設計担当が本コミットで済ませた** | 線を引くと95件が恒久的な例外になる（「除外リストで逃げない」＝起票案の整合検査の設計 §3-1 と同じ判断）。**ファイル名は見ないので命名の揺れは誤検出にならない**（M3・M4） |
| **D7** | 索引の一覧 | **§1 を生成部**にする（`scripts/gen-queue-index.mjs`）。§2-A・§2-B・§5 の表は**撤去**し、§1 の表を指す | 要件2 |
| **D8** | 投稿順（旧 §2-④） | ⚠️ **索引に置かない**。**復旧時に `--posting-plan` で出す** | M6・M7——並べるには git が要る。索引に置くと `validate` が git に依存し、**記録を書いてからコミットするまでの間は順番が決まらない** |
| **D9** | 測れる値 | 索引 §3（3系統の位置）・§0（件数）・§5（週1更新の前回日）を**確認コマンドに置き換えた**（本コミット） | 要件4・M2 |
| **D10** | 既存の検査 `issue drafts in sync` | ⭐ **置き換える**（単位1）。収録漏れ・件数一致は「生成部が最新か」に含まれる。**fail-closed の性質（0件・索引なし・見出し重複で赤）は引き継ぐ** | 生成物と検査が二重になると、どちらを直すか迷う |
| **D11** | `起票案` の前方一致規約 | **維持**。加えて **工程記録の9種類の接頭辞**（§4-1）も同じ扱い（その名前の `.md` は工程記録として検査される） | 起票案の整合検査の設計 §3-1（うるさい誤検知 ＞ 静かな取りこぼし） |
| **D12** | 手書きで残すもの | 改版履歴・§0 の手順・§2 の手順・§4 の経緯・§4-4・§5 の定常作業と方針・§6。⚠️ **手書き部分に件数・現在形の位置を書かない**。⭐ **生成部の外の見出しに `N件` を書くと exit 1** | 旧 D9（見出しの複製を許さない）の後継。**散文の件数は検査できないので規約** |
| **D13** | 運用規約の書き換え | **`CLAUDE.md` は各単位のコミットで開発担当が §8 の文面どおりに**（旧設計 D10 と同じ）。**`.claude/agents/*.md` と `docs/design/README.md` は承認時に設計担当が §10 の文面で** | 検査と説明を同じコミットに入れる／役割定義は設計担当の担当パス |
| **D14** | 本タスクの記録 | ⭐ **起票案から始める**（本フォルダ）。新しい規約に自分自身が従う（§8-2） | D1 |

### 2-2. 未決事項

**なし。** ⭐ **ユーザーの判断が要る点も無い**と判断した（D3 で `公開済み` を採らなかったのは要件の「例」からの逸脱だが、
**要件4（測れる値は書かない）と M2 の実測から一意に決まる**ため。覆すなら §12 の表の最終行を参照）。

---

## 3. 仕様 — 起票案（1行目）

### 3-1. 対象

`docs/design/**` を再帰的に走査し、**基名が `/^(派生)?起票案/` に一致する `.md`**（前方一致・既存と同じ）。

### 3-2. 1行目の書式

```text
**状態: <状態>**（最終確認 YYYY-MM-DD）— 呼び名「<呼び名>」<以降は自由記述>
```

```js
const DRAFT_RE = /^\*\*状態: ([^*]+)\*\*（最終確認 (\d{4}-\d{2}-\d{2})）— 呼び名「([^「」|`*\s]+)」/;
const STATES = ["実装中", "設計中", "生きている", "完了", "取り下げ"]; // ⚠ この順＝生成部の並び順
```

- **BOM は除去してから**、`split(/\r?\n/)[0]` を1行目とする（既存と同じ）
- 呼び名に使えない文字: `「` `」` `|` `` ` `` `*` と空白（表と正規表現を壊すため）
- 日付は形式だけ見る（値・鮮度は見ない＝既存 D4/D5 と同じ）

### 3-3. 起票案ごとの問題（**1ファイルにつき高々1行**・上から順に判定）

| 順 | 条件 | 出す行（`<path>` はリポジトリ相対） |
|---|---|---|
| 1 | 読めない | `起票案の読み込みに失敗: <path>（<message>）` |
| 2 | パスに空白・`(`・`)`・`\|`・`#` を含む | `起票案のパスに使えない文字があります: <path>` |
| 3 | 1行目が `**状態: ` で始まらない | `起票案の1行目に状態行がありません: <path>` |
| 4 | `DRAFT_RE` に一致しない（呼び名が無い等） | `起票案の1行目の書式が違います: <path>` |
| 5 | 状態が `STATES` に無い | `起票案の状態が不明です: <path>（「<語>」）` |

- 読めた起票案の間で**呼び名が重複**したら、重複した呼び名ごとに1行:
  `呼び名が重複しています: 「<呼び名>」（<path1> / <path2>）`（パスは昇順・行の順は各呼び名の先頭パスの昇順）
- **起票案が0件**なら `起票案が1件も見つかりません: docs/design`（既存 D8）

---

## 4. 仕様 — 工程記録（1行目の宣言）

### 4-1. 対象と種類

- 範囲: **`docs/design/` の直下のフォルダのうち、名前が `未採番-` で始まるものと `待ち行列`**（再帰）
- 基名が次の9種類のどれかで**始まる** `.md`（前方一致。種類＝一致した接頭辞）:

  ```js
  const KINDS = ["モック説明", "設計完了", "追加指示", "実装報告", "レビュー判定", "再判定", "承認", "実物確認結果", "取り下げ"];
  ```

- ⚠️ 番号付きフォルダ（`73-PRD新カード和訳/` 等）は**範囲外**（GitHubの issue に属する記録）。⚠️ 復旧後の扱いは派生起票案「**復旧後の起票案と検査の扱い**」

### 4-2. 1行目の書式

```text
**対象: 「<呼び名>」**[（→ 完了）|（→ 継続）|（→ 取り下げ）][ — <自由記述>]
```

```js
const REC_RE = /^\*\*対象: 「([^「」|`*\s]+)」\*\*(?:（→ (完了|継続|取り下げ)）)?(?: — .*)?$/;
```

| 種類 | `（→ …）` |
|---|---|
| `承認` / `実物確認結果` | ⭐ **`完了` か `継続` のどちらかが必須**。`完了`＝**そのタスクを完了とする記録**（⚠️ **軽微な残作業を後に回す承認も `完了`**——実例: 版レベルの意味統一・起票案の整合検査。残作業の `実装報告_` は完了の後に来てよい）。`継続`＝**単位の承認**や、**続きの `追加指示_` を出す承認** |
| `取り下げ` | **`取り下げ` が必須** |
| それ以外の6種類（`モック説明` `設計完了` `追加指示` `実装報告` `レビュー判定` `再判定`） | **付けない** |

### 4-3. 記録ごとの問題（**1ファイルにつき高々1行**・上から順に判定）

| 順 | 条件 | 出す行 |
|---|---|---|
| 1 | 読めない | `工程記録の読み込みに失敗: <path>（<message>）` |
| 2 | 1行目が `**対象: ` で始まらない | `工程記録の1行目に対象の宣言がありません: <path>` |
| 3 | `REC_RE` に一致しない | `工程記録の1行目の書式が違います: <path>` |
| 4 | 呼び名に一致する起票案が無い | `工程記録の対象に一致する起票案がありません: <path>（「<呼び名>」）` |
| 5 | 承認・実物確認結果が `完了`/`継続` でない | `承認_ と 実物確認結果_ の記録は（→ 完了）か（→ 継続）を宣言してください: <path>` |
| 6 | 取り下げが `取り下げ` でない | `取り下げ_ の記録は（→ 取り下げ）を宣言してください: <path>` |
| 7 | それ以外の種類が `（→ …）` を持つ | `（→ <語>）を宣言できない種類の記録です: <path>（<種類>）` |

---

## 5. 仕様 — 生成部と検査（**単位1**）

### 5-1. 新しいファイル `scripts/gen-queue-index.mjs`

- ⚠️ **Node の標準モジュール以外を import しない**（V1 で設計コミットのツリーへ単体コピーして動かすため）
- ルートは `import.meta.url` から決める（`gen-tl-json.mjs` と同じ）
- `validate.mjs` から使う関数を export する（起票案の読み取り・生成部の組み立て・〔単位2〕工程記録の読み取りと突き合わせ）。
  ⭐ **生成と検査で同じ関数を使う**（`buildTlJson` と同じ型）。⚠️ **比べる両辺は「索引に書かれている文字列」と「起票案から今作った文字列」＝別の出所**なので空回りしない
- ⚠️ **コメントに `<file>.js:<行番号>` の形を書かない**（既存の検査で exit 1）

| 起動 | 動作 | 出力と exit |
|---|---|---|
| `node scripts/gen-queue-index.mjs` | 生成部を作り直して書く | 変わったら `索引の生成部を更新しました（起票案N件）`／同じなら**書かずに** `索引の生成部は最新です（起票案N件）`・exit 0 |
| `… --check` | 書かずに比べる | 同じなら `索引の生成部は最新です（起票案N件）`・exit 0／違えば標準エラーに `索引の生成部が起票案と一致しません（最初の差: 生成部のK行目）`・exit 1 |
| `… --posting-plan` | 〔単位3〕§7 | §7 |
| それ以外の引数 | — | 標準エラーに使い方・**exit 2** |

- ⚠️ **起票案の問題（§3-3）・マーカーの問題（§5-2）があれば、どの起動でも書かない**。問題を1行ずつ標準エラーに出し、
  最後に `索引は書き換えていません` を出して **exit 1**
- ⚠️ ⭐ **書き込みは `<索引>.tmp` に書いてから `renameSync` で置き換える**（CLAUDE.md「VS Codeで開いたままのファイルは上書きに失敗する」。索引はエディタで開かれていることが多い）

### 5-2. マーカー

```text
<!-- QUEUE-INDEX:START -->
<!-- QUEUE-INDEX:END -->
```

- ⭐ **行全体がこの文字列と完全一致する行だけ**をマーカーと数える（前後に空白や文字があれば数えない）。
  ⭐ これで「マーカーの後ろに書いた文字の Markdown が死ぬ」置き方（M8）は、**数えられずに exit 1** になる
- START・END とも**ちょうど1行**、かつ **START が END より前**
- 生成部＝**START の次の行から END の前の行まで**。⭐ **生成した文字列（末尾は改行1つ）と、生成部の各行を `\n` でつないで末尾に `\n` を足したものが完全一致すること**

### 5-3. 生成部の中身（⭐ **設計コミットの索引に入っているものが正解**）

```text
                                                         ← 空行
> **この節は生成部**（次の「生成部ここまで」まで）。`node scripts/gen-queue-index.mjs` が作るので**手で編集しない**。
> 状態の正は各起票案の1行目。状態を変えるときは起票案を直してから生成し直す（古いままだと `npm run validate` が exit 1）。
                                                         ← 空行
## 1. 起票案の一覧（**全{全件}件**）
                                                         ← ここから STATES の順に5回くり返す
                                                         ← 空行
{見出し}（**{件数}件**）
                                                         ← 空行
（該当なし）                                              ← 0件のとき。1件以上なら次の表
| 呼び名 | 起票案 | 最終確認 |
|---|---|---|
| **{呼び名}** | [`{docs/design からの相対パス}`]({索引のフォルダからの相対パス}) | {最終確認} |
                                                         ← くり返しここまで
                                                         ← 空行
> **生成部ここまで**
                                                         ← 空行
```

| 状態 | `{見出し}` | 絵文字のコードポイント |
|---|---|---|
| 実装中 | `### 🔧 実装中` | U+1F527 |
| 設計中 | `### 📝 設計中` | U+1F4DD |
| 生きている | `### 🟢 生きている` | U+1F7E2 |
| 完了 | `### ✅ 完了済み` | U+2705 |
| 取り下げ | `### 🚫 取り下げ` | U+1F6AB |

- ⚠️ **上の枠の `←` から右は説明で、実際の行には含まない**（「空行」は0文字の行。行末に空白を付けない）。⭐ **迷ったら設計コミットの索引の生成部を正とする**
- ⚠️ **絵文字に異体字セレクタ（U+FE0F）を付けない**（設計書からコピーするときに混ざりやすい）
- ⚠️ **見出しの語は `完了済み`**（状態の語は `完了`）。**旧検査の見出しの形を保ったのは、単位1までの間も旧検査が通るようにするため**
- 行の順: **リポジトリ相対パスの昇順（JS の既定の `sort()`）**
- リンク: 表示 = `path.posix.relative("docs/design", p)`、行き先 = `path.posix.relative("docs/design/待ち行列", p)`
  （同じフォルダの起票案は `起票案_….md` のようにスラッシュ無しになる）
- ⭐ **時刻・日数など実行のたびに変わる値を入れない**（決定的であること）

### 5-4. `validate.mjs` の変更

- ⚠️ **旧ブロック「起票案の状態行と索引の整合検査」を丸ごと置き換える**（成功行 `issue drafts in sync — …` は**出なくなる**）。
  置き場所は旧ブロックと同じ（cron列挙の検査の後・行番号参照の検査の前）。ファイル冒頭の検査一覧のコメントも1行書き換える
- 索引側の問題（**上から順に・該当したら後続を省略するものに注意**）:

| 順 | 条件 | 出す行 | 後続 |
|---|---|---|---|
| 1 | 索引が読めない | `索引が見つかりません: docs/design/待ち行列/待ち行列と復旧手順.md` | **全部省略** |
| 2 | マーカーが1組でない | `索引の生成部のマーカーが1組ではありません（START a個 / END b個）` | **全部省略** |
| 3 | END が START より前 | `索引の生成部のマーカーの順序が逆です（END が START より前にあります）` | **全部省略** |
| 4 | 生成部の外の行が `/^#{1,6} .*\d+件/` に一致 | 1行ごとに `件数を書いた見出しが生成部の外にあります: <n>行目「<その行>」`（n は索引の1始まりの行番号） | 続ける |
| 5 | 起票案の問題（§3-3）が1件以上 | `生成部の比較を省略しました（起票案の問題が <件数>件 あるため）` | 比較しない |
| 6 | 生成部が作り直したものと違う | `索引の生成部が起票案と一致しません（最初の差: 生成部のK行目）`（K は START の次の行を1とした行番号） | — |

- 出力（成功）: `queue index up to date — 起票案N件（実装中a / 設計中b / 生きているc / 完了d / 取り下げe）`
- 出力（失敗）: 空行 → `QUEUE INDEX DRIFT (docs/design/待ち行列/待ち行列と復旧手順.md):` → 起票案の問題（§3-3）→ 索引の問題（上表）を
  `  - ` 付きで1行ずつ → 次の3行:

  ```text
    → 状態の正は各起票案の1行目です。起票案を直し、node scripts/gen-queue-index.mjs で生成部を作り直してください
    → 「起票案」で始まる名前の .md はすべて起票案として扱われます。設計書・解説文書には別の名前を付けてください
    → 書式と手順は docs/design/待ち行列/待ち行列と復旧手順.md §4
  ```

---

## 6. 仕様 — 突き合わせ（**単位2**）

### 6-1. 実行の条件

- 起票案の問題（§3-3）が1件でもあれば、**突き合わせはせず** `起票案との照合を省略しました（起票案を読めないため。先に QUEUE INDEX DRIFT を直してください）` の1行だけを出す
- 記録ごとの問題（§4-3）が1件でもあれば、それらの行の後に `起票案との照合を省略しました（工程記録の問題が <件数>件 あるため）` を出し、**タスクごとの判定（6-2）はしない**
  （⚠️ 1つの原因で2種類のエラーを出さない＝旧設計 D6 と同じ）

### 6-2. タスクごとの判定（起票案のパスの昇順・**1タスクにつき高々1行**・上から順）

段階: `生きている`(0) < `設計中`(1) < `実装中`(2) < `完了`(3)。記録の種類ごとの**最低段階**:
`モック説明`→`設計中`、`設計完了` `追加指示` `実装報告` `レビュー判定` `再判定` `承認` `実物確認結果`→`実装中`。

| 順 | 条件 | 出す行 |
|---|---|---|
| T1 | `（→ 完了）` と `（→ 取り下げ）` の両方がある | `完了と取り下げの両方が宣言されています: 「<呼び名>」（<記録> / …）` |
| T2 | `（→ 完了）` が2件以上 | `完了の宣言が複数あります: 「<呼び名>」（<記録1> / <記録2> …）` |
| T3 | `（→ 取り下げ）` が2件以上 | `取り下げの宣言が複数あります: 「<呼び名>」（…）` |
| ⭐ **T4** | 終端の宣言が1件あり、状態がそれと違う | ⭐ `状態行が工程記録と食い違っています: 「<呼び名>」は <記録> で<完了\|取り下げ>したのに、起票案の状態が「<状態>」です（<起票案>）` |
| — | 終端の宣言が1件あり、状態と一致 | （問題なし。以下を見ない） |
| T5 | 状態が `完了` なのに `（→ 完了）` が無い | `完了の根拠がありません: 「<呼び名>」の状態は完了ですが、（→ 完了）を宣言した記録がありません（<起票案>）` |
| T6 | 状態が `取り下げ` なのに `（→ 取り下げ）` が無い | `取り下げの根拠がありません: 「<呼び名>」の状態は取り下げですが、（→ 取り下げ）を宣言した記録がありません（<起票案>）` |
| T7 | 状態がそのタスクの記録の最低段階より低い | `状態が工程記録より遅れています: 「<呼び名>」の状態は「<状態>」ですが、<記録>（<種類>）があります（<最低段階> 以上にしてください）`（記録は**最も高い最低段階を要求するもの**・同じなら**パス昇順で先**） |
| T8 | 状態が `実装中` なのに `設計完了_` も `追加指示_` も無い | `実装中の根拠がありません: 「<呼び名>」は実装中ですが、設計完了_ か 追加指示_ の記録がありません（<起票案>）` |

- 記録の列挙（T1〜T3 の括弧内）は**パスの昇順**

### 6-3. 出力

- 成功: `task records in sync — 工程記録N件（完了の根拠a / 継続b / 取り下げの根拠c）`（N は §4-1 の範囲で見つけたファイル数）
- 失敗: 空行 → `TASK RECORD DRIFT:` → 問題を `  - ` 付きで1行ずつ → 次の2行:

  ```text
    → 工程記録の1行目は **対象: 「呼び名」** です（承認_・実物確認結果_ は（→ 完了）か（→ 継続）、取り下げ_ は（→ 取り下げ）を付ける）
    → 状態を進める記録（設計完了_・（→ 完了））を書いたら、同じコミットで起票案の1行目を直し、node scripts/gen-queue-index.mjs を打ってください
  ```

- 置き場所: **単位1のブロックの直後**

### 6-4. ⭐ `（→ 継続）` を必須にした理由（設計中の実測）

「最後の承認で `（→ 完了）` を**付け忘れ**、状態も直し忘れる」と、`（→ 継続）` が任意の設計では**タスクは `実装中` のまま整合して緑**になる
（プロトタイプで再現・V33 の前段）。⚠️ **これまでのずれは7回とも「直し忘れ」**なので、**承認のたびに `完了` か `継続` を選ばせる**。
残る穴は「**選び間違える**」だけになる（§12）。

---

## 7. 仕様 — 投稿計画（**単位3**）

### 7-1. 出力（標準出力）

```text
# 投稿計画 — 起票案<N>件（取り下げ<k>件を除く）・工程記録<M>件
                                                   ← 空行
## 「<呼び名>」— <状態>（工程記録<n>件）
本文: <起票案のパス>
  1. <記録のパス>（<種類>・<ハッシュ先頭8桁> <YYYY-MM-DD HH:MM> UTC）
  …
（工程記録なし）                                    ← n が0のとき
                                                   ← 空行（タスクごと）
```

- タスクは**起票案のパスの昇順**。⚠️ **状態が `取り下げ` のタスクは出さない**（起票しないため）
- 記録の並び: ⭐ **その記録を最初に追加したコミットの時刻（`%ct`）の昇順** → 同時刻は種類の順
  **`承認` `実物確認結果` `取り下げ` `モック説明` `設計完了` `追加指示` `実装報告` `レビュー判定` `再判定`**（⭐ 締めの記録を先・M9）→ パスの昇順

### 7-2. 取り方

- ⭐ **`git -c core.quotepath=false log --diff-filter=A --name-only --format=… -- docs/design` を1回だけ**打ち、各パスについて**最も古い追加**を採る
  （⚠️ **`--follow` を1ファイルずつ打たない**＝M7。範囲内のリネームは0件で、⚠️ **索引 §2 の手順で ③投稿 を ④リネーム より先にした**）
- ⚠️ 一度も追加されていない記録（未コミット）があれば、標準エラーに1件ずつ `未コミットの工程記録があります（投稿順が決まらない）: <path>` を出して **exit 1**
- 起票案・記録の問題（§3-3・§4-3）があれば、`validate` と同じ行を標準エラーに出して **exit 1**
- **ファイルを1つも書かない**

---

## 8. 実装単位と着手順

### 8-1. 単位（**この順に1単位ずつ**。各単位で実装報告 → レビュー → 承認）

| 単位 | 中身 | 変えるファイル | 検証 |
|---|---|---|---|
| **1. 生成部と索引の検査** | §3・§5（旧検査の置き換え） | `scripts/gen-queue-index.mjs`（新規）・`scripts/validate.mjs`・`CLAUDE.md`（§8-3 の U1）・⚠️ **本タスクの起票案と索引の生成部**（§8-2） | V1〜V16 |
| **2. 工程記録の突き合わせ** | §4・§6 | `scripts/gen-queue-index.mjs`・`scripts/validate.mjs`・`CLAUDE.md`（U2） | V20〜V34 |
| **3. 投稿計画** | §7 | `scripts/gen-queue-index.mjs`・`CLAUDE.md`（U3） | V40〜V44 |

- ⭐ **1 を先にする**（2・3 は 1 の起票案の読み取りを使う）。**2 が本丸**（要件3）
- ⚠️ **生成物（`cards/` `sets/` `data/` 等）は0ファイル**。出たら本タスクの成果ではない
- ⚠️ **`scripts/` と `CLAUDE.md` は配信物**＝**コミットまでで止め、公開の可否はユーザーが判断する**

### 8-2. ⚠️ ⭐ 単位1で本タスクの起票案を `実装中` にする

設計コミットの時点では、本タスクの起票案は**旧語の `生きている`**（旧検査が新しい語を通さないため）。単位1のコミットで次を行う:

1. ⭐ **先に V1**（何も変えずに `--check` が「最新です」になること）
2. `docs/design/未採番-索引の転記ドリフト防止/起票案_2026-09-11_索引の転記ドリフト防止.md` の1行目の
   **`**状態: 生きている**` を `**状態: 実装中**` に変える**（それ以外の文字は変えない）
3. `node scripts/gen-queue-index.mjs` で生成部を作り直す（🔧 実装中 が1件・🟢 生きている が1件減る）

⭐ **開発担当が `docs/` を触るのはこの2ファイル（起票案1行目・索引の生成部）だけ**（本タスクに限った許可）。

### 8-3. `CLAUDE.md` の文面（各単位のコミットに含める・**この文面のまま**）

**U1** — 「タスク管理」節の、`- ⚠️ **起票案(…)の状態の正は、各ファイルの1行目**:` で始まる箇条から
`⭐ **黙って通ることはない**——…exit 1** になる` の行まで（2箇条・計14行）を、次で置き換える:

```markdown
- ⚠️ **状態の正は各起票案(`docs/design/**/起票案*.md`・`派生起票案*.md`)の1行目だけ**(2026-09-11〜・**1タスク＝起票案1枚**):
  `**状態: <状態>**（最終確認 YYYY-MM-DD）— 呼び名「<呼び名>」<自由記述>`。
  状態は **`生きている` / `設計中` / `実装中` / `完了` / `取り下げ`** の5つ(⚠️ **公開済みかどうかは状態にしない**＝索引 §3 のコマンドで測る)。
  **呼び名がタスクの識別子**(一意)
- ✅ **索引 §1 は生成物**(`node scripts/gen-queue-index.mjs` がマーカー `<!-- QUEUE-INDEX:START -->` 〜 `END` の間を作り直す)。
  **起票案の状態を変えたら生成し直して同じコミットに入れる**。古いと `npm run validate` が exit 1
  (成功時は `queue index up to date — 起票案N件（実装中… / 設計中… / 生きている… / 完了… / 取り下げ…）`)
  - ⚠️ **生成部を手で直さない**(直すのは起票案)。⚠️ **マーカーは単独の行に置く**(同じ行に何か書くとマーカーとして数えず exit 1)
  - ⚠️ **生成部の外の見出しに件数(`N件`)を書くと exit 1**。⚠️ **手書き部分に件数や現在形の位置(本番・GitHubのハッシュ)を書かない**
    (書いた値から古くなる。実例: 2026-09-04 の本番公開 `a40dcdf2` は本番公開としては索引に記録されないまま1週間残った)
  - ⚠️ **中身の「腐り」は防げない**(状態行が整合したまま前提が崩れる型)。手当ては下の「投函前に実測して再現させる」規約のまま
  - ⚠️ **`docs/design/**` に `起票案` で始まる名前の `.md` を、起票案以外の目的で置けない**(前方一致)。
    ⭐ **黙って通ることはない**——`起票案の1行目に状態行がありません` で **exit 1** になる
```

**U2** — U1 の箇条の直後に1箇条を足す:

```markdown
- ✅ **工程記録の1行目は対象の宣言**(2026-09-11〜・`docs/design/未採番-*/` と `待ち行列/`):
  `**対象: 「<呼び名>」**`。**`承認_`・`実物確認結果_` は `（→ 完了）`(タスクを完了させる記録)か `（→ 継続）` のどちらかが必須**、
  `取り下げ_` は `（→ 取り下げ）`。対象は `モック説明_` `設計完了_` `追加指示_` `実装報告_` `レビュー判定_` `再判定_` `承認_`
  `実物確認結果_` `取り下げ_` で始まる `.md`(前方一致)
  - ⭐ **`npm run validate` が宣言と起票案の状態を突き合わせる**(exit 1): `（→ 完了）` があるのに状態が `完了` でない／
    `完了` なのに根拠が無い／工程記録より状態が遅れている／`実装中` なのに `設計完了_` も `追加指示_` も無い
    (成功時は `task records in sync — 工程記録N件（完了の根拠… / 継続… / 取り下げの根拠…）`)
  - ⚠️ **状態を進める記録を書いたら、同じコミットで起票案の1行目を直し、生成し直す**(`設計完了_` → `実装中`・`（→ 完了）` → `完了`)
  - ⚠️ **最後の承認で `（→ 継続）` を選び間違え、状態も直さないと整合したまま緑になる**(残る穴)。生成部の 🔧 実装中 に残っていないかを見る
```

**U3** — U2 の箇条の最後に1行を足す:

```markdown
  - ⭐ **復旧後の投稿順は `node scripts/gen-queue-index.mjs --posting-plan` が git の追加時刻から作る**(索引に書き写さない)
```

---

## 9. 検証項目

⚠️ **破壊試験（V3〜V15・V21〜V33・V43）は1つずつ行い、次へ進む前に必ず戻す**（併発すると行数が変わる）。
⭐ **「エラー行」＝ `  - ` で始まる行**（見出し行と `→` の行は数えない）。⭐ **以下の期待値は、§5・§6 の仕様どおりのプロトタイプを
設計担当が実行して得た実物**（`docs/` の複製上で実行・2026-09-11）。**実装の出力が食い違ったら、疑うのは実装**。

⭐ **期待値の数え方**: 成功行の件数は、その時点で次のコマンドが返す値と一致すること（⚠️ 絶対数を書かない＝記録は日々増える）:

```bash
find docs/design -name '*.md' | grep -cE '/(派生)?起票案[^/]*$'                                   # 起票案の数
find docs/design/未採番-* docs/design/待ち行列 -name '*.md' \
  | grep -cE '/(モック説明|設計完了|追加指示|実装報告|レビュー判定|再判定|承認|実物確認結果|取り下げ)[^/]*$'   # 工程記録の数
```

### 9-1. 単位1（V1〜V16）

| # | 手順 | 期待 |
|---|---|---|
| **V1** ⭐ | **正解データとの一致**。①開発担当: §8-2 の手順1として、**何も変えずに** `node scripts/gen-queue-index.mjs --check` ②レビュー担当: 設計コミットのツリーを `git archive <設計コミット> \| tar -x -C tmp/review/<タスク>/v1` で展開し、実装後の `scripts/gen-queue-index.mjs` を同じ位置へコピーして `--check` | ①②とも **exit 0**・`索引の生成部は最新です（起票案31件）`。⚠️ **設計コミットの後に起票案が増減していたら①は数が変わる**——そのときは②を正とする |
| **V2** | 単位1のコミット後に `npm run validate` | **exit 0**。`queue index up to date — 起票案<起票案の数>件（実装中1 / 設計中0 / 生きている15 / 完了14 / 取り下げ1）`（⚠️ 設計コミット後に起票案が変わっていなければこの数）。⭐ **`issue drafts in sync` の行は出ない**（`grep -c` が0） |
| **V3** | 生成部の `### 🟢 生きている（**15件**）` を `（**16件**）` に手で変える | exit 1・エラー行 **1行**: `索引の生成部が起票案と一致しません（最初の差: 生成部の17行目）`（⚠️ 17 は 🔧 実装中 が1件・📝 設計中 が0件のとき） |
| **V4** | `docs/design/未採番-版順序の非決定性/起票案_2026-08-30_決定性の自動検査.md` の状態を `設計中` にし、**生成し直さない** | exit 1・エラー行 **1行**: `…（最初の差: 生成部の13行目）` |
| **V4b** | V4 のまま `node scripts/gen-queue-index.mjs` → `npm run validate` | 生成は `索引の生成部を更新しました（起票案31件）`。validate は **exit 0**・`…（実装中1 / 設計中1 / 生きている14 / …）` |
| **V5** | `docs/design/待ち行列/起票案_dummy.md` を1行目 `**状態: 生きている**（最終確認 2026-09-11）— 検証用` で作る（呼び名なし） | exit 1・エラー行 **2行**: `起票案の1行目の書式が違います: docs/design/待ち行列/起票案_dummy.md` / `生成部の比較を省略しました（起票案の問題が 1件 あるため）` |
| **V6** | `起票案_2026-08-30_決定性の自動検査.md` の `呼び名「版順序の決定性検査」` を `呼び名「ブラウザ側の代表画像」` に変える | exit 1・エラー行 **2行**: `呼び名が重複しています: 「ブラウザ側の代表画像」（docs/design/未採番-版順序の非決定性/起票案_2026-08-30_ブラウザ側の代表画像.md / docs/design/未採番-版順序の非決定性/起票案_2026-08-30_決定性の自動検査.md）` / `生成部の比較を省略しました（起票案の問題が 1件 あるため）` |
| **V7** | 索引の END マーカーの行を消す | exit 1・エラー行 **1行**: `索引の生成部のマーカーが1組ではありません（START 1個 / END 0個）` |
| **V8** | START マーカーの行を2行にする | exit 1・**1行**: `…（START 2個 / END 1個）` |
| **V9** ⭐ | START マーカーの行を `<!-- QUEUE-INDEX:START --> 生成部` にする（**同じ行に文字**） | exit 1・**1行**: `…（START 0個 / END 1個）` |
| **V10** | 索引の末尾に `### 🟢 生きている（**15件**）の控え` を1行足す | exit 1・**1行**: `件数を書いた見出しが生成部の外にあります: <足した行の行番号>行目「### 🟢 生きている（**15件**）の控え」` |
| **V11** | `起票案_2026-09-04_検索結果のタブ化.md` の状態を `実装待ち` にする | exit 1・**2行**: `起票案の状態が不明です: docs/design/未採番-デッキ構築ツールUI刷新/起票案_2026-09-04_検索結果のタブ化.md（「実装待ち」）` / `生成部の比較を省略しました（起票案の問題が 1件 あるため）` |
| **V12** | 索引を一時的にリネーム | exit 1・**1行**: `索引が見つかりません: docs/design/待ち行列/待ち行列と復旧手順.md` |
| **V13** | ⭐ **走査の壊れが緑にならない**。起票案の走査ルートの定数を一時的に `docs/design/リリース確認` にする | exit 1。エラー行に `起票案が1件も見つかりません: docs/design/リリース確認` を含む |
| **V14** | ⭐ **壊れた起票案では書かない**。V5 の状態で `node scripts/gen-queue-index.mjs` | **exit 1**・標準エラーの最後が `索引は書き換えていません`。`git diff --quiet -- docs/design/待ち行列/待ち行列と復旧手順.md` が **exit 0**（索引は変わっていない） |
| **V15** | 生成を2回続けて打つ | 2回目は `索引の生成部は最新です（起票案N件）`・索引の mtime が変わらない。`node scripts/gen-queue-index.mjs --bogus` は **exit 2** |
| **V16** | V3〜V15 をすべて戻して `git status --short` → `npm run validate`・`git -c core.quotepath=false show --stat --format= <単位1のコミット>` | 作業ツリーに戻し忘れが無いこと。単位1のコミットの変更は **`scripts/gen-queue-index.mjs`（新規）・`scripts/validate.mjs`・`CLAUDE.md`・本タスクの起票案・索引**の5つだけ。validate は **V2 と同じ出力**。コードの書き込みが `.tmp` → `renameSync` であること（読んで確認） |

### 9-2. 単位2（V20〜V34）

| # | 手順 | 期待 |
|---|---|---|
| **V20** | `npm run validate` | exit 0。V2 の行の直後に `task records in sync — 工程記録<工程記録の数>件（完了の根拠14 / 継続<承認・実物確認結果のうち（→ 継続）の数> / 取り下げの根拠1）` |
| **V21** ⭐⭐ | **本日のずれの再現**。`docs/design/未採番-デッキ構築ツールUI刷新/起票案_2026-09-04_デッキ構築ツールの左ペイン化と検索条件の統一.md` の状態を `完了` → `生きている` にし、**生成し直す**（＝索引と状態行が両方古いまま整合している状態） | ⭐ **queue index は緑**（`…生きている16 / 完了13…`）なのに **exit 1**。エラー行 **ちょうど1行**: `状態行が工程記録と食い違っています: 「デッキ構築の左ペイン化」は docs/design/未採番-デッキ構築ツールUI刷新/実物確認結果_2026-09-10.md で完了したのに、起票案の状態が「生きている」です（docs/design/未採番-デッキ構築ツールUI刷新/起票案_2026-09-04_デッキ構築ツールの左ペイン化と検索条件の統一.md）`。⚠️ **T7（状態が遅れている）が併発しないこと** |
| **V21b** | V21 を**生成し直さずに** | exit 1・エラー行 **2行**（QUEUE の `最初の差: 生成部の17行目` と、V21 と同じ1行） |
| **V22** | `実装報告_A_トークン_2026-09-06.md`（デッキ構築）の1行目を消す | exit 1・**2行**: `工程記録の1行目に対象の宣言がありません: docs/design/未採番-デッキ構築ツールUI刷新/実装報告_A_トークン_2026-09-06.md` / `起票案との照合を省略しました（工程記録の問題が 1件 あるため）` |
| **V23** | 同じファイルの対象を `「デッキ構築の左ペイン」`（1字欠け）にする | exit 1・**2行**: `工程記録の対象に一致する起票案がありません: …/実装報告_A_トークン_2026-09-06.md（「デッキ構築の左ペイン」）` / 省略の1行 |
| **V24** | `承認_A_トークン_2026-09-06.md` の `（→ 継続）` を `（→ 完了）` にする | exit 1・**1行**: `完了の宣言が複数あります: 「デッキ構築の左ペイン化」（docs/design/未採番-デッキ構築ツールUI刷新/実物確認結果_2026-09-10.md / docs/design/未採番-デッキ構築ツールUI刷新/承認_A_トークン_2026-09-06.md）` |
| **V25** | `実装報告_A_トークン_2026-09-06.md` の宣言の直後に `（→ 完了）` を足す | exit 1・**2行**: `（→ 完了）を宣言できない種類の記録です: …/実装報告_A_トークン_2026-09-06.md（実装報告）` / 省略の1行 |
| **V26** | `docs/design/未採番-UI刷新の検討/承認_レアリティ_2026-09-04.md` の `（→ 完了）` を `（→ 継続）` にする | exit 1・**1行**: `完了の根拠がありません: 「レアリティ絞り込み」の状態は完了ですが、（→ 完了）を宣言した記録がありません（docs/design/未採番-UI刷新の検討/起票案_2026-09-03_レアリティ絞り込み.md）` |
| **V27** ⭐ | 同じファイルの `（→ 完了）` を**消す**（＝付け忘れ） | exit 1・**2行**: `承認_ と 実物確認結果_ の記録は（→ 完了）か（→ 継続）を宣言してください: …/承認_レアリティ_2026-09-04.md` / 省略の1行 |
| **V28** | `docs/design/未採番-デッキ構築ツールUI刷新/実装報告_dummy_2026-09-11.md` を1行目 `**対象: 「検索結果のタブ化」**` で作る | exit 1・**1行**: `状態が工程記録より遅れています: 「検索結果のタブ化」の状態は「生きている」ですが、docs/design/未採番-デッキ構築ツールUI刷新/実装報告_dummy_2026-09-11.md（実装報告）があります（実装中 以上にしてください）` |
| **V29** | 同じ場所に `モック説明_dummy_2026-09-11.md`（対象「検索結果のタブ化」）を作る | exit 1・**1行**: `…「生きている」ですが、…/モック説明_dummy_2026-09-11.md（モック説明）があります（設計中 以上にしてください）` |
| **V30** | `起票案_2026-09-04_検索結果のタブ化.md` を `実装中` にし、生成し直す | exit 1・**1行**: `実装中の根拠がありません: 「検索結果のタブ化」は実装中ですが、設計完了_ か 追加指示_ の記録がありません（docs/design/未採番-デッキ構築ツールUI刷新/起票案_2026-09-04_検索結果のタブ化.md）` |
| **V31** | `docs/design/未採番-スターコーリングのハイライト欠落/取り下げ_2026-08-30.md` の `（→ 取り下げ）` を消す | exit 1・**2行**: `取り下げ_ の記録は（→ 取り下げ）を宣言してください: …/取り下げ_2026-08-30.md` / 省略の1行 |
| **V32** | `docs/design/未採番-UI刷新の検討/承認_2026-08-28.md` の `（→ 完了）` を `（→ 取り下げ）` にする | exit 1・**2行**: `承認_ と 実物確認結果_ の記録は（→ 完了）か（→ 継続）を宣言してください: …/承認_2026-08-28.md` / 省略の1行（⚠️ **1記録から2行出ないこと**） |
| **V33** ⭐ | **残る穴の確認**（§12）。`docs/design/未採番-日本語モードの0件表示/承認_2026-09-04.md` の `（→ 完了）` を `（→ 継続）` にし、`派生起票案_2026-09-04_日本語モードで0件になるときの表示.md` を `実装中` にして生成し直す | ⚠️ **exit 0**（**設計どおりの穴**。§12 の記述と一致することを確かめる）。成功行が `完了の根拠13 / 継続<+1>` に変わる |
| **V34** | すべて戻して `npm run validate`・`git status --short`・`git show --stat --format= <単位2のコミット>` | V20 と同じ出力。単位2のコミットの変更は **`scripts/gen-queue-index.mjs`・`scripts/validate.mjs`・`CLAUDE.md`** の3つだけ |

⚠️ **単位2の後に V5・V6・V11 を再実行すると、`TASK RECORD DRIFT:` の下に `起票案との照合を省略しました（起票案を読めないため。先に QUEUE INDEX DRIFT を直してください）` が1行増える**（6-1）。

### 9-3. 単位3（V40〜V44）

| # | 手順 | 期待 |
|---|---|---|
| **V40** | `node scripts/gen-queue-index.mjs --posting-plan > tmp/<役割>/plan.txt` | exit 0。1行目が `# 投稿計画 — 起票案<取り下げ以外の数>件（取り下げ1件を除く）・工程記録<取り下げ以外のタスクの記録の数>件`。⚠️ **「スターコーリングのハイライト欠落」の見出しが無い** |
| **V41** ⭐ | 同じ出力の「行番号参照の是正と検査」の14行 | この順（パスは `docs/design/未採番-UI刷新の検討/` 省略・ハッシュ）: 1 `追加指示_腐った行番号参照_2026-09-04.md`（27b32ee8）2 `実装報告_腐った行番号参照_2026-09-04.md`（df82dd8c）3 `承認_腐った行番号参照_2026-09-04.md`（855b0e14）4 `実装報告_行番号参照の検査_2026-09-04.md`（70f8546f）5 `レビュー判定_行番号参照の検査_2026-09-04.md`（43fc9bbc）6 `承認_行番号参照の検査_2026-09-04.md`（6752068f）7 `追加指示_行番号参照の検査の仕上げ_2026-09-04.md`（6752068f）8 `実装報告_行番号参照の検査の仕上げ_2026-09-05.md`（6eecd734）9 `レビュー判定_行番号参照の検査の仕上げ_2026-09-05.md`（1d844bac）10 `承認_行番号参照の検査の仕上げ_2026-09-05.md`（14734d8d）11 `追加指示_vendor除外コメントの断定_2026-09-05.md`（14734d8d）12 `実装報告_vendor除外コメントの断定_2026-09-05.md`（d144de6d）13 `レビュー判定_vendor除外コメントの断定_2026-09-05.md`（4ebfd437）14 `承認_vendor除外コメントの断定_2026-09-05.md`（6e09f7fb）。⭐ **6→7・10→11 は同時刻で承認が先**（M9） |
| **V42** | 同じ出力で ⑪「版レベルの意味統一」と ⑥「起票案の整合検査」 | ⑪は4件で**最後が `実装報告_S1-1_2026-09-04.md`（8fc41751）**。⑥は5件で**最後が `実装報告_2026-09-02_残作業.md`（64bebfc1）**（M1 の②③が正しく出る）。「デッキ構築の左ペイン化」は**32件で先頭が `モック説明_左ペイン化_2026-09-06.md`（77a93448）・末尾が `実物確認結果_2026-09-10.md`（ecb2b51d）** |
| **V43** | 工程記録を1つ新規作成（未コミット・対象は本タスク）して `--posting-plan` | **exit 1**・標準エラーに `未コミットの工程記録があります（投稿順が決まらない）: <そのパス>` |
| **V44** | V43 を戻し、`--posting-plan` の前後で `git status --short` と索引の mtime を比べる・`time` を測る | 何も変わらない（ファイルを書かない）。実行は数秒以内（設計時の実測 1.0秒） |

---

## 10. 運用規約の更新（**承認時に設計担当が入れる**・この文面を元に）

⚠️ **`.claude/` は配信されない**（ドットフォルダ）・`docs/design/README.md` は `docs/` ＝どちらも公開面を増やさない。

| ファイル | 入れる内容 |
|---|---|
| `.claude/agents/design.md` | 「設計を終えたとき」に**モードBの手順**を追記: ①設計書 → ②`設計完了_<日付>.md`（**1行目 `**対象: 「呼び名」**`**）→ ③起票案の1行目を `実装中` → ④`node scripts/gen-queue-index.mjs` → ⑤`npm run validate` → ⑥①〜④を1コミット。承認時は `承認_…`（**`（→ 完了）` か `（→ 継続）` を必ず選ぶ**）と、完了なら起票案を `完了`。**着手時に `設計中`** へ |
| `.claude/agents/lead.md` | 「モードBの決まり」の工程記録の表に `追加指示_` `再判定_` `実物確認結果_` `取り下げ_` `モック説明_` を足し、⭐ **「各記録の1行目は `**対象: 「呼び名」**`（呼び名は起票案と設計完了記録にある）」**を足す。状態板の見方を「**§1 の生成部の 🔧 実装中・📝 設計中**」に |
| `.claude/agents/dev.md` / `review.md` | 報告・判定の節に1行: 「モードBでは記録ファイルの**1行目を `**対象: 「呼び名」**`**にし、書いたら `npm run validate`」 |
| `docs/design/README.md` | 「今後のタスク」の状態行の書式を §3-2 に合わせる（5つの状態・呼び名） |

---

## 11. 設計担当が本コミットで済ませたデータ移行

⭐ **旧検査（`issue drafts in sync`）が通る形のまま入れた**（本コミットで `npm run validate` は exit 0）。

| 移行 | 件数 | 中身 |
|---|---|---|
| 起票案の1行目に呼び名 | **26件** | `）— ` の直後に `呼び名「…」。` を挿入（それ以外の文字は変えていない） |
| 起票案の新規 | **5件** | 完了済みで起票案の無かった **辞書の抜けと検出経路・フレーバー表示経路・行番号参照の是正と検査**（`完了`）／本タスク（`生きている`＝§8-2 まで旧語）／派生 **復旧後の起票案と検査の扱い**（`生きている`） |
| 完了済みの起票案に「起票時」 | **11件** | 索引 §2-A のタイトル・ラベルと §1 の顛末列を、各起票案の見出しの直下へ移した（索引から消えるため） |
| 工程記録の1行目に宣言 | **95件** | 対象の呼び名＋ `（→ 完了）` **14件**・`（→ 継続）` **13件**・`（→ 取り下げ）` **1件**。末尾に ` — この宣言は 2026-09-11 に遡って追記した（索引の転記ドリフト防止）` |
| 索引 | 1件 | §1 を生成部に・§0/§2/§3/§5 を §2-1 の方針で書き換え（第24版） |

**振り分けの規則**（`未採番-UI刷新の検討/` 以外は **フォルダ＝1タスク**）:

| フォルダ | 対象（呼び名） | `（→ 完了）` の記録 |
|---|---|---|
| `未採番-UI刷新の検討/` の `*_2026-08-28.md` | スマホ絞り込み導線 | `承認_2026-08-28.md` |
| 同 `実装報告_2026-09-03` `レビュー判定_2026-09-03` `再判定_2026-09-03` `承認_2026-09-03` | PC左ペイン | `承認_2026-09-03.md` |
| 同 `*_レアリティ_*` | レアリティ絞り込み | `承認_レアリティ_2026-09-04.md` |
| 同 `*_チップ絞り込み_*` | チップ絞り込み欄 | `承認_チップ絞り込み_2026-09-04.md` |
| 同 `*_意味統一_*` と `実装報告_S1-1_2026-09-04.md` | 版レベルの意味統一 | `承認_意味統一_2026-09-04.md` |
| 同 `*_腐った行番号参照_*` `*_行番号参照の検査_*` `*_行番号参照の検査の仕上げ_*` `*_vendor除外コメントの断定_*` | 行番号参照の是正と検査 | `承認_vendor除外コメントの断定_2026-09-05.md`（他の承認3件は `継続`） |
| `未採番-デッキ構築ツールUI刷新/` | デッキ構築の左ペイン化 | `実物確認結果_2026-09-10.md`（単位承認10件は `継続`） |
| `未採番-ハイライト検査の無言緑/` `未採番-フレーバー表示経路/` `未採番-日本語モードの0件表示/` `未採番-版順序の非決定性/` `未採番-用語ハイライトの語形ずれ/` `未採番-起票案の整合検査/` `未採番-辞書の抜けと検出経路/` | フォルダ名と同じ呼び名（⚠️ `未採番-フレーバー表示経路/` は「フレーバー表示経路」。同居の派生起票案「フレーバー表示の追補」とは別） | 各フォルダの `承認_*.md` |
| `未採番-スターコーリングのハイライト欠落/` | スターコーリングのハイライト欠落 | `取り下げ_2026-08-30.md` が `（→ 取り下げ）` |

⭐ **本コミット時点のプロトタイプの実行結果**: `queue index up to date — 起票案31件（実装中0 / 設計中0 / 生きている16 / 完了14 / 取り下げ1）`
／ `task records in sync` は ⚠️ **本タスクの `設計完了_2026-09-11.md` が T7（状態が遅れている）に当たる**——**§8-2 までの意図した状態**
（単位2の検査はまだ無いので `validate` は赤くならない）。

---

## 12. 検査が防げないもの（**合格にしてよい緑**）と、この設計の寿命

| 防げないもの | なぜ | 手当て |
|---|---|---|
| ⚠️ **最後の承認で `（→ 継続）` を選び間違え、状態も直さない** | 両方が整合している（V33 で実測） | 生成部の先頭が 🔧 実装中 なので**残っているものが目に入る**。承認の文面に「完了か継続か」を書く規約（§10） |
| 手番（実装待ち／レビュー待ち／設計確認待ち）のずれ | 状態語にしていない（D3） | 起票案の自由記述と最新の工程記録で見る（索引 §5） |
| 中身の腐り（スターコーリング型） | 状態と根拠が整合したまま前提が崩れる | 索引 §4-2（投函前の再測定） |
| 手書き部分の散文に書いた件数・位置 | 散文は検査できない（見出しだけ検査する） | 規約（D12）。⚠️ **日付つきの過去の事実は書いてよい**（「2026-09-04 に公開した」は腐らない。「本番は X」は腐る） |
| 本番の位置・公開済みかどうか | ⚠️ **ネットワークと Cloudflare のトークンが要る**ので `validate`（オフライン）に入れない | 索引 §3 のコマンド |
| ⚠️ **GitHub復旧後**（フォルダを `<番号>-` にリネームしたあと） | 工程記録の範囲が `未採番-*` なので、**完了の根拠が見つからず exit 1 になる**（設計どおりの赤） | 派生起票案「**復旧後の起票案と検査の扱い**」で決める（索引 §2 ④ の前） |
| （参考）`公開済み` を状態に足さなかったこと | D3。**覆すなら**、公開のたびに複数の起票案を書き換える運用と、その書き換えを突き合わせる根拠（公開の記録ファイル）が要る＝**転記が増える** | — |

---

## 13. 範囲外（本タスクでは扱わない）

- ⚠️ **`CLAUDE.md` の「未pushなので本番にはまだ出ていない」9箇所**（2026-09-11 実測: 573・584・691・706・744・810・1161・1166・1217行目）。
  経路②で公開済みなので**すべて誤り**で、**同じ「現在形の状態を散文に書いた」型**。⚠️ **`CLAUDE.md` は配信物**なので直すなら公開の判断とセットで、
  **別タスクとして**扱う（リードへ報告済み）
- 各起票案の自由記述に残っている現在形の記述（例:「本番公開も済み（`27b32ee8`）」）——**1か所にしか無いので転記ではない**。直すかは各タスクの起票時に
- 番号付きフォルダ（GitHub 時代）の記録
