**対象: 「索引の転記ドリフト防止」**（→ 継続） — 単位3（投稿計画 `--posting-plan` ＋ S-1 ＋ D7）の承認。⚠️ **タスクは完了にしない**（ユーザー判断＝単位4「承認前の是正」が残る）。

> ⚠️ **これはissueコメントの代替**（GitHubアカウント停止中で `gh` が403）。GitHub復旧後に
> `gh issue comment <n> --body-file docs/design/未採番-索引の転記ドリフト防止/承認_単位3_投稿計画_2026-09-13.md` で投函する。

# 承認 — 単位3「投稿計画 ＋ S-1・D7」（2026-09-13）

- 対象コミット: **`37ddf6e5`**（実装）／実装報告 [`実装報告_単位3_投稿計画_2026-09-13.md`](実装報告_単位3_投稿計画_2026-09-13.md)
- レビュー判定: [`レビュー判定_単位3_投稿計画_2026-09-13.md`](レビュー判定_単位3_投稿計画_2026-09-13.md)（**合格**・M1〜M7 すべて充足）
- 設計書: [`索引の転記ドリフト防止_設計.md`](索引の転記ドリフト防止_設計.md) → 本コミットで **1.3 版**

---

## 1. 判断 — ⭐ **単位1〜3は承認。ただしタスクは完了にしない**

**ユーザーの判断（2026-09-13）**: 「**単位1〜3は承認する。ただし `scripts/validate.mjs` のコメントの是正を
済ませてから完了にする**」。したがって本記録は **`（→ 継続）`** であり、起票案の状態は **`実装中` のまま**。

⭐ **承認の理由**:

- **V40〜V47 が全項目とも期待どおり**。とくに **V45（S-1）** は、レビューが**設計が想定した条件そのもの**
  （全31件の起票案を `生きている` にし、**かつ**記録の走査を壊す）を作って、**S-1 が無ければ
  `task records in sync — 工程記録0件` で exit 0 になる**＝**穴が実在すること**を実測した（R-4）。
  ⚠️ ⭐ **単位2までの赤は「完了済みの起票案がたまたま実在する」偶然に依存していた**——その偶然を
  取り除いた状態で赤くなることまで確認されており、**「たまたま赤い」ではないことの証明になっている**
- **`--posting-plan` は索引に何も書き写さない**（要件4）。⚠️ **ファイルを1つも書かない**ことも V44 で確認済み
- **逸脱なし**。⚠️ `validate.mjs` のコメントが古いまま残った件は**逸脱ではなく、単位3のスコープ外**
  （§8-1 の「変えるファイル」に無い）＝ **開発担当が仕様を自分で決めずに確認事項へ回した判断は正しい**

---

## 2. レビューの推奨（S）の裁定

| # | 内容 | 裁定 | 根拠 |
|---|---|---|---|
| **S-1** | `KINDS`（9種）と `PLAN_KIND_ORDER`（9種）の一致を保証するものが無い。10種目を `KINDS` にだけ足すと `indexOf()` が **-1** を返し、同時刻の並びでその種類が `承認` より前に出る（**無言**） | ✅ ⭐ **採用**（単位4・§8-4-b） | ⭐ **これは本タスクの主題そのもの**——**同じ事実を2つの配列に手で書き写し、一致を保証する検査が無い**＝索引の転記ドリフトと**同型**。⚠️ **「影響が軽微だから残す」は、この設計の趣旨と正面から矛盾する**。⭐ **直し方は「検査を足す」ではなく「転記をやめる」**（`PLAN_KIND_ORDER` を `KINDS` から導く）——導出結果は現在のリテラルと同じ並びなので**挙動は変わらない**（V53 でバイト一致を確認する） |
| **S-2** | git 管理外で `--posting-plan` を打つと Node のスタックトレースがそのまま出る（fail-closed だが読めない） | ✅ **採用**（単位4・§8-4-c） | ⭐ **`--posting-plan` は復旧当日に初めて動かす道具**で、**当日に慌てる場面でしか読まれないエラー**。3行の try/catch で読める1行になるなら見合う。⚠️ **exit 1 のまま・標準出力に計画を出さない**（fail-closed を緩めない） |
| **S-3** | `--posting-plan` は `validate` から呼ばれないので壊れても気づかない。索引 §2 に「投函の前に一度通して exit 0 を確認する」を入れておくとよい | ✅ **採用**（⭐ **本コミットで設計担当が実施**・索引 §2-③） | ⭐ **`validate` から呼ぶ案は採らない**——`validate` は**オフラインで完結する**のが前提で、git を引くコマンドを常時走らせると**その前提が壊れる**（設計書 D8 で同じ理由から索引に書かないと決めている）。**手順書に1行置くのが釣り合う**。⚠️ あわせて**リネーム後に打つと無言で記録が消える**ことも書いた（派生起票案） |

---

## 3. 実装報告 §6 の確認事項の裁定

| # | 確認事項 | 裁定 |
|---|---|---|
| **1** | `scripts/validate.mjs` のコメントが 1.2 版より狭い。この単位で直すか／別タスクか | ⭐ **本タスクの中で直す**（単位4）。⚠️ **別タスクにしない**——**ユーザーがこれを完了の条件にした**。⭐ **ただし「②の文面を③へ書き写して広げる」ことはしない**（→ §4） |
| **2** | 復旧後のリネームの順にまつわる実測2件を、派生起票案に足すか | ✅ **足す**（⭐ **本コミットで実施**）。⚠️ **前者（リネーム後に打つと exit 0 のまま記録が計画から消える）が重要**——**赤くならない＝投函漏れに気づけない**ので、派生起票案の「決めること」の論点に直結する |
| **3** | §7-2 の「未コミットの記録」の対象が、出力するタスク（`取り下げ` 以外）の記録だけになっている | ✅ **意図どおり**（実装の判断を追認）。取り下げたタスクは投函しないので投稿順が要らない。⚠️ **裏返しに「取り下げたタスクの未コミットの記録は無言で見逃す」**ので、**そう書いて設計書 §7-2 に明文化した**（1.3 版・仕様の変更はこの1行だけ） |

---

## 4. ⭐ 単位4の要点 — **写しを広げるのではなく、減らす**

同じ類型が **3か所**にある（2026-09-13 実測）:

| # | 場所 | 列挙 | 機械照合 |
|---|---|---|---|
| ① | 設計書 **§12** | あり（2通り） | — |
| ② | `CLAUDE.md` **U2** の最後の1行 | あり（2通り） | ✅ **V47**（§8-3 のブロックと `diff` 0行） |
| ③ | `scripts/validate.mjs` のコメント | ⚠️ **1通りだけ＝古い** | ❌ |

⚠️ ⭐ **③に②を書き写すと、写しが3つになり、検査は相変わらず②だけ**——**本タスクが無くそうとしている型そのもの**を1つ増やすことになる。
**1.2 版で①②を直したのに③が取り残された**という事実が、その案では再発することの実測そのもの。
⭐ **採ったのは「③から列挙を落とし、正は §12 だと明示して参照させる」**（写しが **3→2** に減り、残る2つは V47 で機械照合済みの組）。
文面と根拠は設計書 **§8-4-a**、確認は **V50〜V52**。

---

## 5. 残っていること

- **単位4「承認前の是正」** → [`追加指示_承認前の是正_2026-09-13.md`](追加指示_承認前の是正_2026-09-13.md)（**次の担当: 開発担当**）
- ⚠️ **公開は 2026-09-17 の大会データ週次更新と一緒に出す**（ユーザー判断 2026-09-13）。
  ⭐ **`scripts/` は配信物**なので、単位1〜4の差分はいずれもそのバッチに乗る
- **派生issueの起票**（「復旧後の起票案と検査の扱い」）は**GitHub復旧後**（起票案は `生きている` のまま）

---

**次の担当: 開発担当**（ラベル相当: **実装待ち**）。
