**対象: 「索引の転記ドリフト防止」**

# 実装報告 — 単位1「生成部と索引の検査」（2026-09-11）

⚠️ **これは issueコメントの代わり**です（GitHubアカウントが停止中で `gh issue comment` が打てないため、ファイルに残しています）。
復旧して採番されたら、本文をそのまま `gh issue comment <n> --body-file docs/design/未採番-索引の転記ドリフト防止/実装報告_単位1_生成部_2026-09-11.md` で投函します。

- 日付: **2026-09-11**
- 設計書（正）: [`索引の転記ドリフト防止_設計.md`](索引の転記ドリフト防止_設計.md)（1.0）／設計完了: [`設計完了_2026-09-11.md`](設計完了_2026-09-11.md)
- 設計コミット（V1 の正解データ）: **`086843a9`**／着手時の HEAD: **`e2ed701c`**（作業ツリーはクリーン）
- ⭐ **実装コミット: `516f62d1`**（この報告は別の記録コミットに入れます）
- **push: していません**（`scripts/` と `CLAUDE.md` は配信物なので、公開するかどうかはユーザーが判断します）
- 範囲: **単位1だけ**。単位2（工程記録の突き合わせ）と単位3（投稿計画）には手を付けていません

---

## 1. 実施内容

| 手順 | 内容 |
|---|---|
| 1 | `scripts/gen-queue-index.mjs` を新規作成（設計書 §3・§5-1〜§5-3）。**Node の標準モジュールしか import していません**（V1②で単体コピーして動かすため） |
| 2 | ⭐ **§8-2 手順1＝V1①**: 何も変えずに `node scripts/gen-queue-index.mjs --check` → **exit 0・`索引の生成部は最新です（起票案31件）`**。あわせて V1② も先回りで実行しました（§2 の表） |
| 3 | `scripts/validate.mjs`: 旧ブロック「起票案の状態行と索引の整合検査」（`issue drafts in sync`）を**丸ごと置き換え**ました（§5-4。置き場所は旧ブロックと同じで、cron列挙の検査の後・行番号参照の検査の前）。ファイル冒頭の検査一覧のコメントも1行書き換え、`gen-queue-index.mjs` から関数を import しています（生成と検査で同じ関数を使う） |
| 4 | ⭐ **§8-2 手順2**: 本タスクの起票案の1行目を **`**状態: 生きている**` → `**状態: 実装中**`** に変えました（ほかの文字は変えていません。`git diff --word-diff` で変わったのはこの1語だけ） |
| 5 | ⭐ **§8-2 手順3**: `node scripts/gen-queue-index.mjs` → `索引の生成部を更新しました（起票案31件）`。🔧 実装中 が 0→**1件**、🟢 生きている が 16→**15件**（索引の差分は生成部の中の9行だけ） |
| 6 | `CLAUDE.md`: 「タスク管理」節の2箇条・14行（67〜80行目）を、**§8-3 U1 の文面に置き換え**ました（設計書のコードブロックから機械的に切り出して差し込んだので、手で書き写してはいません） |
| 7 | `npm run validate` → exit 0 → **5ファイルをパスを明示してステージ**し、`516f62d1` としてコミット |
| 8 | 破壊試験 V3〜V15 は、**`git archive HEAD` で scratchpad に作った複製の上で**1件ずつ実行し、1件ごとに複製を `git reset --hard && git clean -fd` で戻しました（**共有の作業ツリーは一度も変更していません**） |

変更ファイル（`git -c core.quotepath=false show --stat --format= 516f62d1`）:

```text
 CLAUDE.md                                          |  23 +-
 docs/design/待ち行列/待ち行列と復旧手順.md         |   9 +-
 .../起票案_2026-09-11_索引の転記ドリフト防止.md    |   2 +-
 scripts/gen-queue-index.mjs                        | 285 +++++++++++++++++++++
 scripts/validate.mjs                               | 168 ++++--------
 5 files changed, 352 insertions(+), 135 deletions(-)
```

---

## 2. 検証結果（設計書 §9-1 の V 番号に対応）

⭐ 「エラー行」は `  - ` で始まる行のことです（設計書の数え方に合わせています）。V3〜V15 は複製の上で実行しました。

| # | 手順 | 結果 | 実際の出力 |
|---|---|---|---|
| **V1** ⭐ | ①何も変えずに `--check`（手順2）②`git archive 086843a9` を展開し、`516f62d1` の `scripts/gen-queue-index.mjs` を同じ位置に置いて `--check` | ✅ | ①②とも **exit 0**・`索引の生成部は最新です（起票案31件）`。補足: 着手時の索引は設計コミットの索引と `cmp` で完全一致しました（`086843a9` から `e2ed701c` までの間に、生成部と起票案は変わっていません） |
| **V2** | `516f62d1` の後に `npm run validate` | ✅ | **exit 0**・`queue index up to date — 起票案31件（実装中1 / 設計中0 / 生きている15 / 完了14 / 取り下げ1）`。`issue drafts in sync` は `grep -c` で **0**。起票案の数（`find … \| grep -cE '/(派生)?起票案[^/]*$'`）も **31** |
| **V3** | 生成部の `### 🟢 生きている（**15件**）` を 16件 にする | ✅ | exit 1・1行: `索引の生成部が起票案と一致しません（最初の差: 生成部の17行目）` |
| **V4** | `起票案_2026-08-30_決定性の自動検査.md` を `設計中` にする（生成し直さない） | ✅ | exit 1・1行: `…（最初の差: 生成部の13行目）` |
| **V4b** | V4 のまま生成してから validate | ✅ | 生成: `索引の生成部を更新しました（起票案31件）`／validate は **exit 0**・`…（実装中1 / 設計中1 / 生きている14 / 完了14 / 取り下げ1）` |
| **V5** | 呼び名の無い `docs/design/待ち行列/起票案_dummy.md` を作る | ✅ | exit 1・2行: `起票案の1行目の書式が違います: docs/design/待ち行列/起票案_dummy.md` / `生成部の比較を省略しました（起票案の問題が 1件 あるため）` |
| **V6** | 呼び名「版順序の決定性検査」を「ブラウザ側の代表画像」に変える | ✅ | exit 1・2行: `呼び名が重複しています: 「ブラウザ側の代表画像」（docs/design/未採番-版順序の非決定性/起票案_2026-08-30_ブラウザ側の代表画像.md / docs/design/未採番-版順序の非決定性/起票案_2026-08-30_決定性の自動検査.md）` / `生成部の比較を省略しました（起票案の問題が 1件 あるため）` |
| **V7** | END マーカーの行を消す | ✅ | exit 1・1行: `索引の生成部のマーカーが1組ではありません（START 1個 / END 0個）` |
| **V8** | START マーカーの行を2行にする | ✅ | exit 1・1行: `…（START 2個 / END 1個）` |
| **V9** ⭐ | START マーカーの行を `<!-- QUEUE-INDEX:START --> 生成部` にする | ✅ | exit 1・1行: `…（START 0個 / END 1個）` |
| **V10** | 索引の末尾に `### 🟢 生きている（**15件**）の控え` を足す | ✅ | exit 1・1行: `件数を書いた見出しが生成部の外にあります: 521行目「### 🟢 生きている（**15件**）の控え」`（足した後の `wc -l` は 521＝足した行の番号） |
| **V11** | `起票案_2026-09-04_検索結果のタブ化.md` を `実装待ち` にする | ✅ | exit 1・2行: `起票案の状態が不明です: docs/design/未採番-デッキ構築ツールUI刷新/起票案_2026-09-04_検索結果のタブ化.md（「実装待ち」）` / `生成部の比較を省略しました（起票案の問題が 1件 あるため）` |
| **V12** | 索引をリネームする | ✅ | exit 1・1行: `索引が見つかりません: docs/design/待ち行列/待ち行列と復旧手順.md` |
| **V13** ⭐ | `gen-queue-index.mjs` の `DRAFT_ROOT` を `docs/design/リリース確認` にする | ✅ | exit 1・2行: `起票案が1件も見つかりません: docs/design/リリース確認` / `生成部の比較を省略しました（起票案の問題が 1件 あるため）`（validate も同じ定数を import しているので、定数1つの変更で両方に効きます） |
| **V14** ⭐ | V5 の状態で `node scripts/gen-queue-index.mjs` | ✅ | **exit 1**・標準エラーは `起票案の1行目の書式が違います: …/起票案_dummy.md` → **最後の行が `索引は書き換えていません`**（標準出力は0バイト）。`git diff --quiet -- 索引` は **exit 0**。`.tmp` の残骸も0件。同じ状態で `--check` を打っても exit 1 で、同じ2行が出ます |
| **V15** | 生成を2回続けて打つ／`--bogus` | ✅ | 起票案を1件 `設計中` にしてから生成すると1回目は `更新しました`（mtime 13:54:41 → 13:55:17）、**2回目は `索引の生成部は最新です（起票案31件）`で mtime は 13:55:17 のまま**。変更なしで2回打った場合も、2回とも `最新です` で mtime は変わりません。`--bogus` は **exit 2**（使い方を表示）。`--check extra` も exit 2 |
| **V16** | 戻し忘れの確認・変更ファイル・書き込み方式 | ✅ | 共有の作業ツリーは `git status --short` が**0行**（破壊試験は複製の上で行ったため）。`516f62d1` の変更は**指定の5ファイルだけ**（`scripts/`・`CLAUDE.md`・`docs/` 以外は `grep -v` で0件＝**生成物 0ファイル**）。validate は **V2 と同じ出力**・exit 0。書き込みは `writeFileSync(<索引>.tmp)` → `renameSync(tmp, 索引)` です（`gen-queue-index.mjs` の `main()`。rename に失敗したら `.tmp` を消してから例外を投げ直します） |

### 設計書に無い追加の確認（参考・すべて複製の上）

| 確認 | 結果 |
|---|---|
| START と END を入れ替える（§5-4 の3番目） | exit 1・1行: `索引の生成部のマーカーの順序が逆です（END が START より前にあります）` |
| パスに空白を含む起票案 `起票案 x.md`（§3-3 の2番目） | exit 1・2行: `起票案のパスに使えない文字があります: docs/design/待ち行列/起票案 x.md` / 省略の1行 |
| 1行目が `# 見出し` の起票案（§3-3 の3番目） | exit 1・2行: `起票案の1行目に状態行がありません: …` / 省略の1行 |
| BOM 付きの正しい起票案を足して生成する | 生成は `起票案32件`、validate は exit 0（BOM は除去してから読みます） |
| **往復**: 起票案を `設計中` にして生成し、元に戻してもう一度生成する | 索引が元のファイルと **`cmp` で完全一致**（生成部の外の行は1バイトも変えていない） |
| 索引が無い状態で `gen-queue-index.mjs` を打つ | exit 1・`索引が見つかりません: …` → `索引は書き換えていません` |
| 行番号参照の検査 | `no line-number refs in code comments — 0件（47ファイル走査）`（新しいスクリプトの分だけ走査数が1つ増えました） |

---

## 3. 実行できなかった検証と代わりの手段、残っている確認

- **実行できなかった検証はありません**（単位1はオフラインで完結します。Playwright もサーバーも使っていません）
- ⚠️ **V1② はレビュー担当の手順**です。私も同じ手順（`git archive 086843a9` を展開し、コミット済みの `scripts/gen-queue-index.mjs` を置いて `--check`）を scratchpad で先に試し exit 0 を確認しましたが、**正式な確認はレビュー担当が `tmp/review/<タスク>/v1` で行ってください**
- V3〜V15 は**複製の上**で実行しました（`git archive HEAD` で scratchpad に展開し、`git init` してから1件ずつ `reset --hard`）。複製にはスナップショット（`tmp/api-cache/`）が無いため `meta.rarities` の検査はスキップになりますが、**検査の対象（QUEUE INDEX の行）には影響しません**
- 単位2・3 は範囲外です（`--posting-plan` は今のところ**不明な引数として exit 2** になります＝単位3で実装します）

---

## 4. 差分が設計書の想定と食い違ったところ

**ありません。** 変更は指定の5ファイルだけで、索引の差分も生成部の中（🔧 実装中 に1行移り、🟢 生きている が1件減る）だけです。

---

## 5. 設計書から外れたところと、その理由（どれも判断が要るものではないと考えていますが、念のため書いておきます）

| # | 内容 | 理由 |
|---|---|---|
| E1 | 起票案の**ディレクトリが読めない**ときに `起票案の走査に失敗: <dir>（<message>）` を起票案の問題として1行出します（§3-3 の表にはありません） | 旧検査にあった fail-closed をそのまま引き継ぎました（設計書 D10「fail-closed の性質は引き継ぐ」）。読めないディレクトリを黙って飛ばすと、起票案が静かに漏れます。正常時には出ません |
| E2 | 呼び名の重複は、**書式（`DRAFT_RE`）に一致した起票案どうし**で見ています。状態が不明（§3-3 の5番目）でも呼び名は取れているので、重複の判定には含めています | 仕様の「読めた起票案の間で」を「1行目から呼び名を読めた起票案」と解釈しました。V6・V11 の期待値はこの解釈でも、状態の問題が無いものに限る解釈でも同じになります |
| E3 | 索引は `"\n"` だけで行に分けます。**CRLF で保存された索引ではマーカーの行が完全一致しなくなり、`START 0個 / END 0個` で exit 1** になります | 書き戻すときに生成部の外の行を1バイトも変えないためです（往復の `cmp` で一致を確認済み）。今の索引に CR は0個です。安全側（赤）に倒れます |
| E4 | `gen-queue-index.mjs` 自身は「生成部の外の件数見出し」（§5-4 の4番目）を**検査しません**（validate だけが見ます） | §5-1 で「書かない」条件に挙がっているのは起票案の問題とマーカーの問題だけなので、それに従いました。書いた後に validate で止まります |
| E5 | 索引が無いときの `gen-queue-index.mjs` の出力は、validate と同じ `索引が見つかりません: …` → `索引は書き換えていません`・exit 1 にしました | §5-1 には索引が無い場合の記述がありません。「問題があれば書かずに exit 1」の形に合わせました |
| E6 | 見出しの絵文字は `\u{1F527}` のようにコードポイントのエスケープで書きました（`✅` だけは U+2705 をそのまま） | §5-3 の「異体字セレクタを混ぜない」ためです。V1 のバイト一致で確認済みです |

`TRANSLATION.md` などの訳語規約とは関係しない変更です（カードの訳・画面の文言には触れていません）。

---

## 6. 設計担当への確認事項

1. **本タスクの起票案の自由記述が古くなっています。** 1行目の後半に「**状態語が旧3語しか通らない期間なので旧語のまま置いている——単位1のコミットで…`実装中` に変える**」と書かれていて、単位1が入った今は事実と合いません。§8-2 に「それ以外の文字は変えない」とあるので**私は触っていません**。書き換えるかどうか、いつ書き換えるか（承認のときなど）の判断をお願いします
2. それ以外に、判断をお願いしたい点はありません（§5 の E1〜E6 はどれも仕様の範囲内の解釈だと考えています。違っていれば差し戻してください）

---

次の担当: **レビュー担当**（ラベル相当: **レビュー待ち**）
