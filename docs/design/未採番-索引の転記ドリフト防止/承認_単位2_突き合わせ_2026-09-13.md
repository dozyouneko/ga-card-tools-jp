**対象: 「索引の転記ドリフト防止」**（→ 継続） — 単位2（工程記録の突き合わせ）の承認。単位3（投稿計画）が残るのでタスクは継続。

# 承認 — 単位2「工程記録の突き合わせ」（2026-09-13）

⚠️ **これは issueコメントの代わり**です（GitHubアカウントが停止中で `gh issue comment` が打てないため、ファイルに残しています）。
復旧して採番されたら、本文をそのまま `gh issue comment <n> --body-file docs/design/未採番-索引の転記ドリフト防止/承認_単位2_突き合わせ_2026-09-13.md` で投函します。

- 日付: **2026-09-13**
- ⭐ **承認したのはユーザーです**（2026-09-12・リードセッション経由。設計担当が独断で承認したのではありません）。
  ユーザーの判断は「**単位1を承認し、単位2・単位3 まで続けてよい**」で、リードはこれを**単位2の承認としても**扱っています。
  本記録はその判断を、設計担当が根拠つきで書き留めたものです
- 対象コミット: **`d1a255a6`**（実装・3ファイル）／ **`42969cdd`**（実装報告）／ **`5bcc400d`**（レビュー判定）
- 設計書（正）: [`索引の転記ドリフト防止_設計.md`](索引の転記ドリフト防止_設計.md) **1.1 版** §4・§6（＋ §5-4 の S1-2）／検証項目 §9-2 の **V20〜V34** と §9-1 の **V17**
- 実装報告: [`実装報告_単位2_突き合わせ_2026-09-12.md`](実装報告_単位2_突き合わせ_2026-09-12.md)
- レビュー判定: [`レビュー判定_単位2_突き合わせ_2026-09-13.md`](レビュー判定_単位2_突き合わせ_2026-09-13.md)（**合格**）
- ⭐ **本承認で設計書は 1.2 版**になりました（変えたのは**単位3のスコープと検証項目だけ**＝§6）

---

## 1. 判定 — **承認**。⚠️ ただし **`（→ 継続）`**（タスクの完了ではない）

⭐ **1行目に `（→ 完了）` を書かなかった根拠**は設計書 §4-2 の定義です:

> `完了`＝**そのタスクを完了とする記録**／`継続`＝**単位の承認**や、続きの `追加指示_` を出す承認

本タスクは §8-1 で **単位1・2・3** に分かれており、**単位3（投稿計画）が丸ごと残っています**。
単位3は索引 §2 ③（復旧後に工程記録をissueへ投函する手順）が依存している経路で、⚠️ **無いと復旧時に
「どの記録をどの順に投函するか」を手で並べ直すことになり、本タスクが無くそうとしている転記そのものが復活します**。
したがってここで `（→ 完了）` を書くのは事実に反します。

→ **起票案の状態は `実装中` のまま**（`最終確認` を 2026-09-13 に更新し、自由記述を現況＝単位1・2 承認済みに直しました）。

## 2. 承認の理由

| # | 理由 |
|---|---|
| 1 | ⭐ **本丸（確定要件3）が実物で再現している**。V21 は 2026-09-11 に実在したずれ（左ペイン化の完了が起票案に反映されず、**索引まで整合していたので旧検査は緑だった**）の再現で、**queue index は緑のまま `TASK RECORD DRIFT` が1行だけ出る**。⚠️ **T7 が併発しない**ことまで確認されている |
| 2 | ⭐ **両辺が別の出所であることが双方向で実証されている**（レビュー M5）。**記録側だけを壊して赤**（V22〜V25・V27・V31・V32・X2〜X4）／**状態側だけを壊して赤**（V21・V26・V28〜V30）／**範囲そのものを壊して赤**（X1・V13・X5）。本プロジェクトが繰り返し踏んできた「**同源で常に緑**」型ではない |
| 3 | ⭐ **回帰が行単位で確かめられている**。`git archive 41e1207d` の複製と出力を1行ずつ突き合わせ、**差分は `task records in sync — …` の1行だけ**。他の検査の件数（行番号参照47ファイル・トークン68参照・ハイライト2495ページ/2517節）まで完全に一致 |
| 4 | **単位1の回帰が全数やり直されている**（V3〜V15）。`validate.mjs` が起票案の読み取りを1回に集約したため必要な手順で、**§9-2 末尾の「1行増える」注記（V5・V6・V11）まで一致** |
| 5 | **S1-2（CRLF の注記）が条件付きで実装されている**。V17 で注記あり・**V7〜V9 で注記なし**を両方実測＝**無条件に足して V7〜V9 の期待値を動かす**という失敗をしていない |
| 6 | **スコープが設計どおり**（M7）。`d1a255a6` はちょうど3ファイル、生成物0ファイル、`docs/` は**1バイトも変えていない**（単位2は起票案が増減しないので**これが正常**＝単位1の申し送り4） |
| 7 | **`CLAUDE.md` の U2 が設計書 §8-3 のブロックと差分0**（機械照合・位置も U1 の直後）。⚠️ **U3 は入っていない＝正しい** |
| 8 | **「未検証」が1件も無い**。単位2は完全にオフラインで検証でき、push を待つ項目も無い |

⚠️ **差し戻す理由は見つかりませんでした。**

## 3. 実装報告 §7 の確認事項 — 裁定

### 3-1. E-1（`工程記録の走査に失敗` が `validate` から到達しない）→ ⭕ **残す**

- **事実関係**: `readDrafts()` が `docs/design` 全体を再帰で歩くので、読めないフォルダは**必ず起票案側が先に赤くする**（報告 D4）。
  ⭐ レビューの **X8** が `readRecords()` を単体で呼んで `chmod 000` を当て、**正しく1行返し `count` が 100→97 に減る**ことを実測しています
  ＝報告の但し書き「**動くことを一度も確認できていない**」は解消しました
- **裁定**: ⭐ **残します。** 消すと「**記録だけが黙って0件になる**」経路ができ、⚠️ **S-1（次項）と合わさると本当に緑になりえます**。
  「呼ばれないから消す」は、**呼ばれるようになった日に誰も気づかない**型の削除です
- ⚠️ ⭐ **ただし「到達しないコードを残す」と決めた以上、腐らせない手当てが要ります**——
  レビューの推奨どおり **V46（直接呼びの検証）を単位3に足しました**（設計書 §9-3）。⭐ **X8 を検証項目に昇格させただけ**なので追加コストはほぼゼロです

### 3-2. E-4（T1 の記録の並べ方）→ ⭕ **追認。実装のまま（完了・取り下げをまとめてパス昇順）**

- 設計書 §6-2 の脚注は「記録の列挙は**パスの昇順**」としか書いておらず、**実装はこれに反していません**（レビューの評価と同じ）
- ⭐ **さらに「宣言語を書き添えなくてよい」根拠を実測しました**（2026-09-13・使い捨ての小さな木で `readRecords()` → `matchRecords()` を直接実行）:

  ```text
  取り下げ_a.md に（→ 完了）      → 「取り下げ_ の記録は（→ 取り下げ）を宣言してください」で弾かれる
  承認_b.md    に（→ 取り下げ）  → 「承認_ と 実物確認結果_ の記録は（→ 完了）か（→ 継続）を…」で弾かれる
  T1 の出力 → 完了と取り下げの両方が宣言されています: 「T」（…/取り下げ_d.md / …/実物確認結果_c.md）
  ```

  ⭐ **T1 に届く時点で、`（→ 完了）` は `承認_`・`実物確認結果_` からしか、`（→ 取り下げ）` は `取り下げ_` からしか来ません**
  （§4-3 の 5・6 が**記録の問題として先に落とす**ため）。⇒ **接頭辞を見れば宣言語は一意に決まる**ので、
  S-2 が言う「どれが完了でどれが取り下げか読み取れない」は**成り立ちません**（→ §4 の S-2）
- **設計書 §6-2 の脚注に、この根拠を書き足しました**（1.2 版）。⚠️ **将来また同じ議論をしないため**です

### 3-3. D7（9接頭辞のファイル名が工程記録以外に使えなくなる）→ ⭕ **`CLAUDE.md` に足す。文面は §8-3 の U3 に用意した**

- **事実関係**: レビューが `承認メモ.md` 相当で再現し、`工程記録の1行目に対象の宣言がありません` で **exit 1**。
  ⭐ **起票案の前方一致規約とまったく同型**で、**黙って通ることはありません**
- **裁定**: ⭐ **足します。** 起票案側には同じ注意書きが**あるのに**記録側に無いと、⚠️ **「書いていないほうは制約が無い」と読まれます**
- **足す場所と時期**: ⭐ **単位3の U3 と同じ箇条の末尾**（＝**1回の公開判断で済む**。レビューの推奨と同じ）。
  文面は設計書 **§8-3 の U3**（**2行に増やしました**）にそのまま置いてあります
- ⚠️ **開発担当が自分で足さなかったのは正しい判断です**（§8-3 は「この文面のまま」と指定していました）

## 4. 推奨基準の指摘（S）の採否

| # | 指摘 | 裁定 | 根拠 |
|---|---|---|---|
| **S-1** | 工程記録が0件でも咎めない（起票案側には `起票案が1件も見つかりません` があるのに非対称） | ✅ **採る。単位3のスコープに入れた**（設計書 §4-3・**V45**） | 下記 |
| **S-2** | T1 の括弧内に宣言語を添える | ❌ **採らない**（派生issueも起こしません） | ⭐ **実測して否定できました**（§3-2）。**接頭辞から一意に決まる**ので、添えても**同じ情報を2回書くだけ**＝本タスクが無くそうとしている転記の縮小版になります。⚠️ しかも T1 は**起きてはいけない状態**なので、文面を厚くする価値が最も低い場所です |
| **S-3** | 復旧後にフォルダを移すと**完了済みは赤・進行中は無言**という非対称がある | ✅ **採る。本コミットで派生起票案に書き足した**（実装は不要） | 下記 |
| **S-4** | `decl: decl ?? null` が分割代入の既定値と重複 | ❌ **採らない** | レビュー自身が「参考・直す必要は無い」としており、**動作は正しい**。⚠️ **触れば単位3で再レビューが要る**ので、価値が釣り合いません |

### ⭐ S-1 を採る根拠（＋ 期待値を実測で確定させた）

⚠️ **いま赤くなっているのは偶然です。** レビューの X1（`isRecordDir` を常に false）で16行の赤が出ますが、
その理由は「**完了済みの起票案が実在するから T5 が出る**」だけです。**記録0件のとき何が起きるか**を直接測りました:

```text
生きている → （無言＝緑）      設計中 → （無言＝緑）
実装中     → 実装中の根拠がありません     完了 → 完了の根拠がありません     取り下げ → 取り下げの根拠がありません
```

⇒ ⚠️ ⭐ **全タスクが `生きている`・`設計中` だけになった日に走査が壊れると、黙って緑になります。**
これは本設計が最も嫌う型（**検査が空回りして常に緑**）そのものなので、**起票案側の D8 と対称にします**。

- 仕様は設計書 **§4-3** に追記（`工程記録が1件も見つかりません: docs/design/未採番-* と docs/design/待ち行列`）。
  ⭐ **「記録の問題」として出す**ので §6-1 の省略が働き、**T5〜T8 の16行が、原因を名指しする1行に置き換わります**（＝**赤の質も上がる**）
- 検証は **V45**。⚠️ **X1 と同じ壊し方で、エラー行が「ちょうど2行」になり、`…の根拠がありません` が1行も出ないこと**まで期待値にしました
- ⭐ **単位3に入れた理由**: 同じ `readRecords()` を触るので**単位3のコミットに自然に収まり**、
  別タスクにすると ⚠️ **`scripts/` の公開判断が1回増えます**（配信物なので）

### ⭐ S-3 を採る根拠（実装は0行・ドキュメントだけ）

レビューの指摘を実測で**精密化**できました（§4 の表と同じ測定）。**赤くなるのは `完了`・`取り下げ`・`実装中` の3状態**（T5・T6・T8）で、
⚠️ **`生きている`・`設計中` のフォルダを先に畳むと無言で緑**です。

- 派生起票案 [`派生起票案_2026-09-11_復旧後の起票案と検査の扱い.md`](派生起票案_2026-09-11_復旧後の起票案と検査の扱い.md) に、
  **この非対称の表**と、**投函前の再測定に「生きているタスクでも1回試す」**を書き足しました
  （⭐ **そちらが本当の論点**なので、再測定が完了済みフォルダだけだと**論点を測らずに投函する**ことになります）
- ⭐ **あわせて「移したフォルダの数だけ `工程記録N件` の N が減ること」で確かめる**手順を提案として書いています。
  ⚠️ **決めるのは復旧時**（この起票案の役目は予告なので、ここで案を1つに絞りません）
- 設計書 **§12** の「GitHub復旧後」の行にも同じ非対称を明記しました

### 参考（レビューが基準に紐づけずに挙げたもの）

- ⭐ **§12 の「残る穴」の書き方** → ✅ **採りました**。実測のとおり、緑のまま残るのは「選び間違え」だけでなく
  **「最後の承認そのものを書かずに終える」**も同じです（状態 `実装中` ＋ `設計完了_` があれば T8 も満たすため）。
  ⚠️ **穴が設計書より広いのではなく、同型で手当ても同じ**ですが、⭐ **狭く書くと「自分は選び間違えていないから大丈夫」と読まれます**。
  設計書 §12 と、**`CLAUDE.md` の U2 の最後の1行**を直します（＝**単位3のコミットで差し替え**・§8-3・**V47**）
- 「状態を先に進める方向は自由」「NFC/NFD は fail-closed」 → **設計どおり。対応なし**

## 5. 派生issueの要否 → ⭐ **不要**（1件も起こしません）

| 候補 | 判断 |
|---|---|
| S-1 | **単位3のスコープに入れた**ので起票しません（別タスクにすると `scripts/` の公開判断が増える） |
| S-2・S-4 | ❌ **採らないと決めた**ので起票しません。⭐ **「やらないと決めたこと」を起票案にすると待ち行列に永久に居座ります**（単位1の承認と同じ判断） |
| S-3 | ❌ **既存の派生起票案「復旧後の起票案と検査の扱い」に吸収**（新しい論点ではなく、同じ論点の精密化） |
| D7 | ❌ **単位3の U3 に含めた**ので残件なし |
| `CLAUDE.md` の「未pushなので本番にはまだ出ていない」9箇所（設計書 §13） | ⚠️ **本タスクの範囲外**。リードへ報告済みで、別タスクとして扱います |

## 6. 本コミットで設計担当が直したもの

⚠️ **すべて `docs/` 配下＝本番配信物ではありません**（`functions/docs/[[path]].js` が404を返す）。

| ファイル | 直したもの | 出所 |
|---|---|---|
| 設計書（**1.2 版**） | §4-3 に **S-1 の仕様**／§6-2 の脚注に **E-4 の裁定と根拠**／§8-1 の単位3の行／§8-3 に **U3 の2行**と **U2 の1行差し替え**／§9-2 **V33 のフォルダ補記**／§9-3 に **V45・V46・V47**／§12 の2行（残る穴・GitHub復旧後） | レビュー S-1・S-3・参考・確認事項3／報告 E-1・E-4・D7 |
| 派生起票案（復旧後の起票案と検査の扱い） | **状態ごとの非対称の表**と、投函前の再測定の追記 | レビュー **S-3** |
| 本タスクの起票案 | 1行目の自由記述を現況へ（単位1・2 承認済み／残りは単位3）・`最終確認` を 2026-09-13 へ | 規約（§4-1・事実を変えた人が同じコミットで直す） |
| 索引（`待ち行列と復旧手順.md`） | 改版履歴に**第26版**を追記 ＋ **生成部を作り直した**（起票案2件の `最終確認` が動いたため） | 規約 |

### ⚠️ V33 のフォルダ補記について（レビューの確認事項3）

⭐ **自分で実測して確かめました**（`find docs/design -name '*日本語モード*'`）:

- `派生起票案_2026-09-04_日本語モードで0件になるときの表示.md` は **`docs/design/未採番-UI刷新の検討/`** にあります
- `docs/design/未採番-日本語モードの0件表示/` にある派生起票案は **`0件文言の二重定義`** と **`レアリティを索引で判定する`** の2件で、**別のタスク**です
- ⇒ **レビューの指摘は正しく、設計書の誤りでした。** §9-2 の V33 にフォルダを補記し、**そこを探しても無いこと**まで書きました

### ⭐ `CLAUDE.md` を本コミットで直さなかった理由

U2 の1行差し替え（§4 の参考）と U3 の追加は、**どちらも `CLAUDE.md`＝本番配信物**です。

- ⭐ **単位3のコミットにまとめれば、公開の判断は1回で済みます**（レビューの推奨と同じ）
- ⚠️ **いま直すと、単位2のコミット時点の「U2 が §8-3 と差分0」というレビューの検証結果が、
  次に誰かが見たときだけ食い違って見えます**。⭐ **§8-3 に「1.2 版で差し替えた・単位3で直す」と明記**したので、迷いません

## 7. 単位3の進め方

**次の担当: 開発担当（単位3）**（ラベル相当: **実装待ち**）。設計書は **1.2 版**を読むこと。申し送りは §8。

## 8. ⚠️ 単位3の開発担当への申し送り

1. ⭐ **設計書は 1.2 版**。1.1 版との差は**すべて単位3の話**です（§4-3 の S-1／§6-2 の脚注／§8-1／§8-3／§9-2 V33／§9-3 V45〜V47／§12）。
   ⚠️ **単位2までの仕様（§5・§6 の判定）は1文字も変わっていません**
2. ⚠️ **`CLAUDE.md` の変更は2箇所**です。⭐ **U3 の2行を足すだけではありません**——
   **U2 の最後の1行（`最後の承認で…選び間違え…`）を §8-3 の文面に差し替える**のを落とさないこと（**V47**）
3. ⭐ **S-1（§4-3）の実装位置**: `readRecords()` の**走査の直後**（`found.sort()` の前後どちらでもよいが、**1行目の読み取りより前**）。
   ⚠️ **`problems` に積む**こと（`count` を見て `validate` 側で判定しない）——**§6-1 の省略を働かせるため**です
4. ⚠️ **V45 の期待値は「ちょうど2行」**です。⭐ **単位2までは同じ壊し方で16行出ていた**ので、
   **行数が減ることが正しい**（`…の根拠がありません` が1行も出ないことまで見る）
5. ⭐ **V46 は `readRecords()` を直接 import して呼ぶ検証**です（`npm run validate` からは到達しません＝それが分かっていることを報告に書いてください）。
   ⚠️ **`chmod 000` は複製の上で行い、必ず `chmod 755` に戻す**
6. ⚠️ **V40〜V44（投稿計画）の期待値には、この承認のコミットで増えた記録（本ファイル）が入ります。**
   ⭐ **絶対数は §9 冒頭の `find … | grep -cE` で数え直す**こと
7. ⚠️ **`docs/` は1バイトも触らないのが正常**です（単位3でも起票案の状態は `実装中` のまま）。
   ⭐ 例外は、**あなたが書く実装報告そのもの**だけ（1行目は `**対象: 「索引の転記ドリフト防止」**`・`（→ …）` は付けない）
8. ⭐ **破壊試験は共有の作業ツリーで行わないこと**（`git archive HEAD` → scratchpad の複製。単位1・単位2とも実績あり）
9. ⚠️ **`scripts/` と `CLAUDE.md` は配信物**なので、**コミットまでで止める**（公開の可否はユーザーが判断します）
