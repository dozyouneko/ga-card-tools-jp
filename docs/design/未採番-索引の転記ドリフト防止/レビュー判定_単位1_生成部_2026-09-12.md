**対象: 「索引の転記ドリフト防止」**

# レビュー判定 — 単位1「生成部と索引の検査」（2026-09-12）

⚠️ **これは issueコメントの代わり**です（GitHubアカウントが停止中で `gh issue comment` が打てないため、ファイルに残しています）。
復旧して採番されたら、本文をそのまま `gh issue comment <n> --body-file docs/design/未採番-索引の転記ドリフト防止/レビュー判定_単位1_生成部_2026-09-12.md` で投函します。

- 日付: **2026-09-12**
- 対象コミット: **`516f62d1`**（実装）／ **`7a4b693c`**（実装報告）。レビュー範囲 **`e2ed701c..7a4b693c`**
- 設計書（正）: [`索引の転記ドリフト防止_設計.md`](索引の転記ドリフト防止_設計.md)（1.0）§9-1 の **V1〜V16**
- 実装報告: [`実装報告_単位1_生成部_2026-09-11.md`](実装報告_単位1_生成部_2026-09-11.md)
- ⚠️ **報告の「✅」は根拠にしていません。** V1〜V16 は**レビュー担当が独立に再実施**し、
  さらに**報告に無い破壊試験13本**（A1〜A4・B1〜B5・C1〜C4）を足しました
- 検証環境: 共有の作業ツリーは**一度も変更していません**。
  V1② は `tmp/review/未採番-索引の転記ドリフト防止/v1`（`git archive 086843a9`）、
  破壊試験は scratchpad の複製（`git archive 7a4b693c` + `git init`）の上で1件ずつ実施し、毎回 `git checkout -- .` で戻しました。
  回帰の実駆動は **3100番**（`PORT=3100 npm run dev`・検証後に停止済み）

---

## 判定: **合格**

| 基準 | 結果 | 証跡 |
|---|---|---|
| **M1** 設計適合 | ✅ | V1〜V16 の**全番号**に合否が付き、**期待値と完全一致**（下の対応表）。文言・行番号（生成部の 13/17行目・索引の 521行目）・件数（`実装中1 / 設計中0 / 生きている15 / 完了14 / 取り下げ1`）まで照合 |
| **M1-b** 既存条件との対称性 | ✅（該当なし） | 絞り込み・並び替えを足す変更ではない（UIコードは1行も変わらない）。⭐ ただし**同型の「既存で効いていた検査が新条件で効かない」**を独自に検証した——旧検査の **A 収録漏れ**（A1）と **B 件数一致**（A1/A2）が新検査でも赤くなることを実測（D10 の主張の裏取り） |
| **M2** 実駆動 | ✅ | 単位1は**CLIの検査**なので、**レビュー担当が自分で `node scripts/gen-queue-index.mjs` / `--check` / `npm run validate` を22通りの状態で実行**。加えて回帰のため主要5画面を **PC(1400×900) と 375×812 の両方**で Playwright 実駆動（`tmp/review/未採番-索引の転記ドリフト防止/{pc,375}-{top,card,deck,tournaments,modal}.png`） |
| **M3** 回帰 | ✅ | 共有ツリーで `npm run validate` → **exit 0 / `all checks passed.`**。主要5画面（トップ・カード詳細モーダル・カード個別ページ・デッキ構築・大会一覧）が両幅で 200・描画OK。**カード詳細モーダルは実際に開いて `#d-name` が「ロレイン、閃光の剣士」**（両幅） |
| **M4** 生成物の同期 | ✅ | 生成ロジック＝索引 §1 の生成部。**同じコミットで再生成済み**（`--check` が exit 0）。`cards/` `sets/` `data/` の生成物は **0ファイル**（設計書 §8-1 の要求どおり）。`gen-tl-json.mjs` は無関係 |
| **M5** 静かな劣化がない | ✅ | **破壊試験22本すべてが exit 1**。⭐ **両辺が同源でないことを双方向で実証**——索引側だけ変えても赤（V3・B1〜B3・B5）、起票案側だけ変えても赤（V4・A1〜A4）。走査の壊れ（V13）・マーカーの壊れ（V7〜V9・順序逆）・索引の消失（V12）・CRLF（B4）・スクリプトの消失（C2）も**すべて fail-closed**で、**緑に倒れる経路を見つけられなかった** |
| **M6** 公開安全性 | ✅ | 本番配信物の増減は **`scripts/gen-queue-index.mjs` 1ファイル増・`scripts/validate.mjs` と `CLAUDE.md` の更新**のみで意図どおり。秘匿情報の混入 **0件**（`token|secret|password|api_key` の grep が0）。`_headers` / `404.html` / キャッシュの前提には触れていない。**pushしていない**のも規約どおり |
| **M7** スコープ | ✅ | `516f62d1` の変更は **5ファイルちょうど**（`scripts/gen-queue-index.mjs`・`scripts/validate.mjs`・`CLAUDE.md`・本タスクの起票案・索引）。`grep -vE '^(scripts/\|CLAUDE\.md$\|"?docs/)'` が0件。⭐ **開発担当が触った `docs/` は §8-2 が明示的に許可した2ファイルだけ**（起票案の1行目1語・索引の生成部9行）で、実測でも**それ以外の文字は1バイトも変わっていない** |

⚠️ **「未検証」の項目はありません。**

---

## 設計書の検証項目（V1〜V16）との対応

⭐ 「エラー行」＝ `  - ` で始まる行。

| # | 期待値（設計書 §9-1） | レビュー担当の実測 | 合否 |
|---|---|---|---|
| **V1**② ⭐ | `git archive 086843a9` を展開し、実装後の `gen-queue-index.mjs` を同じ位置に置いて `--check` → exit 0・`索引の生成部は最新です（起票案31件）` | **exit 0**・`索引の生成部は最新です（起票案31件）`。⭐ **設計コミットの索引の生成部と、実装が今作る生成部がバイト一致**した＝正解データとの一致を独立に確認 | ✅ |
| **V2** | `npm run validate` が exit 0・`queue index up to date — 起票案31件（実装中1 / 設計中0 / 生きている15 / 完了14 / 取り下げ1）`・`issue drafts in sync` が0件 | **exit 0**・**その文面のまま一致**。`issue drafts in sync` は `grep -c` が **0**（`scripts/` 配下の残存文字列も 0）。起票案の実数も `find … \| grep -cE` で **31** | ✅ |
| **V3** | 生成部の `生きている（**15件**）`→16件 → exit 1・1行 `…（最初の差: 生成部の17行目）` | exit 1・**1行**・**17行目** | ✅ |
| **V4** | 決定性の自動検査の起票案を `設計中` に（生成し直さない）→ exit 1・1行 `…（最初の差: 生成部の13行目）` | exit 1・**1行**・**13行目** | ✅ |
| **V4b** | 生成し直す → `索引の生成部を更新しました（起票案31件）`／validate exit 0・`…（実装中1 / 設計中1 / 生きている14 / …）` | **そのとおり**。索引の差分は**生成部の中の5行だけ**（📝 設計中に1行移り、🟢 が1件減る） | ✅ |
| **V5** | 呼び名なしの `起票案_dummy.md` → exit 1・2行（書式違い／比較省略） | exit 1・**2行**・文面一致 | ✅ |
| **V6** | 呼び名を重複させる → exit 1・2行（重複の1行はパス2本を昇順で／比較省略） | exit 1・**2行**・パスの並びまで文面一致 | ✅ |
| **V7** | END を消す → exit 1・1行 `…（START 1個 / END 0個）` | exit 1・**1行**・一致 | ✅ |
| **V8** | START を2行に → `…（START 2個 / END 1個）` | exit 1・**1行**・一致 | ✅ |
| **V9** ⭐ | START 行に文字を足す → `…（START 0個 / END 1個）` | exit 1・**1行**・一致。⭐ **「同じ行に書くと Markdown が死ぬ」置き方が、数えられずに赤になる**ことを確認 | ✅ |
| **V10** | 索引末尾に件数入り見出しを足す → `件数を書いた見出しが生成部の外にあります: <行番号>行目「…」` | exit 1・**1行**・**521行目**（足した後の `wc -l` と一致） | ✅ |
| **V11** | 状態を `実装待ち` に → exit 1・2行（状態が不明／比較省略） | exit 1・**2行**・文面一致 | ✅ |
| **V12** | 索引をリネーム → exit 1・1行 `索引が見つかりません: …` | exit 1・**1行**・一致 | ✅ |
| **V13** ⭐ | 走査ルートの定数を `docs/design/リリース確認` に → exit 1・`起票案が1件も見つかりません: docs/design/リリース確認` を含む | exit 1・**2行**（上記＋比較省略）。⭐ **生成側も同じ定数で同じく exit 1**（`索引は書き換えていません`）＝定数1つで両方に効く | ✅ |
| **V14** ⭐ | V5 の状態で生成 → exit 1・最後が `索引は書き換えていません`・索引は無変更 | exit 1・標準エラーの**最後の行が `索引は書き換えていません`**・`git diff --quiet -- 索引` が **exit 0**・`.md.tmp` の残骸 **0件**・`--check` も同じ2行で exit 1 | ✅ |
| **V15** | 2回打つと2回目は「最新です」で mtime 不変／`--bogus` は exit 2 | 変更なしで2回 → 2回とも `最新です`・mtime 不変。**起票案を1件変えてから**は 1回目 `更新しました`・2回目 `最新です` で **mtime 同値**。`--bogus` / `--check extra` / `--posting-plan` はいずれも **exit 2**（単位3は未実装なので正） | ✅ |
| **V16** | 戻し忘れなし・変更5ファイル・`.tmp` → `renameSync` | 共有ツリーは `git status --short` が **0行**・`*.md.tmp` **0件**。変更は**5ファイルちょうど**。書き込みは `writeFileSync(<索引>.tmp)` → `renameSync` → 失敗時は `rmSync` で後始末（コード確認）。⭐ **さらに実測で裏取り**——生成後に索引の **inode が 250758 → 67345 に変わった**＝in-place truncate ではなく rename 置換 | ✅ |

### 設計書の文面どおりかの機械照合

| 対象 | 方法 | 結果 |
|---|---|---|
| `CLAUDE.md` の U1 | 設計書 §8-3 の ```markdown ブロックを抜き出し、`CLAUDE.md` の該当13行と `diff` | ⭐ **差分0（完全一致）** |
| 起票案の1行目（§8-2 手順2） | `git show` の word-diff | **`生きている`→`実装中` の1語だけ**。他の文字は不変 |
| 索引（§8-2 手順3） | `git show` | **生成部の中だけ**（🔧 実装中 0→1件・🟢 15件へ） |

---

## レビュー担当が独立に足した破壊試験（報告に無いもの・13本）

⭐ ねらいは **「検査が空回りして常に緑になる経路」**を探すこと。**1本も見つかりませんでした。**

| # | 試験 | 結果 |
|---|---|---|
| **A1** | 起票案を**1件足して**生成し直さない（＝旧検査 A「収録漏れ」相当） | exit 1（生成部の5行目） ✅ |
| **A2** | 起票案を**1件消して**生成し直さない | exit 1（5行目） ✅ |
| **A3** | 起票案の**最終確認の日付だけ**変える | exit 1（33行目） ✅ |
| **A4** | 起票案の**呼び名だけ**変える（重複しない名前へ） | exit 1（33行目） ✅ |
| **B1** | 索引の見出しの絵文字に**異体字セレクタ U+FE0F** を足す | exit 1（7行目） ✅ 目視では区別できない差も止まる |
| **B2** | 生成部の表の行末に**空白1つ** | exit 1（11行目） ✅ |
| **B3** | 生成部の表の**2行を入れ替える**（件数は変わらない） | exit 1（21行目） ✅ 順序も検査対象 |
| **B4** | 索引を**CRLF**に変換（報告 E3 の主張の裏取り） | exit 1（`START 0個 / END 0個`） ✅ **安全側（赤）に倒れる**ことを確認 |
| **B5** | 生成部を**空**にする（START と END を隣接） | exit 1（1行目） ✅ |
| **C1** | `設計中`にして生成 → 戻して生成 の**往復** | 索引が元のファイルと **`cmp` でバイト一致** ✅ 生成部の外を1バイトも変えない |
| **C2** | `scripts/gen-queue-index.mjs` を**消す** | `npm run validate` が **exit 1**（`ERR_MODULE_NOT_FOUND`） ✅ fail-closed |
| **C3** | `buildSection(readDrafts())` を2回呼ぶ | **一致**（63行）＝決定的。時刻依存の値なし ✅ |
| **C4** | 秘匿情報の混入 | `token\|secret\|password\|api_key` の一致 **0件** ✅ |

⭐ **A1〜A4 と V3・B1〜B3・B5 の対**が、この検査の最も重要な性質を示しています——
**索引だけを触っても赤／起票案だけを触っても赤**。両辺の出所が違うので、**「同源で常に通る」型ではありません**。

---

## 実装報告 §5 の解釈 E1〜E6 の評価（⚠️ 判断は設計担当・ここは評価と推奨のみ）

| # | 内容 | レビュー担当の評価 |
|---|---|---|
| **E1** | ディレクトリが読めないとき `起票案の走査に失敗: …` を足した | ⭕ **妥当**。D10「fail-closed の性質は引き継ぐ」に沿う。**追加方向（赤が増える）だけ**なので V の期待値を動かさない。**そのまま追認を推奨** |
| **E2** | 呼び名の重複は「書式に一致した起票案どうし」で見る | ⭕ **妥当**。状態が不明でも呼び名は取れており、重複を見逃す側に倒さない解釈。V6・V11 の期待値はどちらの解釈でも同じ（実測で確認）。**追認を推奨** |
| **E3** | CRLF の索引はマーカー0個で exit 1 | ⭕ **妥当**。**B4 で実測**し、緑ではなく赤に倒れることを確認。生成部の外を1バイトも変えない要件（C1 で確認）とのトレードオフとして合理的。**追認を推奨**（⭐ 将来 CRLF で保存された日に「マーカーが見えるのに0個」と出るので、エラー文に改行コードを添えると親切＝S1-2） |
| **E4** | 生成側は「生成部の外の件数見出し」を見ない | ⭕ **設計書 §5-1 の文言どおり**（書かない条件は起票案とマーカーの問題だけ）。`validate` が門なので穴にならない。**追認を推奨** |
| **E5** | 索引が無いとき生成側も `索引が見つかりません` → `索引は書き換えていません`・exit 1 | ⭕ **妥当**。§5-1 に記述が無い箇所を「問題があれば書かずに exit 1」の形へ寄せた自然な補完。**追認を推奨** |
| **E6** | 絵文字をコードポイントのエスケープで書いた | ⭕ **妥当**かつ**効果あり**。V1② のバイト一致と B1（U+FE0F を足すと赤）で、狙いどおり機能していることを確認。**追認を推奨** |

⭐ **E1〜E6 はいずれも仕様の範囲内の解釈で、設計の意図から外れていません。**
**差し戻す理由にはならない**と判断しました（どれも「赤が増える」方向で、緑に倒す解釈は1つもありません）。

実装報告 **§6 の確認事項1**（本タスクの起票案の自由記述が古くなっている）は、
⭐ **開発担当の判断が正しい**（§8-2 が「それ以外の文字は変えない」と書いている以上、勝手に直すべきではない）。
書き換えの要否・時期は設計担当の判断事項として下に回します。

---

## 推奨基準の指摘（S・合格を妨げないもの。対応要否は設計担当の判断待ち）

| # | 基準 | 指摘 |
|---|---|---|
| **S1-1** | S1 コード品質 | `scripts/gen-queue-index.mjs` は `package.json` の `scripts` に登録されていない（`CLAUDE.md` も `node scripts/…` と直接呼ぶ書き方）。設計書もそう書いているので**逸脱ではない**が、`npm run build:cards` 等との一貫性という意味では `"gen:queue-index"` を足す選択肢がある。⚠️ **足すと `CLAUDE.md`・索引 §4-1 の文面も変わる**ので、やるなら単位3のついでが安い |
| **S1-2** | S1 コード品質 | E3 の CRLF 時のエラーが `索引の生成部のマーカーが1組ではありません（START 0個 / END 0個）` で、**画面上はマーカーが見えているのに0個**と出る（原因が分かりにくい）。`（改行コードが CRLF になっていませんか）` のような一言を足すと1往復減る。⚠️ **緊急性は低い**（今の索引に CR は0個） |
| **S3** | S3 fail-open | fail-open は**1つも見つかりませんでした**（指摘なし）。⭐ 設計の要求（D10）を満たしています |

**S2（応答性）**: `npm run validate` 全体で数秒。新検査そのものは索引1本＋起票案31本の読み取りのみで体感差なし。指摘なし。

---

## 参考（基準に紐づかない・合否に含めない）

1. ⭐ **旧検査を説明した文書が2つ、実態と食い違う状態で残っています**（どちらも `docs/` 配下で本番配信対象外）:
   - `docs/design/待ち行列/起票案_起票案の状態行と索引の整合検査.md`（**状態は `完了`**）の107行目・139行目が、
     期待値として `issue drafts in sync — 起票案11件（…）` を書いている。**この検査はもう存在しない**ので、
     ⚠️ **GitHub復旧後にこの起票案をそのまま投函すると、成立しない検証項目が issue に載る**
   - `docs/design/README.md` の63行目が**旧3語の状態書式**（`生きている|完了|取り下げ`）のまま
   → ⭐ **どちらも設計書 §10・§13 が「承認時に設計担当が入れる／範囲外」と明記している**ので、
     **開発担当の落ち度ではありません**（M7 の逸脱にも当たらない）。**承認時の作業として拾い漏れないよう挙げておきます**
2. 索引 §4-3 の見出しが「⭐ **2026-09-11（第24版）で…広げた**」と単位2まで完了したような書きぶりですが、
   これは**設計コミット `086843a9` で設計担当が書いたもの**で単位1の範囲外。単位2の実装まで実態が追いつきます
3. `no line-number refs in code comments` の走査数が **46→47ファイル**に増えているのは、新しいスクリプトが増えた分で正常

---

## 設計担当への確認事項

1. **実装報告 §5 の E1〜E6 の追認**をお願いします（レビュー担当の評価は上表のとおり**6件とも追認を推奨**。
   仕様の解釈なので、最終的な可否は設計担当の判断です）
2. **本タスクの起票案の自由記述**（実装報告 §6-1）——「状態語が旧3語しか通らない期間なので旧語のまま置いている」という記述が、
   単位1が入った今は事実と合いません。⭐ **書き換えの要否と時期**（単位2のコミット／承認時／タスク完了時）をご指示ください。
   ⚠️ **開発担当が §8-2 の「それ以外の文字は変えない」を守って触らなかったのは正しい判断**です
3. **参考1の2件**（完了済み起票案の中の旧検査の期待値・`docs/design/README.md` の旧状態書式）を、
   §10 の承認時作業に含めるかどうか

---

## 証跡の置き場

- `tmp/review/未採番-索引の転記ドリフト防止/v1/` — V1② 用の設計コミット（`086843a9`）のツリー
- `tmp/review/未採番-索引の転記ドリフト防止/{pc,375}-{top,card,deck,tournaments,modal}.png` — 回帰の実駆動（両幅）
- `tmp/review/未採番-索引の転記ドリフト防止/証跡_V1-V16.md` — 実施メモ
- 破壊試験の複製は scratchpad（セッション固有・リポジトリ外）

⚠️ `tmp/` は gitignore 対象なので、本判定コメントが唯一の永続的な記録です。

---

## 残っている確認（pushしないと確認できない項目）

- **ありません。** 単位1は完全にオフラインで検証できます（ネットワーク・Cloudflare・D1 は関与しない）
- ⚠️ ただし **`scripts/` と `CLAUDE.md` は本番配信物**です。**push（または経路②）の可否は常にユーザーの判断**で、
  レビューはそれを要求しません

---

次の担当: **設計担当**（ラベル相当: **設計確認待ち**）
