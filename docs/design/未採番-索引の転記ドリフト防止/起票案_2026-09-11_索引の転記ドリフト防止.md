**状態: 完了**（最終確認 2026-09-13）— 呼び名「索引の転記ドリフト防止」。⭐ **単位1〜4のすべてが実装・レビュー合格・ユーザー承認まで完了**（[`承認_2026-09-13.md`](承認_2026-09-13.md)・**`（→ 完了）`**）。実装は **`516f62d1`**（単位1）/ **`d1a255a6`**（単位2）/ **`37ddf6e5`**（単位3）/ **`7cf5e1b2`**（単位4）、設計書は [`索引の転記ドリフト防止_設計.md`](索引の転記ドリフト防止_設計.md) **1.4 版**。⚠️ **`scripts/` と `CLAUDE.md` は配信物なので未公開**——⭐ **公開は 2026-09-17 の大会データ週次更新のバッチ**（ユーザー判断）。⚠️ ⭐ **GitHub復旧後に起票が必要**（まだissue番号を持たない。起票時は ✅ 完了済みの扱い＝`実装待ち` を付けない） ／ 索引: [`待ち行列と復旧手順.md`](../待ち行列/待ち行列と復旧手順.md)

# issueの起票案 — 索引が「同じ事実の手書きの写し」を持つため、検査の無い側から黙って古くなる

⚠️ **これは設計書ではなく「issueの下書き」。** GitHubアカウント停止中で `gh issue create` が打てないため、本文をここに用意する。

- ⭐ **起票時**: タイトル「索引が同じ事実の手書きの写しを持つため、検査の無い側から黙って古くなる」／ラベル `enhancement` `着手可`
  （⚠️ **`小タスク` を付けない**＝実装単位が3つある）
- 設計書（正）: [`索引の転記ドリフト防止_設計.md`](索引の転記ドリフト防止_設計.md)
- 要件の出所: 2026-09-11 にリードがユーザーと直接ヒアリングした（方針「転記をなくす」・要件4点）

## 症状

`docs/design/待ち行列/待ち行列と復旧手順.md`（索引）は、**タスクの状態・件数・本番の位置を
§0・§1・§2・§3・§5 と各起票案に手で書き写している**。`npm run validate` が見るのは
**§1 の件数と収録漏れだけ**なので、**検査の無い側から黙って古くなる**。

- 2026-09-11 にも、完了・公開済みの「デッキ構築の左ペイン化」が**起票案の状態行ごと「生きている」のまま**残っていた
  （**索引と状態行がどちらも古いまま整合していたので `validate` は緑**）。是正は `10f71a45`（索引 第23版）
- ⚠️ **同型の是正は改版履歴で7回目**（第3・7・13・14・18・19・23版）。**09-05 に入れた運用規約
  （承認記録を書いたら同じコミットで §2 と §5 も見る）でも止まらなかった**

## 直し方（要旨・詳細は設計書）

1. **状態は起票案の1行目だけに書く**（段階を `設計中` `実装中` まで広げる。公開状態は書かずに測る）
2. **索引の一覧は生成する**（`node scripts/gen-queue-index.mjs`）。古いと `npm run validate` が exit 1
3. **工程記録（承認・実物確認結果ほか）の1行目で対象の起票案を宣言させ、状態行と食い違えば exit 1**
4. **測れば分かる値（本番・GitHub・バックアップの位置、件数）は索引から消し、確認コマンドに置き換える**
