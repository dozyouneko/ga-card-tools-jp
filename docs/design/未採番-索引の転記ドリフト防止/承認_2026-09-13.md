**対象: 「索引の転記ドリフト防止」**（→ 完了）

> ⚠️ **これはissueコメントの代替**（GitHubアカウント停止中で `gh` が403）。GitHub復旧後に
> `gh issue comment <n> --body-file docs/design/未採番-索引の転記ドリフト防止/承認_2026-09-13.md` で投函する。
> ⚠️ **このタスクはまだ起票されていない**ので、投函の前に起票案から `gh issue create` する（→ §7）。

# 最終承認 — 「索引の転記ドリフト防止」（2026-09-13）

- 設計書: [`索引の転記ドリフト防止_設計.md`](索引の転記ドリフト防止_設計.md)（本コミットで **1.4 版**）
- 起票案: [`起票案_2026-09-11_索引の転記ドリフト防止.md`](起票案_2026-09-11_索引の転記ドリフト防止.md)（本コミットで状態を **`完了`** に）
- 実装: **`516f62d1`**（単位1）/ **`d1a255a6`**（単位2）/ **`37ddf6e5`**（単位3）/ **`7cf5e1b2`**（単位4）
- 直前のレビュー判定: [`レビュー判定_単位4_是正_2026-09-13.md`](レビュー判定_単位4_是正_2026-09-13.md)（**合格**・`2aed3e6d`）

---

## 1. 承認の理由

### ⚠️ ⭐ 承認したのはユーザーである

**2026-09-13 にユーザーが「単位1〜3を承認する。`scripts/validate.mjs` のコメントの是正（単位4）を
済ませてから完了にする」と判断した。** 単位4が本日レビュー合格したので、**この条件が満たされた**。
⭐ **設計担当が承認したのではなく、ユーザーの承認条件の充足を設計担当が確認して記録している。**

### 根拠

| # | 見たこと | 実測 |
|---|---|---|
| 1 | **4単位すべてがレビュー合格** | レビュー判定 **4件**・**`再判定_` は0件**＝ ⭐ **差し戻し0往復**（設計書の単位分けが実装可能だったことの傍証） |
| 2 | 設計の意図（**転記をなくす**）が満たされたか | ⭐ **満たされた**。§2 の表のとおり、手書きの写しは**撤去されたか、検査の両辺のどちらかになった**。単位4で **`scripts/` からも列挙が消え**、類型の写しは**設計書 §12 と `CLAUDE.md` U2 の2か所**（V47 で機械照合済みの組）だけになった |
| 3 | 回帰していないか | `npm run validate` **exit 0**。⭐ レビューが**是正前後の全出力を diff して0行**（64の成功行が同一）まで確認している |
| 4 | 公開面 | ⭐ **画面に出るものが1つも変わっていない**（`cards/` `sets/` `data/` `index.html` `tournaments/` `sitemap.xml` の差分**0ファイル**）。変更は `scripts/` 2ファイル ＋ `CLAUDE.md` ＋ `.claude/` ＋ `docs/` |

⚠️ **実装の動作確認はレビュー担当の仕事なので、ここでは再確認していない**（証跡は各レビュー判定にある）。
設計担当が見たのは**設計意図との適合**と**判断を要する問い（Q1・S-1・S-2）**だけ。

---

## 2. ⭐ タスク全体のまとめ — 何がどう変わって、次に同じずれが起きたらどこが止めるのか

### 2-1. 何が問題だったか

索引 `docs/design/待ち行列/待ち行列と復旧手順.md` は、**タスクの状態・件数・本番の位置・投稿順を
複数の節と各起票案に手で書き写していた**。`npm run validate` が見ていたのは **§1 の件数と収録漏れだけ**なので、
**検査の無い側から黙って古くなった**。⚠️ **同型の是正は索引の改版履歴で7回**（第3・7・13・14・18・19・23版）あり、
**2026-09-05 に入れた運用規約（承認記録を書いたら §2 と §5 も見る）でも止まらなかった**。

⭐ **とどめは 2026-09-11 の観測**——完了・公開済みの「デッキ構築の左ペイン化」が**起票案の状態行ごと「生きている」のまま**残っており、
⚠️ **索引 §1 もそれと整合していたので旧検査は緑だった**（＝**転記どうしが揃っていると検査が空回りする**）。

### 2-2. どう変わったか

| | 変更前 | 変更後 |
|---|---|---|
| **状態の正** | 索引 §1 の表と各起票案の1行目（**両方に書いてあった**） | ⭐ **起票案の1行目だけ**。段階を `生きている` / `設計中` / `実装中` / `完了` / `取り下げ` の5語に |
| **索引 §1** | 手書きの表 | ⭐ **生成物**（`node scripts/gen-queue-index.mjs` がマーカー間を作り直す） |
| **完了したという事実** | どこにも根拠が無い（ファイル名からは判定できない＝設計時の実測 M3） | ⭐ **工程記録の1行目の宣言**（`**対象: 「<呼び名>」**（→ 完了）`）が根拠 |
| **投稿順（復旧後の手順）** | ⚠️ 索引 §2-④ に**手書きの一覧**（第24版の時点で「11通」と書いて14ファイルを列挙するなどのずれが残っていた） | ⭐ **撤去**。`--posting-plan` が **git の追加時刻**から作る（§2-③） |
| **本番・GitHub・バックアップの位置** | 索引に書き写し（⚠️ `a40dcdf2` の本番公開が**1週間どこにも記録されないまま**だった） | ⭐ **書かずに測る**（索引 §3 のコマンド） |

### 2-3. ⭐ 次に同じずれが起きたら、どこが止めるのか

**`npm run validate` に成功行が2本増えた**（実測・本コミット時点）:

```text
queue index up to date — 起票案31件（実装中0 / 設計中0 / 生きている15 / 完了15 / 取り下げ1）
task records in sync — 工程記録109件（完了の根拠15 / 継続16 / 取り下げの根拠1）
```

| 起きうるずれ | 止めるもの | 出る行（要旨） |
|---|---|---|
| 起票案の状態を変えたのに索引を生成し直さない | 単位1の検査 | `QUEUE INDEX DRIFT:`（⚠️ **生成部を手で直す**のもここで落ちる） |
| ⭐ **承認を書いたのに起票案の状態を直さない** | **T4** | `状態行が工程記録と食い違っています: 「<呼び名>」は <記録> で完了したのに、起票案の状態が「<状態>」です` |
| 状態を `完了` にしたが宣言した記録が無い | **T5** | `完了の根拠がありません: …` |
| 状態が工程記録より遅れている（`実装報告_` があるのに `生きている` など） | **T7** | `状態が工程記録より遅れています: …` |
| `実装中` なのに `設計完了_` も `追加指示_` も無い | **T8** | `実装中の根拠がありません: …` |
| ⭐ **走査範囲が壊れて記録が全滅する** | 単位3で足した検知 | `工程記録が1件も見つかりません: …`（⚠️ **これが無いと、全タスクが `生きている`・`設計中` だけの日に黙って緑になる**） |
| 投稿順の書き写しが古くなる | ⭐ **転記そのものを撤去した**（`--posting-plan`） | — |

### 2-4. ⚠️ ⭐ それでも残る穴 — **この承認自身がその瞬間だった**

設計書 §12 の筆頭は「**最後の承認で完了を宣言し損ねる**」（`（→ 継続）` の**選び間違え**と、
**承認そのものを書かずに終える**の2通り）。⚠️ **状態と記録が整合しているので検査は緑のまま**。

⭐ **本承認はまさにその場面**なので、**同じコミットで**次を全部やった（＝規約どおり）:

1. 本記録の1行目を **`（→ 完了）`** にする
2. 起票案の1行目を **`完了`** にする
3. `node scripts/gen-queue-index.mjs` で生成部を作り直す
4. `npm run validate` が **exit 0**

⚠️ ⭐ **自分の検査で自分が止められるかを、承認のこの場で実際に壊して確かめた**（どちらも直後に復元し、`git status` で確認済み）:

| 破壊試験 | 実測 |
|---|---|
| **2 を飛ばす**（承認は `（→ 完了）`・起票案は `実装中` のまま生成し直す） | **exit 1**。`TASK RECORD DRIFT:` / `  - 状態行が工程記録と食い違っています: 「索引の転記ドリフト防止」は docs/design/未採番-索引の転記ドリフト防止/承認_2026-09-13.md で完了したのに、起票案の状態が「実装中」です（…）`＝ ⭐ **T4 が名指しで止める** |
| **3 を飛ばす**（起票案は `完了` にしたが生成し直さない） | **exit 1**。`QUEUE INDEX DRIFT (…):` / `  - 索引の生成部が起票案と一致しません（最初の差: 生成部の35行目）` ＋ 案内行 |

⭐ **手当ては「生成部の先頭 🔧 実装中 に残っていないかを見る」**で、本コミット後の生成部は **🔧 実装中（0件）**（実測）。
⚠️ ⭐ **ただし検査が止めるのは「承認記録を書いたあと」だけ**——**承認そのものを書かずにセッションを終えた場合は、
状態も記録も動かないので緑のまま**（＝§12 の2つ目の類型。**構造では受けられないので目視に頼る**）。

---

## 3. ⭐ Q1（V56 の期待値）の裁定 — **実装は現状のまま／設計書の期待値を直す**

**問い**（実装報告 §6・レビュー §3）: git の無い環境で `--posting-plan` を打つと、標準エラーが
**git 自身の `fatal: not a git repository …` ＋ 自前の1行 = 2行**になる。設計書 1.3 版 V56 の期待値は「**1行だけ**」。

### 裁定: ⭐ **実装は変えない。V56 の期待値のほうを実物に合わせて直す**（レビューの推奨どおり）

| 判断 | 根拠 |
|---|---|
| **実装の不足ではない** | 実装は **§8-4-c の指定コードブロックと完全一致**（レビューが機械照合）。自前メッセージは**1行しか出していない** |
| ⭐ **設計書が自己矛盾していた** | 2行目を消すには `firstAdditions()` に `stdio` を足すしかないが、**同じ §8-4-c が「`firstAdditions()` 自体は変えない」と明示している**＝ ⚠️ **1.3 版の期待値は、同じ節の指定を守る限り達成できなかった** |
| ⭐ **2行目は残すほうがよい** | `fatal:` は**「なぜ git が動かなかったか」を言う唯一の行**。塞ぐと `not a git repository` と `git: command not found` が区別できなくなる。⚠️ **`--posting-plan` は GitHub 復旧当日に初めて動かす道具**なので、そこで最も知りたい情報がこれ |
| ねらいは達成済み | スタック行 **10本 → 0本**・stderr **31行 → 2行**・`Error:` 行 **0本**・標準出力 **0バイト**・exit **1**（fail-closed のまま） |

### 本コミットで直したもの

- **§9-4 の V56 の期待値**を「**自前のメッセージがちょうど1行**・**`Error:` 行とスタック行が0本**・**標準出力は空**。
  ⚠️ **git 自身の `fatal:` 1行は `execFileSync` の既定で素通りする＝正常**（合計2行）」に差し替えた
- **§8-4-c に「⭐ 2行目について」を追記**——**残すことが確定した仕様**であること、
  ⚠️ **将来1行に倒すなら `e.stderr` の1行目を必ず添えること**（原因の情報を失わないため）を書いた

⚠️ ⭐ **実装は1文字も変えていない**（`scripts/` は本コミットで触っていない）。

---

## 4. レビューの推奨指摘（S）の採否

### S-1 ⭐ **採用** — `.claude/agents/design.md` の「残る穴」から類型の列挙を落とした（**本コミットで実施**）

- **指摘**: `.claude/agents/design.md` に「残る穴」の説明が1か所あり、⚠️ **2つの類型のうち「選び間違え」しか書いていない**
  （もう1つの「**承認そのものを書かずに終える**」は 1.2 版で足したもの）
- ⭐ **採用の理由**: **本タスクが無くそうとしている型そのもの**だから。設計書 §12 は
  「**類型を書くのはここと `CLAUDE.md` U2 の2か所だけ**」と決めており、**design.md は3か所目**にあたる。
  ⚠️ **単位4で `validate.mjs` から列挙を落としたのとまったく同じ構図**なので、**同じ手当て（案C＝参照に置き換える）**を当てた
- **直した中身**: 1行目を `CLAUDE.md` と同じ言い方（「**完了を宣言し損ねると**」）に直して**特定の類型を名指ししない**形にし、
  **「類型は `CLAUDE.md` の同じ節が正・ここに書き写さないこと」**を添えた
  - ⭐ **V51 には影響しない**（`git grep -c "選び間違" -- CLAUDE.md scripts/` の対象に `.claude/` は入っていない）。実測でも `CLAUDE.md:1` の1行だけのまま
- ⚠️ **この行を入れたのは設計担当のコミット `41e1207d`（単位1の承認）**＝ **レビューが本単位のスコープ外として合否に含めなかったのは正しい**。
  ⭐ **設計担当が入れた写しを設計担当が畳む**のが筋なので、承認と同じコミットで直した

### S-2 ⭐ **不採用**（⚠️ ただし条件付きで将来に申し送り）

- **指摘**: `--posting-plan` のエラー行に「**どこで git を打ったか**」が出ないので、別のディレクトリで打つと気づきにくい
- ⭐ **不採用の理由**: **レビュー自身が「Q1 を『1行だけ』に倒すなら、この不足が表面化する」と書いている**とおり、
  **§3 で2行のままに倒したので前提が消えた**。⭐ **いま `cwd` を足すと、git の `fatal:` が既に言っていることと重複する**
  （＝**同じ事実の写しを増やす**＝本タスクが減らそうとしているもの）
- ⚠️ **申し送り**: 将来 `stdio` を塞いで1行に倒すなら、**そのときは `cwd` か `e.stderr` の1行目を必ず添える**。
  ⭐ **§8-4-c の追記に書いたので、コードを触る人の目に入る**

---

## 5. 実装報告の逸脱2件 — ⭐ **どちらも追認する**

| # | 逸脱 | 追認の理由 |
|---|---|---|
| **①** | **V54 を共有ツリーではなく、作業ツリー外へ複製した `gen-queue-index.mjs` の上で実施**した | ⭐ **望ましい変更**。⚠️ **設計書 V54 自身が「`KINDS` を戻し忘れると V53 の一致が崩れる」と警告していた**——**戻し忘れの余地を構造で消した**のは設計の意図に沿う。`buildPostingPlan()` は `ROOT` を見ないので複製でも挙動が同じ（レビューも独立に確認）。⭐ **副産物として「是正前のリテラルでは症状が出る」ことまで実測できた**＝期待値の裏取りが1段強くなった |
| **②** | **V52 後半（V47 の再実施）を `diff` ではなく完全一致＋`difflib` で実施**した | ⭐ **期待値は変わっていない**（「差分0行」と同値）。⚠️ **`CLAUDE.md` 側は前後に別の行が続くので、切り出し位置を人が決めるとそこで誤りが混入する**——**部分文字列として存在するかを見れば切り出しが要らない**のは妥当。レビューも**別の方法で**やり直して同じ結論を得ている |

⭐ **どちらも「手段の変更で、期待値は変えていない」**。⚠️ **設計書の検証手順のほうを直す必要は無い**
（V54 は「1つずつ行い必ず戻す」という**共有ツリーを使う場合の**注意として引き続き有効）。

---

## 6. 派生issueの要否 — ⭐ **新規の起票は不要**

| 検討したもの | 判断 |
|---|---|
| **復旧後の起票案と検査の扱い**（リネームで記録が範囲外に出ると検査が赤／`--posting-plan` から無言で消える） | ⭐ **既に派生起票案がある**（[`派生起票案_2026-09-11_復旧後の起票案と検査の扱い.md`](派生起票案_2026-09-11_復旧後の起票案と検査の扱い.md)・状態 `生きている`）。⚠️ **単位2・単位3のレビューで得た実測2件も追記済み**＝**重複して起票しない** |
| **S-2（`cwd` を添える）** | ⚠️ **起票しない**。§4 のとおり前提が消えており、⭐ **条件（1行に倒すとき）は §8-4-c のコードのそばに書いた**ほうが届く |
| **`CLAUDE.md` の「未pushなので本番にはまだ出ていない」9箇所**（設計書 §13 の範囲外） | ⚠️ **本タスクでは起票しない**。⭐ **同じ「現在形の状態を散文に書いた」型**だが、`CLAUDE.md` は**配信物**なので公開の判断とセットになる。**リードへ報告済み**で、ユーザーが扱いを決める |
| `--posting-plan` が `npm run validate` から呼ばれない（壊れても復旧当日まで気づかない） | ⭐ **起票しない**。**索引 §2-③ に「投函を始める前に一度 `--posting-plan` を通す」を書いた**（単位3の S-3）ので手順で受けている |

---

## 7. ⚠️ 残件と次の担当

### 7-1. 公開（⭐ **ユーザー判断済み・セッションは実行しない**）

- ⚠️ **`scripts/` と `CLAUDE.md` は本番配信物**なので、本タスクの成果は**まだ公開されていない**
- ⭐ **公開は 2026-09-17 の大会データ週次更新のバッチに乗せる**とユーザーが決めている（ハンドオフラベル相当: **`push待ち`**）
- ⚠️ ⭐ **そのとき「一緒に出るもの」を必ず提示すること**（`CLAUDE.md` の週1更新の節）。
  経路②は `git archive HEAD` から作るので、**未公開の他タスクが乗っていれば一緒に本番へ出る**:

  ```bash
  git diff --stat <本番位置>..HEAD -- . ':(exclude)docs/*'
  ```

- **残っている確認は1つだけ**（レビュー §6）: 公開後に `/scripts/gen-queue-index.mjs` が配信されること。
  ⭐ **公開面の増減は0**なので、公開時の確認項目は増えない

### 7-2. ⚠️ ⭐ GitHub復旧後 — **このタスクはまだ起票されていない**

⚠️ **本タスクは起票案のままで、issue番号を持たない。** 復旧後は索引 §2-② の手順で起票し、
**本記録を含む工程記録14件を §2-③ の `--posting-plan` の順に投函する**。

- ⭐ **起票時の扱いは「✅ 完了済み」の行**＝ **`実装待ち` を付けない**・ラベルは **`push待ち`**
  （§0 の push が済んでいればクローズ）
- ⚠️ **投函は §2-④ のフォルダのリネームより先**。⭐ **リネーム後に `--posting-plan` を打つと、
  そのタスクの記録が exit 0 のまま計画から消える**（無言の投函漏れ。→ 派生起票案）
- ⚠️ **リネームすると本タスクの工程記録が検査の範囲外に出て `完了の根拠がありません` で exit 1 になる**（設計どおりの赤）。
  ⭐ **着手前に派生起票案「復旧後の起票案と検査の扱い」を決めること**

### 7-3. 本コミットで済ませたこと

1. 本記録（1行目 **`（→ 完了）`**）
2. 起票案の1行目を **`完了`** に（`最終確認` も 2026-09-13）
3. `node scripts/gen-queue-index.mjs` で索引 §1 の生成部を再生成
4. 索引の改版履歴に **第28版**・§4-4 の**撤去済みの表への参照**を是正
5. 設計書を **1.4 版**（V56 の期待値・§8-4-c の追記）
6. `.claude/agents/design.md` の S-1
7. `npm run validate` **exit 0**

**次の担当: ユーザー**（公開の可否＝ **2026-09-17 のバッチ**）。⭐ **設計・実装・レビューの工程はこれで終わり。**
