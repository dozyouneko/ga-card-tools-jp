**対象: 「索引の転記ドリフト防止」**

# 設計完了 — 索引の転記ドリフトを構造で止める（2026-09-11）

⚠️ **これは issueコメントの代替**（GitHubアカウント停止中で `gh issue comment` が打てないため、ファイルに残す）。
復旧して採番されたら、本文をそのまま `gh issue comment <n> --body-file docs/design/未採番-索引の転記ドリフト防止/設計完了_2026-09-11.md` で投函する。

- 日付: **2026-09-11**
- 設計時の HEAD: **`10f71a45`**（作業ツリーはクリーン）
- 次の担当: ⭐ **開発担当**（ラベル相当: **`実装待ち`**）——**単位1から**

---

## 1. 設計書のパス

- ⭐ **正**: [`索引の転記ドリフト防止_設計.md`](索引の転記ドリフト防止_設計.md)（1.0）
- 起票案: [`起票案_2026-09-11_索引の転記ドリフト防止.md`](起票案_2026-09-11_索引の転記ドリフト防止.md)（呼び名「**索引の転記ドリフト防止**」）
- ⭐ **正解データ**: 設計コミットの [`待ち行列と復旧手順.md`](../待ち行列/待ち行列と復旧手順.md) の生成部（`<!-- QUEUE-INDEX:START -->` 〜 `END`）。
  **単位1の実装はこれとバイト一致しなければならない**（V1）

## 2. 対象範囲

ユーザーが選んだ方針「**転記をなくす**」の確定要件4点を、**3つの実装単位**で入れる:

| 単位 | 中身 | 変えるファイル |
|---|---|---|
| **1. 生成部と索引の検査** | 起票案の1行目（5つの状態・呼び名）を読み、索引 §1 を生成する `scripts/gen-queue-index.mjs`。`validate` の旧検査 `issue drafts in sync` を**置き換える** | `scripts/gen-queue-index.mjs`（新規）・`scripts/validate.mjs`・`CLAUDE.md`・⚠️ **本タスクの起票案と索引の生成部**（設計書 §8-2） |
| **2. 工程記録の突き合わせ** ⭐ | 工程記録の1行目の `**対象: 「呼び名」**`（承認・実物確認結果は `（→ 完了）`/`（→ 継続）` 必須）と起票案の状態を突き合わせる＝⭐ **本日のずれ（完了の記録があるのに状態が `生きている`）を exit 1 で止める** | `scripts/gen-queue-index.mjs`・`scripts/validate.mjs`・`CLAUDE.md` |
| **3. 投稿計画** | 復旧後の工程記録の投稿順を git の追加時刻から出す `--posting-plan` | `scripts/gen-queue-index.mjs`・`CLAUDE.md` |

⭐ **着手順は 1 → 2 → 3**（2・3 は 1 の起票案の読み取りを使う）。**1単位ずつ**実装報告 → レビュー → 承認。

## 3. 検証項目の要約（設計書 §9・**計38項目**＝17 / 16 / 5）

- **単位1: V1〜V16（17項目・V4b を含む）** — ⭐ **V1＝正解データとのバイト一致**、V3〜V15 は破壊試験（生成部の手編集・生成忘れ・呼び名の欠落／重複・マーカーの欠落／重複／同じ行の文字・生成部の外の件数見出し・未知の状態語・索引なし・走査の壊れ・壊れた起票案では書かない・冪等）
- **単位2: V20〜V34（16項目・V21b を含む）** — ⭐ **V21＝本日のずれの再現**（左ペイン化の起票案を `生きている` に戻して生成し直す → queue index は緑なのに exit 1・エラー行ちょうど1行）。V33 は**残る穴が設計書 §12 どおりであること**（exit 0 が正）
- **単位3: V40〜V44（5項目）** — ⑬の14件の順（同時刻は承認が先）・⑪の `実装報告_S1-1`・⑥の残作業が承認の後に出ること・未コミットで exit 1・ファイルを書かない
- ⭐ **期待値はすべて、設計担当が仕様どおりのプロトタイプを `docs/` の複製上で実行して得た実物**（共有の作業ツリーは触っていない）

## 4. 判断済み事項（設計書 §2-1 の D1〜D14）

- ⭐ **1タスク＝起票案1枚**。起票案の無かった完了タスク3件（辞書・フレーバー・行番号参照）には**後付けした**
- ⭐ **状態は5つ**（`生きている` / `設計中` / `実装中` / `完了` / `取り下げ`）。⚠️ **`公開済み` は足さない**——
  Cloudflare の公開記録を測ったら **2026-09-04 の本番公開 `a40dcdf2` が本番公開として索引に記録されておらず、「日本語モードの0件表示」は 09-04 から公開済みだった**（索引は「09-10」と書いていた）＝**公開状態は書かずに測る**
- ⭐ **タスクの識別子は呼び名**（パスだと復旧時のリネームで全宣言が壊れる）
- ⭐ **過去分は遡って全件に宣言を足した**（日付で線を引かない）。**ファイル名は見ない**ので承認記録の命名の揺れ（9/7/10件）は誤検出にならない
- ⭐ **投稿順（旧 §2-④）は索引に置かず、復旧時にコマンドで出す**（並べるには git が要り、⚠️ **手書きの §2-④ には「11通」と書いて14件・S1-1 の欠落・残作業の順序誤りの3件のずれが残っていた**）
- ⭐ **設計担当が本コミットでデータ移行を済ませた**（起票案26件に呼び名・起票案5件を新規・完了済み11件に「起票時」・工程記録95件に宣言・索引を第24版に）。**旧検査のままで `npm run validate` は exit 0**

## 5. ⚠️ 実装担当への申し送り

1. ⚠️ ⭐ **単位1では、V1（何も変えずに `--check` が最新）を確かめてから、本タスクの起票案の状態を `生きている` → `実装中` にして生成し直す**（設計書 §8-2）。
   ⭐ **開発担当が `docs/` を触るのはこの2ファイル（起票案1行目・索引の生成部）だけ**（本タスクに限った許可）
2. ⚠️ **`CLAUDE.md` は設計書 §8-3 の文面のまま**、各単位のコミットに入れる
3. ⚠️ ⭐ **本タスクの工程記録も新しい規約に従う**——実装報告・レビュー判定の**1行目は `**対象: 「索引の転記ドリフト防止」**`**
   （単位2の検査が入ると、書き忘れは自分の検査で exit 1 になる）。ファイル名は `実装報告_単位1_生成部_<日付>.md` のように単位を入れる
4. ⚠️ **`scripts/` と `CLAUDE.md` は配信物**＝**コミットまでで止め、公開の可否はユーザーが判断する**
5. ⚠️ **生成物（`cards/` `sets/` `data/` 等）は0ファイル**
6. ⚠️ **本コミット時点では、プロトタイプの突き合わせで本タスクだけが「状態が工程記録より遅れています」に当たる**（この設計完了記録があるのに起票案が旧語の `生きている` のため）。
   **単位1の手順2で解消する意図した状態**で、単位2の検査はまだ無いので `validate` は赤くならない

## 6. 未決事項

**なし。** ⭐ **ユーザーの判断が要る点も無い**（`公開済み` を状態に足さなかったのは要件の「例」からの逸脱だが、要件4と実測から一意に決まる。設計書 §2-2）。

## 7. 範囲外（リードへ）

- ⚠️ **`CLAUDE.md` の「未pushなので本番にはまだ出ていない」9箇所**は経路②で公開済みのため**すべて誤り**（同じ「現在形の状態を散文に書いた」型）。
  **`CLAUDE.md` は配信物**なので、公開の判断とセットで**別タスク**として扱うことを勧める（設計書 §13）
- 派生1件を起票案にした: [`派生起票案_2026-09-11_復旧後の起票案と検査の扱い.md`](派生起票案_2026-09-11_復旧後の起票案と検査の扱い.md)
  （GitHub復旧でフォルダをリネームすると本検査が設計どおり exit 1 になる。**着手は復旧時**）
