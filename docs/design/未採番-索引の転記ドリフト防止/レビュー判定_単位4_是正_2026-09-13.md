**対象: 「索引の転記ドリフト防止」** — 単位4（承認前の是正：コメントの正を1つに／`PLAN_KIND_ORDER` の導出／git 無し環境の1行）のレビュー判定。合格。

# レビュー判定 — 単位4「承認前の是正」（2026-09-13）

⚠️ **これは issueコメントの代わり**です（GitHubアカウントが停止中で `gh issue comment` が打てないため、ファイルに残しています）。
復旧して採番されたら、本文をそのまま
`gh issue comment <n> --body-file docs/design/未採番-索引の転記ドリフト防止/レビュー判定_単位4_是正_2026-09-13.md` で投函します。

- 対象コミット: **`7cf5e1b2`**（実装）〜 **`8c633b14`**（実装報告）／範囲 `e609cd28..8c633b14`
- 指示書: [`追加指示_承認前の是正_2026-09-13.md`](追加指示_承認前の是正_2026-09-13.md)／設計書 **1.3 版** の §8-4（仕様）・§9-4（**V50〜V57**）
- 実装報告: [`実装報告_単位4_是正_2026-09-13.md`](実装報告_単位4_是正_2026-09-13.md)
- ⚠️ **報告の「検証済み」は根拠にしていません。** V50〜V57 は**すべてレビュー担当が独立に再実施**し、
  絶対数（工程記録の件数）も**自分で数え直し**ました。証跡は `tmp/review/未採番-索引の転記ドリフト防止/u4/`（gitignore）。

---

## 判定: 合格（M1〜M7 すべて充足）

| 基準 | 結果 | 証跡 |
|---|---|---|
| **M1** 設計適合 | ✅ | V50〜V57 の**全8番号**に合否（下表）。⚠️ **V56 だけは「実装は §8-4-c の指定コードのまま・設計書の期待値のほうが実物と合わない」**と結論（下の §3・Q1）。M1-b の型（絞り込み・並び替えの副作用）は**本単位に該当なし**（画面に出るものが1つも変わらない） |
| **M2** 実駆動 | ✅ | 3100番で PC(1280) と **375px** の両方を操作。トップ・**カード詳細モーダル**・カード個別ページ・セットページ・デッキ構築・大会一覧。`u4/top-pc.png` `top-375.png` `modal-pc.png` `modal-375.png` `cardpage-*.png` `deckbuilder-*.png` `tournaments-*.png` `setpage-pc.png` |
| **M3** 回帰 | ✅ | `npm run validate` **exit 0**（`u4/10-validate-stdout.txt`）。⭐ **さらに強い回帰**として、複製上で**是正前後の validate 全出力を diff**＝**差分0行・64行の成功行が同一**（`u4/13-validate-before-after.txt`）。主要5画面はコンソールエラー無し（残るのは Cloudflare RUM の CORS と、静的サーバーゆえの `/api/me` 404 のみ＝環境要因） |
| **M4** 生成物の同期 | ✅ | 生成ロジックは不変。`cards/` `sets/` `data/` `index.html` `tournaments/` `sitemap.xml` の差分**0ファイル**（`u4/01-scope.txt`）。索引の生成部も `--check` が「最新です」・再生成しても `git status` が空（`u4/12-regression-u1u3.txt`） |
| **M5** 静かな劣化がない | ✅ | 新しいガードを**わざと壊して落ちること**を確認（V55）。⭐ 両辺が同源でない（`PLAN_CLOSING_KINDS` のリテラル ↔ `KINDS`）。⭐ **`PLAN_KIND_ORDER` の -1 バグ（無言で並びが狂う）を是正前のコードで再現し、是正後に消えることを確認**（V54・`u4/06-v54-*.txt`） |
| **M6** 公開安全性 | ✅ | 本番配信物の増減なし（新規/削除ファイル0・変更は `scripts/` 2ファイルのみ）。秘匿情報の混入なし。`_headers` `404.html` `functions/` は**0ファイル**変更。⭐ `CLAUDE.md`（配信物）は**1文字も変わっていない**（V52） |
| **M7** スコープ | ✅ | 変更は **`scripts/validate.mjs` / `scripts/gen-queue-index.mjs` / 実装報告1件**の**計3ファイルだけ**。`docs/` は報告のみ・`.claude/` は0件（`u4/01-scope.txt`）。⭐ **CLAUDE.md の規約に沿っている**: 担当パス（`scripts/` と生成物）のみ・`git add -A` 無し・push していない（`scripts/` は配信物）・記録の1行目の書式を守っている |

---

## 1. 設計書 §9-4 の検証項目との対応（**すべてレビュー担当が独立に再実施**）

| # | 期待（設計書 1.3 版 §9-4） | レビューの実測 | 合否 |
|---|---|---|---|
| **V50** | 是正後 `選び間違`**0** / `残る穴`**1** / `設計書 §12`**1**（是正前は 2/1/1） | 是正後 **0 / 1 / 1**。是正前（`7cf5e1b2^`）を自分で測って **2 / 1 / 1**＝設計書の併記値と一致。⭐ 2本目・3本目が是正前と同値＝「コメントごと消した」ではない | ✅ |
| **V51** | `git grep -c "選び間違" -- CLAUDE.md scripts/` が **`CLAUDE.md:1` の1行だけ**（是正前は2行） | 是正後は **`CLAUDE.md:1` の1行だけ**。是正前は `CLAUDE.md:1` と `scripts/validate.mjs:2` の**2行**（自分で測った）＝**`scripts/` から1行も出ない** | ✅ |
| **V52** | `CLAUDE.md` の差分なし ＋ **V47 の再実施**（U2・U3 が §8-3 と差分0） | `git diff --stat e609cd28..8c633b14 -- CLAUDE.md` は**出力なし**。V47 を**行範囲を切り出して比較**する自前の手順で再実施＝**U1 13行 / U2 9行 / U3 3行すべて差分0**、かつ **U1→U2→U3 が連続**（CLAUDE.md 67〜91行目。`u4/04-v52-v47.txt`） | ✅ |
| **V53** ⭐ | `--posting-plan` の出力が是正前後で**バイト一致** | 共有ツリーを1バイトも触らずに、**是正前後のモジュールを複製して同じ ROOT で呼び分け**て比較＝**`cmp` でバイト一致**。⭐ さらに**複製経由の出力と実物の `node scripts/gen-queue-index.mjs --posting-plan` もバイト一致**（手法の検算。213行 / 21,639バイト。`u4/05-plan-*.txt`） | ✅ |
| **V54** ⭐ | 10種目を足しても `承認_x.md` が `検証メモ_x.md` より**前**（是正前は逆） | 是正後は **1. 承認 → 2. 検証メモ**、是正前は **1. 検証メモ → 2. 承認**＝**S-1 の症状を自分で再現してから、是正後に消えることを確認**。⭐ 設計書どおりの **`KINDS` 末尾への追加**と、**先頭への追加**の**2通り**で実施（どちらも同結果。`u4/06-v54-after.txt` `06-v54-before.txt` `06-v54-tail.txt`） | ✅ |
| **V55** | ガードを壊すと **exit 1**・所定のメッセージ・**索引は書き換わらない**・`validate` も道連れ | リポジトリ外の複製で `PLAN_CLOSING_KINDS` の1語を `"承認案"` に→ **exit 1**・`PLAN_CLOSING_KINDS に KINDS に無い種類があります: 承認案`・**標準出力0バイト**・**索引の md5 不変**・`validate` も **exit 1**（`u4/09-v55.txt`）。⚠️ 実施後に**元へ戻して現物とのバイト一致を確認** | ✅ |
| **V56** ⭐ | リポジトリ外で **exit 1**・標準エラーは自前の**1行だけ**・標準出力は空 | **exit 1**・**標準出力0バイト**・**`Error:` 行0本・スタック行0本**・**自前メッセージはちょうど1行**。⚠️ ただし**標準エラーは2行**（git 自身の `fatal: not a git repository …` が1行先に出る）。是正前を同じ複製で走らせると **31行・スタック10本**（`u4/07-v56-*.txt`） | ⚠️ **下記 §3**（期待値側の不備と判断し、合否は妨げない） |
| **V57** | `npm run validate` **exit 0**・**N が是正の直前の値 ＋1** | **exit 0**。`task records in sync — 工程記録107件（完了の根拠14 / 継続16 / 取り下げの根拠1）`。⭐ **自分で数え直した**: 作業ツリーを歩いて **107件・14/16/1**（validate と独立）、`git ls-tree` で **`e609cd28`=106件 / `7cf5e1b2`=106件 / `8c633b14`=107件**＝**106 ＋1**（`u4/10-v57.txt` `11-record-count-history.txt`） | ✅ |

---

## 2. ⭐ 本単位の主眼「転記を減らしたか」の独立確認

| 見た点 | 実測 |
|---|---|
| `validate.mjs` のコメントが**列挙を持たず設計書 §12 を参照する形**か | ✅ 置き換えた**4行が設計書 §8-4-a の指定文面と完全一致**（Python の完全一致で照合。`u4/03-a-blocks.txt`）。周囲のコメントは差分に現れず**1文字も変わっていない** |
| ⚠️ **別の場所に写しが増えていないか** | ✅ **増えていない**。`scripts/` からは `選び間違` が**0件**になり、列挙は **設計書 §12 と `CLAUDE.md` U2 の2か所**（V47 で機械照合済みの組）に減った。この単位で `.claude/` `docs/`（報告以外）は**0ファイル**変更（`u4/16-m6-s1.txt` `01-scope.txt`） |
| `PLAN_KIND_ORDER` が `KINDS` から**導出**されているか | ✅ リテラルは消え、`[...PLAN_CLOSING_KINDS, ...KINDS.filter(...)]` に。⭐ **導出結果が是正前のリテラル9要素と完全一致**することを、`KINDS` を import して自分で組み立てて照合（`u4/12-regression-u1u3.txt`） |
| ⚠️ **10種目を足しても無言で並びが狂わないか** | ✅ V54 で**是正前の症状を再現 → 是正後に消滅**を確認。さらに `PLAN_CLOSING_KINDS` の綴りずれは V55 の **fail-closed** で止まる（両辺が別の出所＝空回りしない） |
| `PLAN_KIND_ORDER` を検証のために `export` していないか | ✅ `export` 行を全数確認（21本）。`PLAN_KIND_ORDER` は含まれない＝**公開面は増えていない** |

### 追加した独立検証（設計書に無いもの）

1. **是正前後の `npm run validate` 全出力の diff**＝**0行**（64の成功行が同一）。単位1〜3 の検査が1つも挙動を変えていないことの直接の証拠
2. **`--check` と再生成の冪等性**（索引を書き換えない＝単位1の回帰）
3. **導出 `PLAN_KIND_ORDER` と是正前リテラルの要素ごとの照合**（V53 のバイト一致を裏側から確かめる）
4. **V54 を `KINDS` の末尾・先頭の2通り**で実施
5. **V56 の `execFileSync` の挙動そのものを実測**（既定 stdio と `stdio:["ignore","pipe","pipe"]` の比較。§3）
6. **主要5画面の PC / 375px 実駆動**（`scripts/` の変更が画面に出ないことの確認）
7. **リポジトリ全体の写しの棚卸し**（`選び間違` / `承認そのものを書かずに終える` / `残る穴` の出現箇所。→ S-1）

⚠️ **破壊試験（V54・V55・V56）は共有ツリーを1バイトも触らずに実施**しました
（`git archive HEAD` をリポジトリ**外**へ展開した複製と、モジュール単体の複製で実施）。
終了時に **`git status --short` は空**・**`git worktree list` は本体1行のみ**・**3100番のサーバーは停止済み**。

---

## 3. ⚠️ V56 の食い違いの評価（報告 §6 の Q1）— ⭐ **「実装が足りない」ではなく「期待値が実物と違う」**

**切り分けのために実測しました**（`u4/08-q1-stdio-probe.txt`）:

| 実測 | 結果 |
|---|---|
| 実装は設計書 §8-4-c の**指定コードブロックと完全一致**しているか | ✅ **完全一致**（`u4/03-a-blocks.txt`）。自前メッセージは**1行**しか出していない |
| 2行目（`fatal: not a git repository …`）の出どころ | ⭐ **`firstAdditions()` の `execFileSync` が `stdio` を明示していないため、子プロセスの stderr が親へ素通りする**。**是正前の31行にも同じ行が含まれていた**＝**是正で増えた行ではない** |
| `stdio: ["ignore","pipe","pipe"]` を足すとどうなるか | `fatal: …` は**消え**、その文言は `e.stderr` に入る（`e.message` の1行目は `Command failed: git …` のまま）。⭐ **つまり「1行だけ」にできるのは `firstAdditions()` を変えたときだけ**——ところが **§8-4-c は「`firstAdditions()` 自体は変えない」と明示している** |

⭐ **結論**: **設計書 §9-4 の V56 の期待値「標準エラーは…1行だけ」は、同じ §8-4-c が禁じている変更をしない限り達成できません。**
＝**実装の不足ではなく、検証項目の期待値が実物と合っていない**。実装側が勝手に `firstAdditions()` を変えず、
**Q1 として設計担当に上げた判断は規約どおり**（「開発担当は仕様を決めない」）で、**差し戻す理由になりません**。

**S-2 のねらい（スタックトレースを読める形にする）は達成**しています（レビューの実測）:

| | 是正前 | 是正後 |
|---|---|---|
| stderr の行数 | **31行** | **2行** |
| スタック行（`at …`） | **10本** | **0本** |
| `Error:` で始まる行 | あり | **0本** |
| 標準出力 / exit | 0バイト / 1 | **0バイト / 1**（fail-closed のまま） |

### ⭐ Q1 への推奨（裁定は設計担当）

- ⭐ **現状のままにし、V56 の期待値のほうを直す**のが良いと考えます（実装側の見立てに同意）。
  理由は2つ: ① **`fatal: …` は「なぜ git が動かなかったか」を言う唯一の行**で、塞ぐと
  `not a git repository` と `git: command not found` が区別できなくなる（＝**復旧当日に最も知りたい情報**が消える）
  ② 消すには **§8-4-c 自身が禁じた `firstAdditions()` の変更**が要る＝**仕様の中で矛盾している**
- **直す文面の案**: 「標準エラーは**自前のメッセージが1行**・**`Error:` で始まる行とスタック行が0本**・標準出力は空（⚠️ **git 自身の `fatal:` 1行は `execFileSync` の既定で素通りする＝正常**）」
- ⭐ **どうしても1行にしたいなら**、`firstAdditions()` に `stdio: ["ignore","pipe","pipe"]` を足し、
  **自前メッセージ側で `e.stderr` の1行目を出す**（原因の情報を失わない）形が唯一の両立案です。
  ⚠️ **ただし §8-4-c の変更＝設計担当の判断が要る**ので、レビューからは推奨に留めます

---

## 4. 推奨基準の指摘（**合格を妨げないもの**）

### S-1 ⭐ 「残る穴」の説明が `.claude/agents/design.md` にも1か所あり、**片方の類型しか書いていない**

- **該当**: `.claude/agents/design.md`（承認の書き方の節・`（→ 完了）`/`（→ 継続）` の説明の直後の箇条）
- **中身**: `⚠️ **選び間違えて状態も直さないと、整合したまま緑になります**（設計上の残る穴）。`
  ＝ **「承認そのものを書かずに終える」（1.2 版で足したもう1つの類型）が入っていない**
- **経緯**: この行は**本単位ではなく `41e1207d`（単位1のユーザー承認＋ §10 の運用規約更新・設計担当のコミット）**で入りました。
  ⚠️ **本単位のスコープ外**（開発担当は `.claude/` を触れない・V51 の照合範囲も `CLAUDE.md scripts/`）なので**合否には含めません**
- **なぜ挙げるか**: ⭐ **本タスクが無くそうとしている「検査の無い側から黙って古くなる写し」と同じ型**です
  （§8-4-a が `validate.mjs` で直したのとまったく同じ構図）。⭐ ただし design.md の当該行は
  **「承認時に選び間違えるな」という手順の注意**であって**類型の網羅ではない**とも読めるので、
  **対応要否は設計担当の判断**（例: 末尾に「⭐ 類型と根拠は `CLAUDE.md` U2 が正」を1行足すだけで済みます）
- **重大度**: 軽微（現状でも運用は壊れない）

### S-2 `--posting-plan` のエラー行に「どこで git を打ったか」が出ない

- 自前メッセージはコマンド行だけを出すので、**別のディレクトリで打ってしまった場合に気づきにくい**
  （今回は git 側の `fatal:` が補ってくれている）。⭐ **Q1 を「1行だけ」に倒すなら、この不足が表面化します**
  ——その場合は `cwd` か `e.stderr` の1行目のどちらかを必ず添えること。**重大度: 軽微**

### 参考（基準に紐づかない・合否に含めない）

- ⭐ 置き換えた4行のコメントは**自分自身が古くなった経緯（2026-09-13）まで書いてある**ので、
  次に触る人が「ここに列挙を書き足す」誘惑に気づけます。⭐ **案C を選んだ判断は妥当**と考えます
  （案B の「散文どうしの機械照合」は、守る対象1文に対して検査1本が見合わない）
- 実装報告の逸脱2件（**V54 を複製上で実施** / **V47 の再実施を `diff` ではなく完全一致＋`difflib` で実施**）は、
  **どちらも手段の変更で期待値を変えていない**ことをレビューでも確認しました
  （V54 は複製でも `buildPostingPlan()` が `ROOT` を見ないので挙動が同じ・V47 は行範囲の切り出しを**別の方法**でやり直して同じ結論）。
  ⭐ とくに V54 の逸脱は、**設計書自身が警告する「`KINDS` の戻し忘れで V53 が崩れる」危険を構造的に消して**おり、**望ましい変更**です

---

## 5. 設計担当への確認事項

1. ⭐ **Q1（V56 の期待値）の裁定**——上の §3 の推奨（**実装は現状のまま・V56 の期待値を直す**）でよいか。
   ⚠️ **承認記録で「期待値を直した」ことまで書いておかないと、設計書の V56 が実物と食い違ったまま残ります**
2. **S-1（`.claude/agents/design.md` の1行）**を今回の承認コミットで直すか、別タスクにするか（**起票はレビューからは行いません**）

---

## 6. 判定の根拠にしなかったもの（明示）

- ⚠️ **実装報告の「✅」は根拠にしていません**（V50〜V57 は全部レビューが自分で実行し、件数も数え直しました）
- ⚠️ **本番（push 後）での確認はしていません**。`scripts/` は配信物ですが**画面に出るものが1つも変わらず**、
  ⭐ **公開は 2026-09-17 の大会データ週次更新のバッチに乗せる**とユーザーが決めているため、
  **push は要求しません**。**残っている確認は「公開後に `/scripts/gen-queue-index.mjs` が配信されること」だけ**で、
  これは合否に含めていません（本単位は**公開面の増減が0**なので、公開時の確認項目も増えません）

---

**次の担当: 設計担当**（ラベル相当: **設計確認待ち**）。⭐ **これが最後の単位**なので、
承認で **`（→ 完了）`** を選ぶなら、**同じコミットで起票案の1行目を `完了` にして `node scripts/gen-queue-index.mjs` を回し、`npm run validate`** まで。
