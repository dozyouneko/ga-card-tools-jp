**対象: 「索引の転記ドリフト防止」**（→ 継続） — 単位1（生成部と索引の検査）の承認。単位2・単位3 が残るのでタスクは継続。

# 承認 — 単位1「生成部と索引の検査」（2026-09-12）

⚠️ **これは issueコメントの代わり**です（GitHubアカウントが停止中で `gh issue comment` が打てないため、ファイルに残しています）。
復旧して採番されたら、本文をそのまま `gh issue comment <n> --body-file docs/design/未採番-索引の転記ドリフト防止/承認_単位1_生成部_2026-09-12.md` で投函します。

- 日付: **2026-09-12**
- ⭐ **承認したのはユーザーです**（2026-09-12・リードセッション経由。設計担当が独断で承認したのではありません）。
  ユーザーの判断は「**単位1を承認し、単位2・単位3 へ続けてよい**」。本記録はその判断を、設計担当が根拠つきで書き留めたものです
- 対象コミット: **`516f62d1`**（実装）／ **`7a4b693c`**（実装報告）／ **`bbbfd8e3`**（レビュー判定）
- 設計書（正）: [`索引の転記ドリフト防止_設計.md`](索引の転記ドリフト防止_設計.md) §9-1 の **V1〜V16**
- 実装報告: [`実装報告_単位1_生成部_2026-09-11.md`](実装報告_単位1_生成部_2026-09-11.md)
- レビュー判定: [`レビュー判定_単位1_生成部_2026-09-12.md`](レビュー判定_単位1_生成部_2026-09-12.md)（**合格**）

---

## 1. 判定 — **承認**。⚠️ ただし **`（→ 継続）`**（タスクの完了ではない）

⭐ **1行目に `（→ 完了）` を書かなかった根拠**は設計書 §4-2 の定義です:

> `完了`＝**そのタスクを完了とする記録**／`継続`＝**単位の承認**や、続きの `追加指示_` を出す承認

本タスクは §8-1 で **単位1・2・3** に分けてあり、**単位2（工程記録の突き合わせ）が本丸**（確定要件3）、
単位3（投稿計画）は復旧時の手順（§2 ③）が依存しています。⭐ **単位1だけでは要件3が1つも満たされていない**ので、
ここで `（→ 完了）` を書くのは事実に反します（同型の先例: デッキ構築の左ペイン化は単位承認10件すべて `継続` で、
完了の根拠は `実物確認結果_2026-09-10.md` の1件だけ＝設計書 §11）。

→ **起票案の状態は `実装中` のまま**（`最終確認` だけ 2026-09-12 に更新し、自由記述を現況に直しました＝§4-2）。

## 2. 承認の理由

| # | 理由 |
|---|---|
| 1 | ⭐ **正解データとの一致が独立に取れている**。V1② でレビュー担当が設計コミット `086843a9` のツリーを展開し、実装後のスクリプトを置いて `--check` が exit 0。**設計担当が仕様どおりに手で作った生成部と、実装が作る生成部がバイト一致**した＝「実装が自分の出力を自分で採点する」型ではないことが確かめられた |
| 2 | ⭐ **両辺が別の出所であることが双方向で実証されている**（レビュー M5・A1〜A4 と V3・B1〜B3・B5）。**索引だけ触っても赤／起票案だけ触っても赤**。本プロジェクトが繰り返し踏んできた「**同源で常に緑**」型（vendor除外コメントの断定）ではない |
| 3 | ⭐ **fail-closed が広い**。走査ルートの破壊（V13）・マーカーの破壊（V7〜V9）・索引の消失（V12）・CRLF（B4）・スクリプトの消失（C2）が**すべて exit 1**。レビューは**緑に倒れる経路を1つも見つけられなかった**（破壊試験22本） |
| 4 | **旧検査の性質を落としていない**（D10）。旧 `issue drafts in sync` が見ていた **A 収録漏れ**・**B 件数一致**が、新検査でも赤になることを A1／A2 で実測 |
| 5 | **スコープが設計どおり**（M7）。`516f62d1` はちょうど5ファイルで、開発担当が触った `docs/` は §8-2 が明示的に許可した2ファイルだけ。生成物（`cards/` `sets/` `data/`）は0ファイル |
| 6 | **`CLAUDE.md` の U1 が設計書 §8-3 のブロックと差分0**（機械照合）。文面の写し崩れが無い |
| 7 | **「未検証」が1件も無い**。単位1は完全にオフラインで検証でき、push を待つ項目も無い |

⚠️ **差し戻す理由は見つかりませんでした。**

## 3. 実装報告 §5 の E1〜E6 — **6件とも追認します**

| # | 内容 | 裁定 | 根拠 |
|---|---|---|---|
| **E1** | ディレクトリが読めないとき `起票案の走査に失敗: …` を足した | ⭕ **追認** | 設計書 D10「fail-closed の性質は引き継ぐ」の実装。⭐ **黙って飛ばすと起票案が静かに漏れる**＝この設計が最も嫌う型。**赤が増える方向**なので V の期待値も動かない |
| **E2** | 呼び名の重複は「書式に一致した起票案どうし」で見る | ⭕ **追認** | 状態が不明でも呼び名は取れている。**重複を見逃す側に倒していない**。V6・V11 の期待値はどちらの解釈でも同じ（実測済み） |
| **E3** | CRLF の索引はマーカー0個で exit 1 | ⭕ **追認** | B4 で**赤に倒れる**ことを実測。⭐ **「生成部の外を1バイトも変えない」（C1）との引き換え**として合理的。⚠️ **ただしエラー文は分かりにくい** → S1-2 で手当てします（§5） |
| **E4** | 生成側は「生成部の外の件数見出し」を見ない | ⭕ **追認** | 設計書 §5-1 の文言どおり（書かない条件は起票案とマーカーの問題だけ）。**門は `validate`** なので穴にならない |
| **E5** | 索引が無いとき生成側も exit 1・`索引は書き換えていません` | ⭕ **追認** | §5-1 に記述が無かった箇所の補完として自然。⭐ **設計書の書き漏れ**であり、実装の逸脱ではありません |
| **E6** | 絵文字をコードポイントのエスケープで書いた | ⭕ **追認** | §5-3「異体字セレクタを混ぜない」の実装手段として適切。V1② のバイト一致と B1（U+FE0F を足すと赤）で効いていることが確認できている |

⭐ **6件とも「赤が増える／中立」方向で、緑に倒す解釈は1つもありません。**
⚠️ **E1・E5 は設計書の書き漏れを実装が埋めたもの**なので、**設計書の落ち度**として受け止めます（§5 で 1.1 版に追記）。

## 4. レビューの「設計担当への確認事項」への回答

### 4-1. E1〜E6 の追認 → **6件とも追認**（§3）

### 4-2. 本タスクの起票案の自由記述 → ⭐ **本コミット（承認のコミット）で書き換えました**

- ⚠️ **開発担当が触らなかったのは正しい判断です。** §8-2 が「それ以外の文字は変えない」と書いている以上、
  実装者が勝手に書き換えると **V1 の正解データとの比較が濁ります**
- **時期の判断**: ⭐ **「単位2のコミット」ではなく「承認のコミット」が正しい置き場**です。
  理由は、この記述が**古くなった原因が承認そのもの**（＝単位1が入った）だからで、**事実を変えた人が同じコミットで直す**のが
  本タスクの原則（要件1・§4-1）に合います。単位2まで持ち越すと、**その間ずっと嘘が残ります**
- 書き換えた内容: 「旧語のまま置いている」→「**単位1は承認済み（2026-09-12）・残りは単位2と単位3**」。
  `最終確認` も 2026-09-12 へ。⚠️ **「未pushである」とは書きませんでした**——これは
  **設計書 §13 が問題として挙げている「現在形の状態を散文に書いた」型そのもの**なので、
  「`scripts/` と `CLAUDE.md` は配信物なので公開の可否は都度ユーザーが判断する」という**腐らない書き方**に置き換えています

### 4-3. 参考1の2件を §10 の承認時作業に含めるか → ⭐ **含めます。本コミットで両方直しました**（§6）

⚠️ **レビュー担当の指摘が正しく、設計書 §10 の表はこの2件を取りこぼしていました**
（§10 は `docs/design/README.md` の状態書式しか挙げておらず、**完了済み起票案の本文に残った旧検査の期待値**は §13「範囲外」にも入っていない）。
⭐ **「GitHub復旧後にそのまま投函すると、成立しない検証項目が issue に載る」**は実害のある指摘なので、承認時作業として拾いました。

## 5. 推奨基準の指摘（S）の採否

| # | 指摘 | 裁定 | 根拠 |
|---|---|---|---|
| **S1-1** | `package.json` に `"gen:queue-index"` を足す | ❌ **採らない**（派生issueも起こしません） | ⭐ **実測して決めました**（下記） |
| **S1-2** | CRLF のときのエラーに一言足す | ✅ **採る。ただし単位2のコミットで開発担当に入れさせる**（§7 の申し送り。設計書 1.1 版に仕様を追記済み） | E3 は「**画面上はマーカーが見えているのに0個**」＝本プロジェクトが最も嫌う**原因が分からない赤**。1行で1往復減る |
| **S3** | fail-open の指摘なし | — | ⭐ **D10 の要求（fail-closed を引き継ぐ）が満たされたということ** |

### ⚠️ ⭐ S1-1 を採らない根拠 — **実測しました**（2026-09-12・scratchpad の使い捨て package.json）

```text
npm run g --check      → argv: []          ← ⚠️ フラグが黙って落ちる
npm run g -- --check   → argv: ["--check"]
```

- ⚠️ ⭐ **`npm run gen:queue-index --check` と打つと `--check` が落ち、「確かめるつもりで索引を書き換える」**ことになります。
  ⭐ **検査のつもりの操作が、黙って生成に変わる**——**危険側へ倒れる**ので採れません
  （`npm run db:migrate:remote` を exit 1 の案内に変えた #60 と同じ判断の方向です）
- **呼び名が2つに増えます。** `CLAUDE.md`・索引 §4-1・`.claude/agents/*.md` の3箇所は `node scripts/…` で書いてあり、
  npm script を足すと**同じことを指す名前が2系統**になる＝⚠️ **本タスクが無くそうとしている「転記」を新しく作る**
- **経路にならない。** `validate.mjs` は `gen-queue-index.mjs` を **import** して使うので、npm script を挟む余地がありません
- ⭐ **一貫性の議論には根拠があります**が、`build:cards` 等は**引数を取らない**のに対し、
  こちらは `--check` / `--posting-plan` を取る点が決定的に違います

## 6. 本コミットで設計担当が直したもの（設計書 §10 の承認時作業）

⚠️ **どれも `docs/` と `.claude/`＝本番配信物ではありません**（`.claude/` はドットフォルダで Pages が配信しない）。
⚠️ ⭐ **ただし `.claude/` は `docs/` 限定のpush例外に含まれない**ので、**このコミットはpushしません**。

| ファイル | 入れたもの | 出所 |
|---|---|---|
| `.claude/agents/design.md` | 「モードBの手順」節を新設: 着手時に `設計中` → 設計完了の6手順（`設計完了_` の1行目・起票案を `実装中` → 生成 → `validate` → 1コミット）→ 承認時は **`（→ 完了）` か `（→ 継続）` を必ず選ぶ**・完了なら起票案を `完了` | §10 |
| `.claude/agents/lead.md` | 工程記録の表に `モック説明_` `再判定_` `追加指示_` `実物確認結果_` `取り下げ_` を追加。**1行目 `**対象: 「呼び名」**` と `（→ …）` の規約**を追加。**状態板の見方を §1 生成部の 🔧 実装中・📝 設計中 に**。⚠️ 手番は状態語ではない旨も | §10 |
| `.claude/agents/dev.md` / `review.md` | 各1箇条: モードBでは記録の1行目を `**対象: 「呼び名」**` にし、**書いたら `npm run validate`**（`実装報告_` `レビュー判定_` `再判定_` に `（→ …）` は付けない） | §10 |
| `docs/design/README.md` | 状態行の書式を §3-2 に更新（**旧3語 → 5語**・呼び名・生成部の説明・工程記録の宣言） | §10 ＋ **レビュー参考1-②** |
| `docs/design/待ち行列/起票案_起票案の状態行と索引の整合検査.md` | ⭐ **本文 §2〜§5 が「当時の設計」であることを3箇所に明記**（1行目・起票時の注記・§2-3・§5）。⚠️ **`issue drafts in sync` はもう存在しない**ので、**そのまま投函すると成立しない検証項目が issue に載る** | **レビュー参考1-①** |
| 本タスクの起票案 | 1行目の自由記述を現況へ（§4-2） | レビュー確認事項2 |
| 索引（`待ち行列と復旧手順.md`） | 改版履歴に第25版を追記 ＋ **生成部を作り直した**（起票案2件の `最終確認` が動いたため） | 規約 |

- ⚠️ ⭐ **完了済み起票案の本文そのもの（V1〜V7 の期待値）は書き換えていません。**
  **当時の設計を後から書き換えると、何が実装されたのかの記録が消える**ためで、
  ⭐ **「当時の設計である」という注記を足す**ほうが、投函時の事故を防ぎつつ履歴も保てます

### ⭐ レビュー参考2（索引 §4-3 の書きぶり）は **直しません**

§4-3 の「`npm run validate` が見るもの」に**工程記録と突き合わせ（単位2）が既にあるように書いてある**のは事実です。
⚠️ **しかし「まだ実装されていない」と書き足すと、単位2が入った瞬間に消さなければならない注記**＝
**本タスクが無くそうとしている「現在形の状態を散文に書く」型を、自分で1つ作る**ことになります。

- ⭐ **誤解の向きが安全側**です。読んだ人は「宣言を書かないと赤くなる」と思って**宣言を書く**（規約どおりの行動になる）。
  逆に「まだ検査は無い」と書くと**書かない人が出ます**
- ⭐ **単位2は次に着手する単位**で、間隔が短い。§7 の申し送りに「§4-3 は単位2で実態が追いつくので触らない」と明記しました

### レビュー参考3（`no line-number refs in code comments` が 46→47ファイル）

⭐ **正常**。新しいスクリプトが1本増えた分で、**走査数が動くこと自体が「範囲が生きている」信号**です（CLAUDE.md の当該節どおり）。

## 7. 派生issueの要否 → ⭐ **不要**（1件も起こしません）

| 候補 | 判断 |
|---|---|
| S1-1（npm script） | ❌ **採らないと決めた**（§5）ので、起票もしません。⭐ **「やらないと決めたこと」を起票案にすると、待ち行列に永久に居座ります** |
| S1-2（CRLF の一言） | ❌ 別タスクにしません。**単位2のスコープに入れました**（設計書 1.1 版・V17） |
| レビュー参考1の2件 | ❌ **本コミットで直したので残件なし** |
| `CLAUDE.md` の「未pushなので本番にはまだ出ていない」9箇所（設計書 §13） | ⚠️ **既にリードへ報告済みで、別タスクとして扱う**と設計書に書いてあります。**本タスクの承認では触りません**（`CLAUDE.md` は配信物なので、直すなら公開の判断とセット） |

## 8. 単位2・単位3 の進め方

⭐ **設計書は 1.1 版に更新しました**（改版履歴を参照）。**単位2の仕様（§4・§6）は 1.0 版から変えていません**——
変えたのは **①S1-2 の追加（§5-4・V17）** と **②E1・E5 を仕様として明文化したこと**の2点だけです。

**次の担当: 開発担当（単位2）**（ラベル相当: **実装待ち**）。申し送りは §9。

## 9. ⚠️ 単位2の開発担当への申し送り

1. ⭐ **設計書は 1.1 版を読むこと**（1.0 版との差は §5-4 の CRLF の一言・V17・E1/E5 の明文化だけ）
2. ⭐ **S1-2 を単位2のコミットに含める**（`scripts/gen-queue-index.mjs`）。⚠️ **CR を含むときだけ**注記を足すこと——
   **無条件に足すと V7・V8・V9 の期待値が変わります**（今の索引に CR は0個なので、正しく実装すれば V7〜V9 は 1.0 版のまま通る）
3. ⚠️ **索引 §4-3 の「`npm run validate` が見るもの」は、単位2が入って初めて実態と一致します。**
   ⭐ **触らないでください**（既に単位2後の姿で書いてあります）
4. ⚠️ **`docs/` を触ってよいのは、単位1と同じく「本タスクの起票案の1行目」と「索引の生成部」だけ**です。
   ⭐ ただし**単位2では起票案の状態を変える必要がありません**（`実装中` のまま）。
   **起票案が増減しない限り生成部も変わらない**ので、**`docs/` を1バイトも変えないのが正常**です
5. ⚠️ **V20 の成功行の数は、設計書が絶対数を書いていません。** §9 冒頭の `find … | grep -cE` を打って数えること。
   ⭐ **本承認のコミットで工程記録が1件（本ファイル）増えています**
6. ⚠️ **V33 は「exit 0 が正しい」検証項目**です（設計どおりの穴・§12）。**赤くならないことを確認する**もので、失敗ではありません
7. ⚠️ **V21 は本タスクの山**（2026-09-11 に実在したずれの再現）。**T7 が併発しないこと**まで見ること
8. ⭐ **破壊試験は共有の作業ツリーで行わないこと**（単位1の開発担当・レビュー担当がとった
   `git archive HEAD` → scratchpad の複製という手順が実績あり）
