**対象: 「索引の転記ドリフト防止」** — 単位4「承認前の是正」の追加指示（`validate.mjs` のコメント ＋ レビュー S-1・S-2）。

> ⚠️ **これはissueコメントの代替**（GitHubアカウント停止中で `gh` が403）。GitHub復旧後に
> `gh issue comment <n> --body-file docs/design/未採番-索引の転記ドリフト防止/追加指示_承認前の是正_2026-09-13.md` で投函する。

# 追加指示 — 単位4「承認前の是正」（2026-09-13）

⭐ **単位1〜3はユーザー承認済み**（[`承認_単位3_投稿計画_2026-09-13.md`](承認_単位3_投稿計画_2026-09-13.md)・`（→ 継続）`）。
⚠️ ⭐ **タスクを完了にする前に、この単位4を済ませる**というユーザー判断（2026-09-13）による追加指示。

---

## 1. 何をするか（3件）

⭐ **仕様・文面・検証項目はすべて設計書 1.3 版にある。ここには書き写さない**（＝本タスクの趣旨）:

| # | 中身 | 正 |
|---|---|---|
| **a** | ⭐ `scripts/validate.mjs` の「残る穴」コメントから**類型の列挙を落とし、設計書 §12 を参照させる**（置き換える4行はそのまま使う） | 設計書 **§8-4-a** |
| **b** | レビュー **S-1** を採用: `PLAN_KIND_ORDER` を `KINDS` から導く ＋ `PLAN_CLOSING_KINDS` のガード | 設計書 **§8-4-b** |
| **c** | レビュー **S-2** を採用: git の無い環境での `--posting-plan` を**読める1行**にする | 設計書 **§8-4-c** |

**検証項目**: 設計書 **§9-4 の V50〜V57**（**全8項目**）。

- **V50**（`validate.mjs` の3本の grep が **0 / 1 / 1**）・**V51**（`git grep -c "選び間違" -- CLAUDE.md scripts/` が **`CLAUDE.md:1` の1行だけ**）
  … ⭐ **a が「広げた」ではなく「減らした」ことの機械照合**
- **V52**（`CLAUDE.md` の差分が**0**＋ V47 の再実施）… ⚠️ **`CLAUDE.md` を1文字も変えない**ことの確認
- **V53**（`--posting-plan` の出力が是正前後で**バイト一致**）・**V54**（`buildPostingPlan()` を直接呼ぶ破壊試験）・**V55**（ガードの破壊試験）… b
- **V56**（リポジトリ**外**の複製で1行エラー・exit 1・標準出力は空）… c
- **V57**（`npm run validate` exit 0・工程記録 N が **是正の直前に自分で測った値 ＋1**）

---

## 2. ⚠️ 守ること

- ⚠️ ⭐ **`CLAUDE.md` は1文字も変えない。** U2 は 1.2 版が最終形で、**V47 で §8-3 と機械照合されている**
  （変えると V47 と V52 の両方が落ちる）。⭐ **「③にも同じ文面を書き写して広げる」案は不採用**——理由は承認記録 §4
- ⚠️ ⭐ **`docs/` は触らない。** 本単位に要る `docs/` の変更（設計書 §8-4・§9-4・派生起票案・索引 §2-③）は
  **設計担当が本コミットで済ませた**。⭐ **例外は無い**（単位1の §8-2 のような起票案の書き換えは本単位には無い＝状態は `実装中` のまま）
- ⚠️ **`PLAN_KIND_ORDER` を検証のために `export` しない**（V53・V54 は出力側で確かめる）
- ⚠️ **破壊試験（V54・V55・V56）は1つずつ行い、次へ進む前に必ず戻す**。⚠️ **V54 で `KINDS` を戻し忘れると V53 の一致が崩れる**
- ⚠️ **V56 の複製はリポジトリの外へ展開する**——`tmp/` 配下だと親をたどって `.git` を見つけてしまい、**再現しない**
- ⚠️ **生成物（`cards/` `sets/` `data/` 等）は0ファイル。** 出たら本単位の成果ではない
- ⚠️ ⭐ **`scripts/` は配信物**＝**コミットまでで止める**。⭐ **公開は 2026-09-17 の大会データ週次更新と一緒に出す**とユーザーが決めている（単位1〜4の差分はそのバッチに乗る）

---

## 3. 報告に必ず書くこと

- **V50〜V57 の番号に対応づけた表**（⚠️ **V57 の N は、是正の直前に自分で測った値も併記する**）
- ⚠️ **V53 で差分が1行でも出たら、そこで止めて報告する**（`PLAN_CLOSING_KINDS` の3語か `KINDS` の並びを疑う）
- コミットハッシュ・設計書からの逸脱と根拠・設計担当への確認事項

---

**次の担当: 開発担当**（ラベル相当: **実装待ち**）。設計書は [`索引の転記ドリフト防止_設計.md`](索引の転記ドリフト防止_設計.md) の **1.3 版**。
