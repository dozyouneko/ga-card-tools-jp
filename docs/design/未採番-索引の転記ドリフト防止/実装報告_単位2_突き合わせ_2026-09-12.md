**対象: 「索引の転記ドリフト防止」** — 単位2（工程記録の突き合わせ）の実装報告。

# 実装報告 — 単位2「工程記録の突き合わせ」（2026-09-12）

⚠️ **これは issueコメントの代わり**です（GitHubアカウントが停止中で `gh issue comment` が打てないため、ファイルに残しています）。
復旧して採番されたら、本文をそのまま `gh issue comment <n> --body-file docs/design/未採番-索引の転記ドリフト防止/実装報告_単位2_突き合わせ_2026-09-12.md` で投函します。

- 日付: **2026-09-12** ／ 担当: **開発（実装）担当**
- 設計書（正）: [`索引の転記ドリフト防止_設計.md`](索引の転記ドリフト防止_設計.md) **1.1 版** の §4・§6（＋ §5-4 の S1-2）
- 申し送り: [`承認_単位1_生成部_2026-09-12.md`](承認_単位1_生成部_2026-09-12.md) §9（7点）— **7点とも反映しました**（§6）
- 着手前の HEAD: **`7a4b693c`**（作業ツリーはクリーン）

---

## 1. 実施内容

| # | ステップ | 結果 |
|---|---|---|
| 1 | 設計書 1.1 版 §4・§6・§5-4・§9-2 と、承認記録 §9 の申し送り7点を読む | — |
| 2 | `scripts/gen-queue-index.mjs` に工程記録の読み取り（§4）と突き合わせ（§6-2）を追加 | `readRecords` / `matchRecords` / `countDecls` / `kindOf` / `SKIP_BY_DRAFTS` / `skipByRecords` を export |
| 3 | 同ファイルの `inspectIndex()` に **S1-2**（CRLF の注記）を追加 | **CR が1個以上のときだけ**注記を足す |
| 4 | `scripts/validate.mjs` に `TASK RECORD DRIFT` ブロックを**単位1のブロックの直後**に追加 | 成功行 `task records in sync — …` |
| 5 | `CLAUDE.md` に **U2**（設計書 §8-3 の文面のまま）を U1 の箇条の直後に追加 | 機械照合で**差分0**（§5） |
| 6 | コミット | ⭐ **`d1a255a6`**（`CLAUDE.md` / `scripts/gen-queue-index.mjs` / `scripts/validate.mjs` の**3ファイルのみ**） |
| 7 | 破壊試験 30 本（V3〜V13・V17・V20〜V34 ＋ 自前の D1〜D8） | §2・§3 |

- **push: していません**（`scripts/` と `CLAUDE.md` は本番配信物。公開の可否はユーザーの判断）
- ⭐ **`docs/` は1バイトも変えていません**（申し送り #4 の「起票案の状態は `実装中` のままなので、
  `docs/` を変えないのが正常」のとおり。本報告ファイルだけは別コミットで足します）
- 破壊試験は **`git archive HEAD` → scratchpad の複製**の中だけで行いました（申し送り #8）。
  共有の作業ツリーは検証中も最後もクリーンです（§3 V34）
- 再現用のスクリプトを `tmp/dev/索引の転記ドリフト防止-単位2/`（gitignore）に置きました。
  `tree/` を作って `bash t_v20_v21.sh` 等で同じ出力が得られます

## 2. 検証結果（設計書 §9-2 の V20〜V34 ＋ §9-1 の V17）

⭐ **期待値の件数は設計書 §9 冒頭のコマンドで数えました**（絶対数を書かない規約）:

```text
起票案      31 件
工程記録    99 件   ← ⚠️ 本報告ファイルを足すと 100 件になります（§4）
```

⭐ **「エラー行」＝ `  - ` で始まる行**（見出し行と `→` の行は数えていません）。

| # | 手順 | 期待 | 実測 | 判定 |
|---|---|---|---|---|
| **V17** | 索引を CRLF に変換して `npm run validate` | exit 1・1行・`…（START 0個 / END 0個。⚠️ 索引に CR が <wc -l>個あります——改行コードが CRLF になっていませんか）` | exit 1・**1行**・`索引の生成部のマーカーが1組ではありません（START 0個 / END 0個。⚠️ 索引に CR が 521個あります——改行コードが CRLF になっていませんか）`。`wc -l` も **521** | ⭕ |
| **V17（戻し）** | 戻して再実行。CR が0個なら注記が付かない | V20 と同じ出力／V7〜V9 は 1.0 版の文面 | exit 0・V20 と一致。**V7 `（START 1個 / END 0個）`・V8 `（START 2個 / END 1個）`・V9 `（START 0個 / END 1個）`** ＝いずれも注記なし | ⭕ |
| **V20** | `npm run validate` | exit 0・V2 の行の直後に `task records in sync — 工程記録99件（完了の根拠14 / 継続14 / 取り下げの根拠1）` | exit 0。`queue index up to date — 起票案31件（実装中1 / 設計中0 / 生きている15 / 完了14 / 取り下げ1）` の**直後の行**に上記が出た | ⭕ |
| **V21** ⭐⭐ | 「デッキ構築の左ペイン化」の起票案を `完了`→`生きている` にし、**生成し直す** | queue index は緑（生きている16 / 完了13）なのに exit 1・**ちょうど1行**の T4。⚠️ T7 が併発しない | `queue index up to date — 起票案31件（実装中1 / 設計中0 / **生きている16 / 完了13** / 取り下げ1）` ＝**緑**。exit 1・エラー行**ちょうど1行**: `状態行が工程記録と食い違っています: 「デッキ構築の左ペイン化」は docs/design/未採番-デッキ構築ツールUI刷新/実物確認結果_2026-09-10.md で完了したのに、起票案の状態が「生きている」です（docs/design/未採番-デッキ構築ツールUI刷新/起票案_2026-09-04_デッキ構築ツールの左ペイン化と検索条件の統一.md）`。**T7 は出ていない** | ⭕ |
| **V21b** | V21 を**生成し直さずに** | exit 1・2行（QUEUE の `最初の差: 生成部の17行目` ＋ V21 と同じ1行） | exit 1・**2行**・`索引の生成部が起票案と一致しません（最初の差: 生成部の17行目）` ＋ V21 と同一の T4 | ⭕ |
| **V22** | `実装報告_A_トークン_2026-09-06.md` の1行目を消す | exit 1・2行 | exit 1・**2行**: `工程記録の1行目に対象の宣言がありません: …/実装報告_A_トークン_2026-09-06.md` ／ `起票案との照合を省略しました（工程記録の問題が 1件 あるため）` | ⭕ |
| **V23** | 同ファイルの対象を `「デッキ構築の左ペイン」`（1字欠け）に | exit 1・2行 | exit 1・**2行**: `工程記録の対象に一致する起票案がありません: …（「デッキ構築の左ペイン」）` ／ 省略の1行 | ⭕ |
| **V24** | `承認_A_トークン_2026-09-06.md` の `（→ 継続）` を `（→ 完了）` に | exit 1・1行・記録は `実物確認結果_2026-09-10.md / 承認_A_トークン_2026-09-06.md` の順 | exit 1・**1行**・`完了の宣言が複数あります: 「デッキ構築の左ペイン化」（docs/design/未採番-デッキ構築ツールUI刷新/実物確認結果_2026-09-10.md / docs/design/未採番-デッキ構築ツールUI刷新/承認_A_トークン_2026-09-06.md）` | ⭕ |
| **V25** | `実装報告_A_トークン_2026-09-06.md` に `（→ 完了）` を足す | exit 1・2行 | exit 1・**2行**: `（→ 完了）を宣言できない種類の記録です: …/実装報告_A_トークン_2026-09-06.md（実装報告）` ／ 省略の1行 | ⭕ |
| **V26** | `承認_レアリティ_2026-09-04.md` の `（→ 完了）` を `（→ 継続）` に | exit 1・1行 | exit 1・**1行**: `完了の根拠がありません: 「レアリティ絞り込み」の状態は完了ですが、（→ 完了）を宣言した記録がありません（docs/design/未採番-UI刷新の検討/起票案_2026-09-03_レアリティ絞り込み.md）` | ⭕ |
| **V27** ⭐ | 同ファイルの `（→ 完了）` を**消す**（付け忘れ） | exit 1・2行 | exit 1・**2行**: `承認_ と 実物確認結果_ の記録は（→ 完了）か（→ 継続）を宣言してください: …/承認_レアリティ_2026-09-04.md` ／ 省略の1行 | ⭕ |
| **V28** | `実装報告_dummy_2026-09-11.md`（対象「検索結果のタブ化」）を作る | exit 1・1行（実装中 以上に） | exit 1・**1行**: `状態が工程記録より遅れています: 「検索結果のタブ化」の状態は「生きている」ですが、docs/design/未採番-デッキ構築ツールUI刷新/実装報告_dummy_2026-09-11.md（実装報告）があります（実装中 以上にしてください）` | ⭕ |
| **V29** | V28 を戻して `モック説明_dummy_2026-09-11.md` を作る | exit 1・1行（設計中 以上に） | exit 1・**1行**: `…/モック説明_dummy_2026-09-11.md（モック説明）があります（設計中 以上にしてください）` | ⭕ |
| **V30** | `起票案_2026-09-04_検索結果のタブ化.md` を `実装中` にし、生成し直す | exit 1・1行 | queue index は緑（実装中2 / 生きている14）。exit 1・**1行**: `実装中の根拠がありません: 「検索結果のタブ化」は実装中ですが、設計完了_ か 追加指示_ の記録がありません（docs/design/未採番-デッキ構築ツールUI刷新/起票案_2026-09-04_検索結果のタブ化.md）` | ⭕ |
| **V31** | `取り下げ_2026-08-30.md` の `（→ 取り下げ）` を消す | exit 1・2行 | exit 1・**2行**: `取り下げ_ の記録は（→ 取り下げ）を宣言してください: docs/design/未採番-スターコーリングのハイライト欠落/取り下げ_2026-08-30.md` ／ 省略の1行 | ⭕ |
| **V32** | `承認_2026-08-28.md` の `（→ 完了）` を `（→ 取り下げ）` に | exit 1・2行。⚠️ **1記録から2行出ない** | exit 1・**2行**: `承認_ と 実物確認結果_ の記録は（→ 完了）か（→ 継続）を宣言してください: docs/design/未採番-UI刷新の検討/承認_2026-08-28.md` ／ 省略の1行。⭐ **この記録から出た行は1本だけ** | ⭕ |
| **V33** ⭐ | 残る穴（§12）。`承認_2026-09-04.md`(0件表示)を `（→ 継続）` に、対応する派生起票案を `実装中` にして生成し直す | ⚠️ **exit 0**・`完了の根拠13 / 継続15` | **exit 0**・`task records in sync — 工程記録99件（完了の根拠13 / 継続15 / 取り下げの根拠1）`。⭐ **§12 の記述（選び間違えは防げない）と一致** | ⭕ |
| **V34** | すべて戻して `npm run validate`・`git status --short`・`git show --stat` | V20 と同じ出力。コミットは3ファイルだけ | 複製・共有ツリーとも **V20 と同一出力・exit 0**。`git -c core.quotepath=false status --short` は**空**。`git show --stat --format= d1a255a6` = `CLAUDE.md` / `scripts/gen-queue-index.mjs` / `scripts/validate.mjs` の**3ファイル**（生成物 `cards/` `sets/` `data/` は0ファイル） | ⭕ |

### 2-1. 単位1の検証項目の再実行（回帰・§9-2 末尾の注記を含む）

⚠️ **単位2で `validate.mjs` の起票案の読み取りを1回に集約した**（§5 の E-2）ため、単位1の V を全部やり直しました。

| # | 実測 | 判定 |
|---|---|---|
| V3 | exit 1・1行・`…（最初の差: 生成部の17行目）` | ⭕ |
| V4 / V4b | exit 1・1行・`…13行目` ／ 生成後 exit 0・`実装中1 / 設計中1 / 生きている14 / 完了14 / 取り下げ1` | ⭕ |
| **V5** | exit 1・**3行**（`起票案の1行目の書式が違います` ／ `生成部の比較を省略しました（起票案の問題が 1件 あるため）` ／ ⭐ **`起票案との照合を省略しました（起票案を読めないため。先に QUEUE INDEX DRIFT を直してください）`**）＝設計書 §9-2 末尾の「1行増える」と一致 | ⭕ |
| **V6** | exit 1・**3行**（重複 ／ 比較省略 ／ 照合省略）。呼び名の重複行は V6 の期待文面と一致 | ⭕ |
| V7 / V8 / V9 | exit 1・各1行・`（START 1個 / END 0個）`／`（START 2個 / END 1個）`／`（START 0個 / END 1個）`。⭐ **CRLF の注記は付かない** | ⭕ |
| V10 | exit 1・1行・`件数を書いた見出しが生成部の外にあります: 522行目「### 🟢 生きている（**15件**）の控え」` | ⭕ |
| **V11** | exit 1・**3行**（`起票案の状態が不明です: …（「実装待ち」）` ／ 比較省略 ／ 照合省略） | ⭕ |
| V12 | exit 1・1行・`索引が見つかりません: docs/design/待ち行列/待ち行列と復旧手順.md` | ⭕ |
| V13 | 起票案の走査ルートを `docs/design/リリース確認` に変える → exit 1・3行（`起票案が1件も見つかりません: docs/design/リリース確認` を含む） | ⭕ |
| V14 | V5 の状態で `node scripts/gen-queue-index.mjs` → **exit 1**・最後の行が `索引は書き換えていません`・索引は未変更 | ⭕ |
| V15 | 2回続けて打つと2回目は `索引の生成部は最新です（起票案31件）`。`--bogus` は **exit 2** | ⭕ |

## 3. 自前の破壊試験（設計書に無いが、緑に倒れる経路を探した）

⭐ **単位1のレビューが「両辺が別の出所であること」を双方向で確かめた形**にならって、
**記録側だけを触る／状態側だけを触る／範囲そのものを壊す**の3方向で試しました。

| # | 壊し方 | 結果 | 何が言えるか |
|---|---|---|---|
| **D1** | 完了の根拠 `実物確認結果_2026-09-10.md` を**ファイルごと消す** | exit 1・1行 `完了の根拠がありません: 「デッキ構築の左ペイン化」…` | ⭐ **記録の消失で緑にならない**（fail-closed） |
| **D2** | `未採番-デッキ構築ツールUI刷新/` を `99-デッキ構築ツールUI刷新/` に**リネーム**（GitHub復旧後の想定） | exit 1・2行（生成部の差 ＋ `完了の根拠がありません`） | ⭐ **設計書 §12 の「設計どおりの赤」を実地で確認**（派生起票案「復旧後の起票案と検査の扱い」で決める対象） |
| **D3** | 工程記録の走査範囲の判定（`isRecordDir`）を壊して**0件**にする | exit 1・**16行**（完了14・取り下げ1・実装中1 の根拠が全部消える） | ⭐ **範囲が壊れたら黙って緑にならない**。⚠️ 単位1の起票案側（V13）と違い「0件」という専用のエラーは出ないが、**根拠が全滅するので必ず赤になる** |
| **D4** | 記録のフォルダを `chmod 000` | exit 1・3行（**`起票案の走査に失敗: …（EACCES…）`** ／ 比較省略 ／ 照合省略） | ⚠️ **記録側の「走査に失敗」は発火しない**——`readDrafts` が `docs/design` 全体を先に歩くので、そちらが先に赤くする（§5 の E-1） |
| **D5** | 本タスクの `承認_単位1_生成部_2026-09-12.md` を誤って `（→ 完了）` にする | exit 1・1行 `状態行が工程記録と食い違っています: 「索引の転記ドリフト防止」は …承認_単位1_生成部_2026-09-12.md で完了したのに、起票案の状態が「実装中」です` | ⭐ **単位承認を「完了」と書き間違える**型（本タスクで実際に起こりうる）が止まる |
| **D6** | 工程記録を **BOM + CRLF** で保存する | **exit 0**（1行目は正しく読めている） | 記録側は BOM/CRLF に強い（⚠️ **索引だけは CRLF で赤くなる**＝V17。生成部の外を1バイトも変えない設計との引き換え・E3） |
| **D7** | `承認メモ.md`（本文は普通のメモ）を `未採番-*/` に置く | exit 1・2行 `工程記録の1行目に対象の宣言がありません: …/承認メモ.md` | ⚠️ ⭐ **9つの接頭辞で始まる `.md` は、記録以外の目的で置けなくなった**（起票案の前方一致規約と同型）。**黙って通ることはない** → §7 で確認します |
| **D8** | `メモ_2026-09-12.md`（接頭辞に当たらない名前） | **exit 0**（拾わない） | 前方一致の範囲が広がりすぎていないこと |
| **D9** | 番号付きフォルダ（`73-…` 等）の記録 **5件** | 走査対象外（`find` の全数 104 − 範囲内 99 ＝ 5） | ⭐ **§4-1 の範囲制限が実際に効いている**（＝空回りしていない） |

## 4. 実行できなかった検証・残っている確認

| # | 項目 | 代替・扱い |
|---|---|---|
| 1 | **本番での確認** | ⚠️ **push していない**ので本番には出ていません。`scripts/` と `CLAUDE.md` は配信物なので、公開はユーザーの判断待ちです。⭐ **本検査は完全にオフラインで動く**ので、push を待つ検証項目はありません |
| 2 | **工程記録の件数** | ⚠️ V20 の実測 **99件**は「本報告ファイルを足す前」の値です。**本報告を足すと 100件**になり、成功行は `task records in sync — 工程記録100件（完了の根拠14 / 継続14 / 取り下げの根拠1）` になります（本報告は `実装報告_` なので `（→ …）` を持たず、内訳は変わりません）。**レビュー時はこの100件が正** |
| 3 | **`工程記録の走査に失敗:` の発火** | ⚠️ **実測で発火させられませんでした**（D4）。後詰めとして残した理由はコード内のコメントに書いています → §7 で確認します |
| 4 | Playwright での実駆動 | ⚠️ **不要**（本タスクはブラウザに一切出ない。UI・生成物・本番配信の挙動は変えていません） |

## 5. 設計書の想定との差分

⭐ **生成物（`cards/` `sets/` `data/` 等）は0ファイル**。`docs/` も0バイト（§1）。
コミットは設計書 §8-1 の表どおり **`scripts/gen-queue-index.mjs` ・ `scripts/validate.mjs` ・ `CLAUDE.md`** の3つだけです。

`CLAUDE.md` の **U2** は設計書 §8-3 のコードブロックを機械照合しました（`spec in CLAUDE.md` → **True**）＝**写し崩れ0**。

## 6. 設計書からの逸脱と根拠

⚠️ **仕様そのものの逸脱はありません。** 設計書が定めていない箇所を実装で決めた点が **E-1〜E-5** です。
⭐ **5件とも「赤が増える／中立」方向で、緑に倒す解釈は1つもありません**（単位1の E1〜E6 と同じ方針）。

| # | 内容 | 根拠 |
|---|---|---|
| **E-1** | 工程記録の走査でディレクトリが読めないとき、`工程記録の走査に失敗: <dir>（<message>）` を1行足した（§4-3 の表に無い） | 単位1の **E1（承認で追認済み）と同型**の fail-closed。⚠️ **ただし実測では発火しませんでした**（D4。起票案の走査が先に赤くする）。**消すと記録だけが黙って0件になる経路ができる**ので後詰めとして残し、その旨をコードのコメントに明記しました → §7-1 |
| **E-2** | `validate.mjs` で `readDrafts(root)` の呼び出しを**モジュール先頭に1回だけ**にし、単位1のブロックと単位2のブロックで**同じ結果を使い回す**ようにした | ⚠️ 単位2のブロックは「起票案の呼び名の集合」が要るので、素直に書くと **31ファイルを2回読む**ことになります。⭐ **2回読むと、2つのブロックが違う結果を見る可能性**（同一実行中のファイル変更）も生まれます。⭐ **単位1の V3〜V15 を全部やり直して出力が同一**であることを確認しました（§2-1） |
| **E-3** | 工程記録の1行目も **BOM を除去してから**読む（§4-2 に記述なし） | 起票案（§3-2）と同じ扱い。**BOM 付きだと「1行目に宣言がありません」に見えて原因が分からない**。D6 で実測 |
| **E-4** | T1（完了と取り下げの両方）の括弧内は、**その2種類の宣言を持つ記録を全部・パス昇順**で並べた（設計書は `（<記録> / …）` と省略） | §6-2 の「記録の列挙（T1〜T3 の括弧内）は**パスの昇順**」に従った。⚠️ **V に該当項目が無く、期待値との突き合わせができていません** → §7-2 |
| **E-5** | 種類 `取り下げ_` には**最低段階を要求しない**（§6-2 の最低段階の列挙に `取り下げ` が無い） | 列挙に無い＝要求しない、と読んだ。`取り下げ` は段階の尺度（生きている<設計中<実装中<完了）に乗らない語なので、要求しようがない。⭐ **取り下げ済みタスクは T4 で先に決着する**ので、実際にこの分岐に入る経路もありません |

### 6-1. 申し送り7点の反映

| # | 申し送り | 反映 |
|---|---|---|
| 1 | 設計書は 1.1 版を読む | ⭕ 1.1 版で実装（§5-4・V17・E1/E5 の明文化を確認） |
| 2 | **S1-2 を単位2のコミットに含める。CR を含むときだけ**注記を足す | ⭕ 実装。**V17 で注記あり・V7〜V9 で注記なし**を両方実測（§2） |
| 3 | 索引 §4-3 は触らない | ⭕ **`docs/` を1バイトも変えていません** |
| 4 | `docs/` は起票案の1行目と索引の生成部だけ。単位2では変える必要が無い | ⭕ そのとおりでした（起票案は `実装中` のまま・生成部も不変） |
| 5 | V20 の件数は `find … \| grep -cE` で数える | ⭕ 起票案31件・工程記録99件（§2 冒頭。⚠️ 本報告で100件になる＝§4-2） |
| 6 | V33 は exit 0 が正 | ⭕ exit 0 を確認し、§12 の記述と一致することも確かめた |
| 7 | V21 は T7 が併発しないことまで見る | ⭕ **エラー行ちょうど1行**（T4 のみ）を確認 |
| 8 | 破壊試験は共有ツリーで行わない | ⭕ `git archive HEAD` → scratchpad の複製。共有ツリーは最後まで**クリーン** |

## 7. 設計担当への確認事項

### 7-1. E-1（`工程記録の走査に失敗`）を残してよいか

⚠️ **実測で発火させられませんでした**（D4）。`readDrafts` が `docs/design` 全体を再帰で歩くので、
読めないフォルダがあれば**必ず起票案側が先に赤くなり**、`validate` は突き合わせを飛ばします（§6-1）。

- ⭐ **残した理由**: 消すと「記録だけが黙って0件になる」経路（＝この設計が最も嫌う型）が生まれます。
  ただし **⚠️ 動くことを一度も確認できていないコード**でもあるので、**「fail-closed のために残す」か
  「到達しないなら消す」か**の判断をお願いします（私の判断では決めません）
- 単位3（`--posting-plan`）は §7-2 で「起票案・記録の問題があれば標準エラーに出して exit 1」と書かれており、
  そちらでは**記録側だけを見る経路**があり得ます。**単位3の実装前に決まっているほうが安全**です

### 7-2. E-4（T1 の記録の並べ方）の期待値

`完了と取り下げの両方が宣言されています` の括弧内を、**完了・取り下げ両方の記録をまとめてパス昇順**にしました。
⚠️ **V に該当項目が無い**ので、期待値の追認をお願いします（「完了を先に、次に取り下げ」も読めます）。

### 7-3. ⭐ D7 の副作用を `CLAUDE.md` に足すべきか

**9つの接頭辞（`モック説明` `設計完了` `追加指示` `実装報告` `レビュー判定` `再判定` `承認` `実物確認結果` `取り下げ`）で
始まる名前の `.md` は、`docs/design/未採番-*/` と `待ち行列/` に工程記録以外の目的で置けなくなりました**
（`承認メモ.md` を置くと exit 1 ＝ D7）。⭐ **起票案の前方一致規約とまったく同型**です。

- ⚠️ U2 の文面には**「対象は…で始まる `.md`（前方一致）」とは書いてあるが、「別の目的で置けない」とは書いていません**
  （起票案のほうには `⚠️ **`docs/design/**` に `起票案` で始まる名前の `.md` を、起票案以外の目的で置けない**` があります）
- ⭐ **U2 は設計書 §8-3 の文面のまま入れる**指示だったので、**私の判断で足していません**。
  足すなら文面をください（`CLAUDE.md` は配信物なので、公開の判断ともセットになります）

### 7-4. （参考・判断不要）工程記録の件数は報告を書くたびに増える

V20 の成功行の件数は**この報告ファイル自身を含む**ため、レビュー担当が見る値は **100件** です（§4-2）。
⚠️ 設計書が絶対数を書いていないのは正しい判断で、**このままでよい**と思います。

---

**次の担当: レビュー担当**（ラベル相当: **レビュー待ち**）

- 対象コミット: ⭐ **`d1a255a6`**（実装・3ファイル）／ 本報告のコミット
- 検証項目: 設計書 §9-2 の **V20〜V34** ＋ §9-1 の **V17**（＋ 回帰として V3〜V15）
- 再現用スクリプト: `tmp/dev/索引の転記ドリフト防止-単位2/`（gitignore・`tree/` を `git archive` で作ってから実行）
