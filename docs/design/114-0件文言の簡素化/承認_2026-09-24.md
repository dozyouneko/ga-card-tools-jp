**対象: 「0件文言の簡素化」**（→ 完了）

# 承認 — #114 検索結果が0件のときの文言を簡素化する（2026-09-24）

- 設計書: `docs/design/114-0件文言の簡素化/0件文言の簡素化_設計.md`（→ **1.1** に改版）
- 実装: **`b537967f`**（2ファイル・生成物の差分0）
- レビュー: **合格**（2026-09-24・M1〜M7 すべて ✅・破壊試験5ケースをレビュー担当が測り直し）
- ⭐ **実物確認（設計書 §9 の U1〜U6）: 実施済み**（ユーザー・2026-09-24。→ §2）
- ⚠️ ⭐ **クローズはしない。** ラベルを **`設計確認待ち` → `push待ち`** に付け替える
  （**本番公開の可否はユーザーの判断**。push が済んだらラベルを外してクローズする）
- ⚠️ ⭐ **1行目は `（→ 完了）`**（＝**このタスクはこの承認で終わり**）。⚠️ **issue を開いたままにするのは
  `push待ち` の運用のため**で、**残っているのはユーザーの push 判断だけ**（#101 と同じ形）。
  ⚠️ ⭐ **U3・U4 は「#114 の続き」ではなく別タスクへ出した**ので `（→ 継続）` にはしない（→ §4・§5）

---

## 1. ⭐ 承認したのはユーザー（設計担当は記録係）

⭐ **2026-09-24、ユーザーが実物を操作したうえで承認した**（リードが直接聞き取り）。
本書はその**記録**であって、⚠️ **設計担当が可否を判断したものではない。**

⭐ **#101 の承認とはここが違う**——あちらは U1〜U6 を**省いて**承認したが、
**本件は U1〜U6 をすべて実施したうえでの承認**である。

## 2. ⭐ 実物確認（U1〜U6）の結果

| # | 見たところ | 結果 | ⭐ 処理 |
|---|---|---|---|
| **U1** | トップで「アリス」＋`PRD`（0件） | ✅ **OK** | — |
| **U2** | デッキ構築で同じ条件 | ✅ **OK** | — |
| ⚠️ **U3** | `/?q=の&rarity=8` で「もっと見る」を2回 | ⚠️ ⭐ **「3枚は実際にヒットするはずなのに1画面目に出ないのはおかしいです」** | ⭐ **別issueで扱う**（ユーザー判断）→ **#102 に症状②として取り込んだ**（§5） |
| ⚠️ **U4** | 「アリス」＋`PRD`＋並び替え「レベル」 | ⚠️ ⭐ **「分岐の評価順の問題は別issueに起票」** | ⭐ **別issueで扱う**（ユーザー判断）→ **#115 を起票**（§4） |
| **U5** | エレメント `FIRE`＋`WATER` の AND | ✅ **OK**（`element-and` を巻き込まない判断は受け入れられた） | — |
| **U6** | 375px で U1・U3・U5 | ✅ **OK** | — |

⭐ **U3・U4 とも「#114 を差し戻す」ではなく「別issueで扱う」がユーザーの判断**。
⚠️ **したがって `（→ 継続）` で設計に戻すのではなく、`push待ち` へ進める。**

## 3. ⚠️ ⭐ 承認の中身 — **設計書の前提が1つ誤っていた**

⭐ **U4 について、設計書 1.0 の前提が誤りだったことが実測で判明した。**
⚠️ ⭐ **これは #114 の実装の不備ではない**ので差し戻さないが、**記録に残さないと「#114 で有用な案内を失った」
という誤った理解が引き継がれる**ので、**設計書を 1.1 に改版して §3-1-1 に訂正を書いた**（同じコミット）。

### 3-1. 何が誤っていたか

設計書 1.0 の §3-1 と §9 は、**U4（`アリス` ＋ `PRD` ＋ 並び替え「レベル」）**を
「**#43 の有用なヒントが失われるトレードオフの実物**」として提示していた。
⚠️ ⭐ **実際にそこで失われたのは、有用なヒントではなく嘘の案内だった。**

### 3-2. 実測（リードが先に実測し、設計担当が独立に再現した）

before＝`git archive b537967f^` を**別ディレクトリへ展開し、そこの `serve.mjs`** を **3040** で起動
（⚠️ `cd` では配信ルートが切り替わらない）。after＝作業ツリーを **3041**。
⭐ **本測定の前に、3040 に新文言が無く 3041 にあることで検算した。**

| 条件 | 変更前（3040） | 変更後（3041） |
|---|---|---|
| `/?q=アリス` | **8枚** | **8枚**（同一） |
| `/?q=アリス&set=PRD` | 0枚 `日本語テキストには一致しましたが、絞り込み条件に合うカードはありませんでした（絞り込みを外すと表示できます）。` | 0枚 `条件に合うカードがありませんでした。` |
| ⚠️ ⭐ **`/?q=アリス&set=PRD&sort=level`** | 0枚 ⚠️ **`日本語テキストには一致しましたが、レベルを持つカードはありませんでした（並び替えを「名前順」に戻すと表示できます）。`** | 0枚 `条件に合うカードがありませんでした。` |
| ⭐ **`/?q=アリス&sort=level`** | ⭐ **5枚** `5 件を表示（日本語テキスト一致・翻訳済みのみ）（レベルを持つカードのみ）` | ⭐ **5枚**（同一） |

⭐ **最下行が決定打。** 「アリス」は**レベルを持つカードを5枚持っている**ので、
U4 の条件が0件になる原因は **`PRD` の絞り込み**であって並び替えではない。
⚠️ ⭐ **並び替えを「名前順」に戻しても0件のまま**（2行目が実測）。
**それなのに変更前は「レベルを持つカードはありませんでした／名前順に戻すと表示できます」と案内していた＝嘘。**

### 3-3. 原因 — ⚠️ ⭐ **分岐の条件が「その原因かどうか」を判定していない**

変更前の #43 の分岐は **`info.jpMode && info.numericSort && info.jpMatched > 0`** で、
⚠️ ⭐ **`info.jpFiltered`（絞り込みで落ちたか）を見ていない**。
**数値並び替えが選ばれてさえいれば、真の原因が絞り込みでも先に横取りする。**

⚠️ ⭐ **さらに、この条件を検証項目にしていた設計が2つあり、どちらも順序は測ったが妥当性は測っていない**:

| 設計 | 検証項目 | ⚠️ 何を期待値にしていたか |
|---|---|---|
| `未採番-日本語モードの0件表示`（2026-09-04） | **V10** | 「#43 の文言が従来どおり出る（横取りされていないこと）」 |
| `101-0件文言の二重定義` | **V8** | ⭐ **まさに `/?q=アリス&set=PRD&sort=level`** で #43 の文言を期待値にし、⚠️ ⭐ **「『絞り込みを外すと表示できます』が出たら順序が壊れている」と書いた**——⭐ **実際にはそちらが真実だった＝判定が反転していた** |

⭐ **教訓（設計担当あて）**: ⚠️ ⭐ **「どの分岐が先に評価されるか」を測っても、「その説明が正しいか」は測ったことにならない。**
**原因を名乗る文言は、その原因だけを取り除いたときに結果が変わることまで確かめる**（§3-2 の4行目がそれ）。

### 3-4. ⭐ したがって #114 の評価はこうなる

| | |
|---|---|
| ⭐ **U4 の条件（絞り込みが真の原因）** | ⭐ **#114 は改善である。** 嘘の案内が消え、**無内容だが正しい1文**になった |
| ⚠️ ⭐ **決定Aの真のトレードオフ（残っている）** | ⭐ **本当に数値並び替えだけが原因の0件**（絞り込みなし・その項目を持つカードが1枚も無い）では、**「名前順に戻せば出る」ことが画面のどこからも分からない**。⚠️ ⭐ **ここは #114 で失ったままで、代替経路は無い**（設計書 §3-1 の #1 が意味を持つのはこの条件だけ）。同じく **終端3（取得失敗）** も代替経路が無いまま落ちている（§3-1 の #4） |
| ⭐ **構造の問題** | **分岐が原因を判定していない**という型は **#114 では直っていない**（文言ごと消えただけ）。⚠️ ⭐ **将来また原因別の文言を出そうとすると同じ嘘が復活する** → **#115** |

## 4. ⭐ 派生 #115（U4 由来・起票済み）

- **#115「0件の原因を名乗る分岐が、その原因かどうかを判定していない（並び替えのせいだと嘘をつく）」**（`enhancement` / `着手可`）
- ⚠️ ⭐ **起票前に設計担当が実測して再現させた**（§3-2。⚠️ **再現するのは `b537967f^`。HEAD では症状が出ない**）
- ⚠️ ⭐ **issue 本文の冒頭に「いま HEAD で何が起きるか」を書いた**——
  **嘘の文言はもう出ない／残っているのは構造と、それを追認した検証項目2件**。
  ⭐ **これを書かずに「嘘の文言が出る」とだけ書くと、着手した人が再現できず腐る**
- ⚠️ **`小タスク` は付けていない**。⭐ **案A（規約として残す）・案B（条件を直す）・案C（検証項目のひな形にする）で
  規模が桁違い**で、**どれを採るかが設計の中身**だから（#102 も同じ理由で付けていない）

## 5. ⭐ U3 は #102 に取り込んだ（新規起票にしなかった）

**#102**（いまのタイトル: **「索引がレアリティを判定できないため、0件確定でも『もっと見る』を押させ、該当が実在しても1画面目に出ない」**）に
**症状②として取り込み**、⭐ **タイトルを両方が読める形に変更した**。コメント: <https://github.com/dozyouneko/ga-card-tools-jp/issues/102#issuecomment-5812902937>

### ⚠️ ⭐ 新規起票にしなかった根拠（**後から「なぜ1つなのか」を辿れるように**）

| # | 根拠 |
|---|---|
| **1** | ⭐ **原因が同一**——どちらも **レアリティがメタ索引に無く、取得後にしか判定できない**（`metaMatches()` が読み飛ばす）。**片方だけを直す実装は書けない** |
| **2** | ⭐ ⚠️ **#102 の案Aが、実測上どちらも完全に解消する**（→ 下表）。**修正が1つなら issue も1つでよい** |
| **3** | ⚠️ **分けると設計書・実装・検証項目が2本とも同じ中身になり、片方がクローズされてもう片方が腐る** |
| ⚠️ **4** | ⭐ **取り込みの代償は「症状②が忘れられること」**なので、**タイトルを変え**、**コメントに受け入れ条件 A1〜A4 を置いた**（⚠️ **設計時に A1・A2 を両方とも検証項目にする**） |

### ⭐ 案Aが症状②も解消することの裏取り（⚠️ **推測で書かず数えた**）

| 項目 | 実測（2026-09-24・スナップショット2,495枚 / `data/card-meta-index.json`） |
|---|---|
| **レアリティ8を持つカード** | ⭐ **全体で 19枚**（セットは8つ＝すべて `1st Ed.` 系） |
| うちメタ索引にあるもの | ⭐ **19枚（全部）** |
| 1ページの取得枚数 | **40** |

⭐ **`entry[7]` が「そのカードがその版で持つレアリティの集合」になれば、`rarity=8` の候補は最大19枚＝1ページに収まる**
＝ **3枚は1画面目に出て「もっと見る」も出ない**。症状①側は候補が **0件**になり、**1回も押さずに確定する**。
⚠️ ⭐ **「そのセットに rarity 8 がある」というセット単位の集合に読み替えると効かない**（1st Ed. 系は数百枚）ので、
**コメントにその注意も書いた**。

### ⭐ 症状①も HEAD で再現することを確認した

`/?q=の&set=RDOPD&rarity=6` は **#114 適用後も「もっと見る」3回・`/cards/:slug` 141リクエスト**のまま
（⭐ **文言だけが新しいものに変わった**）。**#102 本文の表の文言は 2026-09-21 時点の記録**なので、
**いまの文言をコメントに書き足した**。

## 6. ⭐ 開発担当・レビュー担当の確認事項への回答

| # | 確認事項 | ⭐ 回答 |
|---|---|---|
| **1** | **V17d の期待値**（共有URL `/?q=の&rarity=6&set=RDOPD` は「途中0件」に当たるので文言が必ず変わる） | ⭐ **設計書 1.0 の誤り。実装は正しい。** **期待値を書き換えた**（設計書 §8-4 → 1.1）: **復元結果（`location.search`・`q`・エキスパンション・レアリティ・モード・並び替え・タイル数・「もっと見る」・バッジ）が before と一致すること／文言は V2/V3/V16a と同一の新文言になるのが正**。⚠️ ⭐ **URL は差し替えない**——**#20 の共有URL復元を測るという V17d の目的は、0件でも果たせている**（むしろ「0件でも復元が壊れない」ことまで測れて強い）。⚠️ **実装・レビューが独立に同じ指摘を上げてきたのは正しい振る舞い**（設計側の誤りを止めた） |
| **2** | **ブロック先頭の注意書きの書き換え**（D5 の4箇所に無い5箇所目） | ⭐ **承認する。戻さない。** 旧文は**消えた2分岐（#43・絞り込み）の順序を守れ**という内容で、⚠️ **そのままなら嘘のコメントが残る**（⭐ **しかもその嘘は §3-3 のとおり「順序さえ守れば正しい説明が出る」という誤った前提そのもの**）。新文（`element-and → shown === 0 → 件数行` の順序と、早期returnを後ろへ動かすと前の検索のタイルが残る）は**現在のコードと一致**しており、**D4 が守れと言っている要求はそのまま残っている**。⭐ **文字列リテラルではないので断片数にも影響しない**（V19 が `5断片` で裏取り済み） |
| **3** | **§6-2 の「成功行の数字も直す」はコード変更不要だった** | ⭐ **設計書 1.0 の誤り。実装は正しい。** 成功行は `` `… — ${frags.length}断片 / …` `` と**動的**なので、**コード側に数字は無い**。**§6-2 と「先に読む5点」の 4 を 1.1 で訂正した**（直すのは `SS_MIN` の値とその上のコメントの2つだけ） |
| **4** | **U1〜U6・実ログイン・本番は未検証** | ⭐ **U1〜U6 は本日ユーザーが実施した**（§2）。**実ログインのデッキ構築と本番での挙動は未検証のまま**だが、⭐ **本タスクの文言は `deckData` に一切依存せず `info` だけから組み立てる**こと、**`_headers` / `404.html` / `functions/` を触っていない**ことから、**push 後の確認は `push待ち` の工程に委ねる**（⚠️ **push 時は #101 の `09883941` と2件が一緒に出る**） |

### ⭐ レビューの推奨指摘（S）の扱い

| | ⭐ 扱い |
|---|---|
| **S1**（ブロック先頭の注意書き） | ⭐ **承認**（上の 2.）。**レビューの判断「残すと嘘になるので直すのが正しい」に同意する** |
| **S3**（`app.js` の fail-open が分岐を持たないこと） | ⭐ **そのままでよい**。#101 由来の伝播ラグ対策で、**二重定義が戻る経路にはなっていない**ことをレビューが確認済み |
| **参考**（`jpChecked` / `jpMatched` / `jpFiltered` の消費側が0になった） | ⚠️ ⭐ **起票しない。** **D5 の判断（残す・コメントを書き換える）を維持する**。理由は ⭐ **#115 が「原因別の文言を出すときにこれらを正しく使う」タスクになりうる**から——**いま消すと、#115 で作り直すことになる**。⚠️ **レビューの懸念（根拠が D5 だけ）はそのとおりなので、本書と #115 §5 の案Bがその根拠を補強する記録になる** |

## 7. ⭐ 派生issueの要否（まとめ）

| 由来 | 扱い |
|---|---|
| **U4**（分岐が原因を判定していない） | ⭐ **#115 を新規起票**（§4） |
| **U3**（該当が実在するのに1画面目に出ない） | ⭐ **#102 に症状②として取り込み・タイトル更新**（§5） |
| **U5**（`element-and` だけ長い文が残る） | ⭐ **起票しない。** ユーザーが **OK** と判断した（§2）。⚠️ **揃えるなら「先にスマホでバナーを出す」→「そのうえで潰す」の順**という設計書 §4-2 の記録は残す |
| **U6**（375px） | ⭐ **起票しない。** **OK**。⚠️ **#101 由来の2行化も窮屈という判断は出なかった** |
| **終端3（取得失敗の説明が落ちた）** | ⚠️ ⭐ **いまは起票しない**が、⭐ **代替経路が無いまま落ちている**ことを §3-4 に明記した。**#115 で原因別の文言を扱うときに、終端3を一緒に検討する**（#115 §5 の案B） |
