**対象: 「0件文言の簡素化」**

# 設計完了 — #114 検索結果が0件のときの文言を簡素化する（2026-09-24）

- 設計書: **`docs/design/114-0件文言の簡素化/0件文言の簡素化_設計.md`**（1.0）
- issue: **#114**（`enhancement` / `小タスク` / `着手可` → **`実装待ち`**）
- 前提: **#101 は承認済み・`push待ち`**（`09883941`）。文言は `shared/js/card-search.js` の
  `searchStatus()` **1箇所**にある＝**本タスクはそこだけを書き換える**

---

## 1. 対象範囲 — ⭐ **触るのは2ファイルだけ**

| ファイル | 何を |
|---|---|
| `shared/js/card-search.js` | `SEARCH-STATUS:START/:END` ブロック内の**0件分岐**（7 → 2）＋ **コメント4箇所**（D5） |
| `scripts/validate.mjs` | 断片の**下限 9 → 5**・**成功行の数字**・その上のコメントの「実測は11個」 |

- ⚠️ ⭐ **`app.js` / `tools/deck-builder/app.js` は1行も変えない**（触ったら #101 の検査が exit 1）。
  ⭐ **2画面ぶんの文言が1箇所の書き換えで揃うこと自体が、#101 の成果の確認になる**
- ⚠️ **生成物は1つも変わらない**（`build:cards` を回さない。差分に出たらAPI由来のドリフトなので混ぜない）

## 2. 判断済み事項（⭐ **未決事項ゼロ**）

| # | 決定 |
|---|---|
| **D1** | 変更は上の2ファイルだけ |
| **D2** | ⭐ **終端**（「もっと見る」が出ない）は **`条件に合うカードがありませんでした。` の1文だけ**（**5分岐 → 1**）。⚠️ **括弧書きを一切付けない・JPと英語で出し分けない** |
| **D3** | ⭐ **途中**（「もっと見る」が残る）は **`このページに条件に合うカードはありませんでした。「もっと見る」で続きを確認できます。`**（**2分岐 → 1**）。⚠️ **候補件数・確認済み件数は出さない** |
| **D4** | ⚠️ ⭐ **`element-and` の1文は据え置き**（→ §3）。**早期returnも消さない**（`shown` を問わず先頭で判定するのが役割） |
| **D5** | ⭐ **`info` のフィールドは減らさない**（`jpChecked` / `jpMatched` / `jpFiltered` を読む人がいなくなるが残す）。⚠️ ⭐ **ただし「0件文言の出し分け用」と書いたコメント4箇所は直す**——**V20 の grep がこれを数える** |
| **D6** | ⭐ **#101 の自動検査の下限を 9 → 5**（＝実測と同値）。成功行は **`search status text is single-sourced — 5断片 / 2ファイル走査`** |
| **D7** | 検証は **「合成 `info` の直接呼び出し」＋「実画面」の2段**（終端3・途中Bは実画面で狙って再現できないため） |

## 3. ⚠️ ⭐ `element-and` を巻き込まないのは**実測で決めた**

形のうえでは「終端の0件」なので決定Aを字面どおり当てると潰れるが、**潰さない**。

| 画面 / 幅 | エレメントAND警告バナー（`#fwarn`）の高さ |
|---|---|
| トップ / 1400px | 128px（見える） |
| ⭐ **トップ / 375px** | ⭐ **0px＝描画されない** |
| デッキ構築 / 375px | 128px（描画はされる） |

⭐ **スマホのトップでは件数欄がこの説明の唯一の伝達経路**。しかもこの文言は
**「そういうカードはこの世に無い」という説明で、絞り込みを緩めても永久に出ない**＝
**落とすと利用者が延々と条件を変え続ける**。
⭐ **揃えるなら「先にスマホでバナーを出す」→「そのうえで潰す」の順**で、**別タスク**（R4）。

## 4. ⚠️ ⭐ 引き継ぎの記録 — **決定Aは 2026-09-04 の判断を部分的に取り消す**

あちらは「**従来の文言だと『一致しなかった』と嘘になり、原因が画面のどこにも出ない**」という理由で
ヒントを足した。⭐ **今回それを落とすので逆向きに戻る。** ⚠️ **ユーザーが説明を読んだうえでの判断**なので
**蒸し返さない**。⚠️ **実装側で「親切だから残す」と判断しないこと。**

**見えなくなるもの4つ**（設計書 §3-1 に代替経路の有無つきで表にした）:

1. **#43**: 並び替えを「名前順」に戻せば出ること — ❌ **代替経路なし**
2. **絞り込みで全滅**したこと — ⭐ 選択中の条件チップは見える
3. **未翻訳は日本語検索できない**こと — ⭐ 1件以上出たときの件数行の注記で学べる
4. ⚠️ ⭐ **取得に失敗した**こと（終端3） — ❌ **代替経路なし**。
   **一時的な失敗が「条件に合わなかった」と読める**＝**再試行すれば出るのに条件を変えてしまう**

⚠️ ⭐ **4 は「冗長だから落とす」の対象ではなく副作用として落ちるもの**だが、**決定Aどおり落とす**。
**U4 でユーザーに見てもらう**。⚠️ **実装側で終端3だけ例外にしないこと**（判断が要ると思ったら issue に書いて止まる）。

さらに ⚠️ **途中の0件では「進んでいる手応え」が消える**（いまは `40 → 80 → 120` と増える）。→ **U3**。

## 5. 検証項目の要約（詳細は設計書 §8）

| 群 | 内容 |
|---|---|
| **V1〜V10** | ⭐ **合成 `info` で `GA_CARD_SEARCH.searchStatus()` を直接呼び、全10経路を踏む**（実画面で再現しにくい終端3・途中Bを含む）。⭐ **トップとデッキ構築の両ページで `===` 突き合わせ**。⚠️ **V4〜V8 が5つとも同一の1文になることが本タスクの中身** |
| **V11〜V15** | 実画面の0件4条件＋エレメントAND。⭐ **V14（並び替えレベル）が V11 と同じ文言になるのが正**（#43 の説明が消えた） |
| **V16a〜c** | ⚠️ ⭐ **最重要**。`/?q=の&rarity=8` で **1画面目0枚 →「もっと見る」→ 3枚**。⚠️ **V16a で「もっと見る」が消えたら不合格**（続きに3枚あるのに探すのをやめさせる）。⭐ **設計担当が本日実測した**（V16c は**押しても3枚のまま・もっと見るも残る**） |
| **V17a〜V17g** | 0件でないときを**変えていない**こと。⭐ **M1-b の4観点**（件数表示・共有URL #20・絵柄の追従 #41・バッジ集計）＋ `numericSortNote`。⚠️ **before/after の2サーバー比較**で測り、⚠️ **`cd` では配信ルートが切り替わらない罠**を避け、**測定前に既知の差で検算する** |
| **V18a〜V18e** | ⭐ **破壊試験5ケース**。V18a は**共有側の1文言を壊すと両画面が同時に変わる**こと（⚠️ **片方だけなら未達**）、V18d は ⭐ **`（下限 5個）` と出ること**まで見る（＝**下限を直したことの確認**）。⚠️ ⭐ **復旧に `git checkout -- <path>` を使わない**——**原本をスクラッチへ退避し `cp` で戻し、各ケース後に `git status --short` が空**であることまで報告する |
| **V19〜V22** | `npm run validate` **exit 0** ＋ 成功行が **`5断片`**／⭐ **grep（`docs/` を除外）で落とした文言が0行**（⚠️ **`件を確認しましたが` は変更前 2行＝文言1＋コメント1。コメントを直さないと1行残る**）／`LINEREF` 緑／**生成物の差分0件** |

⭐ **デッキ構築はログイン不要で検証できる**（#101 設計書 §5 の `page.route()` 手順。本設計の実測も全部それで取った）。

## 6. ⚠️ この工程で担保できないもの

- ⭐ **レビュー合格後に「ユーザーが 3000番のローカルサーバーで実物を操作する工程（②）」が入る**。
  見てもらうことは設計書 §9 に **U1〜U6 の番号つき**で書いた
  - ⚠️ ⭐ **U3（押した手応えが消える）・U4（#43 の原因が分からなくなる）・U5（element-and だけ長い文が残る）で
    「戻したい／揃えたい」となったら、そこで止めて設計に戻す**（`（→ 継続）`）。
    **文言を作り直すのは仕様の判断**なので実装側で決めない
  - ⚠️ **#101 由来の 375px の2行化（件数行の注記）は本タスクでは変わらない**。
    **U6 で窮屈だと判断されたら、それは #101 由来の別issue**
- ⚠️ **サーバーは 3000番で上げる**（`forwardPorts` が `[3000]` の1つだけ。3100 で渡すと
  **コンテナ内からは 200 に見えるのにユーザーには何も映らない**）

## 7. 未決事項

⭐ **なし。** 設計書 §5 の D1〜D7 ですべて確定している。
⚠️ **判断が要ることが出てきたら実装側で決めず、issueに書いて設計担当の回答を待つこと。**
