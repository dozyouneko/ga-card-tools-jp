**対象: 「未公開注記の陳腐化」**

# 設計完了 — 2026-09-20（issueコメントの代替）

設計書: [`未公開注記の陳腐化_設計.md`](未公開注記の陳腐化_設計.md)（1.0）
起票案: [`起票案_2026-09-13_未公開注記の陳腐化.md`](起票案_2026-09-13_未公開注記の陳腐化.md) → 状態 **`実装中`**

## 1. 対象範囲

**1単位（分割しない）**。⚠️ **A3 と B2 は同じコミットに入れる**——B2 は fail-closed なので、
10箇所が残ったまま検査だけ入れると即 exit 1 になる。

| # | 内容 | 触るファイル |
|---|---|---|
| A3 | `CLAUDE.md` の10箇所から「未pushなので本番にはまだ出ていない」を落とし、測り方の案内を**1箇所だけ**置く（マーカーで囲う） | `CLAUDE.md` |
| B2 | 禁止パターン検査を追加（`CLAUDE.md` のみ走査・fail-closed） | `scripts/validate.mjs` |

## 2. ユーザー決定（2026-09-20・ヒアリング済み）

- **A3**（測る手順へ寄せる）／**B2**（禁止パターン検査）／走査範囲は **`CLAUDE.md` だけ**／
  **是正が終わり次第すぐ単独で公開**（09-27 の週次バッチを待たない）

## 3. 設計担当の裁定（起票案 §6 の残り2問）

- **裁定①（禁止の広さ）**: **「未push」を含み、かつ公開状態を否定する語を含む行**だけを禁じる。
  ⚠️ 「push」と「本番公開」の同居まで広げない（`CLAUDE.md` に**7行**あり、すべて正当な運用説明）
- **裁定②（受け皿）**: **個別注記で「まだ本番に無い」と言う場面は作らない**。転記をゼロにするのが A3 の狙い

## 4. 実測（2026-09-20・⚠️ 起票案の9件から増えている）

- **10箇所**（起票案の実測時は9箇所）。⚠️ ⭐ 増えた1件は **`c5d456ec`（09-17「締め作業の反映」）**で追加された
  ＝**起票案が問題を記録した4日後に、また同じ形の注記が書き足された**
- ⭐ **人の見落としはこれで3回**（`36394794` 09-06 / `9f90a58a` 09-09 / `c5d456ec` 09-17）。**B2 の根拠**
- ⚠️ **実装時は行番号を信用せず測り直すこと**（`grep -n "未pushなので本番にはまだ出ていない" CLAUDE.md`）

## 5. 検証項目

設計書 §6 の **V1〜V11**。⚠️ ⭐ **V3〜V7 は破壊試験**で、**検査が実際に止めることを確かめるのが本体**
（「動いたか」だけを測ると 09-17「検索結果のタブ化」と同じ型で破れる）。
とくに **V6（`push待ち` の目的説明を誤検出しないこと）** と **V7（案内ブロックの除外が空回りしていないこと）** を省略しない。

## 6. 判断済み・未決

- **判断済み**: 単位分割なし／走査範囲／禁止の広さ／受け皿を作らない／案内の置き場（git運用節の `docs/` 例外の直前）
- **未決（実装側で決めてよい）**: 検査コードの関数分割・変数名・エラーメッセージの細かい語順
- ⚠️ **未決（実装側で決めない）**: 禁止パターンの追加・削除、走査範囲の拡大 → **設計へ戻す**

## 7. ⚠️ 引き受けていない穴（設計書 §7）

- ブラックリストなので**言い換えれば抜けられる**（`LINEREF` と同じ性質）
- ⚠️ **`✅ 本番公開済み` とだけ書くのは止められない**（裁定①が「未push」を含む行だけを見るため）。
  A3 の案内で規約化するところまでが守備範囲

## 8. 公開

⚠️ **`CLAUDE.md` も `scripts/` も配信物**。**公開はユーザーの明示的な指示があるまで実行しない**。
⚠️ 経路②は `git archive HEAD` から作るので、**公開の可否を聞く前に「一緒に出るもの」を提示する**。
