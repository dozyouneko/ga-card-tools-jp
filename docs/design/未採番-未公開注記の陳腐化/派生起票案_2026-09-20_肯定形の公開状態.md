**状態: 生きている**（最終確認 2026-09-21）— 呼び名「肯定形の公開状態」。⚠️ **`CLAUDE.md` の禁止パターン検査は「未push」を含む行だけを見るため、`✅ 本番反映済み` のような肯定形の現在形の主張が素通りする**（2026-09-20 のレビュー実測で該当は**1件**と確定）。⭐ **親タスク「未公開注記の陳腐化」の裁定④でスコープ外にしたもの** — ✅ **#104 として起票済み**（2026-09-21・投函前に実測して再現を確認） ／ 索引: [`待ち行列と復旧手順.md`](../待ち行列/待ち行列と復旧手順.md)

# issueの起票案 — `CLAUDE.md` の肯定形の公開状態（`✅ 本番反映済み`）は検査を素通りする

- ⭐ **起票時**: タイトル「`CLAUDE.md` の肯定形の公開状態が禁止パターン検査を素通りする」／ラベル `enhancement` `小タスク` `検討中`
- 出所: 「未公開注記の陳腐化」の設計書 §7 と、レビュー判定 `b4a892d0` の推奨指摘 **S-3**

## 1. 症状

2026-09-20 に入れた禁止パターン検査（`scripts/validate.mjs`）は、
裁定①により **「未push」を含む行だけ**を見る。したがって **肯定形はすべて素通りする**。

⚠️ **肯定形も現在形の状態なので、同じ型で腐る**——本番が巻き戻る・別経路で出す・
記述の対象が差し替わると、書いた本人に無断で誤りに変わる。

## 2. 実測（2026-09-20・レビュー担当）

⭐ **該当は `CLAUDE.md` に1件だけ**:

```
CLAUDE.md:1172  ✅ **2026-08-06に実装・本番反映済み**(7250f16c)
```

⭐ **この1件は現時点では正しい**（誤りではない）。問題は**正しさではなく書き方**。

⚠️ **件数はここに書いた時点から動く。着手時に測り直すこと**:

```bash
grep -nE "本番(反映|公開)済み|本番に出ている|公開済み" CLAUDE.md
```

## 3. ⚠️ 難しいところ

- ⚠️ **禁止語を広げるほど誤検出が増える**。`CLAUDE.md` には「本番公開」を含む正当な運用説明が多数ある
  （親タスクの裁定①で「push」と「本番公開」の同居まで広げないと決めた根拠がこれ＝**7行**）
- ⭐ **1件しかないなら、検査を足さずに「その1件を書き換える」だけで済むかもしれない**
  （§2 の行を「`7250f16c` で実装」とだけ書き、公開状態を落とす）。
  ⚠️ **ただしそれは再発を止めない**——親タスクが A1 を退けたのと同じ理屈
- ⭐ **親タスクの案内ブロック（`PUBSTATE-NOTE`）は既に「書かない」と規約化している**ので、
  ⚠️ **規約だけで止まるかどうかがまさに争点**（同型は規約で **7回** 止まっていない）

## 4. 選択肢

| 案 | 内容 | ⚠️ 損 |
|---|---|---|
| C1 | 該当1件を書き換えるだけ（検査は足さない） | ⚠️ 再発を止めない |
| C2 | 肯定形も禁止語に足す（`本番反映済み` `本番公開済み` など限定列挙） | ⚠️ 言い換えに弱いのは同じ。誤検出は限定列挙なら小さい |
| C3 | 何もしない（親タスクの案内で規約化済みとみなす） | ⚠️ 規約は7回効かなかった |

⚠️ **決定はユーザーと設計工程で行う。** この起票案は選択肢と実測を並べるところまで。
