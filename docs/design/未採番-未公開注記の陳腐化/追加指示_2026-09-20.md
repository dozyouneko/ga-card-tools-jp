**対象: 「未公開注記の陳腐化」**

# 設計担当の裁定 — 実装報告（`195adc71`）の確認事項4件（2026-09-20）

設計書を **1.1** へ改版した。⚠️ ⭐ **実装の変更は求めない**（4件とも実装は現状のままでよい）。
レビュー担当は **設計書 1.1** を基準に判定すること。

## 裁定①（確認事項1）— ⭐ 設計書 §4-2 の snippet が誤りだった。**実装が正しい**

⚠️ ⭐ **1.0 の snippet は `<!-- PUBSTATE-NOTE:START -->` を行頭に置いており、
その2行下にある自分の警告（「マーカーを行頭に置かない」）に反していた。**
実装は `CRON-ADD` と同じ「**先頭行の末尾**」に置いており、こちらが正しい。

→ **設計書 §4-2 を実測に合わせて改版した**（1.1）。⚠️ **実装は変えない**。

⭐ **これは 09-17「検索結果のタブ化」の Q1 と同じ型**（設計書が達成できない期待値を書いていた）。
**設計書の誤りは設計書を直して解消する**——実装を設計書に合わせない。

## 裁定②（確認事項2）— ⭐ 行頭判定の拡張を**採用する**

実測（GitHub `/markdown` API）で **`- <!-- START -->本文` も Markdown が死ぬ**と確認された以上、
§5-2 の素朴な「行頭」では**穴が開いたまま緑になる**。実装の
「前置きから空白とリスト/引用記号を除いて何も残らなければ行頭」を**正**とする。

→ **設計書 §5-2 の表に定義を書き足した**（1.1）。⚠️ **実装は変えない**。

## 裁定③（確認事項3）— 案内ブロックの除外は**残す**

⚠️ 現在の文面では除外が常時眠っている（§4-2 が「文面に『未push』を使うな」と決めているため）という
指摘はそのとおりだが、**残す**。理由は3つ:

1. ⭐ **マーカー検査そのものは眠っていない**——0組・2組・逆順・行頭・空のいずれでも exit 1 になり、
   **案内が消えたり壊れたりすることを fail-closed で止めている**（除外はその副産物）
2. ⚠️ **除外を外すと「案内にも公開状態を書けない」ことになり、受け皿がゼロになる**。
   将来 §4-2 の文面を変えたくなったときに、**検査を先に直さないと書けない**という順序が生まれる
3. ⭐ **V7 で「注入すれば効く」ことを実測済み**＝空回りではなく、**出番が来ていないだけ**

⚠️ ⭐ **ただし「眠っている」事実は設計書に残す価値がある**ので、レビューはこの点を
**不合格の理由にしない**（S 相当の観察として扱ってよい）。

## 裁定④（確認事項4）— 肯定形（`✅ 本番公開済み`）は**このタスクでは触らない**

設計書 §7 に**既知の穴として明記済み**。⚠️ **禁止語を増やすと誤検出が跳ね上がる**ため、
**承認時に派生起票案へ落とす**（＝スコープ外）。⚠️ **実装側で禁止語を足さないこと**（現状維持が正しい）。

## 体裁の逸脱について

案内の丸括弧を半角にした件は **追認する**（周囲の表記に合わせたもので、語句は設計書のまま）。

## ⚠️ 未実施の検証について

`CLAUDE.md` が読めない場合の exit 1 を共有ツリーで試さなかった判断は**妥当**
（他セッションを巻き込む）。⭐ **先例 `LINEREF` と同一形であること**と
**V5-a（マーカー0組）が同型の fail-closed を通っていること**で代替と認める。
