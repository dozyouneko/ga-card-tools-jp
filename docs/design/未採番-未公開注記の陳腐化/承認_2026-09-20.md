**対象: 「未公開注記の陳腐化」**（→ 完了）

# 承認 — 2026-09-20（issueコメントの代替）

| 項目 | 値 |
|---|---|
| 実装 | **`195adc71`**（`CLAUDE.md` / `scripts/validate.mjs` の2ファイル） |
| 実装報告 | `23787568` |
| 設計書 | **1.2**（裁定で 1.1・レビュー反映で 1.2） |
| レビュー判定 | **`b4a892d0` — 合格・差し戻し0回** |
| 派生起票案 | 2件（下記） |

## 1. 承認の理由

- ⭐ **必須基準 M1〜M7 がすべて ✅**、検証項目 **V1〜V11 をレビュー担当が全部自分で実行し直して一致**
- ⭐ **破壊試験が本体の設計だった**（検査が「実際に止めること」を確かめる）。
  レビューは **14通り**を隔離ツリーで実施し、**§5-2 の条件と1対1で対応**することまで確認した
- ⭐ **レビューが「残っている確認」を1つ潰した**（F3: `CLAUDE.md` が読めないときの exit 1 を
  隔離ツリーで実施）。⚠️ **実装側が共有ツリーを壊さないために見送った判断は妥当**で、
  レビューが環境を作って代わりに実施したのは**あるべき形**
- ⭐ **レビューが V6 を逆方向でも見た**（残る「未push」8行を1行ずつ読み、**落とすべきものの見逃しゼロ**）。
  ⚠️ **「誤検出しない」だけを見ると、見逃しには気づけない**——この見方は横展開する価値がある
- ⭐ **V9 を別手段で裏取りした**（実装は GitHub `/markdown` API・レビューはローカル `marked@15`）。
  **2つの独立した測定で差分が完全一致**

## 2. 設計担当の裁定（実装報告の確認事項4件）→ [`追加指示_2026-09-20.md`](追加指示_2026-09-20.md)

⭐ **4件とも実装は変えずに決着した。** ⚠️ うち**裁定①は設計書自身の誤りの是正**
（1.0 の §4-2 snippet が `START` を行頭に置いており、2行下の自分の警告に反していた）。
⭐ **09-17「検索結果のタブ化」の Q1 と同じ型**——**設計書の誤りは設計書を直す**。

## 3. レビューの推奨指摘 S をどう扱ったか

| # | 内容 | 扱い |
|---|---|---|
| **S-1** | 禁止句が**行をまたぐ**と素通りする | ⭐ **設計書 §7 に穴として追記（1.2）**。⚠️ **実装は変えない**——行を連結して判定すると正当な記述をまたいで誤検出する。⭐ **再発実績のある形（過去3回とも1行に収まる）は捕まる** |
| **S-2** | `END` をファイル末尾へ動かすと**全行が除外され無言で緑** | ⭐ **設計書 §7 に追記 ＋ 派生起票案へ**。⚠️ **既存の `CRON-ADD` も同型**なので、**直すなら2つまとめて** |
| **S-3** | 肯定形（`✅ 本番反映済み`）が素通り。**該当は1件**と実測で確定 | ⭐ **派生起票案へ**（裁定④でスコープ外と決めていたもの。**対象1件**と書けるようになった） |

## 4. 派生起票案（2件・どちらも 🟢 生きている）

1. [`派生起票案_2026-09-20_肯定形の公開状態.md`](派生起票案_2026-09-20_肯定形の公開状態.md)（S-3）
2. [`派生起票案_2026-09-20_マーカーブロックの無制限.md`](派生起票案_2026-09-20_マーカーブロックの無制限.md)（S-2）

⚠️ **どちらも「無言で緑」の型**なので、⭐ **起票案の中に「着手時に測り直すコマンド」を書いてある**
（件数を本文に固定しない＝本タスクで直したのと同じ失敗をしないため）。

## 5. ⚠️ 公開

⭐ **ユーザー決定は「是正が終わり次第すぐ単独で公開」**（2026-09-20 のヒアリング）。

⚠️ **経路②は `git archive HEAD` から作る**ので、**公開の可否を確認する前に
`git diff --stat <本番位置>..HEAD -- . ':(exclude)docs/*'` で「一緒に出るもの」を提示すること**。

## 6. ⭐ 本タスクの意義（記録として残す）

⚠️ ⭐ **この件は「規約では止まらない」ことの実測値が3回ぶん溜まってから、ようやく機械検査になった**
（`36394794` 09-06 / `9f90a58a` 09-09 / **`c5d456ec` 09-17 ＝起票案の4日後**）。
⭐ **3回目は、問題が文書化された後に起きている**——**知っていても止まらなかった**。

⭐ **`LINEREF` に続く2例目の「真偽ではなく書き方を禁止する検査」**。
⚠️ **真偽を測る検査は入らない**（本番位置の測定にネットワークが要り、`validate` はオフライン前提）
という制約の下で採れる形はこれだった、という結論を設計書 §3 に残してある。
