**対象: 「未公開注記の陳腐化」**

# レビュー判定 — 2026-09-20（A3＋B2・issueコメントの代替）

| 項目 | 値 |
|---|---|
| **判定** | ⭐ **合格** |
| レビュー対象コミット | **`195adc71`**（`CLAUDE.md` / `scripts/validate.mjs` の2ファイル） |
| 実装報告 | [`実装報告_2026-09-20.md`](実装報告_2026-09-20.md)（`23787568`） |
| 判定の基準 | [`未公開注記の陳腐化_設計.md`](未公開注記の陳腐化_設計.md) **1.1** ＋ [`追加指示_2026-09-20.md`](追加指示_2026-09-20.md)（裁定4件） |
| 証跡 | `tmp/review/未公開注記の陳腐化/`（gitignore） |
| 差し戻し回数 | **0**（1周目で合格） |

⚠️ **実装報告の記述は一切そのまま採らず、V1〜V11 を全部自分で実行し直した**（破壊試験14通りを含む）。

## 0. 検証のやり方（⚠️ 作業ツリーは触っていない）

⚠️ **共有ツリーの `CLAUDE.md` を書き換えると他セッションを巻き込む**ので、
**`tmp/review/未公開注記の陳腐化/` に隔離ツリーを作ってそこで破壊試験を行った**。

- 隔離ツリーの作り方: リポジトリ直下の各エントリを**シンボリックリンク**で張り、
  **`scripts/` と `CLAUDE.md` だけ実体コピー**にした。`scripts/validate.mjs` の `root` は
  `import.meta.url` から決まるので、**`node <隔離ツリー>/scripts/validate.mjs` で本物の検査一式がそのまま走る**
  （⭐ 実測: 隔離ツリーでも `all checks passed.` / exit 0 が再現する＝`tmp/review/未公開注記の陳腐化/baseline.txt`）
- **最終確認: `git status --short` は空**＝作業ツリーは HEAD と一致。`git checkout` / `stash` / `restore` は一度も使っていない
- Markdown の描画は **ローカルの CommonMark 実装（`marked@15`・スクラッチパッドに導入）**で測った
  （⭐ 実装報告は GitHub `/markdown` API で測っている＝**別手段で裏が取れた**）

## 1. 必須基準（M1〜M7）

| 基準 | 結果 | 証跡 |
|---|---|---|
| **M1** 設計適合 | ✅ | 設計書 1.1 §6 の **V1〜V11 すべて期待値と一致**（§2 の表）。⭐ **M1-b も検討して欠落なし**（§3） |
| **M2** 実駆動 | ✅ | ⭐ **ブラウザUIを持たない変更**なので「操作する面」は `npm run validate` と `CLAUDE.md` の描画。**両方を自分で駆動した**——validate は**本物のツリーで1回＋隔離ツリーで15回**（うち破壊試験14通り）、描画は `marked` で**旧版・新版を実際にレンダリングして差分を取った**（`tmp/review/未公開注記の陳腐化/render.diff`）。⚠️ **375px 幅は適用外**（`195adc71` はブラウザが読むファイルを1つも変えていない＝下の M3） |
| **M3** 回帰 | ✅ | **`npm run validate` が exit 0**・`all checks passed.`（`tmp/review/未公開注記の陳腐化/npm-validate.txt`）。⭐ **主要画面の回帰リスクはゼロ**——変更2ファイルは `CLAUDE.md`（どのページからも参照されない静的アセット）と `scripts/validate.mjs`（node専用のビルド時スクリプト）で、`grep -rl "validate.mjs" --include=*.html .` は**0件** |
| **M4** 生成物の同期 | ✅ | 生成ロジックは無変更（`validate.mjs` は生成器ではない）。`queue index up to date — 起票案36件（実装中1 / …）`・`task records in sync — 工程記録142件` とも緑 |
| **M5** 静かな劣化がない | ✅ | ⭐ **わざと壊して落ちることを14通り確認した**（§4）。⭐ **除外の空回りも潰した**——`inBlock()` の呼び出しを外した版で**同じ入力が exit 1 になる**ことまで測った（F2）。⚠️ **残る穴2件は S へ**（§6・どちらも設計書 §7 の「引き受けていない穴」の範囲か、既存検査と同性質） |
| **M6** 公開安全性 | ✅ | 本番配信物の**増減なし**（既存2ファイルの更新のみ・新規追加0）。秘匿情報なし。`_headers` / `404.html` / キャッシュの前提は**未変更**。⚠️ **pushもデプロイもされていない**（`git status` 清潔・経路②未実行） |
| **M7** スコープ | ✅ | `git -c core.quotepath=false show --name-only --format= 195adc71` → **`CLAUDE.md` / `scripts/validate.mjs` の2件のみ**＝設計書 §3 の表と一致。逸脱3件はすべて実装報告 §4 に記載され、**追加指示（1.1）の裁定①②＋体裁の追認で裁定済み** |

## 2. 設計書 §6 の検証項目（V1〜V11）

⚠️ **すべてレビュー担当が自分で実行した値**。実装報告の数字は照合にのみ使った。

| # | 期待値 | 実測（レビュー） | 合否 |
|---|---|---|---|
| **V1** | `grep -c "未pushなので本番にはまだ出ていない" CLAUDE.md` = 0 | **0**（grep の exit も 1＝1件も無い） | ✅ |
| **V2** | exit 0・`no publication-state claims in CLAUDE.md — 0件（注記ブロック1組）` | **その文言のまま出力**・exit 0 | ✅ |
| **V3** | その形の注記を1行足すと exit 1・**行番号が出る** | **exit 1** / `CLAUDE.md:1462 — 「未push」と「本番にはまだ出ていない」が同居: …`（`out-D1.txt`） | ✅ |
| **V4** | 言い換え `未pushなので未公開` でも exit 1 | **exit 1** / `CLAUDE.md:1462 — 「未push」と「未公開」が同居: …`（`out-D2.txt`） | ✅ |
| **V5** | マーカー5通りとも exit 1・§5-2 の表と1対1 | ⭐ **6通りとも exit 1**（5通り＋設計 1.0 の字面＝リスト記号前置）。メッセージが条件と1対1に対応（§4 の表） | ✅ |
| **V6** | `push待ち` の目的説明の行が**残っていて**検査に当たらない | **149行に現存**（`⭐ 目的は「**未pushの本番配信物が、issueを見ただけで分かる**」状態にすること。`）・`validate` 緑。⭐ **逆方向も見た**——残る `未push` の**8行（149/346/350/354/391/450/457/551）を1行ずつ読み、どれも「特定の変更の公開状態」を語っていない**＝**見逃しゼロ**（§5） | ✅ |
| **V7** | 案内ブロックの除外が効く／⚠️ 除外を外すと当たる | **両方測った**。ブロック内に `⚠️ **未pushなので未公開**` を注入 → **緑**（`out-F1.txt`）。**同じ入力のまま `if (inBlock(i)) return;` を外した版** → **exit 1**（`CLAUDE.md:442`・`out-F2.txt`）＝**空回りではない** | ✅ |
| **V8** | diff が括弧内の該当句の削除だけ。本文・日付・句読点・`):` が無傷 | ⭐ **2通りで裏を取った**。①旧版から `。?\s*⚠️ \*\*(どれも)?未pushなので本番にはまだ出ていない\*\*` を除去（**ヒット10件ちょうど**）し、新版から案内5行を除いたものと**空白無視で完全一致**。②⭐ **描画後のHTMLを旧版/新版で差分**したところ**変更は11箇所だけ**＝案内ブロックの追加1 ＋ 削除10で、**日付・`):`・`。`・本文はすべて無傷**（`render.diff`） | ✅ |
| **V9** | マーカーが行頭に無い。**同じ行のインラインコードが `<code>` で出る** | ⭐ **GitHub API とは別手段（ローカル `marked@15`）で実測**。案内ブロックは `<strong>ある変更が…書かない。</strong>` ＋ `<code>wrangler pages deploy</code>` `<code>P</code>` `<code>git merge-base --is-ancestor &lt;実装のコミット&gt; $P</code>` が出る。**ファイル全体では `<code` +4 / `<strong` −7 / 生の `**` ±0**（＝新たに死んだ強調は0・実装報告の GitHub 実測と**差分が完全一致**） | ✅ |
| **V10** | `cron git add paths in sync` が緑・`8パス（README.md / CLAUDE.md）` | **`cron git add paths in sync — 8パス（README.md / CLAUDE.md）`**（文言完全一致） | ✅ |
| **V11** | `npm run validate` 全体が `all checks passed.` | **`all checks passed.`**・**exit 0** | ✅ |

## 3. M1-b（既存の同種の仕組みで効いている副作用が、新しい検査では効かないか）

⭐ 結論: **欠落なし**。比較対象は `CLAUDE.md` を読む既存のマーカー検査＝**`CRON-ADD`（#70）**。

| 既存 `CRON-ADD` が持つ性質 | 新 `PUBSTATE-NOTE` | 判定 |
|---|---|---|
| 出現数を数えて**1組だけを許す**（0組・2組以上で exit 1） | **ある**（D3 / D4 で実測） | ✅ |
| `END` が `START` より前なら exit 1 | **ある**（E1） | ✅ |
| マーカー間が空なら exit 1 | **ある**（E3） | ✅ |
| 読み込み失敗で exit 1（fail-closed） | **ある**（F3） | ✅ |
| マーカーの**行頭判定** | ⚠️ **`CRON-ADD` には無い。新検査にだけある**（E2 / E4）＝**強くなっている** | ✅ |
| 走査対象に `README.md` を含む | ⭐ **含まない**が、これは**ユーザー決定（走査範囲は `CLAUDE.md` だけ）**＝仕様どおり | ✅ |

- ⭐ **マーカーの置き方も既存と対称**: `CRON-ADD` は CLAUDE.md 1052/1054行で **START＝先頭行の末尾／END＝最終行の末尾**。
  新しい案内（441/445行）も**同じ置き方**で、⭐ **CommonMark 実装で「この置き方なら強調とコードが生きる」ことを実測した**
- ⭐ **副作用の干渉も無い**: 新しい案内は「git運用」節（441〜445行）、`CRON-ADD` は1052〜1054行で**重ならない**。
  V10 が緑なので**既存検査を壊していない**

## 4. 破壊試験の全結果（§5-2 の表と1対1）

⚠️ **すべて隔離ツリーで実施し、毎回 `npm run validate` 相当を丸ごと走らせた**（＝部分実行やロジックの再実装ではない）。

| ID | 何をしたか | §5-2 / §6 の対応 | exit | 出力 |
|---|---|---|---|---|
| **D1** | 末尾に `⚠️ **未pushなので本番にはまだ出ていない**` を1行追加 | **V3** | **1** | `CLAUDE.md:1462 — 「未push」と「本番にはまだ出ていない」が同居` |
| **D2** | 末尾に `⚠️ **未pushなので未公開**` を1行追加 | **V4** | **1** | `CLAUDE.md:1462 — 「未push」と「未公開」が同居` |
| **D3** | 両マーカーを削除 | **0組** | **1** | `マーカーがありません（START 0個 / END 0個）— 案内ごと消えています` |
| **D4** | ブロックを複製 | **2組以上** | **1** | `マーカーが複数あります（START 2個 / END 2個）— 1組だけにしてください` |
| **E1** | START と END を入れ替え | **`END` が先** | **1** | `マーカーが逆順です（END が START より前）` |
| **E2** | START を単独行へ | **行頭** | **1** | `CLAUDE.md:441 — マーカー … が行頭にあります（その行の Markdown が死にます）` |
| **E3** | START と END を隣接させ中身を空に | **マーカー間が空** | **1** | `マーカー間が空です — 案内の中身が消えています` |
| **E4** | ⭐ `- <!-- START -->本文`（**設計 1.0 の snippet の字面**） | **行頭（1.1 の拡張定義）** | **1** | `CLAUDE.md:441 — マーカー … が行頭にあります` |
| **F1** | ブロックの**内側**に禁止文を注入 | **V7 前半** | **0** | `no publication-state claims in CLAUDE.md — 0件（注記ブロック1組）` |
| **F2** | F1 と同じ入力で `if (inBlock(i)) return;` を外す | **V7 後半** | **1** | `CLAUDE.md:442 — 「未push」と「未公開」が同居`＝**除外は空回りしていない** |
| **F3** | ⭐ `CLAUDE.md` を削除（読めない） | **§5-2 の1行目**。⚠️ **実装報告では「未実施」だった項目** | **1** | `CLAUDE.md の読み込みに失敗: ENOENT … — 走査対象の指定が陳腐化しています` |
| **F4** | ⭐ マーカーを削除**したうえで**旧ブロック位置に禁止文を注入 | **追加（レビュー発案）**: 「マーカーが壊れても除外が効いたまま」型の穴が無いか | **1** | **2件とも出る**（`マーカーがありません` ＋ `CLAUDE.md:442 — …が同居`）＝**壊れたら除外は残らない** |
| **G1** | ⭐ END をファイル末尾へ移動（ブロックが全文を覆う） | **追加（レビュー発案）** | **0** | ⚠️ **緑**。→ **S-2** |
| **G2** | ⭐ 禁止句を2行に折り返す | **追加（レビュー発案）** | **0** | ⚠️ **緑**。→ **S-1** |

⭐ **F3・F4 は設計書にも実装報告にも無い確認**で、**レビューで新たに実施して塞がっていることを確かめた**。
とくに **F3 は実装報告が「共有ツリーでは試せない」として未実施にしていた項目**で、
⭐ **隔離ツリーでなら安全に実施できたため、代替ではなく実物で確認した**（＝**残っている確認は無くなった**）。

## 5. V6 の逆方向（落とすべきものを見逃していないか）

`CLAUDE.md` に残る `未push` の**8行を1行ずつ本文で確認した**。⭐ **どれも「特定の変更の公開状態」を語っておらず、落としてはいけない記述**:

| 行 | 内容 | 読み |
|---|---|---|
| 149 | `⭐ 目的は「**未pushの本番配信物が、issueを見ただけで分かる**」状態にすること。` | `push待ち` ラベルの**目的説明**。⭐ 設計書 §5-1 が名指しで「落としてはいけない」と書いている行 |
| 346 | 見出し `#### 未pushコミットが積み上がることを前提にする` | 自律運用の一般則 |
| 350 | `ローカルに未pushコミットを溜めるとリモートと乖離する` | 一般則（cronとの乖離） |
| 354 | `ユーザーへの提示は締め作業と同じ形(未pushの内訳を出し、可否を仰ぐ)` | 手順 |
| 391 | `未コミットの変更が無いこと・未pushの内訳をユーザーに提示する` | 締め作業の手順 |
| 450 | `⚠️ **pushは未push分を全部送る**ため、実行前に必ず次を確認する` | `docs/` 例外の注意 |
| 457 | `もう一方のセッションの未pushコミットも一緒に出ていく` | 同上 |
| 551 | `⭐ **未pushコミットの見た目をpush前に確認できる**のが最大の利点。` | プレビューの利点 |

- ⭐ **否定語側からも測った**: `本番にはまだ出ていない` / `本番にまだ出ていない` / `公開されていない` / `出ていません` は**0件**。
  `未公開` は3件（554 / 595 / 604行）残るが、**いずれも「未公開の変更・他タスク」という一般名詞**で、
  `未push` と同居していないため**正しく当たらない**

## 6. 推奨基準の指摘（⚠️ **合格を妨げない**・対応要否は設計担当の判断待ち）

### S-1（S3 fail-open 系）⚠️ **禁止句が行をまたぐと素通りする**（実測 G2）

- **該当**: `scripts/validate.mjs` の `PUBSTATE` 検査（判定が `lines.forEach` の**行単位**）
- **再現**: `CLAUDE.md` の末尾に `- ⚠️ **未pushなので` と `  本番にはまだ出ていない**` の**2行**を足す → **exit 0（緑）**
- ⚠️ ⭐ **「言い換えれば抜けられる」（設計書 §7）とは別の型**。言い換えていないのに抜ける
- ⭐ **無視できない理由**: `CLAUDE.md` は**強調を行またぎで書く癖が実在する**——
  `**` の個数が奇数の行が **90行**ある（例: 62-63行・101-103行・151-152行）
- ⭐ **軽い理由**: 過去3回の実例（`36394794` / `9f90a58a` / `c5d456ec`）は**3回とも1行に収まっていた**＝
  **再発の実績がある形は捕まる**
- **重大度**: **軽微**（検査の目的「無意識に同じ形を書くのを止める」は果たしている）
- **提案**: 設計書 §7 に**この型を穴として明記する**（実装変更は不要と考える）。塞ぐなら
  「連続行を結合してから判定する」だが、⚠️ **誤検出が増える**ので設計判断が要る

### S-2（S3）⚠️ **`END` をファイル末尾へ動かすと全行が除外され、10箇所が復活しても緑**（実測 G1）

- **該当**: 同検査の `inBlock()`（**ブロックの大きさに上限が無い**）
- **再現**: `PUBSTATE-NOTE:END` をファイル末尾の行へ移す → 禁止文を足しても **exit 0**
- ⭐ **新規の後退ではない**: 既存の `CRON-ADD` 検査も**同じ性質**（`text.indexOf` の1組で範囲を取るだけ）。
  ⭐ **PUBSTATE は CRON-ADD より強い**（行頭・空の検査を足している）ので、M1-b の欠落には当たらない
- **重大度**: **軽微**（END を1000行動かすのは設計書 §7 の「意図的な回避」に近い）
- **提案**: 塞ぐなら「ブロックは N 行以内」を足すのが最小。⚠️ **CRON-ADD 側と揃えるかは設計判断**

### S-3 ⚠️ **肯定形の現在形の主張が1件現存する**（裁定④でスコープ外＝派生起票案の材料）

- **該当**: `CLAUDE.md:1172` — `✅ **2026-08-06に実装・本番反映済み**(`7250f16c`)`
- ⭐ **実測した**: 肯定形（`本番公開済み` / `本番反映済み` / `公開済み` / `本番に出ている`）を全部grepし、
  **現在形の状態を主張しているのはこの1件だけ**（他の3件は「公開済みかどうかは状態にしない」等の**規約の説明**で該当しない）
- ⭐ **派生起票案に「対象は1件」と書ける**＝作業量が確定している
- **重大度**: **軽微**（設計書 §7 と追加指示の裁定④が明示的にスコープ外としている）

### S-4（指摘ではなく補完）

実装報告 §5 が「**残っている確認**」としていた **「`CLAUDE.md` が読めないときの exit 1」は、レビューで実施して exit 1 を確認した**（F3）。
⭐ **この項目に残りは無い**。

## 7. 設計担当への確認事項

⚠️ **合否を左右するものは無い**（＝`実装待ち` へは戻さない）。承認時に扱えばよいもののみ:

1. **S-1 / S-2 を設計書 §7 に穴として追記するか**（実装変更は不要と考える）
2. **S-3 の派生起票案の要否**（裁定④で「承認時に派生起票案へ落とす」とされているもの。⭐ **対象は 1172行の1件だけ**とレビューで確定した）
3. ⭐ **公開について**: 設計書 §8 は「是正が終わり次第すぐ単独で公開」だが、⚠️ **経路②は `git archive HEAD` から作る**ので
   **HEAD にある未公開の配信物が全部出る**。⚠️ **公開の可否を聞く前に
   `git diff --stat <本番位置>..HEAD -- . ':(exclude)docs/*'` を提示すること**（レビューは公開を実行していない）

## 8. 証跡の一覧（`tmp/review/未公開注記の陳腐化/`・gitignore）

| ファイル | 内容 |
|---|---|
| `npm-validate.txt` | ⭐ **本物のツリー**での `npm run validate`（exit 0・`all checks passed.`）＝M3 |
| `baseline.txt` | 隔離ツリーでの `validate`（exit 0）＝隔離環境が本物と同じ結果を出すことの裏取り |
| `out-D1〜D4.txt` / `out-E1〜E4.txt` | 破壊試験 V3・V4・V5（6通り）の全出力 |
| `out-F1〜F4.txt` | V7 の両向き・読み込み失敗・マーカー破壊時の除外 |
| `out-G1.txt` / `out-G2.txt` | S-2 / S-1 の再現 |
| `破壊試験まとめ.txt` | 上記14通りの exit と先頭メッセージの一覧 |
| `old.md` / `new.md` | `4091605d` / `195adc71` の `CLAUDE.md`（V8 の入力） |
| `render-旧.html` / `render-新.html` / `render.diff` | ⭐ `marked@15` による描画と**その差分（変更11箇所のみ）**＝V8・V9 |
